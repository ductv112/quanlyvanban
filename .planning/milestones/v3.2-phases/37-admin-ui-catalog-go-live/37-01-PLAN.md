---
phase: 37-admin-ui-catalog-go-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/backend/src/routes/admin-lgsp.ts                            # NEW
  - e_office_app_new/backend/src/repositories/lgsp-agency-config.repository.ts   # extend
  - e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts   # extend
  - e_office_app_new/backend/src/repositories/lgsp.repository.ts                 # extend (tracking retry)
  - e_office_app_new/backend/src/repositories/inter-organization.repository.ts   # extend (admin CRUD)
  - e_office_app_new/backend/src/server.ts                                       # mount admin-lgsp router
autonomous: true
requirements: [LGSP-UI-01, LGSP-UI-05]
tags: [backend, admin, lgsp, retry, crud, phase-37]

must_haves:
  truths:
    - "Admin gọi GET /api/admin/lgsp-agency-config trả về danh sách 12 row (6 DN × 2 env) KHÔNG kèm secret_key_encrypted (mask '***')"
    - "Admin gọi PUT /api/admin/lgsp-agency-config/:id update credential — secret_key được encrypt bằng SIGNING_SECRET_KEY trước khi lưu, cache LGSP service invalidate sau update"
    - "Admin gọi PATCH /api/admin/lgsp-agency-config/:id/active toggle is_active"
    - "Admin gọi POST /api/admin/lgsp-status-outbox/:id/retry reset event sent_status='pending' + retry_count=0 + next_retry_at=NOW + error_message=NULL (UNIQUE chỉ chấp nhận khi row hiện tại có sent_status='error')"
    - "Admin gọi POST /api/admin/lgsp-tracking/:id/retry reset tracking status='pending' + re-enqueue lgsp-send job với đầy đủ payload (recipient_id, outgoing_doc_id, tracking_id, sender_unit_id, environment)"
    - "Admin gọi CRUD inter_organizations (GET list / POST create / PUT /:id update / DELETE /:id) — code unique, không trùng"
    - "Non-admin user gọi bất kỳ /api/admin/lgsp-* endpoint trả 403 Forbidden"
    - "TypeScript backend strict tsc --noEmit exit 0 (KHÔNG có error mới)"
  artifacts:
    - path: "e_office_app_new/backend/src/routes/admin-lgsp.ts"
      provides: "Router admin-lgsp với 10 endpoints (config list/update/active, outbox retry, tracking retry, inter-org CRUD)"
      contains: "/admin/lgsp-agency-config"
    - path: "e_office_app_new/backend/src/repositories/lgsp-agency-config.repository.ts"
      provides: "Extend method listForAdmin (no secret) + update credential method"
      contains: "listForAdmin"
    - path: "e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts"
      provides: "Method resetForRetry(id) reset sent_status='pending' + retry_count=0 + next_retry_at=NOW + error_message=NULL"
      contains: "resetForRetry"
    - path: "e_office_app_new/backend/src/repositories/lgsp.repository.ts"
      provides: "Method getTrackingById + resetTrackingForRetry (admin retry send)"
      contains: "resetTrackingForRetry"
    - path: "e_office_app_new/backend/src/repositories/inter-organization.repository.ts"
      provides: "CRUD methods listForAdmin/createForAdmin/updateForAdmin/deleteForAdmin"
      contains: "listForAdmin"
    - path: "e_office_app_new/backend/src/server.ts"
      provides: "Mount admin-lgsp router dưới /api/admin/lgsp* với authenticate + requireRoles('Quản trị hệ thống')"
      contains: "adminLgspRoutes"
  key_links:
    - from: "e_office_app_new/backend/src/routes/admin-lgsp.ts"
      to: "lgspAgencyConfigRepository + lgspStatusOutboxRepository + lgspRepository + interOrganizationRepository"
      via: "repository function calls"
      pattern: "lgspAgencyConfigRepository\\.|lgspStatusOutboxRepository\\.|lgspRepository\\.|interOrganizationRepository\\."
    - from: "e_office_app_new/backend/src/routes/admin-lgsp.ts"
      to: "services/signing/crypto.ts encryptSecret + invalidateLgspServiceCache"
      via: "import lazy decrypt-encrypt for credential change"
      pattern: "encryptSecret|invalidateLgspServiceCache"
    - from: "e_office_app_new/backend/src/routes/admin-lgsp.ts"
      to: "enqueueLgspSendJob (lib/queue/lgsp-send-queue.ts)"
      via: "POST /admin/lgsp-tracking/:id/retry re-enqueue job"
      pattern: "enqueueLgspSendJob"
    - from: "e_office_app_new/backend/src/server.ts"
      to: "admin-lgsp.ts"
      via: "app.use('/api/admin', authenticate, requireRoles('Quản trị hệ thống'), adminLgspRoutes)"
      pattern: "/api/admin.*adminLgsp"
---

<objective>
Tạo backend admin namespace `/api/admin/lgsp-*` với 10 endpoints để Phase 37 frontend pages (Plan 37-03/04/05) gọi: cấu hình LGSP per-row, retry outbox event lỗi, retry tracking send job lỗi, CRUD catalog đơn vị ngoài. Repository extensions thêm method admin-only KHÔNG decrypt secret cho list, ưu tiên `setActive` đã có Phase 33 + thêm `resetForRetry` mới cho outbox/tracking.

Purpose: Backend foundation để Wave 2 frontend pages có endpoint sẵn để wire vào. Mọi endpoint protect bằng `requireRoles('Quản trị hệ thống')` (Phase 35-03 verified — KHÔNG dùng literal 'admin').

Output: Router file mới, 4 repository extension, server.ts mount mới. Wave 1 chạy song song với Plan 37-02 (chia file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-admin-ui-catalog-go-live/37-CONTEXT.md

# Existing code reference
@e_office_app_new/backend/src/repositories/lgsp-agency-config.repository.ts
@e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts
@e_office_app_new/backend/src/repositories/lgsp.repository.ts
@e_office_app_new/backend/src/repositories/inter-organization.repository.ts
@e_office_app_new/backend/src/services/signing/crypto.ts
@e_office_app_new/backend/src/services/lgsp.service.ts
@e_office_app_new/backend/src/routes/lgsp.ts
@e_office_app_new/backend/src/middleware/auth.ts
@e_office_app_new/backend/src/server.ts
@e_office_app_new/backend/src/lib/queue/lgsp-send-queue.ts

<interfaces>
Existing repository APIs:

```typescript
// lgsp-agency-config.repository.ts (Phase 33 — đã có)
lgspAgencyConfigRepository.list(): Promise<LgspAgencyConfigListRow[]>   // KHÔNG decrypt — return secret_key_masked='***'
lgspAgencyConfigRepository.getById(id): Promise<LgspAgencyConfigFullRow | null>  // CÓ secret_key_encrypted (Buffer)
lgspAgencyConfigRepository.upsert({ unitId, environment, systemId, secretKeyEncrypted, baseUrl, updatedBy }): Promise<DbResultWithId>
lgspAgencyConfigRepository.setActive(id, isActive, updatedBy): Promise<DbResult>

// lgsp-status-outbox.repository.ts (Phase 36 — đã có)
lgspStatusOutboxRepository.markError(id, errorMessage, nextRetryAt): Promise<DbResult>
// CẦN THÊM: resetForRetry(id) — set sent_status='pending', retry_count=0, next_retry_at=NOW, error_message=NULL

// lgsp.repository.ts (Phase 18 — đã có)
lgspRepository.updateTrackingStatus(id, status, lgspDocId, errorMessage): Promise<UpdateResultRow>
// CẦN THÊM: getTrackingById(id) — Row có outgoing_doc_id + recipient_id + sender_unit_id + environment để re-enqueue
// CẦN THÊM: resetTrackingForRetry(id) — SET status='pending', error_message=NULL

// inter-organization.repository.ts (Phase 35-01 — đã có)
interOrganizationRepository.autoRegisterFromLgsp(code, name): Promise<AutoRegisterResult>
interOrganizationRepository.findByCode(code): Promise<InterOrgRow | null>
// CẦN THÊM: listForAdmin({ search?, isActive?, page, pageSize }) — full row + total_count
// CẦN THÊM: createForAdmin({ code, name, lgspOrganId?, parentId?, isActive }) — return DbResultWithId
// CẦN THÊM: updateForAdmin(id, fields) — return DbResult
// CẦN THÊM: deleteForAdmin(id) — return DbResult (kiểm tra FK trước)

// services/signing/crypto.ts (đã có)
encryptSecret(plaintext: string): Promise<Buffer>
decryptSecret(cipher: Buffer): Promise<string>

// services/lgsp.service.ts (Phase 33 — đã có)
invalidateLgspServiceCache(unitId, environment?): void

// lib/queue/lgsp-send-queue.ts (Phase 34 — đã có)
enqueueLgspSendJob({ recipient_id, outgoing_doc_id, tracking_id, sender_unit_id, environment }): Promise<string>

// middleware/auth.ts (đã có)
authenticate(req, res, next)
requireRoles(...roles)  // Pass 'Quản trị hệ thống'

// server.ts mount pattern (đã có cho /api/lgsp):
// app.use('/api/lgsp', authenticate, lgspRoutes);
// CẦN THÊM: app.use('/api/admin', authenticate, requireRoles('Quản trị hệ thống'), adminLgspRoutes);
```

Existing inter_organizations schema (Phase 18):
```sql
CREATE TABLE edoc.inter_organizations (
  id BIGSERIAL PK,
  code VARCHAR(100) UNIQUE NOT NULL,
  name VARCHAR(500) NOT NULL,
  lgsp_organ_id VARCHAR(100),
  parent_id BIGINT REFERENCES edoc.inter_organizations(id),
  is_active BOOLEAN DEFAULT TRUE,
  address VARCHAR(500),     -- verify exist
  email VARCHAR(200),       -- verify exist
  phone VARCHAR(50),        -- verify exist
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend 4 repository với admin methods</name>
  <read_first>
    - e_office_app_new/backend/src/repositories/lgsp-agency-config.repository.ts (full file)
    - e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts (full file)
    - e_office_app_new/backend/src/repositories/lgsp.repository.ts (full file)
    - e_office_app_new/backend/src/repositories/inter-organization.repository.ts (full file)
    - e_office_app_new/backend/src/lib/db/query.ts (callFunction/callFunctionOne/rawQuery signature)
    - e_office_app_new/backend/src/services/signing/crypto.ts (encryptSecret/decryptSecret pattern)
  </read_first>
  <files>
    e_office_app_new/backend/src/repositories/lgsp-agency-config.repository.ts,
    e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts,
    e_office_app_new/backend/src/repositories/lgsp.repository.ts,
    e_office_app_new/backend/src/repositories/inter-organization.repository.ts
  </files>
  <action>
    **A) `lgsp-agency-config.repository.ts`** — KHÔNG thay đổi method hiện có. THÊM 1 method:

    ```typescript
    /**
     * Phase 37: GetByIdWithDecryptedSecret — admin "Test connection" cần plaintext secret.
     * Decrypt qua services/signing/crypto.decryptSecret. KHÔNG expose qua HTTP response — chỉ dùng nội bộ trong route handler.
     *
     * @returns null nếu không tìm thấy. Throws nếu decrypt fail (key sai).
     */
    async getByIdWithDecryptedSecret(id: number): Promise<{
      id: number;
      unit_id: number;
      environment: 'sandbox' | 'prod';
      system_id: string;
      base_url: string;
      is_active: boolean;
      secret_key_plaintext: string;
    } | null> {
      const row = await callFunctionOne<LgspAgencyConfigFullRow>(
        'edoc.fn_lgsp_agency_config_get_by_id',
        [id],
      );
      if (!row) return null;
      // Lazy import tránh circular dep
      const { decryptSecret } = await import('../services/signing/crypto.js');
      const plaintext = await decryptSecret(row.secret_key_encrypted);
      return {
        id: Number(row.id),
        unit_id: Number(row.unit_id),
        environment: row.environment,
        system_id: row.system_id,
        base_url: row.base_url,
        is_active: row.is_active,
        secret_key_plaintext: plaintext,
      };
    }
    ```

    **B) `lgsp-status-outbox.repository.ts`** — THÊM method:

    ```typescript
    /**
     * Phase 37: Admin "Gửi lại" outbox event lỗi. Reset về sent_status='pending' để worker tick 30s pick up lại.
     *
     * Guard: chỉ reset row có sent_status='error' (idempotent — gọi 2 lần liên tiếp lần 2 sẽ no-op).
     *
     * @returns DbResult { success, message } — false nếu id không tồn tại hoặc sent_status không phải 'error'.
     */
    async resetForRetry(id: number): Promise<DbResult> {
      const result = await rawQuery<{ id: string }>(
        `UPDATE edoc.lgsp_status_outbox
            SET sent_status = 'pending',
                retry_count = 0,
                next_retry_at = NOW(),
                error_message = NULL,
                sent_at = NULL
          WHERE id = $1 AND sent_status = 'error'
          RETURNING id`,
        [id],
      );
      if (result.length === 0) {
        return {
          success: false,
          message: 'Không tìm thấy outbox event đang lỗi với id này (có thể đã được reset hoặc đã thành công)',
        };
      }
      return {
        success: true,
        message: 'Đã reset outbox event, worker sẽ gửi lại trong vòng 30 giây',
      };
    }
    ```

    **C) `lgsp.repository.ts`** — THÊM 2 method + 1 interface mới:

    ```typescript
    /**
     * Phase 37: Row đầy đủ để admin "Gửi lại" send job — cần outgoing_doc_id để re-enqueue.
     * JOIN outgoing_doc_recipients để lấy recipient_id + sender_unit_id + environment.
     */
    export interface LgspTrackingFullRow {
      id: number;
      outgoing_doc_id: number;
      recipient_id: number;
      sender_unit_id: number;     // resolve từ outgoing_docs.unit_id
      environment: 'sandbox' | 'prod';  // resolve từ active lgsp_agency_config
      status: string;
      error_message: string | null;
    }

    // Thêm 2 method cuối repo lgspRepository:

    /**
     * Phase 37: Lấy tracking row + outgoing_doc_id + recipient_id + sender_unit_id + active env.
     * Trả null nếu không tìm thấy hoặc không có config LGSP active cho đơn vị gửi.
     */
    async getTrackingForRetry(trackingId: number): Promise<LgspTrackingFullRow | null> {
      const rows = await rawQuery<LgspTrackingFullRow>(
        `SELECT t.id::bigint                   AS id,
                t.outgoing_doc_id::bigint      AS outgoing_doc_id,
                r.id::bigint                   AS recipient_id,
                od.unit_id::bigint             AS sender_unit_id,
                ac.environment                 AS environment,
                t.status                       AS status,
                t.error_message                AS error_message
           FROM edoc.lgsp_tracking t
           JOIN edoc.outgoing_docs od ON od.id = t.outgoing_doc_id
      LEFT JOIN edoc.outgoing_doc_recipients r
                ON r.outgoing_doc_id = t.outgoing_doc_id
               AND r.recipient_type = 'external_org'
               AND r.recipient_org_code = t.dest_org_code
      LEFT JOIN edoc.lgsp_agency_config ac
                ON ac.unit_id = od.unit_id
               AND ac.is_active = TRUE
          WHERE t.id = $1
          LIMIT 1`,
        [trackingId],
      );
      if (rows.length === 0 || !rows[0].recipient_id || !rows[0].environment) return null;
      return {
        id: Number(rows[0].id),
        outgoing_doc_id: Number(rows[0].outgoing_doc_id),
        recipient_id: Number(rows[0].recipient_id),
        sender_unit_id: Number(rows[0].sender_unit_id),
        environment: rows[0].environment,
        status: rows[0].status,
        error_message: rows[0].error_message,
      };
    },

    /**
     * Phase 37: Reset tracking về 'pending' + clear error_message để worker xử lý lại.
     *
     * Guard: chỉ reset row có status='error'.
     */
    async resetTrackingForRetry(trackingId: number): Promise<UpdateResultRow> {
      const rows = await rawQuery<{ id: string }>(
        `UPDATE edoc.lgsp_tracking
            SET status = 'pending',
                error_message = NULL
          WHERE id = $1 AND status = 'error'
          RETURNING id`,
        [trackingId],
      );
      if (rows.length === 0) {
        return {
          success: false,
          message: 'Không tìm thấy tracking đang lỗi với id này',
        };
      }
      return { success: true, message: 'Đã reset tracking, worker sẽ gửi lại' };
    },
    ```

    **D) `inter-organization.repository.ts`** — THÊM 4 method + 2 interface:

    ```typescript
    export interface InterOrgFullRow {
      id: number;
      code: string;
      name: string;
      lgsp_organ_id: string | null;
      parent_id: number | null;
      is_active: boolean;
      address: string | null;
      email: string | null;
      phone: string | null;
      created_at: string;
      updated_at: string;
      total_count: number;
    }

    export interface InterOrgCreateFields {
      code: string;
      name: string;
      lgspOrganId?: string | null;
      parentId?: number | null;
      isActive?: boolean;
      address?: string | null;
      email?: string | null;
      phone?: string | null;
    }

    // Trong const interOrganizationRepository = { ... } thêm:

    async listForAdmin(filters: {
      search?: string | null;
      isActive?: boolean | null;
      page: number;
      pageSize: number;
    }): Promise<InterOrgFullRow[]> {
      const offset = (filters.page - 1) * filters.pageSize;
      return rawQuery<InterOrgFullRow>(
        `WITH filtered AS (
          SELECT *
            FROM edoc.inter_organizations
           WHERE ($1::TEXT IS NULL OR code ILIKE '%' || $1 || '%' OR name ILIKE '%' || $1 || '%')
             AND ($2::BOOLEAN IS NULL OR is_active = $2)
         )
         SELECT id::bigint              AS id,
                code, name, lgsp_organ_id, parent_id::bigint AS parent_id,
                is_active, address, email, phone,
                created_at, updated_at,
                (SELECT COUNT(*) FROM filtered)::bigint AS total_count
           FROM filtered
          ORDER BY code ASC
          LIMIT $3 OFFSET $4`,
        [filters.search ?? null, filters.isActive ?? null, filters.pageSize, offset],
      );
    },

    async createForAdmin(fields: InterOrgCreateFields): Promise<{ success: boolean; message: string; id: number }> {
      try {
        const rows = await rawQuery<{ id: string }>(
          `INSERT INTO edoc.inter_organizations
             (code, name, lgsp_organ_id, parent_id, is_active, address, email, phone, created_at, updated_at)
           VALUES ($1, $2, $3, $4, COALESCE($5, TRUE), $6, $7, $8, NOW(), NOW())
           RETURNING id`,
          [
            fields.code.slice(0, 100),
            fields.name.slice(0, 500),
            fields.lgspOrganId ?? null,
            fields.parentId ?? null,
            fields.isActive ?? true,
            fields.address ?? null,
            fields.email ?? null,
            fields.phone ?? null,
          ],
        );
        return { success: true, message: 'Đã thêm cơ quan ngoài', id: Number(rows[0].id) };
      } catch (err: unknown) {
        const sqlState = (err as { code?: string }).code;
        if (sqlState === '23505') return { success: false, message: 'Mã cơ quan đã tồn tại', id: 0 };
        throw err;
      }
    },

    async updateForAdmin(id: number, fields: Partial<InterOrgCreateFields>): Promise<{ success: boolean; message: string }> {
      const sets: string[] = [];
      const params: unknown[] = [];
      let i = 1;
      if (fields.code !== undefined)       { sets.push(`code = $${i++}`);          params.push(fields.code.slice(0, 100)); }
      if (fields.name !== undefined)       { sets.push(`name = $${i++}`);          params.push(fields.name.slice(0, 500)); }
      if (fields.lgspOrganId !== undefined){ sets.push(`lgsp_organ_id = $${i++}`); params.push(fields.lgspOrganId); }
      if (fields.parentId !== undefined)   { sets.push(`parent_id = $${i++}`);     params.push(fields.parentId); }
      if (fields.isActive !== undefined)   { sets.push(`is_active = $${i++}`);     params.push(fields.isActive); }
      if (fields.address !== undefined)    { sets.push(`address = $${i++}`);       params.push(fields.address); }
      if (fields.email !== undefined)      { sets.push(`email = $${i++}`);         params.push(fields.email); }
      if (fields.phone !== undefined)      { sets.push(`phone = $${i++}`);         params.push(fields.phone); }
      if (sets.length === 0) return { success: false, message: 'Không có trường nào để cập nhật' };
      sets.push(`updated_at = NOW()`);
      params.push(id);
      try {
        const rows = await rawQuery<{ id: string }>(
          `UPDATE edoc.inter_organizations SET ${sets.join(', ')} WHERE id = $${i} RETURNING id`,
          params,
        );
        if (rows.length === 0) return { success: false, message: 'Không tìm thấy cơ quan ngoài' };
        return { success: true, message: 'Đã cập nhật cơ quan ngoài' };
      } catch (err: unknown) {
        const sqlState = (err as { code?: string }).code;
        if (sqlState === '23505') return { success: false, message: 'Mã cơ quan đã tồn tại' };
        throw err;
      }
    },

    async deleteForAdmin(id: number): Promise<{ success: boolean; message: string }> {
      try {
        const rows = await rawQuery<{ id: string }>(
          'DELETE FROM edoc.inter_organizations WHERE id = $1 RETURNING id',
          [id],
        );
        if (rows.length === 0) return { success: false, message: 'Không tìm thấy cơ quan ngoài' };
        return { success: true, message: 'Đã xóa cơ quan ngoài' };
      } catch (err: unknown) {
        const sqlState = (err as { code?: string }).code;
        if (sqlState === '23503') return { success: false, message: 'Không thể xóa: cơ quan đang được tham chiếu (VB đến/đi)' };
        throw err;
      }
    },
    ```

    **LƯU Ý:**
    - Import `DbResult` từ doc-book.repository.ts đã có sẵn ở lgsp-status-outbox.repository.ts — reuse
    - Verify column `address/email/phone` tồn tại trong `edoc.inter_organizations`. Nếu không có → run `\d edoc.inter_organizations` qua docker first (Phase rule #5 CLAUDE.md), nếu thiếu thì ALTER TABLE ADD COLUMN IF NOT EXISTS trong schema/000_schema_v3.0.sql — APPEND ở cuối FK section. Wrap trong DO block catch 42701 duplicate_column.
  </action>
  <verify>
    <automated>cd e_office_app_new/backend && npx tsc --noEmit 2>&1 | grep -E "(repositories/lgsp-agency-config|repositories/lgsp-status-outbox|repositories/lgsp\.repository|repositories/inter-organization)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "getByIdWithDecryptedSecret" e_office_app_new/backend/src/repositories/lgsp-agency-config.repository.ts` → 0
    - `grep -q "resetForRetry" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` → 0
    - `grep -q "getTrackingForRetry\|resetTrackingForRetry" e_office_app_new/backend/src/repositories/lgsp.repository.ts` (cả 2) → 0 cả 2
    - `grep -q "listForAdmin\|createForAdmin\|updateForAdmin\|deleteForAdmin" e_office_app_new/backend/src/repositories/inter-organization.repository.ts` (cả 4) → 0 cả 4
    - `grep -q "InterOrgFullRow\|InterOrgCreateFields" e_office_app_new/backend/src/repositories/inter-organization.repository.ts` → 0 cả 2
    - `cd e_office_app_new/backend && npx tsc --noEmit` → exit 0 (zero NEW error trên 4 file repo)
    - Nếu phải ALTER TABLE address/email/phone → schema/000_schema_v3.0.sql apply 2 lần liên tiếp PASS không error
  </acceptance_criteria>
  <done>
    4 repository extend với admin methods + 2 interface mới (LgspTrackingFullRow, InterOrgFullRow, InterOrgCreateFields). TS strict backend PASS clean. Repository pattern const object honored. Decrypt secret chỉ chạy trong method getByIdWithDecryptedSecret (lazy import crypto). Idempotent: gọi resetForRetry trên row đã pending → returns success:false với message rõ ràng.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tạo NEW routes/admin-lgsp.ts với 7 endpoints (config + outbox + tracking + inter-org CRUD)</name>
  <read_first>
    - e_office_app_new/backend/src/routes/lgsp.ts (full — mirror pattern)
    - e_office_app_new/backend/src/routes/admin-catalog.ts (full — đọc pattern admin route handlers)
    - e_office_app_new/backend/src/lib/error-handler.ts (handleDbError signature)
    - File output Task 1 (4 repo đã extend)
  </read_first>
  <files>
    e_office_app_new/backend/src/routes/admin-lgsp.ts
  </files>
  <action>
    **TẠO MỚI** `e_office_app_new/backend/src/routes/admin-lgsp.ts` với toàn bộ 7 endpoints sau (test connection + overview defer Plan 37-02). Mount routes RELATIVE (no `/admin` prefix vì server.ts mount tại `/api/admin`):

    ```typescript
    /**
     * Admin LGSP routes — Phase 37 Plan 37-01.
     *
     * Mount tại /api/admin/* (server.ts wrap với requireRoles('Quản trị hệ thống')).
     * Tất cả endpoints REQUIRE admin role — non-admin user → 403 Forbidden.
     *
     * Endpoints:
     *   GET    /lgsp-agency-config              List 12 row (KHÔNG decrypt — secret mask '***')
     *   PUT    /lgsp-agency-config/:id          Update credential (encrypt + invalidate cache)
     *   PATCH  /lgsp-agency-config/:id/active   Toggle is_active
     *   POST   /lgsp-status-outbox/:id/retry    Reset event sent_status='pending'
     *   POST   /lgsp-tracking/:id/retry         Reset tracking + re-enqueue send job
     *   GET    /inter-organizations             List CRUD catalog
     *   POST   /inter-organizations             Create
     *   PUT    /inter-organizations/:id         Update
     *   DELETE /inter-organizations/:id         Delete
     *
     * (Test connection + overview endpoint → Plan 37-02 extend cùng file)
     */

    import { Router, type Request, type Response } from 'express';
    import type { AuthRequest } from '../middleware/auth.js';
    import { lgspAgencyConfigRepository } from '../repositories/lgsp-agency-config.repository.js';
    import { lgspStatusOutboxRepository } from '../repositories/lgsp-status-outbox.repository.js';
    import { lgspRepository } from '../repositories/lgsp.repository.js';
    import { interOrganizationRepository } from '../repositories/inter-organization.repository.js';
    import { encryptSecret } from '../services/signing/crypto.js';
    import { invalidateLgspServiceCache } from '../services/lgsp.service.js';
    import { enqueueLgspSendJob } from '../lib/queue/lgsp-send-queue.js';
    import { handleDbError } from '../lib/error-handler.js';

    const router = Router();

    // ============================================================
    // GET /lgsp-agency-config — List 12 row (KHÔNG decrypt secret)
    // ============================================================
    router.get('/lgsp-agency-config', async (_req: Request, res: Response) => {
      try {
        const rows = await lgspAgencyConfigRepository.list();
        // Mask secret_key — list method KHÔNG trả secret_key_encrypted, vẫn add mask cho UI rõ ràng
        const masked = rows.map((r) => ({ ...r, secret_key_masked: '***' }));
        res.json({ success: true, data: masked });
      } catch (error) {
        handleDbError(error, res);
      }
    });

    // ============================================================
    // PUT /lgsp-agency-config/:id — Update credential
    // Body: { systemId, secretKey, baseUrl, environment }
    //   secretKey OPTIONAL — nếu undefined hoặc rỗng → giữ nguyên ciphertext cũ
    // ============================================================
    router.put('/lgsp-agency-config/:id', async (req: Request, res: Response) => {
      try {
        const id = Number(req.params.id);
        const { staffId } = (req as AuthRequest).user;
        const { systemId, secretKey, baseUrl, environment } = req.body as {
          systemId?: string;
          secretKey?: string;
          baseUrl?: string;
          environment?: 'sandbox' | 'prod';
        };

        const existing = await lgspAgencyConfigRepository.getById(id);
        if (!existing) {
          res.status(404).json({ success: false, message: 'Không tìm thấy cấu hình LGSP' });
          return;
        }

        // Encrypt secret nếu user nhập mới, else giữ ciphertext cũ
        let secretKeyEncrypted: Buffer;
        if (typeof secretKey === 'string' && secretKey.trim() !== '') {
          secretKeyEncrypted = await encryptSecret(secretKey.trim());
        } else {
          secretKeyEncrypted = existing.secret_key_encrypted;
        }

        const result = await lgspAgencyConfigRepository.upsert({
          unitId: Number(existing.unit_id),
          environment: (environment ?? existing.environment) as 'sandbox' | 'prod',
          systemId: (systemId ?? existing.system_id).slice(0, 13),
          secretKeyEncrypted,
          baseUrl: (baseUrl ?? existing.base_url).slice(0, 500),
          updatedBy: staffId,
        });

        if (!result.success) {
          res.status(400).json({ success: false, message: result.message });
          return;
        }

        // Invalidate cache cho cả 2 env (defensive — admin có thể đổi env)
        invalidateLgspServiceCache(Number(existing.unit_id));

        res.json({ success: true, message: 'Đã cập nhật cấu hình LGSP', id: result.id });
      } catch (error) {
        handleDbError(error, res);
      }
    });

    // ============================================================
    // PATCH /lgsp-agency-config/:id/active — Toggle is_active
    // Body: { is_active: boolean }
    // ============================================================
    router.patch('/lgsp-agency-config/:id/active', async (req: Request, res: Response) => {
      try {
        const id = Number(req.params.id);
        const { staffId } = (req as AuthRequest).user;
        const isActive = req.body?.is_active === true;

        const existing = await lgspAgencyConfigRepository.getById(id);
        if (!existing) {
          res.status(404).json({ success: false, message: 'Không tìm thấy cấu hình LGSP' });
          return;
        }

        const result = await lgspAgencyConfigRepository.setActive(id, isActive, staffId);
        if (!result.success) {
          res.status(400).json({ success: false, message: result.message });
          return;
        }

        invalidateLgspServiceCache(Number(existing.unit_id), existing.environment);

        res.json({
          success: true,
          message: isActive ? 'Đã bật kết nối LGSP' : 'Đã tắt kết nối LGSP',
        });
      } catch (error) {
        handleDbError(error, res);
      }
    });

    // ============================================================
    // POST /lgsp-status-outbox/:id/retry — Reset outbox event lỗi
    // ============================================================
    router.post('/lgsp-status-outbox/:id/retry', async (req: Request, res: Response) => {
      try {
        const id = Number(req.params.id);
        const result = await lgspStatusOutboxRepository.resetForRetry(id);
        if (!result.success) {
          res.status(400).json({ success: false, message: result.message });
          return;
        }
        res.json({ success: true, message: result.message });
      } catch (error) {
        handleDbError(error, res);
      }
    });

    // ============================================================
    // POST /lgsp-tracking/:id/retry — Reset tracking + re-enqueue send job
    // ============================================================
    router.post('/lgsp-tracking/:id/retry', async (req: Request, res: Response) => {
      try {
        const trackingId = Number(req.params.id);
        const tracking = await lgspRepository.getTrackingForRetry(trackingId);
        if (!tracking) {
          res.status(404).json({
            success: false,
            message: 'Không tìm thấy tracking hoặc đơn vị gửi chưa có LGSP config active',
          });
          return;
        }

        const resetResult = await lgspRepository.resetTrackingForRetry(trackingId);
        if (!resetResult.success) {
          res.status(400).json({ success: false, message: resetResult.message });
          return;
        }

        // Re-enqueue lgsp-send job (mirror Phase 34-03 enqueue payload)
        await enqueueLgspSendJob({
          recipient_id: tracking.recipient_id,
          outgoing_doc_id: tracking.outgoing_doc_id,
          tracking_id: tracking.id,
          sender_unit_id: tracking.sender_unit_id,
          environment: tracking.environment,
        });

        res.json({
          success: true,
          message: 'Đã reset tracking, worker sẽ gửi lại trong vài giây',
        });
      } catch (error) {
        handleDbError(error, res);
      }
    });

    // ============================================================
    // GET /inter-organizations — List (search, isActive filter, pagination)
    // ============================================================
    router.get('/inter-organizations', async (req: Request, res: Response) => {
      try {
        const search = (req.query.search as string) || null;
        const isActiveQ = req.query.is_active;
        const isActive =
          isActiveQ === 'true' ? true : isActiveQ === 'false' ? false : null;
        const page = Number(req.query.page) || 1;
        const pageSize = Math.min(Number(req.query.pageSize) || 20, 100);

        const rows = await interOrganizationRepository.listForAdmin({
          search,
          isActive,
          page,
          pageSize,
        });
        const total = rows.length > 0 ? Number(rows[0].total_count) : 0;
        res.json({
          success: true,
          data: rows,
          pagination: { total, page, pageSize },
        });
      } catch (error) {
        handleDbError(error, res);
      }
    });

    // ============================================================
    // POST /inter-organizations — Create new
    // Body: { code, name, lgsp_organ_id?, parent_id?, is_active?, address?, email?, phone? }
    // ============================================================
    router.post('/inter-organizations', async (req: Request, res: Response) => {
      try {
        const b = req.body as Record<string, unknown>;
        const code = String(b.code ?? '').trim();
        const name = String(b.name ?? '').trim();
        if (!code) { res.status(400).json({ success: false, message: 'Mã cơ quan không được trống' }); return; }
        if (!name) { res.status(400).json({ success: false, message: 'Tên cơ quan không được trống' }); return; }

        const result = await interOrganizationRepository.createForAdmin({
          code,
          name,
          lgspOrganId: (b.lgsp_organ_id as string) ?? null,
          parentId: b.parent_id == null ? null : Number(b.parent_id),
          isActive: b.is_active === false ? false : true,
          address: (b.address as string) ?? null,
          email: (b.email as string) ?? null,
          phone: (b.phone as string) ?? null,
        });
        if (!result.success) { res.status(409).json({ success: false, message: result.message }); return; }
        res.status(201).json({ success: true, message: result.message, id: result.id });
      } catch (error) {
        handleDbError(error, res);
      }
    });

    // ============================================================
    // PUT /inter-organizations/:id — Update
    // ============================================================
    router.put('/inter-organizations/:id', async (req: Request, res: Response) => {
      try {
        const id = Number(req.params.id);
        const b = req.body as Record<string, unknown>;
        const result = await interOrganizationRepository.updateForAdmin(id, {
          code: b.code as string | undefined,
          name: b.name as string | undefined,
          lgspOrganId: b.lgsp_organ_id as string | null | undefined,
          parentId: b.parent_id == null ? null : Number(b.parent_id),
          isActive: typeof b.is_active === 'boolean' ? (b.is_active as boolean) : undefined,
          address: b.address as string | null | undefined,
          email: b.email as string | null | undefined,
          phone: b.phone as string | null | undefined,
        });
        if (!result.success) { res.status(400).json({ success: false, message: result.message }); return; }
        res.json({ success: true, message: result.message });
      } catch (error) {
        handleDbError(error, res);
      }
    });

    // ============================================================
    // DELETE /inter-organizations/:id — Delete
    // ============================================================
    router.delete('/inter-organizations/:id', async (req: Request, res: Response) => {
      try {
        const id = Number(req.params.id);
        const result = await interOrganizationRepository.deleteForAdmin(id);
        if (!result.success) { res.status(400).json({ success: false, message: result.message }); return; }
        res.json({ success: true, message: result.message });
      } catch (error) {
        handleDbError(error, res);
      }
    });

    export default router;
    ```

    **LƯU Ý:**
    - File phải import `type AuthRequest` (chỉ type) để không conflict với non-existence của requireRoles trong file này (mount qua server.ts)
    - Tất cả response message Vietnamese có dấu (CLAUDE.md mandate)
    - Endpoint mount RELATIVE — server.ts Task 3 sẽ prefix /api/admin
    - Plan 37-02 sẽ THÊM 2 endpoint nữa vào CÙNG file này: test connection + overview
  </action>
  <verify>
    <automated>cd e_office_app_new/backend && npx tsc --noEmit 2>&1 | grep -E "routes/admin-lgsp" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f e_office_app_new/backend/src/routes/admin-lgsp.ts`
    - `grep -c "router\.\(get\|post\|put\|patch\|delete\)" e_office_app_new/backend/src/routes/admin-lgsp.ts` → exactly 9 (1 GET config + 1 PUT config + 1 PATCH active + 1 POST outbox retry + 1 POST tracking retry + 1 GET inter-org + 1 POST inter-org + 1 PUT inter-org + 1 DELETE inter-org)
    - `grep -q "/lgsp-agency-config" e_office_app_new/backend/src/routes/admin-lgsp.ts` → 0
    - `grep -q "/lgsp-status-outbox/:id/retry" e_office_app_new/backend/src/routes/admin-lgsp.ts` → 0
    - `grep -q "/lgsp-tracking/:id/retry" e_office_app_new/backend/src/routes/admin-lgsp.ts` → 0
    - `grep -q "/inter-organizations" e_office_app_new/backend/src/routes/admin-lgsp.ts` → 0
    - `grep -q "enqueueLgspSendJob" e_office_app_new/backend/src/routes/admin-lgsp.ts` → 0
    - `grep -q "invalidateLgspServiceCache" e_office_app_new/backend/src/routes/admin-lgsp.ts` → 0
    - `grep -q "encryptSecret" e_office_app_new/backend/src/routes/admin-lgsp.ts` → 0
    - Vietnamese diacritics present: `grep -q "Đã cập nhật\|Đã bật\|Đã tắt\|Mã cơ quan\|Tên cơ quan" e_office_app_new/backend/src/routes/admin-lgsp.ts` → 0
    - `cd e_office_app_new/backend && npx tsc --noEmit` → exit 0
  </acceptance_criteria>
  <done>
    NEW routes/admin-lgsp.ts với 9 endpoints sẵn sàng. Endpoint paths khớp với CONTEXT D-05/D-06/D-09. Vietnamese error messages. Encrypt secret lazy. Cache invalidate sau update + active toggle. Re-enqueue send job với đầy đủ payload Phase 34 shape.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mount admin-lgsp router vào server.ts với role guard</name>
  <read_first>
    - e_office_app_new/backend/src/server.ts (full)
    - e_office_app_new/backend/src/middleware/auth.ts (authenticate + requireRoles)
  </read_first>
  <files>
    e_office_app_new/backend/src/server.ts
  </files>
  <action>
    THÊM vào server.ts:

    1. **Import** (sau import `lgspRoutes`):
    ```typescript
    import adminLgspRoutes from './routes/admin-lgsp.js';
    ```

    2. **Mount** trong block routes (đặt SAU `app.use('/api/lgsp', authenticate, lgspRoutes);` để không conflict prefix `/api/admin` với existing routes — verify rằng không có route `/api/admin/*` nào khác đang mount):

    ```typescript
    // Phase 37: Admin LGSP namespace — wrap với authenticate + requireRoles('Quản trị hệ thống')
    // 9 endpoints: config CRUD, outbox retry, tracking retry, inter-organizations CRUD
    app.use('/api/admin', authenticate, requireRoles('Quản trị hệ thống'), adminLgspRoutes);
    ```

    Note: `requireRoles` chưa được import sẵn trong server.ts hiện tại — kiểm tra import block. Nếu chưa có thì update import:
    ```typescript
    import { authenticate, requireRightByPathOrNext, requireRightOrNext, requireRoles } from './middleware/auth.js';
    ```

    Mount đặt **ngay sau dòng `app.use('/api/lgsp', authenticate, lgspRoutes);`** để giữ thứ tự rõ ràng.

    KHÔNG sửa logic khác trong server.ts.
  </action>
  <verify>
    <automated>cd e_office_app_new/backend && npx tsc --noEmit 2>&1 | grep -E "server\.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import adminLgspRoutes" e_office_app_new/backend/src/server.ts` → 0
    - `grep -q "/api/admin.*authenticate.*requireRoles.*adminLgspRoutes" e_office_app_new/backend/src/server.ts` (single line) → 0
    - `grep -q "requireRoles" e_office_app_new/backend/src/server.ts` (import line) → 0
    - `cd e_office_app_new/backend && npx tsc --noEmit` → exit 0
    - `cd e_office_app_new/backend && npm run build 2>&1 | tail -5` exit code 0 (production build PASS)
  </acceptance_criteria>
  <done>
    server.ts mount admin-lgsp với role guard 'Quản trị hệ thống'. TS strict + production build PASS. Non-admin user gọi /api/admin/lgsp-* sẽ nhận 403 Forbidden.
  </done>
</task>

</tasks>

<verification>
**TS strict 3 modules backend:**
```bash
cd e_office_app_new/backend && npx tsc --noEmit  # exit 0
cd e_office_app_new/backend && npm run build      # exit 0
```

**Smoke test endpoints (sau khi npm run dev backend):**
```bash
# 1. Non-admin → 403
curl -s -X GET http://localhost:4000/api/admin/lgsp-agency-config -H "Authorization: Bearer <non_admin_token>"
# Expected: {"success":false,"message":"Forbidden — insufficient permissions"}

# 2. Admin → 200 + 12 row
curl -s -X GET http://localhost:4000/api/admin/lgsp-agency-config -H "Authorization: Bearer <admin_token>"
# Expected: {"success":true,"data":[...12 rows...]}

# 3. Inter-organizations list
curl -s "http://localhost:4000/api/admin/inter-organizations?page=1&pageSize=20" -H "Authorization: Bearer <admin_token>"
```
</verification>

<success_criteria>
- 4 repository extend (lgsp-agency-config, lgsp-status-outbox, lgsp, inter-organization) — 7 method mới + 3 interface mới
- NEW routes/admin-lgsp.ts có 9 endpoints với Vietnamese error messages
- server.ts mount router với role guard 'Quản trị hệ thống'
- TS strict backend exit 0
- Production build backend PASS
- Non-admin user → 403; admin user → 200 + JSON data
- KHÔNG breaking change existing routes/lgsp.ts
</success_criteria>

<output>
After completion, create `.planning/phases/37-admin-ui-catalog-go-live/37-01-SUMMARY.md` per template.
</output>
