---
phase: 37-admin-ui-catalog-go-live
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/lgsp/co-quan/page.tsx   # REWRITE — extend từ Phase 18 stub thành full CRUD admin
autonomous: true
requirements: [LGSP-UI-05, LGSP-UI-06]
tags: [frontend, ui, admin, lgsp, inter-organizations, crud, sync, phase-37]

must_haves:
  truths:
    - "Admin truy cập /lgsp/co-quan thấy Table list cơ quan ngoài với columns: code, name, parent, is_active (tag), address/email/phone, created_at, Hành động (Sửa | Xóa)"
    - "Filter dropdown 'Trạng thái' với 3 lựa chọn: Tất cả / Đã xác nhận (is_active=true) / Tự đăng ký (is_active=false)"
    - "Search input theo code + name (gửi `search` query param)"
    - "Button 'Thêm mới' mở Drawer 720 → POST /api/admin/inter-organizations → toast 'Đã thêm cơ quan ngoài' → list refresh"
    - "Button 'Sửa' trên row mở Drawer 720 với form prefilled → PUT /api/admin/inter-organizations/:id → toast 'Đã cập nhật cơ quan ngoài' → refresh"
    - "Popconfirm 'Xóa' trên row → DELETE /api/admin/inter-organizations/:id → toast 'Đã xóa cơ quan ngoài' → refresh"
    - "Button 'Đồng bộ từ trục LGSP' (top right) → POST /api/admin/inter-organizations/sync → toast 'Đồng bộ thành công: thêm mới N, cập nhật M' → refresh"
    - "Pagination 20/page, showSizeChanger, showTotal"
    - "Frontend TS strict không có NEW error"
    - "Production build frontend PASS"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/lgsp/co-quan/page.tsx"
      provides: "Full CRUD admin cho inter_organizations + Filter 3-state + Sync button"
      contains: "Cơ quan liên thông"
      min_lines: 300
  key_links:
    - from: "frontend/src/app/(main)/lgsp/co-quan/page.tsx"
      to: "GET /api/admin/inter-organizations + POST/PUT/DELETE + POST /sync"
      via: "api.get/post/put/delete inside handlers"
      pattern: "/admin/inter-organizations"
    - from: "frontend/src/app/(main)/lgsp/co-quan/page.tsx"
      to: "Inter-org full row interface"
      via: "Type matches backend Plan 37-01 InterOrgFullRow"
      pattern: "interface InterOrg\\|code: string"
---

<objective>
REWRITE trang `/lgsp/co-quan` (hiện tại từ Phase 18 chỉ read-only Table + sync button đơn giản) thành full CRUD admin: Table list + Filter 3-state (Tất cả / Đã xác nhận / Tự đăng ký) + Drawer Add/Edit + Popconfirm Delete + Sync button. Filter "Tự đăng ký" hiển thị các row Phase 35-01 auto-INSERT với `is_active=false` (admin phải verify trước khi enable).

Purpose: LGSP-UI-05 + LGSP-UI-06. Admin có UI quản lý danh mục đơn vị ngoài thay vì SQL update direct.

Output: REWRITE existing page.tsx (~200 → ~350+ lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-admin-ui-catalog-go-live/37-CONTEXT.md
@.planning/phases/37-admin-ui-catalog-go-live/37-01-PLAN.md
@.planning/phases/37-admin-ui-catalog-go-live/37-02-PLAN.md

# Existing file to rewrite
@e_office_app_new/frontend/src/app/(main)/lgsp/co-quan/page.tsx

# Pattern references (existing admin CRUD pages)
@e_office_app_new/frontend/src/lib/api.ts

<interfaces>
Backend endpoints (Plan 37-01 + 37-02):

```typescript
// GET /api/admin/inter-organizations?search=&is_active=&page=&pageSize=
interface InterOrgFullRow {
  id: number;
  code: string;
  name: string;
  lgsp_organ_id: string | null;
  parent_id: number | null;
  is_active: boolean;
  address: string | null;
  email: string | null;
  phone: string | null;
  created_at: string;
  updated_at: string;
  total_count: number;
}
// Response: { success: true, data: [...rows], pagination: { total, page, pageSize } }

// POST /api/admin/inter-organizations
// Body: { code, name, lgsp_organ_id?, parent_id?, is_active?, address?, email?, phone? }
// Response: 201 { success: true, message, id } | 409 { success: false, message: 'Mã cơ quan đã tồn tại' }

// PUT /api/admin/inter-organizations/:id
// Body: same as POST (all fields optional, omit để giữ nguyên)

// DELETE /api/admin/inter-organizations/:id
// Response: { success: true, message } | 400 { success: false, message: 'Không thể xóa: ...' }

// POST /api/admin/inter-organizations/sync
// Response: { success: true, message: 'Đồng bộ thành công: thêm mới N, cập nhật M', data: { total, created, updated, failed } }
```

Project patterns (đọc admin pages khác):
- Drawer size={720}, validateTrigger='onSubmit'
- Vietnamese diacritics
- Popconfirm cho Delete (per project convention)
- maxLength match DB VARCHAR (code=100, name=500, lgsp_organ_id=100, address=500, email=200, phone=50)
- setBackendFieldError pattern map 'Mã cơ quan đã tồn tại' → field 'code' inline error
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: REWRITE page.tsx /lgsp/co-quan thành full CRUD admin</name>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/lgsp/co-quan/page.tsx (full — current Phase 18)
    - e_office_app_new/frontend/src/app/(main)/lgsp/cau-hinh/page.tsx (output Plan 37-03 — pattern Drawer + Modal cùng phase)
    - e_office_app_new/frontend/src/app/(main)/quan-tri/don-vi/page.tsx (nếu tồn tại — pattern admin CRUD project)
  </read_first>
  <files>
    e_office_app_new/frontend/src/app/(main)/lgsp/co-quan/page.tsx
  </files>
  <action>
    REWRITE TOÀN BỘ file (delete + recreate). Cấu trúc:

    ```tsx
    'use client';

    /**
     * Trang: /lgsp/co-quan — Cơ quan liên thông (Admin only)
     *
     * Phase 37 Plan 37-04. REQ: LGSP-UI-05, LGSP-UI-06.
     *
     * REWRITE từ Phase 18 read-only stub thành full CRUD admin:
     *   - List + filter is_active 3-state (Tất cả / Đã xác nhận / Tự đăng ký)
     *   - Search code + name
     *   - Drawer Add/Edit form
     *   - Popconfirm Delete
     *   - Button "Đồng bộ từ trục LGSP"
     *
     * Endpoints (Plan 37-01 + 37-02):
     *   GET    /api/admin/inter-organizations             → list (paginated)
     *   POST   /api/admin/inter-organizations             → create
     *   PUT    /api/admin/inter-organizations/:id         → update
     *   DELETE /api/admin/inter-organizations/:id         → delete
     *   POST   /api/admin/inter-organizations/sync        → batch sync từ LGSP
     */

    import React, { useState, useEffect, useCallback } from 'react';
    import {
      Card, Table, Button, Input, Tag, Space, Drawer, Form, Switch,
      Popconfirm, Select, App, Tooltip, Row, Col,
    } from 'antd';
    import type { ColumnsType, TablePaginationConfig } from 'antd/es/table';
    import {
      SyncOutlined, SearchOutlined, ReloadOutlined, PlusOutlined,
      EditOutlined, DeleteOutlined, BankOutlined, ApiOutlined,
    } from '@ant-design/icons';
    import { api } from '@/lib/api';
    import dayjs from 'dayjs';

    interface InterOrgRow {
      id: number;
      code: string;
      name: string;
      lgsp_organ_id: string | null;
      parent_id: number | null;
      is_active: boolean;
      address: string | null;
      email: string | null;
      phone: string | null;
      created_at: string;
      updated_at: string;
      total_count?: number;
    }

    interface FormValues {
      code: string;
      name: string;
      lgsp_organ_id?: string;
      parent_id?: number | null;
      is_active: boolean;
      address?: string;
      email?: string;
      phone?: string;
    }

    type IsActiveFilter = '' | 'true' | 'false';

    const IS_ACTIVE_OPTIONS = [
      { value: '',      label: 'Tất cả' },
      { value: 'true',  label: 'Đã xác nhận' },
      { value: 'false', label: 'Tự đăng ký' },
    ];

    export default function LgspCoQuanPage(): React.ReactElement {
      const { message: msg, modal } = App.useApp();

      const [data, setData] = useState<InterOrgRow[]>([]);
      const [loading, setLoading] = useState<boolean>(false);
      const [syncing, setSyncing] = useState<boolean>(false);
      const [search, setSearch] = useState<string>('');
      const [isActiveFilter, setIsActiveFilter] = useState<IsActiveFilter>('');
      const [pagination, setPagination] = useState({ current: 1, pageSize: 20, total: 0 });

      const [drawerOpen, setDrawerOpen] = useState<boolean>(false);
      const [editingRow, setEditingRow] = useState<InterOrgRow | null>(null);
      const [saving, setSaving] = useState<boolean>(false);
      const [form] = Form.useForm<FormValues>();

      // ── Fetch list ────────────────────────────────────
      const fetchData = useCallback(async (page = 1, pageSize = 20) => {
        setLoading(true);
        try {
          const { data: res } = await api.get('/admin/inter-organizations', {
            params: {
              search: search.trim() || undefined,
              is_active: isActiveFilter || undefined,
              page,
              pageSize,
            },
          });
          if (res?.success) {
            setData(res.data || []);
            setPagination({
              current: res.pagination?.page || page,
              pageSize: res.pagination?.pageSize || pageSize,
              total: res.pagination?.total || 0,
            });
          } else {
            msg.error(res?.message || 'Không tải được danh sách cơ quan');
          }
        } catch {
          msg.error('Không tải được danh sách cơ quan');
        } finally {
          setLoading(false);
        }
      }, [search, isActiveFilter, msg]);

      useEffect(() => { fetchData(1, pagination.pageSize); /* reset to page 1 khi filter đổi */
      // eslint-disable-next-line react-hooks/exhaustive-deps
      }, [search, isActiveFilter]);

      const handleTableChange = (pag: TablePaginationConfig) => {
        fetchData(pag.current || 1, pag.pageSize || 20);
      };

      // ── Sync ──────────────────────────────────────────
      const handleSync = async () => {
        setSyncing(true);
        try {
          const { data: res } = await api.post('/admin/inter-organizations/sync');
          if (res?.success) {
            msg.success(res.message || 'Đồng bộ thành công');
            fetchData(pagination.current, pagination.pageSize);
          } else {
            msg.error(res?.message || 'Đồng bộ thất bại');
          }
        } catch (err: unknown) {
          const errAny = err as { response?: { data?: { message?: string } } };
          msg.error(errAny.response?.data?.message || 'Đồng bộ thất bại');
        } finally {
          setSyncing(false);
        }
      };

      // ── Drawer open / close ──────────────────────────
      const handleOpenAdd = () => {
        setEditingRow(null);
        form.resetFields();
        form.setFieldsValue({ is_active: true });
        setDrawerOpen(true);
      };

      const handleOpenEdit = (row: InterOrgRow) => {
        setEditingRow(row);
        form.setFieldsValue({
          code: row.code,
          name: row.name,
          lgsp_organ_id: row.lgsp_organ_id ?? '',
          parent_id: row.parent_id,
          is_active: row.is_active,
          address: row.address ?? '',
          email: row.email ?? '',
          phone: row.phone ?? '',
        });
        setDrawerOpen(true);
      };

      const handleCloseDrawer = () => {
        setDrawerOpen(false);
        setEditingRow(null);
        form.resetFields();
      };

      // ── Backend error → inline field error ───────────
      const setBackendFieldError = (msgText: string): boolean => {
        if (msgText.includes('Mã cơ quan đã tồn tại')) {
          form.setFields([{ name: 'code', errors: [msgText] }]);
          return true;
        }
        return false;
      };

      // ── Save ─────────────────────────────────────────
      const handleSave = async () => {
        try {
          const values = await form.validateFields();
          setSaving(true);
          const payload = {
            code: values.code.trim(),
            name: values.name.trim(),
            lgsp_organ_id: values.lgsp_organ_id?.trim() || null,
            parent_id: values.parent_id || null,
            is_active: values.is_active !== false,
            address: values.address?.trim() || null,
            email: values.email?.trim() || null,
            phone: values.phone?.trim() || null,
          };

          let res;
          if (editingRow) {
            ({ data: res } = await api.put(`/admin/inter-organizations/${editingRow.id}`, payload));
          } else {
            ({ data: res } = await api.post('/admin/inter-organizations', payload));
          }
          if (res?.success) {
            msg.success(res.message || (editingRow ? 'Đã cập nhật cơ quan ngoài' : 'Đã thêm cơ quan ngoài'));
            handleCloseDrawer();
            fetchData(pagination.current, pagination.pageSize);
          } else {
            const m = res?.message || 'Lưu thất bại';
            if (!setBackendFieldError(m)) msg.error(m);
          }
        } catch (err: unknown) {
          const errAny = err as { response?: { data?: { message?: string } }, errorFields?: unknown };
          if (errAny.errorFields) return; // form validation, AntD đã hiển thị inline
          const m = errAny.response?.data?.message || 'Lưu thất bại';
          if (!setBackendFieldError(m)) msg.error(m);
        } finally {
          setSaving(false);
        }
      };

      // ── Delete ───────────────────────────────────────
      const handleDelete = async (row: InterOrgRow) => {
        try {
          const { data: res } = await api.delete(`/admin/inter-organizations/${row.id}`);
          if (res?.success) {
            msg.success(res.message || 'Đã xóa cơ quan ngoài');
            fetchData(pagination.current, pagination.pageSize);
          } else {
            msg.error(res?.message || 'Xóa thất bại');
          }
        } catch (err: unknown) {
          const errAny = err as { response?: { data?: { message?: string } } };
          msg.error(errAny.response?.data?.message || 'Xóa thất bại');
        }
      };

      // ── Columns ─────────────────────────────────────
      const columns: ColumnsType<InterOrgRow> = [
        {
          title: 'Mã cơ quan',
          dataIndex: 'code',
          width: 140,
          render: (val: string) => <span style={{ fontFamily: 'monospace', fontWeight: 600 }}>{val}</span>,
        },
        {
          title: 'Tên cơ quan',
          dataIndex: 'name',
          ellipsis: true,
          render: (val: string, row) => (
            <Space size={6}>
              <span>{val}</span>
              {row.lgsp_organ_id && <Tag color="blue" icon={<ApiOutlined />} style={{ fontSize: 10 }}>LGSP</Tag>}
            </Space>
          ),
        },
        {
          title: 'Trạng thái',
          dataIndex: 'is_active',
          width: 130,
          render: (val: boolean) =>
            val ? <Tag color="green">Đã xác nhận</Tag> : <Tag color="orange">Tự đăng ký</Tag>,
        },
        { title: 'Địa chỉ', dataIndex: 'address', ellipsis: true, render: (v: string | null) => v || '—' },
        { title: 'Email', dataIndex: 'email', width: 200, ellipsis: true, render: (v: string | null) => v || '—' },
        { title: 'Điện thoại', dataIndex: 'phone', width: 130, render: (v: string | null) => v || '—' },
        {
          title: 'Ngày tạo',
          dataIndex: 'created_at',
          width: 140,
          render: (val: string) => dayjs(val).format('DD/MM/YYYY HH:mm'),
        },
        {
          title: 'Hành động',
          key: 'actions',
          width: 180,
          render: (_, row) => (
            <Space size={4}>
              <Button size="small" icon={<EditOutlined />} onClick={() => handleOpenEdit(row)}>Sửa</Button>
              <Popconfirm
                title="Xóa cơ quan ngoài?"
                description="Hành động này không thể hoàn tác. Nếu cơ quan đang được tham chiếu bởi VB đến/đi, hệ thống sẽ chặn xóa."
                okText="Xóa"
                okType="danger"
                cancelText="Hủy"
                onConfirm={() => handleDelete(row)}
              >
                <Button size="small" danger icon={<DeleteOutlined />}>Xóa</Button>
              </Popconfirm>
            </Space>
          ),
        },
      ];

      return (
        <>
          <div className="page-header">
            <h2 className="page-title">
              <BankOutlined style={{ marginRight: 8, color: '#1B3A5C' }} />
              Cơ quan liên thông
            </h2>
            <Space>
              <Button icon={<PlusOutlined />} type="primary" onClick={handleOpenAdd}>Thêm mới</Button>
              <Button
                icon={<SyncOutlined spin={syncing} />}
                loading={syncing}
                onClick={handleSync}
              >
                Đồng bộ từ trục LGSP
              </Button>
            </Space>
          </div>

          <Card className="page-card">
            <div className="filter-row" style={{ marginBottom: 12 }}>
              <Space wrap size={12}>
                <Input
                  placeholder="Tìm theo mã / tên cơ quan..."
                  prefix={<SearchOutlined />}
                  value={search}
                  onChange={(e) => setSearch(e.target.value)}
                  onPressEnter={() => fetchData(1, pagination.pageSize)}
                  style={{ width: 320 }}
                  allowClear
                />
                <Select
                  style={{ minWidth: 180 }}
                  value={isActiveFilter}
                  onChange={(v) => setIsActiveFilter(v)}
                  options={IS_ACTIVE_OPTIONS}
                  placeholder="Trạng thái"
                />
                <Tooltip title="Làm mới">
                  <Button icon={<ReloadOutlined />} onClick={() => fetchData(pagination.current, pagination.pageSize)} />
                </Tooltip>
              </Space>
            </div>

            <Table<InterOrgRow>
              rowKey="id"
              columns={columns}
              dataSource={data}
              loading={loading}
              pagination={{
                current: pagination.current,
                pageSize: pagination.pageSize,
                total: pagination.total,
                showSizeChanger: true,
                pageSizeOptions: ['20', '50', '100'],
                showTotal: (total) => `Tổng ${total} cơ quan`,
              }}
              onChange={handleTableChange}
              scroll={{ x: 1200 }}
              size="small"
            />
          </Card>

          {/* ── Drawer Add/Edit ── */}
          <Drawer
            title={editingRow ? `Sửa cơ quan — ${editingRow.code}` : 'Thêm cơ quan ngoài'}
            size={720}
            open={drawerOpen}
            onClose={handleCloseDrawer}
            rootClassName="drawer-gradient"
            extra={
              <Space>
                <Button onClick={handleCloseDrawer}>Hủy</Button>
                <Button type="primary" loading={saving} onClick={handleSave}>Lưu</Button>
              </Space>
            }
          >
            <Form form={form} layout="vertical" validateTrigger="onSubmit" autoComplete="off">
              <Row gutter={16}>
                <Col span={12}>
                  <Form.Item
                    label="Mã cơ quan"
                    name="code"
                    rules={[{ required: true, message: 'Bắt buộc nhập mã cơ quan' }]}
                  >
                    <Input placeholder="VD: H37.BNV" maxLength={100} disabled={!!editingRow} />
                  </Form.Item>
                </Col>
                <Col span={12}>
                  <Form.Item label="Mã LGSP (lgsp_organ_id)" name="lgsp_organ_id">
                    <Input placeholder="Để trống nếu không có" maxLength={100} />
                  </Form.Item>
                </Col>
              </Row>

              <Form.Item
                label="Tên cơ quan"
                name="name"
                rules={[{ required: true, message: 'Bắt buộc nhập tên cơ quan' }]}
              >
                <Input placeholder="VD: Bộ Nội vụ" maxLength={500} />
              </Form.Item>

              <Form.Item label="Địa chỉ" name="address">
                <Input maxLength={500} />
              </Form.Item>

              <Row gutter={16}>
                <Col span={12}>
                  <Form.Item
                    label="Email"
                    name="email"
                    rules={[{ type: 'email', message: 'Email không hợp lệ' }]}
                  >
                    <Input maxLength={200} />
                  </Form.Item>
                </Col>
                <Col span={12}>
                  <Form.Item
                    label="Điện thoại"
                    name="phone"
                    rules={[{ pattern: /^[0-9+\-\s()]*$/, message: 'Số điện thoại không hợp lệ' }]}
                  >
                    <Input maxLength={50} />
                  </Form.Item>
                </Col>
              </Row>

              <Form.Item label="Trạng thái" name="is_active" valuePropName="checked" initialValue={true}>
                <Switch checkedChildren="Đã xác nhận" unCheckedChildren="Tự đăng ký" />
              </Form.Item>
            </Form>
          </Drawer>
        </>
      );
    }
    ```

    **LƯU Ý:**
    - `code` disabled khi edit (KHÔNG cho đổi mã — UNIQUE constraint + tham chiếu FK từ other tables)
    - maxLength match DB VARCHAR (verified: code=100, name=500, lgsp_organ_id=100, address=500, email=200, phone=50)
    - Filter Reset khi đổi isActiveFilter → reset về page 1
    - Vietnamese ALL UI text
    - Popconfirm cho Delete (per project convention)
    - setBackendFieldError map 'Mã cơ quan đã tồn tại' → inline field error
    - `parent_id` field skip ở v3.2 (chưa cần tree select cho v3.2, defer v3.3+) — payload `parent_id: null` luôn
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx tsc --noEmit 2>&1 | grep "lgsp/co-quan" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File length ≥ 300 lines: `wc -l e_office_app_new/frontend/src/app/\(main\)/lgsp/co-quan/page.tsx`
    - `grep -q "Cơ quan liên thông\|Đồng bộ từ trục LGSP\|Tự đăng ký\|Đã xác nhận" e_office_app_new/frontend/src/app/\(main\)/lgsp/co-quan/page.tsx` (4 substring) → 0 cả 4
    - `grep -q "admin/inter-organizations" e_office_app_new/frontend/src/app/\(main\)/lgsp/co-quan/page.tsx` → 0
    - `grep -q "/sync" e_office_app_new/frontend/src/app/\(main\)/lgsp/co-quan/page.tsx` → 0
    - `grep -q "Popconfirm" e_office_app_new/frontend/src/app/\(main\)/lgsp/co-quan/page.tsx` (per project convention delete) → 0
    - `grep -q "size={720}" e_office_app_new/frontend/src/app/\(main\)/lgsp/co-quan/page.tsx` (AntD 6) → 0
    - `grep -q "validateTrigger=\"onSubmit\"" e_office_app_new/frontend/src/app/\(main\)/lgsp/co-quan/page.tsx` → 0
    - `grep -q "setBackendFieldError" e_office_app_new/frontend/src/app/\(main\)/lgsp/co-quan/page.tsx` → 0
    - `cd e_office_app_new/frontend && npx tsc --noEmit 2>&1 | grep "lgsp/co-quan" | wc -l` → 0 (zero NEW errors)
  </acceptance_criteria>
  <done>
    Page /lgsp/co-quan full CRUD admin: list + filter 3-state + search + Drawer Add/Edit + Popconfirm Delete + Sync button. Vietnamese ALL UI. AntD 6. maxLength match DB. Frontend TS strict không có NEW error.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Production build verify</name>
  <read_first>
    - CLAUDE.md (deploy pitfall #2 — NODE_ENV unset cho npm run build frontend)
  </read_first>
  <files></files>
  <action>
    ```powershell
    cd e_office_app_new/frontend
    Remove-Item Env:NODE_ENV -ErrorAction SilentlyContinue
    npm run build 2>&1 | Tee-Object $env:TEMP\frontend-build-37-04.log | Select-Object -Last 30
    ```

    Verify route `/lgsp/co-quan` compile OK trong build output. Nếu fail → in 50 dòng cuối log + fix.
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd e_office_app_new/frontend && npm run build` exit 0
    - Build log có chứa `/lgsp/co-quan` route compiled
  </acceptance_criteria>
  <done>
    Production build PASS. Page /lgsp/co-quan compile OK.
  </done>
</task>

</tasks>

<verification>
**Frontend TS check:**
```bash
cd e_office_app_new/frontend && npx tsc --noEmit | grep -v "ho-so-cong-viec\|van-ban-den/page.tsx\|van-ban-di/page.tsx\|van-ban-du-thao"
# Expected: empty (4 pre-existing Phase 33-05 errors filtered)
```

**Manual visual (defer Plan 37-07 E2E):**
1. Admin login → /lgsp/co-quan
2. Verify Table load với existing rows
3. Filter dropdown 3 options → chọn "Tự đăng ký" → list filter is_active=false
4. Search "BNV" → list filter
5. Click "Thêm mới" → Drawer mở → nhập form → Lưu → toast 'Đã thêm cơ quan ngoài'
6. Click "Sửa" trên row → Drawer mở với prefill → đổi name → Lưu → toast 'Đã cập nhật'
7. Click "Xóa" trên row → Popconfirm → Xóa → toast 'Đã xóa cơ quan ngoài'
8. Click "Đồng bộ từ trục LGSP" → toast với count thêm/cập nhật (cần ≥1 sandbox DN active)
</verification>

<success_criteria>
- REWRITE page.tsx với full CRUD pattern + filter 3-state + sync button
- 5 luồng UI: list/filter, add, edit, delete, sync
- maxLength match DB
- Vietnamese diacritics
- Frontend TS strict không có NEW error
- Production build frontend PASS
</success_criteria>

<output>
After completion, create `.planning/phases/37-admin-ui-catalog-go-live/37-04-SUMMARY.md` per template.
</output>
