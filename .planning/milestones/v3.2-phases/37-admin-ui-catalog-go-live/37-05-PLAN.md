---
phase: 37-admin-ui-catalog-go-live
plan: 05
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx   # REWRITE — extend từ Phase 18 stub thành dashboard overview
autonomous: true
requirements: [LGSP-UI-04]
tags: [frontend, ui, admin, lgsp, overview, dashboard, phase-37]

must_haves:
  truths:
    - "User truy cập /lgsp thấy Dashboard layout: top header với button 'Đồng bộ ngay', 4 stat cards tổng quan (Gửi today / Nhận today / Outbox pending / Outbox error), grid 6 cards (1 per DN) với env active state + last_synced_at + last_sync_error, bottom section tracking history"
    - "Mỗi card DN hiển thị: tên DN, mã LGSP, 2 mini badge prod/sandbox (active/inactive), last_synced_at với tooltip last_sync_error, count gửi today + nhận today + outbox pending"
    - "Button 'Đồng bộ ngay' gọi POST /api/lgsp/sync-now (Phase 35-03 endpoint) → toast 'Đã xếp hàng đồng bộ' → polling 30s tự refresh overview"
    - "Endpoint GET /api/admin/lgsp-overview gọi on mount + sau khi sync → render mới"
    - "Frontend TS strict không có NEW error"
    - "Production build frontend PASS"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx"
      provides: "Overview dashboard 6 DN cards + 4 stat cards + Sync ngay button + giữ section tracking history Phase 18"
      contains: "Liên thông LGSP"
      min_lines: 280
  key_links:
    - from: "frontend/src/app/(main)/lgsp/page.tsx"
      to: "GET /api/admin/lgsp-overview"
      via: "api.get on mount + polling 30s"
      pattern: "/admin/lgsp-overview"
    - from: "frontend/src/app/(main)/lgsp/page.tsx"
      to: "POST /api/lgsp/sync-now (Phase 35-03)"
      via: "Button 'Đồng bộ ngay'"
      pattern: "/lgsp/sync-now"
---

<objective>
REWRITE trang `/lgsp` (hiện tại Phase 18 chỉ là tracking list table) thành Dashboard overview:
- Top header với title + button "Đồng bộ ngay" (Phase 35-03 sync-now endpoint)
- 4 stat cards tổng quan today (gửi / nhận / outbox pending / outbox error)
- Grid 6 cards (1 per DN root unit có lgsp_org_code) với env status + last_synced_at + counts
- Bottom section giữ tracking history Table (extract từ /lgsp/co-quan hoặc giữ inline) — đơn giản hơn KHÔNG cần Phase 19 dedicated tracking page

Purpose: LGSP-UI-04. Admin có dashboard overview nhanh trạng thái 6 DN, KHÔNG cần SQL query DB.

Output: REWRITE existing page.tsx (~207 → ~300+ lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-admin-ui-catalog-go-live/37-CONTEXT.md
@.planning/phases/37-admin-ui-catalog-go-live/37-02-PLAN.md

# Existing file to rewrite
@e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx

<interfaces>
Backend endpoint (Plan 37-02):

```typescript
// GET /api/admin/lgsp-overview
// Response:
// {
//   success: true,
//   data: {
//     units: [
//       {
//         unit_id: number,
//         unit_name: string,
//         lgsp_org_code: string | null,
//         prod_config_id: number | null,
//         prod_is_active: boolean | null,
//         prod_last_synced_at: string | null,
//         prod_last_sync_error: string | null,
//         sandbox_config_id: number | null,
//         sandbox_is_active: boolean | null,
//         sandbox_last_synced_at: string | null,
//         sandbox_last_sync_error: string | null,
//         send_today_total: number,
//         send_today_success: number,
//         send_today_error: number,
//         send_today_pending: number,
//         receive_today_total: number,
//         outbox_today_pending: number,
//         outbox_today_error: number,
//       }, ... (6 row)
//     ],
//     totals: {
//       send_today: number, send_success: number, send_error: number,
//       receive_today: number,
//       outbox_pending: number, outbox_error: number,
//       active_count: number,
//     }
//   }
// }

// POST /api/lgsp/sync-now (Phase 35-03 — đã có sẵn)
// Response: 202 { success: true, message: 'Đã xếp hàng đồng bộ LGSP - worker sẽ chạy trong giây lát', job_id: '...' }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: REWRITE page.tsx /lgsp thành Overview Dashboard</name>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx (full — current Phase 18)
    - e_office_app_new/frontend/src/app/(main)/lgsp/cau-hinh/page.tsx (Plan 37-03 pattern stat cards)
  </read_first>
  <files>
    e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx
  </files>
  <action>
    REWRITE TOÀN BỘ:

    ```tsx
    'use client';

    /**
     * Trang: /lgsp — Liên thông LGSP Dashboard
     *
     * Phase 37 Plan 37-05. REQ: LGSP-UI-04.
     *
     * REWRITE từ Phase 18 tracking list stub thành Overview Dashboard:
     *   - 4 stat cards tổng quan today (gửi / nhận / outbox pending / outbox error)
     *   - 6 DN cards với env status + last_synced_at + count today
     *   - Button "Đồng bộ ngay" gọi /api/lgsp/sync-now
     *   - Tracking history section (giữ Table list từ Phase 18)
     *
     * Endpoint chính:
     *   GET  /api/admin/lgsp-overview  → dashboard data
     *   POST /api/lgsp/sync-now        → trigger cron immediate (Phase 35-03)
     *   GET  /api/lgsp/tracking        → recent tracking (Phase 18 reuse)
     */

    import React, { useState, useEffect, useCallback } from 'react';
    import {
      Card, Row, Col, Button, Space, Tag, Tooltip, Skeleton, Alert, Empty,
      Table, App, Statistic,
    } from 'antd';
    import type { ColumnsType } from 'antd/es/table';
    import {
      SyncOutlined, ApiOutlined, SendOutlined, InboxOutlined,
      ClockCircleOutlined, ExclamationCircleOutlined, CheckCircleOutlined,
      CloseCircleOutlined, ReloadOutlined, ThunderboltOutlined, BankOutlined,
    } from '@ant-design/icons';
    import { api } from '@/lib/api';
    import dayjs from 'dayjs';

    interface OverviewUnit {
      unit_id: number;
      unit_name: string;
      lgsp_org_code: string | null;
      prod_config_id: number | null;
      prod_is_active: boolean | null;
      prod_last_synced_at: string | null;
      prod_last_sync_error: string | null;
      sandbox_config_id: number | null;
      sandbox_is_active: boolean | null;
      sandbox_last_synced_at: string | null;
      sandbox_last_sync_error: string | null;
      send_today_total: number;
      send_today_success: number;
      send_today_error: number;
      send_today_pending: number;
      receive_today_total: number;
      outbox_today_pending: number;
      outbox_today_error: number;
    }

    interface OverviewTotals {
      send_today: number;
      send_success: number;
      send_error: number;
      receive_today: number;
      outbox_pending: number;
      outbox_error: number;
      active_count: number;
    }

    interface TrackingRow {
      id: number;
      outgoing_doc_id: number | null;
      direction: string;
      lgsp_doc_id: string | null;
      dest_org_code: string | null;
      dest_org_name: string | null;
      status: string;
      error_message: string | null;
      sent_at: string | null;
      received_at: string | null;
      created_at: string;
    }

    const POLL_INTERVAL_MS = 30_000;

    const STATUS_COLOR: Record<string, string> = {
      pending: 'orange', processing: 'blue', success: 'green', error: 'red',
    };
    const STATUS_LABEL: Record<string, string> = {
      pending: 'Chờ xử lý', processing: 'Đang xử lý', success: 'Thành công', error: 'Lỗi',
    };

    export default function LgspOverviewPage(): React.ReactElement {
      const { message: msg } = App.useApp();
      const [units, setUnits] = useState<OverviewUnit[]>([]);
      const [totals, setTotals] = useState<OverviewTotals | null>(null);
      const [loading, setLoading] = useState<boolean>(true);
      const [syncing, setSyncing] = useState<boolean>(false);
      const [tracking, setTracking] = useState<TrackingRow[]>([]);
      const [trackingLoading, setTrackingLoading] = useState<boolean>(true);

      // ── Fetch overview ─────────────────────────────────
      const fetchOverview = useCallback(async () => {
        try {
          const { data: res } = await api.get('/admin/lgsp-overview');
          if (res?.success && res.data) {
            setUnits(res.data.units || []);
            setTotals(res.data.totals || null);
          } else {
            msg.error(res?.message || 'Không tải được tổng quan LGSP');
          }
        } catch {
          // Silent — keep previous data
        } finally {
          setLoading(false);
        }
      }, [msg]);

      // ── Fetch tracking (Phase 18 reuse) ────────────────
      const fetchTracking = useCallback(async () => {
        try {
          const { data: res } = await api.get('/lgsp/tracking', {
            params: { page: 1, pageSize: 10 },
          });
          if (res?.success) setTracking(res.data || []);
        } catch {
          // Silent
        } finally {
          setTrackingLoading(false);
        }
      }, []);

      useEffect(() => {
        fetchOverview();
        fetchTracking();
      }, [fetchOverview, fetchTracking]);

      // ── Polling 30s ────────────────────────────────────
      useEffect(() => {
        const id = setInterval(() => {
          fetchOverview();
          fetchTracking();
        }, POLL_INTERVAL_MS);
        return () => clearInterval(id);
      }, [fetchOverview, fetchTracking]);

      // ── Sync now ───────────────────────────────────────
      const handleSyncNow = async () => {
        setSyncing(true);
        try {
          const { data: res } = await api.post('/lgsp/sync-now');
          if (res?.success) {
            msg.success(res.message || 'Đã xếp hàng đồng bộ LGSP — worker sẽ chạy trong giây lát');
            // Refresh sau 8s để cron pick up xong
            setTimeout(() => { fetchOverview(); fetchTracking(); }, 8000);
          } else {
            msg.error(res?.message || 'Không thể trigger đồng bộ');
          }
        } catch (err: unknown) {
          const errAny = err as { response?: { data?: { message?: string } } };
          msg.error(errAny.response?.data?.message || 'Không thể trigger đồng bộ');
        } finally {
          setSyncing(false);
        }
      };

      // ── Tracking columns ───────────────────────────────
      const trackingColumns: ColumnsType<TrackingRow> = [
        {
          title: 'Hướng', dataIndex: 'direction', width: 100,
          render: (val: string) =>
            val === 'send' ? <Tag color="blue" icon={<SendOutlined />}>Gửi đi</Tag>
                            : <Tag color="green" icon={<InboxOutlined />}>Nhận về</Tag>,
        },
        { title: 'Cơ quan', dataIndex: 'dest_org_name', ellipsis: true, render: (v, r) => v || r.dest_org_code || '—' },
        { title: 'Mã LGSP', dataIndex: 'lgsp_doc_id', width: 200, render: (v: string | null) => v || '—' },
        {
          title: 'Trạng thái', dataIndex: 'status', width: 120,
          render: (val: string, row) => (
            <Tooltip title={row.error_message || undefined}>
              <Tag color={STATUS_COLOR[val] || 'default'}>{STATUS_LABEL[val] || val}</Tag>
            </Tooltip>
          ),
        },
        {
          title: 'Thời gian', dataIndex: 'created_at', width: 150,
          render: (v: string) => dayjs(v).format('DD/MM HH:mm'),
        },
      ];

      return (
        <>
          <div className="page-header">
            <h2 className="page-title">
              <ApiOutlined style={{ marginRight: 8, color: '#1B3A5C' }} />
              Liên thông LGSP
            </h2>
            <Space>
              <Button icon={<ReloadOutlined />} onClick={() => { fetchOverview(); fetchTracking(); }}>Làm mới</Button>
              <Button
                type="primary"
                icon={<ThunderboltOutlined />}
                loading={syncing}
                onClick={handleSyncNow}
              >
                Đồng bộ ngay
              </Button>
            </Space>
          </div>

          {totals && totals.active_count === 0 && (
            <Alert
              type="warning"
              showIcon
              style={{ marginBottom: 16 }}
              message="Chưa có đơn vị LGSP nào đang bật kết nối"
              description={<>Truy cập <a href="/lgsp/cau-hinh">Cấu hình kết nối</a> → nhập credential thật → bật <b>Trạng thái</b> để cron tự đồng bộ.</>}
            />
          )}

          {/* ── 4 Stat cards ── */}
          <Row gutter={16} style={{ marginBottom: 16 }}>
            <Col xs={12} sm={6}>
              <Card size="small">
                <Statistic
                  title="VB gửi hôm nay"
                  value={totals?.send_today ?? 0}
                  prefix={<SendOutlined style={{ color: '#1890ff' }} />}
                  suffix={totals && totals.send_error > 0 ? <Tag color="red" style={{ marginLeft: 8 }}>{totals.send_error} lỗi</Tag> : null}
                />
              </Card>
            </Col>
            <Col xs={12} sm={6}>
              <Card size="small">
                <Statistic
                  title="VB nhận hôm nay"
                  value={totals?.receive_today ?? 0}
                  prefix={<InboxOutlined style={{ color: '#52c41a' }} />}
                />
              </Card>
            </Col>
            <Col xs={12} sm={6}>
              <Card size="small">
                <Statistic
                  title="Callback chờ xử lý"
                  value={totals?.outbox_pending ?? 0}
                  prefix={<ClockCircleOutlined style={{ color: '#D97706' }} />}
                />
              </Card>
            </Col>
            <Col xs={12} sm={6}>
              <Card size="small">
                <Statistic
                  title="Callback lỗi"
                  value={totals?.outbox_error ?? 0}
                  prefix={<ExclamationCircleOutlined style={{ color: '#DC2626' }} />}
                  valueStyle={{ color: totals && totals.outbox_error > 0 ? '#DC2626' : undefined }}
                />
              </Card>
            </Col>
          </Row>

          {/* ── 6 DN cards ── */}
          <h3 style={{ color: '#1B3A5C', marginBottom: 12 }}>Trạng thái 6 doanh nghiệp</h3>
          {loading ? (
            <Row gutter={[16, 16]} style={{ marginBottom: 16 }}>
              {[1, 2, 3, 4, 5, 6].map((i) => (
                <Col xs={24} sm={12} lg={8} key={i}>
                  <Card size="small"><Skeleton active paragraph={{ rows: 3 }} /></Card>
                </Col>
              ))}
            </Row>
          ) : units.length === 0 ? (
            <Card style={{ marginBottom: 16 }}>
              <Empty description="Chưa có DN nào được cấu hình LGSP (cần root unit có lgsp_org_code)" />
            </Card>
          ) : (
            <Row gutter={[16, 16]} style={{ marginBottom: 24 }}>
              {units.map((u) => (
                <Col xs={24} sm={12} lg={8} key={u.unit_id}>
                  <Card
                    size="small"
                    title={
                      <Space>
                        <BankOutlined style={{ color: '#1B3A5C' }} />
                        <span style={{ fontWeight: 600 }}>{u.unit_name}</span>
                      </Space>
                    }
                    extra={u.lgsp_org_code && <Tag color="blue">{u.lgsp_org_code}</Tag>}
                  >
                    <Space direction="vertical" size={8} style={{ width: '100%' }}>
                      {/* Env badges */}
                      <Space size={6}>
                        <Tooltip title={u.sandbox_last_sync_error || (u.sandbox_is_active ? 'Sandbox đang bật' : 'Sandbox tắt')}>
                          <Tag color={u.sandbox_is_active ? 'orange' : 'default'} icon={u.sandbox_is_active ? <CheckCircleOutlined /> : <CloseCircleOutlined />}>
                            Sandbox
                          </Tag>
                        </Tooltip>
                        <Tooltip title={u.prod_last_sync_error || (u.prod_is_active ? 'Production đang bật' : 'Production tắt')}>
                          <Tag color={u.prod_is_active ? 'red' : 'default'} icon={u.prod_is_active ? <CheckCircleOutlined /> : <CloseCircleOutlined />}>
                            Production
                          </Tag>
                        </Tooltip>
                      </Space>

                      {/* Last synced */}
                      <div style={{ fontSize: 12, color: '#8c8c8c' }}>
                        Đồng bộ cuối:{' '}
                        {(() => {
                          const lastSynced = u.prod_last_synced_at || u.sandbox_last_synced_at;
                          return lastSynced ? dayjs(lastSynced).fromNow() : <span style={{ color: '#bfbfbf' }}>Chưa đồng bộ</span>;
                        })()}
                      </div>

                      {/* Counts */}
                      <Row gutter={8}>
                        <Col span={8}>
                          <div style={{ textAlign: 'center', padding: 4, background: '#f0f9ff', borderRadius: 6 }}>
                            <div style={{ fontSize: 18, fontWeight: 700, color: '#1890ff' }}>{u.send_today_total}</div>
                            <div style={{ fontSize: 11, color: '#8c8c8c' }}>Gửi today</div>
                          </div>
                        </Col>
                        <Col span={8}>
                          <div style={{ textAlign: 'center', padding: 4, background: '#f6ffed', borderRadius: 6 }}>
                            <div style={{ fontSize: 18, fontWeight: 700, color: '#52c41a' }}>{u.receive_today_total}</div>
                            <div style={{ fontSize: 11, color: '#8c8c8c' }}>Nhận today</div>
                          </div>
                        </Col>
                        <Col span={8}>
                          <div style={{
                            textAlign: 'center', padding: 4,
                            background: u.outbox_today_pending > 0 ? '#fff7e6' : '#fafafa',
                            borderRadius: 6,
                          }}>
                            <div style={{ fontSize: 18, fontWeight: 700, color: u.outbox_today_pending > 0 ? '#D97706' : '#bfbfbf' }}>
                              {u.outbox_today_pending}
                            </div>
                            <div style={{ fontSize: 11, color: '#8c8c8c' }}>Outbox</div>
                          </div>
                        </Col>
                      </Row>

                      {u.send_today_error > 0 && (
                        <Alert
                          type="error"
                          showIcon
                          message={`${u.send_today_error} VB gửi lỗi today`}
                          style={{ padding: '4px 12px' }}
                        />
                      )}
                    </Space>
                  </Card>
                </Col>
              ))}
            </Row>
          )}

          {/* ── Tracking history ── */}
          <Card title="Lịch sử liên thông gần đây (10 bản ghi mới nhất)" className="page-card">
            {trackingLoading ? (
              <Skeleton active paragraph={{ rows: 4 }} />
            ) : tracking.length === 0 ? (
              <Empty description="Chưa có liên thông LGSP nào" />
            ) : (
              <Table<TrackingRow>
                rowKey="id"
                columns={trackingColumns}
                dataSource={tracking}
                pagination={false}
                size="small"
                scroll={{ x: 800 }}
              />
            )}
          </Card>
        </>
      );
    }
    ```

    **LƯU Ý:**
    - Polling 30s — overview + tracking đồng thời (lightweight endpoint OK)
    - Click "Đồng bộ ngay" → sync-now Phase 35-03 → setTimeout 8s refresh để cron pick up
    - `dayjs(...).fromNow()` cần `dayjs.extend(relativeTime)` — đảm bảo dùng `dayjs.extend(relativeTime)` ở đầu file HOẶC chuyển sang `dayjs(...).format('DD/MM HH:mm')`. Để đơn giản: dùng `format('DD/MM HH:mm')` thay vì fromNow để KHÔNG cần thêm dayjs plugin.

    **SỬA LẠI** đoạn fromNow:
    ```tsx
    {(() => {
      const lastSynced = u.prod_last_synced_at || u.sandbox_last_synced_at;
      return lastSynced ? dayjs(lastSynced).format('DD/MM HH:mm') : <span style={{ color: '#bfbfbf' }}>Chưa đồng bộ</span>;
    })()}
    ```
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx tsc --noEmit 2>&1 | grep "lgsp/page.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File length ≥ 280 lines: `wc -l e_office_app_new/frontend/src/app/\(main\)/lgsp/page.tsx`
    - `grep -q "Liên thông LGSP\|Đồng bộ ngay\|Trạng thái 6 doanh nghiệp" e_office_app_new/frontend/src/app/\(main\)/lgsp/page.tsx` (Vietnamese 3 substring) → 0 cả 3
    - `grep -q "/admin/lgsp-overview" e_office_app_new/frontend/src/app/\(main\)/lgsp/page.tsx` → 0
    - `grep -q "/lgsp/sync-now" e_office_app_new/frontend/src/app/\(main\)/lgsp/page.tsx` → 0
    - `grep -q "/lgsp/tracking" e_office_app_new/frontend/src/app/\(main\)/lgsp/page.tsx` (Phase 18 reuse) → 0
    - `grep -q "POLL_INTERVAL_MS\|setInterval" e_office_app_new/frontend/src/app/\(main\)/lgsp/page.tsx` → 0
    - `grep -q "VB gửi hôm nay\|VB nhận hôm nay\|Callback chờ xử lý\|Callback lỗi" e_office_app_new/frontend/src/app/\(main\)/lgsp/page.tsx` (Vietnamese stat labels) → 0 cả 4
    - KHÔNG dùng `dayjs.extend\|fromNow`: `! grep -q "fromNow" e_office_app_new/frontend/src/app/\(main\)/lgsp/page.tsx` (đảm bảo dùng format thay vì fromNow để tránh plugin import)
    - `cd e_office_app_new/frontend && npx tsc --noEmit 2>&1 | grep "lgsp/page.tsx" | wc -l` → 0 NEW errors
  </acceptance_criteria>
  <done>
    Page /lgsp Dashboard overview: 4 stat cards + 6 DN cards + Đồng bộ ngay button + tracking history table. Polling 30s. Vietnamese ALL. Frontend TS strict không có NEW error.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Production build verify</name>
  <read_first>
    - CLAUDE.md (deploy pitfall #2)
  </read_first>
  <files></files>
  <action>
    ```powershell
    cd e_office_app_new/frontend
    Remove-Item Env:NODE_ENV -ErrorAction SilentlyContinue
    npm run build 2>&1 | Tee-Object $env:TEMP\frontend-build-37-05.log | Select-Object -Last 30
    ```
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Exit 0
    - Build log có `/lgsp` route compiled
  </acceptance_criteria>
  <done>
    Production build frontend PASS.
  </done>
</task>

</tasks>

<verification>
**Frontend TS check:**
```bash
cd e_office_app_new/frontend && npx tsc --noEmit | grep -v "ho-so-cong-viec\|van-ban-den/page.tsx\|van-ban-di/page.tsx\|van-ban-du-thao"
# Expected: empty
```

**Manual visual (defer Plan 37-07 E2E):**
1. Admin login → /lgsp
2. Verify 4 stat cards load (counts từ DB)
3. Verify 6 DN cards với env state + count today
4. Click "Đồng bộ ngay" → toast 'Đã xếp hàng đồng bộ' → đợi 8s → refresh overview
5. Polling 30s tự refresh background (verify Network tab)
</verification>

<success_criteria>
- REWRITE page.tsx Dashboard layout
- 4 stat cards + 6 DN cards + Sync now button + tracking history
- Polling 30s
- Vietnamese ALL UI
- Frontend TS strict không có NEW error
- Production build PASS
</success_criteria>

<output>
After completion, create `.planning/phases/37-admin-ui-catalog-go-live/37-05-SUMMARY.md` per template.
</output>
