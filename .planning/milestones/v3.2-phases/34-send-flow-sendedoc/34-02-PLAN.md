---
phase: 34-send-flow-sendedoc
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/workers/src/queues/lgsp-send-queue.ts
  - e_office_app_new/workers/src/jobs/lgsp-send-worker.ts
  - e_office_app_new/workers/src/index.ts
  - e_office_app_new/workers/package.json
  - e_office_app_new/backend/src/lib/queue/lgsp-send-queue.ts
autonomous: true
requirements:
  - LGSP-SEND-04
tags: [worker, bullmq, lgsp, queue, retry]

must_haves:
  truths:
    - "BullMQ Queue name `'lgsp-send'` exposed via `workers/src/queues/lgsp-send-queue.ts` (workers module) + `backend/src/lib/queue/lgsp-send-queue.ts` (backend producer)"
    - "Job name `'send-edoc-recipient'`, job data shape `LgspSendJobData` strict typed"
    - "Worker concurrency=3 (CONTEXT D-04)"
    - "Retry policy `attempts: 5, backoff: { type: 'exponential', delay: 30000 }` (CONTEXT D-10) → delays 30s/60s/120s/240s/480s"
    - "Worker handler per attempt: `getLgspService(sender_unit_id, env)` reload credential (CONTEXT D-14 — rotation cache invalidate)"
    - "Worker handler load outgoing_doc + recipient + attachments (MinIO base64) + sender/dest org info → buildEdxml() → sendDocument(buffer, destOrgCode, docCode)"
    - "Success path → UPDATE lgsp_tracking SET status='success', lgsp_doc_id=<from LGSP response> via SP fn_lgsp_tracking_update_status"
    - "4xx LGSP error (errorCode in {10,15,18,19,20,21,22,23}) → UPDATE tracking status='error' + error_message=mapped Vietnamese, NO throw → BullMQ ko retry (CONTEXT D-11)"
    - "Network/timeout/5xx → throw LgspSendError → BullMQ retry per backoff (CONTEXT D-11)"
    - "worker.on('failed') sau exhausted retry → UPDATE tracking status='error' + error_message='Retry exhausted: <last reason>' (CONTEXT D-13)"
    - "Worker logs structured pino với job_id, recipient_id, tracking_id, sender_unit_id mỗi log entry"
    - "Backend producer enqueue helper `enqueueLgspSendJob(data, jobId?)` exported"
    - "TypeScript compile pass cả workers + backend"
  artifacts:
    - path: "e_office_app_new/workers/src/queues/lgsp-send-queue.ts"
      provides: "Queue init (workers side, NOT used as producer — kept for worker construction reference)"
      min_lines: 30
    - path: "e_office_app_new/workers/src/jobs/lgsp-send-worker.ts"
      provides: "Worker handler đầy đủ build edXML → send → update tracking + on('failed') listener"
      min_lines: 250
    - path: "e_office_app_new/workers/src/index.ts"
      provides: "Import + start lgspSendWorker (modify existing — KHÔNG xóa workers cũ)"
      contains: "lgspSendWorker"
    - path: "e_office_app_new/backend/src/lib/queue/lgsp-send-queue.ts"
      provides: "Backend producer-side Queue + enqueueLgspSendJob helper (for Plan 34-03 route)"
      min_lines: 60
  key_links:
    - from: "Worker handler"
      to: "getLgspService(sender_unit_id, env) → svc.sendDocument(buffer, destOrgCode, docCode)"
      via: "buildEdxml() + MinIO getObject for attachments base64"
      pattern: "fresh credential per attempt (D-14 rotation)"
    - from: "Worker error path"
      to: "UPDATE lgsp_tracking via SP fn_lgsp_tracking_update_status"
      via: "Classify LGSP errorCode (no-retry) vs network (retry)"
      pattern: "CONTEXT D-11 + D-13"
    - from: "Backend enqueueLgspSendJob"
      to: "BullMQ queue 'lgsp-send' job 'send-edoc-recipient'"
      via: "Job data { recipient_id, outgoing_doc_id, tracking_id, sender_unit_id, environment }"
      pattern: "Producer-side (Plan 34-03 route gọi)"
---

<objective>
Setup BullMQ queue + worker MỚI cho LGSP send flow (CONTEXT D-15). Worker chạy ở `workers/` module (terminal #3 ngoài backend + frontend), backend chỉ enqueue.

**Worker handler per job (1 job = 1 external recipient — CONTEXT D-02):**
1. Reload credential qua `getLgspService(sender_unit_id, env)` (CONTEXT D-14 — invalidate cache pickup credential mới nếu admin update giữa retry)
2. Load outgoing_doc + recipient info + sender/dest org + doc_type + attachments (binary from MinIO)
3. Base64 encode attachments + buildEdxml() → Buffer
4. `svc.sendDocument(buffer, destOrgCode, docCode)`
5. Success → UPDATE lgsp_tracking status='success' + lgsp_doc_id
6. LGSP errorCode (4xx) → UPDATE status='error' + Vietnamese message → NO throw (no-retry per D-11)
7. Network/timeout → throw LgspSendError → BullMQ retry per D-10 backoff
8. worker.on('failed') sau exhausted → UPDATE status='error' + 'Retry exhausted: <last>'

**Backend producer-side:** Tạo helper `enqueueLgspSendJob(data)` để route Plan 34-03 enqueue.

**LƯU Ý KHÔNG xóa worker LGSP cũ (`lgspSendWorker` line 210-243 trong `workers/src/index.ts`):** Worker cũ vẫn consume queue `'lgsp-send'` với job data shape `{tracking_id, dest_org_code, edxml_content}` — Plan 34-02 REPLACE nó với handler mới shape `LgspSendJobData`. Producer Phase 18 (`backend/src/lib/queue/client.ts` line 12 `lgspSendQueue`) → backend Plan 34-03 chuyển sang dùng `enqueueLgspSendJob` mới (cleanup pattern).

Purpose: Hoàn tất worker infra để Plan 34-03 enqueue job từ route, Plan 34-05 E2E test.

Output: 4 file source mới + index.ts modify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-send-flow-sendedoc/34-CONTEXT.md
@.planning/phases/33-database-core-infrastructure/33-05-SUMMARY.md
@CLAUDE.md
@e_office_app_new/workers/src/index.ts
@e_office_app_new/workers/package.json
@e_office_app_new/backend/src/lib/queue/signing-queue.ts
@e_office_app_new/backend/src/lib/queue/redis-connection.ts
@e_office_app_new/backend/src/lib/queue/types.ts
@e_office_app_new/backend/src/services/lgsp.service.ts
@e_office_app_new/backend/src/services/lgsp-real.service.ts
@e_office_app_new/backend/src/services/lgsp/edxml-builder.ts
@e_office_app_new/backend/src/services/lgsp/error-codes.ts
@e_office_app_new/backend/src/lib/minio/client.ts
@e_office_app_new/backend/src/repositories/lgsp-agency-config.repository.ts

<read_first>
- `e_office_app_new/workers/src/index.ts` (line 1-321 — entry point hiện có 6 worker; Plan 34-02 REPLACE worker LGSP send line 210-243 với handler mới)
- `e_office_app_new/workers/package.json` (verify deps - cần add `xmlbuilder2`, `form-data`, `minio`, đã có bullmq + ioredis + pg + pino)
- `e_office_app_new/backend/src/lib/queue/signing-queue.ts` (pattern reference - Queue + enqueue helper + close)
- `e_office_app_new/backend/src/lib/queue/redis-connection.ts` (getRedisConnection() pattern)
- `e_office_app_new/backend/src/services/lgsp.service.ts` (getLgspService + invalidateLgspServiceCache exports)
- `e_office_app_new/backend/src/services/lgsp/edxml-builder.ts` (buildEdxml output shape — created Plan 34-01)
- `e_office_app_new/backend/src/services/lgsp/error-codes.ts` (LgspSendError + mapLgspError + isLgspNonRetryableError — created Plan 34-01)
</read_first>

<interfaces>
**Existing worker LGSP send (cũ — sẽ REPLACE):**
```typescript
// workers/src/index.ts line 210-243:
const lgspSendWorker = new Worker(
  'lgsp-send',
  async (job) => {
    const { tracking_id, dest_org_code, edxml_content } = job.data;   // ← old shape
    // ... LGSP_MOCK fallback or lgspSendEdoc() helper inline ...
  },
  { connection },
);
```

**New worker shape (Phase 34):**
```typescript
export interface LgspSendJobData {
  recipient_id: number;       // outgoing_doc_recipients.id (BIGINT)
  outgoing_doc_id: number;    // outgoing_docs.id (BIGINT)
  tracking_id: number;        // lgsp_tracking.id (BIGINT created by SP)
  sender_unit_id: number;     // root unit của DN gửi
  environment: 'sandbox' | 'prod';
}

// Worker = new Worker<LgspSendJobData>('lgsp-send', handler, { connection, concurrency: 3 })
// Job name = 'send-edoc-recipient'
```

**SP `fn_lgsp_tracking_update_status` (existing — line 5876 schema):**
```sql
edoc.fn_lgsp_tracking_update_status(
  p_id bigint, p_status varchar, p_lgsp_doc_id varchar, p_error_message text
) RETURNS TABLE(success boolean, message text)
-- Statuses: 'pending' | 'processing' | 'success' | 'error'
-- Khi p_status='success', tự set sent_at = NOW()
```

**MinIO client (existing - `backend/src/lib/minio/client.ts`):**
```typescript
import { minioClient } from '../../../backend/src/lib/minio/client.js';   // worker import from backend
// OR — worker tự khởi tạo client riêng (xem dưới)
// minioClient.getObject(bucket, path) → Promise<Readable stream>
```

**WORKERS MODULE LIMITATION:** Workers KHÔNG `import` được từ backend qua relative path (separate TS project / different `tsconfig.json`). Phase 34 solution:
- Workers tự khởi tạo `minio.Client` riêng trong `lgsp-send-worker.ts` (đọc env MINIO_*)
- Workers tự gọi pg trực tiếp qua `pg.Pool` (đã có pattern trong index.ts line 21-29)
- Workers IMPORT `getLgspService` + `buildEdxml` + `LgspSendError` + `mapLgspError` từ backend qua **relative path** `../../../backend/src/services/...`

Verify pattern này work bằng cách check `workers/tsconfig.json`:
```powershell
Get-Content e_office_app_new/workers/tsconfig.json
# Nếu có "rootDir": "./src" + "exclude": ["**/backend/**"] → KHÔNG import được
# Nếu rootDir broader → import OK
```

**Fallback nếu không import được:** Worker COPY `buildEdxml` + `LgspSendError` + `mapLgspError` (duplicate code) HOẶC khởi tạo `LGSPRealService` instance trực tiếp trong worker. Plan này thử relative import trước, fallback duplicate nếu fail.

**Recommended approach (theo Phase 18 pattern — workers `index.ts` line 99-167 đã inline LGSP HTTP calls bằng `fetch` thuần):** Worker `lgsp-send-worker.ts` SELF-CONTAINED — duplicate `buildEdxml` + error map + có MinIO client riêng + có pg.Pool riêng + call `LGSPRealService` per attempt. KHÔNG depend backend tsconfig.

**Quyết định Plan 34-02:** Worker self-contained. Import từ backend qua **relative path** chỉ nếu workers tsconfig allow; nếu không → duplicate code (acceptable tradeoff vì chỉ 3 module nhỏ + tránh build coupling).

**Specifically:** Task 2 worker handler will:
1. Khởi tạo `pg.Pool` riêng (line 14-19 index.ts pattern)
2. Khởi tạo `minio.Client` riêng (đọc env)
3. **IMPORT trực tiếp từ backend** qua `../../../backend/src/services/lgsp/edxml-builder.js`, `../../../backend/src/services/lgsp/error-codes.js`, `../../../backend/src/services/lgsp.service.js` — nếu tsconfig allow. Test pattern này TRƯỚC bằng simple import file.
4. Nếu TypeScript fail (rootDir/include error) → fallback: copy 3 file vào `workers/src/lgsp/` (duplicate). Document trong SUMMARY trade-off.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify workers tsconfig + add deps + tạo queue file workers side</name>
  <files>
    e_office_app_new/workers/package.json
    e_office_app_new/workers/src/queues/lgsp-send-queue.ts
  </files>
  <action>
**Bước 1: Verify workers tsconfig include scope:**

```powershell
Get-Content e_office_app_new/workers/tsconfig.json
# Note rootDir / include / exclude paths
```

Nếu rootDir = `./src` và include = `["src/**/*"]` → KHÔNG import được file ngoài rootDir. Phải DECIDE approach:
- **Approach A (preferred):** Edit tsconfig add `"references"` hoặc relax `"rootDir"` để cho phép import từ backend. Pattern: tsconfig `composite: false`, `rootDir` bỏ đi (mặc định = thư mục chứa tsconfig.json + parent), `include` thêm `["src/**/*", "../backend/src/services/lgsp/**/*", "../backend/src/services/lgsp-real.service.ts", "../backend/src/services/lgsp.service.ts", "../backend/src/repositories/lgsp-agency-config.repository.ts"]`. Lưu ý phải thêm tất cả transitive imports (lib/db, services/signing/crypto, ...).
- **Approach B (fallback):** Duplicate `buildEdxml` + `error-codes` + LGSP service vào `workers/src/lgsp/`. Single-source-of-truth bị mất nhưng đơn giản.

**Plan 34-02 prescription:** TRY Approach A first. Test bằng cách Bước 4 thêm import vào `lgsp-send-queue.ts`. Nếu `npx tsc --noEmit` fail trong workers/ thì fallback Approach B (copy 3 file).

**Bước 2: Add deps vào `workers/package.json`:**

```powershell
cd e_office_app_new/workers
$env:NODE_ENV='development'
npm install xmlbuilder2@^3.1.1 form-data@^4.0.1 minio@^8.0.7 --save
# minio để worker tự download attachment từ MinIO
```

Verify:
```powershell
Select-String -Path package.json -Pattern '"xmlbuilder2":'
Select-String -Path package.json -Pattern '"form-data":'
Select-String -Path package.json -Pattern '"minio":'
```

**Bước 3: Tạo NEW dir + file `workers/src/queues/lgsp-send-queue.ts`** (chỉ để expose Queue name constant + job name constant — KHÔNG init Queue ở đây vì worker chỉ là consumer):

```typescript
// ============================================================
// LGSP Send Queue — name + job name constants (workers side)
// Phase 34: Worker only consumes — Queue instance không init trong workers
//           (backend producer side: backend/src/lib/queue/lgsp-send-queue.ts)
// ============================================================

/** Queue name shared giữa backend producer + worker consumer */
export const LGSP_SEND_QUEUE_NAME = 'lgsp-send';

/** Job name */
export const LGSP_SEND_JOB_NAME = 'send-edoc-recipient';

/** Concurrency — CONTEXT D-04 */
export const LGSP_SEND_CONCURRENCY = 3;

/** Retry attempts — CONTEXT D-10 */
export const LGSP_SEND_MAX_ATTEMPTS = 5;

/** Backoff base delay (ms) — exponential factor 2 → 30s/60s/120s/240s/480s */
export const LGSP_SEND_BACKOFF_DELAY = 30_000;

/** Job timeout (ms) — handler must finish trong này */
export const LGSP_SEND_JOB_TIMEOUT = 90_000;

/** Job data shape (shared với backend enqueuer + worker consumer) */
export interface LgspSendJobData {
  recipient_id: number;       // outgoing_doc_recipients.id (BIGINT)
  outgoing_doc_id: number;    // outgoing_docs.id (BIGINT)
  tracking_id: number;        // lgsp_tracking.id (BIGINT) — created by SP fn_outgoing_doc_send_to_recipients
  sender_unit_id: number;     // root unit của DN gửi
  environment: 'sandbox' | 'prod';
}
```

**Bước 4: Test compile + import path từ Approach A:**

```powershell
cd e_office_app_new/workers
npx tsc --noEmit 2>&1 | Tee-Object -FilePath "$env:TEMP/workers_tsc_34_02_pre.log"
$LASTEXITCODE
# Phải = 0 (chỉ test queue file đơn giản — chưa import backend)
```

Sau khi setup file đơn giản này pass, Task 2 sẽ test approach import từ backend.
  </action>
  <verify>
    <automated>
      cd e_office_app_new/workers

      Test-Path package.json
      Select-String -Path package.json -Pattern '"xmlbuilder2":'
      Select-String -Path package.json -Pattern '"form-data":'
      Select-String -Path package.json -Pattern '"minio":'

      Test-Path node_modules/xmlbuilder2
      Test-Path node_modules/form-data
      Test-Path node_modules/minio

      Test-Path src/queues/lgsp-send-queue.ts
      Select-String -Path src/queues/lgsp-send-queue.ts -Pattern "export const LGSP_SEND_QUEUE_NAME = 'lgsp-send'"
      Select-String -Path src/queues/lgsp-send-queue.ts -Pattern "export const LGSP_SEND_JOB_NAME = 'send-edoc-recipient'"
      Select-String -Path src/queues/lgsp-send-queue.ts -Pattern "export const LGSP_SEND_CONCURRENCY = 3"
      Select-String -Path src/queues/lgsp-send-queue.ts -Pattern "export const LGSP_SEND_MAX_ATTEMPTS = 5"
      Select-String -Path src/queues/lgsp-send-queue.ts -Pattern "export interface LgspSendJobData"

      npx tsc --noEmit
      # Exit code = 0
    </automated>
  </verify>
  <done>
    `workers/package.json` có 3 deps mới; `workers/src/queues/lgsp-send-queue.ts` >= 30 dòng với 5 const + LgspSendJobData interface; TS compile pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tạo worker handler `workers/src/jobs/lgsp-send-worker.ts` đầy đủ</name>
  <files>
    e_office_app_new/workers/src/jobs/lgsp-send-worker.ts
    e_office_app_new/workers/tsconfig.json
  </files>
  <action>
**Bước 1: Modify `workers/tsconfig.json` để allow import từ backend (Approach A):**

Backup hiện tại + đọc:
```powershell
Copy-Item e_office_app_new/workers/tsconfig.json e_office_app_new/workers/tsconfig.json.bak
Get-Content e_office_app_new/workers/tsconfig.json
```

Add `"include"` field extending sang backend dependencies (nếu CHƯA có) HOẶC remove `"rootDir"` constraint. Specifically — đảm bảo những file sau resolve được:
- `../backend/src/services/lgsp.service.ts`
- `../backend/src/services/lgsp-real.service.ts`
- `../backend/src/services/lgsp/edxml-builder.ts`
- `../backend/src/services/lgsp/error-codes.ts`
- `../backend/src/repositories/lgsp-agency-config.repository.ts`
- `../backend/src/repositories/doc-book.repository.ts` (transitive — DbResult interface)
- `../backend/src/lib/db/*` (transitive — pool, callFunction)
- `../backend/src/services/signing/crypto.ts` (transitive — decryptSecret)

Simpler approach: Edit tsconfig.json — set `"rootDir"` = `..` (parent of workers/), add `"include": ["src/**/*", "../backend/src/services/**/*", "../backend/src/repositories/**/*", "../backend/src/lib/**/*"]`. Comment lý do trong file.

**Nếu Approach A fail (TS compile errors do other backend imports cascading):** Fallback Approach B (Task 2b dưới):
```
mkdir -p workers/src/lgsp
Copy backend/src/services/lgsp/edxml-builder.ts → workers/src/lgsp/edxml-builder.ts
Copy backend/src/services/lgsp/error-codes.ts → workers/src/lgsp/error-codes.ts
Copy backend/src/services/lgsp-real.service.ts → workers/src/lgsp/lgsp-real.service.ts
(copy phần minimum cần — LGSPRealService class + LgspCredentials interface)
Worker tự load credential từ DB (pg query) + decrypt manually qua pgcrypto SQL function thay vì import decryptSecret.
```

Document trade-off trong SUMMARY.

**Bước 2: Tạo NEW file `workers/src/jobs/lgsp-send-worker.ts` (handler đầy đủ):**

```typescript
// ============================================================
// LGSP Send Worker (Phase 34 - CONTEXT D-02, D-04, D-10, D-11, D-13, D-14, D-15)
//
// Consumer cho queue 'lgsp-send' job 'send-edoc-recipient'.
// 1 job = 1 external recipient (granularity per-recipient — fail ko drag sibling).
//
// Flow per job:
//   1. Lookup outgoing_doc + recipient + dest org + sender info
//   2. Load attachments tu MinIO -> base64
//   3. Build edXML envelope
//   4. Lookup credential qua getLgspService(sender_unit_id, env) — fresh per attempt (D-14)
//   5. svc.sendDocument(buffer, destOrgCode, docCode)
//   6. Success -> UPDATE tracking status='success' + lgsp_doc_id
//   7. LGSP errorCode (4xx) -> UPDATE status='error' + Vietnamese mapped, NO throw (no-retry D-11)
//   8. Network/timeout -> throw LgspSendError (BullMQ retry per D-10 backoff)
//
// on('failed') after retry exhausted -> UPDATE status='error' + 'Retry exhausted: <last reason>' (D-13)
// ============================================================

import { Worker } from 'bullmq';
import IORedis from 'ioredis';
import pg from 'pg';
import pino from 'pino';
import { Client as MinioClient } from 'minio';

import {
  LGSP_SEND_QUEUE_NAME,
  LGSP_SEND_CONCURRENCY,
  type LgspSendJobData,
} from '../queues/lgsp-send-queue.js';

// IMPORT từ backend — Approach A (Task 1 đã verify tsconfig allow)
// Nếu Approach B fallback, đổi paths sang `./lgsp/...`
import { buildEdxml, type BuildEdxmlInput } from '../../../backend/src/services/lgsp/edxml-builder.js';
import { LgspSendError, mapLgspError, isLgspNonRetryableError } from '../../../backend/src/services/lgsp/error-codes.js';
import { getLgspService } from '../../../backend/src/services/lgsp.service.js';

const { Pool } = pg;

const logger = pino({
  name: 'lgsp-send-worker',
  level: process.env.LOG_LEVEL || 'info',
  transport: process.env.NODE_ENV !== 'production' ? { target: 'pino-pretty' } : undefined,
});

const connection = new IORedis({
  host: process.env.REDIS_HOST || 'localhost',
  port: Number(process.env.REDIS_PORT) || 6379,
  password: process.env.REDIS_PASSWORD,
  maxRetriesPerRequest: null,
});

const pool = new Pool({
  host: process.env.PG_HOST || 'localhost',
  port: Number(process.env.PG_PORT) || 5432,
  database: process.env.PG_DATABASE || 'qlvb_dev',
  user: process.env.PG_USER || 'qlvb_admin',
  password: process.env.PG_PASSWORD,
  max: 5,
  idleTimeoutMillis: 30000,
});

const minioClient = new MinioClient({
  endPoint: process.env.MINIO_ENDPOINT || 'localhost',
  port: Number(process.env.MINIO_PORT) || 9000,
  useSSL: process.env.MINIO_USE_SSL === 'true',
  accessKey: process.env.MINIO_ACCESS_KEY || '',
  secretKey: process.env.MINIO_SECRET_KEY || '',
});
const MINIO_BUCKET = process.env.MINIO_BUCKET || 'documents';

// ============================================================
// SQL helpers (worker self-contained — không qua repo)
// ============================================================

interface OutgoingDocForSend {
  id: number;
  unit_id: number;
  notation: string | null;
  document_code: string | null;
  abstract: string | null;
  publish_date: string | null;
  signer: string | null;
  sign_date: string | null;
  number_paper: number | null;
  doc_type_id: number | null;
}

interface RecipientForSend {
  id: number;
  recipient_org_id: number;
  dest_code: string;
  dest_name: string;
}

interface SenderInfo {
  org_code: string | null;     // departments.lgsp_org_code
  org_name: string;            // departments.name
}

interface AttachmentForSend {
  file_name: string;
  file_path: string;
  content_type: string | null;
}

async function loadOutgoingDoc(docId: number): Promise<OutgoingDocForSend | null> {
  const res = await pool.query<OutgoingDocForSend>(
    `SELECT id, unit_id, notation, document_code, abstract,
            publish_date::text AS publish_date, signer, sign_date::text AS sign_date,
            number_paper, doc_type_id
     FROM edoc.outgoing_docs WHERE id = $1`,
    [docId],
  );
  return res.rows[0] ?? null;
}

async function loadRecipient(recipientId: number): Promise<RecipientForSend | null> {
  const res = await pool.query<RecipientForSend>(
    `SELECT r.id, r.recipient_org_id, o.code AS dest_code, o.name AS dest_name
     FROM edoc.outgoing_doc_recipients r
     JOIN edoc.inter_organizations o ON r.recipient_org_id = o.id
     WHERE r.id = $1 AND r.recipient_type = 'external_org'`,
    [recipientId],
  );
  return res.rows[0] ?? null;
}

async function loadSender(senderUnitId: number): Promise<SenderInfo | null> {
  const res = await pool.query<SenderInfo>(
    `SELECT lgsp_org_code AS org_code, name AS org_name
     FROM public.departments WHERE id = $1`,
    [senderUnitId],
  );
  return res.rows[0] ?? null;
}

async function loadDocTypeName(docTypeId: number | null): Promise<string | null> {
  if (!docTypeId) return null;
  const res = await pool.query<{ name: string }>(
    `SELECT name FROM public.doc_types WHERE id = $1`, [docTypeId],
  );
  return res.rows[0]?.name ?? null;
}

async function loadAttachments(docId: number): Promise<AttachmentForSend[]> {
  const res = await pool.query<AttachmentForSend>(
    `SELECT file_name, file_path, content_type
     FROM edoc.attachment_outgoing_docs WHERE outgoing_doc_id = $1
     ORDER BY sort_order, id`,
    [docId],
  );
  return res.rows;
}

async function loadAttachmentBuffer(filePath: string): Promise<Buffer> {
  const stream = await minioClient.getObject(MINIO_BUCKET, filePath);
  const chunks: Buffer[] = [];
  for await (const chunk of stream) {
    chunks.push(Buffer.isBuffer(chunk) ? chunk : Buffer.from(chunk));
  }
  return Buffer.concat(chunks);
}

async function updateTrackingStatus(
  trackingId: number,
  status: 'success' | 'error',
  lgspDocId: string | null,
  errorMessage: string | null,
): Promise<void> {
  await pool.query(
    `SELECT * FROM edoc.fn_lgsp_tracking_update_status($1, $2, $3, $4)`,
    [trackingId, status, lgspDocId, errorMessage],
  );
}

// ============================================================
// Worker handler
// ============================================================

async function handleSendJob(data: LgspSendJobData): Promise<void> {
  const { recipient_id, outgoing_doc_id, tracking_id, sender_unit_id, environment } = data;
  const logCtx = { recipient_id, outgoing_doc_id, tracking_id, sender_unit_id, environment };

  logger.info(logCtx, 'LGSP send job start');

  // 1. Load doc + recipient + sender
  const doc = await loadOutgoingDoc(outgoing_doc_id);
  if (!doc) {
    await updateTrackingStatus(tracking_id, 'error', null, 'Outgoing doc not found');
    logger.error(logCtx, 'Outgoing doc not found — marked tracking error');
    return;
  }
  const recipient = await loadRecipient(recipient_id);
  if (!recipient) {
    await updateTrackingStatus(tracking_id, 'error', null, 'Recipient not found or not external_org');
    logger.error(logCtx, 'Recipient not found — marked tracking error');
    return;
  }
  const sender = await loadSender(sender_unit_id);
  if (!sender || !sender.org_code) {
    await updateTrackingStatus(
      tracking_id, 'error', null,
      `Sender unit_id=${sender_unit_id} chua co lgsp_org_code (chua cau hinh)`,
    );
    logger.error(logCtx, 'Sender lgsp_org_code missing');
    return;
  }
  const docTypeName = await loadDocTypeName(doc.doc_type_id);

  // 2. Load attachments base64
  const attMetas = await loadAttachments(outgoing_doc_id);
  const attachments: BuildEdxmlInput['attachments'] = [];
  for (const att of attMetas) {
    try {
      const buf = await loadAttachmentBuffer(att.file_path);
      attachments.push({
        fileName: att.file_name,
        fileType: att.content_type || 'application/octet-stream',
        contentBase64: buf.toString('base64'),
      });
    } catch (err: unknown) {
      const msg = err instanceof Error ? err.message : String(err);
      logger.warn({ ...logCtx, attachment: att.file_name, err: msg }, 'Failed to load attachment from MinIO — skipping');
      // Continue without this attachment — partial send tốt hơn fail toàn bộ.
      // KH có thể trach VB không có file đính kèm sau, nhưng tránh block toàn flow.
    }
  }

  // 3. Build edXML
  const edxml = buildEdxml({
    senderOrgCode: sender.org_code,
    senderOrgName: sender.org_name,
    destOrgCode: recipient.dest_code,
    destOrgName: recipient.dest_name,
    notation: doc.notation,
    documentCode: doc.document_code,
    abstract: doc.abstract,
    publishDate: doc.publish_date,
    signer: doc.signer,
    signDate: doc.sign_date,
    signerPosition: null,    // outgoing_docs không có column signerPosition (theo CONTEXT D-08: fallback N/A)
    docTypeName,
    numberPaper: doc.number_paper,
    appendix: null,
    attachments,
  });
  logger.info({ ...logCtx, edxmlBytes: edxml.buffer.length, docId: edxml.docId, attachmentCount: attachments.length }, 'edXML built');

  // 4. Lookup service (fresh per attempt — D-14 credential rotation)
  let svc;
  try {
    svc = await getLgspService(sender_unit_id, environment);
  } catch (err: unknown) {
    // Credential missing/disabled/decrypt fail → no-retry, mark error
    const msg = err instanceof Error ? err.message : String(err);
    await updateTrackingStatus(tracking_id, 'error', null, `LGSP credential error: ${msg.slice(0, 500)}`);
    logger.error({ ...logCtx, err: msg }, 'getLgspService failed — marked tracking error');
    return;
  }

  // 5. Send
  try {
    const result = await svc.sendDocument(edxml.buffer, edxml.destOrgCode, edxml.docCode);
    if (result.success) {
      await updateTrackingStatus(tracking_id, 'success', result.lgsp_doc_id || edxml.docId, null);
      logger.info({ ...logCtx, lgsp_doc_id: result.lgsp_doc_id, message: result.message }, 'LGSP send SUCCESS');
      return;
    }
    // result.success = false → LGSP 4xx error (mapped Vietnamese đã có trong result.message)
    if (isLgspNonRetryableError(result.errorCode)) {
      await updateTrackingStatus(tracking_id, 'error', result.lgsp_doc_id || null, result.message);
      logger.warn({ ...logCtx, errorCode: result.errorCode, message: result.message }, 'LGSP send FAIL (no-retry per D-11)');
      return;   // KHÔNG throw → BullMQ ko retry
    }
    // Unknown errorCode → throw để retry (defensive)
    throw new LgspSendError(`LGSP returned success=false with unknown errorCode=${result.errorCode}: ${result.message}`, result.errorCode);
  } catch (err: unknown) {
    if (err instanceof LgspSendError && err.code && isLgspNonRetryableError(err.code)) {
      await updateTrackingStatus(tracking_id, 'error', null, err.vietnameseMessage);
      logger.warn({ ...logCtx, errorCode: err.code, message: err.vietnameseMessage }, 'LgspSendError 4xx — no retry');
      return;
    }
    // Network/timeout/5xx/unknown → throw → BullMQ retry per D-10 backoff
    const msg = err instanceof Error ? err.message : String(err);
    logger.error({ ...logCtx, err: msg }, 'LGSP send transient error — BullMQ will retry');
    throw err;
  }
}

// ============================================================
// Worker instantiation + lifecycle
// ============================================================

export function startLgspSendWorker(): Worker<LgspSendJobData> {
  const worker = new Worker<LgspSendJobData>(
    LGSP_SEND_QUEUE_NAME,
    async (job) => {
      await handleSendJob(job.data);
    },
    {
      connection,
      concurrency: LGSP_SEND_CONCURRENCY,
    },
  );

  worker.on('completed', (job) => {
    logger.debug({ jobId: job.id }, 'LGSP send job completed');
  });

  // CONTEXT D-13: Retry exhausted → mark tracking error
  worker.on('failed', async (job, err) => {
    if (!job) return;
    const attemptsMade = job.attemptsMade ?? 0;
    const maxAttempts = job.opts?.attempts ?? 5;
    if (attemptsMade < maxAttempts) {
      logger.debug(
        { jobId: job.id, attemptsMade, maxAttempts, err: err.message },
        'LGSP send attempt failed — BullMQ will retry',
      );
      return;
    }
    // Exhausted
    const data = job.data as LgspSendJobData;
    try {
      await updateTrackingStatus(
        data.tracking_id,
        'error',
        null,
        `Retry exhausted (${attemptsMade}/${maxAttempts}): ${err.message.slice(0, 500)}`,
      );
      logger.error({ jobId: job.id, ...data, err: err.message }, 'LGSP send EXHAUSTED — marked tracking error');
    } catch (updErr) {
      logger.error({ jobId: job.id, updErr }, 'Failed to update tracking after exhaustion');
    }
  });

  worker.on('error', (err) => {
    logger.error({ err: err.message }, 'LGSP send worker error');
  });

  logger.info({ queue: LGSP_SEND_QUEUE_NAME, concurrency: LGSP_SEND_CONCURRENCY }, 'LGSP send worker started');
  return worker;
}

// Graceful shutdown helper (workers/src/index.ts SIGTERM will call this)
export async function stopLgspSendWorker(worker: Worker): Promise<void> {
  await worker.close();
  await pool.end();
  connection.disconnect();
}
```

**Bước 3: Compile check workers:**

```powershell
cd e_office_app_new/workers
npx tsc --noEmit 2>&1 | Tee-Object -FilePath "$env:TEMP/workers_tsc_34_02.log"
$LASTEXITCODE
```

**Nếu Approach A FAIL (imports backend gây cascading errors):**

Fallback Approach B — copy 3 file vào `workers/src/lgsp/`:

```powershell
mkdir e_office_app_new/workers/src/lgsp -Force
Copy-Item e_office_app_new/backend/src/services/lgsp/edxml-builder.ts e_office_app_new/workers/src/lgsp/edxml-builder.ts
Copy-Item e_office_app_new/backend/src/services/lgsp/error-codes.ts e_office_app_new/workers/src/lgsp/error-codes.ts
# Copy LGSPRealService class (only sendDocument method needed)
# Worker tạo instance LGSPRealService trực tiếp:
#   import { LGSPRealService } from './lgsp/lgsp-real.service.js';
#   const svc = new LGSPRealService({ baseUrl, systemId, secretKey });
# Worker tự query lgsp_agency_config qua pg + decrypt qua SQL `pgp_sym_decrypt`
```

Cập nhật worker import: `'./lgsp/edxml-builder.js'`, `'./lgsp/error-codes.js'`, `'./lgsp/lgsp-real.service.js'`. Worker query DB:

```sql
SELECT base_url, system_id,
       pgp_sym_decrypt(secret_key_encrypted, current_setting('app.signing_secret_key'))::text AS secret_key
FROM edoc.lgsp_agency_config
WHERE unit_id = $1 AND environment = $2 AND is_active = TRUE;
```

(Worker SET `app.signing_secret_key` = `process.env.SIGNING_SECRET_KEY` ở đầu mỗi connection.)

Document Approach chosen trong SUMMARY.

**Critical worker behavior:**
- `pool` + `connection` + `minioClient` module-level singletons (1 instance / worker process)
- Worker self-contained — KHÔNG import backend repo (chỉ raw SQL)
- Handler return void cho success path + no-retry path → BullMQ mark job completed (status='error' đã update vào tracking row, BullMQ side OK)
- Handler throw cho retry path → BullMQ tự xếp lại job per backoff
- `on('failed')` chỉ act khi attemptsMade >= maxAttempts (exhausted)
- File length >= 250 dòng
  </action>
  <verify>
    <automated>
      cd e_office_app_new/workers

      Test-Path src/jobs/lgsp-send-worker.ts
      (Get-Content src/jobs/lgsp-send-worker.ts).Count
      # >= 250 lines

      # Verify required exports/imports/calls
      Select-String -Path src/jobs/lgsp-send-worker.ts -Pattern "export function startLgspSendWorker"
      Select-String -Path src/jobs/lgsp-send-worker.ts -Pattern "export async function stopLgspSendWorker"
      Select-String -Path src/jobs/lgsp-send-worker.ts -Pattern "concurrency: LGSP_SEND_CONCURRENCY"
      Select-String -Path src/jobs/lgsp-send-worker.ts -Pattern "buildEdxml\("
      Select-String -Path src/jobs/lgsp-send-worker.ts -Pattern "getLgspService\("
      Select-String -Path src/jobs/lgsp-send-worker.ts -Pattern "isLgspNonRetryableError\("
      Select-String -Path src/jobs/lgsp-send-worker.ts -Pattern "fn_lgsp_tracking_update_status"
      Select-String -Path src/jobs/lgsp-send-worker.ts -Pattern "worker.on\('failed'"
      Select-String -Path src/jobs/lgsp-send-worker.ts -Pattern "minioClient.getObject"
      Select-String -Path src/jobs/lgsp-send-worker.ts -Pattern "Retry exhausted"

      # TypeScript compile pass
      npx tsc --noEmit
      # Exit code = 0
    </automated>
  </verify>
  <done>
    `workers/src/jobs/lgsp-send-worker.ts` >= 250 dòng, có startLgspSendWorker() + stopLgspSendWorker() exports, on('failed') listener, buildEdxml + getLgspService + sendDocument call chain; TS compile pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Modify workers/src/index.ts (replace LGSP send worker cũ với worker mới) + tạo backend producer-side queue</name>
  <files>
    e_office_app_new/workers/src/index.ts
    e_office_app_new/backend/src/lib/queue/lgsp-send-queue.ts
  </files>
  <action>
**Bước 1: Modify `workers/src/index.ts`:**

(a) Add import ở đầu file (sau import hiện có):
```typescript
import { startLgspSendWorker, stopLgspSendWorker } from './jobs/lgsp-send-worker.js';
```

(b) **DELETE** worker cũ `lgspSendWorker` (line 209-243 — toàn bộ block `const lgspSendWorker = new Worker('lgsp-send', ...)`). Worker mới handle nó.

(c) Add invocation sau khi các worker khác instantiated (gần line ~244 sau lgspSendWorker cũ):
```typescript
// Phase 34 — LGSP send worker (replaces inline lgspSendWorker)
const newLgspSendWorker = startLgspSendWorker();
```

(d) Update SIGTERM handler (line 309-320) — thay `lgspSendWorker.close()` cũ bằng:
```typescript
await stopLgspSendWorker(newLgspSendWorker);
```

(e) Update log message line 306:
```typescript
logger.info('Workers started: email-send, sms-send, lgsp-receive, lgsp-send (Phase 34), fcm-push, zalo-send, notification-send');
```

**Lưu ý:** Helper `lgspSendEdoc()` inline (line 146-167 — toàn bộ function `lgspSendEdoc`) chỉ còn nếu Phase 35/36 dùng. Verify search `grep -n "lgspSendEdoc" workers/src/index.ts` — nếu chỉ dùng trong worker cũ (vừa xóa) → DELETE function này. Nếu còn worker khác dùng → giữ.

Tương tự: `lgspLogin()` (line 104-122) chỉ dùng cho `lgspReceiveList()` + `lgspSendEdoc()` cũ. Verify: nếu `lgspReceiveList()` worker còn → giữ. Phase 35 sẽ refactor receive worker tương tự.

**Bước 2: Tạo NEW file `backend/src/lib/queue/lgsp-send-queue.ts` (producer side):**

```typescript
// ============================================================
// LGSP Send Queue — backend producer side (Phase 34)
//
// Route POST /:id/gui-noi-bo (Plan 34-03) gọi enqueueLgspSendJob() sau khi
// SP fn_outgoing_doc_send_to_recipients commit success — 1 enqueue / external recipient.
//
// Worker side: workers/src/jobs/lgsp-send-worker.ts consume.
// ============================================================

import { Queue } from 'bullmq';
import pino from 'pino';
import { getRedisConnection } from './redis-connection.js';

const logger = pino({ name: 'lgsp-send-queue' });

// MUST match workers/src/queues/lgsp-send-queue.ts constants — duplicate consciously
// vì workers + backend là 2 module riêng (KHÔNG share import).
export const LGSP_SEND_QUEUE_NAME = 'lgsp-send';
export const LGSP_SEND_JOB_NAME = 'send-edoc-recipient';
export const LGSP_SEND_MAX_ATTEMPTS = 5;
export const LGSP_SEND_BACKOFF_DELAY_MS = 30_000;

/** Job data shape — MUST match `LgspSendJobData` trong workers/src/queues/lgsp-send-queue.ts */
export interface LgspSendJobData {
  recipient_id: number;
  outgoing_doc_id: number;
  tracking_id: number;
  sender_unit_id: number;
  environment: 'sandbox' | 'prod';
}

let _queue: Queue<LgspSendJobData> | null = null;

export function getLgspSendQueue(): Queue<LgspSendJobData> {
  if (_queue) return _queue;
  _queue = new Queue<LgspSendJobData>(LGSP_SEND_QUEUE_NAME, {
    connection: getRedisConnection(),
    defaultJobOptions: {
      attempts: LGSP_SEND_MAX_ATTEMPTS,
      backoff: {
        type: 'exponential',
        delay: LGSP_SEND_BACKOFF_DELAY_MS,
      },
      removeOnComplete: { count: 1000 },
      removeOnFail: { count: 5000 },
    },
  });
  logger.info({ queue: LGSP_SEND_QUEUE_NAME, attempts: LGSP_SEND_MAX_ATTEMPTS }, 'LGSP send queue initialized');
  return _queue;
}

/**
 * Enqueue 1 LGSP send job per external recipient.
 *
 * jobId = `lgsp-send-{tracking_id}` — BullMQ dedupe trong cùng queue, tránh double-enqueue
 * khi route handler được retry/replay accidentally.
 *
 * @param data Payload (IDs only — worker re-fetch outgoing_doc + attachment fresh)
 * @returns BullMQ job id
 */
export async function enqueueLgspSendJob(data: LgspSendJobData): Promise<string> {
  const queue = getLgspSendQueue();
  const jobId = `lgsp-send-${data.tracking_id}`;
  const job = await queue.add(LGSP_SEND_JOB_NAME, data, { jobId });
  logger.info({ jobId, ...data }, 'Enqueued LGSP send job');
  return job.id ?? jobId;
}

/** Graceful shutdown — gọi từ server.ts SIGTERM */
export async function closeLgspSendQueue(): Promise<void> {
  if (_queue) {
    try {
      await _queue.close();
    } catch (err) {
      logger.warn({ err }, 'Error closing LGSP send queue');
    }
    _queue = null;
  }
}
```

**Bước 3: Add `closeLgspSendQueue()` vào SIGTERM handler trong `backend/src/server.ts`:**

Tìm SIGTERM section + add line `await closeLgspSendQueue();` sau các queue close khác. Nếu KHÔNG có SIGTERM handler thì SKIP (graceful close không critical cho Phase 34).

```powershell
Select-String -Path e_office_app_new/backend/src/server.ts -Pattern "SIGTERM|SIGINT" -Context 2,5
```

Nếu có signal handler → add import + call. Nếu không → skip (document trong SUMMARY).

**Bước 4: TypeScript compile pass cả 2 module:**

```powershell
cd e_office_app_new/backend
npx tsc --noEmit
# Exit code = 0

cd ../workers
npx tsc --noEmit
# Exit code = 0
```
  </action>
  <verify>
    <automated>
      cd e_office_app_new

      # Verify workers index.ts
      Select-String -Path workers/src/index.ts -Pattern "startLgspSendWorker"
      Select-String -Path workers/src/index.ts -Pattern "stopLgspSendWorker"

      # Verify worker cũ đã xóa (không còn shape job cũ)
      $oldShape = Select-String -Path workers/src/index.ts -Pattern "tracking_id, dest_org_code, edxml_content" -SimpleMatch
      if ($oldShape) { Write-Error "Worker cũ vẫn còn — phải replace"; exit 1 }

      # Verify backend producer
      Test-Path backend/src/lib/queue/lgsp-send-queue.ts
      (Get-Content backend/src/lib/queue/lgsp-send-queue.ts).Count
      # >= 60 lines

      Select-String -Path backend/src/lib/queue/lgsp-send-queue.ts -Pattern "export function enqueueLgspSendJob"
      Select-String -Path backend/src/lib/queue/lgsp-send-queue.ts -Pattern "export function getLgspSendQueue"
      Select-String -Path backend/src/lib/queue/lgsp-send-queue.ts -Pattern "export interface LgspSendJobData"
      Select-String -Path backend/src/lib/queue/lgsp-send-queue.ts -Pattern "attempts: LGSP_SEND_MAX_ATTEMPTS"
      Select-String -Path backend/src/lib/queue/lgsp-send-queue.ts -Pattern "type: 'exponential'"

      # TypeScript compile cả 2 module
      cd backend
      npx tsc --noEmit
      cd ../workers
      npx tsc --noEmit
    </automated>
  </verify>
  <done>
    `workers/src/index.ts` đã thay worker LGSP cũ bằng `startLgspSendWorker()`; `backend/src/lib/queue/lgsp-send-queue.ts` >= 60 dòng với enqueueLgspSendJob + getLgspSendQueue + closeLgspSendQueue exported; TS compile zero error cả 2 module.
  </done>
</task>

</tasks>

<verification>
**Integration check sau 3 tasks (Wave 1 part 2):**

```powershell
cd e_office_app_new

# 1. TypeScript compile cả backend + workers
cd backend
npx tsc --noEmit 2>&1
$lastexitcode

cd ../workers
npx tsc --noEmit 2>&1
$lastexitcode

# 2. Verify exports backend producer
node -e "
import('./backend/dist/lib/queue/lgsp-send-queue.js').then(m => {
  console.log('enqueueLgspSendJob:', typeof m.enqueueLgspSendJob);
  console.log('getLgspSendQueue:', typeof m.getLgspSendQueue);
  console.log('closeLgspSendQueue:', typeof m.closeLgspSendQueue);
  console.log('LGSP_SEND_QUEUE_NAME:', m.LGSP_SEND_QUEUE_NAME);
  console.log('LGSP_SEND_JOB_NAME:', m.LGSP_SEND_JOB_NAME);
  console.log('LGSP_SEND_MAX_ATTEMPTS:', m.LGSP_SEND_MAX_ATTEMPTS);
});
"
# Tất cả phải show đúng giá trị

# 3. Verify worker startup (no enqueue test — Plan 34-05 sẽ test E2E)
# Start workers dev mode trong terminal riêng:
cd workers
$proc = Start-Process npm "run dev" -NoNewWindow -PassThru -RedirectStandardOutput "$env:TEMP/workers_start.log"
Start-Sleep -Seconds 5
Stop-Process -Id $proc.Id -Force
Get-Content "$env:TEMP/workers_start.log" | Select-String -Pattern "LGSP send worker started"
# Phải in: "LGSP send worker started"
```

**Pass criteria:** Tất cả TS compile + exports correct + worker start log show "LGSP send worker started".
</verification>

<success_criteria>
- [x] `workers/package.json` có 3 deps (xmlbuilder2, form-data, minio) trong dependencies
- [x] `workers/src/queues/lgsp-send-queue.ts` >= 30 dòng với 5 const + LgspSendJobData interface
- [x] `workers/src/jobs/lgsp-send-worker.ts` >= 250 dòng với:
  - startLgspSendWorker() + stopLgspSendWorker() exports
  - Concurrency 3, 5 attempts exponential backoff config implied (queue defaultJobOptions ở producer side)
  - Handler: load doc/recipient/sender/attachments → buildEdxml → getLgspService per attempt → sendDocument → classify error retry/no-retry
  - on('failed') listener mark tracking error sau exhausted
  - Self-contained pool + minioClient + connection module-level
- [x] `workers/src/index.ts` thay worker LGSP cũ → invoke startLgspSendWorker() + cập nhật SIGTERM
- [x] `backend/src/lib/queue/lgsp-send-queue.ts` >= 60 dòng với enqueueLgspSendJob() + getLgspSendQueue() + close + LgspSendJobData interface
- [x] Backend producer config attempts=5 + exponential backoff delay=30000
- [x] TypeScript compile cả backend + workers zero error
- [x] Worker startup log show "LGSP send worker started"
</success_criteria>

<output>
After completion, create `.planning/phases/34-send-flow-sendedoc/34-02-SUMMARY.md` với:
- Files created: 3 (workers/queues/, workers/jobs/, backend/lib/queue/lgsp-send-queue.ts)
- Files modified: 2 (workers/index.ts, workers/package.json) + optional backend/server.ts (SIGTERM)
- Approach used: A (import from backend) OR B (duplicate code) — document trade-off
- Deps added (workers): xmlbuilder2, form-data, minio
- Worker config: concurrency=3, attempts=5, backoff exponential delay=30s, timeout=90s
- Error classification: 4xx LGSP (mapped) = no-retry; network/timeout = retry; exhausted = mark error
- TypeScript: PASS / 0 error
- Worker startup smoke test: log "LGSP send worker started" verified
- Next: Plan 34-03 (wire enqueue trong route /:id/gui-noi-bo)
</output>
