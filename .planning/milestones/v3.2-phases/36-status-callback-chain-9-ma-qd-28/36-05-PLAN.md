---
phase: 36-status-callback-chain-9-ma-qd-28
plan: 05
type: execute
wave: 3
depends_on: ["36-01", "36-02", "36-03", "36-04"]
files_modified:
  - .planning/phases/36-status-callback-chain-9-ma-qd-28/36-05-VERIFICATION-REPORT.md
autonomous: true
requirements:
  - LGSP-STATUS-02
  - LGSP-STATUS-03
  - LGSP-STATUS-04
  - LGSP-STATUS-05
  - LGSP-STATUS-06
  - LGSP-STATUS-07
  - LGSP-STATUS-08
  - LGSP-STATUS-09
  - LGSP-STATUS-10

must_haves:
  truths:
    - "TypeScript strict 3 modules (backend + workers + frontend) clean"
    - "Production build pass 3 modules (backend npm run build + frontend npm run build + workers tsc --noEmit)"
    - "Master schema apply 2x lien tiep zero error (idempotent verified)"
    - "SP count baseline >= 361 (Phase 33 + Phase 35 KHÔNG bị drop)"
    - "Both workers smoke test print 'started' log line (lgsp-status-tick + lgsp-status-event)"
    - "E2E sandbox test PASS: trigger 4 action sequence (03→04→05→06) trên DN.001 sandbox LGSP doc → outbox INSERT đầy đủ + worker đẩy success/error"
    - "Dedup test PASS: trigger action lần 2 → UNIQUE chặn, KHÔNG có row outbox thứ 2"
    - "Error path test PASS: tạm UPDATE credential sai → giao-viec → outbox 04 enqueued → worker fail mã 15 → sent_status='error' + retry_count populated"
    - "Approach B audit pass: workers/src/lgsp/lgsp-status-service.ts NOT importing backend"
    - "VERIFICATION-REPORT.md created with PASS/FAIL matrix cho mỗi REQ"
  artifacts:
    - path: ".planning/phases/36-status-callback-chain-9-ma-qd-28/36-05-VERIFICATION-REPORT.md"
      provides: "Final verification matrix + caveats + handoff to Phase 37"
  key_links:
    - from: "E2E test sandbox"
      to: "DN.001 LGSP credential (sandbox active)"
      via: "/v1/updateStatus với credential thật từ Plan 33-02 seed"
      pattern: "VERIFICATION-REPORT.md sandbox section"
---

<objective>
Wave 3 (final) — Verify toàn bộ Phase 36 work end-to-end. Mirror Phase 35-05 verification pattern (sandbox happy/dedup/error tests + Approach B audit + cleanup). Produce comprehensive `36-05-VERIFICATION-REPORT.md` documenting PASS/FAIL cho mỗi REQ + caveats (credential rotation issue if hits, like Phase 34-05 + Phase 35-05 did).

Purpose: Gating Phase 36 → Phase 37. KHÔNG cho phase tiếp theo bắt đầu nếu E2E fail.

Output: VERIFICATION-REPORT.md + commit. Phase 36 marked COMPLETE in STATE.md (Plan 36-05 SUMMARY).

**Test scope per CONTEXT D-16:**
- Active: mã 02/03/04/05/06 (6 mã receiver-side)
- Defer Phase 37: mã 13/15/16 sender-side recall flow
- Caveat: credential rotation (giống Phase 34-05/35-05) — sẽ document NOT block ship
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-status-callback-chain-9-ma-qd-28/36-CONTEXT.md
@.planning/phases/35-receive-flow-cron-syncreceivededoclist/35-05-VERIFICATION-REPORT.md
@.planning/phases/34-send-flow-sendedoc/34-05-VERIFICATION-REPORT.md
@CLAUDE.md
</context>

<threat_model>
N/A — verification plan, no new code surface. Inherits threat models from Plan 36-01 + 02 + 03 + 04.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Static verification — TypeScript + production build + schema idempotent 3 modules</name>
  <files>(no files modified — verification commands only)</files>

  <read_first>
    1. Read Plan 36-01/02/03/04 SUMMARY (created when those plans shipped).
    2. Read Phase 35-05 VERIFICATION-REPORT.md as exact pattern reference.
  </read_first>

  <action>
    Run static verification commands + capture all output. Save outputs cho Task 5 report generation:

    ```bash
    # 1. TypeScript strict 3 modules
    cd D:/ProjectAI/quanlyvanban/e_office_app_new/backend && npx tsc --noEmit && echo "BACKEND TS: PASS"
    cd D:/ProjectAI/quanlyvanban/e_office_app_new/workers && npx tsc --noEmit && echo "WORKERS TS: PASS"
    cd D:/ProjectAI/quanlyvanban/e_office_app_new/frontend && npx tsc --noEmit && echo "FRONTEND TS: PASS"

    # 2. Production build 3 modules (CLAUDE.md pitfall #4 — TS strict regression catch)
    cd D:/ProjectAI/quanlyvanban/e_office_app_new/backend && npm run build && echo "BACKEND BUILD: PASS"
    cd D:/ProjectAI/quanlyvanban/e_office_app_new/frontend && npm run build && echo "FRONTEND BUILD: PASS"
    # workers KHONG co npm run build script (tsx run direct) -> tsc --noEmit la build equivalent

    # 3. Schema apply 2x idempotent
    cd D:/ProjectAI/quanlyvanban
    docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - < e_office_app_new/database/schema/000_schema_v3.0.sql > /tmp/schema_apply_1.log 2>&1
    echo "Apply 1 exit: $?"
    docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - < e_office_app_new/database/schema/000_schema_v3.0.sql > /tmp/schema_apply_2.log 2>&1
    echo "Apply 2 exit: $?"
    # Both must exit 0

    # Apply 3rd time để confirm full idempotency:
    docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - < e_office_app_new/database/schema/000_schema_v3.0.sql > /tmp/schema_apply_3.log 2>&1
    echo "Apply 3 exit: $?"

    # 4. UNIQUE constraint exists in DB
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT con.conname FROM pg_constraint con JOIN pg_class cls ON cls.oid = con.conrelid WHERE cls.relname = 'lgsp_status_outbox' AND con.contype = 'u' AND con.conname = 'uq_lgsp_status_outbox_doc_status';"
    # Expected output: uq_lgsp_status_outbox_doc_status

    # 5. SP count baseline (must be >= 361 from Phase 35 baseline)
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM pg_proc WHERE pronamespace IN ('public'::regnamespace, 'edoc'::regnamespace) AND proname LIKE 'fn_%';"
    # Expected: >= 361

    # 6. SP overload check (must be 0)
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM (SELECT n.nspname, p.proname FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace WHERE n.nspname IN ('public','edoc','esto','cont','iso') AND p.proname LIKE 'fn_%' GROUP BY 1,2 HAVING count(*) > 1) t;"
    # Expected: 0
    ```

    Capture all output strings into temp file `/tmp/phase36_static_verify.log` for Task 5 inclusion.
  </action>

  <verify>
    <automated>
      cd D:/ProjectAI/quanlyvanban/e_office_app_new/backend && npx tsc --noEmit && cd ../workers && npx tsc --noEmit && cd ../frontend && npx tsc --noEmit && echo "3 MODULES TS PASS"
    </automated>
    <automated>
      cd D:/ProjectAI/quanlyvanban/e_office_app_new/backend && npm run build && cd ../frontend && npm run build && echo "BUILD PASS"
    </automated>
    <automated>
      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT con.conname FROM pg_constraint con JOIN pg_class cls ON cls.oid = con.conrelid WHERE cls.relname = 'lgsp_status_outbox' AND con.contype = 'u';" | grep -q "uq_lgsp_status_outbox_doc_status" && echo "UNIQUE EXISTS"
    </automated>
  </verify>

  <acceptance_criteria>
    - Backend `npx tsc --noEmit` exit 0
    - Workers `npx tsc --noEmit` exit 0
    - Frontend `npx tsc --noEmit` exit 0
    - Backend `npm run build` exit 0
    - Frontend `npm run build` exit 0
    - Schema apply lần 1+2+3 all exit 0 (3x idempotent verified)
    - UNIQUE constraint `uq_lgsp_status_outbox_doc_status` exists in dev DB
    - SP count >= 361
    - SP overload count = 0
  </acceptance_criteria>

  <done>
    Static verification all PASS. Output captured for VERIFICATION-REPORT generation. NO commit needed (no files modified). Continue Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Workers smoke test — both status workers start successfully</name>
  <files>(no files modified)</files>

  <read_first>
    1. Phase 35-02 SUMMARY smoke test pattern (timeout 8 npx tsx + grep started log lines).
  </read_first>

  <action>
    Run worker smoke test:

    ```bash
    cd D:/ProjectAI/quanlyvanban/e_office_app_new/workers
    timeout 10 npx tsx src/index.ts 2>&1 > /tmp/phase36_workers_smoke.log
    cat /tmp/phase36_workers_smoke.log

    # Verify both status workers logged "started":
    grep -E "LGSP status (tick|event) worker started" /tmp/phase36_workers_smoke.log | wc -l
    # Expected: >= 2

    # Verify queue + concurrency correct:
    grep "LGSP status tick worker started" /tmp/phase36_workers_smoke.log | grep -q "lgsp-status" && echo "TICK queue OK"
    grep "LGSP status tick worker started" /tmp/phase36_workers_smoke.log | grep -q '"concurrency":1' && echo "TICK concurrency OK"
    grep "LGSP status event worker started" /tmp/phase36_workers_smoke.log | grep -q '"concurrency":5' && echo "EVENT concurrency OK"
    grep "LGSP status event worker started" /tmp/phase36_workers_smoke.log | grep -q '"maxAttempts":5' && echo "EVENT maxAttempts OK"

    # Verify startup summary includes Phase 36:
    grep "lgsp-status (Phase 36" /tmp/phase36_workers_smoke.log && echo "STARTUP SUMMARY OK"

    # Note: Redis NOAUTH errors expected nếu workers/.env không có REDIS_PASSWORD (Phase 35-02 issue,
    # NOT a code regression). Workers vẫn print "started" log trước khi try Redis connect.
    ```

    Capture output cho Task 5.
  </action>

  <verify>
    <automated>
      cd D:/ProjectAI/quanlyvanban/e_office_app_new/workers && timeout 10 npx tsx src/index.ts 2>&1 | grep -c "LGSP status .* worker started" | grep -q "^[2-9]" && echo "BOTH STATUS WORKERS STARTED"
    </automated>
  </verify>

  <acceptance_criteria>
    - Smoke test prints "LGSP status tick worker started" (queue=lgsp-status, concurrency=1, maxAttempts=1)
    - Smoke test prints "LGSP status event worker started" (queue=lgsp-status, concurrency=5, maxAttempts=5)
    - Startup summary log includes "lgsp-status (Phase 36: tick + event)"
    - Phase 34/35 workers still start (regression check): lgsp-send + lgsp-receive (tick + dn) all print started
    - Process exits cleanly after timeout (no infinite loop or hang)
  </acceptance_criteria>

  <done>
    Workers smoke PASS — both Phase 36 status workers print started log lines + Phase 34/35 workers unchanged. Output captured. Continue Task 3.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Approach B audit — workers/src/lgsp-status-* NOT importing backend</name>
  <files>(no files modified)</files>

  <read_first>
    1. Phase 35-05 VERIFICATION-REPORT § "Approach B audit" (parser checksum + import boundary).
  </read_first>

  <action>
    Audit Approach B compliance for Phase 36 worker files:

    ```bash
    cd D:/ProjectAI/quanlyvanban

    # 1. Check NO backend imports trong 3 file Phase 36 workers:
    grep -l "from '../../../backend\|from '../../backend\|from '../../../../backend" \
      e_office_app_new/workers/src/jobs/lgsp-status-tick-worker.ts \
      e_office_app_new/workers/src/jobs/lgsp-status-event-worker.ts \
      e_office_app_new/workers/src/lgsp/lgsp-status-service.ts \
      e_office_app_new/workers/src/queues/lgsp-status-queue.ts
    # Expected: empty output (no matches)

    # 2. Check workers/src/lgsp-status-service vs backend services lgsp-real.service updateStatus -- 
    # NOT required to be identical (workers is per-attempt fresh-load context, not HTTP service class).
    # Just verify both files have similar updateStatus HTTP shape (same endpoint + headers + body shape):
    diff <(grep "/v1/updateStatus" e_office_app_new/backend/src/services/lgsp-real.service.ts) \
         <(grep "/v1/updateStatus" e_office_app_new/workers/src/lgsp/lgsp-status-service.ts)
    # Expected: 1 match in each (no diff content) -- both files have the same endpoint string.

    # 3. Verify Phase 34/35 Approach B parser/builder STILL in sync (regression check):
    md5sum e_office_app_new/backend/src/services/lgsp/edxml-builder.ts e_office_app_new/workers/src/lgsp/edxml-builder.ts
    md5sum e_office_app_new/backend/src/services/lgsp/edxml-parser.ts e_office_app_new/workers/src/lgsp/edxml-parser.ts
    md5sum e_office_app_new/backend/src/services/lgsp/error-codes.ts e_office_app_new/workers/src/lgsp/error-codes.ts
    # Expected: matching pairs of checksums (KHONG drift since Phase 34-05/35-05)
    # NOTE: error-codes có thể NO match nếu Phase 36-01 add updateStatus error code mapping to backend
    # but NOT to workers (KHÔNG necessary — workers reuses Phase 34 mapping). Document deviation.

    # 4. Worker imports inventory cho 3 Phase 36 files:
    echo "=== lgsp-status-tick-worker imports ==="
    grep "^import" e_office_app_new/workers/src/jobs/lgsp-status-tick-worker.ts
    echo "=== lgsp-status-event-worker imports ==="
    grep "^import" e_office_app_new/workers/src/jobs/lgsp-status-event-worker.ts
    echo "=== lgsp-status-service imports ==="
    grep "^import" e_office_app_new/workers/src/lgsp/lgsp-status-service.ts
    # All imports must be relative (./ or ../) OR npm packages (bullmq, ioredis, pg, pino).
    # NOT backend paths.
    ```
  </action>

  <verify>
    <automated>
      cd D:/ProjectAI/quanlyvanban && (grep -l "from '../../../backend\|from '../../backend" e_office_app_new/workers/src/jobs/lgsp-status-*.ts e_office_app_new/workers/src/lgsp/lgsp-status-service.ts e_office_app_new/workers/src/queues/lgsp-status-queue.ts 2>/dev/null; true) | wc -l | grep -q "^0$" && echo "APPROACH B PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - Zero matches grep `from '../../backend|from '../../../backend` trong 4 Phase 36 workers files
    - Phase 34/35 builder/parser/error-codes checksums STILL match between backend + workers (regression catch)
    - All Phase 36 worker imports are relative (./ ../) OR npm packages — verified by inspection
  </acceptance_criteria>

  <done>
    Approach B audit PASS — Phase 36 workers self-contained. Phase 34/35 sync still maintained (or documented deviation). Capture deviations for Task 5. Continue Task 4.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: E2E sandbox test — happy path + dedup + error path</name>
  <files>(no files modified — manual test execution)</files>

  <read_first>
    1. Phase 36 CONTEXT D-16 (E2E test spec).
    2. Phase 35-05 SUMMARY (caveat về credential rotation issue — same will apply Phase 36).
    3. `docs/Trục EDOC Lạng Sơn - QLVB Doanh nghiệp/List.txt` (sandbox URL + DN.001 sandbox credential).
  </read_first>

  <action>
    Execute E2E test sequence trên dev environment với DN.001 sandbox active:

    **Pre-condition:**
    - Backend running on port 4000 (`cd backend && npm run dev`)
    - Workers running (`cd workers && npx tsx src/index.ts` in 2nd terminal)
    - DN.001 sandbox row trong lgsp_agency_config với is_active=TRUE + credential thật (Plan 33-02 seed)
    - Ít nhất 1 incoming_doc source_type='external_lgsp' tồn tại trong DB (có thể từ Phase 35 receive test)
    - Nếu KHÔNG có doc LGSP nào → tạo manual qua INSERT SQL (Phase 35-05 test left 1 doc lý do verify)

    **Step 1: Trigger 4 actions sequence + verify outbox INSERTED:**

    ```bash
    # 1. Lookup DN.001 sandbox incoming_doc external_lgsp:
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT id, external_doc_id, lgsp_sender_org_code, unit_id FROM edoc.incoming_docs WHERE source_type = 'external_lgsp' AND unit_id = (SELECT id FROM public.departments WHERE lgsp_org_code = 'H37.DN.001' LIMIT 1) ORDER BY id DESC LIMIT 1;"
    # Capture: DOC_ID

    # 2. Get admin JWT (login):
    TOKEN=$(curl -s -X POST http://localhost:4000/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"Admin@123"}' | jq -r .data.token)
    echo "TOKEN: $TOKEN"

    # 3a. PATCH /danh-dau-da-doc -> outbox 03
    curl -s -X PATCH "http://localhost:4000/api/van-ban-den/danh-dau-da-doc" \
      -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
      -d "{\"doc_ids\":[$DOC_ID]}"
    sleep 2
    # Verify outbox 03 INSERTED:
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "SELECT id, target_status, sent_status, payload FROM edoc.lgsp_status_outbox WHERE incoming_doc_id = $DOC_ID ORDER BY created_at DESC;"

    # 3b. Wait 35s for tick worker, verify sent_status='success' (or 'error' if credential issue):
    sleep 35
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "SELECT id, target_status, sent_status, error_message, retry_count, sent_at FROM edoc.lgsp_status_outbox WHERE incoming_doc_id = $DOC_ID;"

    # 3c. Repeat for giao-viec (04):
    # Need: name (text), curator_ids (array), end_date (string date). Adapt to dev fixtures.
    curl -s -X POST "http://localhost:4000/api/van-ban-den/$DOC_ID/giao-viec" \
      -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
      -d '{"name":"HSCV test Phase 36","curator_ids":[2],"end_date":"2026-06-30","note":"E2E test"}'
    sleep 2
    # Capture HSCV ID for step 3f later

    # 3d. but-phe (05):
    curl -s -X POST "http://localhost:4000/api/van-ban-den/$DOC_ID/but-phe" \
      -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
      -d '{"content":"E2E test but phe Phase 36"}'
    sleep 2

    # 3e. chuyen-luu-tru (06):
    # Need archive payload (fond_id, warehouse_id, etc.) -- adapt to dev DB.
    # If complex, skip + rely on HSCV complete trigger 06 instead.

    # 3f. HSCV complete (06 via handling-doc.ts hook):
    # PATCH /api/ho-so-cong-viec/<id>/trang-thai with action='complete'
    curl -s -X PATCH "http://localhost:4000/api/ho-so-cong-viec/$HSCV_ID/trang-thai" \
      -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
      -d '{"action":"complete"}'
    sleep 2
    ```

    **Step 2: Wait final tick + verify all 4 statuses:**

    ```bash
    sleep 60  # 2 ticks
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "SELECT id, target_status, sent_status, error_message, retry_count, sent_at FROM edoc.lgsp_status_outbox WHERE incoming_doc_id = $DOC_ID ORDER BY created_at;"
    # Expected: 4 rows (03/04/05/06), all sent_status='success' (or 'error' with credential caveat)
    ```

    **Step 3: Dedup test — trigger mark-read lần 2:**

    ```bash
    curl -s -X PATCH "http://localhost:4000/api/van-ban-den/danh-dau-da-doc" \
      -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
      -d "{\"doc_ids\":[$DOC_ID]}"
    sleep 2
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "SELECT count(*) FROM edoc.lgsp_status_outbox WHERE incoming_doc_id = $DOC_ID AND target_status = '03';"
    # Expected: 1 (UNIQUE constraint chặn duplicate)
    ```

    **Step 4: Error path test — temp UPDATE credential sai:**

    ```bash
    # Backup current credential (save to file):
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT system_id FROM edoc.lgsp_agency_config WHERE unit_id = (SELECT id FROM public.departments WHERE lgsp_org_code = 'H37.DN.001' LIMIT 1) AND environment = 'sandbox';" > /tmp/orig_systemid.txt
    
    # Temp UPDATE to wrong systemId:
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "UPDATE edoc.lgsp_agency_config SET system_id = 'WRONG.ID' WHERE unit_id = (SELECT id FROM public.departments WHERE lgsp_org_code = 'H37.DN.001' LIMIT 1) AND environment = 'sandbox';"

    # Trigger giao-viec lần 2 (different name -- KHONG dedup vì 04 đã có rồi nên cũng dedup, dùng but-phe khác hoặc tạo doc LGSP test 2):
    # Skip if UNIQUE chặn 04. Verify error path via direct outbox insert + invalid:
    
    # Alternative: tao test doc moi va run giao-viec voi credential sai:
    # (Document approach used trong report)
    
    # Wait worker fail, verify sent_status='error', retry_count > 0:
    sleep 90  # 3 ticks + 1 retry attempt
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "SELECT id, target_status, sent_status, error_message, retry_count FROM edoc.lgsp_status_outbox WHERE sent_status = 'error' ORDER BY created_at DESC LIMIT 5;"

    # RESTORE credential:
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "UPDATE edoc.lgsp_agency_config SET system_id = '$(cat /tmp/orig_systemid.txt | tr -d ' ')' WHERE unit_id = (SELECT id FROM public.departments WHERE lgsp_org_code = 'H37.DN.001' LIMIT 1) AND environment = 'sandbox';"
    rm /tmp/orig_systemid.txt
    ```

    **Step 5: UI visual check (manual, document):**

    - Open browser http://localhost:3000/van-ban-den/$DOC_ID (logged in as admin)
    - Verify Phase 35-04 "Nguồn LGSP" card renders
    - Verify Phase 36-04 "Lịch sử trạng thái LGSP" card renders với Timeline 4 entries
    - Verify mỗi entry có Tag mã + sent_status tag + timestamp
    - Hover tooltip → Vietnamese description đầy đủ
    - Document: chụp screenshot OR text observation

    **Step 6: Capture all output for report:**

    Save:
    - `/tmp/phase36_e2e_happy.log` (Step 1-2)
    - `/tmp/phase36_e2e_dedup.log` (Step 3)
    - `/tmp/phase36_e2e_error.log` (Step 4)
    - `/tmp/phase36_e2e_ui.md` (Step 5 visual observation)

    **Document credential caveat** nếu E2E hit credential rotation issue (Phase 34-05/35-05 đã document tương tự).
  </action>

  <verify>
    <automated>
      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM edoc.lgsp_status_outbox;" | grep -qE "^[1-9]" && echo "OUTBOX HAS ROWS"
    </automated>
  </verify>

  <acceptance_criteria>
    - 4 outbox rows created (03/04/05/06) cho test doc
    - Dedup test: 03 chỉ có 1 row sau 2 lần PATCH mark-read
    - Error path test: ít nhất 1 row sent_status='error' với retry_count > 0 + error_message Vietnamese mapped
    - UI visual: section "Lịch sử trạng thái LGSP" render với Timeline 4 entries
    - Credential restored sau test (KHÔNG để lại dev DB hỏng)
    - Caveat documented (credential rotation issue, KHÔNG block ship)
  </acceptance_criteria>

  <done>
    E2E test executed + output captured. Real LGSP sandbox response (success OR credential error) documented. Continue Task 5 (report generation).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Generate VERIFICATION-REPORT.md + commit + mark Phase 36 complete</name>
  <files>.planning/phases/36-status-callback-chain-9-ma-qd-28/36-05-VERIFICATION-REPORT.md, .planning/STATE.md</files>

  <read_first>
    1. Read `.planning/phases/35-receive-flow-cron-syncreceivededoclist/35-05-VERIFICATION-REPORT.md` toàn bộ — exact format/sections to mirror.
    2. Read `.planning/STATE.md` — pattern cho Phase 36 update.
    3. Read all captured logs from Task 1-4.
  </read_first>

  <action>
    **Phần A: Create `36-05-VERIFICATION-REPORT.md`** mirror Phase 35-05 structure:

    Sections:
    1. **Frontmatter** (phase, plan, status, completed_date, mode=verification)
    2. **Executive Summary** (PASS/FAIL overall + caveats list)
    3. **Static Verification (Task 1)** — TypeScript 3 modules, production build 3 modules, schema idempotent 3x, SP count baseline, UNIQUE constraint exists
    4. **Smoke Test (Task 2)** — Workers startup log evidence
    5. **Approach B Audit (Task 3)** — Boundary check, parser/builder/error-codes checksums
    6. **E2E Sandbox Test (Task 4)** — 4-action happy path + dedup + error path with capture
    7. **UI Visual Verification (Task 4 Step 5)** — Text description of detail page section
    8. **REQ Coverage Matrix** — Each REQ-ID (STATUS-02..10) PASS/PARTIAL/DEFER với evidence link
    9. **Known Stubs / Caveats** — credential rotation, LGSP-STATUS-07 full refactor defer Phase 37, LGSP-STATUS-08 sender-side defer Phase 37
    10. **Recommendations for Phase 37** — Specific next-step actions
    11. **Commits inventory** — All commits từ Plan 36-01/02/03/04/05
    12. **Self-check** — Files exist, commits exist, all acceptance gates met

    Use Vietnamese KHÔNG DẤU trong code blocks/log strings (PowerShell safe), Vietnamese có dấu trong narrative prose.

    **Phần B: Update `.planning/STATE.md`:**

    1. Change `stopped_at:` field to reflect Phase 36 complete:
       ```yaml
       stopped_at: Phase 36 COMPLETE (5/5 plans) — moving to Phase 37
       ```

    2. Update `last_activity:` với date 2026-05-21+ (or actual date):
       ```yaml
       last_activity: <DATE> — Phase 36 verification PASS với caveat (credential rotation, same Phase 34-05/35-05 pattern), mark complete, continue Phase 37 (Admin UI + Catalog + Go-live)
       ```

    3. Update `## Current Position`:
       - Phase: 37 (Admin UI + Catalog + Go-live)
       - Status: ready to discuss

    4. Update progress in frontmatter:
       - `completed_phases: 4` (was 3)
       - `completed_plans: 20` (was 15 — add 5)
       - `percent: 80` (was 60)

    5. Add new decision entries (after existing Phase 33 decisions):
       ```
       - [Phase 36]: Plan 36-01: APPEND UNIQUE (incoming_doc_id, target_status) ben canh existing Phase 33 indices (NOT replace). Apply 3x idempotent zero error.
       - [Phase 36]: Plan 36-02: Mirror Phase 35-02 2-worker pattern (tick concurrency=1 repeat 30s + event concurrency=5 retry 5x exp). Approach B duplicate workers/ (no backend import). Reuse Phase 34 error-codes.ts already in workers/.
       - [Phase 36]: Plan 36-03: 6 incoming-doc routes + 1 handling-doc complete hook + 1 GET /lgsp-status-history endpoint + server.ts wiring. Hook failure log warn NOT fail user action.
       - [Phase 36]: Plan 36-04: AntD v6 Timeline items prop. Polling 10s khi co pending only (saves bandwidth).
       - [Phase 36]: Plan 36-05: LGSP-STATUS-07 full SP rename + button label refactor DEFER Phase 37 (CONTEXT scope -- backend hook done Plan 36-03 sufficient). LGSP-STATUS-08 sender-side 13/15/16 DEFER Phase 37.
       ```

    6. Update Plans status under "### Phase 36" entry in v3.2 Phase Plan table (if exists).

    **Phần C: Commit + tag:**

    ```bash
    cd D:/ProjectAI/quanlyvanban
    git add .planning/phases/36-status-callback-chain-9-ma-qd-28/36-05-VERIFICATION-REPORT.md .planning/STATE.md
    git commit -m "docs(36-05): VERIFICATION-REPORT + mark Phase 36 COMPLETE — Status Callback Chain"

    # Optional tag for milestone tracking:
    # git tag v3.2-phase36-complete
    ```

    KHÔNG push (per CLAUDE.md — chỉ commit khi user yêu cầu explicit).
  </action>

  <verify>
    <automated>
      cd D:/ProjectAI/quanlyvanban && test -f .planning/phases/36-status-callback-chain-9-ma-qd-28/36-05-VERIFICATION-REPORT.md && grep -q "Status Callback Chain" .planning/phases/36-status-callback-chain-9-ma-qd-28/36-05-VERIFICATION-REPORT.md && grep -q "REQ Coverage Matrix" .planning/phases/36-status-callback-chain-9-ma-qd-28/36-05-VERIFICATION-REPORT.md && grep -q "Phase 36 COMPLETE" .planning/STATE.md && echo "REPORT + STATE UPDATED"
    </automated>
    <automated>
      cd D:/ProjectAI/quanlyvanban && git log --oneline -1 | grep -q "36-05" && echo "COMMIT EXISTS"
    </automated>
  </verify>

  <acceptance_criteria>
    - VERIFICATION-REPORT.md exists with 12 sections (per Phase 35-05 structure)
    - REQ Coverage Matrix lists STATUS-02..10 with PASS/PARTIAL/DEFER + evidence link
    - Caveat section documents credential rotation issue (if encountered)
    - LGSP-STATUS-07 + LGSP-STATUS-08 marked DEFER Phase 37 with rationale
    - STATE.md updated: completed_phases=4, completed_plans=20, percent=80
    - STATE.md adds 5 Phase 36 decision entries
    - Commit created với message `docs(36-05): VERIFICATION-REPORT + mark Phase 36 COMPLETE — Status Callback Chain`
    - Vietnamese diacritics correct in narrative prose
    - All Task 1-4 evidence linked in report (or inlined)
  </acceptance_criteria>

  <done>
    Phase 36 COMPLETE. VERIFICATION-REPORT.md committed. STATE.md updated. Ready for orchestrator to mark phase done + transition to Phase 37 (Admin UI + Catalog + Go-live).

    Commit: `docs(36-05): VERIFICATION-REPORT + mark Phase 36 COMPLETE — Status Callback Chain`
  </done>
</task>

</tasks>

<verification>
After all 5 tasks shipped:

1. **All Phase 36 files committed:**
   ```bash
   git log --oneline -30 | grep -E "^\w+ (feat|docs)\(36-" | wc -l
   # Expected: ~14 (5 Plan 36-01 + 5 Plan 36-02 + 3 Plan 36-03 + 3 Plan 36-04 + 1 Plan 36-05)
   ```

2. **VERIFICATION-REPORT exists + comprehensive:**
   - PASS/FAIL summary clear
   - REQ matrix complete (STATUS-02..10)
   - Caveats documented (credential rotation if hit)
   - Phase 37 handoff section actionable

3. **STATE.md reflects Phase 36 done:**
   - completed_phases=4
   - Phase 36 entry marked complete
   - Plans 5/5 done

4. **No DB regression:**
   - SP count >= 361
   - UNIQUE constraint persists
   - 4 Phase 33 SPs still callable
   - Apply schema idempotent 3x verified
</verification>

<success_criteria>
- 1 VERIFICATION-REPORT.md created (~200-400 dòng) + STATE.md updated
- All 9 REQ-IDs (STATUS-02..10) classified PASS/PARTIAL/DEFER với evidence
- E2E sandbox happy + dedup + error path tests executed with output captured
- Approach B audit pass (workers self-contained)
- Static verification (TS + build + schema idempotent + SP count) ALL pass
- Workers smoke test prints both Phase 36 workers started log lines
- 1 commit: `docs(36-05): VERIFICATION-REPORT + mark Phase 36 COMPLETE`
</success_criteria>

<output>
After completion, create `.planning/phases/36-status-callback-chain-9-ma-qd-28/36-05-SUMMARY.md` (separate from VERIFICATION-REPORT — SUMMARY is plan-execution summary, VERIFICATION-REPORT is the artifact):
- Frontmatter (phase, plan, subsystem=verification, tags, requirements all STATUS-02..10)
- dependency_graph (requires: 36-01/02/03/04 complete; provides: VERIFICATION-REPORT artifact)
- key_files (created: 36-05-VERIFICATION-REPORT.md, modified: STATE.md)
- decisions honored (D-16 verification gate, defer scope to Phase 37 documented)
- metrics (duration ~30m, all tests passed, tasks_completed=5, files_created=1, files_modified=1, commits=1)
- next_steps: Phase 37 starts — Admin UI + Catalog + Go-live cho 6 DN production
</output>
