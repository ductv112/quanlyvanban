---
phase: 36-status-callback-chain-9-ma-qd-28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/database/schema/000_schema_v3.0.sql
  - e_office_app_new/backend/src/services/lgsp-real.service.ts
  - e_office_app_new/backend/src/services/lgsp.service.ts
  - e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts
autonomous: true
requirements:
  - LGSP-STATUS-08   # Schema-ready for sender-side mã 13/15/16 (active wiring DEFERRED Phase 37)
  - LGSP-STATUS-09   # Worker retry mechanism — schema (UNIQUE) + repo + service contract foundation

must_haves:
  truths:
    - "Schema có UNIQUE constraint (incoming_doc_id, target_status) trên edoc.lgsp_status_outbox"
    - "Apply schema lần 2 zero error (idempotent)"
    - "LGSPRealService.updateStatus() gọi đúng POST {baseUrl}/v1/updateStatus JSON body + headers X-SystemId/X-SecretKey"
    - "lgsp-status-outbox.repository có insertEvent (catch SQLSTATE 23505 → null), getDocStatusHistory"
    - "ILgspService interface có signature updateStatus(docId, status) → Promise<{success, message, errorCode?}>"
    - "TypeScript backend tsc --noEmit exit 0"
  artifacts:
    - path: "e_office_app_new/database/schema/000_schema_v3.0.sql"
      contains: "uq_lgsp_status_outbox_doc_status"
    - path: "e_office_app_new/backend/src/services/lgsp-real.service.ts"
      contains: "/v1/updateStatus"
    - path: "e_office_app_new/backend/src/services/lgsp.service.ts"
      contains: "updateStatus("
    - path: "e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts"
      contains: "insertEvent"
  key_links:
    - from: "edoc.lgsp_status_outbox"
      to: "edoc.incoming_docs"
      via: "FK incoming_doc_id ON DELETE CASCADE (Phase 33) + UNIQUE (incoming_doc_id, target_status) Phase 36"
      pattern: "uq_lgsp_status_outbox_doc_status"
    - from: "lgspStatusOutboxRepository.insertEvent"
      to: "edoc.fn_lgsp_status_outbox_insert SP"
      via: "callFunctionOne — try/catch unique_violation → null"
      pattern: "23505|unique_violation"
    - from: "LGSPRealService.updateStatus"
      to: "{baseUrl}/v1/updateStatus"
      via: "POST JSON body {docId, status} + X-SystemId + X-SecretKey headers"
      pattern: "/v1/updateStatus"
---

<objective>
Wave 1 (foundation) cho Phase 36 — Status Callback Chain. Phase 33 đã có table `edoc.lgsp_status_outbox` (id, incoming_doc_id, target_status, payload, sent_status, retry_count, next_retry_at, error_message) + CHECK target_status IN ('01'..'16') + partial index pending + per-doc index, cộng với 4 SP (insert/get_pending/mark_sent/mark_error). Phase 36 plan này APPEND idempotent UNIQUE constraint `(incoming_doc_id, target_status)` để dedup phía DB, FIX `LGSPRealService.updateStatus()` Phase 18 (sai endpoint giống send/receive), thêm method `updateStatus()` vào `ILgspService` interface, và extend `lgspStatusOutboxRepository` với 2 method mới `insertEvent` (try/catch 23505 → null) + `getDocStatusHistory` (cho UI Timeline Phase 36-04). Sau plan này, các Wave 2 plan có sẵn schema dedup + service contract + repo helper để wire route hook + worker.

Purpose: Tách hạ tầng (schema + interface contract + repo + service fix) khỏi wiring (worker + route hook + UI) để Wave 2 chạy được song song. Pattern mirror Phase 35-01 (schema + LGSPRealService.receiveDocuments fix + repo createFromLgsp).

Output: Schema sẵn sàng dedup, LGSPRealService.updateStatus() đúng spec Postman, ILgspService interface mở rộng, repo có 2 method mới, TS backend clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-status-callback-chain-9-ma-qd-28/36-CONTEXT.md
@.planning/phases/35-receive-flow-cron-syncreceivededoclist/35-02-SUMMARY.md
@e_office_app_new/backend/src/services/lgsp-real.service.ts
@e_office_app_new/backend/src/services/lgsp.service.ts
@e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts
@e_office_app_new/database/schema/000_schema_v3.0.sql
@docs/Trục EDOC Lạng Sơn - QLVB Doanh nghiệp/LTVB_API_TRUC_PROD_TICHHOP.postman_collection.json
@CLAUDE.md

<interfaces>
<!-- Key types + DB SPs the executor needs. Extracted from codebase + master schema. -->
<!-- Executor must use these directly — no codebase exploration needed. -->

From `e_office_app_new/database/schema/000_schema_v3.0.sql` (Phase 33 existing — DO NOT modify, just APPEND UNIQUE):
```sql
-- Table (lines ~28430-28443) — đã có
CREATE TABLE IF NOT EXISTS edoc.lgsp_status_outbox (
  id               BIGSERIAL PRIMARY KEY,
  incoming_doc_id  BIGINT NOT NULL,
  target_status    VARCHAR(2) NOT NULL,
  payload          JSONB NOT NULL DEFAULT '{}'::jsonb,
  sent_status      VARCHAR(10) NOT NULL DEFAULT 'pending',
  sent_at          TIMESTAMPTZ,
  retry_count      INT NOT NULL DEFAULT 0,
  next_retry_at    TIMESTAMPTZ,
  error_message    TEXT,
  created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  CONSTRAINT chk_lgsp_status_outbox_target_status CHECK (target_status IN ('01','02','03','04','05','06','13','15','16')),
  CONSTRAINT chk_lgsp_status_outbox_sent_status CHECK (sent_status IN ('pending','success','error'))
);
-- FK ON DELETE CASCADE — đã có (DO block catch 4 SQLSTATE)
-- Index partial pending + index per-doc — đã có

-- Phase 33 SPs available (4):
edoc.fn_lgsp_status_outbox_insert(p_incoming_doc_id BIGINT, p_target_status VARCHAR, p_payload JSONB)
  RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT)
edoc.fn_lgsp_status_outbox_get_pending(p_limit INTEGER DEFAULT 10)
  RETURNS TABLE(id BIGINT, incoming_doc_id BIGINT, target_status VARCHAR, payload JSONB,
                retry_count INTEGER, next_retry_at TIMESTAMPTZ, created_at TIMESTAMPTZ)
edoc.fn_lgsp_status_outbox_mark_sent(p_id BIGINT, p_sent_at TIMESTAMPTZ DEFAULT NOW())
  RETURNS TABLE(success BOOLEAN, message TEXT)
edoc.fn_lgsp_status_outbox_mark_error(p_id BIGINT, p_error_message TEXT, p_next_retry_at TIMESTAMPTZ)
  RETURNS TABLE(success BOOLEAN, message TEXT)
```

From `e_office_app_new/backend/src/services/lgsp.service.ts` (existing — extend):
```typescript
export interface LgspSendResult {
  success: boolean;
  lgsp_doc_id: string;
  message: string;
  errorCode?: string;
}

export interface ILgspService {
  getToken(): Promise<string>;
  receiveDocuments(fromDateYmd: string, toDateYmd: string): Promise<LgspReceivedDocSummary[]>;
  getEdocById(docId: string): Promise<LgspReceivedDocFull | null>;
  sendDocument(edxmlBuffer: Buffer, destOrgCode: string, docCode: string): Promise<LgspSendResult>;
  syncOrganizations(): Promise<LgspOrganization[]>;
  // Phase 36: thêm updateStatus method
}

export async function getLgspService(
  unitId?: number,
  environment: 'sandbox' | 'prod' = 'prod',
): Promise<ILgspService>;
```

From `e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` (existing — extend):
```typescript
export type LgspTargetStatus = '01' | '02' | '03' | '04' | '05' | '06' | '13' | '15' | '16';

export interface LgspStatusOutboxPendingRow {
  id: number;
  incoming_doc_id: number;
  target_status: LgspTargetStatus;
  payload: Record<string, unknown>;
  retry_count: number;
  next_retry_at: string | null;
  created_at: string;
}

export const lgspStatusOutboxRepository = {
  insert(incomingDocId, targetStatus, payload): Promise<DbResultWithId> { ... },     // existing
  getPending(limit): Promise<LgspStatusOutboxPendingRow[]> { ... },                  // existing — for worker poll
  markSent(id, sentAt?): Promise<DbResult> { ... },                                  // existing
  markError(id, errorMessage, nextRetryAt): Promise<DbResult> { ... },               // existing
  // Phase 36: insertEvent (try/catch 23505 → null), getDocStatusHistory
};
```

From `docs/Trục EDOC Lạng Sơn - QLVB Doanh nghiệp/LTVB_API_TRUC_PROD_TICHHOP.postman_collection.json`:
- `POST {baseUrl}/v1/updateStatus`
- Headers: `Content-Type: application/json`, `X-SystemId`, `X-SecretKey`
- Body: `{ "docId": "<uuid>", "status": "<mã hoặc keyword>" }`
- Sample body: `{ "docId": "2ac3bb00-9469-4fdf-8f35-0a5f72348e4b", "status": "done" }`
- **Note:** Postman sample dùng `"done"` (keyword). Default Phase 36 dùng mã số `"03"`/`"04"`/.../`"06"` (chuẩn QĐ 28). Worker (Plan 36-02) verify thực tế — nếu LGSP reject mã số (errorCode 22), Plan 36-05 sẽ flag để switch sang keyword map (`done`/`processing`/`assigned`/`received`/`rejected`).

Mirror file Phase 18 PATTERN cho updateStatus (đã có trong lgsp-real.service.ts nhưng broken — check section 700+ hoặc grep "updateStatus"):
- Pattern fetch giống sendDocument (lines 208-299) nhưng JSON body + GET-like simple structure
- Pattern fetch giống receiveDocuments (lines 315-353) cho headers + parse JSON
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → LGSP `/v1/updateStatus` | Backend sends `{docId, status}` to external trục — `docId` comes from `lgsp_status_outbox.payload.lgsp_doc_id` originally stored from edXML MessageHeader.DocumentId (LGSP-controlled). `status` is hardcoded enum from CHECK constraint. No user-controlled data flows out. |
| LGSP response → backend | Response `{success, message}` parsed loosely. Error path returns `LgspSendError` with mapped code — Vietnamese message exposed via outbox.error_message column (read-only by admin Phase 37). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | T (Tampering) | `lgsp_status_outbox.target_status` write | mitigate | CHECK constraint enforces enum `('01'..'06','13','15','16')` at DB level (Phase 33). Phase 36 adds UNIQUE `(incoming_doc_id, target_status)` chặn double-fire khi user spam action. Repo `insertEvent` swallows SQLSTATE 23505 → null (idempotent, no exception leak). |
| T-36-02 | I (Information Disclosure) | LGSP credentials (secretKey) in logs | mitigate | LGSPRealService.updateStatus() uses `X-SecretKey` header — must NOT log header value. Use existing pattern from sendDocument (only `systemId ? 'OK' : 'EMPTY'` status string, never raw secret). |
| T-36-03 | D (DoS) | Outbox table unbounded growth on retry loops | accept | Worker (Plan 36-02) has max 5 retries + final sent_status='error'. Admin "Gửi lại" Phase 37 will reset. Outbox rows persist forever for audit — acceptable at 6-DN scale (~max 6 events × N docs/day). v3.3+ may add archival job. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: APPEND UNIQUE constraint vào master schema (idempotent)</name>
  <files>e_office_app_new/database/schema/000_schema_v3.0.sql</files>

  <read_first>
    1. Read `e_office_app_new/database/schema/000_schema_v3.0.sql` lines 28429-28477 (Phase 33 lgsp_status_outbox table + indexes block — context for where to APPEND).
    2. Read `e_office_app_new/database/schema/000_schema_v3.0.sql` lines 29020-29022 (tail — append AFTER last existing block before EOF; mirror Phase 35-04 append style).
    3. Read CLAUDE.md § "DB Migration Strategy" + § "Deploy Pitfalls" #5 (FK forward reference rule — UNIQUE constraint always safe AFTER table exists).
  </read_first>

  <action>
    APPEND vào CUỐI file `e_office_app_new/database/schema/000_schema_v3.0.sql` (sau dòng cuối Phase 35-04 RAISE NOTICE block) một section Phase 36 idempotent:

    ```sql
    -- ============================================================
    -- Phase 36 Plan 36-01: APPEND UNIQUE (incoming_doc_id, target_status) on lgsp_status_outbox
    -- REQ: LGSP-STATUS-09 (foundation cho worker dedup)
    -- CONTEXT D-04: idempotency via UNIQUE constraint thay vi SELECT-then-INSERT (race-safe)
    --
    -- Wrap DO block catch 4 SQLSTATE (CLAUDE.md DB Migration pattern):
    --   42710 duplicate_object, 42P07 duplicate_table,
    --   42P16 invalid_table_definition, 42701 duplicate_column
    -- Apply lan 2 phai zero error.
    -- ============================================================

    DO $$
    BEGIN
      BEGIN
        ALTER TABLE edoc.lgsp_status_outbox
          ADD CONSTRAINT uq_lgsp_status_outbox_doc_status
          UNIQUE (incoming_doc_id, target_status);
      EXCEPTION
        WHEN duplicate_object THEN NULL;
        WHEN duplicate_table THEN NULL;
        WHEN invalid_table_definition THEN NULL;
        WHEN duplicate_column THEN NULL;
        -- 23505 unique_violation khong xay ra o ADD CONSTRAINT (chi xay ra khi INSERT)
        -- 42P10 invalid_column_reference -- khong xay ra vi columns ton tai
      END;
    END $$;

    DO $$ BEGIN
      RAISE NOTICE 'Phase 36 schema: uq_lgsp_status_outbox_doc_status (incoming_doc_id, target_status) -- OK';
    END $$;
    ```

    Sau khi append, apply vào dev DB 2 lần để verify idempotent (theo CLAUDE.md):

    ```powershell
    # Lan 1: apply (constraint chua co se tao)
    docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - < e_office_app_new/database/schema/000_schema_v3.0.sql

    # Lan 2: apply lai (constraint da co -- DO block bat duplicate_object -> NULL -> zero error)
    docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - < e_office_app_new/database/schema/000_schema_v3.0.sql

    # Verify constraint exists:
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT con.conname FROM pg_constraint con JOIN pg_class cls ON cls.oid = con.conrelid WHERE cls.relname = 'lgsp_status_outbox' AND con.contype = 'u';"
    # Expected output line: uq_lgsp_status_outbox_doc_status

    # Verify SP count baseline (Phase 35-05 = 361 + 0 new = 361):
    docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM pg_proc WHERE pronamespace IN ('public'::regnamespace, 'edoc'::regnamespace) AND proname LIKE 'fn_%';"
    # Expected: >= 361
    ```

    KHÔNG sửa table definition Phase 33, KHÔNG drop/recreate SP. Chỉ ADD CONSTRAINT idempotent.

    KHÔNG tạo file migration rời `database/migrations/047_*.sql` — CLAUDE.md cấm.
  </action>

  <verify>
    <automated>
      cd D:/ProjectAI/quanlyvanban && (grep -q "uq_lgsp_status_outbox_doc_status" e_office_app_new/database/schema/000_schema_v3.0.sql && grep -q "Phase 36 Plan 36-01" e_office_app_new/database/schema/000_schema_v3.0.sql && grep -q "ALTER TABLE edoc.lgsp_status_outbox" e_office_app_new/database/schema/000_schema_v3.0.sql && echo "GREP OK") || (echo "GREP FAIL" && exit 1)
    </automated>
    <automated>
      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT con.conname FROM pg_constraint con JOIN pg_class cls ON cls.oid = con.conrelid WHERE cls.relname = 'lgsp_status_outbox' AND con.contype = 'u' AND con.conname = 'uq_lgsp_status_outbox_doc_status';" | grep -q "uq_lgsp_status_outbox_doc_status" && echo "CONSTRAINT EXISTS"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "uq_lgsp_status_outbox_doc_status" e_office_app_new/database/schema/000_schema_v3.0.sql` → exit 0
    - `grep -q "Phase 36 Plan 36-01" e_office_app_new/database/schema/000_schema_v3.0.sql` → exit 0
    - `grep -q "UNIQUE (incoming_doc_id, target_status)" e_office_app_new/database/schema/000_schema_v3.0.sql` → exit 0
    - `grep -q "EXCEPTION" e_office_app_new/database/schema/000_schema_v3.0.sql | wc -l` → count >= 1 trong section Phase 36 (DO block catch SQLSTATE)
    - psql constraint query trả về `uq_lgsp_status_outbox_doc_status` (constraint thực sự tồn tại sau apply)
    - Apply schema lần 2 zero error (NOTICE line "Phase 36 schema: ... -- OK" xuất hiện cả 2 lần, không có ERROR)
    - SP count baseline `>= 361` (KHÔNG drop SP nào)
  </acceptance_criteria>

  <done>
    Master schema chứa UNIQUE constraint `uq_lgsp_status_outbox_doc_status (incoming_doc_id, target_status)` wrapped trong DO block catch 4 SQLSTATE. Constraint thực sự tồn tại trong dev DB. Apply schema lần 2 zero error. Commit: `feat(36-01): them UNIQUE (incoming_doc_id, target_status) cho lgsp_status_outbox dedup`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix LGSPRealService.updateStatus() + thêm signature vào ILgspService interface</name>
  <files>e_office_app_new/backend/src/services/lgsp-real.service.ts, e_office_app_new/backend/src/services/lgsp.service.ts</files>

  <read_first>
    1. Read `e_office_app_new/backend/src/services/lgsp-real.service.ts` toàn bộ — section sendDocument (lines 208-299) làm pattern reference cho updateStatus (cùng fetch flow + LgspSendError throw + headers X-SystemId/X-SecretKey).
    2. Read `e_office_app_new/backend/src/services/lgsp-real.service.ts` section receiveDocuments (lines 315-353) làm pattern reference cho JSON parsing + headers Accept.
    3. Read `e_office_app_new/backend/src/services/lgsp.service.ts` lines 80-113 ILgspService interface — thêm method `updateStatus` signature.
    4. Read `docs/Trục EDOC Lạng Sơn - QLVB Doanh nghiệp/LTVB_API_TRUC_PROD_TICHHOP.postman_collection.json` — verify shape `/v1/updateStatus` (search "updateStatus" trong JSON).
    5. Read `e_office_app_new/backend/src/services/lgsp/error-codes.ts` (re-confirm LgspSendError class + mapLgspError + isLgspNonRetryableError exports).
  </read_first>

  <action>
    **Phần A: Edit `e_office_app_new/backend/src/services/lgsp.service.ts`** — Thêm `updateStatus(docId, status)` vào `ILgspService` interface (sau dòng `syncOrganizations()`):

    ```typescript
    /**
     * Phase 36: status callback ve LGSP truc (9 ma QD 28).
     * POST /v1/updateStatus voi JSON body {docId, status} + X-SystemId/X-SecretKey headers.
     *
     * @param docId LGSP doc UUID (lgsp_status_outbox.payload.lgsp_doc_id, original tu MessageHeader.DocumentId)
     * @param status target_status code per QD 28: '01'-'06', '13', '15', '16'
     * @returns LgspSendResult { success, lgsp_doc_id, message, errorCode? } -- worker xu ly retry per errorCode
     * @throws LgspSendError khi network/timeout/non-JSON response (no code -> retryable per D-10)
     */
    updateStatus(docId: string, status: string): Promise<LgspSendResult>;
    ```

    KHÔNG đổi tên type `LgspSendResult` — reuse vì shape giống nhau (success/message/errorCode). Comment trong service.ts giải thích rõ reuse.

    **Phần B: Edit `e_office_app_new/backend/src/services/lgsp-real.service.ts`** — Tìm method `updateStatus` cũ (Phase 18 broken — có thể grep `updateStatus` trong file). Replace toàn bộ method với implementation đúng spec Postman:

    ```typescript
    /**
     * Phase 36 (Plan 36-01): Fix Phase 18 BROKEN implementation (sai endpoint + body shape + auth).
     *
     * AUTHORITATIVE shape (Postman collection 06.updateStatus):
     *   POST {baseUrl}/v1/updateStatus
     *   Headers: Content-Type: application/json, X-SystemId, X-SecretKey
     *   Body (JSON): { "docId": "<uuid>", "status": "<ma QD 28 hoac keyword>" }
     *
     * Postman sample dung "status": "done" (keyword). Phase 36 default dung MA SO QD 28
     * ('02','03','04','05','06') — chuan ren tu spec QD 28/2018/QD-TTg. Neu LGSP reject ma so
     * (errorCode 22 — format khong dung), Plan 36-05 verification report flag de switch keyword map.
     *
     * Response variants (Postman):
     *   { success: true, message: 'OK' }  -- no docId, no errorCode echo back
     *   { success: false, message: 'Loi', data?: { errorCode, errorDesc } }
     *
     * Reuse LgspSendResult shape (lgsp.service.ts) — Phase 36 worker xu ly errorCode classify
     * retry vs no-retry qua isLgspNonRetryableError() (Phase 34 error-codes.ts shared).
     *
     * @param docId LGSP doc UUID (lgsp_status_outbox.payload.lgsp_doc_id)
     * @param status Target status code per QD 28
     * @returns LgspSendResult { success, lgsp_doc_id (=docId echo), message, errorCode }
     * @throws LgspSendError khi network/timeout/non-JSON response
     */
    async updateStatus(docId: string, status: string): Promise<LgspSendResult> {
      const sysId = this.systemId;
      const secret = this.secretKey;
      if (!sysId || !secret) {
        throw new LgspSendError(
          `LGSP credential missing for updateStatus: systemId=${sysId ? 'OK' : 'EMPTY'}, secretKey=${secret ? 'OK' : 'EMPTY'}`,
        );
      }

      const url = `${this.endpoint}/v1/updateStatus`;
      const bodyJson = JSON.stringify({ docId, status });

      const controller = new AbortController();
      const timer = setTimeout(() => controller.abort(), 30_000);
      try {
        const res = await fetch(url, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'X-SystemId': sysId,
            'X-SecretKey': secret,
            Accept: 'application/json',
          },
          body: bodyJson,
          signal: controller.signal,
        });

        const text = await res.text();
        let json: { success: boolean; message?: string; data?: { errorCode?: string; errorDesc?: string } };
        try {
          json = JSON.parse(text);
        } catch {
          throw new LgspSendError(
            `LGSP /v1/updateStatus HTTP ${res.status} non-JSON response: ${text.slice(0, 200)}`,
          );
        }

        const errorCode = json.data?.errorCode;
        const rawMessage = json.message || json.data?.errorDesc || 'unknown';

        if (!json.success) {
          // No-throw — return result de worker biet mark outbox error
          return {
            success: false,
            lgsp_doc_id: docId,
            message: mapLgspError(errorCode, rawMessage),
            errorCode: errorCode || undefined,
          };
        }

        return {
          success: true,
          lgsp_doc_id: docId,
          message: rawMessage,
          errorCode: '0',
        };
      } catch (err: unknown) {
        if (err instanceof LgspSendError) throw err;
        const msg = err instanceof Error ? err.message : String(err);
        throw new LgspSendError(`LGSP /v1/updateStatus network/timeout: ${msg}`);
      } finally {
        clearTimeout(timer);
      }
    }
    ```

    Place method AFTER `getEdocById` và TRƯỚC `syncOrganizations` (giữ thứ tự logic: send → receive → update → admin).

    **NEU lgsp-real.service.ts CHUA CO method `updateStatus`** (grep cho `updateStatus` trong file để confirm Phase 18 có hay không):
    - Nếu có method cũ broken — replace toàn bộ.
    - Nếu KHÔNG có — chỉ thêm mới method.
  </action>

  <verify>
    <automated>
      cd D:/ProjectAI/quanlyvanban/e_office_app_new/backend && npx tsc --noEmit && echo "BACKEND TS OK"
    </automated>
    <automated>
      cd D:/ProjectAI/quanlyvanban && grep -q "/v1/updateStatus" e_office_app_new/backend/src/services/lgsp-real.service.ts && grep -q "X-SystemId" e_office_app_new/backend/src/services/lgsp-real.service.ts && grep -q "updateStatus(docId" e_office_app_new/backend/src/services/lgsp.service.ts && echo "GREP OK"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "updateStatus(docId: string, status: string)" e_office_app_new/backend/src/services/lgsp.service.ts` → exit 0 (interface signature)
    - `grep -q "/v1/updateStatus" e_office_app_new/backend/src/services/lgsp-real.service.ts` → exit 0 (đúng endpoint)
    - `grep -c "X-SystemId" e_office_app_new/backend/src/services/lgsp-real.service.ts` → count >= 4 (sendDocument + receiveDocuments + getEdocById + updateStatus — verify Phase 36 thêm)
    - `grep -q "JSON.stringify({ docId, status })" e_office_app_new/backend/src/services/lgsp-real.service.ts` → exit 0 (JSON body shape)
    - `grep -q "throw new LgspSendError" e_office_app_new/backend/src/services/lgsp-real.service.ts` trong updateStatus method (verify error throw pattern reuse Phase 34)
    - `grep -q "mapLgspError(errorCode, rawMessage)" e_office_app_new/backend/src/services/lgsp-real.service.ts` → exit 0 (reuse error map)
    - NEGATIVE: `grep -q "/api/lgspedoc/update-status" e_office_app_new/backend/src/services/lgsp-real.service.ts` → exit 1 (broken Phase 18 endpoint REMOVED)
    - `cd e_office_app_new/backend && npx tsc --noEmit` → exit 0
  </acceptance_criteria>

  <done>
    `LGSPRealService.updateStatus(docId, status)` implement đúng spec Postman: POST `/v1/updateStatus` + JSON body `{docId, status}` + X-SystemId/X-SecretKey headers + 30s timeout + LgspSendError throw pattern. `ILgspService` interface có signature mới (executor's downstream worker dùng qua `(svc as ILgspService).updateStatus(docId, status)`). Backend TS clean. Commit: `feat(36-01): fix LGSPRealService.updateStatus per /v1/updateStatus + JSON body + headers`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend lgsp-status-outbox.repository với insertEvent + getDocStatusHistory</name>
  <files>e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts</files>

  <read_first>
    1. Read `e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` toàn bộ (94 dòng — Phase 33 baseline).
    2. Read `e_office_app_new/backend/src/lib/db/query.js` — confirm `callFunction` + `callFunctionOne` + `rawQuery` signatures (extension methods sẽ dùng).
    3. Read `e_office_app_new/backend/src/repositories/incoming-doc.repository.ts` line 530 (existing 23505 catch pattern qua SP message substring — Plan 35-01 dùng pattern này).
  </read_first>

  <action>
    Edit `e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` — KEEP nguyên 4 method Phase 33 (`insert`, `getPending`, `markSent`, `markError`) + ADD 2 method Phase 36:

    **1. Thêm Row interface `LgspStatusOutboxHistoryRow`** (sau interface `LgspStatusOutboxPendingRow`):

    ```typescript
    /**
     * Phase 36 (Plan 36-01): row cho UI Timeline "Lich su trang thai LGSP".
     * Tat ca cot phai match snake_case tu rawQuery SELECT statement ben duoi.
     */
    export interface LgspStatusOutboxHistoryRow {
      id: number;
      target_status: LgspTargetStatus;
      sent_status: 'pending' | 'success' | 'error';
      sent_at: string | null;
      retry_count: number;
      error_message: string | null;
      created_at: string;
    }
    ```

    **2. Thêm interface `InsertEventParams`** (gần đầu file, before repo):

    ```typescript
    /**
     * Phase 36 (Plan 36-01): named params cho insertEvent — clearer than positional.
     */
    export interface InsertEventParams {
      incoming_doc_id: number;
      target_status: LgspTargetStatus;
      payload: Record<string, unknown>;
    }
    ```

    **3. Thêm 2 method vào `lgspStatusOutboxRepository` object** (sau `markError`):

    ```typescript
    /**
     * Phase 36 (Plan 36-01): wrapper goi `edoc.fn_lgsp_status_outbox_insert` (Phase 33 SP) +
     * catch SQLSTATE 23505 unique_violation (do UNIQUE constraint Phase 36-01-Task-1).
     *
     * - Success -> tra ve {success: true, id: <BIGINT>}
     * - Unique violation (dedup) -> tra ve null + log info (caller xu ly NOOP, KHONG fail action)
     * - FK violation hoac other -> throw (caller log warn nhung KHONG fail user action -- D-03)
     *
     * UNIQUE constraint (incoming_doc_id, target_status) chan double-INSERT khi user spam
     * action (vd: bam "Tiep nhan" 2 lan nhanh). Idempotent — first action tao outbox, second
     * action no-op silently.
     */
    async insertEvent(params: InsertEventParams): Promise<DbResultWithId | null> {
      try {
        const row = await callFunctionOne<DbResultWithId>(
          'edoc.fn_lgsp_status_outbox_insert',
          [params.incoming_doc_id, params.target_status, params.payload],
        );
        if (!row) {
          return { success: false, message: 'Khong the tao outbox event', id: 0 };
        }
        // SP co the tra success=false (vd: check_violation, foreign_key_violation).
        // Pass-through cho caller xu ly.
        return row;
      } catch (err: unknown) {
        // pg driver throws DatabaseError with `code` field for SQLSTATE.
        const sqlState = (err as { code?: string } | undefined)?.code;
        if (sqlState === '23505') {
          // Unique violation -- dedup. Caller skip silently, retorna null.
          return null;
        }
        // 23503 FK violation, 23502 not-null, vv -- let caller decide.
        throw err;
      }
    },

    /**
     * Phase 36 (Plan 36-01): query history cua 1 incoming_doc cho UI Timeline (Plan 36-04).
     *
     * Returns ALL outbox events cho 1 doc, ORDER BY created_at ASC (chronological).
     * Includes pending + success + error rows (UI render khac mau per sent_status).
     *
     * Dung rawQuery thay vi SP rieng -- query don gian, khong can SP overhead.
     * Backend Plan 36-03 expose qua GET /api/van-ban-den/:id/lgsp-status-history.
     */
    async getDocStatusHistory(incomingDocId: number): Promise<LgspStatusOutboxHistoryRow[]> {
      return rawQuery<LgspStatusOutboxHistoryRow>(
        `SELECT id,
                target_status,
                sent_status,
                sent_at,
                retry_count,
                error_message,
                created_at
           FROM edoc.lgsp_status_outbox
          WHERE incoming_doc_id = $1
          ORDER BY created_at ASC`,
        [incomingDocId],
      );
    },
    ```

    **4. Update imports** ở đầu file — thêm `rawQuery`:

    ```typescript
    import { callFunction, callFunctionOne, rawQuery } from '../lib/db/query.js';
    ```

    KHÔNG đổi tên method Phase 33 cũ. KHÔNG đổi tên export `lgspStatusOutboxRepository`. KHÔNG thêm cache (D-14: per-attempt fresh load applies to credentials, not repo).
  </action>

  <verify>
    <automated>
      cd D:/ProjectAI/quanlyvanban/e_office_app_new/backend && npx tsc --noEmit && echo "BACKEND TS OK"
    </automated>
    <automated>
      cd D:/ProjectAI/quanlyvanban && grep -q "insertEvent" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts && grep -q "getDocStatusHistory" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts && grep -q "23505" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts && grep -q "rawQuery" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts && echo "GREP OK"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "async insertEvent(params: InsertEventParams)" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` → exit 0
    - `grep -q "async getDocStatusHistory(incomingDocId: number)" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` → exit 0
    - `grep -q "sqlState === '23505'" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` → exit 0 (catch unique violation)
    - `grep -q "ORDER BY created_at ASC" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` → exit 0 (history chronological)
    - `grep -q "export interface LgspStatusOutboxHistoryRow" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` → exit 0
    - `grep -q "export interface InsertEventParams" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` → exit 0
    - `grep -q "import { callFunction, callFunctionOne, rawQuery } from" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` → exit 0 (rawQuery import added)
    - Repo still exports the 4 Phase 33 methods: `grep -c "async insert\|async getPending\|async markSent\|async markError" e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` → count >= 4
    - `cd e_office_app_new/backend && npx tsc --noEmit` → exit 0
  </acceptance_criteria>

  <done>
    Repo extends Phase 33 baseline với `insertEvent({incoming_doc_id, target_status, payload})` (catch SQLSTATE 23505 → null silent dedup) + `getDocStatusHistory(docId)` (chronological ORDER BY created_at ASC cho UI Timeline). All 4 Phase 33 methods unchanged. TS backend clean. Commit: `feat(36-01): extend lgsp-status-outbox repo voi insertEvent (23505 dedup) + getDocStatusHistory`.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks shipped:

1. **Schema verification:**
   - `grep -q "uq_lgsp_status_outbox_doc_status" e_office_app_new/database/schema/000_schema_v3.0.sql` exit 0
   - Apply master schema 2 lần liên tiếp zero error (idempotent verified)
   - psql query constraint trả về `uq_lgsp_status_outbox_doc_status`
   - SP count baseline `>= 361` (KHÔNG drop SP nào)

2. **LGSPRealService.updateStatus verification:**
   - `grep -q "/v1/updateStatus" lgsp-real.service.ts` exit 0
   - `grep -q "JSON.stringify({ docId, status })" lgsp-real.service.ts` exit 0
   - Negative: `grep -q "/api/lgspedoc/update-status" lgsp-real.service.ts` exit 1
   - 4 endpoints có X-SystemId headers (send + receive + getEdoc + updateStatus)

3. **Repo extensions verification:**
   - `insertEvent` + `getDocStatusHistory` exported
   - SQLSTATE 23505 catch handler present
   - `rawQuery` imported
   - 4 Phase 33 methods unchanged

4. **TypeScript:**
   - `cd e_office_app_new/backend && npx tsc --noEmit` exit 0

5. **Smoke test SP signature compatibility:**
   ```bash
   docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc \
     "SELECT pg_get_function_arguments(oid) FROM pg_proc WHERE proname = 'fn_lgsp_status_outbox_insert' AND pronamespace = 'edoc'::regnamespace;"
   # Expected: p_incoming_doc_id bigint, p_target_status character varying, p_payload jsonb
   ```
</verification>

<success_criteria>
- Schema APPEND idempotent + verified 2 lần apply zero error
- UNIQUE constraint thực sự tồn tại trong dev DB
- LGSPRealService.updateStatus() implements per Postman authoritative
- ILgspService interface includes updateStatus signature
- Repo extended với insertEvent (SQLSTATE 23505 dedup) + getDocStatusHistory (chronological)
- Backend TypeScript strict mode clean
- 3 commits with feat(36-01): prefix
- KHÔNG drop SP nào, KHÔNG sửa Phase 33 table definition, KHÔNG tạo migration file rời
</success_criteria>

<output>
After completion, create `.planning/phases/36-status-callback-chain-9-ma-qd-28/36-01-SUMMARY.md` with:
- Frontmatter (phase, plan, subsystem=schema+backend, tags, requirements)
- dependency_graph (provides: UNIQUE constraint, updateStatus method, insertEvent/getDocStatusHistory repo methods)
- key_files (modified: 4 files)
- decisions honored (D-04, D-08, D-13)
- metrics (duration, ts_errors_backend=0, schema_apply_2x_zero_error, acceptance_grep_checks_passed)
- next_steps: Plan 36-02 (worker infra) can now consume `lgspStatusOutboxRepository.insertEvent/getDocStatusHistory` + worker can implement updateStatus call via ILgspService contract
</output>
