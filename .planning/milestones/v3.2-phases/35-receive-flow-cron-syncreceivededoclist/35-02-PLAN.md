---
phase: 35-receive-flow-cron-syncreceivededoclist
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - e_office_app_new/workers/package.json
  - e_office_app_new/workers/src/queues/lgsp-receive-queue.ts
  - e_office_app_new/workers/src/jobs/lgsp-receive-tick-worker.ts
  - e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts
  - e_office_app_new/workers/src/lgsp/edxml-parser.ts
  - e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts
  - e_office_app_new/workers/src/index.ts
  - e_office_app_new/backend/src/lib/queue/lgsp-receive-queue.ts
autonomous: true
requirements:
  - LGSP-RECV-01
  - LGSP-RECV-02
  - LGSP-RECV-03
  - LGSP-RECV-04
  - LGSP-RECV-05
  - LGSP-RECV-06
  - LGSP-RECV-07
tags: [worker, bullmq, lgsp, cron, parser, phase-35]

must_haves:
  truths:
    - "BullMQ queue 'lgsp-receive' exists with 2 distinct job names: 'receive-tick' (concurrency=1) + 'receive-dn' (concurrency=3)"
    - "Tick worker handler queries lgspAgencyConfigRepository.getAllActive() (or inline SP call) → enqueues 1 'receive-dn' job per active DN, then completes"
    - "DN worker handler: loadCredentials → fromDate = COALESCE(last_synced_at, NOW()-7d) formatted YYYY/MM/DD → call /v1/syncReceivedEdocList → for each docId not yet in DB call /v1/getEdoc → parseEdxml → autoRegisterFromLgsp(sender) → createFromLgsp → upload attachments to MinIO key 'lgsp/<docId>/<fileName>' → createLgspAttachment → INSERT lgsp_status_outbox row target_status='01' sent_status='pending' → UPDATE lgsp_agency_config.last_synced_at=NOW(), last_sync_error=NULL"
    - "DN worker per-attempt error → UPDATE last_sync_error (last_synced_at unchanged) + throw to trigger BullMQ retry (3 attempts exponential 30s/60s/120s)"
    - "Dedup: createFromLgsp returns skipped=true on duplicate → worker logs INFO + continues to next doc (NOT throw)"
    - "Attachment >50MB → skip + log warn (LGSP spec limit + RAM safety per D-07)"
    - "Backend producer module exports `getLgspReceiveQueue()` + `enqueueReceiveTick()` + `registerReceiveTickRepeatJob()` + `closeLgspReceiveQueue()`"
    - "Worker module compiles standalone (npx tsc --noEmit exit 0)"
    - "workers/src/index.ts starts both tick + dn workers + adds them to SIGTERM cleanup chain"
  artifacts:
    - path: "e_office_app_new/workers/src/queues/lgsp-receive-queue.ts"
      provides: "Queue name + job name constants + LgspReceiveDnJobData interface + 30s/3-attempt retry config"
      min_lines: 50
    - path: "e_office_app_new/workers/src/jobs/lgsp-receive-tick-worker.ts"
      provides: "Tick handler: query active DNs → enqueue 1 receive-dn job per DN"
      min_lines: 80
    - path: "e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts"
      provides: "DN handler: full sync per single DN (credentials → list → getEdoc loop → parse → DB + MinIO + outbox → update last_synced_at)"
      min_lines: 300
    - path: "e_office_app_new/workers/src/lgsp/edxml-parser.ts"
      provides: "Duplicated parser (Approach B per Phase 34 D-15)"
      min_lines: 80
    - path: "e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts"
      provides: "Worker-context LGSP HTTP client: loadLgspCredentials + syncReceivedList + getEdocFull (inline pg pgp_sym_decrypt + native fetch + X-SystemId/X-SecretKey headers)"
      min_lines: 200
    - path: "e_office_app_new/backend/src/lib/queue/lgsp-receive-queue.ts"
      provides: "Backend producer-side: getLgspReceiveQueue + enqueueReceiveTick + registerReceiveTickRepeatJob + closeLgspReceiveQueue"
      min_lines: 100
    - path: "e_office_app_new/workers/src/index.ts"
      provides: "Wire startLgspReceiveTickWorker + startLgspReceiveDnWorker + SIGTERM cleanup"
  key_links:
    - from: "workers/src/jobs/lgsp-receive-tick-worker.ts handler"
      to: "workers/src/queues/lgsp-receive-queue.ts.LGSP_RECEIVE_QUEUE_NAME + enqueue 'receive-dn'"
      via: "new Queue(LGSP_RECEIVE_QUEUE_NAME).add('receive-dn', { unit_id, environment }, { attempts: 3, backoff: exponential 30000 })"
      pattern: "receive-dn"
    - from: "workers/src/jobs/lgsp-receive-dn-worker.ts handler"
      to: "workers/src/lgsp/lgsp-receive-service.ts syncReceivedList + getEdocFull"
      via: "fetch via X-SystemId + X-SecretKey headers"
      pattern: "syncReceivedEdocList"
    - from: "DN handler"
      to: "workers/src/lgsp/edxml-parser.ts parseEdxml"
      via: "function call after getEdocFull returns edxml string"
      pattern: "parseEdxml"
    - from: "DN handler MinIO upload"
      to: "MinIO bucket 'documents' key 'lgsp/<docId>/<fileName>'"
      via: "minioClient.putObject"
      pattern: "lgsp/"
    - from: "DN handler"
      to: "edoc.fn_lgsp_status_outbox_insert SP"
      via: "rawQuery with (incoming_doc_id, '01', payload jsonb)"
      pattern: "fn_lgsp_status_outbox_insert"
    - from: "backend/src/lib/queue/lgsp-receive-queue.ts registerReceiveTickRepeatJob"
      to: "BullMQ Queue.add with repeat: { every: 300000 }"
      via: "Singleton jobId 'lgsp-receive-tick-singleton' to prevent duplicate scheduler"
      pattern: "every: 300000"
---

<objective>
Wave 2 (Plan 35-02 of 5) — BullMQ infrastructure: 2-tier worker system mirroring Phase 34 Approach B (self-contained, duplicated modules).

Create the `lgsp-receive` queue with 2 job names:
- `'receive-tick'` (concurrency=1, retry=1) — fired every 5 min by repeat scheduler OR by Plan 35-03 manual route. Handler queries `lgsp_agency_config.is_active=TRUE` → enqueues 1 child job per DN.
- `'receive-dn'` (concurrency=3, retry=3 exponential 30s/60s/120s) — handles 1 DN end-to-end: credential lookup → sync list → loop getEdoc → parse → create incoming_doc (with dedup) → upload attachments to MinIO → INSERT outbox status='01' for Phase 36 → UPDATE last_synced_at.

Backend producer module exports `getLgspReceiveQueue()`, `enqueueReceiveTick()`, `registerReceiveTickRepeatJob()` (called by Plan 35-03 from server.ts startup), and `closeLgspReceiveQueue()` (called from SIGTERM chain in server.ts).

Per Phase 34 Approach B (ratified — see SUMMARY 34-02 "Decision: APPROACH B"): the worker is **self-contained**. Duplicate `edxml-parser.ts` from `backend/src/services/lgsp/` (Plan 35-01) into `workers/src/lgsp/`. Create a NEW worker-context `lgsp-receive-service.ts` that does inline `pg pgp_sym_decrypt` credential loading + native fetch with X-SystemId/X-SecretKey headers (no backend imports — clean tsconfig rootDir).

Purpose: Plan 35-03 wires the producer queue + repeat job registration into server startup. Plan 35-04 (frontend) shows the data this worker produces. Plan 35-05 (verification) runs E2E.

Output: 7 new files (1 backend queue + 2 worker job handlers + 1 queue + 2 worker-local lgsp modules + workers/index.ts edit). `cd workers && npx tsc --noEmit` and `cd backend && npx tsc --noEmit` both exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/35-receive-flow-cron-syncreceivededoclist/35-CONTEXT.md
@.planning/phases/35-receive-flow-cron-syncreceivededoclist/35-01-PLAN.md
@.planning/phases/34-send-flow-sendedoc/34-02-SUMMARY.md
@e_office_app_new/workers/src/queues/lgsp-send-queue.ts
@e_office_app_new/workers/src/jobs/lgsp-send-worker.ts
@e_office_app_new/workers/src/lgsp/lgsp-send-service.ts
@e_office_app_new/workers/src/index.ts
@e_office_app_new/backend/src/lib/queue/lgsp-send-queue.ts
@e_office_app_new/backend/src/repositories/lgsp-agency-config.repository.ts
@CLAUDE.md

<interfaces>
<!-- Authoritative shapes from Phase 35-01 (Plan 35-01 outputs that Plan 35-02 consumes) + Phase 33 + existing schema -->

From `workers/src/queues/lgsp-receive-queue.ts` (CREATE in this plan — see Task 1):
  export const LGSP_RECEIVE_QUEUE_NAME = 'lgsp-receive';
  export const LGSP_RECEIVE_TICK_JOB_NAME = 'receive-tick';
  export const LGSP_RECEIVE_DN_JOB_NAME = 'receive-dn';
  export const LGSP_RECEIVE_TICK_CONCURRENCY = 1;
  export const LGSP_RECEIVE_DN_CONCURRENCY = 3;       // CONTEXT D-04
  export const LGSP_RECEIVE_DN_MAX_ATTEMPTS = 3;      // CONTEXT D-11
  export const LGSP_RECEIVE_DN_BACKOFF_DELAY = 30_000; // 30s -> 60s -> 120s exponential
  export const LGSP_RECEIVE_TICK_INTERVAL_MS = 5 * 60 * 1000; // 5 minutes
  export const LGSP_RECEIVE_TICK_JOB_ID = 'lgsp-receive-tick-singleton';
  export interface LgspReceiveTickJobData {}  // empty marker
  export interface LgspReceiveDnJobData { unit_id: number; environment: 'sandbox' | 'prod'; }

From Plan 35-01 (already exists when this plan runs):
  - `backend/src/services/lgsp/edxml-parser.ts` exports `parseEdxml(xml)` + ParsedEdxml type
    → This plan COPIES this file to `workers/src/lgsp/edxml-parser.ts` (Approach B)
  - `backend/src/repositories/incoming-doc.repository.ts` exports `createFromLgsp` + `createLgspAttachment`
    → Worker does NOT import these (Approach B). Instead, calls SP `edoc.fn_incoming_doc_create` directly via pg pool.
  - `backend/src/repositories/inter-organization.repository.ts` exports `autoRegisterFromLgsp`
    → Worker does NOT import. Inlines INSERT ... ON CONFLICT (code) DO NOTHING in worker.

From Phase 33 SPs (already in schema/000_schema_v3.0.sql):
  - `edoc.fn_lgsp_agency_config_get_all_active(environment varchar?)`
    Returns: TABLE(id bigint, unit_id integer, environment varchar, system_id varchar,
                   secret_key_encrypted bytea, base_url varchar, last_synced_at timestamptz)
  - `edoc.fn_lgsp_agency_config_update_last_synced(p_unit_id integer, p_environment varchar, p_last_synced_at timestamptz, p_error text)`
    Returns: TABLE(success boolean, message text)
  - `edoc.fn_lgsp_status_outbox_insert(p_incoming_doc_id bigint, p_target_status varchar(2), p_payload jsonb)`
    Returns: TABLE(success boolean, message text, id bigint)

From Phase 33 + 34 reference (Approach B pattern proven):
  - `workers/src/lgsp/lgsp-send-service.ts` has function `loadLgspCredentials(pool, unitId, env, signingSecretKey)`
    Implementation: SQL `SELECT system_id, base_url, pgp_sym_decrypt(secret_key_encrypted, $signing_secret_key)::text AS secret_key FROM edoc.lgsp_agency_config WHERE unit_id=$1 AND environment=$2 AND is_active=TRUE`
    → Plan 35-02 reuses the IDENTICAL pattern for the receive service.

LGSP API endpoints (per Postman, mirrored from Plan 35-01):
  - GET {baseUrl}/v1/syncReceivedEdocList?messageType=edoc&fromDate=YYYY/MM/DD&toDate=YYYY/MM/DD
    Headers: X-SystemId, X-SecretKey
    Response: { success, count, data: [{ docId, from, to, status, statusDesc, createdTime, updatedTime }] }
  - GET {baseUrl}/v1/getEdoc?docId=<uuid>
    Headers: X-SystemId, X-SecretKey
    Response: { success, data: { docId, from, fromName?, edocCode?, edocAbstract?, edxml: string, attachments?: [{ fileName, fileContent (base64) }] } }

MinIO client (workers can import 'minio' lib directly — already added as dep in Phase 34-02):
  import { Client as MinioClient } from 'minio';
  const minioClient = new MinioClient({
    endPoint: process.env.MINIO_ENDPOINT || 'localhost',
    port: Number(process.env.MINIO_PORT) || 9000,
    useSSL: process.env.MINIO_USE_SSL === 'true',
    accessKey: process.env.MINIO_ACCESS_KEY!,
    secretKey: process.env.MINIO_SECRET_KEY!,
  });
  await minioClient.putObject('documents', objectKey, buffer, buffer.length, { 'Content-Type': mimeType });

attachments table (schema/000_schema_v3.0.sql — pre-existing):
  - id BIGINT PK, outgoing_doc_id BIGINT NULL, incoming_doc_id BIGINT NULL,
    file_name VARCHAR(500), file_path VARCHAR(1000), file_size BIGINT,
    content_type VARCHAR(200), uploaded_by INTEGER, created_at TIMESTAMPTZ
  Worker INSERTs raw (no SP needed — same approach as Phase 34 sendWorker used for reading attachments).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create workers/src/queues/lgsp-receive-queue.ts + duplicate edxml-parser.ts + add fast-xml-parser dep to workers</name>
  <files>
    e_office_app_new/workers/package.json,
    e_office_app_new/workers/src/queues/lgsp-receive-queue.ts,
    e_office_app_new/workers/src/lgsp/edxml-parser.ts
  </files>
  <read_first>
    - e_office_app_new/workers/src/queues/lgsp-send-queue.ts (entire file — mirror exactly its constant + interface pattern)
    - e_office_app_new/workers/package.json (current deps — confirm fast-xml-parser NOT yet present)
    - e_office_app_new/backend/src/services/lgsp/edxml-parser.ts (Plan 35-01 created — this file gets COPIED verbatim to workers/src/lgsp/)
    - .planning/phases/34-send-flow-sendedoc/34-02-SUMMARY.md "APPROACH B chosen" decision — Plan 35-02 follows same pattern
  </read_first>
  <action>
    **A. Add `fast-xml-parser` dependency to `workers/package.json`:**

    Add to `"dependencies"` (alphabetical, after `bullmq`):
    ```json
    "fast-xml-parser": "^4.4.0"
    ```

    Then run `cd e_office_app_new/workers && NODE_ENV=development npm install`. This updates workers/package-lock.json + downloads the package locally (already in backend node_modules but workers is a separate module per Approach B).

    **B. Create `e_office_app_new/workers/src/queues/lgsp-receive-queue.ts` (~60 lines):**

    ```ts
    // ============================================================
    // LGSP Receive Queue constants + job data types - Phase 35 Plan 35-02
    // REQ: LGSP-RECV-01
    // CONTEXT D-01 (BullMQ repeat 5min), D-02 (tick + dn split), D-04 (concurrencies),
    //         D-11 (DN retry 3x exponential)
    // Mirror pattern: workers/src/queues/lgsp-send-queue.ts (Phase 34)
    // ============================================================

    export const LGSP_RECEIVE_QUEUE_NAME = 'lgsp-receive';

    /** Parent tick job — fires every 5 min OR manual via /api/lgsp/sync-now. Spawns N child receive-dn jobs. */
    export const LGSP_RECEIVE_TICK_JOB_NAME = 'receive-tick';

    /** Child job — 1 DN end-to-end sync (list + getEdoc loop + INSERT + MinIO + outbox). */
    export const LGSP_RECEIVE_DN_JOB_NAME = 'receive-dn';

    /** Concurrency: only 1 tick at a time to avoid race when cron + manual trigger fire close together. */
    export const LGSP_RECEIVE_TICK_CONCURRENCY = 1;

    /** Concurrency: 3 DNs sync in parallel (D-04 — mirror Phase 34 send worker; avoid spamming LGSP). */
    export const LGSP_RECEIVE_DN_CONCURRENCY = 3;

    /** Tick rarely fails (DB query only). attempts=1 means no retry — next scheduled tick covers it. */
    export const LGSP_RECEIVE_TICK_MAX_ATTEMPTS = 1;

    /** DN retry: 3 attempts exponential 30s/60s/120s (D-11). Resume from last_synced_at on next tick. */
    export const LGSP_RECEIVE_DN_MAX_ATTEMPTS = 3;
    export const LGSP_RECEIVE_DN_BACKOFF_DELAY = 30_000;

    /** Repeat interval — every 5 minutes (D-01). */
    export const LGSP_RECEIVE_TICK_INTERVAL_MS = 5 * 60 * 1000;

    /** Deterministic jobId for the repeat scheduler — BullMQ uses this to prevent duplicate cron schedulers. */
    export const LGSP_RECEIVE_TICK_REPEAT_JOB_ID = 'lgsp-receive-tick-singleton';

    /** Per-DN job timeout — list (30s) + N x getEdoc (60s each, max 50 docs) + uploads = generous 10 min cap. */
    export const LGSP_RECEIVE_DN_JOB_TIMEOUT_MS = 10 * 60 * 1000;

    /** Tick job carries no data — it just triggers the fan-out. */
    export interface LgspReceiveTickJobData {
      /** Optional: who triggered (cron / manual route). Reserved for observability. */
      trigger_source?: 'cron' | 'manual';
      /** Optional: caller staffId when manual route fires. */
      triggered_by_staff_id?: number;
    }

    /** DN job — 1 active LGSP agency config row to sync. */
    export interface LgspReceiveDnJobData {
      unit_id: number;
      environment: 'sandbox' | 'prod';
    }
    ```

    **C. Duplicate `edxml-parser.ts` from backend to workers (Approach B):**

    Copy `e_office_app_new/backend/src/services/lgsp/edxml-parser.ts` → `e_office_app_new/workers/src/lgsp/edxml-parser.ts`.

    Use `cp` (or PowerShell `Copy-Item`):
    ```powershell
    Copy-Item -Path e_office_app_new/backend/src/services/lgsp/edxml-parser.ts -Destination e_office_app_new/workers/src/lgsp/edxml-parser.ts
    ```

    Add a comment block at the TOP indicating the duplicate:
    ```ts
    // ============================================================
    // edXML Parser — DUPLICATED from backend/src/services/lgsp/edxml-parser.ts
    // Approach B (Phase 34 ratified): worker self-contained, no backend imports.
    // ⚠️ MUST stay in sync with backend version. Plan 35-05 verification audit checksum.
    // ============================================================
    ```

    Verify: `cd e_office_app_new/workers && npx tsc --noEmit` exits 0 (after Task 2 finishes — for now, just confirm file exists).
  </action>
  <verify>
    <automated>grep -q "fast-xml-parser" e_office_app_new/workers/package.json &amp;&amp; grep -q "LGSP_RECEIVE_QUEUE_NAME" e_office_app_new/workers/src/queues/lgsp-receive-queue.ts &amp;&amp; test -f e_office_app_new/workers/src/lgsp/edxml-parser.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"fast-xml-parser"' e_office_app_new/workers/package.json` → exit 0
    - `grep -q "fast-xml-parser" e_office_app_new/workers/package-lock.json` → exit 0 (npm install ran)
    - `grep -q "LGSP_RECEIVE_QUEUE_NAME = 'lgsp-receive'" e_office_app_new/workers/src/queues/lgsp-receive-queue.ts` → exit 0
    - `grep -q "LGSP_RECEIVE_TICK_JOB_NAME = 'receive-tick'" e_office_app_new/workers/src/queues/lgsp-receive-queue.ts` → exit 0
    - `grep -q "LGSP_RECEIVE_DN_JOB_NAME = 'receive-dn'" e_office_app_new/workers/src/queues/lgsp-receive-queue.ts` → exit 0
    - `grep -q "LGSP_RECEIVE_DN_CONCURRENCY = 3" e_office_app_new/workers/src/queues/lgsp-receive-queue.ts` → exit 0
    - `grep -q "LGSP_RECEIVE_DN_MAX_ATTEMPTS = 3" e_office_app_new/workers/src/queues/lgsp-receive-queue.ts` → exit 0
    - `grep -q "LGSP_RECEIVE_TICK_INTERVAL_MS = 5 \* 60 \* 1000" e_office_app_new/workers/src/queues/lgsp-receive-queue.ts` → exit 0
    - `grep -q "interface LgspReceiveDnJobData" e_office_app_new/workers/src/queues/lgsp-receive-queue.ts` → exit 0
    - `test -f e_office_app_new/workers/src/lgsp/edxml-parser.ts` → exit 0
    - `grep -q "export function parseEdxml" e_office_app_new/workers/src/lgsp/edxml-parser.ts` → exit 0
    - `grep -q "DUPLICATED from backend" e_office_app_new/workers/src/lgsp/edxml-parser.ts` → exit 0
  </acceptance_criteria>
  <done>
    Queue constants file mirrors Phase 34 pattern. Parser duplicated to workers per Approach B with sync warning comment. fast-xml-parser installed in workers module.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create workers/src/lgsp/lgsp-receive-service.ts (credential load + 2 fetch helpers)</name>
  <files>
    e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts
  </files>
  <read_first>
    - e_office_app_new/workers/src/lgsp/lgsp-send-service.ts (entire ~216 lines — mirror its structure: loadLgspCredentials inline pgp_sym_decrypt, sendDocument via native fetch + headers; this plan adds syncReceivedList + getEdocFull functions same way)
    - .planning/phases/34-send-flow-sendedoc/34-02-SUMMARY.md "Approach B" section + decisions D-14
    - .planning/phases/35-receive-flow-cron-syncreceivededoclist/35-CONTEXT.md D-10 (date format YYYY/MM/DD, fromDate fallback NOW-7d) + D-14 (per-attempt credential fresh-load)
  </read_first>
  <action>
    Create `e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts` (~250 lines):

    ```ts
    // ============================================================
    // LGSP Receive Service — worker-context HTTP client - Phase 35 Plan 35-02
    // REQ: LGSP-RECV-02, LGSP-RECV-03
    // Mirror: workers/src/lgsp/lgsp-send-service.ts (Phase 34 Approach B)
    // - Inline pg pgp_sym_decrypt for credential load (no backend imports)
    // - Native fetch + AbortController + X-SystemId/X-SecretKey headers
    // - Per-attempt fresh credential load (CONTEXT D-14)
    // ============================================================
    import type { Pool } from 'pg';
    import pino from 'pino';

    const logger = pino({ name: 'lgsp-receive-service' });

    export interface WorkerLgspCredentials {
      baseUrl: string;
      systemId: string;
      secretKey: string;
      lastSyncedAt: string | null;
    }

    export interface LgspReceivedSummary {
      lgsp_doc_id: string;
      from_org_code: string;
      to_org_code: string;
      status: string;
      status_desc: string;
      created_time: string;
      updated_time: string;
    }

    export interface LgspReceivedFullAttachment {
      file_name: string;
      file_content_base64: string;
    }

    export interface LgspReceivedFull {
      lgsp_doc_id: string;
      sender_org_code: string;
      sender_org_name: string;
      edoc_code: string;
      edoc_abstract: string;
      edxml: string;
      attachments: LgspReceivedFullAttachment[];
    }

    /**
     * Load LGSP credential for a single (unit_id, environment) row. Always reads fresh from DB
     * (per CONTEXT D-14 — credential rotation pickup without restart).
     *
     * Throws if row not found or is_active=false (caller's job to surface as no-op).
     */
    export async function loadLgspCredentials(
      pool: Pool,
      unitId: number,
      environment: 'sandbox' | 'prod',
      signingSecretKey: string,
    ): Promise<WorkerLgspCredentials> {
      const rs = await pool.query<{
        system_id: string;
        base_url: string;
        secret_key: string;
        last_synced_at: string | null;
      }>(
        `SELECT system_id,
                base_url,
                pgp_sym_decrypt(secret_key_encrypted, $3)::text AS secret_key,
                last_synced_at
           FROM edoc.lgsp_agency_config
          WHERE unit_id = $1
            AND environment = $2
            AND is_active = TRUE
          LIMIT 1`,
        [unitId, environment, signingSecretKey],
      );
      if (rs.rowCount === 0) {
        throw new Error(`LGSP credential not found or inactive: unit_id=${unitId}, env=${environment}`);
      }
      const row = rs.rows[0];
      return {
        baseUrl: row.base_url.replace(/\/$/, ''),
        systemId: row.system_id,
        secretKey: row.secret_key,
        lastSyncedAt: row.last_synced_at,
      };
    }

    /** Format Date to YYYY/MM/DD (LGSP-required per CONTEXT D-10). */
    export function formatLgspDate(d: Date): string {
      const y = d.getUTCFullYear();
      const m = String(d.getUTCMonth() + 1).padStart(2, '0');
      const day = String(d.getUTCDate()).padStart(2, '0');
      return `${y}/${m}/${day}`;
    }

    /**
     * GET /v1/syncReceivedEdocList — returns summaries of received docs for the calling DN
     * in the given date window. fromDate / toDate are 'YYYY/MM/DD' strings.
     *
     * Caller (DN worker) computes fromDate = COALESCE(lastSyncedAt, NOW-7d), toDate = NOW.
     */
    export async function syncReceivedList(
      credentials: WorkerLgspCredentials,
      fromDateYmd: string,
      toDateYmd: string,
      timeoutMs = 30_000,
    ): Promise<LgspReceivedSummary[]> {
      const url = `${credentials.baseUrl}/v1/syncReceivedEdocList?messageType=edoc&fromDate=${encodeURIComponent(fromDateYmd)}&toDate=${encodeURIComponent(toDateYmd)}`;
      const controller = new AbortController();
      const timer = setTimeout(() => controller.abort(), timeoutMs);
      try {
        const res = await fetch(url, {
          method: 'GET',
          headers: {
            'X-SystemId': credentials.systemId,
            'X-SecretKey': credentials.secretKey,
            Accept: 'application/json',
          },
          signal: controller.signal,
        });
        const text = await res.text();
        if (!res.ok) {
          throw new Error(`LGSP /v1/syncReceivedEdocList HTTP ${res.status}: ${text.slice(0, 300)}`);
        }
        let json: { success: boolean; count?: number; data?: any[]; message?: string };
        try {
          json = JSON.parse(text);
        } catch {
          throw new Error(`LGSP /v1/syncReceivedEdocList non-JSON response: ${text.slice(0, 200)}`);
        }
        if (!json.success || !Array.isArray(json.data)) {
          logger.warn({ from: fromDateYmd, to: toDateYmd, message: json.message }, 'LGSP returned success=false or empty data');
          return [];
        }
        return json.data.map((d: any) => ({
          lgsp_doc_id: String(d.docId ?? ''),
          from_org_code: String(d.from ?? ''),
          to_org_code: String(d.to ?? ''),
          status: String(d.status ?? ''),
          status_desc: String(d.statusDesc ?? ''),
          created_time: String(d.createdTime ?? ''),
          updated_time: String(d.updatedTime ?? ''),
        }));
      } finally {
        clearTimeout(timer);
      }
    }

    /**
     * GET /v1/getEdoc?docId=<uuid> — returns full edXML + base64 attachments for one doc.
     * Returns null when LGSP reports success=false or data missing (doc may have been recalled).
     */
    export async function getEdocFull(
      credentials: WorkerLgspCredentials,
      lgspDocId: string,
      timeoutMs = 60_000,
    ): Promise<LgspReceivedFull | null> {
      const url = `${credentials.baseUrl}/v1/getEdoc?docId=${encodeURIComponent(lgspDocId)}`;
      const controller = new AbortController();
      const timer = setTimeout(() => controller.abort(), timeoutMs);
      try {
        const res = await fetch(url, {
          method: 'GET',
          headers: {
            'X-SystemId': credentials.systemId,
            'X-SecretKey': credentials.secretKey,
            Accept: 'application/json',
          },
          signal: controller.signal,
        });
        const text = await res.text();
        if (!res.ok) {
          throw new Error(`LGSP /v1/getEdoc HTTP ${res.status} for docId=${lgspDocId}: ${text.slice(0, 300)}`);
        }
        let json: { success: boolean; data?: any; message?: string };
        try {
          json = JSON.parse(text);
        } catch {
          throw new Error(`LGSP /v1/getEdoc non-JSON response for docId=${lgspDocId}: ${text.slice(0, 200)}`);
        }
        if (!json.success || !json.data) {
          logger.warn({ lgspDocId, message: json.message }, 'LGSP /v1/getEdoc returned success=false or missing data');
          return null;
        }
        const d = json.data;
        return {
          lgsp_doc_id: String(d.docId ?? lgspDocId),
          sender_org_code: String(d.from ?? ''),
          sender_org_name: String(d.fromName ?? ''),
          edoc_code: String(d.edocCode ?? ''),
          edoc_abstract: String(d.edocAbstract ?? ''),
          edxml: String(d.edxml ?? ''),
          attachments: Array.isArray(d.attachments)
            ? d.attachments.map((a: any) => ({
                file_name: String(a.fileName ?? ''),
                file_content_base64: String(a.fileContent ?? ''),
              }))
            : [],
        };
      } finally {
        clearTimeout(timer);
      }
    }
    ```

    Verify: `cd e_office_app_new/workers && npx tsc --noEmit` exits 0.
  </action>
  <verify>
    <automated>cd e_office_app_new/workers &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd e_office_app_new/workers &amp;&amp; npx tsc --noEmit` → exit 0
    - `grep -q "export async function loadLgspCredentials" e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts` → exit 0
    - `grep -q "export async function syncReceivedList" e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts` → exit 0
    - `grep -q "export async function getEdocFull" e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts` → exit 0
    - `grep -q "export function formatLgspDate" e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts` → exit 0
    - `grep -q "pgp_sym_decrypt" e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts` → exit 0
    - `grep -q "X-SystemId" e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts` → exit 0
    - `grep -q "X-SecretKey" e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts` → exit 0
    - `grep -q "/v1/syncReceivedEdocList" e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts` → exit 0
    - `grep -q "/v1/getEdoc" e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts` → exit 0
  </acceptance_criteria>
  <done>
    Worker-context LGSP service module created with credential load + 2 fetch helpers + YYYY/MM/DD formatter. Workers tsc clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create tick worker (workers/src/jobs/lgsp-receive-tick-worker.ts)</name>
  <files>
    e_office_app_new/workers/src/jobs/lgsp-receive-tick-worker.ts
  </files>
  <read_first>
    - e_office_app_new/workers/src/jobs/lgsp-send-worker.ts (entire 581 lines — mirror structure: module-level singletons connection + pool, startXxxWorker / stopXxxWorker exports, on('completed') / on('failed') / on('error') listeners)
    - .planning/phases/35-receive-flow-cron-syncreceivededoclist/35-CONTEXT.md D-02 (tick job: query active DNs → enqueue 1 receive-dn per DN, no doc work in tick handler)
  </read_first>
  <action>
    Create `e_office_app_new/workers/src/jobs/lgsp-receive-tick-worker.ts` (~120 lines):

    ```ts
    // ============================================================
    // LGSP Receive Tick Worker - Phase 35 Plan 35-02
    // REQ: LGSP-RECV-01
    // CONTEXT D-02 (tick spawns N receive-dn jobs), D-04 (tick concurrency=1)
    //
    // Handler: query lgsp_agency_config WHERE is_active=TRUE → for each row enqueue 'receive-dn'.
    // No doc work here (delegated to receive-dn). Tick failures don't retry — next scheduled
    // tick covers it (D-11 — tick attempts=1).
    // ============================================================
    import { Queue, Worker, Job } from 'bullmq';
    import IORedis from 'ioredis';
    import pg from 'pg';
    import pino from 'pino';
    import {
      LGSP_RECEIVE_QUEUE_NAME,
      LGSP_RECEIVE_TICK_JOB_NAME,
      LGSP_RECEIVE_DN_JOB_NAME,
      LGSP_RECEIVE_TICK_CONCURRENCY,
      LGSP_RECEIVE_TICK_MAX_ATTEMPTS,
      LGSP_RECEIVE_DN_MAX_ATTEMPTS,
      LGSP_RECEIVE_DN_BACKOFF_DELAY,
      LGSP_RECEIVE_DN_JOB_TIMEOUT_MS,
      type LgspReceiveTickJobData,
      type LgspReceiveDnJobData,
    } from '../queues/lgsp-receive-queue.js';

    const { Pool } = pg;

    const logger = pino({ name: 'lgsp-receive-tick-worker' });

    let connection: IORedis | null = null;
    let pool: pg.Pool | null = null;
    let dnQueue: Queue<LgspReceiveDnJobData> | null = null;

    function getConnection(): IORedis {
      if (!connection) {
        connection = new IORedis({
          host: process.env.REDIS_HOST || 'localhost',
          port: Number(process.env.REDIS_PORT) || 6379,
          password: process.env.REDIS_PASSWORD,
          maxRetriesPerRequest: null,
        });
      }
      return connection;
    }

    function getPool(): pg.Pool {
      if (!pool) {
        pool = new Pool({
          host: process.env.PG_HOST || 'localhost',
          port: Number(process.env.PG_PORT) || 5432,
          database: process.env.PG_DATABASE || 'qlvb_dev',
          user: process.env.PG_USER || 'qlvb_admin',
          password: process.env.PG_PASSWORD,
          max: 2,
          idleTimeoutMillis: 30000,
        });
      }
      return pool;
    }

    function getDnQueue(): Queue<LgspReceiveDnJobData> {
      if (!dnQueue) {
        dnQueue = new Queue<LgspReceiveDnJobData>(LGSP_RECEIVE_QUEUE_NAME, {
          connection: getConnection(),
          defaultJobOptions: {
            attempts: LGSP_RECEIVE_DN_MAX_ATTEMPTS,
            backoff: { type: 'exponential', delay: LGSP_RECEIVE_DN_BACKOFF_DELAY },
            removeOnComplete: { count: 500 },
            removeOnFail: { count: 2000 },
            timeout: LGSP_RECEIVE_DN_JOB_TIMEOUT_MS,
          } as any,
        });
      }
      return dnQueue;
    }

    /**
     * Tick handler: list active DNs → enqueue 1 receive-dn job per DN.
     * Only processes jobs with `job.name === 'receive-tick'` (filters out same-queue 'receive-dn' jobs).
     */
    async function handleTick(job: Job<LgspReceiveTickJobData>): Promise<{ enqueued: number; agency_count: number }> {
      if (job.name !== LGSP_RECEIVE_TICK_JOB_NAME) {
        // Same queue but different job name — let the DN worker handle it.
        return { enqueued: 0, agency_count: 0 };
      }

      const tickId = job.id ?? `tick-${Date.now()}`;
      const trigger = job.data?.trigger_source ?? 'cron';
      logger.info({ tickId, trigger }, 'LGSP receive tick: querying active agencies');

      // Query all active DNs (both env). SP from Phase 33.
      const rs = await getPool().query<{
        id: string;
        unit_id: string;
        environment: 'sandbox' | 'prod';
        system_id: string;
        base_url: string;
        last_synced_at: string | null;
      }>(
        `SELECT * FROM edoc.fn_lgsp_agency_config_get_all_active(NULL)`,
      );

      const dnQ = getDnQueue();
      let enqueued = 0;
      for (const row of rs.rows) {
        const unitId = Number(row.unit_id);
        const env = row.environment;
        const jobId = `lgsp-receive-dn-${unitId}-${env}-${tickId}`;
        try {
          await dnQ.add(
            LGSP_RECEIVE_DN_JOB_NAME,
            { unit_id: unitId, environment: env },
            { jobId },
          );
          enqueued += 1;
        } catch (err: any) {
          logger.warn({ unitId, env, err: err?.message }, 'Failed to enqueue receive-dn job');
        }
      }

      logger.info({ tickId, trigger, agency_count: rs.rows.length, enqueued }, 'LGSP receive tick: fan-out complete');
      return { enqueued, agency_count: rs.rows.length };
    }

    export function startLgspReceiveTickWorker(): Worker<LgspReceiveTickJobData> {
      const worker = new Worker<LgspReceiveTickJobData>(
        LGSP_RECEIVE_QUEUE_NAME,
        async (job) => handleTick(job),
        {
          connection: getConnection(),
          concurrency: LGSP_RECEIVE_TICK_CONCURRENCY,
          autorun: true,
        },
      );

      worker.on('completed', (job, result) => {
        if (job.name === LGSP_RECEIVE_TICK_JOB_NAME) {
          logger.info({ jobId: job.id, result }, 'LGSP receive tick completed');
        }
      });
      worker.on('failed', (job, err) => {
        if (job?.name === LGSP_RECEIVE_TICK_JOB_NAME) {
          logger.error({ jobId: job?.id, err: err?.message }, 'LGSP receive tick failed');
        }
      });
      worker.on('error', (err) => {
        logger.error({ err: err.message }, 'LGSP receive tick worker error');
      });

      logger.info(
        { queue: LGSP_RECEIVE_QUEUE_NAME, concurrency: LGSP_RECEIVE_TICK_CONCURRENCY, maxAttempts: LGSP_RECEIVE_TICK_MAX_ATTEMPTS },
        'LGSP receive tick worker started',
      );
      return worker;
    }

    export async function stopLgspReceiveTickWorker(worker: Worker<LgspReceiveTickJobData>): Promise<void> {
      try {
        await worker.close();
      } catch (err) {
        logger.warn({ err: (err as Error).message }, 'Error closing tick worker');
      }
      try {
        if (dnQueue) {
          await dnQueue.close();
          dnQueue = null;
        }
      } catch (err) {
        logger.warn({ err: (err as Error).message }, 'Error closing dn queue (from tick worker)');
      }
      try {
        if (pool) {
          await pool.end();
          pool = null;
        }
      } catch (err) {
        logger.warn({ err: (err as Error).message }, 'Error ending tick pool');
      }
      try {
        if (connection) {
          connection.disconnect();
          connection = null;
        }
      } catch (err) {
        logger.warn({ err: (err as Error).message }, 'Error disconnecting tick connection');
      }
    }
    ```

    Verify: `cd e_office_app_new/workers && npx tsc --noEmit` exits 0.
  </action>
  <verify>
    <automated>cd e_office_app_new/workers &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd e_office_app_new/workers &amp;&amp; npx tsc --noEmit` → exit 0
    - `grep -q "export function startLgspReceiveTickWorker" e_office_app_new/workers/src/jobs/lgsp-receive-tick-worker.ts` → exit 0
    - `grep -q "export async function stopLgspReceiveTickWorker" e_office_app_new/workers/src/jobs/lgsp-receive-tick-worker.ts` → exit 0
    - `grep -q "fn_lgsp_agency_config_get_all_active" e_office_app_new/workers/src/jobs/lgsp-receive-tick-worker.ts` → exit 0
    - `grep -q "job.name !== LGSP_RECEIVE_TICK_JOB_NAME" e_office_app_new/workers/src/jobs/lgsp-receive-tick-worker.ts` → exit 0 (job name filter)
    - `grep -q "LGSP_RECEIVE_DN_JOB_NAME" e_office_app_new/workers/src/jobs/lgsp-receive-tick-worker.ts` → exit 0 (enqueues child)
    - `grep -q "concurrency: LGSP_RECEIVE_TICK_CONCURRENCY" e_office_app_new/workers/src/jobs/lgsp-receive-tick-worker.ts` → exit 0
    - `grep -q "agency_count" e_office_app_new/workers/src/jobs/lgsp-receive-tick-worker.ts` → exit 0 (D-07 log shape)
  </acceptance_criteria>
  <done>
    Tick worker queries active DNs + enqueues fan-out + structured pino log. Workers tsc clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create DN worker (workers/src/jobs/lgsp-receive-dn-worker.ts) — full per-DN sync pipeline</name>
  <files>
    e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts
  </files>
  <read_first>
    - e_office_app_new/workers/src/jobs/lgsp-send-worker.ts (entire — see how it loads attachments via MinIO, how it does UPDATEs, how on('failed') marks final tracking)
    - e_office_app_new/workers/src/queues/lgsp-receive-queue.ts (Task 1 outputs)
    - e_office_app_new/workers/src/lgsp/lgsp-receive-service.ts (Task 2 outputs)
    - e_office_app_new/workers/src/lgsp/edxml-parser.ts (Task 1 copy)
    - .planning/phases/35-receive-flow-cron-syncreceivededoclist/35-CONTEXT.md ALL decisions D-06 (mapping), D-07 (attachment storage), D-08 (auto-register), D-09 (dedup), D-10 (resume), D-11 (per-DN error), D-12 (outbox INSERT)
    - e_office_app_new/database/schema/000_schema_v3.0.sql lines 26929+ (fn_incoming_doc_create signature) + lines 28694+ (fn_lgsp_status_outbox_insert)
  </read_first>
  <action>
    Create `e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` (~400 lines). This is the biggest file in Phase 35 — it's the full per-DN sync pipeline.

    Structure (sections, see exact code below):

    1. Imports + module-level singletons (Redis connection, pg Pool, MinIO client)
    2. SQL helpers: `findExistingByLgspDocId(pool, docId)`, `autoRegisterSender(pool, code, name)`, `insertIncomingDoc(pool, mapped)`, `insertAttachment(pool, ...)`, `insertOutboxStatus01(pool, incomingDocId, payload)`, `updateLastSynced(pool, unitId, env, errOrNull)`
    3. MinIO helper: `uploadAttachmentToMinio(buffer, key, contentType)`
    4. Mapping helper: `mapEdxmlToIncomingDoc(parsed, unitId, docId)`
    5. Main handler: `handleDnSync(job)` — orchestrates load creds → list → loop docs → parse → dedup check → autoRegister → createDoc → upload attachments → outbox → log
    6. `startLgspReceiveDnWorker()` + `stopLgspReceiveDnWorker()` exports + on() listeners

    ```ts
    // ============================================================
    // LGSP Receive DN Worker - Phase 35 Plan 35-02
    // REQ: LGSP-RECV-02..07
    // CONTEXT D-06 (field mapping), D-07 (MinIO 'lgsp/<docId>/<fileName>' + 50MB skip),
    //         D-08 (auto-register sender), D-09 (dedup catch SQLSTATE 23505),
    //         D-10 (last_synced_at resume), D-11 (per-attempt error handling),
    //         D-12 (outbox status='01' INSERT — Phase 36 consumes)
    //
    // Handler: 1 DN end-to-end sync.
    //   1. loadLgspCredentials (fresh per attempt — D-14)
    //   2. Compute fromDate = COALESCE(last_synced_at, NOW-7d) → YYYY/MM/DD
    //   3. GET /v1/syncReceivedEdocList
    //   4. For each docId NOT yet in DB:
    //        a. GET /v1/getEdoc (full payload)
    //        b. parseEdxml(payload.edxml)
    //        c. autoRegisterSender(From.OrganId, From.OrganName)
    //        d. INSERT incoming_docs via fn_incoming_doc_create (source_type='external_lgsp')
    //           — on SQLSTATE 23505 (duplicate) skip + log INFO (do NOT throw)
    //        e. For each attachment: decode base64 → MinIO put → INSERT attachments row
    //           — skip if >50MB + log WARN
    //        f. INSERT lgsp_status_outbox row target_status='01' sent_status='pending'
    //   5. SUCCESS → UPDATE lgsp_agency_config.last_synced_at=NOW(), last_sync_error=NULL
    //   6. FAIL → UPDATE lgsp_agency_config.last_sync_error=<truncated message>;
    //            DO NOT update last_synced_at; THROW for BullMQ retry (3 attempts exp 30s).
    // ============================================================
    import { Worker, Job } from 'bullmq';
    import IORedis from 'ioredis';
    import pg from 'pg';
    import { Client as MinioClient } from 'minio';
    import pino from 'pino';
    import {
      LGSP_RECEIVE_QUEUE_NAME,
      LGSP_RECEIVE_DN_JOB_NAME,
      LGSP_RECEIVE_DN_CONCURRENCY,
      LGSP_RECEIVE_DN_MAX_ATTEMPTS,
      type LgspReceiveDnJobData,
    } from '../queues/lgsp-receive-queue.js';
    import {
      loadLgspCredentials,
      syncReceivedList,
      getEdocFull,
      formatLgspDate,
      type LgspReceivedFull,
    } from '../lgsp/lgsp-receive-service.js';
    import { parseEdxml, type ParsedEdxml } from '../lgsp/edxml-parser.js';

    const { Pool } = pg;
    const logger = pino({ name: 'lgsp-receive-dn-worker' });

    const MAX_ATTACHMENT_BYTES = 50 * 1024 * 1024; // 50MB cap (CONTEXT D-07 + LGSP spec)
    const MINIO_BUCKET = process.env.MINIO_BUCKET || 'documents';
    const SYSTEM_STAFF_ID = 1; // TODO Phase 37: dedicated lgsp-system staff user

    let connection: IORedis | null = null;
    let pool: pg.Pool | null = null;
    let minioClient: MinioClient | null = null;

    function getConnection(): IORedis {
      if (!connection) {
        connection = new IORedis({
          host: process.env.REDIS_HOST || 'localhost',
          port: Number(process.env.REDIS_PORT) || 6379,
          password: process.env.REDIS_PASSWORD,
          maxRetriesPerRequest: null,
        });
      }
      return connection;
    }
    function getPool(): pg.Pool {
      if (!pool) {
        pool = new Pool({
          host: process.env.PG_HOST || 'localhost',
          port: Number(process.env.PG_PORT) || 5432,
          database: process.env.PG_DATABASE || 'qlvb_dev',
          user: process.env.PG_USER || 'qlvb_admin',
          password: process.env.PG_PASSWORD,
          max: 5,
          idleTimeoutMillis: 30000,
        });
      }
      return pool;
    }
    function getMinio(): MinioClient {
      if (!minioClient) {
        minioClient = new MinioClient({
          endPoint: process.env.MINIO_ENDPOINT || 'localhost',
          port: Number(process.env.MINIO_PORT) || 9000,
          useSSL: process.env.MINIO_USE_SSL === 'true',
          accessKey: process.env.MINIO_ACCESS_KEY || 'minioadmin',
          secretKey: process.env.MINIO_SECRET_KEY || 'minioadmin',
        });
      }
      return minioClient;
    }

    // ============================================================
    // SQL helpers
    // ============================================================

    async function findExistingByLgspDocId(p: pg.Pool, lgspDocId: string): Promise<number | null> {
      const rs = await p.query<{ id: string }>(
        `SELECT id FROM edoc.incoming_docs
          WHERE external_doc_id = $1 AND source_type = 'external_lgsp'
          LIMIT 1`,
        [lgspDocId.slice(0, 200)],
      );
      return rs.rowCount ? Number(rs.rows[0].id) : null;
    }

    async function autoRegisterSender(p: pg.Pool, code: string, name: string): Promise<number> {
      const trimmedCode = code.slice(0, 100);
      const trimmedName = (name || code).slice(0, 500);
      const inserted = await p.query<{ id: string }>(
        `INSERT INTO edoc.inter_organizations (code, name, lgsp_organ_id, is_active, created_at, updated_at)
         VALUES ($1, $2, $1, FALSE, NOW(), NOW())
         ON CONFLICT (code) DO NOTHING RETURNING id`,
        [trimmedCode, trimmedName],
      );
      if (inserted.rowCount) {
        logger.info({ code: trimmedCode, name: trimmedName }, 'Auto-registered new external org from LGSP');
        return Number(inserted.rows[0].id);
      }
      const existing = await p.query<{ id: string }>(
        `SELECT id FROM edoc.inter_organizations WHERE code = $1 LIMIT 1`,
        [trimmedCode],
      );
      return Number(existing.rows[0].id);
    }

    interface IncomingDocInsertInput {
      unit_id: number;
      external_doc_id: string;
      lgsp_sender_org_code: string;
      publish_unit: string;
      notation: string;
      document_code: string;
      abstract: string;
      signer: string;
      sign_date: string | null;
      publish_date: string | null;
      number_paper: number;
      recipients_text: string;
    }

    interface IncomingDocInsertResult {
      inserted: boolean;
      skipped: boolean;
      id: number | null;
      message: string;
    }

    async function insertIncomingDoc(p: pg.Pool, x: IncomingDocInsertInput): Promise<IncomingDocInsertResult> {
      try {
        const rs = await p.query<{ success: boolean; message: string; id: string | null }>(
          `SELECT * FROM edoc.fn_incoming_doc_create(
             $1, $2::timestamptz, NULL::integer, $3, $4, $5, $6, $7::timestamptz, $8, $9::timestamptz,
             NULL::integer, NULL::integer, NULL::integer,
             1::smallint, 1::smallint, $10::integer, 1::integer,
             NULL::timestamptz, $11, NULL::text,
             FALSE::boolean, $12::integer, $1::integer,
             'external_lgsp'::edoc.doc_source_type, FALSE::boolean, $6,
             NULL::bigint, $13
           )`,
          [
            x.unit_id,                              // $1 p_unit_id + p_department_id
            new Date().toISOString(),               // $2 p_received_date NOW
            x.notation.slice(0, 50),                // $3
            x.document_code.slice(0, 100),          // $4
            x.abstract,                             // $5
            x.publish_unit.slice(0, 500),           // $6 publish_unit + p_unit_send (reuse)
            x.publish_date,                         // $7
            x.signer.slice(0, 200),                 // $8
            x.sign_date,                            // $9
            x.number_paper,                         // $10
            x.recipients_text.slice(0, 1000),       // $11
            SYSTEM_STAFF_ID,                        // $12 created_by
            x.external_doc_id.slice(0, 200),        // $13
          ],
        );
        const row = rs.rows[0];
        if (!row) return { inserted: false, skipped: false, id: null, message: 'SP returned no row' };
        if (row.success && row.id) {
          await p.query(
            `UPDATE edoc.incoming_docs SET lgsp_sender_org_code = $1 WHERE id = $2`,
            [x.lgsp_sender_org_code.slice(0, 13), row.id],
          );
          return { inserted: true, skipped: false, id: Number(row.id), message: row.message };
        }
        // Dedup detection (CONTEXT D-09)
        const m = (row.message || '').toLowerCase();
        if (m.includes('idx_incoming_docs_external_dedupe') || m.includes('duplicate key') || m.includes('unique constraint')) {
          return { inserted: false, skipped: true, id: null, message: `dedup skipped: ${x.external_doc_id}` };
        }
        return { inserted: false, skipped: false, id: null, message: row.message };
      } catch (err: any) {
        // Direct pg error (rare — SP wraps most). 23505 = unique_violation.
        if (err?.code === '23505') {
          return { inserted: false, skipped: true, id: null, message: `dedup skipped (pg 23505): ${x.external_doc_id}` };
        }
        throw err;
      }
    }

    async function insertAttachmentRow(
      p: pg.Pool,
      params: { incoming_doc_id: number; file_name: string; file_path: string; file_size: number; content_type: string },
    ): Promise<number> {
      const rs = await p.query<{ id: string }>(
        `INSERT INTO edoc.attachments
           (incoming_doc_id, file_name, file_path, file_size, content_type, uploaded_by, created_at)
         VALUES ($1, $2, $3, $4, $5, $6, NOW())
         RETURNING id`,
        [
          params.incoming_doc_id,
          params.file_name.slice(0, 500),
          params.file_path.slice(0, 1000),
          params.file_size,
          (params.content_type || 'application/octet-stream').slice(0, 200),
          SYSTEM_STAFF_ID,
        ],
      );
      return Number(rs.rows[0].id);
    }

    async function insertOutboxStatus01(
      p: pg.Pool,
      incomingDocId: number,
      payload: Record<string, unknown>,
    ): Promise<void> {
      await p.query(
        `SELECT * FROM edoc.fn_lgsp_status_outbox_insert($1::bigint, $2::varchar, $3::jsonb)`,
        [incomingDocId, '01', JSON.stringify(payload)],
      );
    }

    async function updateLastSyncedSuccess(p: pg.Pool, unitId: number, env: 'sandbox' | 'prod'): Promise<void> {
      await p.query(
        `SELECT * FROM edoc.fn_lgsp_agency_config_update_last_synced($1::integer, $2::varchar, $3::timestamptz, NULL::text)`,
        [unitId, env, new Date().toISOString()],
      );
    }

    async function updateLastSyncError(p: pg.Pool, unitId: number, env: 'sandbox' | 'prod', errMsg: string): Promise<void> {
      try {
        await p.query(
          `UPDATE edoc.lgsp_agency_config
             SET last_sync_error = $3, updated_at = NOW()
           WHERE unit_id = $1 AND environment = $2`,
          [unitId, env, errMsg.slice(0, 1000)],
        );
      } catch (err) {
        logger.warn({ unitId, env, err: (err as Error).message }, 'Failed to write last_sync_error');
      }
    }

    // ============================================================
    // MinIO upload helper
    // ============================================================

    async function uploadAttachmentToMinio(
      buffer: Buffer,
      objectKey: string,
      contentType: string,
    ): Promise<void> {
      const mc = getMinio();
      await mc.putObject(MINIO_BUCKET, objectKey, buffer, buffer.length, {
        'Content-Type': contentType || 'application/octet-stream',
      });
    }

    // ============================================================
    // Mapping helper: ParsedEdxml + LgspReceivedFull → IncomingDocInsertInput (CONTEXT D-06)
    // ============================================================

    function mapEdxmlToInsertInput(
      parsed: ParsedEdxml,
      docFull: LgspReceivedFull,
      unitId: number,
    ): IncomingDocInsertInput {
      const mh = parsed.messageHeader;
      const publishUnit = mh.from.organName || docFull.sender_org_name || mh.from.organId;
      const notation = mh.code.codeNumber || docFull.edoc_code || '';
      const documentCode = mh.code.codeNotation || docFull.edoc_code || '';
      const subject = mh.subject || docFull.edoc_abstract || '';
      const signer = mh.signerInfo.signer || '';
      const signDate = mh.promulgationInfo.promulgationDate || null;
      const numberPaper = mh.otherInfo.pageAmount && Number.isFinite(mh.otherInfo.pageAmount)
        ? Math.max(1, mh.otherInfo.pageAmount)
        : 1;
      const recipientsText = `${mh.from.organName || mh.from.organId} -> ${mh.to.organName || mh.to.organId}`;

      return {
        unit_id: unitId,
        external_doc_id: docFull.lgsp_doc_id,
        lgsp_sender_org_code: mh.from.organId || docFull.sender_org_code || '',
        publish_unit: publishUnit,
        notation,
        document_code: documentCode,
        abstract: subject,
        signer,
        sign_date: signDate,
        publish_date: signDate,
        number_paper: numberPaper,
        recipients_text: recipientsText,
      };
    }

    // ============================================================
    // Main handler
    // ============================================================

    interface DnSyncResult {
      docs_seen: number;
      docs_inserted: number;
      docs_skipped: number;
      docs_failed: number;
      attachments_uploaded: number;
      attachments_skipped: number;
    }

    async function handleDnSync(job: Job<LgspReceiveDnJobData>): Promise<DnSyncResult> {
      if (job.name !== LGSP_RECEIVE_DN_JOB_NAME) {
        // Wrong job name for this worker — skip silently (tick worker handles its own).
        return { docs_seen: 0, docs_inserted: 0, docs_skipped: 0, docs_failed: 0, attachments_uploaded: 0, attachments_skipped: 0 };
      }

      const { unit_id, environment } = job.data;
      const signingSecretKey = process.env.SIGNING_SECRET_KEY;
      if (!signingSecretKey) {
        throw new Error('SIGNING_SECRET_KEY env var not set — cannot decrypt LGSP credentials');
      }
      const p = getPool();
      const result: DnSyncResult = { docs_seen: 0, docs_inserted: 0, docs_skipped: 0, docs_failed: 0, attachments_uploaded: 0, attachments_skipped: 0 };

      let creds;
      try {
        creds = await loadLgspCredentials(p, unit_id, environment, signingSecretKey);
      } catch (err: any) {
        logger.warn({ unit_id, environment, err: err?.message }, 'LGSP credential not loadable — skipping DN');
        // Not an error to retry — config is genuinely missing. Mark + return.
        await updateLastSyncError(p, unit_id, environment, `credential not loadable: ${err?.message ?? err}`);
        return result;
      }

      // Compute date window: fromDate = last_synced_at OR NOW-7d. toDate = NOW.
      const now = new Date();
      const fallbackFrom = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1000);
      const fromDate = creds.lastSyncedAt ? new Date(creds.lastSyncedAt) : fallbackFrom;
      // Guard: if last_synced_at is older than 7 days, still use NOW-7d (LGSP rejects wide windows)
      const effectiveFrom = fromDate < fallbackFrom ? fallbackFrom : fromDate;
      const fromYmd = formatLgspDate(effectiveFrom);
      const toYmd = formatLgspDate(now);

      logger.info({ unit_id, environment, fromYmd, toYmd }, 'LGSP DN sync: querying list');

      let summaries;
      try {
        summaries = await syncReceivedList(creds, fromYmd, toYmd);
      } catch (err: any) {
        const msg = `syncReceivedList failed: ${err?.message ?? err}`;
        await updateLastSyncError(p, unit_id, environment, msg);
        throw new Error(msg); // BullMQ retry
      }
      result.docs_seen = summaries.length;
      logger.info({ unit_id, environment, count: summaries.length }, 'LGSP DN sync: got list');

      for (const sum of summaries) {
        try {
          // Pre-check: already in DB? (cheap; avoids redundant getEdoc fetch)
          const existingId = await findExistingByLgspDocId(p, sum.lgsp_doc_id);
          if (existingId) {
            result.docs_skipped += 1;
            continue;
          }

          // Fetch full edXML
          const full = await getEdocFull(creds, sum.lgsp_doc_id);
          if (!full) {
            logger.warn({ unit_id, lgspDocId: sum.lgsp_doc_id }, 'getEdoc returned null — skipping');
            result.docs_failed += 1;
            continue;
          }

          // Parse
          let parsed: ParsedEdxml;
          try {
            parsed = parseEdxml(full.edxml);
          } catch (err: any) {
            logger.warn({ unit_id, lgspDocId: sum.lgsp_doc_id, err: err?.message }, 'parseEdxml failed — skipping doc');
            result.docs_failed += 1;
            continue;
          }

          // Auto-register sender (D-08)
          await autoRegisterSender(
            p,
            parsed.messageHeader.from.organId || full.sender_org_code || sum.from_org_code,
            parsed.messageHeader.from.organName || full.sender_org_name || '',
          );

          // INSERT incoming_docs
          const insertInput = mapEdxmlToInsertInput(parsed, full, unit_id);
          const ins = await insertIncomingDoc(p, insertInput);
          if (ins.skipped) {
            result.docs_skipped += 1;
            logger.info({ unit_id, lgspDocId: sum.lgsp_doc_id }, ins.message);
            continue;
          }
          if (!ins.inserted || !ins.id) {
            logger.warn({ unit_id, lgspDocId: sum.lgsp_doc_id, msg: ins.message }, 'insertIncomingDoc failed — skipping');
            result.docs_failed += 1;
            continue;
          }
          const newDocId = ins.id;

          // Attachments
          for (const att of parsed.attachments) {
            if (att.content.length > MAX_ATTACHMENT_BYTES) {
              result.attachments_skipped += 1;
              logger.warn({ unit_id, lgspDocId: sum.lgsp_doc_id, fileName: att.fileName, bytes: att.content.length }, 'Attachment exceeds 50MB — skipping (D-07)');
              continue;
            }
            const objectKey = `lgsp/${sum.lgsp_doc_id}/${att.fileName}`;
            try {
              await uploadAttachmentToMinio(att.content, objectKey, att.mimeType ?? 'application/octet-stream');
              await insertAttachmentRow(p, {
                incoming_doc_id: newDocId,
                file_name: att.fileName,
                file_path: objectKey,
                file_size: att.content.length,
                content_type: att.mimeType ?? 'application/octet-stream',
              });
              result.attachments_uploaded += 1;
            } catch (err: any) {
              logger.warn({ unit_id, lgspDocId: sum.lgsp_doc_id, fileName: att.fileName, err: err?.message }, 'Attachment upload/insert failed — skipping');
              result.attachments_skipped += 1;
            }
          }

          // Outbox status '01' (Phase 36 worker consumes)
          try {
            await insertOutboxStatus01(p, newDocId, {
              lgsp_doc_id: sum.lgsp_doc_id,
              sender_org_code: parsed.messageHeader.from.organId,
              ack_received_at: new Date().toISOString(),
            });
          } catch (err: any) {
            logger.warn({ unit_id, lgspDocId: sum.lgsp_doc_id, err: err?.message }, 'Outbox INSERT failed (status 01) — Phase 36 may not fire callback');
          }

          result.docs_inserted += 1;
          logger.info({ unit_id, lgspDocId: sum.lgsp_doc_id, newDocId, attachments: parsed.attachments.length }, 'LGSP doc imported');
        } catch (err: any) {
          // Per-doc error: log + continue (don't fail entire DN sync because of 1 bad doc)
          logger.error({ unit_id, lgspDocId: sum.lgsp_doc_id, err: err?.message }, 'Doc processing failed (continuing with next)');
          result.docs_failed += 1;
        }
      }

      // SUCCESS path: update last_synced_at + clear error (D-10 + D-11)
      try {
        await updateLastSyncedSuccess(p, unit_id, environment);
      } catch (err: any) {
        logger.warn({ unit_id, environment, err: err?.message }, 'Failed to update last_synced_at');
      }

      logger.info(
        { unit_id, environment, ...result },
        'LGSP DN sync complete',
      );
      return result;
    }

    export function startLgspReceiveDnWorker(): Worker<LgspReceiveDnJobData> {
      const worker = new Worker<LgspReceiveDnJobData>(
        LGSP_RECEIVE_QUEUE_NAME,
        async (job) => handleDnSync(job),
        {
          connection: getConnection(),
          concurrency: LGSP_RECEIVE_DN_CONCURRENCY,
          autorun: true,
        },
      );

      worker.on('completed', (job, result) => {
        if (job.name === LGSP_RECEIVE_DN_JOB_NAME) {
          logger.info({ jobId: job.id, unit_id: job.data?.unit_id, result }, 'LGSP DN worker completed');
        }
      });

      worker.on('failed', async (job, err) => {
        if (!job || job.name !== LGSP_RECEIVE_DN_JOB_NAME) return;
        const attemptsMade = job.attemptsMade ?? 0;
        const maxAttempts = LGSP_RECEIVE_DN_MAX_ATTEMPTS;
        logger.warn(
          { jobId: job.id, unit_id: job.data?.unit_id, attemptsMade, maxAttempts, err: err?.message },
          'LGSP DN worker job failed',
        );
        // Final-failure path: also persist error to last_sync_error so admin sees it (Phase 37 UI)
        if (attemptsMade >= maxAttempts) {
          try {
            await updateLastSyncError(
              getPool(),
              job.data?.unit_id,
              job.data?.environment,
              `Retry exhausted (${attemptsMade}/${maxAttempts}): ${err?.message ?? 'unknown'}`,
            );
          } catch (e) {
            logger.warn({ err: (e as Error).message }, 'Failed to persist final retry exhausted error');
          }
        }
      });

      worker.on('error', (err) => {
        logger.error({ err: err.message }, 'LGSP receive DN worker error');
      });

      logger.info(
        { queue: LGSP_RECEIVE_QUEUE_NAME, concurrency: LGSP_RECEIVE_DN_CONCURRENCY, maxAttempts: LGSP_RECEIVE_DN_MAX_ATTEMPTS },
        'LGSP receive DN worker started',
      );
      return worker;
    }

    export async function stopLgspReceiveDnWorker(worker: Worker<LgspReceiveDnJobData>): Promise<void> {
      try { await worker.close(); } catch (err) { logger.warn({ err: (err as Error).message }, 'Error closing DN worker'); }
      try { if (pool) { await pool.end(); pool = null; } } catch (err) { logger.warn({ err: (err as Error).message }, 'Error closing DN pool'); }
      try { if (connection) { connection.disconnect(); connection = null; } } catch (err) { logger.warn({ err: (err as Error).message }, 'Error closing DN connection'); }
    }
    ```

    Verify: `cd e_office_app_new/workers && npx tsc --noEmit` exits 0.
  </action>
  <verify>
    <automated>cd e_office_app_new/workers &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd e_office_app_new/workers &amp;&amp; npx tsc --noEmit` → exit 0
    - `grep -q "export function startLgspReceiveDnWorker" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "export async function stopLgspReceiveDnWorker" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "loadLgspCredentials" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "syncReceivedList" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "getEdocFull" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "parseEdxml" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "autoRegisterSender\|ON CONFLICT (code)" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "external_lgsp" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "lgsp/" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0 (MinIO key pattern)
    - `grep -q "MAX_ATTACHMENT_BYTES = 50" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "fn_lgsp_status_outbox_insert" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "'01'" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0 (outbox target_status)
    - `grep -q "fn_lgsp_agency_config_update_last_synced" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "last_sync_error" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "concurrency: LGSP_RECEIVE_DN_CONCURRENCY" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "fn_incoming_doc_create" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
    - `grep -q "lgsp_sender_org_code" e_office_app_new/workers/src/jobs/lgsp-receive-dn-worker.ts` → exit 0
  </acceptance_criteria>
  <done>
    DN worker is full pipeline: cred load → list → loop docs (existence check → getEdoc → parse → auto-register → INSERT with dedup → MinIO upload attachments with 50MB skip → outbox status 01) → update last_synced_at + error handling. Workers tsc clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create backend producer (backend/src/lib/queue/lgsp-receive-queue.ts) + wire workers/index.ts (replace Phase 18 inline lgspReceiveWorker)</name>
  <files>
    e_office_app_new/backend/src/lib/queue/lgsp-receive-queue.ts,
    e_office_app_new/workers/src/index.ts
  </files>
  <read_first>
    - e_office_app_new/backend/src/lib/queue/lgsp-send-queue.ts (entire — mirror exactly: getXxxQueue lazy singleton, enqueueXxx helper, closeXxxQueue, defaultJobOptions)
    - e_office_app_new/workers/src/index.ts (entire — see how Phase 34 replaced inline LGSP send worker with start/stop calls; need to do same for receive — DELETE the Phase 18 inline lgspReceiveWorker block + lgspLogin/lgspReceiveList helpers since both are now in workers/src/lgsp/lgsp-receive-service.ts)
  </read_first>
  <action>
    **A. Create `e_office_app_new/backend/src/lib/queue/lgsp-receive-queue.ts` (~140 lines):**

    ```ts
    // ============================================================
    // LGSP Receive Queue - backend producer side - Phase 35 Plan 35-02
    // REQ: LGSP-RECV-01
    // CONTEXT D-01 (repeat 5min), D-03 (manual route enqueue tick)
    //
    // Mirror pattern: backend/src/lib/queue/lgsp-send-queue.ts (Phase 34)
    //
    // Exports:
    //   - getLgspReceiveQueue(): lazy singleton Queue
    //   - enqueueReceiveTick(data?): used by POST /api/lgsp/sync-now (Plan 35-03)
    //   - registerReceiveTickRepeatJob(): called from server.ts on startup
    //   - closeLgspReceiveQueue(): called from SIGTERM cleanup chain in server.ts
    // ============================================================
    import { Queue } from 'bullmq';
    import { getRedisConnection } from './client.js';
    import pino from 'pino';

    const logger = pino({ name: 'lgsp-receive-queue' });

    // Constants — duplicated from workers/src/queues/lgsp-receive-queue.ts (separate module per Approach B)
    export const LGSP_RECEIVE_QUEUE_NAME = 'lgsp-receive';
    export const LGSP_RECEIVE_TICK_JOB_NAME = 'receive-tick';
    export const LGSP_RECEIVE_DN_JOB_NAME = 'receive-dn';
    export const LGSP_RECEIVE_TICK_MAX_ATTEMPTS = 1;
    export const LGSP_RECEIVE_DN_MAX_ATTEMPTS = 3;
    export const LGSP_RECEIVE_DN_BACKOFF_DELAY = 30_000;
    export const LGSP_RECEIVE_TICK_INTERVAL_MS = 5 * 60 * 1000;
    export const LGSP_RECEIVE_TICK_REPEAT_JOB_ID = 'lgsp-receive-tick-singleton';

    export interface LgspReceiveTickJobData {
      trigger_source?: 'cron' | 'manual';
      triggered_by_staff_id?: number;
    }
    export interface LgspReceiveDnJobData {
      unit_id: number;
      environment: 'sandbox' | 'prod';
    }

    let queue: Queue<LgspReceiveTickJobData | LgspReceiveDnJobData> | null = null;

    export function getLgspReceiveQueue(): Queue<LgspReceiveTickJobData | LgspReceiveDnJobData> {
      if (!queue) {
        queue = new Queue<LgspReceiveTickJobData | LgspReceiveDnJobData>(
          LGSP_RECEIVE_QUEUE_NAME,
          {
            connection: getRedisConnection(),
            defaultJobOptions: {
              removeOnComplete: { count: 500 },
              removeOnFail: { count: 2000 },
            },
          },
        );
      }
      return queue;
    }

    /**
     * Manual enqueue (Plan 35-03 `POST /api/lgsp/sync-now` calls this).
     * Default trigger_source='manual'. Job runs as soon as a tick worker is free.
     */
    export async function enqueueReceiveTick(data?: LgspReceiveTickJobData): Promise<string> {
      const q = getLgspReceiveQueue();
      const j = await q.add(
        LGSP_RECEIVE_TICK_JOB_NAME,
        { trigger_source: data?.trigger_source ?? 'manual', triggered_by_staff_id: data?.triggered_by_staff_id },
        {
          attempts: LGSP_RECEIVE_TICK_MAX_ATTEMPTS,
          removeOnComplete: { count: 500 },
        },
      );
      logger.info({ jobId: j.id, trigger: data?.trigger_source ?? 'manual', staffId: data?.triggered_by_staff_id }, 'Enqueued LGSP receive tick');
      return String(j.id);
    }

    /**
     * Idempotent registration of the 5-min repeat scheduler.
     * Called once from server.ts startup. BullMQ uses the deterministic jobId to prevent dup schedulers.
     */
    export async function registerReceiveTickRepeatJob(): Promise<void> {
      const q = getLgspReceiveQueue();
      try {
        // Remove any pre-existing repeat (idempotent — handles config changes / restarts)
        const existing = await q.getRepeatableJobs();
        for (const r of existing) {
          if (r.name === LGSP_RECEIVE_TICK_JOB_NAME) {
            try {
              await q.removeRepeatableByKey(r.key);
              logger.info({ key: r.key }, 'Removed pre-existing LGSP receive tick repeat scheduler');
            } catch (err) {
              logger.warn({ err: (err as Error).message }, 'Failed to remove pre-existing repeat scheduler (continuing)');
            }
          }
        }
      } catch (err) {
        logger.warn({ err: (err as Error).message }, 'getRepeatableJobs failed (continuing to add)');
      }

      await q.add(
        LGSP_RECEIVE_TICK_JOB_NAME,
        { trigger_source: 'cron' },
        {
          repeat: { every: LGSP_RECEIVE_TICK_INTERVAL_MS },
          jobId: LGSP_RECEIVE_TICK_REPEAT_JOB_ID,
          attempts: LGSP_RECEIVE_TICK_MAX_ATTEMPTS,
          removeOnComplete: { count: 200 },
        },
      );
      logger.info(
        { intervalMs: LGSP_RECEIVE_TICK_INTERVAL_MS, jobId: LGSP_RECEIVE_TICK_REPEAT_JOB_ID },
        'Registered LGSP receive tick repeat scheduler',
      );
    }

    export async function closeLgspReceiveQueue(): Promise<void> {
      if (queue) {
        try {
          await queue.close();
          queue = null;
        } catch (err) {
          logger.warn({ err: (err as Error).message }, 'Error closing LGSP receive queue');
        }
      }
    }
    ```

    **B. Edit `e_office_app_new/workers/src/index.ts`:**

    - Add imports at top (near the Phase 34 send-worker import):
      ```ts
      import {
        startLgspReceiveTickWorker,
        stopLgspReceiveTickWorker,
      } from './jobs/lgsp-receive-tick-worker.js';
      import {
        startLgspReceiveDnWorker,
        stopLgspReceiveDnWorker,
      } from './jobs/lgsp-receive-dn-worker.js';
      ```
    - **DELETE** the entire Phase 18 inline block:
      - `lgspLogin()` function (lines ~108-126)
      - `LgspReceivedItem` interface (lines ~128-132)
      - `lgspReceiveList()` function (lines ~134-148)
      - The `lgspReceiveWorker = new Worker('lgsp-receive', ...)` block (lines ~155-192) and its 2 inline `pool.query(...fn_incoming_doc_create...)` calls
      - The `LGSP_MOCK` / `LGSP_TOKEN_TTL_MS` / `lgspToken` / `lgspTokenExp` module vars (lines ~103-106) — no longer used after delete

    - **REPLACE** with 2-line start calls (place near the Phase 34 `const lgspSendWorker = startLgspSendWorker();`):
      ```ts
      // --- LGSP Receive Workers (Phase 35) ---
      // REPLACED Phase 18 inline polling. New: BullMQ 2-worker system on queue 'lgsp-receive':
      //   - 'receive-tick' (concurrency=1): repeat every 5 min OR manual via POST /api/lgsp/sync-now
      //     Handler queries lgsp_agency_config active rows → enqueues N 'receive-dn' jobs.
      //   - 'receive-dn' (concurrency=3, retry 3x exp 30s): full per-DN sync pipeline
      //     (list + getEdoc + parse + INSERT incoming_docs + MinIO + outbox status 01)
      // Repeat job is registered from backend/src/server.ts on startup
      // (registerReceiveTickRepeatJob in backend/src/lib/queue/lgsp-receive-queue.ts).
      const lgspReceiveTickWorker = startLgspReceiveTickWorker();
      const lgspReceiveDnWorker = startLgspReceiveDnWorker();
      ```

    - Update the startup log line to reflect the new state:
      ```ts
      logger.info('Workers started: email-send, sms-send, lgsp-receive (Phase 35: tick + dn), lgsp-send (Phase 34), fcm-push, zalo-send, notification-send');
      ```

    - Update SIGTERM cleanup chain — replace `await lgspReceiveWorker.close();` with:
      ```ts
      await stopLgspReceiveTickWorker(lgspReceiveTickWorker);
      await stopLgspReceiveDnWorker(lgspReceiveDnWorker);
      ```

    **C. Compile both modules:**
    ```bash
    cd e_office_app_new/backend && npx tsc --noEmit
    cd e_office_app_new/workers && npx tsc --noEmit
    ```
    Both must exit 0.
  </action>
  <verify>
    <automated>cd e_office_app_new/backend &amp;&amp; npx tsc --noEmit &amp;&amp; cd ../workers &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd e_office_app_new/backend &amp;&amp; npx tsc --noEmit` → exit 0
    - `cd e_office_app_new/workers &amp;&amp; npx tsc --noEmit` → exit 0
    - `grep -q "export function getLgspReceiveQueue" e_office_app_new/backend/src/lib/queue/lgsp-receive-queue.ts` → exit 0
    - `grep -q "export async function enqueueReceiveTick" e_office_app_new/backend/src/lib/queue/lgsp-receive-queue.ts` → exit 0
    - `grep -q "export async function registerReceiveTickRepeatJob" e_office_app_new/backend/src/lib/queue/lgsp-receive-queue.ts` → exit 0
    - `grep -q "export async function closeLgspReceiveQueue" e_office_app_new/backend/src/lib/queue/lgsp-receive-queue.ts` → exit 0
    - `grep -q "every: LGSP_RECEIVE_TICK_INTERVAL_MS" e_office_app_new/backend/src/lib/queue/lgsp-receive-queue.ts` → exit 0
    - `grep -q "removeRepeatableByKey" e_office_app_new/backend/src/lib/queue/lgsp-receive-queue.ts` → exit 0 (idempotent re-register)
    - `grep -q "lgsp-receive-tick-singleton" e_office_app_new/backend/src/lib/queue/lgsp-receive-queue.ts` → exit 0
    - `grep -q "startLgspReceiveTickWorker" e_office_app_new/workers/src/index.ts` → exit 0
    - `grep -q "startLgspReceiveDnWorker" e_office_app_new/workers/src/index.ts` → exit 0
    - `grep -q "stopLgspReceiveTickWorker" e_office_app_new/workers/src/index.ts` → exit 0
    - `grep -q "stopLgspReceiveDnWorker" e_office_app_new/workers/src/index.ts` → exit 0
    - `grep -q "Phase 35" e_office_app_new/workers/src/index.ts` → exit 0
    - `grep -q "function lgspLogin" e_office_app_new/workers/src/index.ts` → exit 1 (Phase 18 helper DELETED)
    - `grep -q "function lgspReceiveList" e_office_app_new/workers/src/index.ts` → exit 1 (Phase 18 helper DELETED)
    - `grep -q "received-edocs" e_office_app_new/workers/src/index.ts` → exit 1 (Phase 18 broken endpoint REMOVED)
  </acceptance_criteria>
  <done>
    Backend producer queue created with manual + repeat + close. workers/index.ts wires both new workers and removes all Phase 18 inline LGSP polling code (login helper + receive helper + inline Worker). Both modules tsc clean.
  </done>
</task>

</tasks>

<verification>
After all 5 tasks complete:

```bash
# 1. Both TypeScript modules
cd e_office_app_new/backend && npx tsc --noEmit
cd e_office_app_new/workers && npx tsc --noEmit
# Each: exit 0

# 2. Smoke test: start worker, verify both workers register (no actual job processing — that's Plan 35-05 E2E)
cd e_office_app_new/workers && npm run dev 2>&1 | head -30 &
sleep 5
# Expect log lines:
#   "LGSP receive tick worker started" { queue: "lgsp-receive", concurrency: 1, maxAttempts: 1 }
#   "LGSP receive DN worker started" { queue: "lgsp-receive", concurrency: 3, maxAttempts: 3 }
#   "Workers started: email-send, sms-send, lgsp-receive (Phase 35: tick + dn), lgsp-send (Phase 34), ..."
# Kill worker (Ctrl-C / pkill node)

# 3. All grep acceptance from each task PASS (per <acceptance_criteria>)
```
</verification>

<success_criteria>
- 6 new files created (1 backend queue + 1 worker queue + 2 worker job handlers + 1 worker LGSP service + 1 worker parser duplicate)
- workers/index.ts updated: replaces Phase 18 inline lgspReceiveWorker + helpers
- Backend + workers both TypeScript strict pass
- fast-xml-parser added to workers/package.json
- BullMQ Queue + 2 Worker instances + repeat scheduler primitives all in place
- LGSP HTTP client (workers-local): real `/v1/syncReceivedEdocList` + `/v1/getEdoc` with X-SystemId/X-SecretKey headers + YYYY/MM/DD date format
- All 5 task acceptance grep checks PASS
- Smoke test: workers process starts, prints both new "started" log lines, no crash
</success_criteria>

<output>
After all 5 tasks complete, create `.planning/phases/35-receive-flow-cron-syncreceivededoclist/35-02-SUMMARY.md` following template. Include:
- Wave + REQ-IDs (LGSP-RECV-01 to 07)
- Files created/modified with line counts
- Decisions honored (D-01/D-02/D-04/D-06/D-07/D-08/D-09/D-10/D-11/D-12/D-14)
- Approach B (Approach decision section): parser duplicated, lgsp-receive-service is worker-context
- Smoke test output ("workers started" log lines)
- TypeScript both modules exit 0
- Known stub: SYSTEM_STAFF_ID=1 (TODO Phase 37 dedicated lgsp-system user)
- Known stub: outbox payload schema not formally typed (loose Record<string,unknown> for now — Phase 36 will define ack payload contract)
- Approach B sync requirement: edxml-parser.ts now lives in both backend + workers — Plan 35-05 will checksum-audit
</output>
