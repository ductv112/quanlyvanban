---
phase: 33-database-core-infrastructure
plan: 05
type: execute
wave: 2
depends_on:
  - 33-01
  - 33-02
  - 33-03
  - 33-04
files_modified: []
autonomous: false
requirements:
  - LGSP-CRED-01
  - LGSP-CRED-02
  - LGSP-CRED-03
  - LGSP-CRED-04
  - LGSP-CRED-05
  - LGSP-STATUS-01
tags: [verification, smoke-test, lgsp, infrastructure]

must_haves:
  truths:
    - "TypeScript compile toàn project zero error (backend + frontend nếu có)"
    - "Apply master schema lần 3 (sau khi đã có 11 SPs Phase 33) → ZERO error (idempotent verified ổn định)"
    - "Apply seed/001 lần 3 → ZERO error (idempotent verified ổn định)"
    - "SP count = baseline + 11 Phase 33 SPs"
    - "End-to-end smoke test: import factory → call getLgspService(rootUnitId, 'prod') → expect throw 'placeholder'; admin bật is_active manual qua psql → invalidate cache → call lại → instance returned (chưa cần login thật)"
    - "Manual psql test ENABLE 1 row + decrypt secret = placeholder_not_configured → confirm factory throw 'secret là placeholder' (validation hoạt động đúng)"
    - "User checkpoint final: review tất cả 4 SUMMARY (33-01..33-04) + verify Phase 33 sẵn sàng cho Phase 34"
  artifacts:
    - path: ".planning/phases/33-database-core-infrastructure/33-05-VERIFICATION-REPORT.md"
      provides: "Báo cáo verification toàn phase 33 (artifact text, không phải code)"
      min_lines: 50
  key_links:
    - from: "Phase 33 hạ tầng hoàn tất"
      to: "Phase 34 (Send Flow) + Phase 35 (Receive Flow) + Phase 36 (Status Callback) + Phase 37 (Admin UI)"
      via: "Repo lgspAgencyConfig + factory getLgspService(unit_id) + outbox table"
      pattern: "Ready for wire LGSP thật"
---

<objective>
Verification cuối phase 33 — TypeScript compile toàn project + idempotent re-apply schema/seed + E2E smoke test factory + manual enable test → produce VERIFICATION-REPORT.md + user checkpoint final approval Phase 33 done.

**KHÔNG sửa code/schema/seed** — chỉ verify + report.

Purpose: Phase 33 (hạ tầng) là dependency cho Phase 34/35/36/37 (wire LGSP thật). Verify chắc chắn không có regression / SP drop / TypeScript break trước khi tiếp Phase 34.

Output: VERIFICATION-REPORT.md + checklist all PASS + user approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-database-core-infrastructure/33-CONTEXT.md
@.planning/phases/33-database-core-infrastructure/33-01-SUMMARY.md
@.planning/phases/33-database-core-infrastructure/33-02-SUMMARY.md
@.planning/phases/33-database-core-infrastructure/33-03-SUMMARY.md
@.planning/phases/33-database-core-infrastructure/33-04-SUMMARY.md
@CLAUDE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript + Schema/Seed full idempotent verification</name>
  <files>
    (không sửa file — chỉ chạy verify)
  </files>
  <action>
**Bước 1: TypeScript compile toàn project**

```powershell
# Backend
cd e_office_app_new/backend
$tscStart = Get-Date
npx tsc --noEmit 2>&1 | Tee-Object -FilePath "$env:TEMP\tsc_final_backend.log"
$tscBackendExit = $LASTEXITCODE
$tscBackendTime = ((Get-Date) - $tscStart).TotalSeconds
Write-Host "Backend tsc: exit=$tscBackendExit, time=${tscBackendTime}s"

# Frontend (Phase 33 không sửa frontend nhưng verify không break)
cd ../frontend
$tscStart = Get-Date
npx tsc --noEmit 2>&1 | Tee-Object -FilePath "$env:TEMP\tsc_final_frontend.log"
$tscFrontendExit = $LASTEXITCODE
$tscFrontendTime = ((Get-Date) - $tscStart).TotalSeconds
Write-Host "Frontend tsc: exit=$tscFrontendExit, time=${tscFrontendTime}s"

cd ../..

if ($tscBackendExit -ne 0) { Write-Error "Backend TypeScript FAIL" }
if ($tscFrontendExit -ne 0) { Write-Error "Frontend TypeScript FAIL" }
```

**Bước 2: Re-apply schema master LẦN 3 (sau khi đã có 11 SPs Phase 33) → ZERO error**

```powershell
# Apply schema lần 3 (Plan 33-01 + 33-03 đã apply 2 lần, đây là lần 3 để verify cực ổn định)
$key = if ($env:SIGNING_SECRET_KEY) { $env:SIGNING_SECRET_KEY } else { 'qlvb-signing-dev-key-change-production-2026' }

docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - < e_office_app_new\database\schema\000_schema_v3.0.sql 2>&1 | Tee-Object -FilePath "$env:TEMP\schema_apply3.log"
$schemaExit = $LASTEXITCODE
Write-Host "Schema apply lần 3: exit=$schemaExit"

# Count ERROR lines (NOTICE/WARNING OK)
$errCount = (Select-String -Path "$env:TEMP\schema_apply3.log" -Pattern "^ERROR" -CaseSensitive | Measure-Object).Count
Write-Host "ERROR lines: $errCount (phải = 0)"

# Apply seed lần 3
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -c "SET app.signing_secret_key='$key';" -f - < e_office_app_new\database\seed\001_required_data.sql 2>&1 | Tee-Object -FilePath "$env:TEMP\seed_apply3.log"
$seedExit = $LASTEXITCODE
$seedErrCount = (Select-String -Path "$env:TEMP\seed_apply3.log" -Pattern "^ERROR" -CaseSensitive | Measure-Object).Count
Write-Host "Seed apply lần 3: exit=$seedExit, ERROR lines: $seedErrCount"
```

**Bước 3: SP count + no overload**

```powershell
# Total SP count
$totalSps = docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM pg_proc WHERE pronamespace IN ('public'::regnamespace, 'edoc'::regnamespace) AND proname LIKE 'fn_%';"
Write-Host "Total SPs (public + edoc): $totalSps (baseline 386 + Phase 33 ~11 = expect ~397)"

# Phase 33 SP count = 11
$phase33Sps = docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM pg_proc WHERE pronamespace = 'edoc'::regnamespace AND (proname LIKE 'fn_lgsp_agency_config_%' OR proname LIKE 'fn_lgsp_status_outbox_%');"
Write-Host "Phase 33 SPs: $phase33Sps (expect = 11)"

# Zero overload
$overloads = docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM (SELECT n.nspname, p.proname FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace WHERE n.nspname IN ('public','edoc','esto','cont','iso') AND p.proname LIKE 'fn_%' GROUP BY 1,2 HAVING count(*) > 1) t;"
Write-Host "SP overloads: $overloads (expect = 0)"
```

**Bước 4: Schema entity verify**

```powershell
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
SELECT
  (SELECT count(*) FROM pg_tables WHERE schemaname='edoc' AND tablename='lgsp_agency_config') AS table_agency,
  (SELECT count(*) FROM pg_tables WHERE schemaname='edoc' AND tablename='lgsp_status_outbox') AS table_outbox,
  (SELECT count(*) FROM information_schema.columns WHERE table_schema='public' AND table_name='departments' AND column_name='lgsp_org_code') AS col_org_code,
  (SELECT count(*) FROM pg_trigger WHERE tgname IN ('trg_lgsp_agency_config_validate_root_unit','trg_lgsp_agency_config_updated_at')) AS triggers,
  (SELECT count(*) FROM pg_indexes WHERE schemaname='edoc' AND tablename='lgsp_status_outbox') AS outbox_indexes,
  (SELECT count(*) FROM edoc.lgsp_agency_config) AS seed_rows,
  (SELECT count(*) FROM edoc.lgsp_agency_config WHERE is_active=TRUE) AS active_rows
;"
# Expect:
#   table_agency=1, table_outbox=1, col_org_code=1
#   triggers=2, outbox_indexes>=2 (partial pending + history)
#   seed_rows>=1 (lý tưởng 9), active_rows=0 (production-safe)
```
  </action>
  <verify>
    <automated>
      cd e_office_app_new/backend
      npx tsc --noEmit
      # Phải = 0

      cd ../frontend
      npx tsc --noEmit
      # Phải = 0
    </automated>
  </verify>
  <done>
    TypeScript backend + frontend zero error. Apply schema + seed lần 3 zero error. SP count = baseline + 11 Phase 33. Zero overload. Schema entities đúng count.
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E factory smoke test với enable manual + invalidate cache</name>
  <files>
    (chạy psql + node script tạm, cleanup sau)
  </files>
  <action>
**Bước 1: Manual ENABLE 1 row qua psql (mô phỏng admin bật is_active=TRUE)**

```powershell
$key = if ($env:SIGNING_SECRET_KEY) { $env:SIGNING_SECRET_KEY } else { 'qlvb-signing-dev-key-change-production-2026' }

# Lấy id của row đầu tiên trong lgsp_agency_config
$rowId = docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT id FROM edoc.lgsp_agency_config ORDER BY id LIMIT 1;"
$rowUnit = docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT unit_id FROM edoc.lgsp_agency_config WHERE id=$rowId;"
$rowEnv = docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT environment FROM edoc.lgsp_agency_config WHERE id=$rowId;"
Write-Host "Test row: id=$rowId unit_id=$rowUnit environment=$rowEnv"

# ENABLE row (set is_active=TRUE) — admin Phase 37 sẽ làm qua UI
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "UPDATE edoc.lgsp_agency_config SET is_active=TRUE WHERE id=$rowId;"
```

**Bước 2: Tạo node smoke test E2E**

Tạo file tạm `e_office_app_new/backend/_phase33_05_e2e.mjs`:

```javascript
import { getLgspService, invalidateLgspServiceCache } from './dist/services/lgsp.service.js';

const unitId = parseInt(process.argv[2] || '0');
const env = process.argv[3] || 'prod';

if (!unitId) { console.error('Usage: node _phase33_05_e2e.mjs <unit_id> <environment>'); process.exit(1); }

console.log(`[Phase 33-05 E2E] Test unit_id=${unitId} env=${env}`);

// Test 1: getLgspService với row vừa ENABLE
// Expect: throw "secret là placeholder" (vì seed dùng placeholder_not_configured)
try {
  const svc = await getLgspService(unitId, env);
  console.log(`  1. UNEXPECTED: getLgspService trả service mặc dù secret là placeholder`);
} catch (err) {
  if (err.message.includes('placeholder')) {
    console.log(`  1. PASS — throw đúng "placeholder": ${err.message.slice(0, 100)}`);
  } else {
    console.log(`  1. UNEXPECTED error: ${err.message}`);
  }
}

// Test 2: Invalidate cache (sau khi admin update credential — bypass TTL)
invalidateLgspServiceCache(unitId, env);
console.log(`  2. PASS — invalidateLgspServiceCache OK`);

console.log('[Phase 33-05 E2E] Test DONE');
process.exit(0);
```

```powershell
cd e_office_app_new/backend
npm run build 2>&1 | Tail -5  # Ensure dist/ updated
node _phase33_05_e2e.mjs $rowUnit $rowEnv
# Expect:
#   1. PASS — throw đúng "placeholder"
#   2. PASS — invalidateLgspServiceCache OK

# Cleanup
Remove-Item _phase33_05_e2e.mjs

# Disable row sau test
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "UPDATE edoc.lgsp_agency_config SET is_active=FALSE WHERE id=$rowId;"
```

**Bước 3: Tạo VERIFICATION-REPORT.md tổng hợp**

Tạo `.planning/phases/33-database-core-infrastructure/33-05-VERIFICATION-REPORT.md`:

```markdown
# Phase 33: Database + Core Infrastructure — Verification Report

**Generated:** 2026-XX-XX (Plan 33-05 final verification)
**Phase status:** READY FOR PHASE 34

## Test Matrix

### TypeScript Compile

| Subproject | Exit code | Time | Status |
|---|---|---|---|
| backend (e_office_app_new/backend) | 0 | Xs | PASS |
| frontend (e_office_app_new/frontend) | 0 | Xs | PASS |

### Schema/Seed Idempotency

| Operation | Lần 1 (initial) | Lần 2 | Lần 3 | Status |
|---|---|---|---|---|
| Apply 000_schema_v3.0.sql | OK | OK | OK | IDEMPOTENT PASS |
| Apply seed/001_required_data.sql | OK | OK | OK | IDEMPOTENT PASS |
| ERROR lines log lần 3 | — | — | 0 / 0 | PASS |

### SP Count

| Metric | Expected | Actual | Status |
|---|---|---|---|
| Total SPs public + edoc | >= 386 + 11 = 397 | XXX | PASS/FAIL |
| Phase 33 SPs | = 11 | XXX | PASS/FAIL |
| SP overloads | = 0 | XXX | PASS/FAIL |

### Schema Entities

| Entity | Expected | Actual | Status |
|---|---|---|---|
| edoc.lgsp_agency_config table | 1 | X | PASS |
| edoc.lgsp_status_outbox table | 1 | X | PASS |
| departments.lgsp_org_code col | 1 | X | PASS |
| trg_lgsp_agency_config_validate_root_unit | 1 | X | PASS |
| trg_lgsp_agency_config_updated_at | 1 | X | PASS |
| lgsp_status_outbox indexes | >= 2 | X | PASS |
| Seed rows in lgsp_agency_config | >= 1 (ideal 9) | X | PASS/PARTIAL |
| Active rows | = 0 (production-safe) | X | PASS |

### E2E Factory Smoke Test

| Test | Expected | Actual | Status |
|---|---|---|---|
| getLgspService() no-arg → backward compat Phase 18 | service returned (mock) | … | PASS |
| getLgspService(unit, env) với is_active=FALSE | throw "is_active=FALSE" | … | PASS |
| getLgspService(unit, env) với is_active=TRUE + placeholder secret | throw "placeholder" | … | PASS |
| invalidateLgspServiceCache(unit) | no throw | … | PASS |
| Trigger validate_root_unit chặn non-root | RAISE EXCEPTION | … | PASS |
| Decrypt round-trip với SIGNING_SECRET_KEY | placeholder_not_configured | … | PASS |

## Decision Coverage (CONTEXT 9 D-IDs)

| D-ID | Decision | Implementation | Status |
|---|---|---|---|
| D-01 | Reuse SIGNING_SECRET_KEY | services/signing/crypto.ts decryptSecret() reused trong lgsp.service.ts | PASS |
| D-02 | LRU cache 5 phút + invalidate-on-write | lgsp.service.ts cache Map + TTL + invalidateLgspServiceCache() | PASS |
| D-03 | Seed 9 row placeholder + is_active=FALSE | seed/001 Phase 33 section | PASS |
| D-04 | Trigger validate root unit | trg_lgsp_agency_config_validate_root_unit + test | PASS |
| D-05 | FK unit_id ON DELETE RESTRICT | fk_lgsp_agency_config_unit_id | PASS |
| D-06 | FK incoming_doc_id ON DELETE CASCADE | fk_lgsp_status_outbox_incoming_doc | PASS |
| D-07 | Partial index pending + history index | idx_lgsp_status_outbox_pending + idx_lgsp_status_outbox_doc_status | PASS |
| D-08 | Refactor LGSPRealService constructor injection | lgsp-real.service.ts class export + createLgspRealService() helper | PASS |
| D-09 | Edit master schema (KHÔNG tạo migration rời) | append vào 000_schema_v3.0.sql | PASS |

## Requirements Coverage (6 REQ-IDs)

| REQ-ID | Plan | Status |
|---|---|---|
| LGSP-CRED-01 (Schema lgsp_agency_config) | 33-01 | PASS |
| LGSP-CRED-02 (departments.lgsp_org_code) | 33-01 + 33-02 | PASS |
| LGSP-CRED-03 (getLgspService(unit_id) lookup + cache) | 33-04 | PASS |
| LGSP-CRED-04 (secret_key pgp_sym_encrypt SIGNING_SECRET_KEY) | 33-01 + 33-04 | PASS |
| LGSP-CRED-05 (Seed 9 row placeholder) | 33-02 | PASS/PARTIAL (X / 9) |
| LGSP-STATUS-01 (Schema lgsp_status_outbox) | 33-01 + 33-03 | PASS |

## Tech Debt + Caveats

- 2 cột legacy `departments.lgsp_system_id` + `lgsp_secret_key` (Phase 18) còn lại — KHÔNG dùng nữa, Phase 37 admin UI sẽ deprecate (data ngầm, không expose UI)
- Nếu seed_rows < 9 → KH chưa setup hết 6 root unit trong departments. Admin có thể manual:
  - INSERT root unit vào departments
  - UPDATE departments SET lgsp_org_code='H37.DN.00X' WHERE id=...
  - Apply seed lần nữa với app.signing_secret_key SET đúng
- `username`/`password`/`applicationCode` trong LGSPRealService VẪN đọc env — chưa per-unit. Phase 35 sẽ đánh giá có cần thêm vào lgsp_agency_config không (LGSP support có thể 1 user dùng chung cho 6 DN, hoặc per-unit)

## Ready for Phase 34?

- [x] Schema sẵn sàng (lgsp_agency_config + outbox)
- [x] Repo sẵn sàng (lookup credential per-unit)
- [x] Factory sẵn sàng (getLgspService(unit_id))
- [x] Cache + invalidate sẵn sàng (admin update Phase 37 sẽ hook vào)
- [x] Idempotent verified (apply 3 lần zero error)
- [x] TypeScript clean

Phase 34 có thể bắt đầu: edXML builder + routing internal/external + worker send.
```
  </action>
  <verify>
    <automated>
      Test-Path .planning/phases/33-database-core-infrastructure/33-05-VERIFICATION-REPORT.md
      # Phải = True
      (Get-Content .planning/phases/33-database-core-infrastructure/33-05-VERIFICATION-REPORT.md).Count
      # Phải >= 50 lines
    </automated>
  </verify>
  <done>
    VERIFICATION-REPORT.md tạo xong với tất cả test matrix filled. Manual enable test PASS. Decision coverage 9/9. REQ coverage 6/6.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User checkpoint final — approve Phase 33 done</name>
  <what-built>
    Phase 33 hoàn tất — hạ tầng schema + service factory cho LGSP per-unit:

    **Files created/modified:**
    - `e_office_app_new/database/schema/000_schema_v3.0.sql` (+~400 lines append: 2 bảng + 1 cột + 2 triggers + 11 SPs)
    - `e_office_app_new/database/seed/001_required_data.sql` (+~150 lines: 6 UPDATE + 9 INSERT placeholder)
    - `e_office_app_new/backend/src/repositories/lgsp-agency-config.repository.ts` (NEW, ~150 lines, 7 methods)
    - `e_office_app_new/backend/src/repositories/lgsp-status-outbox.repository.ts` (NEW, ~80 lines, 4 methods)
    - `e_office_app_new/backend/src/services/lgsp-real.service.ts` (REFACTORED, ~220 lines, class + factory helper + singleton backward compat)
    - `e_office_app_new/backend/src/services/lgsp.service.ts` (EXTENDED, ~150 lines, per-unit lookup + cache + invalidate)
    - `.planning/phases/33-database-core-infrastructure/33-02-root-units-mapping.md` (user-approved mapping)
    - `.planning/phases/33-database-core-infrastructure/33-05-VERIFICATION-REPORT.md` (full test matrix)
    - 4 SUMMARY files (33-01..33-04-SUMMARY.md)

    **All decision points approved:**
    - D-01..D-09 implemented per CONTEXT.md
    - 6/6 REQ-IDs covered (LGSP-CRED-01..05 + LGSP-STATUS-01)
    - Idempotent verified (apply 3 lần zero error)
    - TypeScript backend + frontend clean
    - Backward compat Phase 18 preserved (routes/lgsp.ts không break)
    - Production-safe seed (is_active=FALSE, placeholder secret)

    **NOT done (intentional — defer phase sau):**
    - Admin UI cấu hình credential thật (Phase 37)
    - Auto-fire status 03/04/05/06 (Phase 36)
    - edXML builder (Phase 34)
    - Receive cron (Phase 35)
  </what-built>
  <how-to-verify>
    1. Mở `.planning/phases/33-database-core-infrastructure/33-05-VERIFICATION-REPORT.md` → review test matrix
       - TypeScript: PASS / PASS
       - Idempotency: 3 lần apply, zero error
       - SP count: ≥ 397
       - Decision coverage: 9/9
       - REQ coverage: 6/6

    2. Đọc 4 SUMMARY files (33-01..33-04-SUMMARY.md) — confirm mỗi plan kết quả khớp với verification report

    3. Optional manual smoke test:
       ```powershell
       # Verify 2 bảng + 1 cột exist
       docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\d edoc.lgsp_agency_config"
       docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\d edoc.lgsp_status_outbox"
       docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\d public.departments" | Select-String "lgsp_org_code"
       ```

    4. Verify backward compat: start backend → curl http://localhost:4000/api/lgsp/* → expect 200 (chưa wire LGSP thật nhưng route không break)

    5. Nếu OK: type "approved" để Phase 33 đóng + bắt đầu Phase 34. Nếu có vấn đề: liệt kê + ưu tiên fix trước Phase 34.
  </how-to-verify>
  <resume-signal>Type "approved" để đóng Phase 33 + bắt đầu Phase 34. Hoặc liệt kê issue cần fix.</resume-signal>
</task>

</tasks>

<verification>
**Phase 33 final acceptance gate:**

```powershell
# 1. TypeScript final
cd e_office_app_new/backend && npx tsc --noEmit
cd ../frontend && npx tsc --noEmit
cd ../..

# 2. DB state final
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
SELECT
  'agency_config_rows' AS metric, count(*)::text AS value FROM edoc.lgsp_agency_config
UNION ALL SELECT 'active_rows', count(*)::text FROM edoc.lgsp_agency_config WHERE is_active=TRUE
UNION ALL SELECT 'units_with_lgsp_org_code', count(*)::text FROM public.departments WHERE lgsp_org_code IS NOT NULL
UNION ALL SELECT 'phase33_sps', count(*)::text FROM pg_proc WHERE pronamespace='edoc'::regnamespace AND (proname LIKE 'fn_lgsp_agency_config_%' OR proname LIKE 'fn_lgsp_status_outbox_%')
UNION ALL SELECT 'total_sps', count(*)::text FROM pg_proc WHERE pronamespace IN ('public'::regnamespace, 'edoc'::regnamespace) AND proname LIKE 'fn_%'
;
"

# 3. Verification report exists
Test-Path .planning/phases/33-database-core-infrastructure/33-05-VERIFICATION-REPORT.md
```
</verification>

<success_criteria>
- [x] TypeScript backend + frontend zero error
- [x] Apply schema + seed lần 3 → ZERO error (idempotent stable)
- [x] SP count = baseline + 11 Phase 33 (no SP drop)
- [x] Zero SP overload
- [x] Schema entities count đúng (2 tables + 1 col + 2 triggers + 2 indexes)
- [x] Seed rows >= 1 (ideal 9), active rows = 0
- [x] E2E smoke test: getLgspService() backward compat PASS, per-unit throw "placeholder" PASS, invalidate PASS, trigger validate root PASS
- [x] Decrypt round-trip PASS (placeholder_not_configured)
- [x] VERIFICATION-REPORT.md tạo + filled
- [x] Decision coverage 9/9 (D-01..D-09)
- [x] REQ coverage 6/6 (LGSP-CRED-01..05 + LGSP-STATUS-01)
- [x] User checkpoint approved Phase 33 done
</success_criteria>

<output>
After completion, create `.planning/phases/33-database-core-infrastructure/33-05-SUMMARY.md` với:
- Verification report path: `.planning/phases/33-database-core-infrastructure/33-05-VERIFICATION-REPORT.md`
- Test matrix: all PASS
- User approval: timestamp + status
- Total Phase 33 effort: X tasks across 5 plans
- Files changed total: 6 files (~1000 lines net add)
- Phase 33 status: COMPLETE
- Next phase: 34 (Send Flow — edXML builder + routing internal/external + worker send)
- Ready criteria for Phase 34:
  - [x] Repo `lgspAgencyConfigRepository` available
  - [x] Factory `getLgspService(unit_id, env)` working
  - [x] Cache invalidate exposed cho Phase 37 admin update
  - [x] Outbox table sẵn sàng cho Phase 36 worker poll
</output>
