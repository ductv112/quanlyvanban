---
phase: 33-database-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/database/schema/000_schema_v3.0.sql
autonomous: true
requirements:
  - LGSP-CRED-01
  - LGSP-CRED-02
  - LGSP-CRED-04
  - LGSP-STATUS-01
tags: [database, schema, lgsp, infrastructure]

must_haves:
  truths:
    - "psql \\d edoc.lgsp_agency_config thấy đủ cột (unit_id, environment, system_id, secret_key_encrypted, base_url, is_active, last_synced_at, last_sync_error, created_at, updated_at) + UNIQUE(unit_id, environment)"
    - "psql \\d public.departments thấy cột lgsp_org_code VARCHAR(13) (NULL cho non-LGSP unit)"
    - "psql \\d edoc.lgsp_status_outbox thấy đủ cột (incoming_doc_id, target_status, payload JSONB, sent_at, sent_status, retry_count, next_retry_at, error_message, created_at) + 2 index (partial pending + history per doc)"
    - "Trigger fn_lgsp_agency_config_validate_root_unit() BEFORE INSERT/UPDATE chặn được unit_id không phải root (parent_id IS NULL)"
    - "Apply master schema lần 2 trên DB đã có schema → ZERO error (idempotent)"
    - "SP count sau apply ≥ baseline (≥ 386 SPs trước Phase 33)"
  artifacts:
    - path: "e_office_app_new/database/schema/000_schema_v3.0.sql"
      provides: "Master schema v3.0 + Phase 33 LGSP infrastructure tables"
      contains: "edoc.lgsp_agency_config, edoc.lgsp_status_outbox, departments.lgsp_org_code, fn_lgsp_agency_config_validate_root_unit"
  key_links:
    - from: "edoc.lgsp_agency_config.unit_id"
      to: "public.departments(id)"
      via: "FK ON DELETE RESTRICT + trigger validate parent_id IS NULL"
      pattern: "fk_lgsp_agency_config_unit_id"
    - from: "edoc.lgsp_status_outbox.incoming_doc_id"
      to: "edoc.incoming_docs(id)"
      via: "FK ON DELETE CASCADE"
      pattern: "fk_lgsp_status_outbox_incoming_doc"
    - from: "edoc.lgsp_agency_config.secret_key_encrypted"
      to: "SIGNING_SECRET_KEY env var"
      via: "pgp_sym_encrypt(plaintext, SIGNING_SECRET_KEY)"
      pattern: "BYTEA NOT NULL"
---

<objective>
Append Phase 33 LGSP infrastructure schema vào master file `database/schema/000_schema_v3.0.sql`: 2 bảng mới (`edoc.lgsp_agency_config`, `edoc.lgsp_status_outbox`) + 1 cột mới (`departments.lgsp_org_code`) + 1 trigger validate root unit. Toàn bộ idempotent — apply lần 2 phải zero error.

Purpose: Hạ tầng schema chuẩn bị cho Phase 33-37 wire LGSP thật cho 6 DN Lạng Sơn. Phase 33-02 sẽ seed 9 row placeholder + UPDATE `lgsp_org_code` cho 6 root unit. Phase 33-03 dùng schema này build repository. Phase 34/35/36 wire worker + outbox consume.

Output: File master schema mở rộng ~200-300 dòng + DB đã apply + verify idempotent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-database-core-infrastructure/33-CONTEXT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@e_office_app_new/database/schema/000_schema_v3.0.sql
@e_office_app_new/database/archive/v2.0-incrementals/040_signing_schema.sql

<interfaces>
<!-- Existing departments table (relevant columns) — Phase 33 ADD COLUMN lgsp_org_code only -->

From `public.departments` (line 15190 master schema):
```sql
CREATE TABLE IF NOT EXISTS public.departments (
    id integer NOT NULL,
    parent_id integer,                          -- NULL = root unit (= 1 trong 6 DN)
    code character varying(50),
    name character varying(200) NOT NULL,
    is_unit boolean DEFAULT false,
    lgsp_system_id character varying(50),       -- LEGACY (Phase 18 cũ) — KHÔNG ĐỘNG
    lgsp_secret_key character varying(100),     -- LEGACY (Phase 18 cũ) — KHÔNG ĐỘNG
    is_locked boolean DEFAULT false,
    is_deleted boolean DEFAULT false,
    -- ... timestamps + audit cols
);
-- FK: departments.parent_id → departments(id) (self-ref tree)
```

Phase 33 ADD COLUMN `lgsp_org_code VARCHAR(13)` — chỉ root unit (parent_id IS NULL) sẽ có giá trị H37.DN.001..006. Cột này KHÁC `lgsp_system_id` legacy: `lgsp_org_code` là Vietnamese-government standard org code (QĐ 28/2018), `system_id` (sẽ trong `lgsp_agency_config`) là LGSP application code dùng để auth API.

From `edoc.incoming_docs`:
```sql
-- Bảng VB đến hiện tại — Phase 33 chỉ refer qua FK trong lgsp_status_outbox
-- KHÔNG ALTER bảng này
CREATE TABLE IF NOT EXISTS edoc.incoming_docs (
    id bigint NOT NULL,
    -- ... nhiều cột
);
```

Pattern reference từ `signing_provider_config`:
```sql
-- Encrypted credential pattern (line 15534 master schema):
-- client_secret BYTEA NOT NULL  -- pgp_sym_encrypt(plaintext, SIGNING_SECRET_KEY)
-- Partial unique index pattern (line 18515):
-- CREATE UNIQUE INDEX uq_signing_provider_config_active
--   ON public.signing_provider_config (is_active) WHERE is_active = TRUE;
```
</interfaces>

**Reference docs:**
- CLAUDE.md § "DB Migration Strategy (v2.0+)" — append vào master file + idempotent + verify SP count
- CLAUDE.md § "Deploy Pitfalls" #5 (FK forward reference), #14 (SIGNING_SECRET_KEY)
- `docs/Trục EDOC Lạng Sơn - QLVB Doanh nghiệp/QLVBDNAgencies.xlsx` — 6 org_code H37.DN.001..006

**Project Skills check:** Không có project skills (`.claude/skills/` empty per CLAUDE.md output).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append schema mới (2 bảng + 1 cột + trigger) vào master file</name>
  <files>
    e_office_app_new/database/schema/000_schema_v3.0.sql
  </files>
  <action>
**Append vào CUỐI file `e_office_app_new/database/schema/000_schema_v3.0.sql`** (sau dòng cuối cùng hiện tại — sau `fn_auth_get_password_hash`). KHÔNG xóa hoặc sửa nội dung hiện có. KHÔNG tạo file migration rời (`database/migrations/*.sql`) — vi phạm CLAUDE.md DB Migration Strategy.

Nội dung append (toàn bộ wrap trong header section Phase 33):

```sql
-- ============================================================================
-- Phase 33: LGSP Production Go-live — DB Infrastructure (REQ LGSP-CRED-01/02/04, LGSP-STATUS-01)
-- Goal: Per-unit credential schema cho 6 DN Lạng Sơn + outbox pattern cho status callback
-- Pattern: mirror signing_provider_config (encrypted BYTEA via pgp_sym_encrypt + SIGNING_SECRET_KEY)
-- ============================================================================

-- ─── 1. ALTER public.departments: ADD COLUMN lgsp_org_code (idempotent) ───
ALTER TABLE public.departments
  ADD COLUMN IF NOT EXISTS lgsp_org_code VARCHAR(13);

COMMENT ON COLUMN public.departments.lgsp_org_code IS
  'Phase 33 — Mã định danh QĐ 28/2018 (VD: H37.DN.001). Chỉ root unit của 6 DN Lạng Sơn có giá trị, NULL cho subtree.';

-- Index phục vụ Phase 35 receive flow (find unit_id từ MessageHeader.To.OrganId)
CREATE INDEX IF NOT EXISTS idx_departments_lgsp_org_code
  ON public.departments (lgsp_org_code)
  WHERE lgsp_org_code IS NOT NULL;

-- ─── 2. edoc.lgsp_agency_config — Per-unit credential (REQ LGSP-CRED-01/04) ───
CREATE TABLE IF NOT EXISTS edoc.lgsp_agency_config (
  id                   BIGSERIAL PRIMARY KEY,
  unit_id              INTEGER NOT NULL,                          -- FK departments(id) root unit only — enforce qua trigger
  environment          VARCHAR(10) NOT NULL,                      -- 'sandbox' | 'prod'
  system_id            VARCHAR(13) NOT NULL,                      -- LGSP system_id (= lgsp_org_code, VD: H37.DN.001)
  secret_key_encrypted BYTEA NOT NULL,                            -- pgp_sym_encrypt(plaintext, SIGNING_SECRET_KEY)
  base_url             VARCHAR(500) NOT NULL,                     -- 'https://apiltvb.langson.gov.vn' | 'https://trucltvb.langson.gov.vn/apithunghiem'
  is_active            BOOLEAN NOT NULL DEFAULT FALSE,            -- Admin bật qua UI Phase 37 sau khi nhập credential thật
  last_synced_at       TIMESTAMPTZ,                               -- Phase 35 cron update sau mỗi vòng receive
  last_sync_error      TEXT,                                      -- Error message vòng cron gần nhất (NULL nếu OK)
  created_by           INT REFERENCES public.staff(id),
  created_at           TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_by           INT REFERENCES public.staff(id),
  updated_at           TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  CONSTRAINT chk_lgsp_agency_config_environment CHECK (environment IN ('sandbox','prod'))
);

-- UNIQUE constraint per CONTEXT D-09 + REQ LGSP-CRED-01
DO $$
BEGIN
  BEGIN
    ALTER TABLE edoc.lgsp_agency_config
      ADD CONSTRAINT uq_lgsp_agency_config_unit_env UNIQUE (unit_id, environment);
  EXCEPTION
    WHEN duplicate_object THEN NULL;        -- 42710
    WHEN duplicate_table THEN NULL;         -- 42P07
    WHEN invalid_table_definition THEN NULL; -- 42P16
    WHEN duplicate_column THEN NULL;        -- 42701
  END;
END $$;

-- FK to departments — ON DELETE RESTRICT (D-05): chặn xóa root unit nếu còn LGSP config
DO $$
BEGIN
  BEGIN
    ALTER TABLE edoc.lgsp_agency_config
      ADD CONSTRAINT fk_lgsp_agency_config_unit_id
      FOREIGN KEY (unit_id) REFERENCES public.departments(id) ON DELETE RESTRICT;
  EXCEPTION
    WHEN duplicate_object THEN NULL;
    WHEN duplicate_table THEN NULL;
    WHEN invalid_table_definition THEN NULL;
    WHEN duplicate_column THEN NULL;
  END;
END $$;

COMMENT ON TABLE edoc.lgsp_agency_config IS
  'Phase 33 — Cấu hình LGSP per root unit (6 DN Lạng Sơn). secret_key_encrypted dùng SIGNING_SECRET_KEY (reuse signing module key).';
COMMENT ON COLUMN edoc.lgsp_agency_config.secret_key_encrypted IS
  'BYTEA = pgp_sym_encrypt(plaintext, SIGNING_SECRET_KEY). Decrypt bằng decryptSecret() trong services/signing/crypto.ts.';

-- ─── 3. Trigger validate root unit (D-04) — chặn INSERT/UPDATE với non-root unit ───
CREATE OR REPLACE FUNCTION edoc.fn_lgsp_agency_config_validate_root_unit()
RETURNS TRIGGER
LANGUAGE plpgsql
AS $func$
DECLARE
  v_parent INTEGER;
  v_exists BOOLEAN;
BEGIN
  -- Verify unit_id tồn tại trong departments + parent_id IS NULL (root)
  SELECT parent_id, TRUE INTO v_parent, v_exists
    FROM public.departments
    WHERE id = NEW.unit_id;

  IF NOT FOUND THEN
    RAISE EXCEPTION 'unit_id=% không tồn tại trong departments', NEW.unit_id
      USING ERRCODE = '23503'; -- foreign_key_violation
  END IF;

  IF v_parent IS NOT NULL THEN
    RAISE EXCEPTION 'unit_id=% không phải root unit (parent_id=% phải NULL). LGSP chỉ cấu hình cho 6 DN cấp cao nhất.',
      NEW.unit_id, v_parent
      USING ERRCODE = '23514'; -- check_violation
  END IF;

  RETURN NEW;
END;
$func$;

DROP TRIGGER IF EXISTS trg_lgsp_agency_config_validate_root_unit ON edoc.lgsp_agency_config;
CREATE TRIGGER trg_lgsp_agency_config_validate_root_unit
  BEFORE INSERT OR UPDATE OF unit_id ON edoc.lgsp_agency_config
  FOR EACH ROW
  EXECUTE FUNCTION edoc.fn_lgsp_agency_config_validate_root_unit();

-- ─── 4. Trigger auto-update updated_at (mirror trg_departments_updated_at pattern) ───
DROP TRIGGER IF EXISTS trg_lgsp_agency_config_updated_at ON edoc.lgsp_agency_config;
CREATE TRIGGER trg_lgsp_agency_config_updated_at
  BEFORE UPDATE ON edoc.lgsp_agency_config
  FOR EACH ROW
  EXECUTE FUNCTION public.fn_set_updated_at();
-- Nếu public.fn_set_updated_at chưa tồn tại, tạo inline (idempotent — chắc chắn đã có cho trg_departments_updated_at)

-- ─── 5. edoc.lgsp_status_outbox — Outbox pattern status callback (REQ LGSP-STATUS-01) ───
CREATE TABLE IF NOT EXISTS edoc.lgsp_status_outbox (
  id               BIGSERIAL PRIMARY KEY,
  incoming_doc_id  BIGINT NOT NULL,                                -- FK incoming_docs(id) ON DELETE CASCADE
  target_status    VARCHAR(2) NOT NULL,                            -- '01' đã gửi, '02' từ chối, '03' tiếp nhận, '04' phân công, '05' đang xử lý, '06' hoàn thành, '13' lấy lại, '15' đồng ý lấy lại, '16' từ chối lấy lại
  payload          JSONB NOT NULL DEFAULT '{}'::jsonb,             -- Phase 36 worker đọc: { lgsp_doc_id, sender_org_code, reason?, ... }
  sent_status      VARCHAR(10) NOT NULL DEFAULT 'pending',         -- 'pending' | 'success' | 'error'
  sent_at          TIMESTAMPTZ,                                    -- Thời điểm worker đẩy thành công lên trục
  retry_count      INT NOT NULL DEFAULT 0,                         -- Số lần worker đã retry (max 5 per LGSP-STATUS-09)
  next_retry_at    TIMESTAMPTZ,                                    -- Worker poll WHERE sent_status='pending' AND (next_retry_at IS NULL OR next_retry_at <= NOW())
  error_message    TEXT,                                           -- Error gần nhất (NULL nếu success)
  created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  CONSTRAINT chk_lgsp_status_outbox_target_status CHECK (target_status IN ('01','02','03','04','05','06','13','15','16')),
  CONSTRAINT chk_lgsp_status_outbox_sent_status CHECK (sent_status IN ('pending','success','error'))
);

-- FK ON DELETE CASCADE (D-06): outbox event mồ côi không có nghĩa khi VB nguồn bị xóa
DO $$
BEGIN
  BEGIN
    ALTER TABLE edoc.lgsp_status_outbox
      ADD CONSTRAINT fk_lgsp_status_outbox_incoming_doc
      FOREIGN KEY (incoming_doc_id) REFERENCES edoc.incoming_docs(id) ON DELETE CASCADE;
  EXCEPTION
    WHEN duplicate_object THEN NULL;
    WHEN duplicate_table THEN NULL;
    WHEN invalid_table_definition THEN NULL;
    WHEN duplicate_column THEN NULL;
  END;
END $$;

-- Index 1: Partial index cho worker poll (D-07) — index nhỏ, optimize cực tốt
CREATE INDEX IF NOT EXISTS idx_lgsp_status_outbox_pending
  ON edoc.lgsp_status_outbox (created_at)
  WHERE sent_status = 'pending';

-- Index 2: Query history trạng thái của 1 VB cho UI VB đến chi tiết
CREATE INDEX IF NOT EXISTS idx_lgsp_status_outbox_doc_status
  ON edoc.lgsp_status_outbox (incoming_doc_id, target_status, sent_at DESC);

COMMENT ON TABLE edoc.lgsp_status_outbox IS
  'Phase 33 — Outbox queue cho LGSP status callback (9 mã QĐ 28). Phase 36 worker poll mỗi 30s, exponential backoff.';
COMMENT ON COLUMN edoc.lgsp_status_outbox.target_status IS
  '9 mã QĐ 28/2018: 01 đã gửi, 02 từ chối tiếp nhận, 03 đã tiếp nhận, 04 phân công, 05 đang xử lý, 06 hoàn thành, 13 lấy lại, 15 đồng ý lấy lại, 16 từ chối lấy lại.';

-- ─── 6. Đảm bảo function public.fn_set_updated_at tồn tại (idempotent guard) ───
-- Pattern đã có ở trg_departments_updated_at nhưng safe-guard tạo lại nếu chưa
CREATE OR REPLACE FUNCTION public.fn_set_updated_at()
RETURNS TRIGGER
LANGUAGE plpgsql
AS $func$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$func$;

DO $$ BEGIN
  RAISE NOTICE 'Phase 33 schema: lgsp_agency_config + lgsp_status_outbox + departments.lgsp_org_code + trigger validate_root_unit — OK';
END $$;
```

**Critical rules:**
- DROP TRIGGER IF EXISTS TRƯỚC CREATE TRIGGER (idempotent)
- CREATE OR REPLACE FUNCTION cho all SPs/triggers (idempotent)
- ADD CONSTRAINT trong DO block catch 4 SQLSTATE per CLAUDE.md pitfall #5
- KHÔNG dùng `DROP FUNCTION ... LIKE 'fn_%'` (CLAUDE.md DB Migration Strategy WARNING)
- KHÔNG tạo file `database/migrations/*.sql` rời
- KHÔNG đụng `lgsp_system_id` và `lgsp_secret_key` legacy columns trong departments (Phase 18 cũ)
  </action>
  <verify>
    <automated>
      # Verify file size tăng (~200-300 dòng append)
      wc -l e_office_app_new/database/schema/000_schema_v3.0.sql
      # Phải > 28400 (baseline 28304 + ~150-200 dòng Phase 33)

      # Verify section header Phase 33 có trong file
      grep -c "Phase 33: LGSP Production Go-live" e_office_app_new/database/schema/000_schema_v3.0.sql
      # Phải = 1

      # Verify 3 entities key
      grep -c "CREATE TABLE IF NOT EXISTS edoc.lgsp_agency_config" e_office_app_new/database/schema/000_schema_v3.0.sql
      grep -c "CREATE TABLE IF NOT EXISTS edoc.lgsp_status_outbox" e_office_app_new/database/schema/000_schema_v3.0.sql
      grep -c "ADD COLUMN IF NOT EXISTS lgsp_org_code" e_office_app_new/database/schema/000_schema_v3.0.sql
      # Mỗi cái phải = 1
    </automated>
  </verify>
  <done>
    File `000_schema_v3.0.sql` đã append nội dung Phase 33 ở cuối (sau function `fn_auth_get_password_hash` hiện tại); chưa apply vào DB (Task 2 sẽ làm).
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply schema vào DB qua reset-db-windows.ps1 + verify idempotent (apply lần 2)</name>
  <files>
    (không sửa file — chỉ chạy script + verify)
  </files>
  <action>
**Bước 1: Set SIGNING_SECRET_KEY env var (nếu chưa)**

```powershell
# Verify SIGNING_SECRET_KEY có trong backend/.env
Select-String -Path "e_office_app_new\backend\.env" -Pattern "^SIGNING_SECRET_KEY="
# Phải thấy SIGNING_SECRET_KEY=... (>= 16 ký tự)

# Nếu chưa có, copy từ .env.example:
# (script reset-db-windows.ps1 tự fallback dùng default key nếu env var không set)
```

**Bước 2: Reset DB clean + apply schema mới (lần 1)**

```powershell
# Chạy reset-db-windows.ps1 -Force để skip prompt (CLAUDE.md pitfall #11)
# Script tự động: drop schemas → init schemas → apply 000_schema_v3.0.sql → seed 001 + 002
.\deploy\reset-db-windows.ps1 -Force

# Verify exit code 0 + không error đỏ trong output
$LASTEXITCODE  # Phải = 0
```

**Bước 3: Verify 3 entities key tồn tại trong DB**

```powershell
$env:PGPASSWORD = 'qlvb_dev_pass'  # hoặc lấy từ docker-compose env

# Verify table lgsp_agency_config
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\d edoc.lgsp_agency_config"
# Phải thấy: id, unit_id, environment, system_id, secret_key_encrypted, base_url, is_active, last_synced_at, last_sync_error, created_at, updated_at
# + UNIQUE uq_lgsp_agency_config_unit_env
# + FK fk_lgsp_agency_config_unit_id

# Verify table lgsp_status_outbox
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\d edoc.lgsp_status_outbox"
# Phải thấy: id, incoming_doc_id, target_status, payload, sent_status, sent_at, retry_count, next_retry_at, error_message, created_at
# + 2 index: idx_lgsp_status_outbox_pending (partial), idx_lgsp_status_outbox_doc_status
# + FK fk_lgsp_status_outbox_incoming_doc (CASCADE)

# Verify column departments.lgsp_org_code
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\d public.departments" | Select-String "lgsp_org_code"
# Phải thấy: lgsp_org_code | character varying(13) |

# Verify trigger validate root unit
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "SELECT tgname FROM pg_trigger WHERE tgname = 'trg_lgsp_agency_config_validate_root_unit';"
# Phải thấy 1 row
```

**Bước 4: Test trigger validate root unit chặn được non-root unit**

```powershell
# Lấy 1 department có parent_id NOT NULL (non-root) để test
$nonRoot = docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT id FROM public.departments WHERE parent_id IS NOT NULL LIMIT 1;"
Write-Host "Non-root unit id để test: $nonRoot"

# INSERT với non-root unit → phải FAIL với check_violation
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "INSERT INTO edoc.lgsp_agency_config (unit_id, environment, system_id, secret_key_encrypted, base_url) VALUES ($nonRoot, 'sandbox', 'TEST.001', 'placeholder'::bytea, 'https://test.local');"
# Phải FAIL với: ERROR:  unit_id=X không phải root unit (parent_id=Y phải NULL)

# INSERT với root unit (parent_id IS NULL) → phải PASS
$root = docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT id FROM public.departments WHERE parent_id IS NULL LIMIT 1;"
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "INSERT INTO edoc.lgsp_agency_config (unit_id, environment, system_id, secret_key_encrypted, base_url) VALUES ($root, 'sandbox', 'TEST.001', pgp_sym_encrypt('test', current_setting('app.signing_secret_key', TRUE)), 'https://test.local');"
# CÓ THỂ FAIL nếu app.signing_secret_key chưa set trong session — dùng key cứng:
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "INSERT INTO edoc.lgsp_agency_config (unit_id, environment, system_id, secret_key_encrypted, base_url) VALUES ($root, 'sandbox', 'TEST.001', pgp_sym_encrypt('test', 'qlvb-signing-dev-key-change-production-2026'), 'https://test.local');"
# Phải PASS

# Cleanup test row
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "DELETE FROM edoc.lgsp_agency_config WHERE system_id = 'TEST.001';"
```

**Bước 5: Apply schema LẦN 2 → ZERO error (idempotent verification, CLAUDE.md mandatory)**

```powershell
# Apply schema master lần 2 trên DB đã có sẵn schema từ lần 1
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - < e_office_app_new\database\schema\000_schema_v3.0.sql 2>&1 | Tee-Object -FilePath "$env:TEMP\schema_apply2.log"

# Verify exit code 0 + ZERO ERROR trong log (NOTICE OK, WARNING OK, ERROR KHÔNG OK)
$LASTEXITCODE  # Phải = 0
Select-String -Path "$env:TEMP\schema_apply2.log" -Pattern "^ERROR" -CaseSensitive
# Phải EMPTY (zero ERROR line)
```

**Bước 6: Verify SP count ≥ baseline**

```powershell
# Count SPs trong public + edoc schemas (CLAUDE.md DB Migration: baseline 386 + Phase 33 thêm ~2-3 SPs)
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM pg_proc WHERE pronamespace IN ('public'::regnamespace, 'edoc'::regnamespace) AND proname LIKE 'fn_%';"
# Phải >= 386 (Phase 33 chưa thêm SPs nghiệp vụ ngoài fn_lgsp_agency_config_validate_root_unit + fn_set_updated_at — ít nhất bằng baseline)

# Verify zero SP overload (duplicate signature)
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM (SELECT n.nspname, p.proname, count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace WHERE n.nspname IN ('public','edoc','esto','cont','iso') AND p.proname LIKE 'fn_%' GROUP BY 1,2 HAVING count(*) > 1) t;"
# Phải = 0
```
  </action>
  <verify>
    <automated>
      # Combined integration check (output mỗi bước, fail-fast nếu bất kỳ verify fail)
      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\dt edoc.lgsp_agency_config edoc.lgsp_status_outbox" 2>&1 | Select-String "lgsp_agency_config|lgsp_status_outbox"
      # Phải thấy 2 dòng (2 bảng)

      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM information_schema.columns WHERE table_schema = 'public' AND table_name = 'departments' AND column_name = 'lgsp_org_code';"
      # Phải = 1
    </automated>
  </verify>
  <done>
    DB có 2 bảng mới (`edoc.lgsp_agency_config`, `edoc.lgsp_status_outbox`), cột `departments.lgsp_org_code` đã add, trigger validate root unit hoạt động (chặn non-root + cho phép root), apply schema lần 2 zero error, SP count ≥ baseline 386.
  </done>
</task>

</tasks>

<verification>
**Sau khi 2 tasks done, integration check Wave 1:**

```powershell
# 1. Schema file size tăng
$lines = (Get-Content e_office_app_new/database/schema/000_schema_v3.0.sql).Count
Write-Host "Schema lines: $lines (baseline 28304, expect ~28450-28500)"

# 2. 2 bảng + 1 cột + trigger tồn tại trong DB
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
SELECT 'lgsp_agency_config' AS entity, count(*) AS exists FROM pg_tables WHERE schemaname='edoc' AND tablename='lgsp_agency_config'
UNION ALL SELECT 'lgsp_status_outbox', count(*) FROM pg_tables WHERE schemaname='edoc' AND tablename='lgsp_status_outbox'
UNION ALL SELECT 'departments.lgsp_org_code', count(*) FROM information_schema.columns WHERE table_schema='public' AND table_name='departments' AND column_name='lgsp_org_code'
UNION ALL SELECT 'trigger validate_root', count(*) FROM pg_trigger WHERE tgname='trg_lgsp_agency_config_validate_root_unit'
UNION ALL SELECT 'trigger updated_at', count(*) FROM pg_trigger WHERE tgname='trg_lgsp_agency_config_updated_at'
;
"
# Tất cả phải = 1

# 3. Apply lần 2 zero error (idempotent)
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - < e_office_app_new\database\schema\000_schema_v3.0.sql > $env:TEMP\apply2.log 2>&1
if ($LASTEXITCODE -ne 0) { Write-Error "Idempotent check FAIL — apply lần 2 có error. Xem $env:TEMP\apply2.log" }

# 4. Zero SP overload
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM (SELECT n.nspname, p.proname FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace WHERE n.nspname IN ('public','edoc') AND p.proname LIKE 'fn_%' GROUP BY 1,2 HAVING count(*) > 1) t;"
# Phải = 0
```
</verification>

<success_criteria>
- [x] Cột `public.departments.lgsp_org_code VARCHAR(13)` đã add (idempotent)
- [x] Bảng `edoc.lgsp_agency_config` có đủ 12 cột + UNIQUE(unit_id, environment) + FK RESTRICT to departments
- [x] Bảng `edoc.lgsp_status_outbox` có đủ 10 cột + 2 index (partial pending + history per doc) + FK CASCADE to incoming_docs
- [x] Trigger `trg_lgsp_agency_config_validate_root_unit` BEFORE INSERT/UPDATE chặn non-root unit (test pass)
- [x] Trigger `trg_lgsp_agency_config_updated_at` BEFORE UPDATE auto-set NOW()
- [x] Apply schema lần 2 → ZERO error (idempotent verified)
- [x] SP count sau apply ≥ 386 baseline (Phase 11.1 v2.0.2)
- [x] Zero SP overload (no duplicate signature)
- [x] Không tạo file `database/migrations/*.sql` rời
- [x] Không đụng `lgsp_system_id`/`lgsp_secret_key` legacy columns
</success_criteria>

<output>
After completion, create `.planning/phases/33-database-core-infrastructure/33-01-SUMMARY.md` với:
- File modified: `e_office_app_new/database/schema/000_schema_v3.0.sql` (+~150-200 lines append)
- DB state: 2 bảng mới + 1 cột + 2 triggers + 1 helper function
- Apply lần 2 result: zero error / X seconds
- SP count: before / after (≥ 386)
- Test trigger validate_root_unit: PASS (non-root reject + root accept)
- Tech debt: 2 cột legacy `departments.lgsp_system_id` + `lgsp_secret_key` còn lại (Phase 18 cũ) — Phase 37 admin UI sẽ deprecate
- Next: Plan 33-02 seed 9 row + UPDATE lgsp_org_code cho 6 root unit
</output>
