---
phase: 11.1-db-consolidation-seed-strategy
plan: 03
type: execute
wave: 3
depends_on:
  - 11.1-01
  - 11.1-02
files_modified:
  - deploy/reset-db.sh
  - deploy/reset-db-windows.ps1
  - deploy/deploy.sh
  - deploy/deploy-windows.ps1
  - deploy/README.md
  - CLAUDE.md
  - e_office_app_new/database/archive/v1.0-migrations/000_full_schema.sql
  - e_office_app_new/database/archive/v1.0-migrations/quick_260418_hlj_hscv.sql
  - e_office_app_new/database/archive/v1.0-migrations/quick_260418_hlj_recall.sql
  - e_office_app_new/database/archive/v1.0-migrations/quick_260418_hlj_signature.sql
  - e_office_app_new/database/archive/v1.0-migrations/quick_260418_jsd_hscv_cancel.sql
  - e_office_app_new/database/archive/v1.0-migrations/quick_260418_jsd_hscv_transfer.sql
  - e_office_app_new/database/archive/v1.0-migrations/quick_260418_jsd_opinion_forward.sql
  - e_office_app_new/database/archive/v1.0-migrations/quick_260418_jsd_sign_otp.sql
  - e_office_app_new/database/archive/v1.0-migrations/quick_260418_jsd_truc_cp.sql
  - e_office_app_new/database/archive/v1.0-migrations/quick_260418_missing_helpers.sql
  - e_office_app_new/database/archive/v1.0-migrations/quick_260418_restore_030_to_037.sql
  - e_office_app_new/database/archive/v1.0-migrations/quick_260418_zz_cleanup_duplicates.sql
  - e_office_app_new/database/archive/v2.0-incrementals/040_signing_schema.sql
  - e_office_app_new/database/archive/v2.0-incrementals/041_migrate_sign_phone.sql
  - e_office_app_new/database/archive/v2.0-incrementals/042_signing_stats_sp.sql
  - e_office_app_new/database/archive/v2.0-incrementals/043_seed_default_providers.sql
  - e_office_app_new/database/archive/v2.0-incrementals/045_sign_flow_attachment_helpers.sql
  - e_office_app_new/database/archive/v2.0-incrementals/046_sign_list_pending_docs.sql
  - e_office_app_new/database/archive/v1.0-migrations/README.md
  - e_office_app_new/database/archive/v2.0-incrementals/README.md
autonomous: false
requirements:
  - DB-CONSOLIDATE-05
  - DB-CONSOLIDATE-06
tags: [deploy-scripts, docs, claude-rules, archive]

must_haves:
  truths:
    - "deploy/reset-db.sh flow mới: drop → init schemas → schema master → seed 001 → seed 002 (skip 002 if --no-demo flag)"
    - "deploy/reset-db-windows.ps1 flow mới tương tự reset-db.sh — chạy trên Windows"
    - "deploy/deploy.sh + deploy-windows.ps1 cập nhật: first-time deploy dùng schema master (không loop quick_*.sql)"
    - "deploy/README.md document flow mới + hướng dẫn cấu hình app.signing_secret_key"
    - "CLAUDE.md có section DB Migration Strategy mới: edit schema/000_schema_v2.X.sql (không thêm migrations/047+) + bump version khi milestone"
    - "Migrations cũ (6 file 040-046 + 7 file quick_*.sql + 000_full_schema.sql) đã MOVE sang archive/v1.0-migrations hoặc archive/v2.0-incrementals"
    - "Folder migrations/ RỖNG hoặc chỉ còn README pointer — không còn file .sql rời trong migrations/"
    - "Checkpoint human-verify: user chạy reset-db.sh / reset-db-windows.ps1 trên môi trường test và confirm app khởi động OK, login admin Admin@123 thành công, trang /van-ban-den hiển thị ≥ 50 record"
  artifacts:
    - path: "deploy/reset-db.sh"
      provides: "Reset DB script Linux — flow mới (schema + seed 001 + seed 002)"
      min_lines: 100
    - path: "deploy/reset-db-windows.ps1"
      provides: "Reset DB script Windows — flow mới"
      min_lines: 120
    - path: "deploy/README.md"
      provides: "Documentation flow mới — schema master + seed tách, app.signing_secret_key setup"
      min_lines: 150
    - path: "CLAUDE.md"
      provides: "Rules mới cho DB migrations — edit master file, bump version, không thêm incremental files"
    - path: "e_office_app_new/database/archive/v1.0-migrations/README.md"
      provides: "Pointer docs giải thích folder archive là gì, merge vào file nào"
    - path: "e_office_app_new/database/archive/v2.0-incrementals/README.md"
      provides: "Pointer docs tương tự cho v2.0"
  key_links:
    - from: "deploy/reset-db.sh"
      to: "e_office_app_new/database/schema/000_schema_v2.0.sql"
      via: "apply schema master sau drop + init"
      pattern: "database/schema/000_schema_v2.0.sql"
    - from: "deploy/reset-db.sh"
      to: "e_office_app_new/database/seed/001_required_data.sql"
      via: "apply required data với SET app.signing_secret_key"
      pattern: "seed/001_required_data.sql|app.signing_secret_key"
    - from: "deploy/reset-db.sh"
      to: "e_office_app_new/database/seed/002_demo_data.sql"
      via: "apply demo data nếu không có --no-demo flag"
      pattern: "seed/002_demo_data.sql"
    - from: "CLAUDE.md DB Migration Strategy section"
      to: "e_office_app_new/database/schema/000_schema_v2.0.sql"
      via: "rule: edit master file for new migration, bump version when milestone"
      pattern: "schema/000_schema_v2"
---

<objective>
Khép lại Phase 11.1 bằng 3 công việc cuối: (1) update deploy scripts sang flow mới (schema master + 2 seed files thay vì loop 18 migrations), (2) move 18 file migration cũ vào archive folder giữ lịch sử, (3) update CLAUDE.md rules để developer tương lai (và Claude agent) biết luật: edit schema master + bump version, KHÔNG thêm file `migrations/047_*.sql` rời.

Purpose: Điểm đích cuối của phase 11.1 — từ giờ pull code máy mới chỉ cần chạy `bash reset-db.sh` (hoặc `.\reset-db-windows.ps1`) là có app chạy ngon với 150+ records demo data. Developer mới đọc CLAUDE.md hiểu ngay DB rules mà không phải reverse-engineer từ migration history.

Output:
- 4 deploy scripts updated (reset-db.sh, reset-db-windows.ps1, deploy.sh, deploy-windows.ps1)
- deploy/README.md cập nhật đầy đủ workflow
- CLAUDE.md thêm section "DB Migration Strategy" (~30-50 dòng)
- 18 file migration cũ moved sang archive/ + 2 README.md documenting archive
- Folder migrations/ sạch (rỗng hoặc chỉ có pointer README)
- 1 checkpoint human-verify cuối cùng: user chạy reset-db trên test env + confirm app hoạt động
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11.1-db-consolidation-seed-strategy/11.1-01-PLAN.md
@.planning/phases/11.1-db-consolidation-seed-strategy/11.1-02-PLAN.md
@CLAUDE.md
@deploy/reset-db.sh
@deploy/reset-db-windows.ps1
@deploy/deploy.sh
@deploy/deploy-windows.ps1
@deploy/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 4 deploy scripts (reset-db + deploy cho cả Linux + Windows)</name>
  <files>
    - deploy/reset-db.sh
    - deploy/reset-db-windows.ps1
    - deploy/deploy.sh
    - deploy/deploy-windows.ps1
  </files>
  <read_first>
    - deploy/reset-db.sh (full — cần hiểu structure cũ trước khi rewrite)
    - deploy/reset-db-windows.ps1 (full)
    - deploy/deploy.sh (dòng 150-250 phần migration apply — find section dùng 000_full_schema.sql)
    - deploy/deploy-windows.ps1 (phần tương tự)
    - e_office_app_new/database/schema/000_schema_v2.0.sql (verify path)
    - e_office_app_new/database/seed/001_required_data.sql
    - e_office_app_new/database/seed/002_demo_data.sql
  </read_first>
  <action>
**UPDATE FLOW CHO TẤT CẢ 4 SCRIPTS:**

Flow mới:
```
1. DROP schemas (edoc, esto, cont, iso, public)
2. CREATE SCHEMA public + GRANT
3. Apply database/init/01_create_schemas.sql       (CREATE SCHEMAs edoc/esto/cont/iso + extensions)
4. Apply database/schema/000_schema_v2.0.sql       (MASTER: tables + SPs + triggers + indexes)
5. Apply database/seed/001_required_data.sql       (REQUIRED: admin + roles + rights + 2 providers)
   PHẢI set app.signing_secret_key trước (match env SIGNING_SECRET_KEY của backend)
6. Apply database/seed/002_demo_data.sql           (DEMO: 150+ records) — SKIP nếu --no-demo
```

**1. `deploy/reset-db.sh` (Linux — rewrite)**

```bash
#!/bin/bash
# ============================================================
# e-Office — Reset DB (CHỈ DÙNG CHO TEST SERVER)
# Flow v2.0 consolidated: schema master + seed files (KHÔNG loop migrations/)
#
# ⚠️ CẢNH BÁO: Xóa TOÀN BỘ data — KHÔNG dùng trên production thật
# Usage: sudo bash reset-db.sh [--no-demo]
#   --no-demo: bỏ qua seed/002 demo data (production deploy dùng flag này)
# ============================================================

set -e

APP_DIR="/opt/eoffice/quanlyvanban"
WORK_DIR="$APP_DIR/e_office_app_new"
PG_DB="qlvb_prod"
PG_USER="qlvb_admin"
SIGNING_KEY="${SIGNING_SECRET_KEY:-qlvb-signing-dev-key-change-production-2026}"

NO_DEMO=false
for arg in "$@"; do
  [ "$arg" == "--no-demo" ] && NO_DEMO=true
done

GREEN='\033[0;32m'; YELLOW='\033[1;33m'; RED='\033[0;31m'; NC='\033[0m'
log()  { echo -e "${GREEN}[✓]${NC} $1"; }
warn() { echo -e "${YELLOW}[!]${NC} $1"; }
err()  { echo -e "${RED}[✗]${NC} $1"; exit 1; }

[ "$EUID" -ne 0 ] && err "Chạy với sudo: sudo bash reset-db.sh"

echo ""
echo "================================================================"
echo -e "  ${RED}⚠ RESET DB — sẽ xóa SẠCH toàn bộ data trong $PG_DB${NC}"
[ "$NO_DEMO" == "true" ] && echo -e "  ${YELLOW}(--no-demo: bỏ qua seed 002)${NC}"
echo "================================================================"
echo ""

read -p "Xác nhận xóa sạch DB? Gõ 'yes' để tiếp tục: " confirm
[ "$confirm" != "yes" ] && { echo "Đã hủy."; exit 0; }
echo ""

# ─── 1. Pull code (optional) ───
if [ -d "$APP_DIR/.git" ]; then
  log "Pull code mới..."
  cd "$APP_DIR" && git fetch --all -q && git reset --hard origin/main -q
fi

# ─── 2. Drop schemas ───
log "Drop schemas..."
docker exec qlvb_postgres psql -U $PG_USER -d $PG_DB -v ON_ERROR_STOP=1 -c "
DROP SCHEMA IF EXISTS edoc CASCADE;
DROP SCHEMA IF EXISTS esto CASCADE;
DROP SCHEMA IF EXISTS cont CASCADE;
DROP SCHEMA IF EXISTS iso  CASCADE;
DROP SCHEMA IF EXISTS public CASCADE;
CREATE SCHEMA public;
GRANT ALL ON SCHEMA public TO $PG_USER;
GRANT ALL ON SCHEMA public TO PUBLIC;
" > /dev/null || err "Drop schemas thất bại"

# ─── 3. Apply init (schemas + extensions) ───
log "Apply init/01_create_schemas.sql..."
docker exec -i qlvb_postgres psql -U $PG_USER -d $PG_DB -v ON_ERROR_STOP=1 -f - \
  < "$WORK_DIR/database/init/01_create_schemas.sql" > /dev/null || err "Init schemas thất bại"

# ─── 4. Apply master schema ───
log "Apply schema/000_schema_v2.0.sql (master)..."
docker exec -i qlvb_postgres psql -U $PG_USER -d $PG_DB -v ON_ERROR_STOP=1 -f - \
  < "$WORK_DIR/database/schema/000_schema_v2.0.sql" > /dev/null || err "Schema master thất bại"

# ─── 5. Apply seed 001 (required) ───
log "Apply seed/001_required_data.sql..."
docker exec -i qlvb_postgres psql -U $PG_USER -d $PG_DB -v ON_ERROR_STOP=1 \
  -c "SET app.signing_secret_key='$SIGNING_KEY';" \
  -f - < "$WORK_DIR/database/seed/001_required_data.sql" > /dev/null || err "Seed 001 thất bại"

# ─── 6. Apply seed 002 (demo) — skip if --no-demo ───
if [ "$NO_DEMO" == "false" ]; then
  log "Apply seed/002_demo_data.sql (rich demo data)..."
  docker exec -i qlvb_postgres psql -U $PG_USER -d $PG_DB -v ON_ERROR_STOP=1 -f - \
    < "$WORK_DIR/database/seed/002_demo_data.sql" > /dev/null || err "Seed 002 thất bại"
else
  warn "Skip seed/002 (--no-demo)"
fi

# ─── 7. Rebuild (optional) ───
read -p "Rebuild backend + frontend? [y/N] " rebuild
if [ "$rebuild" = "y" ] || [ "$rebuild" = "Y" ]; then
  log "Build backend..."
  cd "$WORK_DIR/backend" && npm install --omit=dev > /dev/null 2>&1 && npm run build > /dev/null 2>&1
  log "Build frontend (2-3 phút)..."
  cd "$WORK_DIR/frontend" && npm install > /dev/null 2>&1 && npm run build > /dev/null 2>&1
  log "Restart pm2..."
  pm2 restart all > /dev/null 2>&1 || warn "pm2 restart thất bại"
fi

echo ""
echo "================================================================"
log "DB reset OK"
echo ""
echo "  Admin: username=admin, password=Admin@123"
echo "  Login: http://<server-ip>"
[ "$NO_DEMO" == "false" ] && echo "  Demo data: 10 users, 50 VB đến, 30 VB đi, 20 dự thảo, 15 HSCV"
echo "================================================================"
```

**2. `deploy/reset-db-windows.ps1` (Windows — rewrite tương tự)**

Chuyển đổi shell sang PowerShell. Ưu tiên dùng `& $psqlExe` local (không qua docker). Flow giống hệt:
1. Drop schemas via psql -c
2. Apply init file (-f)
3. Apply schema master (-f)
4. Apply seed 001 với `-c "SET app.signing_secret_key='...';" -f seed1.sql`
5. Apply seed 002 nếu không có flag `-NoDemo`
6. Prompt rebuild

Param flag pattern PowerShell:
```powershell
param(
  [switch]$NoDemo
)
$SIGNING_KEY = if ($env:SIGNING_SECRET_KEY) { $env:SIGNING_SECRET_KEY } else { 'qlvb-signing-dev-key-change-production-2026' }
```

**3. `deploy/deploy.sh` (Linux — cập nhật phần DB migration step)**

Tìm section "BƯỚC X: Chạy migrations + seed" (thường đoạn dùng 000_full_schema.sql + for loop quick_*.sql + seed_full_demo.sql).

Thay thế HOÀN TOÀN section đó bằng block mới:
```bash
# ============================================================
# BƯỚC X: Apply DB schema + seed (v2.0 consolidated)
# ============================================================
log "Bước X/10: Apply DB schema + seed..."

docker exec -i qlvb_postgres psql -U $PG_USER -d $PG_DB -v ON_ERROR_STOP=1 -f - \
  < "$WORK_DIR/database/init/01_create_schemas.sql"

docker exec -i qlvb_postgres psql -U $PG_USER -d $PG_DB -v ON_ERROR_STOP=1 -f - \
  < "$WORK_DIR/database/schema/000_schema_v2.0.sql"

docker exec -i qlvb_postgres psql -U $PG_USER -d $PG_DB -v ON_ERROR_STOP=1 \
  -c "SET app.signing_secret_key='$JWT_SECRET';" \
  -f - < "$WORK_DIR/database/seed/001_required_data.sql"

# Production: skip demo seed (chỉ seed required data)
log "Bước X hoàn thành — DB ready, admin=admin/Admin@123"
```

**Lưu ý quan trọng:** deploy.sh là FIRST-TIME deploy cho production → chỉ chạy seed 001 (không 002). Nếu customer cần demo data sau đó, chạy riêng `bash reset-db.sh` (có demo).

**4. `deploy/deploy-windows.ps1` — cập nhật tương tự**

Tìm section migration apply, thay bằng block mới tương tự deploy.sh.

**LƯU Ý:** Ưu tiên giữ toàn bộ logic khác của script (cài Docker, clone repo, build, nginx config) — chỉ thay section DB migration.
  </action>
  <verify>
    <automated>bash -n deploy/reset-db.sh && grep -q "schema/000_schema_v2.0.sql" deploy/reset-db.sh && grep -q "seed/001_required_data.sql" deploy/reset-db.sh && grep -q "app.signing_secret_key" deploy/reset-db.sh && grep -q "schema/000_schema_v2.0.sql" deploy/reset-db-windows.ps1 && grep -q "schema/000_schema_v2.0.sql" deploy/deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/reset-db.sh` syntax valid (`bash -n` pass), chứa tham chiếu tới 3 file mới: schema/000_schema_v2.0.sql, seed/001_required_data.sql, seed/002_demo_data.sql
    - `deploy/reset-db.sh` hỗ trợ flag `--no-demo` để skip seed 002
    - `deploy/reset-db.sh` set session variable `app.signing_secret_key` trước khi apply seed 001
    - `deploy/reset-db-windows.ps1` param `-NoDemo` switch + tương tự logic
    - `deploy/deploy.sh` section DB migration dùng schema master + seed 001 (KHÔNG dùng 002 cho production first-time)
    - `deploy/deploy-windows.ps1` section DB migration tương tự
    - KHÔNG còn tham chiếu tới `000_full_schema.sql`, `quick_*.sql` hoặc `seed_full_demo.sql` trong 4 script mới (grep confirm = 0 match)
  </acceptance_criteria>
  <done>4 deploy scripts cập nhật flow mới, syntax valid, tham chiếu đúng paths mới</done>
</task>

<task type="auto">
  <name>Task 2: Move 18 migration files vào archive + tạo README archive + update deploy/README.md + CLAUDE.md</name>
  <files>
    - e_office_app_new/database/archive/v1.0-migrations/000_full_schema.sql
    - e_office_app_new/database/archive/v1.0-migrations/quick_260418_*.sql (11 file)
    - e_office_app_new/database/archive/v2.0-incrementals/040_signing_schema.sql
    - e_office_app_new/database/archive/v2.0-incrementals/041_migrate_sign_phone.sql
    - e_office_app_new/database/archive/v2.0-incrementals/042_signing_stats_sp.sql
    - e_office_app_new/database/archive/v2.0-incrementals/043_seed_default_providers.sql
    - e_office_app_new/database/archive/v2.0-incrementals/045_sign_flow_attachment_helpers.sql
    - e_office_app_new/database/archive/v2.0-incrementals/046_sign_list_pending_docs.sql
    - e_office_app_new/database/archive/v1.0-migrations/README.md
    - e_office_app_new/database/archive/v2.0-incrementals/README.md
    - e_office_app_new/database/migrations/README.md (pointer cuối)
    - deploy/README.md
    - CLAUDE.md
  </files>
  <read_first>
    - CLAUDE.md dòng 332-463 (section "Phase Execution Rules") để biết insert CLAUDE.md "DB Migration Strategy" section ở đâu đúng style
    - deploy/README.md (full)
    - .planning/phases/11.1-db-consolidation-seed-strategy/11.1-01-PLAN.md (reference cho archive README)
  </read_first>
  <action>
**1. Move 18 file migration cũ vào archive**

Dùng git mv để giữ history:

```bash
cd /d/ProjectAI/quanlyvanban

# ─── v1.0 migrations: 000_full_schema.sql + 11 quick_*.sql ───
git mv e_office_app_new/database/migrations/000_full_schema.sql \
       e_office_app_new/database/archive/v1.0-migrations/

git mv e_office_app_new/database/migrations/quick_260418_hlj_hscv.sql \
       e_office_app_new/database/archive/v1.0-migrations/
git mv e_office_app_new/database/migrations/quick_260418_hlj_recall.sql \
       e_office_app_new/database/archive/v1.0-migrations/
git mv e_office_app_new/database/migrations/quick_260418_hlj_signature.sql \
       e_office_app_new/database/archive/v1.0-migrations/
git mv e_office_app_new/database/migrations/quick_260418_jsd_hscv_cancel.sql \
       e_office_app_new/database/archive/v1.0-migrations/
git mv e_office_app_new/database/migrations/quick_260418_jsd_hscv_transfer.sql \
       e_office_app_new/database/archive/v1.0-migrations/
git mv e_office_app_new/database/migrations/quick_260418_jsd_opinion_forward.sql \
       e_office_app_new/database/archive/v1.0-migrations/
git mv e_office_app_new/database/migrations/quick_260418_jsd_sign_otp.sql \
       e_office_app_new/database/archive/v1.0-migrations/
git mv e_office_app_new/database/migrations/quick_260418_jsd_truc_cp.sql \
       e_office_app_new/database/archive/v1.0-migrations/
git mv e_office_app_new/database/migrations/quick_260418_missing_helpers.sql \
       e_office_app_new/database/archive/v1.0-migrations/
git mv e_office_app_new/database/migrations/quick_260418_restore_030_to_037.sql \
       e_office_app_new/database/archive/v1.0-migrations/
git mv e_office_app_new/database/migrations/quick_260418_zz_cleanup_duplicates.sql \
       e_office_app_new/database/archive/v1.0-migrations/

# ─── v2.0 incrementals: 040-046 ───
git mv e_office_app_new/database/migrations/040_signing_schema.sql \
       e_office_app_new/database/archive/v2.0-incrementals/
git mv e_office_app_new/database/migrations/041_migrate_sign_phone.sql \
       e_office_app_new/database/archive/v2.0-incrementals/
git mv e_office_app_new/database/migrations/042_signing_stats_sp.sql \
       e_office_app_new/database/archive/v2.0-incrementals/
git mv e_office_app_new/database/migrations/043_seed_default_providers.sql \
       e_office_app_new/database/archive/v2.0-incrementals/
git mv e_office_app_new/database/migrations/045_sign_flow_attachment_helpers.sql \
       e_office_app_new/database/archive/v2.0-incrementals/
git mv e_office_app_new/database/migrations/046_sign_list_pending_docs.sql \
       e_office_app_new/database/archive/v2.0-incrementals/

# ─── Move seed_full_demo.sql cũ cũng đi (đã bị replace bởi seed/001 + 002) ───
git mv e_office_app_new/database/seed_full_demo.sql \
       e_office_app_new/database/archive/v1.0-migrations/seed_full_demo.sql
```

Sau khi move: `ls e_office_app_new/database/migrations/` phải empty (hoặc chỉ còn README.md pointer).

**2. Tạo 2 README trong archive**

**`e_office_app_new/database/archive/v1.0-migrations/README.md`:**
```markdown
# Archive: v1.0 Migrations

## ⚠️ KHÔNG CHẠY FILE NÀO Ở ĐÂY

Thư mục này chứa lịch sử migrations được merge vào `database/schema/000_schema_v2.0.sql` trong Phase 11.1 (2026-04-22).

## File trong thư mục

- `000_full_schema.sql` — consolidated schema v1.0 (Phase 1-7), 17466 dòng
- `quick_260418_hlj_*.sql` — HDSD compliance v1.0 (3 file)
- `quick_260418_jsd_*.sql` — bug fixes cuối v1.0 (5 file)
- `quick_260418_missing_helpers.sql` — 2 function helpers bị bỏ sót
- `quick_260418_restore_030_to_037.sql` — restore block, 5045 dòng
- `quick_260418_zz_cleanup_duplicates.sql` — band-aid drop SP overload (đã loại bỏ trong master v2.0)
- `seed_full_demo.sql` — seed cũ v1.0 (replaced by `database/seed/001_required_data.sql` + `002_demo_data.sql`)

## Nếu cần revert

Nếu schema master v2.0 bị lỗi, có thể tạm thời apply lại thứ tự cũ:
```bash
psql < 000_full_schema.sql
for f in quick_260418_*.sql; do psql < $f; done
psql < quick_260418_zz_cleanup_duplicates.sql  # BẮT BUỘC chạy CUỐI CÙNG
psql < seed_full_demo.sql
```

**Nhưng sẽ gặp lại các lỗi:**
- SP overload ambiguous (cần zz_cleanup_duplicates)
- Không có 2 provider config seeded
- Seed trộn lẫn với schema

## Nguồn gốc

Xem `.planning/phases/11.1-db-consolidation-seed-strategy/` để biết lý do consolidate.
```

**`e_office_app_new/database/archive/v2.0-incrementals/README.md`:**
```markdown
# Archive: v2.0 Incrementals (Phase 8-11)

## ⚠️ KHÔNG CHẠY FILE NÀO Ở ĐÂY

Thư mục này chứa các migration incremental của v2.0 Phase 8-11 — đã merge vào `database/schema/000_schema_v2.0.sql` trong Phase 11.1.

## File trong thư mục

- `040_signing_schema.sql` — Phase 8 Plan 01: 3 bảng signing + ALTER 4 attachment tables + 15 SPs
- `041_migrate_sign_phone.sql` — Phase 8 Plan 02: data migration staff.sign_phone → staff_signing_config (final state đã reflect trong master)
- `042_signing_stats_sp.sql` — Phase 9 Plan 02: dashboard stats SP
- `043_seed_default_providers.sql` — Phase 9 Plan 03: seed 2 provider rows (MOVED sang `seed/001_required_data.sql`)
- `045_sign_flow_attachment_helpers.sql` — Phase 11 Plan 01: 4 SPs (finalize_sign + can_sign + list/count txn)
- `046_sign_list_pending_docs.sql` — Phase 11 Plan 05: SP fn_sign_list_pending_docs

## Lưu ý

Data migration 041 chạy 1 lần (migrate existing staff.sign_phone). Trong master schema v2.0, bảng `staff` đã không còn column `sign_phone` — reflect final state sau migration.

## Khi thêm feature mới

**KHÔNG thêm file `047_*.sql` hoặc `048_*.sql` vào folder này.**

Thay vào đó, edit trực tiếp `database/schema/000_schema_v2.0.sql` (hoặc bump version `000_schema_v2.1.sql` nếu milestone mới). Xem rules trong `CLAUDE.md` section "DB Migration Strategy".
```

**`e_office_app_new/database/migrations/README.md`** (pointer file giữ folder empty):
```markdown
# migrations/ folder

Đã consolidate vào `database/schema/000_schema_v2.0.sql` trong Phase 11.1.

- **Current schema:** `database/schema/000_schema_v2.0.sql`
- **Current seeds:** `database/seed/001_required_data.sql` + `database/seed/002_demo_data.sql`
- **Archive:** `database/archive/v1.0-migrations/` + `database/archive/v2.0-incrementals/`

## Thêm migration mới

Edit trực tiếp file schema master. KHÔNG thêm file `.sql` rời vào folder này.

Xem `CLAUDE.md` section "DB Migration Strategy" để biết rules đầy đủ.
```

**3. Update `deploy/README.md`**

Append sections mới (không xóa nội dung cũ, chỉ update):

```markdown
## Cấu trúc DB (v2.0 consolidated)

```
e_office_app_new/database/
├── init/
│   └── 01_create_schemas.sql         # Bootstrap Docker — schemas + extensions
├── schema/
│   └── 000_schema_v2.0.sql           # MASTER idempotent schema (tables + SPs + triggers)
├── seed/
│   ├── 001_required_data.sql         # BẮT BUỘC: admin + roles + rights + 2 providers
│   └── 002_demo_data.sql             # OPTIONAL: 150+ records demo (skip cho production)
└── archive/
    ├── v1.0-migrations/              # Lịch sử Phase 1-7 (KHÔNG chạy)
    └── v2.0-incrementals/            # Lịch sử Phase 8-11 (KHÔNG chạy)
```

## Reset DB flow (v2.0)

**Linux:**
```bash
sudo bash /opt/eoffice/quanlyvanban/deploy/reset-db.sh
# Hoặc skip demo data cho test production-like:
sudo bash /opt/eoffice/quanlyvanban/deploy/reset-db.sh --no-demo
```

**Windows:**
```powershell
.\reset-db-windows.ps1
# Hoặc:
.\reset-db-windows.ps1 -NoDemo
```

Script tự động:
1. Drop schemas (edoc, esto, cont, iso, public)
2. Apply `init/01_create_schemas.sql`
3. Apply `schema/000_schema_v2.0.sql` (master)
4. Apply `seed/001_required_data.sql` — PHẢI set `app.signing_secret_key` trước (env `SIGNING_SECRET_KEY` hoặc hardcoded)
5. Apply `seed/002_demo_data.sql` (skip nếu `--no-demo` / `-NoDemo`)
6. Prompt rebuild + restart

## Environment variable BẮT BUỘC

Trước khi chạy seed 001, backend + DB PHẢI dùng chung key để encrypt/decrypt `client_secret`:

**Backend `.env`:**
```
SIGNING_SECRET_KEY=<32+ ký tự random hex>
```

**Reset-db script** đọc biến env `SIGNING_SECRET_KEY` và set vào session `app.signing_secret_key` trước apply seed 001. Nếu không có env var, dùng hardcoded dev key (chỉ dev — production PHẢI override).

**Đổi key sau deploy:** khi đổi `SIGNING_SECRET_KEY`, client_secret của provider config bị KHÔNG decrypt được — Admin PHẢI login vào `/ky-so/cau-hinh` và nhập lại credentials (không phải reset DB).
```

**4. Update `CLAUDE.md` — thêm section "DB Migration Strategy"**

Insert ngay SAU section "Phase Execution Rules" (dòng ~463), TRƯỚC `## Developer Profile` (dòng 464).

```markdown
## DB Migration Strategy (v2.0+)

### Source of truth

Từ Phase 11.1, toàn bộ schema + SPs + triggers nằm trong 1 file MASTER duy nhất:
**`e_office_app_new/database/schema/000_schema_v2.0.sql`**

### Flow reset DB

```
1. drop schemas (edoc, esto, cont, iso, public)
2. apply init/01_create_schemas.sql       (schemas + extensions)
3. apply schema/000_schema_v2.0.sql       (MASTER — tables + SPs + triggers)
4. apply seed/001_required_data.sql       (admin + roles + rights + 2 providers)
5. apply seed/002_demo_data.sql           (optional — rich demo)
```

Script: `deploy/reset-db.sh` (Linux) / `deploy/reset-db-windows.ps1` (Windows).

### Khi thêm feature mới cần schema change

**KHÔNG tạo file `database/migrations/047_*.sql` rời.**

Thay vào đó:

1. **Edit trực tiếp `database/schema/000_schema_v2.0.sql`:**
   - Thêm CREATE TABLE IF NOT EXISTS ở phần Tables
   - Thêm CREATE OR REPLACE FUNCTION ở phần Functions
   - Nếu ALTER cột hiện có → inline ALTER vào CREATE TABLE gốc (tránh ALTER rời)

2. **Đảm bảo idempotent:** DROP IF EXISTS trước CREATE cho SPs/triggers. Dùng IF NOT EXISTS cho tables/indexes/columns.

3. **Chạy trên dev DB để verify:**
   ```bash
   bash deploy/reset-db.sh
   # Rồi apply lại lần 2:
   docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
     -f - < e_office_app_new/database/schema/000_schema_v2.0.sql
   # Lần 2 PHẢI zero error
   ```

4. **Verify không có SP overload:**
   ```sql
   SELECT count(*) FROM (
     SELECT n.nspname, p.proname, count(*)
     FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
     WHERE n.nspname IN ('public','edoc','esto','cont','iso') AND p.proname LIKE 'fn_%'
     GROUP BY 1,2 HAVING count(*) > 1
   ) t;
   -- Phải = 0
   ```

### Khi nào bump version (v2.1, v2.2, ...)

Khi kết thúc 1 milestone lớn (v2.0 ký số xong → v2.1 báo cáo thống kê mới):

1. Rename `000_schema_v2.0.sql` → `000_schema_v2.1.sql` (giữ file tại `schema/`)
2. Move migrations v2.0 của milestone cũ sang `archive/v2.0-finalized/`
3. Update paths trong deploy scripts + deploy/README.md
4. Update reference này trong CLAUDE.md

**Lý do bump:** Khi file master > 25,000 dòng, hoặc khi cần cắt "clean slate" cho customer mới (không load history v1.0 nữa).

### Migration trong production

**First-time deploy** (`deploy/deploy.sh`): apply init + schema master + seed 001 (KHÔNG seed 002).

**Update code**: dùng `deploy/update.sh` (pull code + rebuild + KHÔNG reset DB). Nếu schema có thay đổi, apply file schema master bằng tay:
```bash
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_prod -v ON_ERROR_STOP=1 \
  -f - < /opt/eoffice/quanlyvanban/e_office_app_new/database/schema/000_schema_v2.0.sql
```
Do file master idempotent, apply lại an toàn — không mất data.

### Seed data

- **seed/001_required_data.sql** — idempotent, có thể chạy nhiều lần (ON CONFLICT DO NOTHING). Production deploy chạy.
- **seed/002_demo_data.sql** — chỉ test env, production KHÔNG chạy. Có guard kiểm tra 001 đã apply.

### Archive folder

- `database/archive/v1.0-migrations/` — lịch sử migrations Phase 1-7 (KHÔNG chạy)
- `database/archive/v2.0-incrementals/` — lịch sử migrations Phase 8-11 (KHÔNG chạy)

Giữ để revert nếu cần. KHÔNG xóa khỏi git.
```

**LƯU Ý khi edit CLAUDE.md:**
- Insert section mới ở giữa "Phase Execution Rules" và "Developer Profile"
- KHÔNG xóa/sửa các section khác
- Giữ format markdown nhất quán với file gốc (##, ###, bullet list)
  </action>
  <verify>
    <automated>test -f e_office_app_new/database/archive/v1.0-migrations/000_full_schema.sql && test -f e_office_app_new/database/archive/v2.0-incrementals/040_signing_schema.sql && test -f e_office_app_new/database/archive/v2.0-incrementals/046_sign_list_pending_docs.sql && test -f e_office_app_new/database/archive/v1.0-migrations/README.md && test -f e_office_app_new/database/archive/v2.0-incrementals/README.md && grep -q "DB Migration Strategy" CLAUDE.md && grep -q "schema/000_schema_v2.0.sql" deploy/README.md && test ! -f e_office_app_new/database/migrations/040_signing_schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - 12 file cũ đã move từ `migrations/` sang `archive/v1.0-migrations/` (000_full_schema + 11 quick_*.sql)
    - 6 file đã move từ `migrations/` sang `archive/v2.0-incrementals/` (040, 041, 042, 043, 045, 046)
    - `seed_full_demo.sql` cũ đã move sang archive/v1.0-migrations/
    - Folder `migrations/` chỉ còn `README.md` pointer (hoặc empty)
    - 2 README.md trong archive có nội dung rõ ràng: không được chạy file nào, lý do archive, cách revert nếu cần
    - `deploy/README.md` có section mới "Cấu trúc DB (v2.0 consolidated)" và "Reset DB flow (v2.0)" với env var `SIGNING_SECRET_KEY`
    - `CLAUDE.md` có section mới "## DB Migration Strategy (v2.0+)" với ≥ 50 dòng, insert đúng vị trí (sau Phase Execution Rules, trước Developer Profile)
    - Git mv giữ được file history (`git log --follow archive/.../040_signing_schema.sql` trả commits từ migrations/)
  </acceptance_criteria>
  <done>18 file moved vào archive, 2 README mới, deploy/README.md + CLAUDE.md cập nhật rules đầy đủ, folder migrations/ sạch</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — chạy reset-db trên test env + confirm app hoạt động</name>
  <files>(human verification — no code changes)</files>
  <read_first>
    - deploy/reset-db.sh (verify flow mới đã tạo ra ở Task 1)
    - deploy/reset-db-windows.ps1 (verify flow mới đã tạo ra ở Task 1)
    - "action" section dưới đây — steps for human to execute
  </read_first>
  <action>
**WHAT WAS BUILT**

Phase 11.1 hoàn thành với 3 plans:
- **Plan 01:** 1 file schema master `database/schema/000_schema_v2.0.sql` (idempotent, no SP overload)
- **Plan 02:** 2 file seed `database/seed/001_required_data.sql` + `002_demo_data.sql` (150+ records)
- **Plan 03:** 4 deploy scripts updated + 18 migrations moved to archive + CLAUDE.md rules

Goal: user chạy reset-db.sh (hoặc .ps1) trên test env, confirm app hoạt động E2E.

**HOW TO VERIFY — human performs these steps manually**

**Chạy trên test server (Windows hoặc Linux):**

1. **Backup DB hiện tại** (phòng khi cần rollback):
   ```bash
   docker exec qlvb_postgres pg_dump -U qlvb_admin qlvb_dev > ~/qlvb_dev_backup_$(date +%Y%m%d).sql
   ```

2. **Pull code mới** (Phase 11.1 đã commit):
   ```bash
   cd /opt/eoffice/quanlyvanban && git pull origin main
   ```

3. **Chạy reset-db:**
   - Linux: `sudo bash deploy/reset-db.sh`
   - Windows: `.\deploy\reset-db-windows.ps1`

   Xác nhận `yes`. Script sẽ chạy:
   - Drop schemas
   - Apply init/01_create_schemas.sql (extensions + schemas)
   - Apply schema/000_schema_v2.0.sql (master)
   - Apply seed/001_required_data.sql (cần `SIGNING_SECRET_KEY` env var)
   - Apply seed/002_demo_data.sql (demo)
   - (Optional) rebuild backend + frontend

4. **Verify admin login:**
   - Vào `http://<server-ip>/login`
   - Username: `admin`, Password: `Admin@123`
   - Kỳ vọng: login thành công, redirect `/dashboard`

5. **Verify data demo:**
   - Mở `/van-ban-den` → kỳ vọng ≥ 50 VB đến với mix urgent/secret levels
   - Mở `/van-ban-di` → kỳ vọng ≥ 30 VB đi
   - Mở `/du-thao` → kỳ vọng ≥ 20 dự thảo
   - Mở `/ho-so-cong-viec` → kỳ vọng ≥ 15 HSCV với mix status 0→4 + thu hồi + hủy
   - Mở `/ky-so/cau-hinh` (admin) → kỳ vọng 2 card: SmartCA VNPT (active) + MySign Viettel (inactive)

6. **Verify chạy lại reset-db lần 2 (idempotent):**
   ```bash
   sudo bash deploy/reset-db.sh
   # Gõ 'yes' xác nhận
   ```
   Kỳ vọng: zero error, complete trong thời gian tương tự lần 1.

7. **Verify `--no-demo` flag:**
   ```bash
   sudo bash deploy/reset-db.sh --no-demo
   # (Windows: .\reset-db-windows.ps1 -NoDemo)
   ```
   Kỳ vọng: login admin OK, nhưng trang `/van-ban-den` RỖNG (không seed 002) — đây là production simulation.

**Nếu step nào fail:** type "issue: <mô tả>" để orchestrator biết phần nào cần fix.

**Nếu tất cả OK:** type "approved".
  </action>
  <verify>
    <automated>MISSING — human-verify checkpoint (chạy deploy script + login + browse UI; không tự động hóa được)</automated>
  </verify>
  <done>User confirm "approved": reset-db chạy xong OK, admin login Admin@123 thành công, /van-ban-den hiển thị ≥ 50 record, /ky-so/cau-hinh thấy 2 provider, re-run reset-db lần 2 không lỗi, --no-demo flag hoạt động đúng</done>
  <acceptance_criteria>
    - User chạy `sudo bash deploy/reset-db.sh` (hoặc PowerShell) thành công
    - Login admin/Admin@123 vào /login thành công, redirect dashboard
    - Trang /van-ban-den hiển thị ≥ 50 row
    - Trang /van-ban-di hiển thị ≥ 30 row
    - Trang /du-thao hiển thị ≥ 20 row
    - Trang /ho-so-cong-viec hiển thị ≥ 15 row
    - Trang /ky-so/cau-hinh (admin) hiển thị 2 card (SmartCA active, MySign inactive)
    - Re-run reset-db lần 2 liên tiếp: zero error
    - Flag --no-demo: admin login OK nhưng data pages RỖNG
    - User resume-signal: "approved" hoặc "issue: <mô tả>"
  </acceptance_criteria>
  <resume-signal>Type "approved" sau khi verify các bước trên thành công, hoặc "issue: <mô tả>" nếu có vấn đề</resume-signal>
</task>

</tasks>

<verification>
## Plan-level verification (sau 3 tasks)

```bash
# Archive structure
ls e_office_app_new/database/archive/v1.0-migrations/
# Expected: 000_full_schema.sql + 11 quick_*.sql + seed_full_demo.sql + README.md (13 file)
ls e_office_app_new/database/archive/v2.0-incrementals/
# Expected: 040-046 (6 file) + README.md (7 file)

# migrations/ folder sạch
ls e_office_app_new/database/migrations/
# Expected: chỉ README.md (hoặc empty)

# Deploy scripts chạy OK (syntax check)
bash -n deploy/reset-db.sh
bash -n deploy/deploy.sh
# PowerShell: syntax check qua Test-Path + Invoke-Expression hoặc chạy thực tế

# CLAUDE.md có section DB Migration Strategy
grep -n "DB Migration Strategy" CLAUDE.md

# deploy/README.md có section mới
grep -n "Cấu trúc DB (v2.0 consolidated)" deploy/README.md
grep -n "SIGNING_SECRET_KEY" deploy/README.md

# Git log giữ history
git log --follow --oneline e_office_app_new/database/archive/v2.0-incrementals/040_signing_schema.sql | head -3
# Expected: commit history từ khi file được tạo
```
</verification>

<success_criteria>
1. 4 deploy scripts (reset-db.sh/.ps1 + deploy.sh/.ps1) đã update sang flow mới, syntax valid
2. 18 file migration cũ moved vào `archive/` (12 v1.0 + 6 v2.0), giữ git history (qua git mv)
3. `seed_full_demo.sql` cũ moved vào archive
4. Folder `migrations/` sạch (empty hoặc chỉ có README pointer)
5. 2 README.md trong archive giải thích rõ không chạy file nào, merge vào file nào
6. `deploy/README.md` cập nhật đầy đủ: cấu trúc DB mới + reset flow mới + env var SIGNING_SECRET_KEY
7. `CLAUDE.md` có section mới "## DB Migration Strategy (v2.0+)" với rules: edit master file, không thêm incremental files, bump version khi milestone
8. Human-verify checkpoint pass: user confirm reset-db chạy OK trên test env, login admin OK, data hiển thị ≥ 50 VB đến
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-db-consolidation-seed-strategy/11.1-03-SUMMARY.md` documenting:
- 4 deploy scripts updated (list changes summary)
- 18 file moved vào archive (danh sách)
- CLAUDE.md section mới (link tới dòng bắt đầu + end)
- deploy/README.md sections mới
- Kết quả human-verify checkpoint (screenshot hoặc text log từ user)
- Post-phase state: từ giờ pull code + reset-db = 1 command chạy xong

Tạo thêm file phase-level SUMMARY:
- `.planning/phases/11.1-db-consolidation-seed-strategy/PHASE-SUMMARY.md` tổng hợp 3 plans, kết quả cuối (tổng records, file changes), decisions key (vì sao tách seed 001 vs 002, vì sao dùng schema folder riêng)
</output>
