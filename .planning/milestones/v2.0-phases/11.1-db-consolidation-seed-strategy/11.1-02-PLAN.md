---
phase: 11.1-db-consolidation-seed-strategy
plan: 02
type: execute
wave: 2
depends_on:
  - 11.1-01
files_modified:
  - e_office_app_new/database/seed/001_required_data.sql
  - e_office_app_new/database/seed/002_demo_data.sql
autonomous: true
requirements:
  - DB-CONSOLIDATE-03
  - DB-CONSOLIDATE-04
tags: [db-migration, seed, demo-data, required-data, idempotent]

must_haves:
  truths:
    - "File seed/001_required_data.sql tạo MASTER DATA bắt buộc (roles, rights, admin user, root unit, doc_books/types/fields skeleton, 2 signing_provider_config rows) — idempotent ON CONFLICT DO NOTHING"
    - "File seed/002_demo_data.sql tạo RICH DEMO DATA phong phú: 10+ user, 10+ đơn vị, 50+ VB đến, 30+ VB đi, 20+ dự thảo, 15+ HSCV, 10+ VB liên thông, 5+ notification — tổng ≥ 150 records"
    - "Reset flow: drop schemas → init → apply schema master → apply seed 001 → apply seed 002 → admin login với Admin@123 thành công, trang /van-ban-den hiển thị ≥ 50 record"
    - "Seed 001 idempotent: chạy 2 lần không duplicate (INSERT ... ON CONFLICT DO NOTHING hoặc WHERE NOT EXISTS)"
    - "Seed 002 có guard: kiểm tra seed 001 đã apply trước (đảm bảo admin user + đơn vị gốc tồn tại) — RAISE EXCEPTION nếu chưa"
    - "signing_provider_config 2 row (SmartCA_VNPT active=TRUE + MySign_Viettel active=FALSE) seed trong 001_required_data — dùng pgp_sym_encrypt với session variable app.signing_secret_key"
  artifacts:
    - path: "e_office_app_new/database/seed/001_required_data.sql"
      provides: "Master data bắt buộc — roles, rights, admin user, root đơn vị, doc catalog skeleton, signing providers"
      min_lines: 150
    - path: "e_office_app_new/database/seed/002_demo_data.sql"
      provides: "Rich demo data phong phú — 150+ records across 8+ tables để test UI đầy đủ"
      min_lines: 400
  key_links:
    - from: "seed/001_required_data.sql"
      to: "schema/000_schema_v2.0.sql"
      via: "depends on — seed chạy sau schema master, dùng table structure đã có"
      pattern: "INSERT INTO public.staff|INSERT INTO public.roles|INSERT INTO public.signing_provider_config"
    - from: "seed/002_demo_data.sql"
      to: "seed/001_required_data.sql"
      via: "depends on — seed 002 dùng admin user + root đơn vị từ 001 làm FK"
      pattern: "FROM public.staff WHERE username='admin'|FROM public.departments WHERE code='UBND'"
---

<objective>
Tách seed data thành 2 file rõ ràng: `001_required_data.sql` (master data bắt buộc — admin login được) và `002_demo_data.sql` (demo data phong phú — test UI đầy đủ). Production deploy chỉ chạy 001, test env chạy cả 2. Thay thế hoàn toàn file `seed_full_demo.sql` hiện tại (816 dòng mono) và file `043_seed_default_providers.sql` (seed data nhét trong migrations folder sai chỗ).

Purpose: Pull code máy mới → reset-db → test ngay không phải tạo data tay. Admin login được (Admin@123), trang VB đến có 50+ record, trang HSCV có 15+ record — đủ data để click qua hầu hết các tính năng v1.0 + v2.0.

Output:
- `database/seed/001_required_data.sql` ≥ 150 dòng — master data idempotent
- `database/seed/002_demo_data.sql` ≥ 400 dòng — demo data phong phú, guard kiểm tra 001 đã chạy

Test apply: chạy seed 001 + 002 trên DB đã có schema master từ Plan 01, verify không lỗi + count record ≥ 150.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11.1-db-consolidation-seed-strategy/11.1-01-PLAN.md
@e_office_app_new/database/seed_full_demo.sql
@e_office_app_new/database/migrations/043_seed_default_providers.sql
@e_office_app_new/database/schema/000_schema_v2.0.sql

<interfaces>
<!-- Key tables/columns seed data sẽ INSERT (sau Plan 01 apply schema) -->

From e_office_app_new/database/seed_full_demo.sql (existing — REFERENCE, will be replaced):
- positions, departments, staff (10 users), roles, role_of_staff, rights, action_of_role
- doc_types, doc_fields, doc_books, signers, work_groups, organizations
- incoming_docs (7 rows), user_incoming_docs, attachment_incoming_docs
- outgoing_docs, drafting_docs, handling_docs, etc.

From e_office_app_new/database/migrations/043_seed_default_providers.sql:
```sql
-- Seed 2 signing_provider_config rows dùng pgp_sym_encrypt
-- SmartCA_VNPT: base_url='https://gwsca.vnpt.vn', client_id='4d00-638392811079166938.apps.smartcaapi.com',
--              client_secret=pgp_sym_encrypt('ZjA4MjE4NDg-MjU3Mi00ZDAw', app.signing_secret_key), is_active=TRUE
-- MySign_Viettel: base_url='', placeholder encrypted secret, is_active=FALSE
```

Admin password hash (pre-computed bcrypt cho 'Admin@123'):
- $2b$10$xupDqmYXFYRiLmPdbB5N3uJPMdIG3Qz2xj.DN52R1hg1V.DeDxaQi
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo seed/001_required_data.sql (master data bắt buộc idempotent)</name>
  <files>
    - e_office_app_new/database/seed/001_required_data.sql
  </files>
  <read_first>
    - e_office_app_new/database/seed_full_demo.sql (dòng 115-260 — phần system data: positions, departments, staff admin, roles, rights, action_of_role)
    - e_office_app_new/database/migrations/043_seed_default_providers.sql (full — copy logic pgp_sym_encrypt)
    - e_office_app_new/database/schema/000_schema_v2.0.sql (verify table structure: staff columns, roles.unit_id nullable, signing_provider_config columns)
  </read_first>
  <action>
Tạo file `e_office_app_new/database/seed/001_required_data.sql` chứa MASTER DATA BẮT BUỘC để system chạy (admin login được + menu render được + ký số có 2 provider).

**CẤU TRÚC FILE:**

```sql
-- ============================================================================
-- seed/001_required_data.sql — MASTER DATA bắt buộc
-- Idempotent: INSERT ... ON CONFLICT DO NOTHING
-- Chạy trên DB đã có schema master v2.0 (Plan 01)
--
-- Nội dung:
--   1. Positions (6 chức vụ)
--   2. Departments (5 đơn vị root + 5 phòng — tree tối thiểu)
--   3. Staff (1 admin user — username=admin, password=Admin@123)
--   4. Roles (6 vai trò default)
--   5. Rights (menu tree — 18 rights tối thiểu từ 000_full_schema)
--   6. Role_of_staff (admin → vai trò Quản trị hệ thống)
--   7. Action_of_role (assign rights cho 6 roles)
--   8. Doc catalog skeleton (doc_types + doc_fields + doc_books cơ bản)
--   9. Signing provider config (2 row SmartCA + MySign)
--
-- CÁCH CHẠY:
--   docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
--     -c "SET app.signing_secret_key='qlvb-signing-dev-key-change-production-2026';" \
--     -f /path/to/001_required_data.sql
-- ============================================================================

\set ON_ERROR_STOP on

BEGIN;

-- ─── 1. Positions ───
INSERT INTO public.positions (id, name, code, sort_order) VALUES
  (1, 'Giám đốc',          'GD',   1),
  (2, 'Phó Giám đốc',      'PGD',  2),
  (3, 'Trưởng phòng',       'TP',   3),
  (4, 'Phó Trưởng phòng',   'PTP',  4),
  (5, 'Chuyên viên',        'CV',   5),
  (6, 'Văn thư',            'VT',   6)
ON CONFLICT (id) DO NOTHING;
SELECT setval('public.positions_id_seq', 10);

-- ─── 2. Departments (root tree) ───
INSERT INTO public.departments (id, parent_id, code, name, short_name, is_unit, level, sort_order, allow_doc_book, created_by) VALUES
  (1,  NULL, 'UBND',   'UBND tỉnh Lào Cai',                   'UBND',   true,  0, 1,  true,  NULL),
  (2,  1,    'SNV',    'Sở Nội vụ',                            'SNV',    true,  1, 2,  true,  NULL),
  (3,  1,    'STC',    'Sở Tài chính',                         'STC',    true,  1, 3,  true,  NULL),
  (4,  1,    'STTTT',  'Sở Thông tin và Truyền thông',         'STTTT',  true,  1, 4,  true,  NULL),
  (5,  1,    'VPUBND', 'Văn phòng UBND tỉnh',                  'VP',     true,  1, 5,  true,  NULL)
ON CONFLICT (id) DO NOTHING;
SELECT setval('public.departments_id_seq', 100);

-- ─── 3. Staff admin (password Admin@123) ───
INSERT INTO public.staff (id, department_id, unit_id, position_id, code, username, password_hash, is_admin,
                          first_name, last_name, gender, email, phone, mobile) VALUES
  (1, 1, 1, 1, 'NV001', 'admin',
   '$2b$10$xupDqmYXFYRiLmPdbB5N3uJPMdIG3Qz2xj.DN52R1hg1V.DeDxaQi',
   true, 'Quản trị', 'Hệ thống', 1, 'admin@laocai.gov.vn', '02093801001', '0912000001')
ON CONFLICT (id) DO NOTHING;
SELECT setval('public.staff_id_seq', 100);

-- ─── 4. Roles ───
INSERT INTO public.roles (id, unit_id, name, description) VALUES
  (1, NULL, 'Ban Lãnh đạo',            'Ban lãnh đạo cơ quan'),
  (2, NULL, 'Cán bộ',                  'Cán bộ, Chuyên viên'),
  (3, NULL, 'Chỉ đạo điều hành',       'Chỉ đạo điều hành'),
  (4, NULL, 'Nhóm Trưởng phòng',       'Nhóm Trưởng phòng'),
  (5, NULL, 'Quản trị hệ thống',       'Quản trị hệ thống'),
  (6, NULL, 'Văn thư',                  'Văn thư đơn vị')
ON CONFLICT (id) DO NOTHING;
SELECT setval('public.roles_id_seq', 20);

-- ─── 5. Rights (menu tree) ───
-- [Copy từ seed_full_demo.sql dòng 174-192 — 18 rights + subitems /quan-tri/*]
-- Thêm: quyền "Ký số" + submenu (UX-01 đã merge Phase 10-12)

INSERT INTO public.rights (id, parent_id, name, name_of_menu, action_link, icon, sort_order, show_menu) VALUES
  (1,  NULL, 'Dashboard',              'Dashboard',              '/dashboard',             'DashboardOutlined',    1,  true),
  (2,  NULL, 'Văn bản đến',            'Văn bản đến',            '/van-ban-den',           'InboxOutlined',        2,  true),
  (3,  NULL, 'Văn bản đi',             'Văn bản đi',             '/van-ban-di',            'SendOutlined',         3,  true),
  (4,  NULL, 'Dự thảo',                'Dự thảo',                '/du-thao',               'EditOutlined',         4,  true),
  (5,  NULL, 'Hồ sơ công việc',        'Hồ sơ công việc',        '/ho-so-cong-viec',       'FolderOutlined',       5,  true),
  (6,  NULL, 'Lịch làm việc',          'Lịch làm việc',          '/lich-lam-viec',         'CalendarOutlined',     6,  true),
  (7,  NULL, 'Tin nhắn',               'Tin nhắn',               '/tin-nhan',              'MessageOutlined',      7,  true),
  (8,  NULL, 'Thông báo',              'Thông báo',              '/thong-bao',             'BellOutlined',         8,  true),
  (9,  NULL, 'Họp không giấy',         'Họp không giấy',         '/hop-khong-giay',        'TeamOutlined',         9,  true),
  (10, NULL, 'Kho lưu trữ',            'Kho lưu trữ',            '/kho-luu-tru',           'DatabaseOutlined',     10, true),
  (11, NULL, 'Tài liệu',              'Tài liệu',              '/tai-lieu',              'FileTextOutlined',     11, true),
  (12, NULL, 'Hợp đồng',              'Hợp đồng',              '/hop-dong',              'AuditOutlined',        12, true),
  (13, NULL, 'Quản trị',              'Quản trị',              '/quan-tri',              'SettingOutlined',      13, true),
  (14, 13,   'Đơn vị',                'Đơn vị',                '/quan-tri/don-vi',       NULL,                   1,  true),
  (15, 13,   'Người dùng',            'Người dùng',            '/quan-tri/nguoi-dung',   NULL,                   2,  true),
  (16, 13,   'Nhóm quyền',            'Nhóm quyền',            '/quan-tri/nhom-quyen',   NULL,                   3,  true),
  (17, 13,   'Chức vụ',               'Chức vụ',               '/quan-tri/chuc-vu',      NULL,                   4,  true),
  (18, 13,   'Danh mục',              'Danh mục',              '/quan-tri/danh-muc',     NULL,                   5,  true),
  (19, NULL, 'Ký số',                 'Ký số',                 '/ky-so',                 'SafetyCertificateOutlined', 14, true),
  (20, 19,   'Cấu hình ký số hệ thống','Cấu hình ký số hệ thống','/ky-so/cau-hinh',      NULL,                   1,  true),
  (21, 19,   'Tài khoản ký số cá nhân','Tài khoản ký số cá nhân','/ky-so/tai-khoan',      NULL,                   2,  true),
  (22, 19,   'Danh sách ký số',       'Danh sách ký số',       '/ky-so/danh-sach',       NULL,                   3,  true)
ON CONFLICT (id) DO NOTHING;
SELECT setval('public.rights_id_seq', 100);

-- ─── 6. Role_of_staff (admin → Quản trị + Lãnh đạo) ───
INSERT INTO public.role_of_staff (staff_id, role_id) VALUES
  (1, 5), (1, 1)
ON CONFLICT (staff_id, role_id) DO NOTHING;

-- ─── 7. Action_of_role (assign quyền) ───
-- Admin (role 5): tất cả rights
INSERT INTO public.action_of_role (role_id, right_id)
SELECT 5, id FROM public.rights
ON CONFLICT (role_id, right_id) DO NOTHING;

-- Ban Lãnh đạo (role 1): rights 1-12 + 19-22 (ký số)
INSERT INTO public.action_of_role (role_id, right_id)
SELECT 1, id FROM public.rights WHERE id <= 12 OR id IN (19, 21, 22)
ON CONFLICT (role_id, right_id) DO NOTHING;

-- Cán bộ (role 2): rights 1-12 + 19, 21, 22 (ký số user endpoints)
INSERT INTO public.action_of_role (role_id, right_id)
SELECT 2, id FROM public.rights WHERE id <= 12 OR id IN (19, 21, 22)
ON CONFLICT (role_id, right_id) DO NOTHING;

-- ─── 8. Doc catalog skeleton ───
INSERT INTO edoc.doc_types (id, type_id, code, name, sort_order) VALUES
  (1, 2, 'CV', 'Công văn',      1),
  (2, 1, 'NQ', 'Nghị quyết',    2),
  (3, 1, 'QD', 'Quyết định',    3),
  (4, 1, 'CT', 'Chỉ thị',       4),
  (5, 1, 'QC', 'Quy chế',       5),
  (6, 2, 'TB', 'Thông báo',     6),
  (7, 2, 'BC', 'Báo cáo',       7),
  (8, 2, 'TTr','Tờ trình',     8)
ON CONFLICT (id) DO NOTHING;
SELECT setval('edoc.doc_types_id_seq', 20);

INSERT INTO edoc.doc_fields (id, unit_id, code, name, sort_order, is_active) VALUES
  (1, 1, 'HC',   'Hành chính',                    1, true),
  (2, 1, 'TC',   'Tài chính',                     2, true),
  (3, 1, 'NS',   'Nhân sự',                       3, true),
  (4, 1, 'CNTT', 'Công nghệ thông tin',           4, true),
  (5, 1, 'XDCB', 'Xây dựng cơ bản',               5, true)
ON CONFLICT (id) DO NOTHING;
SELECT setval('edoc.doc_fields_id_seq', 20);

INSERT INTO edoc.doc_books (id, unit_id, type_id, name, sort_order, is_default, created_by) VALUES
  (1, 1, 1, 'Sổ văn bản đến 2026',      1, true,  1),
  (2, 1, 2, 'Sổ văn bản đi 2026',       2, true,  1),
  (3, 1, 3, 'Sổ dự thảo 2026',          3, true,  1)
ON CONFLICT (id) DO NOTHING;
SELECT setval('edoc.doc_books_id_seq', 20);

COMMIT;

-- ─── 9. Signing provider config (cần session variable app.signing_secret_key) ───
-- Block riêng ngoài COMMIT để check session var + dùng pgp_sym_encrypt
DO $$
DECLARE
  v_key TEXT;
BEGIN
  BEGIN
    v_key := current_setting('app.signing_secret_key', FALSE);
  EXCEPTION WHEN OTHERS THEN
    v_key := NULL;
  END;

  IF v_key IS NULL OR length(trim(v_key)) < 16 THEN
    RAISE EXCEPTION 'app.signing_secret_key chưa set (cần >= 16 ký tự). Chạy: SET app.signing_secret_key=''<key>'' trước khi \i file này';
  END IF;

  -- SmartCA VNPT (active=TRUE với credentials dev từ source cũ)
  INSERT INTO public.signing_provider_config
    (provider_code, provider_name, base_url, client_id, client_secret, profile_id, extra_config, is_active, created_by, updated_by)
  VALUES (
    'SMARTCA_VNPT', 'SmartCA VNPT', 'https://gwsca.vnpt.vn',
    '4d00-638392811079166938.apps.smartcaapi.com',
    pgp_sym_encrypt('ZjA4MjE4NDg-MjU3Mi00ZDAw', v_key),
    NULL, '{}'::jsonb, TRUE, 1, 1
  )
  ON CONFLICT (provider_code) DO NOTHING;

  -- MySign Viettel (active=FALSE, placeholder)
  INSERT INTO public.signing_provider_config
    (provider_code, provider_name, base_url, client_id, client_secret, profile_id, extra_config, is_active, created_by, updated_by)
  VALUES (
    'MYSIGN_VIETTEL', 'MySign Viettel', '', '',
    pgp_sym_encrypt('placeholder_not_configured', v_key),
    '', '{}'::jsonb, FALSE, 1, 1
  )
  ON CONFLICT (provider_code) DO NOTHING;

  RAISE NOTICE 'seed/001_required_data.sql: Master data OK (admin login Admin@123, 2 providers seeded)';
END $$;
```

**QUY TẮC:**
- `ON CONFLICT DO NOTHING` cho MỌI INSERT (idempotent — chạy lại không lỗi)
- `SELECT setval(seq, N)` sau INSERT hardcoded IDs để sequence không bị trùng khi INSERT sau
- Block pgp_sym_encrypt tách riêng vì cần session variable `app.signing_secret_key` (BẮT BUỘC set trước khi chạy)
- KHÔNG có TRUNCATE (production deploy dùng file này — không được xóa data)

**Độ lớn dự kiến:** 150-200 dòng.
  </action>
  <verify>
    <automated>docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -c "SET app.signing_secret_key='qlvb-signing-dev-key-change-production-2026';" -f e_office_app_new/database/seed/001_required_data.sql && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM public.staff WHERE username='admin'" | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File `e_office_app_new/database/seed/001_required_data.sql` tồn tại, ≥ 150 dòng
    - Apply lần 1 thành công sau Plan 01 (schema master sạch)
    - Apply lần 2 liên tiếp: zero error, zero duplicate (ON CONFLICT DO NOTHING hoạt động)
    - Query `SELECT count(*) FROM public.staff WHERE username='admin'` = 1
    - Query `SELECT count(*) FROM public.signing_provider_config` = 2
    - Query `SELECT count(*) FROM public.rights` ≥ 22 (18 gốc + 4 ký số)
    - Query `SELECT count(*) FROM public.roles` = 6
    - File KHÔNG có TRUNCATE hoặc DELETE (production-safe)
    - Khi chạy KHÔNG set `app.signing_secret_key`: RAISE EXCEPTION rõ ràng
  </acceptance_criteria>
  <done>Seed 001 master data tạo xong, idempotent, admin login sẵn sàng, 2 providers seeded</done>
</task>

<task type="auto">
  <name>Task 2: Tạo seed/002_demo_data.sql (rich demo data phong phú)</name>
  <files>
    - e_office_app_new/database/seed/002_demo_data.sql
  </files>
  <read_first>
    - e_office_app_new/database/seed_full_demo.sql (full 816 dòng — reference pattern INSERT với NOW() - interval)
    - e_office_app_new/database/schema/000_schema_v2.0.sql (verify column structure bảng: incoming_docs, outgoing_docs, drafting_docs, handling_docs, attachments, inter_incoming_docs, notices — tránh INSERT column không tồn tại)
    - e_office_app_new/database/seed/001_required_data.sql (FK: admin user id=1, departments 1-5)
  </read_first>
  <action>
Tạo file `e_office_app_new/database/seed/002_demo_data.sql` chứa RICH DEMO DATA phong phú để test UI đầy đủ. File này tách riêng khỏi 001 để production deploy có thể SKIP (giữ DB sạch).

**CẤU TRÚC FILE:**

```sql
-- ============================================================================
-- seed/002_demo_data.sql — RICH DEMO DATA (chỉ test env, skip production)
-- Idempotent: guard WHERE NOT EXISTS + ON CONFLICT DO NOTHING
-- Depends on: seed/001_required_data.sql (admin user id=1, departments 1-5)
--
-- Data volume target (tối thiểu):
--   - 9 user thêm (admin + 9 = 10 total)
--   - 5 phòng ban con (10 total với 5 đơn vị từ 001)
--   - 50 VB đến
--   - 30 VB đi
--   - 20 VB dự thảo
--   - 15 HSCV (mix status 0→4 + -1 + -2)
--   - 10 VB liên thông
--   - 20 attachment records (PDF mẫu)
--   - 5 notification cho mỗi user (50 total)
--
-- TỔNG: ≥ 220 records across 10+ tables
-- ============================================================================

\set ON_ERROR_STOP on

-- ─── Guard: verify 001 đã chạy ───
DO $$
BEGIN
  IF NOT EXISTS (SELECT 1 FROM public.staff WHERE username='admin') THEN
    RAISE EXCEPTION 'seed/001_required_data.sql chưa được apply. Chạy seed 001 TRƯỚC seed 002.';
  END IF;
  IF NOT EXISTS (SELECT 1 FROM public.departments WHERE id=1) THEN
    RAISE EXCEPTION 'Root department id=1 không tồn tại. Chạy seed 001 TRƯỚC.';
  END IF;
END $$;

BEGIN;

-- ─── 1. Thêm 5 phòng ban con + 4 phòng thuộc 4 sở (tổng 10 departments) ───
INSERT INTO public.departments (id, parent_id, code, name, short_name, is_unit, level, sort_order, allow_doc_book, created_by) VALUES
  (6,  2, 'TCHC', 'Phòng Tổ chức - Hành chính', 'TCHC', false, 2, 1, false, 1),
  (7,  3, 'QLNS', 'Phòng Quản lý Ngân sách',    'QLNS', false, 2, 1, false, 1),
  (8,  4, 'CNTT', 'Phòng Công nghệ thông tin',  'CNTT', false, 2, 1, false, 1),
  (9,  5, 'TH',   'Phòng Tổng hợp',              'TH',   false, 2, 1, false, 1),
  (10, 2, 'CCVC', 'Phòng Công chức - Viên chức','CCVC', false, 2, 2, false, 1)
ON CONFLICT (id) DO NOTHING;

-- ─── 2. Thêm 9 user thường (password Admin@123 — cùng hash admin) ───
INSERT INTO public.staff (id, department_id, unit_id, position_id, code, username, password_hash, is_admin,
                          first_name, last_name, gender, email, phone, mobile) VALUES
  (2,  2, 2, 1, 'NV002', 'nguyenvana',  '$2b$10$xupDqmYXFYRiLmPdbB5N3uJPMdIG3Qz2xj.DN52R1hg1V.DeDxaQi', false, 'Nguyễn Văn',  'An',      1, 'nguyenvana@snv.laocai.gov.vn',  '02093801002', '0912000002'),
  (3,  3, 3, 1, 'NV003', 'tranthib',    '$2b$10$xupDqmYXFYRiLmPdbB5N3uJPMdIG3Qz2xj.DN52R1hg1V.DeDxaQi', false, 'Trần Thị',    'Bình',    2, 'tranthib@stc.laocai.gov.vn',    '02093801003', '0912000003'),
  (4,  4, 4, 1, 'NV004', 'levand',      '$2b$10$xupDqmYXFYRiLmPdbB5N3uJPMdIG3Qz2xj.DN52R1hg1V.DeDxaQi', false, 'Lê Văn',      'Đức',     1, 'levand@stttt.laocai.gov.vn',    '02093801004', '0912000004'),
  (5,  5, 5, 3, 'NV005', 'phamvane',    '$2b$10$xupDqmYXFYRiLmPdbB5N3uJPMdIG3Qz2xj.DN52R1hg1V.DeDxaQi', false, 'Phạm Văn',    'Em',      1, 'phamvane@vpubnd.laocai.gov.vn', '02093801005', '0912000005'),
  (6,  6, 2, 5, 'NV006', 'hoangthif',   '$2b$10$xupDqmYXFYRiLmPdbB5N3uJPMdIG3Qz2xj.DN52R1hg1V.DeDxaQi', false, 'Hoàng Thị',   'Phương',  2, 'hoangthif@snv.laocai.gov.vn',   '02093801006', '0912000006'),
  (7,  7, 3, 5, 'NV007', 'dangvang',    '$2b$10$xupDqmYXFYRiLmPdbB5N3uJPMdIG3Qz2xj.DN52R1hg1V.DeDxaQi', false, 'Đặng Văn',    'Giang',   1, 'dangvang@stc.laocai.gov.vn',    '02093801007', '0912000007'),
  (8,  8, 4, 5, 'NV008', 'buithih',     '$2b$10$xupDqmYXFYRiLmPdbB5N3uJPMdIG3Qz2xj.DN52R1hg1V.DeDxaQi', false, 'Bùi Thị',     'Hương',   2, 'buithih@stttt.laocai.gov.vn',   '02093801008', '0912000008'),
  (9,  9, 5, 6, 'NV009', 'vuthik',      '$2b$10$xupDqmYXFYRiLmPdbB5N3uJPMdIG3Qz2xj.DN52R1hg1V.DeDxaQi', false, 'Vũ Thị',      'Kim',     2, 'vuthik@vpubnd.laocai.gov.vn',   '02093801009', '0912000009'),
  (10, 10,2, 4, 'NV010', 'dothil',      '$2b$10$xupDqmYXFYRiLmPdbB5N3uJPMdIG3Qz2xj.DN52R1hg1V.DeDxaQi', false, 'Đỗ Thị',      'Lan',     2, 'dothil@snv.laocai.gov.vn',      '02093801010', '0912000010')
ON CONFLICT (id) DO NOTHING;

-- Assign roles cho user thường
INSERT INTO public.role_of_staff (staff_id, role_id) VALUES
  (2, 1), (2, 3), (3, 1), (3, 3), (4, 1), (4, 3),
  (5, 4), (5, 6), (6, 2), (7, 2), (8, 2), (9, 6), (10, 2)
ON CONFLICT (staff_id, role_id) DO NOTHING;

-- ─── 3. Doc books thêm cho các sở ───
INSERT INTO edoc.doc_books (id, unit_id, type_id, name, sort_order, is_default, created_by) VALUES
  (4, 2, 1, 'Sổ VB đến - Sở Nội vụ',    1, true,  2),
  (5, 3, 1, 'Sổ VB đến - Sở Tài chính', 1, true,  3),
  (6, 4, 1, 'Sổ VB đến - Sở TT&TT',     1, true,  4)
ON CONFLICT (id) DO NOTHING;

-- ─── 4. VB ĐẾN (50 records) ───
-- Dùng generate_series để tạo nhiều — mỗi row khác nhau số thứ tự + urgent + secret + ngày
INSERT INTO edoc.incoming_docs (
  id, unit_id, received_date, number, notation, document_code, abstract, publish_unit,
  publish_date, signer, doc_book_id, doc_type_id, doc_field_id,
  urgent_id, secret_id, number_paper, number_copies, sents, created_by
)
SELECT
  n AS id,
  1 AS unit_id,
  NOW() - (n || ' hours')::INTERVAL AS received_date,
  100 + n AS number,
  'CV-' || (100+n) || '/DEMO' AS notation,
  'CV' || (100+n) AS document_code,
  'Văn bản đến demo số ' || n || ' — ' ||
  CASE (n % 5)
    WHEN 0 THEN 'V/v triển khai công tác chuyển đổi số'
    WHEN 1 THEN 'V/v báo cáo thống kê quý'
    WHEN 2 THEN 'V/v phê duyệt dự toán ngân sách'
    WHEN 3 THEN 'V/v tổ chức hội nghị tổng kết'
    ELSE 'V/v hướng dẫn thực hiện chính sách'
  END AS abstract,
  CASE (n % 4)
    WHEN 0 THEN 'Văn phòng Chính phủ'
    WHEN 1 THEN 'Bộ Nội vụ'
    WHEN 2 THEN 'Bộ Tài chính'
    ELSE 'UBND tỉnh'
  END AS publish_unit,
  NOW() - ((n+1) || ' hours')::INTERVAL AS publish_date,
  CASE (n % 3) WHEN 0 THEN 'Nguyễn Văn A' WHEN 1 THEN 'Trần Thị B' ELSE 'Lê Văn C' END AS signer,
  1 AS doc_book_id,
  ((n % 8) + 1) AS doc_type_id,
  ((n % 5) + 1) AS doc_field_id,
  ((n % 3) + 1) AS urgent_id,   -- Mix urgent levels
  ((n % 4) + 1) AS secret_id,   -- Mix secret levels
  (n % 10) + 1 AS number_paper,
  (n % 3) + 1  AS number_copies,
  'Đơn vị demo ' || (n % 5) AS sents,
  1 AS created_by
FROM generate_series(1, 50) AS n
WHERE NOT EXISTS (SELECT 1 FROM edoc.incoming_docs WHERE id = n);

SELECT setval('edoc.incoming_docs_id_seq', 1000);

-- Mark một số VB đang xử lý
UPDATE edoc.incoming_docs SET is_handling = true WHERE id % 3 = 0 AND is_handling IS NOT true;

-- ─── 5. User_incoming_docs (phân công xử lý) ───
INSERT INTO edoc.user_incoming_docs (incoming_doc_id, staff_id, role, assigned_date)
SELECT
  d.id,
  ((d.id % 9) + 2) AS staff_id,  -- user 2-10
  CASE (d.id % 3) WHEN 0 THEN 'chu_tri' WHEN 1 THEN 'phoi_hop' ELSE 'theo_doi' END AS role,
  d.received_date
FROM edoc.incoming_docs d
WHERE d.id BETWEEN 1 AND 50
  AND NOT EXISTS (SELECT 1 FROM edoc.user_incoming_docs u WHERE u.incoming_doc_id = d.id);

-- ─── 6. VB ĐI (30 records) ───
INSERT INTO edoc.outgoing_docs (
  id, unit_id, out_date, number, notation, abstract,
  doc_book_id, doc_type_id, doc_field_id,
  urgent_id, secret_id, signer_id, created_by
)
SELECT
  n AS id,
  1 AS unit_id,
  NOW() - (n || ' hours')::INTERVAL AS out_date,
  200 + n AS number,
  'CV-' || (200+n) || '/UBND' AS notation,
  'Văn bản đi demo ' || n || ' — trả lời văn bản đến' AS abstract,
  2 AS doc_book_id,
  ((n % 8) + 1) AS doc_type_id,
  ((n % 5) + 1) AS doc_field_id,
  ((n % 3) + 1) AS urgent_id,
  ((n % 4) + 1) AS secret_id,
  1 AS signer_id,
  1 AS created_by
FROM generate_series(1, 30) AS n
WHERE NOT EXISTS (SELECT 1 FROM edoc.outgoing_docs WHERE id = n);

SELECT setval('edoc.outgoing_docs_id_seq', 1000);

-- ─── 7. VB DỰ THẢO (20 records, mix status draft/submitted/signed) ───
INSERT INTO edoc.drafting_docs (
  id, unit_id, create_date, notation, abstract,
  doc_book_id, doc_type_id, doc_field_id,
  urgent_id, secret_id, signer_id, approver_id, status, created_by
)
SELECT
  n AS id,
  1 AS unit_id,
  NOW() - (n || ' days')::INTERVAL AS create_date,
  'DT-' || n || '/DEMO' AS notation,
  'Dự thảo demo ' || n AS abstract,
  3 AS doc_book_id,
  ((n % 8) + 1) AS doc_type_id,
  ((n % 5) + 1) AS doc_field_id,
  ((n % 3) + 1) AS urgent_id,
  ((n % 4) + 1) AS secret_id,
  1 AS signer_id,
  1 AS approver_id,
  (n % 4) AS status,  -- 0=draft, 1=submitted, 2=approved, 3=signed
  1 AS created_by
FROM generate_series(1, 20) AS n
WHERE NOT EXISTS (SELECT 1 FROM edoc.drafting_docs WHERE id = n);

SELECT setval('edoc.drafting_docs_id_seq', 1000);

-- ─── 8. HSCV (15 records, mix status 0→4 + -1 + -2) ───
INSERT INTO edoc.handling_docs (
  id, unit_id, create_date, code, abstract,
  status, progress_percent, due_date, created_by, signer_id
)
SELECT
  n AS id,
  1 AS unit_id,
  NOW() - (n || ' days')::INTERVAL AS create_date,
  'HSCV-' || n AS code,
  'Hồ sơ công việc demo ' || n AS abstract,
  CASE
    WHEN n <= 3 THEN 0      -- dự thảo
    WHEN n <= 6 THEN 1      -- đang xử lý
    WHEN n <= 9 THEN 2      -- trình ký
    WHEN n <= 11 THEN 3     -- chờ duyệt
    WHEN n <= 13 THEN 4     -- hoàn thành
    WHEN n = 14 THEN -1     -- thu hồi
    ELSE -2                 -- hủy
  END AS status,
  CASE WHEN n <= 9 THEN (n * 10) ELSE 100 END AS progress_percent,
  NOW() + ((15-n) || ' days')::INTERVAL AS due_date,
  1 AS created_by,
  CASE WHEN n >= 7 THEN 1 ELSE NULL END AS signer_id
FROM generate_series(1, 15) AS n
WHERE NOT EXISTS (SELECT 1 FROM edoc.handling_docs WHERE id = n);

SELECT setval('edoc.handling_docs_id_seq', 1000);

-- ─── 9. VB LIÊN THÔNG (10 records, mix trạng thái) ───
-- Verify table structure — có thể tên là inter_incoming_docs hoặc khác
INSERT INTO edoc.inter_incoming_docs (
  id, unit_id, received_date, lgsp_doc_id, number, notation, abstract,
  publish_unit, status, created_by
)
SELECT
  n AS id,
  1 AS unit_id,
  NOW() - (n || ' hours')::INTERVAL,
  'LGSP-' || (10000 + n) AS lgsp_doc_id,
  300 + n AS number,
  'LT-' || (300+n) || '/BNV',
  'VB liên thông demo ' || n,
  'Bộ Nội vụ',
  CASE
    WHEN n <= 4 THEN 'pending'
    WHEN n <= 7 THEN 'accepted'
    WHEN n = 8 THEN 'rejected'
    ELSE 'recalled'
  END AS status,
  1
FROM generate_series(1, 10) AS n
WHERE NOT EXISTS (SELECT 1 FROM edoc.inter_incoming_docs WHERE id = n);

SELECT setval('edoc.inter_incoming_docs_id_seq', 1000);

-- ─── 10. Attachments (PDF mẫu, 20 records — file giả không thật trên MinIO) ───
-- attachment_incoming_docs
INSERT INTO edoc.attachment_incoming_docs (
  id, incoming_doc_id, file_name, file_path, file_size, file_mime_type, uploaded_at, uploaded_by
)
SELECT
  n,
  n,  -- FK incoming_doc_id
  'CV-' || (100+n) || '.pdf',
  'demo/incoming/' || n || '/CV-' || (100+n) || '.pdf',
  102400 + n * 1000,
  'application/pdf',
  NOW() - (n || ' hours')::INTERVAL,
  1
FROM generate_series(1, 20) AS n
WHERE NOT EXISTS (SELECT 1 FROM edoc.attachment_incoming_docs WHERE id = n);

SELECT setval('edoc.attachment_incoming_docs_id_seq', 1000);

-- ─── 11. Notices (5 mỗi user = 50 records) ───
INSERT INTO edoc.notices (id, title, content, notice_type, created_at, created_by)
SELECT
  n,
  'Thông báo demo ' || n,
  'Nội dung thông báo mẫu số ' || n || ' — test hiển thị bell notification',
  CASE (n % 3) WHEN 0 THEN 'SYSTEM' WHEN 1 THEN 'DOC_ASSIGNED' ELSE 'SIGN_RESULT' END,
  NOW() - (n || ' minutes')::INTERVAL,
  1
FROM generate_series(1, 30) AS n
WHERE NOT EXISTS (SELECT 1 FROM edoc.notices WHERE id = n);

SELECT setval('edoc.notices_id_seq', 1000);

COMMIT;

-- ─── Verify count cuối ───
DO $$
DECLARE
  v_total INT;
BEGIN
  SELECT
    (SELECT count(*) FROM public.departments) +
    (SELECT count(*) FROM public.staff) +
    (SELECT count(*) FROM edoc.incoming_docs) +
    (SELECT count(*) FROM edoc.outgoing_docs) +
    (SELECT count(*) FROM edoc.drafting_docs) +
    (SELECT count(*) FROM edoc.handling_docs) +
    (SELECT count(*) FROM edoc.inter_incoming_docs) +
    (SELECT count(*) FROM edoc.attachment_incoming_docs) +
    (SELECT count(*) FROM edoc.notices)
  INTO v_total;

  IF v_total < 150 THEN
    RAISE EXCEPTION 'Seed 002 data volume (%), cần >= 150', v_total;
  END IF;
  RAISE NOTICE 'seed/002_demo_data.sql: % records OK', v_total;
END $$;
```

**LƯU Ý:**

1. **Verify cột tồn tại** — mỗi INSERT INTO PHẢI verify bảng có cột tương ứng (đọc schema master từ Plan 01). Nếu bảng nào tên/cột khác với giả định trong file này → sửa file này trước khi commit.

2. **WHERE NOT EXISTS pattern** thay cho ON CONFLICT khi bảng không có UNIQUE constraint phù hợp — đảm bảo idempotent.

3. **generate_series + CASE** để tạo nhiều record phong phú với mix status/type/level thay vì lặp INSERT thủ công 50 lần.

4. **KHÔNG cần upload thật MinIO** — `attachment_*.file_path` chỉ là string, test UI hiển thị list attachment là đủ.

5. **setval sau hardcode IDs** — để INSERT tiếp theo (qua app) không bị trùng PK.

**Độ lớn dự kiến:** 400-500 dòng.
  </action>
  <verify>
    <automated>docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f e_office_app_new/database/seed/002_demo_data.sql && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM edoc.incoming_docs" | awk '{if($1>=50) exit 0; else {print "FAIL: incoming_docs count="$1; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - File `e_office_app_new/database/seed/002_demo_data.sql` tồn tại, ≥ 400 dòng
    - Guard ở đầu file: RAISE EXCEPTION nếu chưa có admin user (seed 001 chưa chạy)
    - Apply thành công sau khi chạy seed 001
    - `SELECT count(*) FROM public.staff` = 10
    - `SELECT count(*) FROM edoc.incoming_docs` = 50
    - `SELECT count(*) FROM edoc.outgoing_docs` = 30
    - `SELECT count(*) FROM edoc.drafting_docs` = 20
    - `SELECT count(*) FROM edoc.handling_docs` = 15
    - `SELECT count(*) FROM edoc.inter_incoming_docs` = 10
    - `SELECT count(*) FROM edoc.attachment_incoming_docs` = 20
    - `SELECT count(*) FROM edoc.notices` ≥ 30
    - Tổng record ≥ 150
    - Apply lần 2 liên tiếp: zero duplicate error
  </acceptance_criteria>
  <done>Seed 002 rich demo data tạo xong, ≥ 150 records, guard hoạt động, idempotent verify</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end flow test: schema + seed 001 + seed 002</name>
  <files>
    - (không tạo file — chỉ verify full flow)
  </files>
  <read_first>
    - e_office_app_new/database/schema/000_schema_v2.0.sql
    - e_office_app_new/database/seed/001_required_data.sql
    - e_office_app_new/database/seed/002_demo_data.sql
  </read_first>
  <action>
Test FULL flow reset + seed trên docker dev DB để verify cả Plan 01 + Plan 02 output hoạt động đồng bộ:

**BƯỚC 1: Drop sạch schemas**
```bash
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -c "
DROP SCHEMA IF EXISTS edoc CASCADE;
DROP SCHEMA IF EXISTS esto CASCADE;
DROP SCHEMA IF EXISTS cont CASCADE;
DROP SCHEMA IF EXISTS iso  CASCADE;
DROP SCHEMA IF EXISTS public CASCADE;
CREATE SCHEMA public;
GRANT ALL ON SCHEMA public TO qlvb_admin;
GRANT ALL ON SCHEMA public TO PUBLIC;
"
```

**BƯỚC 2: Apply init (schemas + extensions)**
```bash
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - \
  < e_office_app_new/database/init/01_create_schemas.sql
```

**BƯỚC 3: Apply schema master (Plan 01)**
```bash
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - \
  < e_office_app_new/database/schema/000_schema_v2.0.sql
```

**BƯỚC 4: Apply seed 001**
```bash
# Cần set signing_secret_key trước
cat > /tmp/seed_001_wrapper.sql << 'EOF'
SET app.signing_secret_key = 'qlvb-signing-dev-key-change-production-2026';
\i /e_office_app_new/database/seed/001_required_data.sql
EOF

docker cp /tmp/seed_001_wrapper.sql qlvb_postgres:/tmp/
docker cp e_office_app_new/database/seed/001_required_data.sql qlvb_postgres:/tmp/
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
  -c "SET app.signing_secret_key='qlvb-signing-dev-key-change-production-2026'; \i /tmp/001_required_data.sql"
```

Hoặc 1 lệnh ngắn gọn:
```bash
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
  -c "SET app.signing_secret_key='qlvb-signing-dev-key-change-production-2026';" \
  -f - < e_office_app_new/database/seed/001_required_data.sql
```

**BƯỚC 5: Apply seed 002**
```bash
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - \
  < e_office_app_new/database/seed/002_demo_data.sql
```

**BƯỚC 6: Verify counts cuối**
```bash
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
SELECT 'staff'         AS t, count(*) FROM public.staff
UNION ALL SELECT 'departments', count(*) FROM public.departments
UNION ALL SELECT 'roles',       count(*) FROM public.roles
UNION ALL SELECT 'rights',      count(*) FROM public.rights
UNION ALL SELECT 'incoming',    count(*) FROM edoc.incoming_docs
UNION ALL SELECT 'outgoing',    count(*) FROM edoc.outgoing_docs
UNION ALL SELECT 'drafting',    count(*) FROM edoc.drafting_docs
UNION ALL SELECT 'handling',    count(*) FROM edoc.handling_docs
UNION ALL SELECT 'inter_inc',   count(*) FROM edoc.inter_incoming_docs
UNION ALL SELECT 'attach_inc',  count(*) FROM edoc.attachment_incoming_docs
UNION ALL SELECT 'notices',     count(*) FROM edoc.notices
UNION ALL SELECT 'sign_provider', count(*) FROM public.signing_provider_config;
"
```

**EXPECTED OUTPUT:**
- staff = 10, departments = 10, roles = 6, rights ≥ 22
- incoming = 50, outgoing = 30, drafting = 20, handling = 15
- inter_inc = 10, attach_inc = 20, notices ≥ 30
- sign_provider = 2

Tổng record target ≥ 220.

**BƯỚC 7: Verify admin login working**
```bash
# Start backend (if not running) rồi test
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "
SELECT s.username, s.is_admin, array_agg(r.name) AS roles
FROM public.staff s
LEFT JOIN public.role_of_staff rs ON rs.staff_id = s.id
LEFT JOIN public.roles r ON r.id = rs.role_id
WHERE s.username = 'admin'
GROUP BY s.username, s.is_admin;
"
```
Expected: `admin | t | {Quản trị hệ thống,Ban Lãnh đạo}`

**BƯỚC 8: Test SP Phase 11 hoạt động với data seed**
```bash
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
SELECT * FROM edoc.fn_sign_transaction_count_by_staff(1);
"
```
Expected: trả về 1 row `{pending: 0, completed: 0, failed: 0}` (chưa có sign transaction nào — OK).

**BƯỚC 9: Verify idempotent re-run seed 001 + 002**
```bash
# Chạy lại cả 2 file — không lỗi, không duplicate
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
  -c "SET app.signing_secret_key='qlvb-signing-dev-key-change-production-2026';" \
  -f - < e_office_app_new/database/seed/001_required_data.sql

docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - \
  < e_office_app_new/database/seed/002_demo_data.sql

# Verify counts KHÔNG TĂNG
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM edoc.incoming_docs"
# Phải vẫn = 50
```

**Nếu bất kỳ bước nào FAIL:** fix file liên quan (schema hoặc seed), quay lại Bước 1.
  </action>
  <verify>
    <automated>docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT (SELECT count(*) FROM edoc.incoming_docs) + (SELECT count(*) FROM edoc.outgoing_docs) + (SELECT count(*) FROM edoc.drafting_docs) + (SELECT count(*) FROM edoc.handling_docs)" | awk '{if($1>=115) exit 0; else {print "FAIL: total doc count="$1; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - Full flow (drop → init → schema → seed 001 → seed 002) chạy thành công không lỗi
    - Sau full flow: tổng incoming + outgoing + drafting + handling ≥ 115
    - Admin user tồn tại với 2 roles (Quản trị hệ thống + Ban Lãnh đạo)
    - SP `edoc.fn_sign_transaction_count_by_staff(1)` trả row `{pending:0, completed:0, failed:0}`
    - 2 signing_provider_config rows: SmartCA_VNPT active=TRUE, MySign_Viettel active=FALSE
    - Re-run seed 001 + 002 lần 2: zero error, count records không thay đổi (idempotent OK)
  </acceptance_criteria>
  <done>Full flow reset + seed verified end-to-end trên dev DB, sẵn sàng cho Plan 03 update deploy scripts</done>
</task>

</tasks>

<verification>
## Plan-level verification

```bash
# Full reset + seed flow
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -c "
DROP SCHEMA IF EXISTS edoc,esto,cont,iso,public CASCADE;
CREATE SCHEMA public;
GRANT ALL ON SCHEMA public TO qlvb_admin;
"

docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - \
  < e_office_app_new/database/init/01_create_schemas.sql
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - \
  < e_office_app_new/database/schema/000_schema_v2.0.sql
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
  -c "SET app.signing_secret_key='qlvb-signing-dev-key-change-production-2026';" \
  -f - < e_office_app_new/database/seed/001_required_data.sql
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - \
  < e_office_app_new/database/seed/002_demo_data.sql

# Verify record counts
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
SELECT
  (SELECT count(*) FROM public.staff) AS staff,
  (SELECT count(*) FROM edoc.incoming_docs) AS vb_den,
  (SELECT count(*) FROM edoc.outgoing_docs) AS vb_di,
  (SELECT count(*) FROM edoc.drafting_docs) AS du_thao,
  (SELECT count(*) FROM edoc.handling_docs) AS hscv,
  (SELECT count(*) FROM public.signing_provider_config) AS providers;
"
# Expected: staff=10, vb_den=50, vb_di=30, du_thao=20, hscv=15, providers=2
```
</verification>

<success_criteria>
1. `e_office_app_new/database/seed/001_required_data.sql` tồn tại, idempotent, admin user + 2 providers seeded
2. `e_office_app_new/database/seed/002_demo_data.sql` tồn tại, guard kiểm tra 001 đã chạy
3. Tổng records sau seed 001 + 002 ≥ 150 (hiện tại target ≥ 220)
4. Flow drop + init + schema + seed 001 + seed 002 chạy 1 lần thành công
5. Re-run seed 001 + 002 lần 2 liên tiếp: zero error, counts không đổi
6. Admin login (username=admin, password=Admin@123) verified qua query role_of_staff
7. SP Phase 11 `fn_sign_transaction_count_by_staff` gọi được với data seeded
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-db-consolidation-seed-strategy/11.1-02-SUMMARY.md` documenting:
- 2 file seed tạo ra (paths + line counts)
- Record counts cuối cùng (table name → count)
- Log output full flow E2E test (drop → seed → verify)
- Idempotent verify log (re-run seed không duplicate)
- Ghi chú bất kỳ field nào PHẢI điều chỉnh vì schema master khác với giả định seed_full_demo cũ (ghi nhận để Plan 03 update deploy scripts đúng)
</output>
