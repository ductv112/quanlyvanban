---
phase: 08-schema-foundation-pdf-signing-generic-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/database/migrations/040_signing_schema.sql
autonomous: true
requirements:
  - MIG-01
  - MIG-02
requirements_addressed:
  - MIG-01
  - MIG-02
must_haves:
  truths:
    - "Bảng public.signing_provider_config tồn tại với partial unique index chặn 2 provider cùng is_active=true"
    - "Bảng public.staff_signing_config tồn tại với composite PK (staff_id, provider_code)"
    - "Bảng edoc.sign_transactions tồn tại với status enum-like CHECK + FK đến staff và provider_config"
    - "Column client_secret của signing_provider_config là BYTEA (encrypted bằng pgp_sym_encrypt)"
    - "3 bảng attachment hiện tại (attachment_incoming/outgoing/drafting_docs) có thêm 2 cột sign_provider_code + sign_transaction_id nullable"
    - "Tất cả SP CRUD (fn_signing_provider_config_*, fn_staff_signing_config_*, fn_sign_transaction_*) tạo thành công và callable"
  artifacts:
    - path: "e_office_app_new/database/migrations/040_signing_schema.sql"
      provides: "Migration tạo 3 bảng mới + ALTER 3 attachment tables + 10+ SP CRUD"
      contains: "CREATE TABLE public.signing_provider_config"
  key_links:
    - from: "public.signing_provider_config"
      to: "public.staff_signing_config"
      via: "provider_code VARCHAR(20) matching (không FK cứng, cho phép giữ config cũ sau khi provider bị xoá)"
      pattern: "provider_code VARCHAR\\(20\\)"
    - from: "edoc.sign_transactions"
      to: "public.staff"
      via: "staff_id BIGINT REFERENCES public.staff(id)"
      pattern: "staff_id\\s+(BIG)?INT\\s+(NOT NULL\\s+)?REFERENCES\\s+public\\.staff"
    - from: "edoc.attachment_*_docs.sign_transaction_id"
      to: "edoc.sign_transactions.id"
      via: "FK nullable"
      pattern: "sign_transaction_id\\s+BIGINT"
---

<objective>
Tạo migration SQL duy nhất `040_signing_schema.sql` chứa toàn bộ thay đổi schema cho hệ thống ký số multi-provider:

1. **3 bảng mới:**
   - `public.signing_provider_config` — Admin cấp 1: cấu hình provider (1 row active/time)
   - `public.staff_signing_config` — User cấp 2: user_id + credential_id của mỗi user cho mỗi provider
   - `edoc.sign_transactions` — Audit log mọi lần ký (pending/completed/failed/cancelled/expired)

2. **ALTER 3 bảng attachment existing** (`attachment_incoming_docs`, `attachment_outgoing_docs`, `attachment_drafting_docs`):
   - Thêm `sign_provider_code VARCHAR(20)` nullable — lưu provider đã dùng để ký
   - Thêm `sign_transaction_id BIGINT` nullable FK — trace về transaction

3. **10+ Stored functions CRUD** cho 3 bảng mới (fn_signing_provider_config_*, fn_staff_signing_config_*, fn_sign_transaction_*).

Purpose: Foundation cho Phase 9+ (admin config UI, sign flow, async worker) — không có bảng này thì không phase nào sau ship được.
Output: 1 file migration SQL (~600-800 lines), chạy vào DB thật (qlvb_postgres) và verify hoạt động.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@e_office_app_new/database/migrations/000_full_schema.sql

<interfaces>
<!-- Key existing schema details extracted from 000_full_schema.sql — executor should NOT re-explore -->

**pgcrypto already enabled** (line 35 of 000_full_schema.sql):
```sql
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
```

**Existing `public.staff` table** (line 142+) — note `staff.id` is `SERIAL` (INT not BIGINT):
```sql
CREATE TABLE IF NOT EXISTS public.staff (
  id              SERIAL PRIMARY KEY,    -- INT !!!
  ...
  sign_phone      VARCHAR(20),           -- added line 2058 via ALTER (NOTE: Plan 02 sẽ migrate+drop)
  sign_ca         TEXT,
  sign_image      VARCHAR(500),
  ...
);
```

**Existing `public.departments` table** — unit_id references departments.id (INT).

**Existing attachment tables (3 tables, NOT một bảng `attachments` duy nhất):**
```sql
-- edoc.attachment_incoming_docs (line 562)
-- edoc.attachment_outgoing_docs (line 700)
-- edoc.attachment_drafting_docs (line 712)
-- Mỗi bảng có: id BIGSERIAL, file_path VARCHAR(1000), created_by INT REFERENCES public.staff(id)
-- Đã thêm (migration cũ 026): is_ca BOOLEAN, ca_date TIMESTAMPTZ, signed_file_path VARCHAR(1000)
```

**Schema convention:**
- `public` schema: system entities (staff, departments, roles)
- `edoc` schema: document entities (incoming_docs, outgoing_docs, sign_transactions)
- SP naming: `{schema}.fn_{module}_{action}`
- SP return: `TABLE(success BOOLEAN, message TEXT, id BIGINT)` cho mutations
</interfaces>

<reserved_words>
**PostgreSQL reserved words PHẢI quote bằng `""` trong RETURNS TABLE và SELECT:**
`position`, `offset`, `limit`, `order`, `user`, `type`, `comment`, `key`, `value`, `name`, `status`.

Cột `status` trong `sign_transactions` PHẢI quote trong RETURNS TABLE: `"status" VARCHAR(20)`.
</reserved_words>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo migration SQL với 3 bảng mới + ALTER 3 attachment tables</name>
  <files>e_office_app_new/database/migrations/040_signing_schema.sql</files>
  <read_first>
    - e_office_app_new/database/migrations/000_full_schema.sql (line 142-190 staff, line 562-722 attachment tables, line 34-37 extensions)
    - e_office_app_new/database/migrations/000_full_schema.sql (line 2050-2080 — staff ALTER with sign_phone)
    - CLAUDE.md (sections: Naming Patterns, Database Access Pattern, Reserved words checklist)
  </read_first>
  <action>
Tạo file `e_office_app_new/database/migrations/040_signing_schema.sql` với nội dung đúng sau (structure bắt buộc):

```sql
-- ============================================================================
-- Migration 040: Signing schema foundation (Phase 8, Plan 01)
-- Requirements: MIG-01, MIG-02
-- Depends on: 000_full_schema.sql (pgcrypto extension, staff table, edoc schema)
-- ============================================================================

-- Đảm bảo pgcrypto (idempotent — migration 000 đã tạo nhưng safe guard)
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- ============================================================================
-- 1. public.signing_provider_config — Admin cấp 1 (provider + credentials hệ thống)
-- ============================================================================
CREATE TABLE IF NOT EXISTS public.signing_provider_config (
  id              BIGSERIAL PRIMARY KEY,
  provider_code   VARCHAR(20) NOT NULL,               -- 'SMARTCA_VNPT' | 'MYSIGN_VIETTEL'
  provider_name   VARCHAR(100) NOT NULL,              -- Hiển thị UI: 'SmartCA VNPT', 'MySign Viettel'
  base_url        VARCHAR(500) NOT NULL,              -- https://gwsca.vnpt.vn/sca/sp769/v1
  client_id       VARCHAR(200) NOT NULL,
  client_secret   BYTEA NOT NULL,                     -- Encrypted bằng pgp_sym_encrypt (plaintext không bao giờ lưu)
  profile_id      VARCHAR(200),                       -- MySign only, SmartCA nullable
  extra_config    JSONB DEFAULT '{}'::jsonb,          -- Extension point cho config riêng mỗi provider
  is_active       BOOLEAN NOT NULL DEFAULT FALSE,
  last_tested_at  TIMESTAMPTZ,                        -- Lần cuối test connection OK
  test_result     TEXT,                               -- 'OK' | error message
  created_by      INT REFERENCES public.staff(id),
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_by      INT REFERENCES public.staff(id),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  CONSTRAINT uq_signing_provider_config_code UNIQUE (provider_code),
  CONSTRAINT chk_provider_code CHECK (provider_code IN ('SMARTCA_VNPT', 'MYSIGN_VIETTEL'))
);

-- Partial unique index: chỉ 1 provider active tại 1 thời điểm
CREATE UNIQUE INDEX IF NOT EXISTS uq_signing_provider_config_active
  ON public.signing_provider_config (is_active)
  WHERE is_active = TRUE;

COMMENT ON TABLE public.signing_provider_config IS 'Cấu hình provider ký số cấp hệ thống (Admin). Partial unique index đảm bảo single-active.';
COMMENT ON COLUMN public.signing_provider_config.client_secret IS 'Encrypted bằng pgp_sym_encrypt(plaintext, key). Key từ env SIGNING_SECRET_KEY.';

-- ============================================================================
-- 2. public.staff_signing_config — User cấp 2 (user_id + certificate của mỗi user/provider)
-- ============================================================================
CREATE TABLE IF NOT EXISTS public.staff_signing_config (
  staff_id            INT NOT NULL REFERENCES public.staff(id) ON DELETE CASCADE,
  provider_code       VARCHAR(20) NOT NULL,         -- Match signing_provider_config.provider_code
  user_id             VARCHAR(200) NOT NULL,        -- SĐT hoặc mã định danh user trên provider
  credential_id       VARCHAR(200),                 -- MySign only — ID certificate chọn từ list
  certificate_data    TEXT,                         -- Base64 cert (lấy về khi verify)
  certificate_subject VARCHAR(500),                 -- Extracted từ cert, hiển thị UI
  certificate_serial  VARCHAR(200),
  is_verified         BOOLEAN NOT NULL DEFAULT FALSE,
  last_verified_at    TIMESTAMPTZ,
  last_error          TEXT,
  created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  CONSTRAINT pk_staff_signing_config PRIMARY KEY (staff_id, provider_code),
  CONSTRAINT chk_staff_signing_provider_code CHECK (provider_code IN ('SMARTCA_VNPT', 'MYSIGN_VIETTEL'))
);

CREATE INDEX IF NOT EXISTS idx_staff_signing_config_staff
  ON public.staff_signing_config (staff_id);

COMMENT ON TABLE public.staff_signing_config IS 'Cấu hình ký số cá nhân per user per provider (composite PK — user có thể có config cho cả 2 provider song song).';

-- ============================================================================
-- 3. edoc.sign_transactions — Audit log mọi giao dịch ký (pending/completed/failed/cancelled/expired)
-- ============================================================================
CREATE TABLE IF NOT EXISTS edoc.sign_transactions (
  id                  BIGSERIAL PRIMARY KEY,
  staff_id            INT NOT NULL REFERENCES public.staff(id),
  provider_code       VARCHAR(20) NOT NULL,        -- Snapshot provider lúc tạo transaction
  provider_txn_id     VARCHAR(200),                -- ID trên phía provider (trả về sau khi tạo sign request)

  -- Liên kết tới attachment đang ký (1 trong 3 loại)
  attachment_id       BIGINT NOT NULL,
  attachment_type     VARCHAR(20) NOT NULL,        -- 'incoming' | 'outgoing' | 'drafting'
  doc_id              BIGINT,                      -- outgoing_doc_id / drafting_doc_id / handling_doc_id
  doc_type            VARCHAR(20),                 -- 'outgoing_doc' | 'drafting_doc' | 'handling_doc'

  -- Dữ liệu ký
  file_hash_sha256    VARCHAR(64),                 -- SHA256 hex của PDF byte-range (PAdES)
  signature_base64    TEXT,                        -- PKCS7 detached signature (provider trả về)
  signed_file_path    VARCHAR(1000),               -- MinIO path của file đã embed signature

  -- Trạng thái
  "status"            VARCHAR(20) NOT NULL DEFAULT 'pending',  -- quote vì status hiểu được nhưng an toàn
  error_message       TEXT,
  retry_count         INT NOT NULL DEFAULT 0,

  -- Timestamps
  created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  started_at          TIMESTAMPTZ,
  completed_at        TIMESTAMPTZ,
  expires_at          TIMESTAMPTZ,                 -- created_at + 3 phút (poll timeout)

  CONSTRAINT chk_sign_transaction_status CHECK ("status" IN ('pending','completed','failed','cancelled','expired')),
  CONSTRAINT chk_sign_transaction_attachment_type CHECK (attachment_type IN ('incoming','outgoing','drafting')),
  CONSTRAINT chk_sign_transaction_provider_code CHECK (provider_code IN ('SMARTCA_VNPT','MYSIGN_VIETTEL'))
);

CREATE INDEX IF NOT EXISTS idx_sign_transactions_staff_status
  ON edoc.sign_transactions (staff_id, "status");
CREATE INDEX IF NOT EXISTS idx_sign_transactions_provider_txn
  ON edoc.sign_transactions (provider_code, provider_txn_id)
  WHERE provider_txn_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_sign_transactions_attachment
  ON edoc.sign_transactions (attachment_type, attachment_id);
CREATE INDEX IF NOT EXISTS idx_sign_transactions_pending
  ON edoc.sign_transactions ("status", created_at)
  WHERE "status" = 'pending';

COMMENT ON TABLE edoc.sign_transactions IS 'Audit log ký số — mọi lần ký tạo 1 record (pending → completed/failed/cancelled/expired).';
COMMENT ON COLUMN edoc.sign_transactions."status" IS 'pending → completed/failed/cancelled/expired. Ký lại = tạo transaction MỚI (giữ record cũ).';

-- ============================================================================
-- 4. ALTER 3 bảng attachment: thêm sign_provider_code + sign_transaction_id
-- ============================================================================
ALTER TABLE edoc.attachment_incoming_docs
  ADD COLUMN IF NOT EXISTS sign_provider_code VARCHAR(20),
  ADD COLUMN IF NOT EXISTS sign_transaction_id BIGINT REFERENCES edoc.sign_transactions(id) ON DELETE SET NULL;

ALTER TABLE edoc.attachment_outgoing_docs
  ADD COLUMN IF NOT EXISTS sign_provider_code VARCHAR(20),
  ADD COLUMN IF NOT EXISTS sign_transaction_id BIGINT REFERENCES edoc.sign_transactions(id) ON DELETE SET NULL;

ALTER TABLE edoc.attachment_drafting_docs
  ADD COLUMN IF NOT EXISTS sign_provider_code VARCHAR(20),
  ADD COLUMN IF NOT EXISTS sign_transaction_id BIGINT REFERENCES edoc.sign_transactions(id) ON DELETE SET NULL;

-- attachment_handling_docs (HSCV trình ký)
ALTER TABLE edoc.attachment_handling_docs
  ADD COLUMN IF NOT EXISTS sign_provider_code VARCHAR(20),
  ADD COLUMN IF NOT EXISTS sign_transaction_id BIGINT REFERENCES edoc.sign_transactions(id) ON DELETE SET NULL;

COMMENT ON COLUMN edoc.attachment_outgoing_docs.sign_provider_code IS 'Provider đã dùng để ký (snapshot, không đổi khi admin switch provider).';

DO $$ BEGIN
  RAISE NOTICE '✅ Migration 040 Part 1: Schema (3 bảng mới + ALTER 4 attachment tables)';
END $$;
```

**Sau schema là 10+ stored functions** — Task 2 sẽ append tiếp vào cùng file.

Lưu ý bắt buộc:
- `client_secret BYTEA` (encrypt bằng pgp_sym_encrypt ở Plan 04, KHÔNG plaintext).
- `"status"` trong `sign_transactions` quote double-quote ở cả CREATE và RETURNS TABLE sau.
- `is_active` partial unique index WHERE is_active = TRUE — đây là key constraint cho CFG-01 (Phase 9).
- Composite PK `(staff_id, provider_code)` — key cho CFG-02.
  </action>
  <verify>
    <automated>
      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -f /dev/stdin < e_office_app_new/database/migrations/040_signing_schema.sql > /tmp/mig040.log 2>&1 && grep -q "Migration 040 Part 1" /tmp/mig040.log && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\d public.signing_provider_config" | grep -q "client_secret" && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\d public.staff_signing_config" | grep -q "provider_code" && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\d edoc.sign_transactions" | grep -q "provider_txn_id" && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\d edoc.attachment_outgoing_docs" | grep -q "sign_transaction_id"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `e_office_app_new/database/migrations/040_signing_schema.sql` tồn tại và chứa chuỗi `CREATE TABLE IF NOT EXISTS public.signing_provider_config`, `CREATE TABLE IF NOT EXISTS public.staff_signing_config`, `CREATE TABLE IF NOT EXISTS edoc.sign_transactions`
    - File chứa chuỗi `CREATE UNIQUE INDEX IF NOT EXISTS uq_signing_provider_config_active` và `WHERE is_active = TRUE`
    - File chứa chuỗi `CONSTRAINT pk_staff_signing_config PRIMARY KEY (staff_id, provider_code)`
    - File chứa `client_secret   BYTEA NOT NULL`
    - File chứa `ALTER TABLE edoc.attachment_incoming_docs` ... `ADD COLUMN IF NOT EXISTS sign_provider_code VARCHAR(20)`
    - Chạy migration vào qlvb_postgres container thành công (exit 0), log chứa "Migration 040 Part 1"
    - Query `\d public.signing_provider_config` trong psql trả về column `client_secret` với type `bytea`
    - Query `\d edoc.sign_transactions` trả về column `provider_txn_id`
    - Query `\d edoc.attachment_outgoing_docs` trả về column `sign_transaction_id`
    - Kiểm tra partial unique index: `SELECT indexname FROM pg_indexes WHERE tablename='signing_provider_config' AND indexdef LIKE '%WHERE%'` phải trả về `uq_signing_provider_config_active`
  </acceptance_criteria>
  <done>Migration schema part chạy thành công vào qlvb_postgres, 3 bảng mới + 4 ALTER tables verified via `\d`.</done>
</task>

<task type="auto">
  <name>Task 2: Append 10 stored functions (CRUD) vào cùng migration file 040</name>
  <files>e_office_app_new/database/migrations/040_signing_schema.sql</files>
  <read_first>
    - e_office_app_new/database/migrations/040_signing_schema.sql (file từ Task 1)
    - e_office_app_new/database/migrations/000_full_schema.sql (grep SP pattern edoc.fn_digital_signature_create line 16000+ làm reference)
    - CLAUDE.md (SP return pattern: `TABLE(success BOOLEAN, message TEXT, id BIGINT)`)
  </read_first>
  <action>
Append các stored functions sau vào cuối file `040_signing_schema.sql` (sau phần schema ở Task 1).

**QUAN TRỌNG:**
- Dùng `CREATE OR REPLACE FUNCTION` để idempotent
- Return shape thống nhất: mutation trả `TABLE(success BOOLEAN, message TEXT, id BIGINT)`, read trả `TABLE(...)` match column tên DB
- KHÔNG encrypt/decrypt client_secret trong SP — Plan 04 sẽ xử lý qua Node-side pgp_sym_encrypt SQL wrapper (SP chỉ nhận/trả BYTEA)
- Quote `"status"` reserved word trong tất cả RETURNS TABLE

```sql
-- ============================================================================
-- STORED FUNCTIONS — signing_provider_config (4 functions)
-- ============================================================================

-- GET LIST — trả mọi provider (Admin xem tất cả)
CREATE OR REPLACE FUNCTION public.fn_signing_provider_config_list()
RETURNS TABLE (
  id BIGINT,
  provider_code VARCHAR,
  provider_name VARCHAR,
  base_url VARCHAR,
  client_id VARCHAR,
  profile_id VARCHAR,
  extra_config JSONB,
  is_active BOOLEAN,
  last_tested_at TIMESTAMPTZ,
  test_result TEXT,
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
)
LANGUAGE plpgsql STABLE AS $$
BEGIN
  RETURN QUERY
    SELECT c.id, c.provider_code, c.provider_name, c.base_url, c.client_id,
           c.profile_id, c.extra_config, c.is_active, c.last_tested_at, c.test_result,
           c.created_at, c.updated_at
    FROM public.signing_provider_config c
    ORDER BY c.is_active DESC, c.provider_code ASC;
END;
$$;

-- GET BY CODE (full — có client_secret BYTEA cho backend decrypt phía Node)
CREATE OR REPLACE FUNCTION public.fn_signing_provider_config_get_by_code(
  p_provider_code VARCHAR
)
RETURNS TABLE (
  id BIGINT,
  provider_code VARCHAR,
  provider_name VARCHAR,
  base_url VARCHAR,
  client_id VARCHAR,
  client_secret BYTEA,
  profile_id VARCHAR,
  extra_config JSONB,
  is_active BOOLEAN,
  last_tested_at TIMESTAMPTZ,
  test_result TEXT
)
LANGUAGE plpgsql STABLE AS $$
BEGIN
  RETURN QUERY
    SELECT c.id, c.provider_code, c.provider_name, c.base_url, c.client_id,
           c.client_secret, c.profile_id, c.extra_config, c.is_active,
           c.last_tested_at, c.test_result
    FROM public.signing_provider_config c
    WHERE c.provider_code = p_provider_code;
END;
$$;

-- GET ACTIVE — lấy provider đang active duy nhất
CREATE OR REPLACE FUNCTION public.fn_signing_provider_config_get_active()
RETURNS TABLE (
  id BIGINT,
  provider_code VARCHAR,
  provider_name VARCHAR,
  base_url VARCHAR,
  client_id VARCHAR,
  client_secret BYTEA,
  profile_id VARCHAR,
  extra_config JSONB
)
LANGUAGE plpgsql STABLE AS $$
BEGIN
  RETURN QUERY
    SELECT c.id, c.provider_code, c.provider_name, c.base_url, c.client_id,
           c.client_secret, c.profile_id, c.extra_config
    FROM public.signing_provider_config c
    WHERE c.is_active = TRUE
    LIMIT 1;
END;
$$;

-- UPSERT (insert nếu chưa có, update nếu có) — cập nhật toàn bộ fields (trừ is_active)
-- Note: client_secret_bytea đã encrypted từ Node-side bằng pgp_sym_encrypt
CREATE OR REPLACE FUNCTION public.fn_signing_provider_config_upsert(
  p_provider_code     VARCHAR,
  p_provider_name     VARCHAR,
  p_base_url          VARCHAR,
  p_client_id         VARCHAR,
  p_client_secret     BYTEA,
  p_profile_id        VARCHAR,
  p_extra_config      JSONB,
  p_last_tested_at    TIMESTAMPTZ,
  p_test_result       TEXT,
  p_updated_by        INT
)
RETURNS TABLE (success BOOLEAN, message TEXT, id BIGINT)
LANGUAGE plpgsql AS $$
DECLARE v_id BIGINT;
BEGIN
  INSERT INTO public.signing_provider_config (
    provider_code, provider_name, base_url, client_id, client_secret,
    profile_id, extra_config, last_tested_at, test_result, created_by, updated_by
  )
  VALUES (
    p_provider_code, p_provider_name, p_base_url, p_client_id, p_client_secret,
    p_profile_id, COALESCE(p_extra_config, '{}'::jsonb), p_last_tested_at, p_test_result, p_updated_by, p_updated_by
  )
  ON CONFLICT (provider_code) DO UPDATE SET
    provider_name   = EXCLUDED.provider_name,
    base_url        = EXCLUDED.base_url,
    client_id       = EXCLUDED.client_id,
    client_secret   = EXCLUDED.client_secret,
    profile_id      = EXCLUDED.profile_id,
    extra_config    = EXCLUDED.extra_config,
    last_tested_at  = EXCLUDED.last_tested_at,
    test_result     = EXCLUDED.test_result,
    updated_by      = EXCLUDED.updated_by,
    updated_at      = NOW()
  RETURNING public.signing_provider_config.id INTO v_id;

  RETURN QUERY SELECT TRUE, 'Lưu cấu hình provider thành công'::TEXT, v_id;
END;
$$;

-- SET ACTIVE — atomic swap: deactivate tất cả provider, activate provider được chọn
CREATE OR REPLACE FUNCTION public.fn_signing_provider_config_set_active(
  p_provider_code VARCHAR,
  p_updated_by    INT
)
RETURNS TABLE (success BOOLEAN, message TEXT)
LANGUAGE plpgsql AS $$
DECLARE v_exists BOOLEAN;
BEGIN
  SELECT EXISTS(SELECT 1 FROM public.signing_provider_config WHERE provider_code = p_provider_code)
    INTO v_exists;
  IF NOT v_exists THEN
    RETURN QUERY SELECT FALSE, 'Không tìm thấy provider'::TEXT; RETURN;
  END IF;

  -- Deactivate all (respects partial unique index)
  UPDATE public.signing_provider_config
    SET is_active = FALSE, updated_at = NOW(), updated_by = p_updated_by
    WHERE is_active = TRUE;

  -- Activate chosen
  UPDATE public.signing_provider_config
    SET is_active = TRUE, updated_at = NOW(), updated_by = p_updated_by
    WHERE provider_code = p_provider_code;

  RETURN QUERY SELECT TRUE, 'Kích hoạt provider thành công'::TEXT;
END;
$$;

-- ============================================================================
-- STORED FUNCTIONS — staff_signing_config (4 functions)
-- ============================================================================

-- GET LIST BY STAFF
CREATE OR REPLACE FUNCTION public.fn_staff_signing_config_list_by_staff(
  p_staff_id INT
)
RETURNS TABLE (
  staff_id INT,
  provider_code VARCHAR,
  user_id VARCHAR,
  credential_id VARCHAR,
  certificate_subject VARCHAR,
  certificate_serial VARCHAR,
  is_verified BOOLEAN,
  last_verified_at TIMESTAMPTZ,
  last_error TEXT,
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
)
LANGUAGE plpgsql STABLE AS $$
BEGIN
  RETURN QUERY
    SELECT c.staff_id, c.provider_code, c.user_id, c.credential_id,
           c.certificate_subject, c.certificate_serial, c.is_verified,
           c.last_verified_at, c.last_error, c.created_at, c.updated_at
    FROM public.staff_signing_config c
    WHERE c.staff_id = p_staff_id
    ORDER BY c.provider_code;
END;
$$;

-- GET BY STAFF + PROVIDER (cho sign flow)
CREATE OR REPLACE FUNCTION public.fn_staff_signing_config_get(
  p_staff_id      INT,
  p_provider_code VARCHAR
)
RETURNS TABLE (
  staff_id INT,
  provider_code VARCHAR,
  user_id VARCHAR,
  credential_id VARCHAR,
  certificate_data TEXT,
  certificate_subject VARCHAR,
  certificate_serial VARCHAR,
  is_verified BOOLEAN,
  last_verified_at TIMESTAMPTZ
)
LANGUAGE plpgsql STABLE AS $$
BEGIN
  RETURN QUERY
    SELECT c.staff_id, c.provider_code, c.user_id, c.credential_id,
           c.certificate_data, c.certificate_subject, c.certificate_serial,
           c.is_verified, c.last_verified_at
    FROM public.staff_signing_config c
    WHERE c.staff_id = p_staff_id AND c.provider_code = p_provider_code;
END;
$$;

-- UPSERT user config
CREATE OR REPLACE FUNCTION public.fn_staff_signing_config_upsert(
  p_staff_id            INT,
  p_provider_code       VARCHAR,
  p_user_id             VARCHAR,
  p_credential_id       VARCHAR,
  p_certificate_data    TEXT,
  p_certificate_subject VARCHAR,
  p_certificate_serial  VARCHAR,
  p_is_verified         BOOLEAN,
  p_last_verified_at    TIMESTAMPTZ,
  p_last_error          TEXT
)
RETURNS TABLE (success BOOLEAN, message TEXT)
LANGUAGE plpgsql AS $$
BEGIN
  INSERT INTO public.staff_signing_config (
    staff_id, provider_code, user_id, credential_id, certificate_data,
    certificate_subject, certificate_serial, is_verified, last_verified_at, last_error
  )
  VALUES (
    p_staff_id, p_provider_code, p_user_id, p_credential_id, p_certificate_data,
    p_certificate_subject, p_certificate_serial, COALESCE(p_is_verified, FALSE),
    p_last_verified_at, p_last_error
  )
  ON CONFLICT (staff_id, provider_code) DO UPDATE SET
    user_id             = EXCLUDED.user_id,
    credential_id       = EXCLUDED.credential_id,
    certificate_data    = COALESCE(EXCLUDED.certificate_data, public.staff_signing_config.certificate_data),
    certificate_subject = COALESCE(EXCLUDED.certificate_subject, public.staff_signing_config.certificate_subject),
    certificate_serial  = COALESCE(EXCLUDED.certificate_serial, public.staff_signing_config.certificate_serial),
    is_verified         = EXCLUDED.is_verified,
    last_verified_at    = EXCLUDED.last_verified_at,
    last_error          = EXCLUDED.last_error,
    updated_at          = NOW();

  RETURN QUERY SELECT TRUE, 'Lưu cấu hình ký số cá nhân thành công'::TEXT;
END;
$$;

-- DELETE config (khi user muốn xóa cấu hình)
CREATE OR REPLACE FUNCTION public.fn_staff_signing_config_delete(
  p_staff_id      INT,
  p_provider_code VARCHAR
)
RETURNS TABLE (success BOOLEAN, message TEXT)
LANGUAGE plpgsql AS $$
BEGIN
  DELETE FROM public.staff_signing_config
    WHERE staff_id = p_staff_id AND provider_code = p_provider_code;
  IF NOT FOUND THEN
    RETURN QUERY SELECT FALSE, 'Không tìm thấy cấu hình'::TEXT; RETURN;
  END IF;
  RETURN QUERY SELECT TRUE, 'Xóa cấu hình thành công'::TEXT;
END;
$$;

-- ============================================================================
-- STORED FUNCTIONS — sign_transactions (6 functions)
-- ============================================================================

-- CREATE transaction (status='pending', expires_at = now + 3 phút)
CREATE OR REPLACE FUNCTION edoc.fn_sign_transaction_create(
  p_staff_id         INT,
  p_provider_code    VARCHAR,
  p_attachment_id    BIGINT,
  p_attachment_type  VARCHAR,
  p_doc_id           BIGINT,
  p_doc_type         VARCHAR,
  p_file_hash_sha256 VARCHAR
)
RETURNS TABLE (success BOOLEAN, message TEXT, id BIGINT)
LANGUAGE plpgsql AS $$
DECLARE v_id BIGINT;
BEGIN
  IF p_attachment_type NOT IN ('incoming','outgoing','drafting') THEN
    RETURN QUERY SELECT FALSE, 'Loại file đính kèm không hợp lệ'::TEXT, 0::BIGINT; RETURN;
  END IF;

  INSERT INTO edoc.sign_transactions (
    staff_id, provider_code, attachment_id, attachment_type, doc_id, doc_type,
    file_hash_sha256, "status", expires_at
  )
  VALUES (
    p_staff_id, p_provider_code, p_attachment_id, p_attachment_type, p_doc_id, p_doc_type,
    p_file_hash_sha256, 'pending', NOW() + INTERVAL '3 minutes'
  )
  RETURNING edoc.sign_transactions.id INTO v_id;

  RETURN QUERY SELECT TRUE, 'Tạo giao dịch ký số thành công'::TEXT, v_id;
END;
$$;

-- SET provider_txn_id (sau khi call provider API thành công)
CREATE OR REPLACE FUNCTION edoc.fn_sign_transaction_set_provider_txn(
  p_id              BIGINT,
  p_provider_txn_id VARCHAR
)
RETURNS TABLE (success BOOLEAN, message TEXT)
LANGUAGE plpgsql AS $$
BEGIN
  UPDATE edoc.sign_transactions
    SET provider_txn_id = p_provider_txn_id, started_at = NOW()
    WHERE id = p_id AND "status" = 'pending';
  IF NOT FOUND THEN
    RETURN QUERY SELECT FALSE, 'Không tìm thấy giao dịch pending'::TEXT; RETURN;
  END IF;
  RETURN QUERY SELECT TRUE, 'Cập nhật provider_txn_id thành công'::TEXT;
END;
$$;

-- COMPLETE transaction (signature_base64 + signed_file_path)
CREATE OR REPLACE FUNCTION edoc.fn_sign_transaction_complete(
  p_id               BIGINT,
  p_signature_base64 TEXT,
  p_signed_file_path VARCHAR
)
RETURNS TABLE (success BOOLEAN, message TEXT)
LANGUAGE plpgsql AS $$
BEGIN
  UPDATE edoc.sign_transactions
    SET "status" = 'completed',
        signature_base64 = p_signature_base64,
        signed_file_path = p_signed_file_path,
        completed_at = NOW()
    WHERE id = p_id AND "status" = 'pending';
  IF NOT FOUND THEN
    RETURN QUERY SELECT FALSE, 'Không tìm thấy giao dịch pending'::TEXT; RETURN;
  END IF;
  RETURN QUERY SELECT TRUE, 'Hoàn tất giao dịch ký số'::TEXT;
END;
$$;

-- FAIL / EXPIRE / CANCEL (gộp 1 SP, tham số p_status)
CREATE OR REPLACE FUNCTION edoc.fn_sign_transaction_update_status(
  p_id            BIGINT,
  p_status        VARCHAR,     -- 'failed' | 'expired' | 'cancelled'
  p_error_message TEXT
)
RETURNS TABLE (success BOOLEAN, message TEXT)
LANGUAGE plpgsql AS $$
BEGIN
  IF p_status NOT IN ('failed','expired','cancelled') THEN
    RETURN QUERY SELECT FALSE, 'Trạng thái không hợp lệ'::TEXT; RETURN;
  END IF;
  UPDATE edoc.sign_transactions
    SET "status" = p_status,
        error_message = p_error_message,
        completed_at = NOW()
    WHERE id = p_id AND "status" = 'pending';
  IF NOT FOUND THEN
    RETURN QUERY SELECT FALSE, 'Không tìm thấy giao dịch pending'::TEXT; RETURN;
  END IF;
  RETURN QUERY SELECT TRUE, 'Cập nhật trạng thái thành công'::TEXT;
END;
$$;

-- INCREMENT retry_count (worker mỗi lần poll failed retry)
CREATE OR REPLACE FUNCTION edoc.fn_sign_transaction_increment_retry(
  p_id BIGINT
)
RETURNS TABLE (success BOOLEAN, retry_count INT)
LANGUAGE plpgsql AS $$
DECLARE v_count INT;
BEGIN
  UPDATE edoc.sign_transactions
    SET retry_count = retry_count + 1
    WHERE id = p_id AND "status" = 'pending'
    RETURNING edoc.sign_transactions.retry_count INTO v_count;
  IF v_count IS NULL THEN
    RETURN QUERY SELECT FALSE, 0; RETURN;
  END IF;
  RETURN QUERY SELECT TRUE, v_count;
END;
$$;

-- GET BY ID (worker đọc full context của transaction)
CREATE OR REPLACE FUNCTION edoc.fn_sign_transaction_get_by_id(
  p_id BIGINT
)
RETURNS TABLE (
  id BIGINT,
  staff_id INT,
  provider_code VARCHAR,
  provider_txn_id VARCHAR,
  attachment_id BIGINT,
  attachment_type VARCHAR,
  doc_id BIGINT,
  doc_type VARCHAR,
  file_hash_sha256 VARCHAR,
  signature_base64 TEXT,
  signed_file_path VARCHAR,
  "status" VARCHAR,
  error_message TEXT,
  retry_count INT,
  created_at TIMESTAMPTZ,
  started_at TIMESTAMPTZ,
  completed_at TIMESTAMPTZ,
  expires_at TIMESTAMPTZ
)
LANGUAGE plpgsql STABLE AS $$
BEGIN
  RETURN QUERY
    SELECT t.id, t.staff_id, t.provider_code, t.provider_txn_id,
           t.attachment_id, t.attachment_type, t.doc_id, t.doc_type,
           t.file_hash_sha256, t.signature_base64, t.signed_file_path,
           t."status", t.error_message, t.retry_count,
           t.created_at, t.started_at, t.completed_at, t.expires_at
    FROM edoc.sign_transactions t
    WHERE t.id = p_id;
END;
$$;

DO $$ BEGIN
  RAISE NOTICE '✅ Migration 040 Part 2: 15 stored functions (provider_config + staff_config + sign_transactions)';
END $$;
```

Sau khi append xong, chạy lại migration vào DB để verify tất cả SP tạo OK.
  </action>
  <verify>
    <automated>
      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -f /dev/stdin < e_office_app_new/database/migrations/040_signing_schema.sql > /tmp/mig040_part2.log 2>&1 && grep -q "Migration 040 Part 2" /tmp/mig040_part2.log && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\\df public.fn_signing_provider_config_list" | grep -q "fn_signing_provider_config_list" && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\\df public.fn_signing_provider_config_get_active" | grep -q "fn_signing_provider_config_get_active" && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\\df public.fn_staff_signing_config_upsert" | grep -q "fn_staff_signing_config_upsert" && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\\df edoc.fn_sign_transaction_create" | grep -q "fn_sign_transaction_create" && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\\df edoc.fn_sign_transaction_complete" | grep -q "fn_sign_transaction_complete" && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "SELECT * FROM public.fn_signing_provider_config_list() LIMIT 1;" && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "SELECT * FROM edoc.fn_sign_transaction_create(1, 'SMARTCA_VNPT', 1, 'outgoing', NULL, NULL, 'abc123');"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `040_signing_schema.sql` chứa đúng 15 CREATE OR REPLACE FUNCTION statements:
      - 5 function `fn_signing_provider_config_*` (list, get_by_code, get_active, upsert, set_active)
      - 4 function `fn_staff_signing_config_*` (list_by_staff, get, upsert, delete)
      - 6 function `edoc.fn_sign_transaction_*` (create, set_provider_txn, complete, update_status, increment_retry, get_by_id)
    - Chạy migration vào qlvb_postgres thành công, log chứa "Migration 040 Part 2"
    - Query `\df public.fn_signing_provider_config_list` trả về 1 row (function exists)
    - Query `\df edoc.fn_sign_transaction_create` trả về 1 row
    - Query `SELECT * FROM public.fn_signing_provider_config_list() LIMIT 1` chạy không lỗi (empty set OK vì chưa có data)
    - Query `SELECT * FROM edoc.fn_sign_transaction_create(1, 'SMARTCA_VNPT', 1, 'outgoing', NULL, NULL, 'abc123');` trả về `success=true`, `id > 0` (tạo transaction test)
    - Query `SELECT "status" FROM edoc.sign_transactions WHERE id = {id_from_above};` trả về `'pending'` (verify status column quoting hoạt động)
    - Cleanup test transaction: `DELETE FROM edoc.sign_transactions WHERE file_hash_sha256 = 'abc123';`
  </acceptance_criteria>
  <done>15 stored functions tạo thành công trong qlvb_postgres, test SP create transaction trả success=true.</done>
</task>

</tasks>

<verification>
**Sau cả 2 tasks:**

1. File `e_office_app_new/database/migrations/040_signing_schema.sql` tồn tại, chạy idempotent 2 lần liên tiếp không lỗi.
2. Schema verify:
   ```bash
   docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
   \\d public.signing_provider_config
   \\d public.staff_signing_config
   \\d edoc.sign_transactions
   "
   ```
   Phải thấy đủ columns với đúng type (client_secret=bytea, is_active=boolean, status=varchar, expires_at=timestamptz).

3. Stored functions:
   ```bash
   docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
   \\df public.fn_signing_provider_config_*
   \\df public.fn_staff_signing_config_*
   \\df edoc.fn_sign_transaction_*
   "
   ```
   Phải thấy đủ 15 functions.

4. Test end-to-end CRUD một transaction demo, verify INSERT/UPDATE/SELECT đều hoạt động đúng.
</verification>

<success_criteria>
- [x] File migration 040 tồn tại với schema + SP đầy đủ
- [x] 3 bảng mới có constraints đúng (partial unique index, composite PK, CHECK, FK)
- [x] 4 bảng attachment (incoming/outgoing/drafting/handling) có 2 cột mới nullable
- [x] pgcrypto extension xác nhận enabled
- [x] 15 SPs tạo OK, test SP create transaction trả success
- [x] MIG-01 (3 bảng mới) + MIG-02 (attachments 2 cột mới) covered 100%
</success_criteria>

<output>
After completion, create `.planning/phases/08-schema-foundation-pdf-signing-generic-layer/08-01-SUMMARY.md` documenting:
- Migration file path
- 3 tables schema summary (columns, constraints)
- 15 SPs created (names + signatures)
- Verification commands run
- Any deviations from plan
</output>
