---
phase: 08-schema-foundation-pdf-signing-generic-layer
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - e_office_app_new/backend/package.json
  - e_office_app_new/backend/src/services/signing/pdf-signer.ts
  - e_office_app_new/backend/src/services/signing/pdf-signer.test.ts
  - e_office_app_new/backend/src/services/signing/types.ts
autonomous: true
requirements:
  - SIGN-04
requirements_addressed:
  - SIGN-04
must_haves:
  truths:
    - "Hàm `signPdf(pdfBuffer, signatureBase64)` export từ `backend/src/services/signing/pdf-signer.ts`, nhận Buffer + base64 string, trả Promise<Buffer> chứa PDF đã embed PKCS7 detached"
    - "Hàm `computePdfHash(pdfBuffer)` export, trả SHA256 hex string của byte range (PAdES compliant)"
    - "Hàm `addSignaturePlaceholder(pdfBuffer, options)` export, trả `{ pdfWithPlaceholder: Buffer, byteRange: number[] }` dùng pdf-lib + node-signpdf pattern"
    - "Package `@signpdf/signpdf`, `@signpdf/signer-p12`, `node-forge`, `pdf-lib` được install và khai báo dependencies trong package.json backend"
    - "Unit test với mock PKCS7 signature (từ test fixture) generate PDF output có signature placeholder filled — verify file structure bằng grep `/ByteRange` marker"
  artifacts:
    - path: "e_office_app_new/backend/src/services/signing/pdf-signer.ts"
      provides: "Generic PDF signing API — computePdfHash, addSignaturePlaceholder, signPdf"
      min_lines: 120
    - path: "e_office_app_new/backend/src/services/signing/types.ts"
      provides: "Types cho sign flow: SignatureMetadata, HashResult, SignResult"
    - path: "e_office_app_new/backend/src/services/signing/pdf-signer.test.ts"
      provides: "Unit test coverage cho 3 function chính"
  key_links:
    - from: "backend/src/services/signing/pdf-signer.ts"
      to: "@signpdf/signpdf package"
      via: "import { SignPdf } from '@signpdf/signpdf'"
      pattern: "@signpdf/signpdf"
    - from: "backend/src/services/signing/pdf-signer.ts"
      to: "pdf-lib package"
      via: "import { PDFDocument } from 'pdf-lib'"
      pattern: "from 'pdf-lib'"
---

<objective>
Tạo layer generic PDF signing Pure JS — KHÔNG spawn Java/DotNet runtime. Cung cấp 3 function:
1. `computePdfHash(pdfBuffer: Buffer): { hash: string, placeholderPdf: Buffer, byteRange: number[] }` — tính SHA256 của byte range sau khi thêm placeholder (PAdES compliant)
2. `signPdf(pdfWithPlaceholder: Buffer, signatureBase64: string, byteRange: number[]): Buffer` — embed PKCS7 detached signature vào placeholder
3. `addSignaturePlaceholder(pdfBuffer, options)` — helper thêm ByteRange placeholder (internal, nhưng export cho test)

**Why này là generic layer:** Cả SmartCA VNPT và MySign Viettel đều trả PKCS7 detached signature base64. Adapter layer (Phase 9) chỉ cần call provider → lấy signature → gọi `signPdf()` này → có output chuẩn PAdES verify được bằng Adobe Reader.

**Stack quyết định (theo spec ROADMAP):**
- `@signpdf/signpdf` v3.x — embed PKCS7 vào PDF placeholder (fork mới của node-signpdf, maintained)
- `@signpdf/signer-p12` — optional cho test với self-signed cert
- `node-forge` — tạo PKCS7 structure khi test local (production dùng signature từ provider)
- `pdf-lib` — thao tác PDF (đã có sẵn trong package.json)

Output: 3 TS files + updated package.json, unit test chạy xanh với fixture PDF + mock signature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-schema-foundation-pdf-signing-generic-layer/08-01-PLAN.md
@e_office_app_new/backend/package.json
@e_office_app_new/backend/tsconfig.json

<interfaces>
**Existing backend config:**
- `package.json` đã có: `pdf-lib@^1.17.1`, `pg@^8.20.0`, `bullmq@^5.73.5`
- `tsconfig.json`: ESM module, target ES2022, `strict: true`, `@/*` → `./src/*`
- Import convention: `.js` suffix bắt buộc (ESM)

**Conventions:**
- Services dir: `backend/src/services/` (existing: auth.service.ts, lgsp.service.ts, signing.service.ts legacy từ v1.0)
- NEW subdir: `backend/src/services/signing/` (tách namespace clean khỏi `signing.service.ts` cũ)
- Test files: co-located `{name}.test.ts`. KHÔNG có test runner cài sẵn — dùng `tsx --test` (Node.js built-in test runner) để không thêm dependency

**@signpdf/signpdf API reference (v3.x):**
```typescript
import { SignPdf } from '@signpdf/signpdf';
import { plainAddPlaceholder } from '@signpdf/placeholder-plain';

// Step 1: Add placeholder to PDF
const pdfWithPlaceholder = plainAddPlaceholder({
  pdfBuffer,
  reason: 'Ký số',
  contactInfo: '',
  name: 'Signer',
  location: 'Vietnam',
  signatureLength: 16384,  // enough for PKCS7 detached
});

// Step 2: Compute byte range hash → send to external signer
// Step 3: Embed signature back using SignPdf
const signer = new SignPdf();
const signedPdf = signer.sign(pdfWithPlaceholder, signatureBuffer);
```

**PAdES byte range structure:**
PDF signature có `/ByteRange [ a b c d ]` nghĩa:
- `a..a+b`: bytes trước `/Contents <...>`
- `c..c+d`: bytes sau `/Contents <...>`
- Hash = SHA256 of (a..a+b) concat (c..c+d) — tức là toàn PDF TRỪ vùng `/Contents`
</interfaces>

<api_constraints>
- `signPdf(pdfBuffer, signatureBase64)` signature CHUẨN — KHÔNG thay đổi
  - Input: `pdfBuffer: Buffer` (PDF có placeholder, từ `addSignaturePlaceholder` hoặc compose sẵn)
  - Input: `signatureBase64: string` (PKCS7 detached từ provider)
  - Output: `Promise<Buffer>` (PDF đã ký, verify được bằng Adobe Reader)
- Error handling: throw `Error` với message tiếng Việt rõ ràng (VD: "PDF không có signature placeholder", "Signature base64 không hợp lệ")
</api_constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install packages + types + types.ts</name>
  <files>
    e_office_app_new/backend/package.json,
    e_office_app_new/backend/src/services/signing/types.ts
  </files>
  <read_first>
    - e_office_app_new/backend/package.json (hiện tại)
    - e_office_app_new/backend/tsconfig.json (ESM config)
    - CLAUDE.md (Naming Patterns, Import Organization với `.js` suffix)
  </read_first>
  <behavior>
    - Import 3 packages (@signpdf/signpdf, @signpdf/placeholder-plain, node-forge) không lỗi TypeScript
    - File `types.ts` export interface `SignatureMetadata`, `PdfHashResult`, `PdfSignResult`
    - Chạy `tsc --noEmit` không lỗi sau khi cài packages
  </behavior>
  <action>
**Bước 1:** Cài 4 packages (npm install trong `e_office_app_new/backend/`):
```bash
cd e_office_app_new/backend
npm install @signpdf/signpdf@^3.2.4 @signpdf/placeholder-plain@^3.2.4 @signpdf/signer-p12@^3.2.4 node-forge@^1.3.1
npm install --save-dev @types/node-forge@^1.3.11
```

**Bước 2:** Tạo file `e_office_app_new/backend/src/services/signing/types.ts`:

```typescript
/**
 * Generic PDF signing types — shared between pdf-signer và adapter layers (Phase 9+)
 */

/**
 * Metadata embed vào signature placeholder.
 * Hiển thị trong Adobe Reader "Signature Properties" panel.
 */
export interface SignatureMetadata {
  /** Lý do ký (hiển thị trong PDF) — VD: "Phê duyệt văn bản" */
  reason?: string;

  /** Email/thông tin liên lạc */
  contactInfo?: string;

  /** Tên người ký — VD: "Nguyễn Văn A" */
  name?: string;

  /** Địa điểm ký — VD: "Hà Nội" */
  location?: string;
}

/**
 * Kết quả computePdfHash: PDF đã có placeholder + SHA256 byte range.
 * Client gọi provider với `hash`, provider trả PKCS7 signature base64.
 * Sau đó dùng `placeholderPdf` + signature để embed → output final PDF.
 */
export interface PdfHashResult {
  /** SHA256 hex của byte range (PAdES) — gửi cho provider để ký */
  hash: string;

  /** PDF Buffer đã insert placeholder `/Contents <...>` — giữ lại để embed signature */
  placeholderPdf: Buffer;

  /** Byte range array [start, len, start2, len2] — debug/verify */
  byteRange: number[];
}

/**
 * Kết quả sau khi embed signature vào placeholder.
 */
export interface PdfSignResult {
  /** PDF buffer đã ký hoàn chỉnh — verify được bằng Adobe Reader */
  signedPdf: Buffer;

  /** SHA256 hex của PDF final (để log/audit) */
  finalHash: string;
}

/**
 * Options cho `addSignaturePlaceholder()` — wrapper quanh @signpdf/placeholder-plain.
 */
export interface PlaceholderOptions extends SignatureMetadata {
  /**
   * Độ dài placeholder (bytes). PKCS7 detached thường ~8KB, set 16384 để an toàn.
   * @default 16384
   */
  signatureLength?: number;
}
```

**Bước 3:** Verify TypeScript compile:
```bash
cd e_office_app_new/backend
npx tsc --noEmit
```
  </action>
  <verify>
    <automated>
      cd e_office_app_new/backend && npm ls @signpdf/signpdf 2>&1 | grep -q "@signpdf/signpdf@" && npm ls @signpdf/placeholder-plain 2>&1 | grep -q "@signpdf/placeholder-plain@" && npm ls node-forge 2>&1 | grep -q "node-forge@" && npm ls @types/node-forge 2>&1 | grep -q "@types/node-forge@" && test -f src/services/signing/types.ts && grep -q "export interface SignatureMetadata" src/services/signing/types.ts && grep -q "export interface PdfHashResult" src/services/signing/types.ts && grep -q "export interface PdfSignResult" src/services/signing/types.ts && npx tsc --noEmit 2>&1 | tee /tmp/tsc.log && ! grep -i "error TS" /tmp/tsc.log
    </automated>
  </verify>
  <acceptance_criteria>
    - `e_office_app_new/backend/package.json` `dependencies` chứa: `@signpdf/signpdf`, `@signpdf/placeholder-plain`, `@signpdf/signer-p12`, `node-forge`
    - `e_office_app_new/backend/package.json` `devDependencies` chứa `@types/node-forge`
    - `npm ls @signpdf/signpdf` exit 0 với version `3.x.x`
    - File `e_office_app_new/backend/src/services/signing/types.ts` tồn tại
    - File types.ts chứa chuỗi `export interface SignatureMetadata`, `export interface PdfHashResult`, `export interface PdfSignResult`, `export interface PlaceholderOptions`
    - `npx tsc --noEmit` exit 0 không có `error TS`
  </acceptance_criteria>
  <done>4 packages cài xong, types.ts compile clean, tsc không lỗi.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pdf-signer.ts với 3 function core</name>
  <files>
    e_office_app_new/backend/src/services/signing/pdf-signer.ts,
    e_office_app_new/backend/src/services/signing/pdf-signer.test.ts
  </files>
  <read_first>
    - e_office_app_new/backend/src/services/signing/types.ts (từ Task 1)
    - e_office_app_new/backend/node_modules/@signpdf/signpdf/package.json (confirm main export + version)
    - e_office_app_new/backend/node_modules/@signpdf/placeholder-plain/dist/plainAddPlaceholder.js (confirm API signature)
    - e_office_app_new/backend/tsconfig.json
  </read_first>
  <behavior>
    **Test 1:** `addSignaturePlaceholder(samplePdfBuffer, { reason: 'Test' })` trả Buffer có chứa chuỗi `/ByteRange` và `/Contents <` khi decode.
    **Test 2:** `computePdfHash(samplePdfBuffer)` trả object với `hash` là string 64 ký tự hex (SHA256), `placeholderPdf` là Buffer, `byteRange` là array 4 số integer.
    **Test 3:** `signPdf(pdfWithPlaceholder, mockSignatureBase64)` trả Buffer cùng size với input (signature chỉ fill placeholder, không thay đổi size tổng). Mock signature từ test fixture (tạo bằng node-forge với self-signed cert).
    **Test 4:** `signPdf(pdfWithoutPlaceholder, ...)` throw Error chứa chuỗi tiếng Việt về "placeholder".
    **Test 5:** `signPdf(pdfBuffer, 'invalid_base64_@@@')` throw Error về signature không hợp lệ.
  </behavior>
  <action>
**Tạo file `e_office_app_new/backend/src/services/signing/pdf-signer.ts`:**

```typescript
/**
 * Generic PDF signing layer — Pure JS (không spawn Java/DotNet).
 * Dùng chung cho cả SmartCA VNPT và MySign Viettel (Phase 9+).
 *
 * Flow:
 *   1. addSignaturePlaceholder(pdf) → pdf có /Contents <placeholder>
 *   2. computePdfHash(pdf) → SHA256 byte range hex
 *   3. Client call provider.sign(hash) → nhận PKCS7 detached base64
 *   4. signPdf(placeholderPdf, signatureBase64) → PDF đã ký
 *
 * Output PDF verify được bằng Adobe Reader (PAdES PKCS7 detached).
 */

import { createHash } from 'node:crypto';
import { SignPdf } from '@signpdf/signpdf';
import { plainAddPlaceholder } from '@signpdf/placeholder-plain';
import type {
  PdfHashResult,
  PdfSignResult,
  PlaceholderOptions,
} from './types.js';

const DEFAULT_SIGNATURE_LENGTH = 16384;  // PKCS7 detached thường ~8KB, 16KB để an toàn

/**
 * Thêm signature placeholder vào PDF (PAdES compliant).
 * Placeholder là chuỗi 0x00 dài `signatureLength` bytes tại `/Contents <...>`.
 *
 * @throws Error nếu PDF malformed
 */
export function addSignaturePlaceholder(
  pdfBuffer: Buffer,
  options: PlaceholderOptions = {}
): Buffer {
  if (!Buffer.isBuffer(pdfBuffer) || pdfBuffer.length === 0) {
    throw new Error('PDF buffer không hợp lệ (rỗng hoặc không phải Buffer)');
  }

  const pdfString = pdfBuffer.subarray(0, Math.min(8, pdfBuffer.length)).toString('ascii');
  if (!pdfString.startsWith('%PDF-')) {
    throw new Error('File không phải PDF hợp lệ (thiếu header %PDF-)');
  }

  try {
    const withPlaceholder = plainAddPlaceholder({
      pdfBuffer,
      reason:       options.reason       ?? 'Ký số điện tử',
      contactInfo:  options.contactInfo  ?? '',
      name:         options.name         ?? 'E-Office User',
      location:     options.location     ?? 'Vietnam',
      signatureLength: options.signatureLength ?? DEFAULT_SIGNATURE_LENGTH,
    });
    return withPlaceholder;
  } catch (err: unknown) {
    const msg = err instanceof Error ? err.message : String(err);
    throw new Error(`Không thể thêm signature placeholder vào PDF: ${msg}`);
  }
}

/**
 * Parse `/ByteRange [ a b c d ]` từ PDF buffer.
 * Trả array 4 số integer hoặc throw nếu không tìm thấy.
 */
function extractByteRange(pdfBuffer: Buffer): number[] {
  const content = pdfBuffer.toString('binary');
  const match = content.match(/\/ByteRange\s*\[\s*(\d+)\s+(\d+)\s+(\d+)\s+(\d+)\s*\]/);
  if (!match) {
    throw new Error('PDF không có signature placeholder (không tìm thấy /ByteRange)');
  }
  return [
    parseInt(match[1], 10),
    parseInt(match[2], 10),
    parseInt(match[3], 10),
    parseInt(match[4], 10),
  ];
}

/**
 * Tính SHA256 hash của PDF byte range (không bao gồm `/Contents <...>`).
 * Hash này gửi cho provider để ký (provider trả PKCS7 detached).
 *
 * Input: PDF đã có placeholder (từ addSignaturePlaceholder).
 * Nếu truyền PDF chưa có placeholder → throw.
 */
export function computePdfHash(placeholderPdf: Buffer): PdfHashResult {
  if (!Buffer.isBuffer(placeholderPdf) || placeholderPdf.length === 0) {
    throw new Error('PDF buffer không hợp lệ');
  }

  const byteRange = extractByteRange(placeholderPdf);
  const [start1, len1, start2, len2] = byteRange;

  // SHA256 of byte range: (start1..start1+len1) concat (start2..start2+len2)
  const hasher = createHash('sha256');
  hasher.update(placeholderPdf.subarray(start1, start1 + len1));
  hasher.update(placeholderPdf.subarray(start2, start2 + len2));
  const hash = hasher.digest('hex');

  return {
    hash,
    placeholderPdf,
    byteRange,
  };
}

/**
 * Convenience: addSignaturePlaceholder + computePdfHash in 1 step.
 */
export function prepareSignPdf(
  pdfBuffer: Buffer,
  options: PlaceholderOptions = {}
): PdfHashResult {
  const withPlaceholder = addSignaturePlaceholder(pdfBuffer, options);
  return computePdfHash(withPlaceholder);
}

/**
 * Embed PKCS7 detached signature (base64) vào PDF placeholder.
 *
 * @param placeholderPdf PDF buffer đã có placeholder (từ addSignaturePlaceholder).
 * @param signatureBase64 PKCS7 detached signature (base64) từ provider SmartCA/MySign.
 * @returns PdfSignResult { signedPdf, finalHash }
 * @throws Error nếu PDF không có placeholder hoặc signature không hợp lệ.
 */
export async function signPdf(
  placeholderPdf: Buffer,
  signatureBase64: string
): Promise<PdfSignResult> {
  if (!Buffer.isBuffer(placeholderPdf)) {
    throw new Error('placeholderPdf phải là Buffer');
  }
  if (typeof signatureBase64 !== 'string' || signatureBase64.trim() === '') {
    throw new Error('signatureBase64 không hợp lệ (rỗng hoặc không phải string)');
  }

  // Validate base64 format
  const base64Pattern = /^[A-Za-z0-9+/]*={0,2}$/;
  const cleaned = signatureBase64.replace(/\s/g, '');
  if (!base64Pattern.test(cleaned)) {
    throw new Error('signatureBase64 không phải base64 hợp lệ');
  }

  let signatureBuffer: Buffer;
  try {
    signatureBuffer = Buffer.from(cleaned, 'base64');
    if (signatureBuffer.length === 0) {
      throw new Error('Signature decode ra 0 bytes');
    }
  } catch (err: unknown) {
    const msg = err instanceof Error ? err.message : String(err);
    throw new Error(`Không thể decode signature base64: ${msg}`);
  }

  // Verify placeholder exists
  extractByteRange(placeholderPdf);  // throws if no placeholder

  try {
    const signer = new SignPdf();
    const signedPdf = signer.sign(placeholderPdf, signatureBuffer);

    const finalHash = createHash('sha256').update(signedPdf).digest('hex');

    return {
      signedPdf: Buffer.isBuffer(signedPdf) ? signedPdf : Buffer.from(signedPdf),
      finalHash,
    };
  } catch (err: unknown) {
    const msg = err instanceof Error ? err.message : String(err);
    throw new Error(`Không thể embed signature vào PDF: ${msg}`);
  }
}
```

**Tạo test fixture + test file `e_office_app_new/backend/src/services/signing/pdf-signer.test.ts`:**

```typescript
/**
 * Unit tests cho pdf-signer.ts
 * Run: node --test --experimental-strip-types src/services/signing/pdf-signer.test.ts
 * Hoặc: npx tsx --test src/services/signing/pdf-signer.test.ts
 */

import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { PDFDocument } from 'pdf-lib';
import * as forge from 'node-forge';

import {
  addSignaturePlaceholder,
  computePdfHash,
  signPdf,
  prepareSignPdf,
} from './pdf-signer.js';

/**
 * Helper: tạo PDF tối giản 1 trang chứa text "Test PDF".
 */
async function createSamplePdf(): Promise<Buffer> {
  const doc = await PDFDocument.create();
  const page = doc.addPage([400, 400]);
  page.drawText('Test PDF for signing', { x: 50, y: 350, size: 20 });
  const bytes = await doc.save();
  return Buffer.from(bytes);
}

/**
 * Helper: tạo PKCS7 detached signature mock bằng self-signed cert.
 * Dùng node-forge để generate hợp lệ (dù không trust chain).
 */
function createMockPkcs7Signature(dataToSign: Buffer): string {
  // Generate self-signed RSA cert
  const keys = forge.pki.rsa.generateKeyPair(2048);
  const cert = forge.pki.createCertificate();
  cert.publicKey = keys.publicKey;
  cert.serialNumber = '01';
  cert.validity.notBefore = new Date();
  cert.validity.notAfter = new Date(Date.now() + 365 * 24 * 60 * 60 * 1000);
  const attrs = [{ name: 'commonName', value: 'Test Signer' }];
  cert.setSubject(attrs);
  cert.setIssuer(attrs);
  cert.sign(keys.privateKey, forge.md.sha256.create());

  // Build PKCS7 detached
  const p7 = forge.pkcs7.createSignedData();
  p7.content = forge.util.createBuffer(dataToSign.toString('binary'));
  p7.addCertificate(cert);
  p7.addSigner({
    key: keys.privateKey,
    certificate: cert,
    digestAlgorithm: forge.pki.oids.sha256,
    authenticatedAttributes: [
      { type: forge.pki.oids.contentType, value: forge.pki.oids.data },
      { type: forge.pki.oids.messageDigest },
      { type: forge.pki.oids.signingTime, value: new Date() },
    ],
  });
  p7.sign({ detached: true });

  const der = forge.asn1.toDer(p7.toAsn1()).getBytes();
  return forge.util.encode64(der);
}

describe('addSignaturePlaceholder', () => {
  it('thêm placeholder /ByteRange vào PDF', async () => {
    const pdf = await createSamplePdf();
    const result = addSignaturePlaceholder(pdf, { reason: 'Test' });
    assert.ok(Buffer.isBuffer(result), 'result phải là Buffer');
    const content = result.toString('binary');
    assert.match(content, /\/ByteRange\s*\[/, 'phải có /ByteRange marker');
    assert.match(content, /\/Contents\s*</, 'phải có /Contents marker');
    assert.ok(result.length > pdf.length, 'PDF sau khi thêm placeholder phải lớn hơn');
  });

  it('throw khi input không phải PDF', () => {
    assert.throws(
      () => addSignaturePlaceholder(Buffer.from('not a pdf')),
      /không phải PDF hợp lệ/
    );
  });

  it('throw khi Buffer rỗng', () => {
    assert.throws(
      () => addSignaturePlaceholder(Buffer.alloc(0)),
      /không hợp lệ/
    );
  });
});

describe('computePdfHash', () => {
  it('trả hash SHA256 hex 64 ký tự + byteRange 4 phần tử', async () => {
    const pdf = await createSamplePdf();
    const placeholderPdf = addSignaturePlaceholder(pdf);
    const result = computePdfHash(placeholderPdf);
    assert.equal(result.hash.length, 64, 'SHA256 hex phải 64 ký tự');
    assert.match(result.hash, /^[0-9a-f]{64}$/, 'phải là lowercase hex');
    assert.equal(result.byteRange.length, 4, 'byteRange phải có 4 số');
    assert.ok(result.byteRange.every((n) => Number.isInteger(n) && n >= 0), 'mỗi số phải là non-negative integer');
  });

  it('throw khi PDF chưa có placeholder', async () => {
    const pdf = await createSamplePdf();
    assert.throws(
      () => computePdfHash(pdf),
      /không có signature placeholder/
    );
  });
});

describe('prepareSignPdf', () => {
  it('combine addPlaceholder + computeHash', async () => {
    const pdf = await createSamplePdf();
    const result = prepareSignPdf(pdf, { reason: 'Test' });
    assert.ok(result.hash);
    assert.ok(Buffer.isBuffer(result.placeholderPdf));
    assert.equal(result.byteRange.length, 4);
  });
});

describe('signPdf', () => {
  it('embed mock PKCS7 signature thành công', async () => {
    const pdf = await createSamplePdf();
    const prep = prepareSignPdf(pdf, { reason: 'Test' });

    // Dùng byte range bytes làm "data to sign" (PKCS7 content = byte range data)
    const [s1, l1, s2, l2] = prep.byteRange;
    const dataToSign = Buffer.concat([
      prep.placeholderPdf.subarray(s1, s1 + l1),
      prep.placeholderPdf.subarray(s2, s2 + l2),
    ]);
    const mockSig = createMockPkcs7Signature(dataToSign);

    const result = await signPdf(prep.placeholderPdf, mockSig);
    assert.ok(Buffer.isBuffer(result.signedPdf), 'signedPdf phải là Buffer');
    assert.equal(result.signedPdf.length, prep.placeholderPdf.length, 'signed PDF phải cùng size với placeholder PDF (signature fill vào vùng sẵn có)');
    assert.equal(result.finalHash.length, 64);

    // Verify signature đã fill (không còn toàn 0x00 trong /Contents)
    const signedContent = result.signedPdf.toString('binary');
    const contentMatch = signedContent.match(/\/Contents\s*<([0-9a-fA-F]+)>/);
    assert.ok(contentMatch, 'phải có /Contents <hex>');
    const hexContent = contentMatch![1];
    assert.ok(!/^0+$/.test(hexContent), '/Contents không được toàn 0 (signature chưa fill)');
  });

  it('throw khi signatureBase64 rỗng', async () => {
    const pdf = await createSamplePdf();
    const placeholder = addSignaturePlaceholder(pdf);
    await assert.rejects(
      () => signPdf(placeholder, ''),
      /không hợp lệ/
    );
  });

  it('throw khi signature không phải base64', async () => {
    const pdf = await createSamplePdf();
    const placeholder = addSignaturePlaceholder(pdf);
    await assert.rejects(
      () => signPdf(placeholder, '!!!not_base64@@@'),
      /base64 hợp lệ/
    );
  });

  it('throw khi PDF chưa có placeholder', async () => {
    const pdf = await createSamplePdf();
    const validBase64 = Buffer.from('fake').toString('base64');
    await assert.rejects(
      () => signPdf(pdf, validBase64),
      /signature placeholder/
    );
  });
});
```

**Chạy tests:**
```bash
cd e_office_app_new/backend
npx tsx --test src/services/signing/pdf-signer.test.ts
```
  </action>
  <verify>
    <automated>
      cd e_office_app_new/backend && test -f src/services/signing/pdf-signer.ts && test -f src/services/signing/pdf-signer.test.ts && grep -q "export function addSignaturePlaceholder" src/services/signing/pdf-signer.ts && grep -q "export function computePdfHash" src/services/signing/pdf-signer.ts && grep -q "export async function signPdf" src/services/signing/pdf-signer.ts && grep -q "import { SignPdf } from '@signpdf/signpdf'" src/services/signing/pdf-signer.ts && grep -q "import { plainAddPlaceholder } from '@signpdf/placeholder-plain'" src/services/signing/pdf-signer.ts && npx tsc --noEmit 2>&1 | tee /tmp/tsc2.log && ! grep -i "error TS" /tmp/tsc2.log && npx tsx --test src/services/signing/pdf-signer.test.ts 2>&1 | tee /tmp/test.log && grep -qE "pass [0-9]+" /tmp/test.log && ! grep -qE "^not ok|fail [1-9]" /tmp/test.log
    </automated>
  </verify>
  <acceptance_criteria>
    - File `e_office_app_new/backend/src/services/signing/pdf-signer.ts` tồn tại (min 120 lines)
    - File chứa chuỗi `export function addSignaturePlaceholder`
    - File chứa chuỗi `export function computePdfHash`
    - File chứa chuỗi `export async function signPdf`
    - File chứa chuỗi `export function prepareSignPdf`
    - File chứa import `import { SignPdf } from '@signpdf/signpdf'`
    - File chứa import `import { plainAddPlaceholder } from '@signpdf/placeholder-plain'`
    - File `pdf-signer.test.ts` tồn tại với `describe('signPdf')` block
    - `npx tsc --noEmit` không có `error TS`
    - `npx tsx --test src/services/signing/pdf-signer.test.ts` exit 0
    - Test output chứa "pass" count >= 8 và KHÔNG chứa "not ok" / "fail"
    - Tất cả 5+ test cases PASS (addSignaturePlaceholder 3 cases, computePdfHash 2, prepareSignPdf 1, signPdf 4)
  </acceptance_criteria>
  <done>3 function core (computePdfHash, signPdf, addSignaturePlaceholder) implemented + tested, tất cả unit test PASS.</done>
</task>

</tasks>

<verification>
**Sau cả 2 tasks:**

1. 4 packages cài đúng phiên bản: `npm ls @signpdf/signpdf @signpdf/placeholder-plain node-forge @types/node-forge`
2. TypeScript compile: `cd e_office_app_new/backend && npx tsc --noEmit` → exit 0
3. Unit test pass: `npx tsx --test src/services/signing/pdf-signer.test.ts` → tất cả pass
4. Export API đúng: `grep "^export" src/services/signing/pdf-signer.ts` trả về `addSignaturePlaceholder`, `computePdfHash`, `prepareSignPdf`, `signPdf`
</verification>

<success_criteria>
- [x] 4 packages installed: `@signpdf/signpdf`, `@signpdf/placeholder-plain`, `@signpdf/signer-p12`, `node-forge`
- [x] 3 file mới tạo: `types.ts`, `pdf-signer.ts`, `pdf-signer.test.ts`
- [x] 4 function export: `addSignaturePlaceholder`, `computePdfHash`, `prepareSignPdf`, `signPdf`
- [x] Unit tests >= 8 cases all PASS
- [x] `signPdf(pdfBuffer, signatureBase64)` signature chuẩn theo SIGN-04 spec
- [x] Error messages tiếng Việt rõ ràng
- [x] SIGN-04 covered 100%
</success_criteria>

<output>
After completion, create `.planning/phases/08-schema-foundation-pdf-signing-generic-layer/08-03-SUMMARY.md` documenting:
- 4 packages added với version chính xác
- API reference: `signPdf(pdfBuffer: Buffer, signatureBase64: string): Promise<PdfSignResult>` — for Phase 9+ adapter layers
- Test coverage: test cases + run command
- Note for Phase 11 (sign flow): cách gọi `prepareSignPdf()` + `provider.sign(hash)` + `signPdf(placeholder, sig)` end-to-end
</output>
