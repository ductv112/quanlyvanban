---
phase: 12
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - e_office_app_new/database/test_data/phase12_seed_sign_states.sql
  - .planning/phases/12-menu-ky-so-danh-sach-4-tab/12-03-E2E-SCRIPT.md
autonomous: false
requirements: [UX-12]
tags: [e2e, uat, seed, verification, regression, no-impl]

must_haves:
  truths:
    - "4 test records (1 attachment chưa ký + 3 sign_transactions pending/completed/failed) tồn tại trong DB dev sau khi chạy seed script"
    - "E2E script chạy qua toàn bộ 8 acceptance criterion (5 AC ROADMAP + 3 regression AC#5) — tất cả GREEN"
    - "Trang detail VB đi / VB dự thảo / HSCV vẫn giữ button Ký số trên file đính kèm (KHÔNG regression Phase 11-07/08)"
    - "Nút Ký số trong trang detail dùng cùng SignModal (useSigning hook) như tab Cần ký — verify qua grep + browser test"
    - "Socket realtime hoạt động: tạo pending transaction → manual update DB → FE list tự refresh không F5"
    - "UAT checkpoint: user confirm 5 AC ROADMAP + AC#5 regression OK trước khi mark phase complete"
  artifacts:
    - path: "e_office_app_new/database/test_data/phase12_seed_sign_states.sql"
      provides: "Seed SQL idempotent tạo 4 test state"
      contains: "INSERT INTO edoc.sign_transactions"
    - path: ".planning/phases/12-menu-ky-so-danh-sach-4-tab/12-03-E2E-SCRIPT.md"
      provides: "Script chạy 8 test case end-to-end"
      contains: "AC#5 regression"
  key_links:
    - from: "12-03-E2E-SCRIPT.md test AC5"
      to: "Phase 11-07 migration detail VB di + du thao + Phase 11-08 HSCV button"
      via: "regression verify qua grep + browser"
      pattern: "useSigning"
    - from: "seed phase12_seed_sign_states.sql"
      to: "4 tab trong /ky-so/danh-sach"
      via: "DB to API to UI render"
      pattern: "sign_transactions"
---

<objective>
Plan VERIFY-ONLY (không implement feature). Mục tiêu:
1. Seed 4 test record (1 need_sign + 3 transaction pending/completed/failed) vào DB dev để test 4 tab non-empty
2. Chạy E2E script verify 5 AC ROADMAP Phase 12 + AC#5 regression (detail VB vẫn giữ nút "Ký số")
3. UAT checkpoint với user trước khi mark phase complete

Purpose: AC#5 "Trang chi tiết VB giữ button 'Ký số' — đường dẫn thứ 2 vào flow ký, mở cùng modal" ĐÃ XONG ở Phase 11-07 (VB đi + dự thảo) + Phase 11-08 (HSCV). Plan 12-03 CHỈ VERIFY không regression — không implement lại.

Output:
- 1 SQL seed file (idempotent, production-unsafe — chỉ dev/test)
- 1 E2E script Markdown mô tả 8 test case với expected result
- 1 UAT checkpoint chờ user confirm
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-menu-ky-so-danh-sach-4-tab/12-CONTEXT.md
@.planning/phases/12-menu-ky-so-danh-sach-4-tab/12-01-PLAN.md
@.planning/phases/12-menu-ky-so-danh-sach-4-tab/12-02-PLAN.md
@.planning/phases/11-sign-flow-async-worker/11-05-SUMMARY.md
@CLAUDE.md
@e_office_app_new/backend/src/routes/ky-so-sign.ts
@e_office_app_new/backend/src/routes/ky-so-danh-sach.ts

## Phase 11-07 + 11-08 regression — grep evidence cần verify

Plan 11-07 SUMMARY (trích STATE.md dòng 150): "Migrate VB đi + VB dự thảo detail pages sang useSigning hook. Pure migration — remove mock OTP state + /ky-so/mock/sign, thay bằng openSign() + renderSignModal()."

Plan 11-08 (dòng 151): "HSCV Ký số button — first-time integration. canSignHandling gate = signer_id === user.staffId + status in 2 or 3. attachmentType='handling' lần đầu có FE consumer."

File cần grep để confirm AC#5 regression (đường dẫn path trong repo có đặc biệt do Next.js route groups (main) + dynamic [id]):

- `e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx` → grep `useSigning`
- `e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx`
- `e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx`

Nếu 1 trong 3 file KHÔNG có `useSigning` → REGRESSION — STOP và notify user.

## Seed data constraints

- Bảng `edoc.sign_transactions`: id, staff_id, provider_code, provider_txn_id, attachment_id, attachment_type, doc_id, doc_type, signature_base64, signed_file_path, status, error_message, retry_count, created_at, started_at, completed_at, expires_at
- Tab Cần ký hiển thị theo SP 046 `fn_sign_need_list_by_staff` — permission mirror `fn_attachment_can_sign`: signer match, approver, created_by, is_admin
- Tab pending/completed/failed hiển thị theo SP `fn_sign_transaction_list_by_staff` — filter `staff_id = :p_staff_id AND status = :p_tab`
- Cần 1 outgoing_doc có attachment chưa ký để tab Cần ký hiện row. Nếu seed 002_demo_data có sẵn → pick 1 attachment. Nếu chưa → gracefully skip
- signed_file_path cho tab Đã ký không cần tồn tại MinIO — chỉ cần string NOT NULL để row hiện. Endpoint Plan 12-01 khi gọi presignedGetObject cho key không tồn tại vẫn trả URL (MinIO chỉ check sign, không check object existence) → download cuối cùng HTTP 404
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regression grep — 3 detail VB page phải có useSigning</name>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx
    - e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx
    - e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx
    - .planning/phases/11-sign-flow-async-worker/11-07-SUMMARY.md (nếu tồn tại)
    - .planning/phases/11-sign-flow-async-worker/11-08-SUMMARY.md (nếu tồn tại)
  </read_first>
  <files>(read-only — không sửa)</files>
  <action>
Dùng Grep tool (KHÔNG dùng bash grep qua shell) để verify 3 file detail có useSigning. Next.js route group `(main)` cần escape hợp lý — dùng glob pattern `**/page.tsx` rồi filter.

**Bước 1 — Grep 3 file đồng thời (filter theo tên):**

Dùng Grep tool với:
- pattern: `useSigning|renderSignModal|openSign`
- glob: `e_office_app_new/frontend/src/app/**/page.tsx`
- output_mode: `files_with_matches`

Lọc kết quả chỉ gồm 3 path detail:
- `van-ban-di/[id]/page.tsx`
- `van-ban-du-thao/[id]/page.tsx`
- `ho-so-cong-viec/[id]/page.tsx`

**Bước 2 — Grep per file để đếm match:**

Dùng Grep tool lần lượt cho 3 file, pattern `useSigning`, output_mode `content`, -n true.

Expected output mỗi file:

```
X: import { useSigning } from '@/hooks/use-signing';
Y:   const { openSign, renderSignModal } = useSigning();
```

Ít nhất 2 match/file (1 import + 1 call).

**Bước 3 — Grep đảm bảo mock legacy đã xóa:**

Dùng Grep tool:
- pattern: `ky-so/mock/sign|SigningModal[^a-zA-Z]`
- path: `e_office_app_new/frontend/src/app`
- output_mode: `files_with_matches`

Expected: 0 file (hoặc chỉ 1 file legacy `SigningModal.tsx` standalone không bị import — cần confirm bằng grep import của nó).

**Bước 4 — Report:**

Báo cáo format:

```
AC#5 REGRESSION CHECK:
- VB đi detail: [PASS/FAIL] — useSigning: X lines matched
- VB dự thảo detail: [PASS/FAIL] — useSigning: X lines matched
- HSCV detail: [PASS/FAIL] — useSigning: X lines matched
- Mock legacy cleaned: [PASS/FAIL] — legacy files: [list]
```

Nếu BẤT KỲ PASS/FAIL nào = FAIL → STOP. Báo cho user: "Regression phát hiện trong Phase 11-07/08. Cần điều tra trước khi tiếp tục Phase 12-03."

Nếu tất cả PASS → tiếp tục Task 2.

**KHÔNG tạo file output** — chỉ in kết quả vào output Claude để user đọc trực tiếp.
  </action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - Grep tool kết quả: 3 file detail đều có ít nhất 1 match cho `useSigning`
    - Mỗi file có ít nhất 2 line match (import + call) khi pattern là `useSigning`
    - 0 file import từ legacy `ky-so/mock/sign` path
    - Report được in rõ ràng 4 section status
  </acceptance_criteria>
  <done>
    - User đọc report trong chat → confirm "không regression"
    - Task 2 proceed nếu tất cả PASS
  </done>
</task>

<task type="auto">
  <name>Task 2: Tạo seed SQL idempotent cho 4 test state</name>
  <read_first>
    - e_office_app_new/database/schema/000_schema_v2.0.sql (Phần 2 — bảng sign_transactions + attachment_outgoing_docs + provider config)
    - e_office_app_new/database/seed/001_required_data.sql (admin staff_id=1 + 2 provider SMARTCA_VNPT + MYSIGN_VIETTEL)
    - e_office_app_new/database/seed/002_demo_data.sql (50+ VB đi, pick 1 doc có sẵn để dùng attachment)
    - e_office_app_new/backend/src/repositories/sign-transaction.repository.ts (interface SignTransactionRow để biết column names)
  </read_first>
  <files>e_office_app_new/database/test_data/phase12_seed_sign_states.sql</files>
  <action>
Tạo thư mục `e_office_app_new/database/test_data/` nếu chưa có. Tạo file seed SQL idempotent.

**Cấu trúc file (nội dung đầy đủ):**

```sql
-- ============================================================================
-- Phase 12 Plan 03: Seed 4 sign states cho UAT /ky-so/danh-sach
-- ============================================================================
-- KHÔNG dùng trong production — chỉ dev/test sau khi chạy seed 001+002.
-- Idempotent: re-run OK (DELETE + INSERT với marker).
--
-- Sau khi apply, tab /ky-so/danh-sach của admin staff_id=1 sẽ có:
--   - need_sign: 1+ row (tùy seed 002 có sẵn) — script không tạo thêm attachment
--   - pending: 1 row (txn fake đang chờ — sẽ expired sau 3 phút vì expires_at=NOW()+3min)
--   - completed: 1 row (signed_file_path fake — download sẽ 404 MinIO, UI vẫn render row)
--   - failed: 1 row (error_message mẫu)
-- ============================================================================

BEGIN;

-- 1. Verify prerequisites (admin + providers)
DO $$
BEGIN
  IF NOT EXISTS (SELECT 1 FROM public.staff WHERE id = 1) THEN
    RAISE EXCEPTION 'Seed 001 required_data.sql chưa chạy — admin staff_id=1 không tồn tại';
  END IF;
  IF NOT EXISTS (SELECT 1 FROM public.signing_provider_config WHERE provider_code = 'SMARTCA_VNPT') THEN
    RAISE EXCEPTION 'Seed 001 chưa chạy — signing_provider_config trống';
  END IF;
END $$;

-- 2. Cleanup marker-based trước khi insert (idempotent)
DELETE FROM edoc.sign_transactions
WHERE error_message LIKE 'PHASE12_SEED_%' OR provider_txn_id LIKE 'PHASE12_%';

-- 3. Insert 3 txn: pending / completed / failed (dùng 1 attachment mẫu nếu có)
DO $$
DECLARE
  v_attachment_id BIGINT;
  v_doc_id BIGINT;
BEGIN
  SELECT a.id, a.outgoing_doc_id
    INTO v_attachment_id, v_doc_id
    FROM edoc.attachment_outgoing_docs a
    ORDER BY a.id
    LIMIT 1;

  IF v_attachment_id IS NULL THEN
    RAISE NOTICE 'Không có attachment_outgoing_docs — skip seed txn. Tab pending/completed/failed sẽ trống.';
    RETURN;
  END IF;

  -- Pending transaction
  INSERT INTO edoc.sign_transactions
    (staff_id, provider_code, provider_txn_id, attachment_id, attachment_type, doc_id, doc_type,
     status, retry_count, created_at, started_at, expires_at)
  VALUES
    (1, 'SMARTCA_VNPT', 'PHASE12_PENDING_001', v_attachment_id, 'outgoing', v_doc_id, 'outgoing_doc',
     'pending', 0, NOW(), NOW(), NOW() + INTERVAL '3 minutes');

  -- Completed transaction
  INSERT INTO edoc.sign_transactions
    (staff_id, provider_code, provider_txn_id, attachment_id, attachment_type, doc_id, doc_type,
     signature_base64, signed_file_path,
     status, retry_count, created_at, started_at, completed_at, expires_at)
  VALUES
    (1, 'SMARTCA_VNPT', 'PHASE12_COMPLETED_001', v_attachment_id, 'outgoing', v_doc_id, 'outgoing_doc',
     'FAKE_SIGNATURE_BASE64_FOR_UAT_ONLY', 'signed/phase12/fake-signed-uat.pdf',
     'completed', 0, NOW() - INTERVAL '1 hour', NOW() - INTERVAL '59 minutes',
     NOW() - INTERVAL '58 minutes', NOW() + INTERVAL '2 minutes');

  -- Failed transaction
  INSERT INTO edoc.sign_transactions
    (staff_id, provider_code, provider_txn_id, attachment_id, attachment_type, doc_id, doc_type,
     status, error_message, retry_count, created_at, started_at, completed_at, expires_at)
  VALUES
    (1, 'SMARTCA_VNPT', 'PHASE12_FAILED_001', v_attachment_id, 'outgoing', v_doc_id, 'outgoing_doc',
     'failed',
     'PHASE12_SEED_Provider phản hồi: Người dùng từ chối xác nhận OTP trong thời gian cho phép (3 phút). Vui lòng thử lại.',
     2, NOW() - INTERVAL '2 hours', NOW() - INTERVAL '119 minutes',
     NOW() - INTERVAL '116 minutes', NOW() - INTERVAL '117 minutes');

  RAISE NOTICE 'Seed Phase 12: 3 sign_transactions (pending/completed/failed) inserted cho attachment_id=%, doc_id=%', v_attachment_id, v_doc_id;
END $$;

-- 4. Verify results
DO $$
DECLARE
  v_count INT;
BEGIN
  SELECT COUNT(*) INTO v_count
    FROM edoc.sign_transactions
    WHERE provider_txn_id LIKE 'PHASE12_%';
  IF v_count NOT IN (0, 3) THEN
    RAISE EXCEPTION 'Expected 0 or 3 seed txns, got %', v_count;
  END IF;
  RAISE NOTICE 'Phase 12 seed verify: % sign_transactions với marker PHASE12_*', v_count;
END $$;

COMMIT;

-- ============================================================================
-- Cleanup (nếu muốn xóa seed Phase 12 sau UAT):
--   DELETE FROM edoc.sign_transactions WHERE provider_txn_id LIKE 'PHASE12_%';
-- ============================================================================
```

**Lưu ý:**

- Marker-based cleanup `provider_txn_id LIKE 'PHASE12_%'` + `error_message LIKE 'PHASE12_SEED_%'` — idempotent re-run OK
- DO block với early RETURN nếu không có attachment → không crash
- Dùng staff_id=1 (admin từ seed 001) — luôn có và luôn có quyền xem
- `expires_at` pending = NOW() + 3 phút → nếu worker chạy, sẽ tự expire
- signed_file_path fake `signed/phase12/fake-signed-uat.pdf` — key không tồn tại MinIO → download endpoint Plan 12-01 trả presigned URL nhưng download HTTP 404 — KHÔNG regression endpoint logic
- KHÔNG tạo attachment mới — nếu seed 002 chưa có attachment_outgoing_docs, seed này gracefully skip

**Cách chạy trên Linux/macOS:**

```bash
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
  -f - < e_office_app_new/database/test_data/phase12_seed_sign_states.sql
```

**Windows PowerShell:**

```powershell
Get-Content e_office_app_new/database/test_data/phase12_seed_sign_states.sql | `
  docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1
```
  </action>
  <verify>
    <automated>test -f e_office_app_new/database/test_data/phase12_seed_sign_states.sql &amp;&amp; grep -c "PHASE12_" e_office_app_new/database/test_data/phase12_seed_sign_states.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `e_office_app_new/database/test_data/phase12_seed_sign_states.sql` tồn tại
    - grep PHASE12_ trả về ít nhất 5 matches (cleanup DELETE + pending + completed + failed + error_message marker + verify query)
    - File chứa `BEGIN;` và `COMMIT;` wrap idempotent
    - File có DO block guard admin staff_id=1 tồn tại + provider config tồn tại
    - Chạy file trong dev DB lần 1 → 0 error, NOTICE "Seed Phase 12: 3 sign_transactions inserted..." (hoặc "skip seed txn" nếu không có attachment)
    - Chạy lần 2 (idempotent test) → 0 error, row count vẫn 3 hoặc 0
    - Post-seed: `SELECT COUNT(*) FROM edoc.sign_transactions WHERE provider_txn_id LIKE 'PHASE12_%'` = 3 hoặc 0
  </acceptance_criteria>
  <done>
    - Seed file committed vào git
    - User/dev chạy được lệnh docker psql apply seed trên dev DB không lỗi
    - API `/api/ky-so/danh-sach/counts` của admin trả `pending:1, completed:1, failed:1` (nếu có attachment base)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT Checkpoint — E2E verify 5 AC + AC#5 regression</name>
  <files>.planning/phases/12-menu-ky-so-danh-sach-4-tab/12-03-E2E-SCRIPT.md</files>
  <action>
Trước khi prompt user UAT, Claude tạo file `.planning/phases/12-menu-ky-so-danh-sach-4-tab/12-03-E2E-SCRIPT.md` chứa toàn bộ 8 test case dưới đây (để user có bản in/tham khảo tách biệt). File này commit cùng phase. Sau đó chờ user thực hiện manual UAT.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-menu-ky-so-danh-sach-4-tab/12-03-E2E-SCRIPT.md</automated>
  </verify>
  <done>User type `approved` hoặc `fail: ...` hoặc `skip realtime`</done>
  <what-built>
    - Plan 12-01: Endpoint BE `GET /api/ky-so/sign/:id/download` + sidebar submenu "Danh sách ký số" + breadcrumb
    - Plan 12-02: Trang `/ky-so/danh-sach` 4 tab (Cần ký / Đang xử lý / Đã ký / Thất bại) với badge count, actions (Ký số / Hủy / Tải / Ký lại), realtime socket
    - Plan 12-03 Task 1: Verify AC#5 không regression (3 detail page vẫn dùng useSigning)
    - Plan 12-03 Task 2: Seed SQL tạo 4 test state cho UAT
  </what-built>
  <how-to-verify>

**Chuẩn bị:**

1. Backend + frontend running (`cd e_office_app_new/backend && npm run dev`, `cd e_office_app_new/frontend && npm run dev`)
2. Login admin user `admin / 123456` (hoặc credentials seed 001)
3. Chạy seed Task 2: `docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -f - < e_office_app_new/database/test_data/phase12_seed_sign_states.sql`

***

**AC#1 — Sidebar menu Ký số với 3 submenu:**

1. Non-admin user login: kiểm tra sidebar — group "KÝ SỐ" có 2 submenu: "Tài khoản ký số cá nhân" + "Danh sách ký số". Icon `SafetyCertificate` cho cả 2.
2. Admin user login: kiểm tra sidebar — group "KÝ SỐ" có 3 submenu: "Cấu hình ký số hệ thống" + "Tài khoản ký số cá nhân" + "Danh sách ký số".

Expected: PASS — phân quyền đúng theo role.

***

**AC#2 — Trang /ky-so/danh-sach với 4 tab badge:**

3. Click "Danh sách ký số" → URL `/ky-so/danh-sach?tab=need_sign&page=1&pageSize=20`
4. Kiểm tra 4 tab label: "Cần ký" / "Đang xử lý" / "Đã ký" / "Thất bại"
5. Mỗi tab label có Badge count (sau seed: pending=1, completed=1, failed=1; need_sign tùy data)

Expected: PASS — đủ 4 tab + badge, màu sắc phân biệt (warning / info / success / danger).

***

**AC#3 — Tab Cần ký → button Ký số mở modal:**

6. Click tab "Cần ký". Nếu có row, click button "Ký số" trên 1 row.
7. SignModal mở với file_name + attachmentType đúng.
8. Bấm "Hủy" đóng modal — list không thay đổi.

Expected: PASS — modal mở, có thể đóng mà không crash.

***

**AC#4.a — Tab Đang xử lý → button Hủy:**

9. Click tab "Đang xử lý" → thấy row pending từ seed (provider_txn_id='PHASE12_PENDING_001')
10. Click "Hủy" → Modal.confirm "Bạn có chắc muốn hủy giao dịch ký cho file ...?"
11. Click "Hủy giao dịch" → message "Đã hủy giao dịch ký số" → row biến mất khỏi tab → badge pending giảm 1

Expected: PASS — API `/cancel` gọi thành công, UI cập nhật.

***

**AC#4.b — Tab Đã ký → button Tải file đã ký:**

12. Click tab "Đã ký" → thấy row completed từ seed (signed_file_path fake)
13. Click "Tải file đã ký" → Network tab DevTools: request `GET /api/ky-so/sign/<id>/download` → response 200 JSON `{ url: "...", file_name: "signed_fake-signed-uat.pdf", expires_in: 600 }`
14. Response header `Cache-Control: no-store`
15. Tab browser mới mở với URL → HTTP 404 (MinIO không có object thật — KHÔNG phải lỗi BE)

Expected: PASS — endpoint trả shape đúng, browser trigger download. 404 MinIO là do seed fake, KHÔNG phải regression.

***

**AC#4.c — Tab Thất bại → button Ký lại:**

16. Click tab "Thất bại" → thấy row failed, Tooltip hover error_message hiển thị full text "Provider phản hồi: Người dùng từ chối xác nhận OTP..."
17. Click "Ký lại" → SignModal mở tạo transaction mới (record cũ giữ nguyên trong tab Thất bại cho audit)
18. Đóng modal → tab Thất bại vẫn có row cũ, tab Đang xử lý có thêm row mới

Expected: PASS — ký lại tạo txn mới không reset record cũ, audit trail giữ.

***

**AC#5 — Detail VB vẫn giữ button Ký số (regression check):**

19. Mở `/van-ban-di/<id>` (1 VB đi có attachment PDF). File attachment row có button "Ký số" → click → SignModal mở với attachmentType='outgoing'. Đóng.
20. Mở `/van-ban-du-thao/<id>` (1 dự thảo có attachment PDF). Button "Ký số" → modal mở với attachmentType='drafting'. Đóng.
21. Mở `/ho-so-cong-viec/<id>` (1 HSCV có status 2 hoặc 3 + attachment PDF). Button "Ký số" → modal mở với attachmentType='handling'. Đóng.

Expected: PASS — 3 trang detail đều có button, mở CÙNG SignModal như tab "Cần ký" → AC#5 NOT regressed từ Phase 11-07/08.

***

**AC#2.b — Realtime socket update:**

22. Mở 2 browser tab: Tab A = `/ky-so/danh-sach?tab=pending`, Tab B = DevTools Network tab
23. Chạy SQL: `UPDATE edoc.sign_transactions SET status = 'completed', completed_at = NOW() WHERE provider_txn_id = 'PHASE12_PENDING_001';` (giả lập worker hoàn tất)
24. Backend cần emit socket event — nếu backend KHÔNG tự emit khi UPDATE trực tiếp DB, có thể skip test realtime này và note là "cần worker thực chạy để test"
25. Alternative: chạy lại seed + user trigger thật (click "Ký số" từ detail VB) → worker fail vì provider mock → socket SIGN_FAILED emit → Tab A list tự refresh

Expected: PASS hoặc PARTIAL (note "cần worker thực test").

***

**AC#2.c — URL sync:**

26. Ở tab bất kỳ, đổi pageSize → URL update `?pageSize=50`
27. Reload trang → vẫn giữ tab + page + pageSize
28. Tab switch → `page=1` reset, URL update

Expected: PASS.

***

**Self-check grep (Claude làm, không cần user):**

29. Grep `/api/ky-so/sign/.*download` trong frontend → 1+ match trong page.tsx Plan 12-02
30. Grep `useSigning()` trong page.tsx Plan 12-02 → 1 match
31. Grep `SOCKET_EVENTS.SIGN_COMPLETED` trong page.tsx → 1 match
32. Grep `key: '/ky-so/danh-sach'` trong MainLayout.tsx → 1 match

***

**Chấp nhận kết quả:**

- Tất cả 5 AC ROADMAP PASS → Phase 12 complete
- AC#5 regression PASS → Phase 11-07/08 không bị phá
- Nếu AC nào FAIL → user ghi rõ bug, Claude tạo quick-fix plan sau

  </how-to-verify>
  <resume-signal>
Type `approved` nếu 5 AC + AC#5 regression đều PASS.

Type `fail: <chi tiết bug>` nếu AC nào FAIL — Claude sẽ tạo gap plan.

Type `skip realtime` nếu chỉ test realtime bị skip (acceptable) nhưng 7 test case kia PASS.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Không mở trust boundary mới — Plan 12-03 chỉ verify + seed data + UAT. Seed SQL là file test_data/, không chạy production.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | I (Info Disclosure) | Seed SQL có `signature_base64 = 'FAKE_...'` | accept | Seed chỉ chạy dev env, KHÔNG production (deploy scripts Phase 14 không include folder `test_data/`). Docstring comment rõ "KHÔNG dùng trong production". |
| T-12-10 | I (Info Disclosure) | Seed log leak marker PHASE12_ | accept | Marker public, không chứa credential. Cleanup hướng dẫn inline. |
| T-12-11 | T (Tampering) | User bypass UAT checkpoint ký "approved" | accept | Trust user (solo developer) — không có mechanism nào enforce được. Risk thấp vì Claude log rõ AC nào verify. |

**Regression risk T-12-12**: AC#5 regression từ Phase 11-07/08 — mitigate qua Task 1 grep trước khi proceed; nếu grep FAIL → STOP plan.
</threat_model>

<verification>
**Tự động:**

```bash
# Task 1 verify: 3 file đều có useSigning (dùng Grep tool, không bash)
# Kỳ vọng: 3/3 file trả về match

# Task 2 verify: seed file exists + có markers
test -f e_office_app_new/database/test_data/phase12_seed_sign_states.sql
grep -c "PHASE12_" e_office_app_new/database/test_data/phase12_seed_sign_states.sql
# Expected: >= 5

# Task 2 verify apply seed (dev DB only):
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
  -f - < e_office_app_new/database/test_data/phase12_seed_sign_states.sql
# Expected: 0 error, NOTICE messages

# Post-seed verify:
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc \
  "SELECT COUNT(*) FROM edoc.sign_transactions WHERE provider_txn_id LIKE 'PHASE12_%'"
# Expected: 3 (or 0 if no attachment base — both acceptable)

# Task 2 verify re-run idempotent:
# Chạy lần 2 → vẫn 0 error, count vẫn 3 hoặc 0
```

**Thủ công (UAT):**

User chạy 8 test case trong Task 3 `how-to-verify`. Claude KHÔNG tự mark phase complete — chờ user type `approved`.
</verification>

<success_criteria>
- [ ] Task 1 regression grep: 3/3 detail VB page có `useSigning` — AC#5 NOT regressed
- [ ] Task 2 seed file tồn tại, idempotent, chạy 2 lần đều PASS
- [ ] Task 2 sau seed: `sign_transactions` có 3 row marker `PHASE12_*` (hoặc 0 nếu không có attachment base)
- [ ] Task 3 E2E script Markdown được tạo ở `.planning/phases/12-menu-ky-so-danh-sach-4-tab/12-03-E2E-SCRIPT.md`
- [ ] User confirm `approved` → 5 AC ROADMAP + AC#5 regression all PASS
- [ ] Nếu user `fail: ...` → tạo gap plan theo standard flow
- [ ] Không implement feature mới — Plan 12-03 thuần verify/seed/UAT
</success_criteria>

<output>
After completion (user approved), create `.planning/phases/12-menu-ky-so-danh-sach-4-tab/12-03-SUMMARY.md` theo template `summary.md`.

Cũng tạo `.planning/phases/12-menu-ky-so-danh-sach-4-tab/12-03-E2E-SCRIPT.md` chứa toàn bộ nội dung `how-to-verify` của Task 3 (dùng làm reference cho UAT sau này).

Commit messages:
- Task 1: không commit (chỉ verify, không sửa code)
- Task 2: `test(12-03): seed SQL 4 sign states (pending/completed/failed) cho UAT`
- Task 3: `docs(12-03): E2E script + UAT checkpoint Phase 12`
</output>
