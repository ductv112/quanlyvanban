---
phase: 05-kho-l-u-tr-t-i-li-u-h-p
plan: 04
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/kho-luu-tru/page.tsx
  - e_office_app_new/frontend/src/app/(main)/kho-luu-tru/muon-tra/page.tsx
autonomous: true
requirements: [KHO-01, KHO-02, KHO-03]

must_haves:
  truths:
    - "Admin can manage Kho tree and Phong tree (CRUD)"
    - "User can view/filter/CRUD ho so luu tru by fond/warehouse"
    - "User can create borrow request, admin can approve/reject, flow works end-to-end"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/kho-luu-tru/page.tsx"
      provides: "Archive management page with tree + record list + borrow"
      min_lines: 400
    - path: "e_office_app_new/frontend/src/app/(main)/kho-luu-tru/muon-tra/page.tsx"
      provides: "Borrow/return management page"
      min_lines: 300
  key_links:
    - from: "kho-luu-tru/page.tsx"
      to: "/api/kho-luu-tru"
      via: "api.get/api.post"
      pattern: "api\\.(get|post|put|delete).*kho-luu-tru"
---

<objective>
Create frontend pages for Archive module — Kho/Phong tree management, Ho so luu tru list/CRUD, and borrow/return flow.

Purpose: Users can manage archive storage — warehouse/fond hierarchy, records, and borrow/return requests.
Output: 2 page files for archive management
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-kho-l-u-tr-t-i-li-u-h-p/05-02-SUMMARY.md

<interfaces>
From e_office_app_new/backend/src/routes/archive.ts (API endpoints):
```
GET    /api/kho-luu-tru/kho          — warehouse tree
POST   /api/kho-luu-tru/kho          — create warehouse
PUT    /api/kho-luu-tru/kho/:id      — update warehouse
DELETE /api/kho-luu-tru/kho/:id      — delete warehouse
GET    /api/kho-luu-tru/phong        — fond tree
POST   /api/kho-luu-tru/phong        — create fond
PUT    /api/kho-luu-tru/phong/:id    — update fond
DELETE /api/kho-luu-tru/phong/:id    — delete fond
GET    /api/kho-luu-tru/ho-so        — record list (query: fond_id, warehouse_id, keyword, page, page_size)
POST   /api/kho-luu-tru/ho-so        — create record
PUT    /api/kho-luu-tru/ho-so/:id    — update record
DELETE /api/kho-luu-tru/ho-so/:id    — delete record
GET    /api/kho-luu-tru/muon-tra     — borrow list (query: status, keyword, page, page_size)
POST   /api/kho-luu-tru/muon-tra     — create borrow request (body: name, record_ids, emergency, notice, borrow_date)
PATCH  /api/kho-luu-tru/muon-tra/:id/approve  — approve
PATCH  /api/kho-luu-tru/muon-tra/:id/reject   — reject (body: notice)
PATCH  /api/kho-luu-tru/muon-tra/:id/checkout  — checkout (da muon)
PATCH  /api/kho-luu-tru/muon-tra/:id/return    — return (da tra)
```

From e_office_app_new/frontend/src/lib/tree-utils.ts:
```typescript
export function buildTree<T extends { id: number; parent_id: number }>(items: T[], rootParentId?: number): TreeNode<T>[];
export function filterTree<T>(tree: TreeNode<T>[], predicate: (item: T) => boolean): TreeNode<T>[];
export function flattenTreeForSelect<T extends { id: number; name?: string }>(tree: TreeNode<T>[]): { value: number; label: string; disabled?: boolean }[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive main page — Kho/Phong tree + Ho so luu tru list + Drawer CRUD</name>
  <files>e_office_app_new/frontend/src/app/(main)/kho-luu-tru/page.tsx</files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/quan-tri/don-vi/page.tsx
    - e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx
    - e_office_app_new/frontend/src/lib/tree-utils.ts
    - e_office_app_new/frontend/src/lib/api.ts
    - e_office_app_new/frontend/src/config/theme.ts
    - e_office_app_new/frontend/src/app/globals.css
  </read_first>
  <action>
Create `kho-luu-tru/page.tsx` following established patterns. Per D-00c, use CSS classes from globals.css, Inter font, Deep Navy palette.

**Layout:** Two-column layout (similar to don-vi/page.tsx pattern):
- Left panel (width ~300px): Tabs with 2 tabs — "Kho luu tru" (warehouse tree) and "Phong luu tru" (fond tree)
  - Each tab shows an Ant Design Tree with CRUD actions (add/edit/delete via Dropdown on each node)
  - Use `buildTree()` from `tree-utils.ts` to convert flat API response to tree
- Right panel: Table showing `Ho so luu tru` records filtered by selected kho/phong

**Page structure:**
```tsx
'use client';
import { useState, useEffect, useCallback } from 'react';
import { Table, Tree, Tabs, Button, Drawer, Form, Input, Select, DatePicker, Space, Dropdown, Modal, Tag, message, Skeleton } from 'antd';
import { PlusOutlined, EditOutlined, DeleteOutlined, MoreOutlined } from '@ant-design/icons';
import { api } from '@/lib/api';
import { buildTree } from '@/lib/tree-utils';
```

**State:**
- `warehouses` / `fonds` — tree data
- `selectedWarehouseId` / `selectedFondId` — filter
- `records` — table data
- `pagination` — { current, pageSize, total }
- `keyword` — search
- `loading` / `treeLoading`
- `drawerOpen` / `editingRecord` / `drawerType` ('warehouse' | 'fond' | 'record')

**Warehouse/Fond Tree:**
- Fetch on mount: `api.get('/kho-luu-tru/kho')` and `api.get('/kho-luu-tru/phong')`
- Use `buildTree(data, 0)` with `parent_id` field
- Tree node title: `{code} - {name}` for warehouse, `{fond_code} - {fond_name}` for fond
- Click tree node: set selectedWarehouseId/selectedFondId, fetch records
- Each node has Dropdown (MoreOutlined) with: Them con (add child), Sua, Xoa (Popconfirm)

**Record Table:**
- Columns: STT, Ma ho so (file_code), Ten ho so (title), Phong (fond_name), Kho (warehouse_name), Ngay bat dau (start_date), Ngay hoan thanh (complete_date), So tai lieu (total_doc), Thao tac
- Fetch: `api.get('/kho-luu-tru/ho-so', { params: { fond_id, warehouse_id, keyword, page, page_size } })`
- Actions column: Dropdown with Sua, Xoa (Modal.confirm with danger:true)

**Drawers:**
- Warehouse Drawer (width: 720): code, name, phone_number, address, description, parent_id (Select from tree), type_id, is_unit checkbox
- Fond Drawer (width: 720): fond_code, fond_name, fond_history, archives_time, paper_total, paper_digital, description, parent_id
- Record Drawer (width: 720): file_code, title, fond_id (Select from fond tree), warehouse_id (Select from warehouse tree), in_charge_staff_id, start_date, complete_date, total_doc, description, keyword, maintenance, language, format

Use `rootClassName="drawer-gradient"` on all drawers. Title dynamic: "Them moi kho" / "Chinh sua kho", etc. Footer: Save + Cancel buttons in drawer `extra`.

**Page header:** Use `.page-header` class with title "Kho luu tru" and add button.
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `e_office_app_new/frontend/src/app/(main)/kho-luu-tru/page.tsx` exists
    - Contains `'use client'`
    - Contains `api.get('/kho-luu-tru/kho'`
    - Contains `api.get('/kho-luu-tru/phong'`
    - Contains `api.get('/kho-luu-tru/ho-so'`
    - Contains `buildTree`
    - Contains `<Tree` (Ant Design Tree component)
    - Contains `<Table` (Ant Design Table component)
    - Contains `<Drawer` with `rootClassName="drawer-gradient"`
    - Contains `page-header` CSS class
    - Contains Vietnamese labels: `Kho luu tru`, `Phong luu tru`, `Ho so luu tru`
    - Next.js build succeeds
  </acceptance_criteria>
  <done>Archive page with left tree panel (warehouse/fond tabs with CRUD), right record table (filtered, paginated, CRUD via Drawer). All UI in Vietnamese. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Borrow/Return page — request list + create/approve/reject/checkout/return flow</name>
  <files>e_office_app_new/frontend/src/app/(main)/kho-luu-tru/muon-tra/page.tsx</files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/kho-luu-tru/page.tsx
    - e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx
    - e_office_app_new/frontend/src/lib/api.ts
    - e_office_app_new/frontend/src/app/globals.css
  </read_first>
  <action>
Create `kho-luu-tru/muon-tra/page.tsx` for borrow/return management. Per D-03, follow source code cu flow.

**Layout:**
- Page header: "Quan ly muon/tra ho so" with "Tao yeu cau muon" button
- Filter row: Status filter (Tabs or Select: Tat ca, Moi, Da duyet, Da muon, Da tra, Tu choi) + keyword search
- Table with columns: STT, Ten yeu cau (name), Nguoi yeu cau (creator_name), Ngay muon (borrow_date), Trang thai (status Tag), Ngay tao (created_date), Thao tac

**Status Tags:**
```typescript
const STATUS_MAP: Record<number, { label: string; color: string }> = {
  0: { label: 'Moi', color: 'default' },
  1: { label: 'Da duyet', color: 'blue' },
  2: { label: 'Da muon', color: 'orange' },
  3: { label: 'Da tra', color: 'green' },
  [-1]: { label: 'Tu choi', color: 'red' },
};
```

**Actions (conditional by status):**
- Status 0 (Moi): Duyet, Tu choi, Xoa
- Status 1 (Da duyet): Cho muon (checkout)
- Status 2 (Da muon): Tra lai (return)
- Status 3 / -1: View only

**Create Borrow Request Drawer (width: 720):**
- Fields: name (ten yeu cau), borrow_date (DatePicker), emergency (Select: 0=Binh thuong, 1=Khan), notice (TextArea)
- Record selection: Transfer component or Table with checkboxes to select records to borrow
  - Load records from `api.get('/kho-luu-tru/ho-so')` in the drawer
  - Selected record_ids sent in body

**Approve/Reject:**
- Approve: `api.patch(/kho-luu-tru/muon-tra/${id}/approve)` — show success message
- Reject: Modal.confirm with TextArea for rejection reason (notice), then `api.patch(.../reject, { notice })`

**Checkout/Return:**
- Checkout: `api.patch(.../checkout)` — confirm dialog
- Return: `api.patch(.../return)` — confirm dialog

Fetch list: `api.get('/kho-luu-tru/muon-tra', { params: { status, keyword, page, page_size } })`

Use `.page-header`, `.page-card`, `.filter-row` CSS classes. Vietnamese UI text throughout.
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `e_office_app_new/frontend/src/app/(main)/kho-luu-tru/muon-tra/page.tsx` exists
    - Contains `'use client'`
    - Contains `api.get('/kho-luu-tru/muon-tra'`
    - Contains `api.patch` calls for approve, reject, checkout, return
    - Contains `api.post('/kho-luu-tru/muon-tra'` for creating requests
    - Contains status Tag rendering with 5 statuses (Moi, Da duyet, Da muon, Da tra, Tu choi)
    - Contains `<Drawer` for create borrow request
    - Contains Vietnamese labels
    - Next.js build succeeds
  </acceptance_criteria>
  <done>Borrow/return page with status-filtered list, create request Drawer with record selection, approve/reject/checkout/return actions. Full borrow lifecycle visible. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> API | All actions via authenticated API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering | Borrow approve button | mitigate | Button only shown when status=0; backend validates status in SP |
| T-05-15 | Info Disclosure | Record list | accept | Records filtered by unit_id from JWT at API level |
</threat_model>

<verification>
Next.js build succeeds. Pages render with correct data from API.
</verification>

<success_criteria>
- kho-luu-tru/page.tsx: Tree panel + record table + 3 drawers (warehouse/fond/record)
- kho-luu-tru/muon-tra/page.tsx: Borrow list + create drawer + approve/reject/checkout/return flow
- All Vietnamese UI text, follows existing CSS patterns
- Next.js build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-kho-l-u-tr-t-i-li-u-h-p/05-04-SUMMARY.md`
</output>
