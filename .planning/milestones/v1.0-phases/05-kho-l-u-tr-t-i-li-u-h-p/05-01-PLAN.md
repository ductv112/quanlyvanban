---
phase: 05-kho-l-u-tr-t-i-li-u-h-p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/database/migrations/016_sprint11_archive_storage.sql
  - e_office_app_new/database/migrations/017_sprint12_documents_contracts.sql
  - e_office_app_new/database/migrations/018_sprint13_meetings.sql
autonomous: true
requirements: [KHO-01, KHO-02, KHO-03, DOC-01, DOC-02, HOP-01, HOP-02, HOP-03, HOP-04]

must_haves:
  truths:
    - "Kho/Phong tree tables exist in esto schema with correct columns from source code cu"
    - "Record (ho so luu tru) table exists with borrow_requests table for muon/tra flow"
    - "Document categories and contracts tables exist in iso/cont schemas"
    - "Meeting rooms, schedules, questions, answers tables exist in edoc schema"
    - "All stored functions follow naming convention {schema}.fn_{module}_{action}"
  artifacts:
    - path: "e_office_app_new/database/migrations/016_sprint11_archive_storage.sql"
      provides: "Archive tables + stored functions in esto schema"
      contains: "esto.warehouses"
    - path: "e_office_app_new/database/migrations/017_sprint12_documents_contracts.sql"
      provides: "Document + contract tables + stored functions in iso/cont schemas"
      contains: "cont.contracts"
    - path: "e_office_app_new/database/migrations/018_sprint13_meetings.sql"
      provides: "Meeting tables + stored functions in edoc schema"
      contains: "edoc.rooms"
  key_links:
    - from: "016_sprint11_archive_storage.sql"
      to: "esto schema"
      via: "CREATE TABLE esto.warehouses"
      pattern: "esto\\.fn_"
    - from: "017_sprint12_documents_contracts.sql"
      to: "cont/iso schemas"
      via: "CREATE TABLE cont.contracts"
      pattern: "cont\\.fn_|iso\\.fn_"
    - from: "018_sprint13_meetings.sql"
      to: "edoc schema"
      via: "CREATE TABLE edoc.rooms"
      pattern: "edoc\\.fn_room"
---

<objective>
Create all database migrations for Phase 5 — Sprint 11 (archive/storage), Sprint 12 (documents/contracts), Sprint 13 (meetings). Three SQL migration files with tables and stored functions.

Purpose: Foundation layer — all backend repositories depend on these stored functions existing.
Output: 3 migration files in database/migrations/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@e_office_app_new/database/init/01_create_schemas.sql
@e_office_app_new/database/migrations/014_sprint9_calendar_directory.sql
@e_office_app_new/database/migrations/010_sprint5_handling_doc_sps.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sprint 11 migration — Archive/Storage tables + stored functions (esto schema)</name>
  <files>e_office_app_new/database/migrations/016_sprint11_archive_storage.sql</files>
  <read_first>
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/esto/Warehouse.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/esto/Fond.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/esto/Record.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/esto/BorrowRequest.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/esto/EstoCategory.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/esto/DocumentArchive.cs
    - docs/source_code_cu/sources/OneWin.WebApp/Areas/Storage/Controllers/WarehouseController.cs
    - docs/source_code_cu/sources/OneWin.WebApp/Areas/Storage/Controllers/FondController.cs
    - docs/source_code_cu/sources/OneWin.WebApp/Areas/Storage/Controllers/RecordController.cs
    - docs/source_code_cu/sources/OneWin.WebApp/Areas/Storage/Controllers/BorrowerController.cs
    - e_office_app_new/database/init/01_create_schemas.sql
    - e_office_app_new/database/migrations/010_sprint5_handling_doc_sps.sql
  </read_first>
  <action>
Create `016_sprint11_archive_storage.sql` in esto schema. Per D-00a, reference source code cu (.NET) data objects for exact field names and business logic. Per D-00b, SP must return exact DB column names (snake_case), no aliases. Per D-11, use existing `esto` schema.

**Tables to create (map from .NET source code cu, convert PascalCase to snake_case):**

1. `esto.warehouses` — Kho luu tru (from Warehouse.cs):
   - id SERIAL PRIMARY KEY, unit_id INT NOT NULL REFERENCES public.departments(id), type_id INT, code VARCHAR(50), name VARCHAR(200) NOT NULL, phone_number VARCHAR(50), address VARCHAR(500), status BOOLEAN DEFAULT true, description TEXT, parent_id INT DEFAULT 0, is_unit BOOLEAN DEFAULT false, warehouse_level INT DEFAULT 0, limit_child INT DEFAULT 0, position VARCHAR(200), is_deleted BOOLEAN DEFAULT false, created_user_id INT NOT NULL, created_date TIMESTAMPTZ DEFAULT NOW(), modified_user_id INT, modified_date TIMESTAMPTZ
   - Indexes: idx_warehouses_unit_id, idx_warehouses_parent_id
   - Unique: uq_warehouses_code (unit_id, code)

2. `esto.fonds` — Phong luu tru (from Fond.cs):
   - id SERIAL PRIMARY KEY, unit_id INT NOT NULL, parent_id INT DEFAULT 0, fond_code VARCHAR(50), fond_name VARCHAR(200) NOT NULL, fond_history TEXT, archives_time VARCHAR(100), paper_total DECIMAL, paper_digital DECIMAL, keys_group VARCHAR(200), other_type VARCHAR(200), language VARCHAR(100), lookup_tools VARCHAR(200), coppy_number DECIMAL, status INT DEFAULT 1, description TEXT, version DECIMAL, created_user_id INT NOT NULL, created_date TIMESTAMPTZ DEFAULT NOW(), modified_user_id INT, modified_date TIMESTAMPTZ
   - Indexes: idx_fonds_unit_id
   - Unique: uq_fonds_code (unit_id, fond_code)

3. `esto.records` — Ho so luu tru (from Record.cs):
   - id BIGSERIAL PRIMARY KEY, unit_id INT NOT NULL, fond_id INT NOT NULL REFERENCES esto.fonds(id), file_code VARCHAR(100), file_catalog INT, file_notation VARCHAR(200), title VARCHAR(500) NOT NULL, maintenance VARCHAR(200), rights VARCHAR(200), language VARCHAR(100), start_date DATE, complete_date DATE, total_doc INT, description TEXT, infor_sign VARCHAR(200), keyword VARCHAR(500), total_paper DECIMAL, page_number DECIMAL, format INT DEFAULT 0, archive_date DATE, reception_archive_id INT, in_charge_staff_id INT NOT NULL, parent_id INT DEFAULT 0, warehouse_id INT NOT NULL REFERENCES esto.warehouses(id), reception_date DATE, reception_from INT DEFAULT 0, transfer_staff VARCHAR(200), is_document_original BOOLEAN, number_of_copy INT, doc_field_id INT, transfer_online_status BOOLEAN DEFAULT false, created_user_id INT, created_date TIMESTAMPTZ DEFAULT NOW(), modified_user_id INT, modified_date TIMESTAMPTZ
   - Indexes: idx_records_unit_id, idx_records_fond_id, idx_records_warehouse_id

4. `esto.borrow_requests` — Yeu cau muon/tra (from BorrowRequest.cs):
   - id BIGSERIAL PRIMARY KEY, name VARCHAR(200) NOT NULL, unit_id INT NOT NULL, emergency INT, notice TEXT, borrow_date DATE, status INT DEFAULT 0 (0=Moi, 1=Da duyet, 2=Da muon, 3=Da tra, -1=Tu choi), created_user_id INT NOT NULL, created_date TIMESTAMPTZ DEFAULT NOW(), modified_user_id INT, modified_date TIMESTAMPTZ
   - Index: idx_borrow_requests_unit_id, idx_borrow_requests_status

5. `esto.borrow_request_records` — Link muon den ho so cu the:
   - id BIGSERIAL PRIMARY KEY, borrow_request_id BIGINT NOT NULL REFERENCES esto.borrow_requests(id), record_id BIGINT NOT NULL REFERENCES esto.records(id), return_date DATE, actual_return_date DATE, UNIQUE(borrow_request_id, record_id)

**Stored Functions (per D-12 convention `esto.fn_{module}_{action}`):**

Warehouse (Kho):
- `esto.fn_warehouse_get_tree(p_unit_id)` — RETURNS TABLE with all columns, ORDER BY parent_id, name. Filter by unit_id.
- `esto.fn_warehouse_get_by_id(p_id)` — Single warehouse row
- `esto.fn_warehouse_create(...)` — Validate name required, INSERT, RETURNS TABLE(success, message, id)
- `esto.fn_warehouse_update(...)` — UPDATE, RETURNS TABLE(success, message)
- `esto.fn_warehouse_delete(p_id)` — Check no child records linked, soft delete is_deleted=TRUE, RETURNS TABLE(success, message)

Fond (Phong):
- `esto.fn_fond_get_tree(p_unit_id)` — RETURNS TABLE, ORDER BY parent_id, fond_name
- `esto.fn_fond_get_by_id(p_id)` — Single fond row
- `esto.fn_fond_create(...)` — Validate fond_name required, INSERT, RETURNS TABLE(success, message, id)
- `esto.fn_fond_update(...)` — UPDATE, RETURNS TABLE(success, message)
- `esto.fn_fond_delete(p_id)` — Check no child records, RETURNS TABLE(success, message)

Record (Ho so luu tru):
- `esto.fn_record_get_list(p_unit_id, p_fond_id, p_warehouse_id, p_keyword, p_page, p_page_size)` — Paginated list with total_count window, JOIN fond/warehouse names, ILIKE keyword on title/file_code
- `esto.fn_record_get_by_id(p_id)` — Full detail with joined names
- `esto.fn_record_create(...)` — Validate title required, INSERT, RETURNS TABLE(success, message, id)
- `esto.fn_record_update(...)` — UPDATE, RETURNS TABLE(success, message)
- `esto.fn_record_delete(p_id)` — Check no borrow requests linked, DELETE (hard), RETURNS TABLE(success, message)

Borrow (Muon/tra):
- `esto.fn_borrow_request_get_list(p_unit_id, p_status, p_keyword, p_page, p_page_size)` — Paginated, JOIN creator name, filter by status
- `esto.fn_borrow_request_get_by_id(p_id)` — Detail with records list
- `esto.fn_borrow_request_create(p_name, p_unit_id, p_emergency, p_notice, p_borrow_date, p_created_user_id, p_record_ids INT[])` — INSERT request + loop INSERT borrow_request_records, RETURNS TABLE(success, message, id)
- `esto.fn_borrow_request_approve(p_id, p_modified_user_id)` — status 0 -> 1, RETURNS TABLE(success, message)
- `esto.fn_borrow_request_reject(p_id, p_modified_user_id, p_notice)` — status 0 -> -1, RETURNS TABLE(success, message)
- `esto.fn_borrow_request_checkout(p_id, p_modified_user_id)` — status 1 -> 2 (da muon), RETURNS TABLE(success, message)
- `esto.fn_borrow_request_return(p_id, p_modified_user_id)` — status 2 -> 3 (da tra), set actual_return_date=NOW() on all linked records, RETURNS TABLE(success, message)

Total: ~20 stored functions.
  </action>
  <verify>
    <automated>cd e_office_app_new && docker exec -i qlvb-postgres psql -U postgres -d qlvb_dev -f /dev/stdin < database/migrations/016_sprint11_archive_storage.sql 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `e_office_app_new/database/migrations/016_sprint11_archive_storage.sql` exists
    - Contains `CREATE TABLE esto.warehouses`
    - Contains `CREATE TABLE esto.fonds`
    - Contains `CREATE TABLE esto.records`
    - Contains `CREATE TABLE esto.borrow_requests`
    - Contains `CREATE TABLE esto.borrow_request_records`
    - Contains `esto.fn_warehouse_get_tree`
    - Contains `esto.fn_fond_get_tree`
    - Contains `esto.fn_record_get_list`
    - Contains `esto.fn_borrow_request_create`
    - Contains `esto.fn_borrow_request_approve`
    - Contains `esto.fn_borrow_request_return`
    - All column names are snake_case matching .NET source (per D-00b)
    - No column aliases in RETURNS TABLE definitions
  </acceptance_criteria>
  <done>5 tables created in esto schema, ~20 stored functions for warehouse/fond/record/borrow CRUD + borrow flow (approve/reject/checkout/return). Migration runs without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Sprint 12 migration — Documents (iso) + Contracts (cont) tables + stored functions</name>
  <files>e_office_app_new/database/migrations/017_sprint12_documents_contracts.sql</files>
  <read_first>
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/esto/EstoCategory.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/cont/Contract.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/cont/ContractType.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/cont/SubContract.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/cont/AttachmentOfContract.cs
    - docs/source_code_cu/sources/OneWin.WebApp/Areas/Contract/Controllers/ContractController.cs
    - docs/source_code_cu/sources/OneWin.WebApp/Areas/Contract/Controllers/ContractTypeController.cs
    - e_office_app_new/database/init/01_create_schemas.sql
  </read_first>
  <action>
Create `017_sprint12_documents_contracts.sql`. Per D-04/D-06, reference source code cu for field names.

**Tables:**

1. `iso.document_categories` — Danh muc tai lieu (tree structure, per EstoCategory.cs pattern):
   - id SERIAL PRIMARY KEY, parent_id INT DEFAULT 0, code VARCHAR(50), name VARCHAR(200) NOT NULL, date_process DECIMAL, status INT DEFAULT 1, description TEXT, version DECIMAL, unit_id INT, created_user_id INT NOT NULL, created_date TIMESTAMPTZ DEFAULT NOW(), modified_user_id INT, modified_date TIMESTAMPTZ
   - Unique: uq_doc_categories_code (unit_id, code)

2. `iso.documents` — Tai lieu chung:
   - id BIGSERIAL PRIMARY KEY, unit_id INT NOT NULL, category_id INT REFERENCES iso.document_categories(id), title VARCHAR(500) NOT NULL, description TEXT, file_name VARCHAR(500), file_path VARCHAR(1000), file_size BIGINT, mime_type VARCHAR(200), keyword VARCHAR(500), status INT DEFAULT 1 (1=active, 0=inactive), created_user_id INT NOT NULL, created_date TIMESTAMPTZ DEFAULT NOW(), modified_user_id INT, modified_date TIMESTAMPTZ, is_deleted BOOLEAN DEFAULT false
   - Indexes: idx_documents_unit_id, idx_documents_category_id

3. `cont.contract_types` — Loai hop dong (from ContractType.cs):
   - id SERIAL PRIMARY KEY, unit_id INT, parent_id INT DEFAULT 0, code VARCHAR(50), name VARCHAR(200) NOT NULL, note TEXT, sort_order INT DEFAULT 0, created_user_id INT NOT NULL, created_date TIMESTAMPTZ DEFAULT NOW(), modified_user_id INT, modified_date TIMESTAMPTZ
   - Unique: uq_contract_types_code (unit_id, code)

4. `cont.contracts` — Hop dong (from Contract.cs):
   - id SERIAL PRIMARY KEY, code_index INT, contract_type_id INT REFERENCES cont.contract_types(id), department_id INT, type_of_contract INT DEFAULT 0, contact_id INT, contact_name VARCHAR(200), unit_id INT NOT NULL, code VARCHAR(100), sign_date DATE, input_date DATE, receive_date DATE, name VARCHAR(500) NOT NULL, signer VARCHAR(200), number INT, ballot VARCHAR(200), marker VARCHAR(200), curator_name VARCHAR(200), currency VARCHAR(50), transporter VARCHAR(200), staff_id INT, note TEXT, status INT DEFAULT 0 (0=Moi, 1=Dang thuc hien, 2=Hoan thanh, -1=Huy), amount VARCHAR(200), payment_amount DECIMAL, created_user_id INT NOT NULL, created_date TIMESTAMPTZ DEFAULT NOW(), modified_user_id INT, modified_date TIMESTAMPTZ
   - Indexes: idx_contracts_unit_id, idx_contracts_contract_type_id

5. `cont.contract_attachments` — Dinh kem hop dong:
   - id BIGSERIAL PRIMARY KEY, contract_id INT NOT NULL REFERENCES cont.contracts(id) ON DELETE CASCADE, file_name VARCHAR(500) NOT NULL, file_path VARCHAR(1000) NOT NULL, file_size BIGINT, mime_type VARCHAR(200), created_user_id INT NOT NULL, created_date TIMESTAMPTZ DEFAULT NOW()

**Stored Functions:**

Document Categories (iso.fn_doc_category_*):
- `iso.fn_doc_category_get_tree(p_unit_id)` — RETURNS TABLE, tree structure
- `iso.fn_doc_category_create(...)` — RETURNS TABLE(success, message, id)
- `iso.fn_doc_category_update(...)` — RETURNS TABLE(success, message)
- `iso.fn_doc_category_delete(p_id)` — Check no children or documents linked

Documents (iso.fn_document_*):
- `iso.fn_document_get_list(p_unit_id, p_category_id, p_keyword, p_page, p_page_size)` — Paginated with category_name JOIN, total_count
- `iso.fn_document_get_by_id(p_id)` — Detail with category name
- `iso.fn_document_create(...)` — RETURNS TABLE(success, message, id)
- `iso.fn_document_update(...)` — RETURNS TABLE(success, message)
- `iso.fn_document_delete(p_id)` — Soft delete is_deleted=TRUE

Contract Types (cont.fn_contract_type_*):
- `cont.fn_contract_type_get_list(p_unit_id)` — All types for unit
- `cont.fn_contract_type_create(...)` — RETURNS TABLE(success, message, id)
- `cont.fn_contract_type_update(...)` — RETURNS TABLE(success, message)
- `cont.fn_contract_type_delete(p_id)` — Check no contracts linked

Contracts (cont.fn_contract_*):
- `cont.fn_contract_get_list(p_unit_id, p_contract_type_id, p_status, p_keyword, p_page, p_page_size)` — Paginated with type_name JOIN, total_count
- `cont.fn_contract_get_by_id(p_id)` — Detail with type name, attachments count
- `cont.fn_contract_create(...)` — RETURNS TABLE(success, message, id)
- `cont.fn_contract_update(...)` — RETURNS TABLE(success, message)
- `cont.fn_contract_delete(p_id)` — Check status=0 only, RETURNS TABLE(success, message)
- `cont.fn_contract_get_attachments(p_contract_id)` — List attachments

Total: ~18 stored functions.
  </action>
  <verify>
    <automated>cd e_office_app_new && docker exec -i qlvb-postgres psql -U postgres -d qlvb_dev -f /dev/stdin < database/migrations/017_sprint12_documents_contracts.sql 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `e_office_app_new/database/migrations/017_sprint12_documents_contracts.sql` exists
    - Contains `CREATE TABLE iso.document_categories`
    - Contains `CREATE TABLE iso.documents`
    - Contains `CREATE TABLE cont.contract_types`
    - Contains `CREATE TABLE cont.contracts`
    - Contains `CREATE TABLE cont.contract_attachments`
    - Contains `iso.fn_document_get_list`
    - Contains `cont.fn_contract_get_list`
    - Contains `cont.fn_contract_type_get_list`
    - All column names are snake_case matching .NET source
  </acceptance_criteria>
  <done>5 tables (2 iso, 3 cont) and ~18 stored functions for document categories, documents, contract types, contracts CRUD. Migration runs without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Sprint 13 migration — Meeting tables + stored functions (edoc schema)</name>
  <files>e_office_app_new/database/migrations/018_sprint13_meetings.sql</files>
  <read_first>
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/edoc/Room.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/edoc/RoomGroups.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/edoc/RoomSchedule.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/edoc/RoomScheduleStaff.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/edoc/RoomScheduleQuestion.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/edoc/RoomScheduleResult.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/edoc/RoomScheduleContent.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/edoc/RoomScheduleAttachment.cs
    - docs/source_code_cu/sources/OneWin.Data.Object/Base/edoc/MeetingSchedule.cs
    - docs/source_code_cu/sources/OneWin.WebApp/Areas/Document/Controllers/RoomController.cs
    - docs/source_code_cu/sources/OneWin.WebApp/Areas/Document/Controllers/RoomScheduleController.cs
    - docs/source_code_cu/sources/OneWin.WebApp/Areas/Document/Controllers/VoteController.cs
    - docs/source_code_cu/sources/OneWin.WebApp/Areas/Manager/Views/Categories/MeetingType.cshtml
    - e_office_app_new/database/migrations/014_sprint9_calendar_directory.sql
  </read_first>
  <action>
Create `018_sprint13_meetings.sql` in edoc schema. Per D-07/D-08/D-09, reference source code cu.

**Tables:**

1. `edoc.rooms` — Phong hop (from Room.cs):
   - id SERIAL PRIMARY KEY, unit_id INT NOT NULL, name VARCHAR(200) NOT NULL, code VARCHAR(50), location VARCHAR(500), note TEXT, sort_order INT DEFAULT 0, show_in_calendar BOOLEAN DEFAULT true, is_deleted BOOLEAN DEFAULT false, created_user_id INT NOT NULL, created_date TIMESTAMPTZ DEFAULT NOW(), modified_user_id INT, modified_date TIMESTAMPTZ
   - Unique: uq_rooms_code (unit_id, code)

2. `edoc.meeting_types` — Loai cuoc hop (from MeetingType cshtml + RoomGroups.cs pattern):
   - id SERIAL PRIMARY KEY, unit_id INT NOT NULL, name VARCHAR(200) NOT NULL, description TEXT, sort_order INT DEFAULT 0, is_deleted BOOLEAN DEFAULT false, created_user_id INT NOT NULL, created_date TIMESTAMPTZ DEFAULT NOW(), modified_user_id INT, modified_date TIMESTAMPTZ

3. `edoc.room_schedules` — Cuoc hop (from RoomSchedule.cs):
   - id SERIAL PRIMARY KEY, unit_id INT NOT NULL, room_id INT NOT NULL REFERENCES edoc.rooms(id), meeting_type_id INT REFERENCES edoc.meeting_types(id), name VARCHAR(500) NOT NULL, content TEXT, component VARCHAR(500), start_date DATE NOT NULL, end_date DATE, start_time VARCHAR(10), end_time VARCHAR(10), master_id INT (chu toa - staff_id), secretary_id INT, approved INT DEFAULT 0 (0=Chua duyet, 1=Da duyet, -1=Tu choi), approved_date TIMESTAMPTZ, approved_staff_id INT, rejection_reason TEXT, meeting_status INT DEFAULT 0 (0=Chua hop, 1=Dang hop, 2=Da hop, 3=Huy), online_link VARCHAR(500), is_cancel INT DEFAULT 0, created_user_id INT NOT NULL, created_date TIMESTAMPTZ DEFAULT NOW(), modified_user_id INT, modified_date TIMESTAMPTZ
   - Indexes: idx_room_schedules_unit_id, idx_room_schedules_room_id, idx_room_schedules_start_date

4. `edoc.room_schedule_staff` — Thanh vien hop (from RoomScheduleStaff.cs):
   - id SERIAL PRIMARY KEY, room_schedule_id INT NOT NULL REFERENCES edoc.room_schedules(id) ON DELETE CASCADE, staff_id INT NOT NULL, user_type INT DEFAULT 0 (0=Thanh vien, 1=Chu toa, 2=Thu ky), is_secretary BOOLEAN DEFAULT false, is_represent BOOLEAN DEFAULT false, attendance BOOLEAN DEFAULT false, attendance_date TIMESTAMPTZ, attendance_note TEXT, received_appointment INT DEFAULT 0, received_appointment_date TIMESTAMPTZ, view_date TIMESTAMPTZ
   - Unique: uq_room_schedule_staff (room_schedule_id, staff_id)

5. `edoc.room_schedule_attachments` — Tai lieu hop:
   - id BIGSERIAL PRIMARY KEY, room_schedule_id INT NOT NULL REFERENCES edoc.room_schedules(id) ON DELETE CASCADE, file_name VARCHAR(500) NOT NULL, file_path VARCHAR(1000) NOT NULL, file_size BIGINT, mime_type VARCHAR(200), description TEXT, created_user_id INT NOT NULL, created_date TIMESTAMPTZ DEFAULT NOW()

6. `edoc.room_schedule_questions` — Cau hoi bieu quyet (from RoomScheduleQuestion.cs):
   - id UUID DEFAULT uuid_generate_v4() PRIMARY KEY, room_schedule_id INT NOT NULL REFERENCES edoc.room_schedules(id) ON DELETE CASCADE, name VARCHAR(500) NOT NULL, start_time TIMESTAMPTZ, stop_time TIMESTAMPTZ, duration INT DEFAULT 60 (seconds), status INT DEFAULT 0 (0=Chua bat dau, 1=Dang bieu quyet, 2=Ket thuc), question_type INT DEFAULT 0 (0=Single choice, 1=Multiple choice), order_no INT DEFAULT 0
   - Index: idx_questions_room_schedule_id

7. `edoc.room_schedule_answers` — Dap an bieu quyet (from RoomScheduleAnswer in Question.cs):
   - id UUID DEFAULT uuid_generate_v4() PRIMARY KEY, room_schedule_id INT NOT NULL, room_schedule_question_id UUID NOT NULL REFERENCES edoc.room_schedule_questions(id) ON DELETE CASCADE, name VARCHAR(500) NOT NULL, order_no INT DEFAULT 0, is_other BOOLEAN DEFAULT false

8. `edoc.room_schedule_votes` — Ket qua bieu quyet (realtime votes):
   - id BIGSERIAL PRIMARY KEY, room_schedule_id INT NOT NULL, question_id UUID NOT NULL REFERENCES edoc.room_schedule_questions(id) ON DELETE CASCADE, answer_id UUID NOT NULL REFERENCES edoc.room_schedule_answers(id) ON DELETE CASCADE, staff_id INT NOT NULL, other_text TEXT, voted_at TIMESTAMPTZ DEFAULT NOW()
   - Unique: uq_votes_question_staff (question_id, staff_id) for single-choice, or (question_id, answer_id, staff_id) for multi-choice — use single-choice constraint as default

**Stored Functions (edoc.fn_room_*, edoc.fn_meeting_type_*, edoc.fn_room_schedule_*):**

Rooms:
- `edoc.fn_room_get_list(p_unit_id)` — All rooms for unit, ORDER BY sort_order
- `edoc.fn_room_create(...)` — RETURNS TABLE(success, message, id)
- `edoc.fn_room_update(...)` — RETURNS TABLE(success, message)
- `edoc.fn_room_delete(p_id)` — Check no schedules linked, soft delete

Meeting Types:
- `edoc.fn_meeting_type_get_list(p_unit_id)` — All types
- `edoc.fn_meeting_type_create(...)` — RETURNS TABLE(success, message, id)
- `edoc.fn_meeting_type_update(...)` — RETURNS TABLE(success, message)
- `edoc.fn_meeting_type_delete(p_id)` — Soft delete

Room Schedules (Meetings):
- `edoc.fn_room_schedule_get_list(p_unit_id, p_room_id, p_status, p_from_date, p_to_date, p_keyword, p_page, p_page_size)` — Paginated with room_name, meeting_type_name, master_name JOINs, total_count
- `edoc.fn_room_schedule_get_by_id(p_id)` — Full detail with all JOINs
- `edoc.fn_room_schedule_create(...)` — RETURNS TABLE(success, message, id)
- `edoc.fn_room_schedule_update(...)` — RETURNS TABLE(success, message)
- `edoc.fn_room_schedule_delete(p_id)` — Only when approved=0, RETURNS TABLE(success, message)
- `edoc.fn_room_schedule_approve(p_id, p_approved_staff_id)` — approved 0->1, RETURNS TABLE(success, message)
- `edoc.fn_room_schedule_reject(p_id, p_approved_staff_id, p_reason)` — approved 0->-1, RETURNS TABLE(success, message)

Staff Management:
- `edoc.fn_room_schedule_get_staff(p_room_schedule_id)` — List with staff names/positions JOINed
- `edoc.fn_room_schedule_assign_staff(p_room_schedule_id, p_staff_ids INT[], p_user_type)` — Bulk assign, ON CONFLICT DO NOTHING
- `edoc.fn_room_schedule_remove_staff(p_room_schedule_id, p_staff_id)` — DELETE

Voting (Bieu quyet):
- `edoc.fn_vote_question_get_list(p_room_schedule_id)` — Questions with answers
- `edoc.fn_vote_question_create(p_room_schedule_id, p_name, p_question_type, p_duration, p_order_no)` — RETURNS TABLE(success, message, id)
- `edoc.fn_vote_answer_create(p_question_id, p_room_schedule_id, p_name, p_order_no, p_is_other)` — RETURNS TABLE(success, message, id)
- `edoc.fn_vote_cast(p_question_id, p_answer_id, p_staff_id, p_other_text)` — INSERT vote, ON CONFLICT update for re-vote (single choice), RETURNS TABLE(success, message)
- `edoc.fn_vote_question_start(p_question_id)` — status 0->1, set start_time=NOW()
- `edoc.fn_vote_question_stop(p_question_id)` — status 1->2, set stop_time=NOW()
- `edoc.fn_vote_get_results(p_question_id)` — Aggregate: answer_id, answer_name, vote_count, voter names

Statistics:
- `edoc.fn_room_schedule_stats(p_unit_id, p_year)` — Monthly count, by room, by meeting_type — 3 result sets or 1 combined with grouping

Total: ~28 stored functions.
  </action>
  <verify>
    <automated>cd e_office_app_new && docker exec -i qlvb-postgres psql -U postgres -d qlvb_dev -f /dev/stdin < database/migrations/018_sprint13_meetings.sql 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `e_office_app_new/database/migrations/018_sprint13_meetings.sql` exists
    - Contains `CREATE TABLE edoc.rooms`
    - Contains `CREATE TABLE edoc.meeting_types`
    - Contains `CREATE TABLE edoc.room_schedules`
    - Contains `CREATE TABLE edoc.room_schedule_staff`
    - Contains `CREATE TABLE edoc.room_schedule_questions`
    - Contains `CREATE TABLE edoc.room_schedule_answers`
    - Contains `CREATE TABLE edoc.room_schedule_votes`
    - Contains `edoc.fn_room_get_list`
    - Contains `edoc.fn_room_schedule_get_list`
    - Contains `edoc.fn_room_schedule_approve`
    - Contains `edoc.fn_vote_cast`
    - Contains `edoc.fn_vote_get_results`
    - Contains `edoc.fn_room_schedule_stats`
    - All column names snake_case per D-00b
  </acceptance_criteria>
  <done>8 tables + ~28 stored functions for rooms, meeting types, room schedules (CRUD + approve/reject), staff assignment, voting (question/answer/cast/results), and statistics. Migration runs without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Database | All data access through stored functions with parameter binding |
| Borrow approval | Status transitions must validate current state |
| Vote integrity | One vote per staff per question (enforced by unique constraint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | esto.fn_borrow_request_approve | mitigate | Validate status=0 before allowing transition to 1 |
| T-05-02 | Tampering | edoc.fn_room_schedule_approve | mitigate | Validate approved=0 before allowing transition to 1 |
| T-05-03 | Tampering | edoc.fn_vote_cast | mitigate | UNIQUE constraint on (question_id, staff_id) prevents double voting |
| T-05-04 | Info Disclosure | esto.fn_record_get_list | mitigate | Filter by p_unit_id on all list queries |
| T-05-05 | Elevation | edoc.fn_room_schedule_delete | mitigate | Only allow delete when approved=0 (not yet approved) |
</threat_model>

<verification>
All 3 migration files execute without errors on PostgreSQL 16. Verify table counts and function counts per schema.
</verification>

<success_criteria>
- 016_sprint11_archive_storage.sql: 5 tables, ~20 functions in esto schema
- 017_sprint12_documents_contracts.sql: 5 tables, ~18 functions in iso/cont schemas
- 018_sprint13_meetings.sql: 8 tables, ~28 functions in edoc schema
- Total: 18 new tables, ~66 stored functions
- All migrations run clean on qlvb_dev database
</success_criteria>

<output>
After completion, create `.planning/phases/05-kho-l-u-tr-t-i-li-u-h-p/05-01-SUMMARY.md`
</output>
