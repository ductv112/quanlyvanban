---
phase: 05-kho-l-u-tr-t-i-li-u-h-p
plan: 06
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/cuoc-hop/page.tsx
  - e_office_app_new/frontend/src/app/(main)/cuoc-hop/[id]/page.tsx
  - e_office_app_new/frontend/src/app/(main)/cuoc-hop/thong-ke/page.tsx
  - e_office_app_new/frontend/src/components/layout/MainLayout.tsx
  - e_office_app_new/frontend/src/app/globals.css
autonomous: false
requirements: [HOP-01, HOP-02, HOP-03, HOP-04]

must_haves:
  truths:
    - "Admin can manage rooms and meeting types"
    - "User can create/view/filter meetings with calendar view"
    - "Meeting detail page shows staff, attachments, and voting"
    - "Realtime voting works via Socket.IO"
    - "Meeting statistics show charts via @ant-design/charts"
    - "Sidebar nav includes all Phase 5 modules"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/cuoc-hop/page.tsx"
      provides: "Meeting list + room/type management"
      min_lines: 500
    - path: "e_office_app_new/frontend/src/app/(main)/cuoc-hop/[id]/page.tsx"
      provides: "Meeting detail with tabs (info, staff, attachments, voting)"
      min_lines: 600
    - path: "e_office_app_new/frontend/src/app/(main)/cuoc-hop/thong-ke/page.tsx"
      provides: "Meeting statistics with charts"
      min_lines: 200
  key_links:
    - from: "cuoc-hop/page.tsx"
      to: "/api/cuoc-hop"
      via: "api.get/api.post"
      pattern: "api\\.(get|post|put|delete|patch).*cuoc-hop"
    - from: "cuoc-hop/[id]/page.tsx"
      to: "Socket.IO"
      via: "socket.on('vote_update')"
      pattern: "socket\\.on.*vote"
    - from: "MainLayout.tsx"
      to: "sidebar menuItems"
      via: "menuItems array"
      pattern: "kho-luu-tru|tai-lieu|hop-dong|cuoc-hop"
---

<objective>
Create frontend pages for Meeting module (list + detail + statistics with charts), update sidebar navigation with all Phase 5 modules, and add a human verification checkpoint.

Purpose: Complete meeting functionality with realtime voting and statistics charts. Wire all Phase 5 into navigation.
Output: 3 meeting pages, updated MainLayout, updated globals.css, checkpoint verification
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-kho-l-u-tr-t-i-li-u-h-p/05-03-SUMMARY.md

<interfaces>
From backend routes (Plan 03):
```
Meeting API (/api/cuoc-hop):
GET    /phong-hop             — room list
POST   /phong-hop             — create room
PUT    /phong-hop/:id         — update room
DELETE /phong-hop/:id         — delete room
GET    /loai-cuoc-hop         — meeting type list
POST   /loai-cuoc-hop         — create type
PUT    /loai-cuoc-hop/:id     — update type
DELETE /loai-cuoc-hop/:id     — delete type
GET    /thong-ke              — meeting stats (query: year)
GET    /                      — schedule list (query: room_id, status, from_date, to_date, keyword, page, page_size)
GET    /:id                   — schedule detail
POST   /                      — create schedule
PUT    /:id                   — update schedule
DELETE /:id                   — delete schedule
PATCH  /:id/approve           — approve
PATCH  /:id/reject            — reject (body: rejection_reason)
GET    /:id/thanh-vien        — get staff list
POST   /:id/thanh-vien        — assign staff (body: staff_ids, user_type)
DELETE /:id/thanh-vien/:staffId — remove staff
POST   /:id/tai-lieu          — upload attachment
GET    /:id/bieu-quyet        — get vote questions
POST   /:id/bieu-quyet/cau-hoi — create question
POST   /:id/bieu-quyet/cau-hoi/:qId/dap-an — create answer
PATCH  /:id/bieu-quyet/cau-hoi/:qId/bat-dau — start voting
PATCH  /:id/bieu-quyet/cau-hoi/:qId/ket-thuc — stop voting
POST   /:id/bieu-quyet/cau-hoi/:qId/vote — cast vote (body: answer_id, other_text)
GET    /:id/bieu-quyet/cau-hoi/:qId/ket-qua — get results
```

Socket.IO events (from Plan 03):
- `vote_update` — { room_schedule_id, question_id }
- `vote_status_change` — { question_id, status }

From e_office_app_new/frontend/src/lib/socket.ts:
```typescript
import { io, Socket } from 'socket.io-client';
// socket instance export
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Meeting list page + room/type management + create/approve flow</name>
  <files>e_office_app_new/frontend/src/app/(main)/cuoc-hop/page.tsx</files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx
    - e_office_app_new/frontend/src/app/(main)/lich/co-quan/page.tsx
    - e_office_app_new/frontend/src/lib/api.ts
    - e_office_app_new/frontend/src/lib/socket.ts
    - e_office_app_new/frontend/src/app/globals.css
    - e_office_app_new/frontend/src/config/constants.ts
  </read_first>
  <action>
Create `cuoc-hop/page.tsx` for meeting management. Per D-07 + D-08.

**Layout:**
- Page header: "Cuoc hop" with buttons: "Dang ky cuoc hop", "Phong hop" (opens room management), "Loai cuoc hop" (opens type management)
- Filter row: room_id (Select from rooms), approved status (Select: Tat ca/Chua duyet/Da duyet/Tu choi), from_date/to_date (DatePicker.RangePicker), keyword
- Meeting table

**Room Management Drawer (width: 600):**
- Table of rooms: code, name, location, sort_order, note, show_in_calendar
- Add/edit form inline or sub-drawer
- Fetch: `api.get('/cuoc-hop/phong-hop')`

**Meeting Type Management Drawer (width: 600):**
- Table of types: name, description, sort_order
- Fetch: `api.get('/cuoc-hop/loai-cuoc-hop')`

**Meeting Table:**
- Columns: STT, Ten cuoc hop (name), Phong hop (room_name), Loai (meeting_type_name), Ngay hop (start_date), Gio (start_time - end_time), Chu toa (master_name), Trang thai duyet (approved Tag), Trang thai hop (meeting_status Tag), Thao tac
- Approved Tags: { 0: 'Chua duyet' (default), 1: 'Da duyet' (green), [-1]: 'Tu choi' (red) }
- Meeting Status Tags: { 0: 'Chua hop' (default), 1: 'Dang hop' (processing), 2: 'Da hop' (success), 3: 'Huy' (error) }
- Actions: Dropdown with Xem chi tiet (navigate to /cuoc-hop/[id]), Sua, Xoa (only if approved=0), Duyet (PATCH approve), Tu choi (Modal with rejection_reason input)

**Create/Edit Meeting Drawer (width: 720):**
- Fields: name (required), room_id (Select from rooms), meeting_type_id (Select from types), content (TextArea), start_date (DatePicker required), end_date (DatePicker), start_time (TimePicker format HH:mm), end_time (TimePicker), master_id (Select staff from directory API or simple input), secretary_id, online_link, component (thanh phan tham du)
- 2-column layout with Row/Col
- rootClassName="drawer-gradient"

**Click row:** Navigate to `/cuoc-hop/${record.id}` for detail page.

Fetch list: `api.get('/cuoc-hop', { params: { room_id, status: approved, from_date, to_date, keyword, page, page_size } })`
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `e_office_app_new/frontend/src/app/(main)/cuoc-hop/page.tsx` exists
    - Contains `'use client'`
    - Contains `api.get('/cuoc-hop'` for schedule list
    - Contains `api.get('/cuoc-hop/phong-hop'` for rooms
    - Contains `api.get('/cuoc-hop/loai-cuoc-hop'` for types
    - Contains `api.post('/cuoc-hop'` for creating schedule
    - Contains `api.patch` for approve/reject
    - Contains approved Tag and meeting_status Tag
    - Contains `<Drawer` for create/edit + room management + type management
    - Contains Vietnamese labels
    - Next.js build succeeds
  </acceptance_criteria>
  <done>Meeting list page with filter, room/type management drawers, create/edit meeting drawer, approve/reject actions. Vietnamese UI. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Meeting detail page with tabs (info, staff, attachments, voting) + stats page + sidebar + CSS</name>
  <files>
    e_office_app_new/frontend/src/app/(main)/cuoc-hop/[id]/page.tsx,
    e_office_app_new/frontend/src/app/(main)/cuoc-hop/thong-ke/page.tsx,
    e_office_app_new/frontend/src/components/layout/MainLayout.tsx,
    e_office_app_new/frontend/src/app/globals.css
  </files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx
    - e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/bao-cao/page.tsx
    - e_office_app_new/frontend/src/components/layout/MainLayout.tsx
    - e_office_app_new/frontend/src/app/globals.css
    - e_office_app_new/frontend/src/lib/socket.ts
    - e_office_app_new/frontend/src/config/constants.ts
    - e_office_app_new/frontend/src/config/theme.ts
  </read_first>
  <action>
**cuoc-hop/[id]/page.tsx — Meeting Detail (per D-08 + D-09):**

Use tabs pattern similar to ho-so-cong-viec/[id]/page.tsx.

Fetch detail: `api.get(/cuoc-hop/${id})`

**Tabs:**

1. **Thong tin chung** — Meeting info display:
   - Descriptions component showing: name, room_name, meeting_type_name, start_date, end_date, start_time, end_time, master_name, secretary_name, content, component, approved status, meeting_status, online_link
   - Action buttons in header: Duyet, Tu choi (if approved=0), Bat dau hop (meeting_status 0->1), Ket thuc hop (1->2)

2. **Thanh vien** — Staff management:
   - Table: staff_name, position_name, department_name, user_type Tag (Thanh vien/Chu toa/Thu ky), attendance checkbox, attendance_note
   - Button "Them thanh vien" opens Transfer or Modal with staff selection (fetch from `/api/danh-ba`)
   - Assign: `api.post(/cuoc-hop/${id}/thanh-vien, { staff_ids, user_type })`
   - Remove: `api.delete(/cuoc-hop/${id}/thanh-vien/${staffId})`

3. **Tai lieu** — Attachments:
   - File list: file_name, file_size, created_date, download link, delete button
   - Upload button using Upload.Dragger: `api.post(/cuoc-hop/${id}/tai-lieu, formData)`

4. **Bieu quyet** — Voting (per D-09, realtime via Socket.IO):
   - List of questions with status badges (Chua bat dau/Dang bieu quyet/Ket thuc)
   - Button "Them cau hoi" opens form: name, question_type (Radio: Don le/Nhieu lua chon), duration (seconds)
   - After creating question, add answers: name, order_no, is_other
   - **Chu toa actions:** Bat dau bieu quyet (PATCH .../bat-dau), Ket thuc bieu quyet (PATCH .../ket-thuc)
   - **Thanh vien voting:** When question status=1 (Dang bieu quyet), show Radio.Group (single) or Checkbox.Group (multi) with answers, submit button calls `api.post(.../vote, { answer_id })`
   - **Results display:** After question status=2, fetch `api.get(.../ket-qua)`, show results: answer_name, vote_count, voter_names. Use Progress bar or simple bars to visualize.
   - **Socket.IO realtime:** Connect to socket, listen for `vote_update` and `vote_status_change` events:
     ```typescript
     import { socket } from '@/lib/socket';
     useEffect(() => {
       socket.on('vote_update', (data) => {
         if (data.room_schedule_id === Number(id)) { fetchVoteQuestions(); }
       });
       socket.on('vote_status_change', (data) => {
         if (data.question_id) { fetchVoteQuestions(); }
       });
       return () => { socket.off('vote_update'); socket.off('vote_status_change'); };
     }, [id]);
     ```

**cuoc-hop/thong-ke/page.tsx — Meeting Statistics (per D-10):**

Install @ant-design/charts if not in package.json (executor should check and `npm install @ant-design/charts` if needed).

Fetch: `api.get('/cuoc-hop/thong-ke', { params: { year } })`

Charts using `@ant-design/charts`:
1. **Bar chart** — Cuoc hop theo thang: x=month (Thang 1..12), y=meeting_count
2. **Pie chart** — Cuoc hop theo phong: segment=room_name, value=meeting_count
3. **Bar chart** — Cuoc hop theo loai: x=meeting_type_name, y=meeting_count

Layout: Year selector (Select or DatePicker picker="year"), then 3 charts in a grid (Row/Col).

Use Ant Design Card for each chart container. Colors from theme: primary #1B3A5C, accent #0891B2.

```tsx
import { Column, Pie } from '@ant-design/charts';
// Column for bar charts, Pie for pie chart
```

**MainLayout.tsx — Add Phase 5 menu items:**

Read current menuItems array. Add new menu group items after existing items. Use `getItem()` helper pattern from existing code.

Add these menu items:
```typescript
// Kho luu tru group
getItem('Kho luu tru', 'kho-luu-tru', <DatabaseOutlined />, [
  getItem('Danh muc kho/phong', '/kho-luu-tru'),
  getItem('Muon/tra ho so', '/kho-luu-tru/muon-tra'),
]),
// Tai lieu
getItem('Tai lieu', '/tai-lieu', <FileTextOutlined />),
// Hop dong
getItem('Hop dong', '/hop-dong', <AuditOutlined />),
// Cuoc hop group
getItem('Cuoc hop', 'cuoc-hop', <TeamOutlined />, [
  getItem('Danh sach cuoc hop', '/cuoc-hop'),
  getItem('Thong ke', '/cuoc-hop/thong-ke'),
]),
```

Import new icons from `@ant-design/icons`: `DatabaseOutlined`, `FileTextOutlined`, `AuditOutlined`, `TeamOutlined`.

**globals.css — Add any needed CSS classes:**
Only add if needed. Check if existing `.page-header`, `.page-card`, `.filter-row`, `.drawer-gradient`, `.detail-header` are sufficient. If meeting-specific styles needed (e.g., vote progress bars, meeting status indicators), add them following existing naming patterns.
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `e_office_app_new/frontend/src/app/(main)/cuoc-hop/[id]/page.tsx` exists
    - Contains `api.get` calls with `/cuoc-hop/${id}`
    - Contains `socket.on('vote_update'` for realtime voting
    - Contains `api.post` for vote casting
    - Contains `<Tabs` for meeting detail sections
    - Contains Vietnamese labels: Thong tin chung, Thanh vien, Tai lieu, Bieu quyet
    - `e_office_app_new/frontend/src/app/(main)/cuoc-hop/thong-ke/page.tsx` exists
    - Contains import from `@ant-design/charts` (Column or Pie)
    - Contains `api.get('/cuoc-hop/thong-ke'`
    - `MainLayout.tsx` contains `kho-luu-tru` in menuItems
    - `MainLayout.tsx` contains `tai-lieu` in menuItems
    - `MainLayout.tsx` contains `hop-dong` in menuItems
    - `MainLayout.tsx` contains `cuoc-hop` in menuItems
    - Next.js build succeeds
  </acceptance_criteria>
  <done>Meeting detail with 4 tabs (info, staff, attachments, voting with Socket.IO realtime), statistics page with 3 charts (@ant-design/charts), sidebar updated with all Phase 5 modules. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Phase 5 UI and functionality</name>
  <files>none</files>
  <action>
Human verifies all Phase 5 pages render correctly and features work. Complete Phase 5 includes: Kho luu tru (warehouse/fond tree, records, borrow/return), Tai lieu (category tree, document upload), Hop dong (types, CRUD, attachments), Cuoc hop (rooms, types, schedule CRUD, approve, staff, attachments, realtime voting, statistics charts). All wired into sidebar navigation.

Steps:
1. Start backend: `cd e_office_app_new/backend && npm run dev`
2. Start frontend: `cd e_office_app_new/frontend && npm run dev`
3. Open http://localhost:3000, login, check sidebar has new menu items
4. Navigate to each new module and verify pages render
5. Test key flows: create warehouse, create record, create borrow request, upload document, create contract, create meeting, vote
  </action>
  <verify>
    <automated>echo "Manual verification checkpoint — human must confirm"</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar shows: Kho luu tru (sub-items), Tai lieu, Hop dong, Cuoc hop (sub-items)
    - Kho luu tru page shows tree panel + record table
    - Muon/tra page shows borrow request list
    - Tai lieu page shows category tree + document list
    - Hop dong page shows contract type management + contract list
    - Cuoc hop page shows meeting list with filter
    - Cuoc hop detail page shows 4 tabs
    - Thong ke page shows charts
  </acceptance_criteria>
  <done>Human confirms all Phase 5 pages render correctly and key flows work. Type "approved" or describe issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend Socket.IO -> Backend | JWT-authenticated socket connection |
| Vote UI -> Vote API | Staff_id from JWT, not from UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Spoofing | Vote casting UI | mitigate | staff_id extracted from JWT on backend, not from frontend request |
| T-05-19 | Info Disclosure | Meeting detail | accept | Any authenticated user can view meetings for their unit; unit_id filter at API |
| T-05-20 | Denial of Service | Socket.IO vote events | accept | Events only emitted to meeting participants; authenticated socket |
</threat_model>

<verification>
All pages build successfully. Sidebar shows all Phase 5 modules. Charts render with @ant-design/charts. Socket.IO voting updates in realtime.
</verification>

<success_criteria>
- cuoc-hop/page.tsx: Meeting list with room/type drawers, filter, create/approve
- cuoc-hop/[id]/page.tsx: Detail with 4 tabs including realtime voting
- cuoc-hop/thong-ke/page.tsx: 3 charts using @ant-design/charts
- MainLayout.tsx: Sidebar includes all Phase 5 modules (4 new menu groups)
- Human checkpoint verifies all pages render correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-kho-l-u-tr-t-i-li-u-h-p/05-06-SUMMARY.md`
</output>
