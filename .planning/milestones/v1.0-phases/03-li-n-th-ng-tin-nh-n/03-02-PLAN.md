---
phase: 03-li-n-th-ng-tin-nh-n
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - e_office_app_new/backend/src/repositories/inter-incoming.repository.ts
  - e_office_app_new/backend/src/routes/inter-incoming.ts
  - e_office_app_new/backend/src/routes/incoming-doc.ts
  - e_office_app_new/backend/src/server.ts
autonomous: true
requirements:
  - VBLT-01
  - VBLT-02
  - VBLT-03
must_haves:
  truths:
    - "GET /api/van-ban-lien-thong returns paginated list of inter-incoming docs"
    - "GET /api/van-ban-lien-thong/:id returns detail of a single inter-incoming doc"
    - "POST /api/van-ban-den/:id/giao-viec creates HSCV and links the incoming doc"
    - "POST /api/van-ban-den/:id/nhan-ban-giao updates incoming doc handover status"
    - "POST /api/van-ban-den/:id/chuyen-lai requires reason and returns success"
    - "POST /api/van-ban-den/:id/huy-duyet cancels approval on incoming doc"
  artifacts:
    - path: "e_office_app_new/backend/src/repositories/inter-incoming.repository.ts"
      provides: "Inter-incoming doc data access"
      exports: ["interIncomingRepository"]
    - path: "e_office_app_new/backend/src/routes/inter-incoming.ts"
      provides: "Inter-incoming doc API routes"
      exports: ["default (Router)"]
  key_links:
    - from: "inter-incoming.repository.ts"
      to: "edoc.fn_inter_incoming_get_list"
      via: "callFunction"
      pattern: "callFunction.*fn_inter_incoming"
    - from: "server.ts"
      to: "inter-incoming.ts"
      via: "app.use('/api/van-ban-lien-thong')"
      pattern: "van-ban-lien-thong"
---

<objective>
Create backend API layer for VB lien thong (inter-incoming docs) and incoming doc action endpoints (giao viec, nhan ban giao, chuyen lai, huy duyet).

Purpose: Provides REST endpoints for the frontend VB lien thong pages and incoming doc toolbar actions.
Output: Repository + route files for inter-incoming, extended incoming-doc routes, server.ts mount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-li-n-th-ng-tin-nh-n/03-CONTEXT.md

<interfaces>
From e_office_app_new/backend/src/lib/db/query.ts:
```typescript
export async function callFunction<T extends QueryResultRow>(functionName: string, params: unknown[]): Promise<T[]>;
export async function callFunctionOne<T extends QueryResultRow>(functionName: string, params: unknown[]): Promise<T | null>;
```

From e_office_app_new/backend/src/lib/error-handler.ts:
```typescript
export function handleDbError(error: unknown, res: Response): void;
```

From e_office_app_new/backend/src/middleware/auth.ts:
```typescript
export interface AuthRequest extends Request { user: { staffId: number; unitId: number; departmentId: number; roles: string[] } }
export function authenticate(req: Request, res: Response, next: NextFunction): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inter-incoming repository + routes</name>
  <files>
    e_office_app_new/backend/src/repositories/inter-incoming.repository.ts,
    e_office_app_new/backend/src/routes/inter-incoming.ts,
    e_office_app_new/backend/src/server.ts
  </files>
  <read_first>
    - e_office_app_new/backend/src/repositories/incoming-doc.repository.ts (pattern for repository structure, row types, method signatures)
    - e_office_app_new/backend/src/routes/incoming-doc.ts (pattern for route handlers, AuthRequest usage, error handling)
    - e_office_app_new/backend/src/server.ts (where to mount new routes)
    - e_office_app_new/backend/src/lib/error-handler.ts (handleDbError import)
  </read_first>
  <action>
**Repository: `inter-incoming.repository.ts`**
Define row interfaces:
- `InterIncomingListRow`: id, unit_id, received_date, notation, document_code, abstract, publish_unit, publish_date, signer, sign_date, expired_date, doc_type_id, status, source_system, external_doc_id, created_by, created_at, total_count
- `InterIncomingDetailRow`: same fields without total_count

Export `interIncomingRepository` object with methods:
- `getList(unitId, filters)` — calls `edoc.fn_inter_incoming_get_list` with params [unitId, keyword, status, fromDate, toDate, page, pageSize]
- `getById(id)` — calls `edoc.fn_inter_incoming_get_by_id` with [id]

**Routes: `inter-incoming.ts`**
Create Router with:
- `GET /` — list with query params: keyword, status, from_date, to_date, page, page_size. Extract unitId from AuthRequest.user. Return `{ success: true, data: rows, pagination: { total, page, pageSize } }`.
- `GET /:id` — detail. Return `{ success: true, data: row }` or 404 `{ success: false, message: 'Khong tim thay van ban lien thong' }`.

Both routes use try/catch with handleDbError.

**server.ts update:**
Add import: `import interIncomingRoutes from './routes/inter-incoming.js';`
Add mount: `app.use('/api/van-ban-lien-thong', authenticate, interIncomingRoutes);` after the van-ban-du-thao line.
  </action>
  <verify>
    <automated>cd D:/ProjectAI/quanlyvanban/e_office_app_new/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - inter-incoming.repository.ts exports interIncomingRepository with getList and getById methods
    - inter-incoming.ts has GET / and GET /:id routes
    - server.ts imports and mounts interIncomingRoutes at /api/van-ban-lien-thong with authenticate middleware
    - All files use .js extension in imports (ESM requirement)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>GET /api/van-ban-lien-thong and GET /api/van-ban-lien-thong/:id endpoints available behind auth</done>
</task>

<task type="auto">
  <name>Task 2: Incoming doc action endpoints — giao viec, nhan ban giao, chuyen lai, huy duyet</name>
  <files>e_office_app_new/backend/src/routes/incoming-doc.ts</files>
  <read_first>
    - e_office_app_new/backend/src/routes/incoming-doc.ts (full file — add new routes at end)
    - e_office_app_new/backend/src/repositories/incoming-doc.repository.ts (check existing methods, add new ones if needed)
  </read_first>
  <action>
**Add to `incoming-doc.repository.ts`** (4 new methods):
- `createHandlingDocFromDoc(docId, docType, name, startDate, endDate, curatorIds, note, createdBy)` — calls `edoc.fn_handling_doc_create_from_doc`
- `handover(docId, staffId)` — calls `edoc.fn_incoming_doc_handover`
- `returnDoc(docId, returnedBy, reason)` — calls `edoc.fn_incoming_doc_return`
- `cancelApprove(docId, cancelledBy)` — calls `edoc.fn_incoming_doc_cancel_approve`

**Add to `incoming-doc.ts`** route file (per D-04 through D-08), new section at bottom:

```
// ============================================================
// 3.x VB DEN ACTIONS — Giao viec, Nhan ban giao, Chuyen lai, Huy duyet
// ============================================================
```

1. `POST /:id/giao-viec` — Extract { name, start_date, end_date, curator_ids, note } from body. Validate: name required, curator_ids required (non-empty array). Call `incomingDocRepository.createHandlingDocFromDoc(id, 'incoming', name, startDate, endDate, curatorIds, note, staffId)`. Return 201 `{ success: true, data: result, message: 'Giao viec thanh cong' }`.

2. `POST /:id/nhan-ban-giao` — No body needed. Call `incomingDocRepository.handover(id, staffId)`. Return `{ success: true, message: 'Nhan ban giao thanh cong' }`.

3. `POST /:id/chuyen-lai` — Extract { reason } from body. Validate: reason required, min length 10. Call `incomingDocRepository.returnDoc(id, staffId, reason)`. Return `{ success: true, message: 'Chuyen lai van ban thanh cong' }`.

4. `POST /:id/huy-duyet` — No body needed. Call `incomingDocRepository.cancelApprove(id, staffId)`. Return `{ success: true, message: 'Da huy duyet van ban' }`.

All use try/catch with handleDbError. Vietnamese messages without diacritics in API responses (frontend displays diacritics).
  </action>
  <verify>
    <automated>cd D:/ProjectAI/quanlyvanban/e_office_app_new/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - incoming-doc.repository.ts has 4 new methods: createHandlingDocFromDoc, handover, returnDoc, cancelApprove
    - incoming-doc.ts has 4 new POST routes: /:id/giao-viec, /:id/nhan-ban-giao, /:id/chuyen-lai, /:id/huy-duyet
    - POST /:id/chuyen-lai validates reason is present and min 10 chars
    - POST /:id/giao-viec validates name and curator_ids required
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All 4 incoming doc action endpoints functional behind auth, validation in place</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | All incoming doc actions require authenticated user (authenticate middleware) |
| API -> DB | Staff_id from JWT, not from request body — prevents impersonation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Spoofing | giao-viec endpoint | mitigate | staffId extracted from JWT token, not from request body |
| T-03-05 | Tampering | chuyen-lai endpoint | mitigate | Validate reason min length 10 chars server-side |
| T-03-06 | Elevation | huy-duyet endpoint | accept | Currently any authenticated user can cancel — role check deferred to Phase 7 (UI-02 sidebar RBAC) |
| T-03-07 | Info Disclosure | van-ban-lien-thong list | mitigate | Filter by unitId from JWT — users only see their unit's documents |
</threat_model>

<verification>
TypeScript compiles. All routes mountable. Manual curl test:
```bash
curl -H "Authorization: Bearer $TOKEN" http://localhost:4000/api/van-ban-lien-thong
```
</verification>

<success_criteria>
- 6 new API endpoints created and mounted
- All use authenticate middleware
- Input validation on giao-viec and chuyen-lai
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-li-n-th-ng-tin-nh-n/03-02-SUMMARY.md`
</output>
