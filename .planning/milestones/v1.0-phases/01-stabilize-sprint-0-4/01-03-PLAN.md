---
phase: 01-stabilize-sprint-0-4
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
  - 01-02-PLAN.md
files_modified:
  - e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx
  - e_office_app_new/frontend/src/components/layout/MainLayout.tsx
autonomous: false
requirements:
  - STAB-01
  - STAB-03

must_haves:
  truths:
    - "User can login successfully and land on the dashboard"
    - "User can navigate to /quan-tri/don-vi and see the department tree render without errors"
    - "User can navigate to /van-ban-den and see a paginated table of incoming documents"
    - "User can open the Add drawer in /van-ban-den and save a new incoming document"
    - "User can navigate to /van-ban-di and see a paginated table of outgoing documents"
    - "User can navigate to /van-ban-du-thao and see a paginated table of draft documents"
    - "All visible UI text is in Vietnamese with proper diacritics"
    - "No console errors on any of the golden path pages"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx"
      provides: "Incoming doc list with correct pagination"
    - path: "e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx"
      provides: "Outgoing doc list with correct pagination"
  key_links:
    - from: "van-ban-den/page.tsx"
      to: "/api/van-ban-den"
      via: "api.get('/van-ban-den', { params })"
      pattern: "api.get.*van-ban-den"
    - from: "van-ban-di/page.tsx"
      to: "/api/van-ban-di"
      via: "api.get('/van-ban-di', { params })"
      pattern: "api.get.*van-ban-di"
---

<objective>
Walk the golden path (Login → Dashboard → Admin modules → VB đến → VB đi → VB dự thảo) and fix
any visible bugs found. Then hand off to the human to confirm the path is working end-to-end.

Purpose: Ensure Sprint 0-4 is demo-ready. Any bug that a user would encounter during a demo
must be identified and fixed before handing off (per D-03 and D-04).

This plan executes in Wave 2 — after Plans 01 and 02 have applied their refactoring.
The main job here is: (1) fix known bugs found during golden path testing, and (2) get human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-stabilize-sprint-0-4/01-01-SUMMARY.md
@.planning/phases/01-stabilize-sprint-0-4/01-02-SUMMARY.md

<known_bugs>
The following issues were identified during codebase analysis and must be checked during golden path:

1. van-ban-den/page.tsx line 171: Backslash path in API call
   ```typescript
   await api.patch('\van-ban-den\danh-dau-da-doc', ...)
   ```
   Should be forward slashes: `/van-ban-den/danh-dau-da-doc`
   (Windows path separator accidentally used — will fail as HTTP URL)

2. Simplified handleDbError in doc routes (fixed in Plan 02 — verify it took effect):
   - incoming-doc.ts, drafting-doc.ts, outgoing-doc.ts now use shared error-handler
   - Verify: creating a duplicate VB đến should show Vietnamese message, not raw error.message

3. MainLayout.tsx notification bell (line ~293):
   - Has `TODO: notification drawer` comment — check if this causes any visible JS error
   - Should show empty/disabled state gracefully, not crash

4. van-ban-di/page.tsx DepartmentNode interface:
   - File defines its own DepartmentNode interface inline for tree usage in forms
   - Check if this causes any type errors or rendering issues (not critical, just verify it works)
</known_bugs>

<css_convention>
Per project convention (feedback_css_first_layout.md):
- Layout/structural styles MUST use CSS classes from globals.css
- NO inline styles for layout properties
- If fixing layout bugs, add to globals.css, do not use style={{}}
</css_convention>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix known code bugs before golden path walk</name>
  <files>
    e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx
    e_office_app_new/frontend/src/components/layout/MainLayout.tsx
  </files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx (full file — search for backslash paths and other issues)
    - e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx (full file — check DepartmentNode usage)
    - e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx (full file — check for similar backslash paths)
    - e_office_app_new/frontend/src/components/layout/MainLayout.tsx (lines 280-310 — notification bell area)
    - .planning/phases/01-stabilize-sprint-0-4/01-01-SUMMARY.md (confirm tree refactor completed)
    - .planning/phases/01-stabilize-sprint-0-4/01-02-SUMMARY.md (confirm error-handler refactor completed)
  </read_first>
  <action>
    Apply the following concrete bug fixes:

    **Fix 1 — van-ban-den/page.tsx backslash path (CRITICAL):**
    Find the line: `await api.patch('\van-ban-den\danh-dau-da-doc', { doc_ids: selectedRowKeys });`
    Replace with: `await api.patch('/van-ban-den/danh-dau-da-doc', { doc_ids: selectedRowKeys });`
    Also scan the entire file for any other backslash-separated paths in api.get/post/put/patch/delete calls.
    Repeat the same scan for van-ban-di/page.tsx and van-ban-du-thao/page.tsx.

    **Fix 2 — MainLayout.tsx notification bell:**
    Read lines ~285-310. If the notification bell area contains TODO comments that cause runtime
    errors (e.g., referencing undefined variables, calling undefined functions), fix them:
    - If the bell icon has an onClick that references an undefined handler, add a no-op: `onClick={() => {}}`
    - If there is a badge count from an API call that fails, wrap in try/catch that silently ignores errors
    - Do NOT implement notification functionality — just ensure no visible crash or console error

    **Fix 3 — Scan for Vietnamese text without diacritics:**
    In van-ban-den/page.tsx, van-ban-di/page.tsx, van-ban-du-thao/page.tsx:
    Search for common non-diacritic Vietnamese patterns in user-visible strings (button labels, column headers,
    drawer titles, empty state messages). Fix any found (e.g., "Them moi" → "Thêm mới",
    "Van ban den" → "Văn bản đến"). Skip backend error messages that are already correct.

    **Fix 4 — Apply all fixes from Plan 01 and 02 are verified:**
    Run `npx tsc --noEmit` from both frontend and backend directories.
    If there are compilation errors introduced by Plan 01/02 edits, fix them now before proceeding.

    Constraint per D-04: Only fix visible bugs. Do NOT refactor logic, change layouts, or add features.
  </action>
  <verify>
    <automated>cd D:/ProjectAI/quanlyvanban/e_office_app_new/frontend && npx tsc --noEmit 2>&1 | head -20 && cd D:/ProjectAI/quanlyvanban/e_office_app_new/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\\\\van-ban" e_office_app_new/frontend/src/` returns NO matches (no more backslash paths in API calls)
    - `grep -rn "\\\\api" e_office_app_new/frontend/src/` returns NO matches
    - TypeScript compilation is clean in BOTH frontend and backend (0 errors)
    - MainLayout.tsx compiles without errors
  </acceptance_criteria>
  <done>
    All known code bugs fixed. TypeScript builds clean for both frontend and backend.
    Pages are ready for golden path walk.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01, 02, and 03 Task 1 are complete:
    - Frontend tree utilities extracted to shared lib (Plan 01)
    - Backend handleDbError extracted to shared lib, doc route error handling upgraded (Plan 02)
    - Backslash path bug fixed in van-ban-den, Vietnamese text corrected (Plan 03 Task 1)
  </what-built>
  <how-to-verify>
    Walk the golden path manually. For each step, confirm there are no visible errors,
    no console errors (open browser DevTools), and all text is in Vietnamese with diacritics.

    **Golden Path Steps:**

    Start backend: `cd e_office_app_new/backend && npm run dev`
    Start frontend: `cd e_office_app_new/frontend && npm run dev`
    Open: http://localhost:3000

    **Step 1 — Login:**
    - Navigate to http://localhost:3000/login
    - Login with valid credentials
    - Expected: Redirected to /dashboard, sidebar visible, no JS errors

    **Step 2 — Dashboard:**
    - Verify dashboard loads with stat cards
    - Expected: 4 KPI cards visible (may show 0), no error banners

    **Step 3 — Admin: Đơn vị (Department tree):**
    - Click sidebar → Quản trị → Đơn vị
    - Expected: Tree renders in left panel, table on right, no "Cannot read properties of undefined"
    - Click a tree node — table should filter to that unit's departments

    **Step 4 — Admin: Người dùng (Staff):**
    - Click sidebar → Quản trị → Người dùng
    - Expected: Staff table loads with pagination, no tree errors
    - Click "Thêm mới" — drawer opens correctly with form fields

    **Step 5 — Văn bản đến (Incoming docs):**
    - Click sidebar → Văn bản đến
    - Expected: Table loads with pagination showing correct count
    - Click "Thêm mới" — drawer opens with all required fields (Sổ VB, Loại VB, Trích yếu, etc.)
    - Fill and submit — should succeed or show Vietnamese validation messages

    **Step 6 — Văn bản đi (Outgoing docs):**
    - Click sidebar → Văn bản đi
    - Expected: Table loads, filters work, pagination correct

    **Step 7 — Văn bản dự thảo (Draft docs):**
    - Click sidebar → Văn bản dự thảo
    - Expected: Table loads correctly

    **What to look for:**
    - Any English text in labels/buttons/columns that should be Vietnamese
    - Any "[object Object]" or "undefined" appearing in the UI
    - Any console errors in browser DevTools (F12)
    - Pagination not working (shows 0 records when there should be records)
    - Tree not rendering (blank left panel)
    - Drawer not opening or crashing on open

    If issues are found, describe them and type "issues: [description]".
    If the path is clean, type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" if golden path is clean, or "issues: [describe what you found]" to continue fixing</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Frontend | User input in form fields (document metadata, filters) |
| Frontend → Backend API | Axios requests with JWT token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Browser DevTools console | accept | Console logs in dev mode are acceptable for an internal admin tool; no sensitive data logged |
| T-03-02 | Tampering | Form submission without validation | accept | Backend stored procedures enforce data integrity; frontend validation is a UX concern only |
| T-03-03 | Elevation of Privilege | Admin pages accessible without RBAC | accept | RBAC deferred to DEEP-01 (next week); authentication is enforced; demo audience is internal |
</threat_model>

<verification>
After checkpoint approved:
1. Backend TypeScript compiles clean: `cd backend && npx tsc --noEmit` exits 0
2. Frontend TypeScript compiles clean: `cd frontend && npx tsc --noEmit` exits 0
3. No backslash paths in API calls: `grep -rn "\\\\van-ban\|\\\\api\|\\\\quan-tri" frontend/src/` returns 0 matches
4. Human has confirmed golden path works end-to-end
</verification>

<success_criteria>
- All golden path steps pass without visible errors
- Both TypeScript builds are clean
- Human has typed "approved" on the checkpoint
- Sprint 0-4 is demo-ready for deadline 2026-04-18/19
</success_criteria>

<output>
After completion, create .planning/phases/01-stabilize-sprint-0-4/01-03-SUMMARY.md
</output>
