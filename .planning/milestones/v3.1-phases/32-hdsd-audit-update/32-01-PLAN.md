---
phase: 32-hdsd-audit-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Audit report exists with per-file diff entries for all 22 HDSD files (20 module + index + full)"
    - "Each file entry lists: button/menu diff, field diff, workflow gap, notification diff, screenshot retake list"
    - "3 GAP critical (VB den 3.6, VB di 3.7, VB di 3.8) explicitly enumerated with exact insertion line numbers"
    - "Audit report is the single source-of-truth that Plan 02-05 will use to update text"
  artifacts:
    - path: ".planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md"
      provides: "Per-file diff analysis + GAP enumeration"
      min_lines: 400
  key_links:
    - from: ".planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md"
      to: "docs/hdsd/HDSD_*.md (20 files)"
      via: "Each HDSD file has a section in audit report listing required changes"
      pattern: "HDSD_(van_ban|ho_so|dashboard|thong_bao|ky_so|quan_tri|dang_nhap|cau_hinh|index|full)"
    - from: ".planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md"
      to: "32-CONTEXT.md GAP 1/2/3"
      via: "Audit explicitly references CONTEXT.md exact text blocks for 3 GAPs"
      pattern: "GAP (1|2|3)"
---

<objective>
Audit all 22 HDSD files (20 module + HDSD_index.md + HDSD_full.md) against current code (after Phase 21 LGSP foundation + Phase 31 fix-gom UI). Produce a single audit report `32-AUDIT-REPORT.md` that enumerates per-file required changes (button/menu diff, field diff, workflow gap, notification, screenshot retake). This report is the source-of-truth for plans 02-05.

Purpose: Without a coherent audit, parallel text-update plans would each have to re-read code separately and diverge. One up-front audit gives all downstream plans the same picture.

Output: `.planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md` with sections per HDSD file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-hdsd-audit-update/32-CONTEXT.md
@CLAUDE.md
@docs/hdsd/HDSD_index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory & open each HDSD file + key code references</name>
  <read_first>
    - .planning/phases/32-hdsd-audit-update/32-CONTEXT.md (canonical refs + 3 GAP exact text)
    - docs/hdsd/HDSD_index.md (file order)
    - docs/hdsd/HDSD_van_ban_den.md (lines 270-330 — mục 3.6 Drawer giao việc)
    - docs/hdsd/HDSD_van_ban_di.md (lines 260-340 — mục 3.7 Modal Ban hành + 3.8 Drawer giao việc)
    - docs/hdsd/HDSD_ho_so_cong_viec.md (full — section "Mới tạo")
    - e_office_app_new/backend/src/routes/incoming-doc.ts lines 880-980 (giao việc VB đến — notifyBell)
    - e_office_app_new/backend/src/routes/outgoing-doc.ts lines 640-730 (Ban hành & Gửi)
    - e_office_app_new/backend/src/routes/outgoing-doc.ts lines 880-940 (giao việc VB đi — KHÔNG có notifyBell, gap)
    - e_office_app_new/database/schema/000_schema_v3.0.sql lines 27800-27960 (SP fn_outgoing_doc_send_to_recipients)
    - e_office_app_new/workers/src/index.ts lines 200-250 (worker LGSP send)
    - e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx lines 160-220 (Drawer Giao việc VB đến)
    - e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx lines 200-360, 490-500
    - e_office_app_new/frontend/src/config/hidden-routes.ts (full — Phase 31 mới)
    - e_office_app_new/frontend/src/components/layout/MainLayout.tsx (sidebar menu)
  </read_first>
  <files>.planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md</files>
  <action>
    Create `.planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md` with this exact structure:

    ```markdown
    # Phase 32 Audit Report — HDSD vs Current Code

    **Audit date:** 2026-05-11
    **Scope:** 22 HDSD files in docs/hdsd/
    **Reference baseline:** Phase 21 LGSP foundation + Phase 31 fix-gom UI

    ## Summary

    - Total files audited: 22
    - Critical GAPs: 3 (enumerated below)
    - Files needing text changes: {count}
    - Files needing screenshot retake: {count}
    - Files clean (no change needed): {count}

    ## Critical GAPs

    ### GAP 1 — HDSD_van_ban_den.md mục 3.6 (Drawer giao việc)
    - File: docs/hdsd/HDSD_van_ban_den.md
    - Current state: Bảng "Thông báo của hệ thống" kết thúc ở line ~296
    - Missing: Subsection "Sau khi giao việc — văn bản xuất hiện ở đâu?"
    - Source of truth for new text: 32-CONTEXT.md `<specifics>` block 1 (lines 188-206)
    - Insertion point: Sau line 296 (sau bảng "Thông báo của hệ thống"), trước "### 3.7. Modal chuyển lại văn bản"

    ### GAP 2 — HDSD_van_ban_di.md mục 3.8 (Drawer giao việc VB đi)
    - File: docs/hdsd/HDSD_van_ban_di.md
    - Current state: Mục 3.8 hiện chỉ tả form + button. KHÔNG có note về gap chuông bell.
    - Missing: Subsection "Sau khi giao việc — văn bản xuất hiện ở đâu?" + note rõ KHÔNG có chuông bell, KHÔNG validate Người phụ trách + Hạn hoàn thành rỗng
    - Note text required:
      ```
      Lưu ý: hiện tại VB đi giao việc CHƯA có chuông thông báo cho người được giao (gap code chưa fix — sẽ bổ sung ở phiên bản sau v3.2+). Form cũng KHÔNG validate `Người phụ trách` rỗng + `Hạn hoàn thành` rỗng — đề nghị nhập đủ trước khi bấm Tạo.
      ```
    - Plus same VB-xuất-hiện-ở-đâu subsection (HSCV → tab "Mới tạo")
    - Insertion point: Cuối mục 3.8 (sau bảng "Thông báo của hệ thống" của 3.8)

    ### GAP 3 — HDSD_van_ban_di.md mục 3.7 (Modal Ban hành & Gửi)
    - File: docs/hdsd/HDSD_van_ban_di.md
    - Current state: Modal mô tả xong ở line ~309 (bảng "Thông báo của hệ thống")
    - Missing: Subsection "Luồng kỹ thuật khi Gửi (3 bước nối tiếp)"
    - Source of truth for new text: 32-CONTEXT.md `<specifics>` block 2 (lines 213-238)
    - Insertion point: Sau bảng "Thông báo của hệ thống" của 3.7 (line ~310), trước "### 3.8. Drawer giao việc"

    ## Per-File Audit

    {For each of 22 files, write a section with format below}

    ### docs/hdsd/HDSD_van_ban_den.md
    - **Button/menu diff:** {list any button names changed, deprecated, or added based on van-ban-den/[id]/page.tsx and list page.tsx}
    - **Field diff:** {list form field changes}
    - **Workflow gap:** GAP 1 (see above)
    - **Notification diff:** {confirm task_assigned bell mentioned, list any missing notifications}
    - **Screenshot retake:** {list of png files that may need retake — yes for Drawer Giao việc to show actual current UI}
    - **Verdict:** Update text (GAP 1) + retake screenshots {N}

    {Continue for all 22 files}
    ```

    Audit instructions per file:
    1. Open file in docs/hdsd/HDSD_<name>.md
    2. Scan section headings (## and ###) and list them
    3. Cross-check button names in HDSD vs current frontend pages (use Grep on frontend/src/app/(main)/<corresponding_dir>/page.tsx)
    4. Cross-check form fields mentioned vs current code
    5. Look at hidden-routes.ts + MainLayout.tsx to see if HDSD mentions any route that's now hidden, or fails to mention new routes
    6. Note whether screenshots referenced exist in docs/hdsd/screenshots/ (use Glob)
    7. Mark verdict: "No change", "Text update only", "Screenshot only", "Both", "GAP 1/2/3 + both"

    Group findings by HDSD file. Be terse — bullet points, max 8 lines per file unless it has a GAP.

    Required minimum content: All 22 files must appear in the audit report (even those marked "No change"). The 3 GAP files (van_ban_den, van_ban_di, ho_so_cong_viec) must have detailed entries (≥ 15 lines each).

    HDSD_full.md and HDSD_index.md treatment:
    - HDSD_full.md: Mark as "Re-merge required after all text updates" (Plan 07 will handle)
    - HDSD_index.md: Mark as "Verify TOC matches updated section headings"
  </action>
  <verify>
    <automated>powershell -Command "if ((Get-Item .planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md).Length -gt 8000) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>
    - .planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md exists
    - File size > 8KB (≈ 400+ lines)
    - All 22 HDSD files appear as section headings in the report
    - 3 GAPs explicitly listed under "## Critical GAPs" with file + line + source-of-truth pointer
    - Each per-file section has "Verdict:" line
  </done>
</task>

</tasks>

<verification>
After this plan completes:
- Run: `grep -c "^### docs/hdsd/HDSD_" .planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md` should return ≥ 22
- Run: `grep -c "^### GAP " .planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md` should return ≥ 3
- Audit report becomes the contract for plans 02-05
</verification>

<success_criteria>
- 32-AUDIT-REPORT.md is complete and self-contained
- Downstream plans 02-05 can be executed by reading only the audit report + CONTEXT.md
- The 3 GAP entries explicitly cite 32-CONTEXT.md `<specifics>` blocks
</success_criteria>

<output>
After completion, create `.planning/phases/32-hdsd-audit-update/32-01-SUMMARY.md` summarizing:
- How many files needed updates vs clean
- Total screenshots flagged for retake
- 3 GAP confirmations
</output>
