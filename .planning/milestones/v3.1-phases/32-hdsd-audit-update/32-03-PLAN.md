---
phase: 32-hdsd-audit-update
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/hdsd/HDSD_ho_so_cong_viec.md
  - docs/hdsd/HDSD_dashboard.md
  - docs/hdsd/HDSD_thong_bao.md
  - docs/hdsd/HDSD_cau_hinh_gui_nhanh.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "HDSD_ho_so_cong_viec.md has note that VB tự xuất hiện ở tab 'Mới tạo' khi được giao việc (from VB đến hoặc VB đi)"
    - "All 4 files (HSCV, Dashboard, Thông báo, Cấu hình gửi nhanh) reflect current UI per audit report"
    - "task_assigned notification correctly described in HDSD_thong_bao.md"
  artifacts:
    - path: "docs/hdsd/HDSD_ho_so_cong_viec.md"
      provides: "HSCV HDSD with giao-việc-arrival note"
      contains: "Mới tạo"
    - path: "docs/hdsd/HDSD_dashboard.md"
      provides: "Dashboard HDSD aligned with current widgets"
    - path: "docs/hdsd/HDSD_thong_bao.md"
      provides: "Thông báo HDSD with task_assigned notification documented"
    - path: "docs/hdsd/HDSD_cau_hinh_gui_nhanh.md"
      provides: "Cấu hình gửi nhanh HDSD aligned"
  key_links:
    - from: "docs/hdsd/HDSD_ho_so_cong_viec.md"
      to: "docs/hdsd/HDSD_van_ban_den.md GAP 1"
      via: "Both files describe the same flow from different sides"
      pattern: "tab.*Mới tạo"
---

<objective>
Update text of HSCV + Dashboard + Thông báo + Cấu hình gửi nhanh HDSD per audit report. Critical: HSCV file must note where giao-việc-from-VB arrives (tab "Mới tạo") — this is the "other side" of GAP 1 + GAP 2.

Output: 4 updated HDSD files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md
@.planning/phases/32-hdsd-audit-update/32-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update HDSD_ho_so_cong_viec.md — add "VB tự xuất hiện ở tab Mới tạo" note + audit diffs</name>
  <read_first>
    - .planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md (section for HDSD_ho_so_cong_viec.md)
    - docs/hdsd/HDSD_ho_so_cong_viec.md (full)
    - e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx (tab structure, list query)
    - e_office_app_new/backend/src/routes/incoming-doc.ts lines 909-973 (giao việc → create HSCV)
    - e_office_app_new/backend/src/routes/outgoing-doc.ts lines 899-926 (giao việc VB đi → create HSCV)
  </read_first>
  <files>docs/hdsd/HDSD_ho_so_cong_viec.md</files>
  <action>
    1. Locate the section describing tab "Mới tạo" (or the section listing HSCV statuses/tabs). If no such section exists, locate "## 2. Quy trình nghiệp vụ" or "## 3. Các màn hình chức năng".
    2. Insert (or modify if exists) a paragraph/subsection describing the "automatic arrival" flow:

    ```
    #### HSCV tự xuất hiện ở tab "Mới tạo" khi được giao việc

    Có 2 nguồn HSCV xuất hiện tự động ở tab **"Mới tạo"** (status mặc định khi giao việc):

    1. **Giao việc từ Văn bản đến:** Khi lãnh đạo bấm "Tạo và giao việc" ở chi tiết VB đến, hệ thống tạo HSCV mới gắn với VB đến đó và đặt người được chọn làm Người phụ trách. HSCV xuất hiện ở tab "Mới tạo" của người được giao. Người được giao **nhận chuông thông báo** "Bạn vừa được giao việc..." → bấm vào điều hướng sang HSCV chi tiết.

    2. **Giao việc từ Văn bản đi:** Khi người soạn bấm "Tạo và giao việc" ở chi tiết VB đi, HSCV mới được tạo gắn với VB đi đó. HSCV cũng xuất hiện ở tab "Mới tạo" của người được giao. **Lưu ý:** Hiện tại VB đi giao việc CHƯA có chuông thông báo (gap code chưa fix — sẽ bổ sung ở phiên bản sau v3.2+) — người được giao phải tự vào trang HSCV để biết.

    Trong cả hai trường hợp, HSCV chi tiết hiển thị link đến văn bản nguồn (đến hoặc đi). Văn bản nguồn KHÔNG bị di chuyển — vẫn nằm ở trang VB đến/đi gốc của người tiếp nhận/người soạn.
    ```

    3. Insert position: After the section describing the tab list, OR at the start of "## 3. Các màn hình chức năng" if no tab section exists. Pick the most natural position based on file structure.

    4. Apply remaining audit diffs (button/field/notification changes per audit report).
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content -Raw docs/hdsd/HDSD_ho_so_cong_viec.md; $a = $c -match 'HSCV tự xuất hiện ở tab'; $b = $c -match 'Mới tạo'; $d = $c -match 'CHƯA có chuông thông báo'; if ($a -and $b -and $d) { exit 0 } else { Write-Host \"a=$a b=$b d=$d\"; exit 1 }"</automated>
  </verify>
  <done>
    - New subsection "HSCV tự xuất hiện ở tab 'Mới tạo' khi được giao việc" inserted
    - Both nguồn giao việc (VB đến + VB đi) described
    - Note about VB đi không có chuông
  </done>
</task>

<task type="auto">
  <name>Task 2: Update HDSD_dashboard.md per audit</name>
  <read_first>
    - .planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md (section for HDSD_dashboard.md)
    - docs/hdsd/HDSD_dashboard.md (full)
    - e_office_app_new/frontend/src/app/(main)/page.tsx (dashboard root, may be redirect)
    - e_office_app_new/frontend/src/app/(main)/dashboard/page.tsx if exists
    - e_office_app_new/frontend/src/components/dashboard/*
  </read_first>
  <files>docs/hdsd/HDSD_dashboard.md</files>
  <action>
    Per audit report for HDSD_dashboard.md:
    1. Cross-check widget names/labels in HDSD vs current dashboard component code.
    2. Update text where audit shows diff (e.g., chart titles, KPI card labels, status filters).
    3. Verify screenshot references resolve to existing files.
    4. If audit verdict is "No change", leave file untouched but note in commit.
  </action>
  <verify>
    <automated>powershell -Command "if (Test-Path docs/hdsd/HDSD_dashboard.md) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>
    - File exists and reflects audit-listed diffs (or unchanged if audit said no change)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update HDSD_thong_bao.md + HDSD_cau_hinh_gui_nhanh.md per audit</name>
  <read_first>
    - .planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md (sections for both files)
    - docs/hdsd/HDSD_thong_bao.md
    - docs/hdsd/HDSD_cau_hinh_gui_nhanh.md
    - e_office_app_new/frontend/src/app/(main)/thong-bao/page.tsx
    - e_office_app_new/frontend/src/app/(main)/cau-hinh-gui-nhanh/page.tsx
    - e_office_app_new/backend/src/routes/notification.ts (or wherever notify types are defined)
  </read_first>
  <files>docs/hdsd/HDSD_thong_bao.md, docs/hdsd/HDSD_cau_hinh_gui_nhanh.md</files>
  <action>
    **HDSD_thong_bao.md:**
    1. Cross-check notification types listed in HDSD vs current code (grep `notification_type` or notification factory).
    2. Verify `task_assigned` notification (VB đến giao việc → bell) is documented; if missing, add a row in the notification types table.
    3. If a notification type was renamed/added in Phase 21+, update HDSD accordingly.
    4. Apply remaining audit diffs.

    **HDSD_cau_hinh_gui_nhanh.md:**
    1. Cross-check form fields, button labels, send templates.
    2. Apply audit-listed diffs.
  </action>
  <verify>
    <automated>powershell -Command "$f1 = Test-Path docs/hdsd/HDSD_thong_bao.md; $f2 = Test-Path docs/hdsd/HDSD_cau_hinh_gui_nhanh.md; if ($f1 -and $f2) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>
    - Both files reflect audit-listed diffs
    - task_assigned notification documented in HDSD_thong_bao.md
  </done>
</task>

</tasks>

<verification>
- `grep -c "HSCV tự xuất hiện" docs/hdsd/HDSD_ho_so_cong_viec.md` should be ≥ 1
- `grep -c "task_assigned\\|Giao việc\\|Bạn vừa được giao việc" docs/hdsd/HDSD_thong_bao.md` should be ≥ 1
</verification>

<success_criteria>
- HSCV HDSD describes the giao-việc-arrival flow from both VB đến and VB đi sides
- task_assigned notification appears in HDSD_thong_bao.md
- All 4 files reflect current UI per audit
</success_criteria>

<output>
Create `.planning/phases/32-hdsd-audit-update/32-03-SUMMARY.md` listing changed sections per file.
</output>
