---
phase: 21-automation-foundation
plan: 07
type: execute
wave: 1
depends_on: ["21-02", "21-05"]
files_modified:
  - e_office_app_new/backend/tools/test-db-reset.ts
  - e_office_app_new/backend/.env.test.example
  - e_office_app_new/database/seed/003_test_fixtures.sql
  - docs/automation-test/README.md
autonomous: true
gap_closure: true
requirements: [AUTO-02, AUTO-04, RPT-07]
must_haves:
  truths:
    - "Fresh `npm run test:db:reset` trên Windows local KHÔNG cần thao tác thủ công thêm — auto-detect Docker container chạy"
    - ".env.test.example tồn tại trong backend dir với PG_DOCKER_CONTAINER=qlvb_postgres pre-set + comment Windows-specific"
    - "Login test_admin trả `fullName` tiếng Việt CÓ DẤU (e.g. 'TEST Quản trị', 'TEST Văn thư', 'TEST Lãnh đạo', 'TEST Cán bộ')"
    - "README có 2 entry mới về Windows PG_DOCKER_CONTAINER (section 1.3 + section 8) + 1 section CI integration mới + 1 section Add test mới"
    - "Re-run `npm run test:db:reset` sau update sql + seed → fixture mới apply, login API verify diacritics OK"
  artifacts:
    - path: "e_office_app_new/backend/tools/test-db-reset.ts"
      provides: "Updated useDocker logic — Windows process.platform detection + execSync(docker ps) check làm fallback"
      contains: "process.platform === 'win32'"
    - path: "e_office_app_new/backend/.env.test.example"
      provides: "NEW file — template env cho test, Windows dev có sẵn PG_DOCKER_CONTAINER"
      contains: "PG_DOCKER_CONTAINER=qlvb_postgres"
      min_lines: 40
    - path: "e_office_app_new/database/seed/003_test_fixtures.sql"
      provides: "Updated 6 INSERT staff với last_name có dấu tiếng Việt"
      contains: "Quản trị"
    - path: "docs/automation-test/README.md"
      provides: "Updated 2 section (1.3 + 8) + 2 section mới (7.5 + 8.5)"
      contains: "PG_DOCKER_CONTAINER"
  key_links:
    - from: "tools/test-db-reset.ts"
      to: "process.platform + child_process.execSync"
      via: "Windows fallback Docker detection khi không có /var/run/docker.sock"
      pattern: "process\\.platform === 'win32'"
    - from: "deploy/test-db-setup.ps1"
      to: "backend/.env.test.example"
      via: "auto-copy template khi backend/.env.test missing"
      pattern: "Copy-Item.*env.test.example"
    - from: "Login API response"
      to: "seed/003_test_fixtures.sql staff fixture"
      via: "fullName field reflects last_name diacritics"
      pattern: "Quản trị|Văn thư|Lãnh đạo|Cán bộ"
---

<objective>
Đóng 3 gap UAT Phase 21 phát hiện sau partial verification (3 PASS / 2 ISSUE / 2 BLOCKED-deferred-CI):

- **Gap 1 BLOCKER (test 1):** `npm run test:db:reset` ENOENT trên Windows vì `useDocker` logic chỉ check `/var/run/docker.sock` (Linux only). Workaround đã apply (`PG_DOCKER_CONTAINER=qlvb_postgres` trong `.env.test`). **Permanent fix:** Auto-detect Docker khi `process.platform === 'win32'` + tạo `.env.test.example` với env pre-set + README docs update.
- **Gap 2 MAJOR (test 7):** README onboarding thiếu (a) Windows-specific note PG_DOCKER_CONTAINER ở section 1.3 + 8 (b) section "8.5 CI integration" giải thích test-pr.yml workflow (c) section "7.5 Add test mới" với boilerplate template.
- **Gap 3 MINOR (test 3):** Seed 003 fixture `last_name` dùng KHÔNG DẤU ('Quan tri'/'Van thu'/'Lanh dao'/'Can bo') — vi phạm CLAUDE.md feedback #4. UI test sẽ KHÔNG exercise UTF-8 path đúng. Fix: đổi sang tiếng Việt có dấu (last_name có dấu, first_name 'TEST' giữ ASCII).

Purpose: Phase 21 acceptance criteria #6 yêu cầu "QA mới đọc README ≤ 30 phút chạy được test đầu tiên" — gap 1 + 2 BLOCK Windows QA mới, vượt 30 phút target. Gap 3 fix UTF-8 coverage cho E2E test.

Output: 4 file (1 NEW: .env.test.example + 3 modified: test-db-reset.ts + 003_test_fixtures.sql + README.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21-automation-foundation/21-UAT.md
@.planning/phases/21-automation-foundation/21-02-SUMMARY.md
@.planning/phases/21-automation-foundation/21-05-SUMMARY.md
@CLAUDE.md
@e_office_app_new/backend/tools/test-db-reset.ts
@e_office_app_new/backend/.env.test
@e_office_app_new/database/seed/003_test_fixtures.sql
@docs/automation-test/README.md

<interfaces>
<!-- Current useDocker logic at test-db-reset.ts:122-124 (BUGGY on Windows): -->
<!-- const useDocker = process.env.USE_DOCKER_PSQL === 'true' || -->
<!--   (!process.env.USE_DOCKER_PSQL && fs.existsSync('/var/run/docker.sock')) || -->
<!--   process.env.PG_DOCKER_CONTAINER !== undefined; -->

<!-- Working .env.test (workaround already applied) at e_office_app_new/backend/.env.test:18: -->
<!-- # Windows + Docker: bat buoc set PG_DOCKER_CONTAINER vi auto-detect /var/run/docker.sock -->
<!-- # trong test-db-reset.ts:122-124 chi work tren Linux -->
<!-- PG_DOCKER_CONTAINER=qlvb_postgres -->

<!-- Current seed 003 fixture lines 73-90 (ASCII only — needs diacritics fix): -->
<!-- VALUES (9001, ..., 'TEST', 'Quan tri', ...) -->
<!-- VALUES (9002, ..., 'TEST', 'Van thu', ...) -->
<!-- VALUES (9003, ..., 'TEST', 'Lanh dao', ...) -->
<!-- VALUES (9004, ..., 'TEST', 'Can bo', ...) -->
<!-- VALUES (9005, ..., 'TEST', 'CB don vi khac', ...) -->
<!-- VALUES (9099, ..., 'TEST', 'User da khoa', ...) -->

<!-- Existing README structure (348 lines, 11 sections — DO NOT renumber existing): -->
<!-- 0. Tổng quan stack -->
<!-- 1. Cài đặt môi trường (1.1 Pre-req, 1.2 Clone+install, 1.3 .env.test, 1.4 Docker) -->
<!-- 2. Setup test DB -->
<!-- 3. Boot backend+frontend+mocks -->
<!-- 4. Chạy smoke test đầu tiên -->
<!-- 5. Sync results về Excel -->
<!-- 6. Khi test fail — debug -->
<!-- 7. Convention quan trọng (7.1 Naming, 7.2 Tags, 7.3 Storage state, 7.4 Locator) -->
<!-- 8. Troubleshooting (10-row table) -->
<!-- 9. Tài nguyên -->
<!-- 10. FAQ -->

<!-- Files reference for "TEST_USERS / TEST_DOCS" import path in section 7.5: -->
<!-- - tests/fixtures/auth.ts (storageStateFor — ROOT level) -->
<!-- - e_office_app_new/backend/tests/fixtures/users.ts (TEST_USERS) -->
<!-- - e_office_app_new/backend/tests/fixtures/docs.ts (TEST_DOCS) -->

<!-- CI workflow path for section 8.5: -->
<!-- - .github/workflows/test-pr.yml (3 job: build-check + integration-tests + e2e-smoke) -->
<!-- - Plan 21-06 SUMMARY confirms: workflow_dispatch trigger + ubuntu-latest + 8min timeout -->
</interfaces>
</context>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════════════════
     WAVE 1 — PERMANENT FIX CODE PATH (auto-detect Docker on Windows)
     ═══════════════════════════════════════════════════════════════════════════ -->

<task type="auto" tdd="false">
  <name>Task 1.1: Update test-db-reset.ts — auto-detect Docker trên Windows</name>
  <files>e_office_app_new/backend/tools/test-db-reset.ts</files>
  <read_first>
    - e_office_app_new/backend/tools/test-db-reset.ts (TOÀN BỘ — hiện 246 dòng, đặc biệt line 27 import + line 121-125 useDocker logic)
    - .planning/phases/21-automation-foundation/21-UAT.md (Gap 1 missing[] line 120 — exact fix description)
    - CLAUDE.md (pitfall #1 — script .ts OK với tiếng Việt nhưng comment trong file giữ KHÔNG DẤU theo style hiện tại)
  </read_first>
  <action>
**Mục tiêu:** Permanent fix gap 1 — `useDocker` logic phải auto-detect Docker trên Windows mà không cần user phải set `PG_DOCKER_CONTAINER` thủ công. Workaround `.env.test` hiện đang có vẫn giữ làm fallback explicit, nhưng fresh checkout không có `.env.test` cũng phải work nếu có Docker container `qlvb_postgres` chạy.

**Bước 1 — Đọc file hiện tại** (đặc biệt section import + useDocker logic):

Hiện tại:
```typescript
// line 27 (current import)
import { execFileSync } from 'child_process';

// line 121-125 (BUGGY logic)
const useDocker = process.env.USE_DOCKER_PSQL === 'true' ||
  (!process.env.USE_DOCKER_PSQL && fs.existsSync('/var/run/docker.sock')) ||
  process.env.PG_DOCKER_CONTAINER !== undefined;
const dockerContainer = process.env.PG_DOCKER_CONTAINER || 'qlvb_postgres';
```

**Bước 2 — Update import line 27:**

Thay `import { execFileSync } from 'child_process';` bằng:
```typescript
import { execFileSync, execSync } from 'child_process';
```

(Cần `execSync` cho Windows Docker detection — không thể dùng `execFileSync` cho `docker ps -q -f name=...` vì cần parse stdout dài hơn 1 line.)

**Bước 3 — Replace useDocker logic ở line 120-125** bằng helper function + smarter detection:

```typescript
// 3. Apply 4 SQL files via psql CLI (faster than pg lib for big schema files)
// Auto-detect psql trong docker hoac local
// Priority order:
//   1. USE_DOCKER_PSQL=true env -> force docker mode
//   2. PG_DOCKER_CONTAINER env set -> docker mode (explicit)
//   3. Linux + /var/run/docker.sock exists -> docker mode (legacy auto-detect)
//   4. Windows + container 'qlvb_postgres' running -> docker mode (NEW: Windows fallback)
//   5. Otherwise -> direct psql CLI
function detectDockerMode(): { useDocker: boolean; container: string } {
  const explicitContainer = process.env.PG_DOCKER_CONTAINER;
  if (explicitContainer) {
    return { useDocker: true, container: explicitContainer };
  }
  if (process.env.USE_DOCKER_PSQL === 'true') {
    return { useDocker: true, container: 'qlvb_postgres' };
  }
  // Linux: check unix socket
  if (process.platform !== 'win32' && fs.existsSync('/var/run/docker.sock')) {
    return { useDocker: true, container: 'qlvb_postgres' };
  }
  // Windows: probe `docker ps` for qlvb_postgres container
  if (process.platform === 'win32') {
    try {
      const out = execSync('docker ps -q -f name=qlvb_postgres', {
        stdio: ['ignore', 'pipe', 'pipe'],
        encoding: 'utf-8',
        timeout: 5000,
      });
      if (out.trim().length > 0) {
        console.log('[test-db-reset] Auto-detected Docker container qlvb_postgres on Windows');
        return { useDocker: true, container: 'qlvb_postgres' };
      }
    } catch {
      // docker CLI not in PATH or daemon down -> fallback to direct psql
    }
  }
  return { useDocker: false, container: 'qlvb_postgres' };
}

const { useDocker, container: dockerContainer } = detectDockerMode();
```

**Bước 4 — Update execution branch line 136** để bỏ check thừa `&& process.env.PG_DOCKER_CONTAINER`:

Hiện tại (line 136):
```typescript
if (useDocker && process.env.PG_DOCKER_CONTAINER) {
```

Thay bằng (vì `useDocker` giờ đã đảm bảo `dockerContainer` valid):
```typescript
if (useDocker) {
```

**Bước 5 — Verify TS check** sau khi edit:
```powershell
cd e_office_app_new/backend
npx tsc --noEmit
# phai exit 0
```

**Bước 6 — Manual smoke test** (verify Windows path work khi KHÔNG có .env.test):
```powershell
cd e_office_app_new/backend
# Backup workaround env
Copy-Item .env.test .env.test.bak
# Xoa PG_DOCKER_CONTAINER khoi .env.test (giả lập fresh checkout)
(Get-Content .env.test) -replace '^PG_DOCKER_CONTAINER=.*', '# PG_DOCKER_CONTAINER removed for test' | Set-Content .env.test
# Chay reset — phai pass nho auto-detect Docker trên Windows
npm run test:db:reset
# Restore .env.test
Move-Item .env.test.bak .env.test -Force
```

Expected output: log `[test-db-reset] Auto-detected Docker container qlvb_postgres on Windows` xuất hiện trước "Applying 01_create_schemas.sql..." và reset complete < 30s.

Commit ngay sau khi pass:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "fix(21-07): auto-detect Docker on Windows in test-db-reset" --files e_office_app_new/backend/tools/test-db-reset.ts
```
  </action>
  <verify>
    <automated>grep -c "process.platform === 'win32'" e_office_app_new/backend/tools/test-db-reset.ts — phai >= 1</automated>
    <automated>grep -c "execSync" e_office_app_new/backend/tools/test-db-reset.ts — phai >= 2 (1 import + 1 invocation)</automated>
    <automated>grep -c "detectDockerMode\|docker ps -q -f name=qlvb_postgres" e_office_app_new/backend/tools/test-db-reset.ts — phai >= 2</automated>
    <automated>cd e_office_app_new/backend && npx tsc --noEmit 2>&1 | tail -5 — exit 0, 0 TS error</automated>
    <automated>cd e_office_app_new/backend && npm run test:db:reset 2>&1 | tail -10 — exit 0, log "DONE in &lt;Xms" voi X &lt; 30000</automated>
  </verify>
  <acceptance_criteria>
    - File test-db-reset.ts có function `detectDockerMode()` với 4 priority case (explicit container env / USE_DOCKER_PSQL flag / Linux socket / Windows execSync probe)
    - Import line 27 chứa cả `execFileSync, execSync`
    - Logic không còn dùng `process.env.PG_DOCKER_CONTAINER !== undefined` ở dòng quyết định useDocker (đã refactor vào function)
    - `npx tsc --noEmit` từ backend dir → exit 0
    - `npm run test:db:reset` chạy ngay cả KHI .env.test không có dòng PG_DOCKER_CONTAINER (Windows auto-detect work)
    - Khi auto-detect Windows trigger → log "[test-db-reset] Auto-detected Docker container qlvb_postgres on Windows"
    - Reset complete < 30s (giữ AUTO-02 acceptance)
    - Backward compat: explicit PG_DOCKER_CONTAINER env vẫn override (Linux dev / CI có thể force container name khác)
  </acceptance_criteria>
  <done>
    test-db-reset.ts có Windows Docker auto-detection, fresh checkout trên Windows chạy `npm run test:db:reset` không cần thao tác thủ công. Workaround `.env.test` set PG_DOCKER_CONTAINER vẫn work làm explicit override. Linux + CI behavior không đổi.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.2: Tạo backend/.env.test.example template</name>
  <files>e_office_app_new/backend/.env.test.example</files>
  <read_first>
    - e_office_app_new/backend/.env.test (TOÀN BỘ — hiện 52 dòng, copy nguyên content + add comment Windows ở line PG_DOCKER_CONTAINER)
    - .planning/phases/21-automation-foundation/21-02-SUMMARY.md (line 165-168 deviation #2 — wrapper script auto-copy .env.test.example pattern đã setup ở task 21-02)
    - deploy/test-db-setup.ps1 (verify đường dẫn copy template — script hiện expect `.env.test.example` tại đâu?)
  </read_first>
  <action>
**Mục tiêu:** Tạo `.env.test.example` ở `e_office_app_new/backend/.env.test.example` để fresh checkout có template ready (`.env.test` đã gitignored — không thể commit). File này sẽ:
- Có FULL content giống `.env.test` hiện tại (52 dòng)
- Pre-set `PG_DOCKER_CONTAINER=qlvb_postgres` với comment giải thích Windows dev
- Comment đầu file giải thích cách dùng (`Copy-Item .env.test.example .env.test`)

**Verify location:** Đọc `deploy/test-db-setup.ps1` để xem nó copy từ đâu. Nếu script expect file ở repo root (`$REPO_ROOT/.env.test.example`) chứ không phải backend dir, **vẫn tạo ở backend dir** (`e_office_app_new/backend/.env.test.example`) vì:
1. `.env.test` thực tế nằm ở backend dir
2. Convention Node project: `.env.example` cùng dir với `.env`
3. Update wrapper script ở task khác nếu cần — task này chỉ tạo file template

**Tạo file `e_office_app_new/backend/.env.test.example` với content sau:**

```
# QLVB Test Environment — Template
# Copy file nay thanh .env.test:
#   Windows: Copy-Item .env.test.example .env.test
#   Linux:   cp .env.test.example .env.test
#
# Test DB tach hoan toan khoi dev DB (qlvb_test vs qlvb_dev)

# --- Server ---
NODE_ENV=test
PORT=4000
CORS_ORIGIN=http://localhost:3000

# --- PostgreSQL TEST DB (qlvb_test, KHONG dung qlvb_dev) ---
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=qlvb_test
PG_USER=qlvb_admin
PG_PASSWORD=QlvbDev@2026
PG_MAX_CONNECTIONS=10
# Windows + Docker Desktop: explicit set container name de skip auto-detect probe
# (test-db-reset.ts auto-detect Docker trên Windows tu Phase 21-07, nhung explicit nhanh hon ~50ms)
# Linux: comment dong nay neu psql CLI native (CI ubuntu-latest)
PG_DOCKER_CONTAINER=qlvb_postgres

# --- MongoDB ---
MONGODB_URI=mongodb://qlvb_admin:QlvbMongo@2026@localhost:27017/qlvb_logs_test?authSource=admin

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=QlvbRedis@2026

# --- MinIO ---
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=qlvb_admin
MINIO_SECRET_KEY=QlvbMinio@2026
MINIO_USE_SSL=false
MINIO_BUCKET=documents-test

# --- JWT ---
JWT_SECRET=qlvb-test-jwt-secret-32chars-fixed-for-deterministic-testing
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
SIGNING_SECRET_KEY=qlvb-test-signing-key-32chars-fixed-for-deterministic-tests

# --- Mock servers (plan 21-03) ---
SMARTCA_BASE_URL=http://localhost:8181
MYSIGN_BASE_URL=http://localhost:8182
LGSP_ENDPOINT=http://localhost:8183
SIGNING_MOCK_MODE=true
MOCK_EXTERNAL=true

# --- Playwright ---
PLAYWRIGHT_BASE_URL=http://localhost:3000
PLAYWRIGHT_API_URL=http://localhost:4000
```

**Verify content match `.env.test` hiện tại** (chỉ khác ở header comment + comment block PG_DOCKER_CONTAINER giải thích Phase 21-07 auto-detect):
```powershell
# Side-by-side check (Windows):
fc.exe e_office_app_new/backend/.env.test e_office_app_new/backend/.env.test.example
# Phai chi khac comment block (KHÔNG khác value)
```

Commit:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(21-07): add backend/.env.test.example template" --files e_office_app_new/backend/.env.test.example
```
  </action>
  <verify>
    <automated>test -f e_office_app_new/backend/.env.test.example — file exist</automated>
    <automated>grep -c "PG_DOCKER_CONTAINER=qlvb_postgres" e_office_app_new/backend/.env.test.example — phai = 1</automated>
    <automated>grep -c "PG_DATABASE=qlvb_test" e_office_app_new/backend/.env.test.example — phai = 1</automated>
    <automated>grep -c "Copy-Item .env.test.example" e_office_app_new/backend/.env.test.example — phai >= 1 (comment huong dan Windows)</automated>
    <automated>wc -l e_office_app_new/backend/.env.test.example — phai >= 40 lines</automated>
    <automated>grep -c "JWT_SECRET\|SIGNING_SECRET_KEY\|MINIO_ENDPOINT\|MOCK_EXTERNAL\|PLAYWRIGHT_BASE_URL" e_office_app_new/backend/.env.test.example — phai >= 5 (full content match .env.test)</automated>
  </verify>
  <acceptance_criteria>
    - File .env.test.example exist tại `e_office_app_new/backend/.env.test.example`
    - File chứa FULL set env vars giống `.env.test` (POSTGRES + MONGO + REDIS + MINIO + JWT + MOCK + PLAYWRIGHT — 7 group)
    - Có dòng `PG_DOCKER_CONTAINER=qlvb_postgres` pre-set
    - Có comment block 2-3 dòng giải thích Windows Docker Desktop + tham chiếu Phase 21-07 auto-detect
    - Có comment header hướng dẫn `Copy-Item` (Windows) + `cp` (Linux)
    - File ≥ 40 dòng (full template, không phải stub)
    - Tất cả tiếng Việt KHÔNG DẤU (vì user có thể parse env file bằng dotenv không support BOM)
    - Comment đề cập `PG_DOCKER_CONTAINER` để Windows dev biết explicit set sẽ skip auto-detect probe
  </acceptance_criteria>
  <done>
    backend/.env.test.example tạo xong với full env template + Windows-specific comment cho PG_DOCKER_CONTAINER. Fresh clone trên Windows: `cd backend && Copy-Item .env.test.example .env.test` rồi `npm run test:db:reset` chạy ngay.
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════════════
     WAVE 2 — FIXTURE DIACRITICS
     ═══════════════════════════════════════════════════════════════════════════ -->

<task type="auto" tdd="false">
  <name>Task 2.1: Update seed/003_test_fixtures.sql — last_name có dấu tiếng Việt</name>
  <files>e_office_app_new/database/seed/003_test_fixtures.sql</files>
  <read_first>
    - e_office_app_new/database/seed/003_test_fixtures.sql (TÒAN BỘ — đặc biệt line 65-95: 6 INSERT staff cần update last_name + line 97-106 role_of_staff giữ nguyên + comment)
    - .planning/phases/21-automation-foundation/21-UAT.md (Gap 3 missing[] line 133 — exact mapping ASCII → có dấu)
    - CLAUDE.md (feedback #4 "LUÔN viết tiếng Việt có dấu trong UI" — confirm rule)
    - e_office_app_new/database/seed/001_required_data.sql line ~50-100 (verify roles/departments có dấu — pattern tham khảo)
  </read_first>
  <action>
**Mục tiêu:** Đổi 6 dòng INSERT staff trong seed 003 — column `last_name` từ ASCII sang tiếng Việt CÓ DẤU. Giữ:
- `first_name = 'TEST'` (ASCII — prefix nhận biết test fixture)
- `code = 'TEST001'..'TEST005'/'TEST099'` (ASCII — code chuẩn)
- `username = 'test_admin'..'test_locked'` (ASCII — login credential)
- `email = '...@test.local'` (ASCII)
- `phone/mobile` (số)

CHỈ đổi `last_name` (column thứ 5 sau `first_name` trong INSERT VALUES — vị trí thứ 11 trong tuple).

**Mapping (per UAT Gap 3 line 133):**

| Hiện tại (ASCII) | Mới (có dấu) |
|------------------|--------------|
| `'Quan tri'` | `'Quản trị'` |
| `'Van thu'` | `'Văn thư'` |
| `'Lanh dao'` | `'Lãnh đạo'` |
| `'Can bo'` | `'Cán bộ'` |
| `'CB don vi khac'` | `'Cán bộ đơn vị khác'` |
| `'User da khoa'` | `'User đã khóa'` |

**Bước 1 — Đọc file** để verify exact format hiện tại:

Hiện tại (line 73-90 confirmed bằng Read):
```sql
  (9001, 1, 1, 1, 'TEST001', 'test_admin',
   '$2b$10$y/GpbkimP4hyef0/QLVNwuW2xb4tbDNuKREpqenGy7WMnN8QtXsOe',
   true,  'TEST', 'Quan tri',     1, 'test_admin@test.local',   '0900000001', '0900000001', false),
  (9002, 2, 2, 6, 'TEST002', 'test_vanthu',
   '$2b$10$y/GpbkimP4hyef0/QLVNwuW2xb4tbDNuKREpqenGy7WMnN8QtXsOe',
   false, 'TEST', 'Van thu',      2, 'test_vanthu@test.local',  '0900000002', '0900000002', false),
  (9003, 2, 2, 1, 'TEST003', 'test_lanhdao',
   '$2b$10$y/GpbkimP4hyef0/QLVNwuW2xb4tbDNuKREpqenGy7WMnN8QtXsOe',
   false, 'TEST', 'Lanh dao',     1, 'test_lanhdao@test.local', '0900000003', '0900000003', false),
  (9004, 2, 2, 5, 'TEST004', 'test_canbo',
   '$2b$10$y/GpbkimP4hyef0/QLVNwuW2xb4tbDNuKREpqenGy7WMnN8QtXsOe',
   false, 'TEST', 'Can bo',       1, 'test_canbo@test.local',   '0900000004', '0900000004', false),
  (9005, 3, 3, 5, 'TEST005', 'test_canbo_x',
   '$2b$10$y/GpbkimP4hyef0/QLVNwuW2xb4tbDNuKREpqenGy7WMnN8QtXsOe',
   false, 'TEST', 'CB don vi khac', 2, 'test_canbo_x@test.local', '0900000005', '0900000005', false),
  (9099, 2, 2, 5, 'TEST099', 'test_locked',
   '$2b$10$y/GpbkimP4hyef0/QLVNwuW2xb4tbDNuKREpqenGy7WMnN8QtXsOe',
   false, 'TEST', 'User da khoa', 1, 'test_locked@test.local',  '0900000099', '0900000099', true)
```

**Bước 2 — Apply 6 Edit (Edit tool)** thay 6 string cụ thể:

Mỗi Edit cần unique context (để Edit tool match đúng dòng — dùng full VALUES tuple làm anchor):

1. Edit 1: `'TEST', 'Quan tri',     1, 'test_admin@test.local'` → `'TEST', 'Quản trị',     1, 'test_admin@test.local'`
2. Edit 2: `'TEST', 'Van thu',      2, 'test_vanthu@test.local'` → `'TEST', 'Văn thư',     2, 'test_vanthu@test.local'`
3. Edit 3: `'TEST', 'Lanh dao',     1, 'test_lanhdao@test.local'` → `'TEST', 'Lãnh đạo',    1, 'test_lanhdao@test.local'`
4. Edit 4: `'TEST', 'Can bo',       1, 'test_canbo@test.local'` → `'TEST', 'Cán bộ',      1, 'test_canbo@test.local'`
5. Edit 5: `'TEST', 'CB don vi khac', 2, 'test_canbo_x@test.local'` → `'TEST', 'Cán bộ đơn vị khác', 2, 'test_canbo_x@test.local'`
6. Edit 6: `'TEST', 'User da khoa', 1, 'test_locked@test.local'` → `'TEST', 'User đã khóa', 1, 'test_locked@test.local'`

(Spacing có thể adjust để giữ alignment cột — không bắt buộc preserve exact spacing nhưng nên giữ alignment đẹp.)

**Bước 3 — Verify file UTF-8 encoding** (vì PowerShell có thể save UTF-8 with BOM gây parse error psql):
```powershell
# Check BOM (3 byte EF BB BF dau file)
$bytes = [System.IO.File]::ReadAllBytes("e_office_app_new/database/seed/003_test_fixtures.sql")[0..2]
Write-Host "First 3 bytes: $($bytes[0]):$($bytes[1]):$($bytes[2])"
# Phai KHONG la EF:BB:BF (BOM)
```

Nếu có BOM, dùng:
```powershell
$content = Get-Content "e_office_app_new/database/seed/003_test_fixtures.sql" -Raw
[System.IO.File]::WriteAllText("e_office_app_new/database/seed/003_test_fixtures.sql", $content, [System.Text.UTF8Encoding]::new($false))
```

(`UTF8Encoding($false)` = UTF-8 NO BOM.)

**Bước 4 — Verify file content có UTF-8 đúng** bằng grep:
```powershell
# 4 dấu kiểm tra core
grep "Quản trị" e_office_app_new/database/seed/003_test_fixtures.sql
grep "Văn thư" e_office_app_new/database/seed/003_test_fixtures.sql
grep "Lãnh đạo" e_office_app_new/database/seed/003_test_fixtures.sql
grep "Cán bộ" e_office_app_new/database/seed/003_test_fixtures.sql
# Moi grep phai return >= 1 line
```

Commit:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "fix(21-07): test fixture last_name có dấu tiếng Việt" --files e_office_app_new/database/seed/003_test_fixtures.sql
```
  </action>
  <verify>
    <automated>grep -c "Quản trị" e_office_app_new/database/seed/003_test_fixtures.sql — phai = 1</automated>
    <automated>grep -c "Văn thư" e_office_app_new/database/seed/003_test_fixtures.sql — phai = 1</automated>
    <automated>grep -c "Lãnh đạo" e_office_app_new/database/seed/003_test_fixtures.sql — phai = 1</automated>
    <automated>grep -c "Cán bộ đơn vị khác" e_office_app_new/database/seed/003_test_fixtures.sql — phai = 1</automated>
    <automated>grep -c "User đã khóa" e_office_app_new/database/seed/003_test_fixtures.sql — phai = 1</automated>
    <automated>grep -E "'TEST', 'Cán bộ'," e_office_app_new/database/seed/003_test_fixtures.sql | wc -l — phai = 1 (test_canbo, không phải canbo_x)</automated>
    <automated>grep -c "'Quan tri'\|'Van thu'\|'Lanh dao'" e_office_app_new/database/seed/003_test_fixtures.sql — phai = 0 (không còn ASCII version)</automated>
  </verify>
  <acceptance_criteria>
    - 6 string ASCII cũ đã thay bằng 6 string có dấu mới
    - File UTF-8 NO BOM (psql apply không lỗi)
    - 6 user fixture vẫn giữ id 9001-9099, code TEST*, username test_*, password_hash bcrypt giữ nguyên
    - first_name = 'TEST' (giữ ASCII — prefix test marker)
    - Cột alignment (spacing) acceptable — không bắt buộc preserve exact whitespace nhưng phải readable
    - Total file size không giảm so với hiện tại (chỉ thay text, không xóa block)
  </acceptance_criteria>
  <done>
    seed/003_test_fixtures.sql có 6 last_name tiếng Việt có dấu. File UTF-8 NO BOM. Re-run reset DB sẽ apply fixture mới (verify ở task 2.2).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2.2: Re-apply seed + verify login API trả full_name có dấu</name>
  <files></files>
  <read_first>
    - .planning/phases/21-automation-foundation/21-UAT.md (Test 3 line 41-49 — pattern login API verify full_name)
    - e_office_app_new/backend/.env.test (verify PG_DATABASE=qlvb_test + PG_DOCKER_CONTAINER=qlvb_postgres still set)
  </read_first>
  <action>
**Mục tiêu:** Re-run `npm run test:db:reset` (apply seed mới đã update task 2.1) → start backend → POST /api/auth/login → verify response field `fullName` chứa "Quản trị" / "Văn thư" / "Lãnh đạo" / "Cán bộ".

**Bước 1 — Re-apply seed:**

```powershell
cd e_office_app_new/backend
npm run test:db:reset 2>&1 | Tee-Object -FilePath /tmp/reset.log | Select-Object -Last 10
# Phai thay log "DONE in <Xms" voi X < 30000
```

Verify staff fixture mới apply (DB query trực tiếp):
```powershell
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_test -c "SELECT username, last_name, full_name FROM public.staff WHERE id IN (9001, 9002, 9003, 9004, 9005, 9099) ORDER BY id"
# Output expected:
#  username     | last_name           | full_name
#  test_admin   | Quản trị            | TEST Quản trị
#  test_vanthu  | Văn thư             | TEST Văn thư
#  test_lanhdao | Lãnh đạo            | TEST Lãnh đạo
#  test_canbo   | Cán bộ              | TEST Cán bộ
#  test_canbo_x | Cán bộ đơn vị khác  | TEST Cán bộ đơn vị khác
#  test_locked  | User đã khóa        | TEST User đã khóa
```

**Bước 2 — Boot backend (background)** dùng PG_DATABASE=qlvb_test:

```powershell
cd e_office_app_new/backend
$env:PG_DATABASE='qlvb_test'
# Start trong background — log ra file
Start-Process -FilePath "npm" -ArgumentList "run","dev" -RedirectStandardOutput "/tmp/backend.log" -RedirectStandardError "/tmp/backend.err" -NoNewWindow -PassThru
Start-Sleep -Seconds 8
# Verify health
curl http://localhost:4000/api/health
# Expected: {"status":"ok",...}
```

**Bước 3 — POST /api/auth/login** với test_admin và verify response:

```powershell
$body = @{ username='test_admin'; password='Test@123' } | ConvertTo-Json
$response = Invoke-RestMethod -Uri 'http://localhost:4000/api/auth/login' -Method POST -ContentType 'application/json' -Body $body
$response | ConvertTo-Json
# Expected: response.user.fullName phai chua "Quản trị"
$fullName = $response.user.fullName
Write-Host "FullName: $fullName"
if ($fullName -match 'Quản trị') {
  Write-Host "✓ Diacritics OK in login response"
} else {
  Write-Host "✗ FAIL — fullName='$fullName' khong chua 'Quản trị'"
  exit 1
}
```

Hoặc dùng curl + grep (POSIX style):
```powershell
$loginRes = curl -s -X POST -H "Content-Type: application/json" -d '{\"username\":\"test_admin\",\"password\":\"Test@123\"}' http://localhost:4000/api/auth/login
# Verify chứa "Quản trị" trong JSON response
if ($loginRes -match 'Qu[ảa]n tr[ịi]') {
  Write-Host "✓ Login API trả fullName có chứa Quản trị"
}
```

**Bước 4 — Verify thêm 3 user khác** (vanthu, lanhdao, canbo):

Login mỗi user, verify fullName chứa text có dấu tương ứng:
- test_vanthu → "Văn thư"
- test_lanhdao → "Lãnh đạo"
- test_canbo → "Cán bộ"

**Bước 5 — Stop backend:**

```powershell
Get-Process -Name node -ErrorAction SilentlyContinue | Where-Object { $_.MainWindowTitle -match 'qlvb' -or $_.Path -match 'backend' } | Stop-Process -Force
# Hoac brute force (chap nhan stop het node):
# Get-Process -Name node | Stop-Process -Force
```

**Lưu ý — Task này KHÔNG modify file** (chỉ verify). Nếu fail bất kỳ bước nào, return về task 2.1 fix lại seed.

KHÔNG commit cho task này (verify-only).
  </action>
  <verify>
    <automated>cd e_office_app_new/backend && npm run test:db:reset 2>&1 | tail -5 — exit 0</automated>
    <automated>docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_test -tAc "SELECT count(*) FROM public.staff WHERE last_name IN ('Quản trị', 'Văn thư', 'Lãnh đạo', 'Cán bộ', 'Cán bộ đơn vị khác', 'User đã khóa')" — phai = 6</automated>
    <automated>docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_test -tAc "SELECT full_name FROM public.staff WHERE username = 'test_admin'" | grep -c "Quản trị" — phai = 1</automated>
    <automated>docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_test -tAc "SELECT full_name FROM public.staff WHERE username = 'test_vanthu'" | grep -c "Văn thư" — phai = 1</automated>
    <automated>docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_test -tAc "SELECT full_name FROM public.staff WHERE username = 'test_lanhdao'" | grep -c "Lãnh đạo" — phai = 1</automated>
    <automated>cd e_office_app_new/backend; $env:PG_DATABASE='qlvb_test'; Start-Process -FilePath "npm" -ArgumentList "run","dev" -RedirectStandardOutput "/tmp/backend.log" -NoNewWindow; Start-Sleep -Seconds 8; $r = Invoke-RestMethod -Uri 'http://localhost:4000/api/auth/login' -Method POST -ContentType 'application/json' -Body '{"username":"test_admin","password":"Test@123"}'; Write-Host $r.user.fullName; if ($r.user.fullName -notmatch 'Quản trị') { exit 1 }; Get-Process -Name node | Stop-Process -Force — exit 0</automated>
  </verify>
  <acceptance_criteria>
    - npm run test:db:reset chạy thành công (exit 0, < 30s)
    - DB query verify 6 staff có last_name tiếng Việt có dấu
    - column `full_name` (GENERATED) chứa diacritics chính xác (test_admin → 'TEST Quản trị', test_vanthu → 'TEST Văn thư', etc.)
    - Backend boot OK trên qlvb_test (health endpoint trả ok)
    - POST /api/auth/login với test_admin/Test@123 → response.user.fullName chứa "Quản trị"
    - POST với test_vanthu → "Văn thư", test_lanhdao → "Lãnh đạo", test_canbo → "Cán bộ"
    - Backend được stop sau verification (cleanup)
  </acceptance_criteria>
  <done>
    Seed mới apply OK, DB có 6 fixture với diacritics, login API verify trả `fullName` tiếng Việt có dấu. Gap 3 closed. Note vào summary: cosmetic fix mở UTF-8 path cho E2E test render text correctly.
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════════════
     WAVE 3 — README ONBOARDING COMPLETENESS
     ═══════════════════════════════════════════════════════════════════════════ -->

<task type="auto" tdd="false">
  <name>Task 3.1: README section 1.3 — Windows PG_DOCKER_CONTAINER callout</name>
  <files>docs/automation-test/README.md</files>
  <read_first>
    - docs/automation-test/README.md (TÒAN BỘ — đặc biệt section 1.3 line 60-67 hiện chỉ có 1 dòng `Copy-Item .env.test.example ...`)
    - .planning/phases/21-automation-foundation/21-UAT.md (Gap 2 missing[] line 147 — exact instruction)
    - e_office_app_new/backend/.env.test.example (đã tạo task 1.2 — verify exists trước khi viết README)
  </read_first>
  <action>
**Mục tiêu:** Section 1.3 hiện tại quá ngắn (chỉ 4 dòng nội dung), thiếu thông tin về Windows-specific PG_DOCKER_CONTAINER. QA Windows mới copy template xong sẽ thấy `PG_DOCKER_CONTAINER=qlvb_postgres` đã pre-set nhưng không hiểu tại sao — section này phải explain.

**Bước 1 — Đọc section 1.3 hiện tại** (line 60-67):

```markdown
### 1.3. Setup .env.test

```powershell
# Backend test config
Copy-Item .env.test.example e_office_app_new/backend/.env.test
# File đã có sẵn: qlvb_test DB + mock URLs + JWT secret
```
```

**Bước 2 — Thay thế bằng version chi tiết hơn** (sử dụng Edit tool):

Find:
```
### 1.3. Setup .env.test

```powershell
# Backend test config
Copy-Item .env.test.example e_office_app_new/backend/.env.test
# File đã có sẵn: qlvb_test DB + mock URLs + JWT secret
```
```

Replace with:
```
### 1.3. Setup .env.test

Template `.env.test.example` nằm trong `e_office_app_new/backend/`. Copy thành `.env.test`:

**Windows (PowerShell):**
```powershell
cd e_office_app_new/backend
Copy-Item .env.test.example .env.test
cd ../..
```

**Linux/macOS:**
```bash
cd e_office_app_new/backend
cp .env.test.example .env.test
cd ../..
```

**File đã có sẵn:** qlvb_test DB credential + mock URLs (8181/8182/8183) + JWT secret + signing key.

> **⚠️ Windows + Docker Desktop dev — IMPORTANT:**
> Template đã pre-set `PG_DOCKER_CONTAINER=qlvb_postgres`. **KHÔNG xóa dòng này** trên Windows.
> Phase 21-07 đã thêm auto-detect Docker trên Windows trong `test-db-reset.ts`, nhưng explicit set qua env nhanh hơn (~50ms) và tránh phụ thuộc `docker ps` trong PATH.
> CI ubuntu-latest dùng psql native — comment hoặc xóa dòng này nếu chạy CI local mà KHÔNG có Docker container `qlvb_postgres`.
```

**Bước 3 — Verify markdown render OK:**

```powershell
# Section 1.3 phai co block code rieng cho Windows + Linux + callout warning
grep -A 30 "^### 1.3" docs/automation-test/README.md | head -35
```

Commit:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(21-07): README section 1.3 add Windows PG_DOCKER_CONTAINER note" --files docs/automation-test/README.md
```
  </action>
  <verify>
    <automated>grep -c "PG_DOCKER_CONTAINER" docs/automation-test/README.md — phai >= 1 (sau task này, sẽ thêm o task 3.2)</automated>
    <automated>grep -A 5 "^### 1.3" docs/automation-test/README.md | grep -c "Windows" — phai >= 1</automated>
    <automated>grep -c "Phase 21-07\|auto-detect Docker" docs/automation-test/README.md — phai >= 1</automated>
    <automated>grep -A 30 "^### 1.3" docs/automation-test/README.md | grep -c "PowerShell\|powershell\|Linux/macOS\|bash" — phai >= 2 (block code Windows + Linux)</automated>
  </verify>
  <acceptance_criteria>
    - Section 1.3 vẫn giữ tên `### 1.3. Setup .env.test`
    - Có 2 block code riêng biệt: Windows (PowerShell `Copy-Item`) + Linux/macOS (bash `cp`)
    - Có callout block với keyword `⚠️ Windows + Docker Desktop dev` hoặc tương tự (markdown blockquote `>`)
    - Callout đề cập (a) PG_DOCKER_CONTAINER pre-set (b) Phase 21-07 auto-detect (c) CI ubuntu khi nào comment dòng này
    - Section 1.3 dài hơn version cũ (cũ ~7 dòng → mới ~25 dòng)
  </acceptance_criteria>
  <done>
    Section 1.3 README có Windows note rõ ràng — QA Windows mới copy template + biết tại sao PG_DOCKER_CONTAINER có sẵn. Đóng 1/3 sub-gap của Gap 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.2: README section 8 — thêm row troubleshooting "spawnSync psql ENOENT"</name>
  <files>docs/automation-test/README.md</files>
  <read_first>
    - docs/automation-test/README.md (TÒAN BỘ — đặc biệt section 8 Troubleshooting line 289-302 — bảng 10 row)
    - .planning/phases/21-automation-foundation/21-UAT.md (Gap 2 missing[] line 148 — exact troubleshoot row)
  </read_first>
  <action>
**Mục tiêu:** Bảng troubleshooting hiện 10 row nhưng KHÔNG có entry cho `spawnSync psql ENOENT` — đây chính là blocker của Test 1 UAT, QA Windows mới sẽ stuck ngay step DB reset nếu không có entry này.

**Bước 1 — Đọc section 8 hiện tại** (line 289-302):

```markdown
## 8. Troubleshooting

| Vấn đề | Nguyên nhân | Fix |
|--------|-------------|-----|
| `[globalSetup] Backend không phản hồi tại http://localhost:4000` | Backend chưa chạy hoặc PG_DATABASE sai | Bước 3 Terminal 1 — set `PG_DATABASE=qlvb_test` rồi `npm run dev` |
| `Login fail cho admin (test_admin)` | Test DB chưa seed fixture | Bước 2 — `npm run test:db:reset` |
...
```

**Bước 2 — Thêm row mới ở ĐẦU bảng** (sau header, trước row đầu tiên):

Find:
```
## 8. Troubleshooting

| Vấn đề | Nguyên nhân | Fix |
|--------|-------------|-----|
| `[globalSetup] Backend không phản hồi tại http://localhost:4000` | Backend chưa chạy hoặc PG_DATABASE sai | Bước 3 Terminal 1 — set `PG_DATABASE=qlvb_test` rồi `npm run dev` |
```

Replace with:
```
## 8. Troubleshooting

| Vấn đề | Nguyên nhân | Fix |
|--------|-------------|-----|
| `Error: spawnSync psql ENOENT` khi chạy `npm run test:db:reset` trên Windows | psql CLI không có trong PATH + auto-detect Docker probe fail (docker daemon down hoặc container `qlvb_postgres` không chạy) | (1) Verify Docker Desktop đang chạy + container `qlvb_postgres` UP: `docker ps --filter name=qlvb_postgres`. (2) Set explicit `PG_DOCKER_CONTAINER=qlvb_postgres` trong `e_office_app_new/backend/.env.test` (template `.env.test.example` đã pre-set — copy lại nếu lỡ xoá). (3) Restart Docker Desktop nếu container không list. |
| `[globalSetup] Backend không phản hồi tại http://localhost:4000` | Backend chưa chạy hoặc PG_DATABASE sai | Bước 3 Terminal 1 — set `PG_DATABASE=qlvb_test` rồi `npm run dev` |
```

**Bước 3 — Verify count entry:**

```powershell
# Bang phai co them 1 row -> total 11 row
$count = (Get-Content docs/automation-test/README.md | Select-String -Pattern '^\| ' | Measure-Object).Count
Write-Host "Pipe-prefix lines: $count"
# Section 8 truoc co 11 line bat dau bang | (1 header + 1 separator + 9 data row + 1 phia FAQ).
# Sau khi them: phai >= 13 lines
```

Hoặc cụ thể hơn:
```powershell
grep -c "^| " docs/automation-test/README.md
# Phai >= 16 (multiple bang trong README)
```

Commit:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(21-07): README section 8 add spawnSync psql ENOENT troubleshoot row" --files docs/automation-test/README.md
```
  </action>
  <verify>
    <automated>grep -c "spawnSync psql ENOENT" docs/automation-test/README.md — phai = 1</automated>
    <automated>grep -c "PG_DOCKER_CONTAINER" docs/automation-test/README.md — phai >= 2 (section 1.3 task 3.1 + section 8 task này)</automated>
    <automated>grep -c "docker ps --filter name=qlvb_postgres" docs/automation-test/README.md — phai = 1</automated>
    <automated>grep -A 2 "^## 8. Troubleshooting" docs/automation-test/README.md | grep -c "spawnSync\|psql ENOENT" — phai >= 1 (row dau bang)</automated>
  </verify>
  <acceptance_criteria>
    - Bảng troubleshoot section 8 có row `Error: spawnSync psql ENOENT` ở vị trí ĐẦU bảng (visible nhất cho QA Windows scan)
    - Cell "Fix" có 3 step rõ ràng: (1) verify Docker (2) set PG_DOCKER_CONTAINER (3) restart Docker
    - Cell "Nguyên nhân" giải thích cả 2 case: psql không in PATH + auto-detect probe fail
    - Bảng giữ format markdown chuẩn (3 cột)
    - Tổng số row trong bảng = 11 (cũ 10 + 1 mới)
  </acceptance_criteria>
  <done>
    Section 8 README có entry mới cho spawnSync psql ENOENT — Windows QA mới gặp lỗi sẽ tìm thấy fix trong < 30s. Đóng 1/3 sub-gap của Gap 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.3: README — thêm section mới "7.5 Add test mới" với boilerplate</name>
  <files>docs/automation-test/README.md</files>
  <read_first>
    - docs/automation-test/README.md (TÒAN BỘ — đặc biệt section 7 line 228-285 hiện 4 sub-section: 7.1/7.2/7.3/7.4)
    - tests/smoke/auth.spec.ts (sample boilerplate hiện tại — copy structure)
    - e_office_app_new/backend/tests/fixtures/users.ts (verify TEST_USERS export — boilerplate import path)
    - e_office_app_new/backend/tests/fixtures/docs.ts (verify TEST_DOCS export)
    - .planning/phases/21-automation-foundation/21-UAT.md (Gap 2 missing[] line 150 — boilerplate spec)
  </read_first>
  <action>
**Mục tiêu:** Thêm section MỚI `### 7.5. Add test mới` SAU section 7.4 (Locator best practice) và TRƯỚC `## 8. Troubleshooting`. Section này cung cấp 3 boilerplate template (smoke / integration API / E2E full flow) để QA mới copy-paste khi viết TC mới.

> **⚠️ LƯU Ý CHO EXECUTOR:** Các block markdown trong task này dùng `* * *` thay vì `---` (markdown horizontal rule) — lý do: tránh confuse YAML frontmatter parser của gsd-tools (regex pattern match `---
...
---` greedy). **Khi áp dụng vào file README thực tế, dùng `---` (3 dashes) NHƯ NGUYÊN BẢN, KHÔNG dùng `* * *`.** Markdown semantic giống nhau (đều render thành horizontal rule).

**Bước 1 — Tìm vị trí insert** (sau line cuối của section 7.4 — `### 7.4. Locator best practice` block):

Find (last 5 dòng của section 7.4 hiện tại):
```
page.locator('div > div > button:nth-child(2)')  // brittle XPath/selector chain
```
\`\`\`

* * *

## 8. Troubleshooting
```

(Lưu ý: 3 backtick + 3 dấu gạch ngang + section 8 header. Cần unique anchor — dùng "brittle XPath/selector chain" làm anchor.)

Replace với:
```
page.locator('div > div > button:nth-child(2)')  // brittle XPath/selector chain
```

* * *

### 7.5. Add test mới (boilerplate)

Khi QA muốn thêm TC mới (smoke / integration / E2E), copy 1 trong 3 template dưới đây:

#### 7.5.1. Smoke E2E (Playwright) — UI flow

File path: `tests/smoke/<module>.spec.ts`

```typescript
import { test, expect } from '@playwright/test';
import { storageStateFor } from '../fixtures/auth';
import { TEST_USERS } from '../../e_office_app_new/backend/tests/fixtures/users';
import { TEST_DOCS } from '../../e_office_app_new/backend/tests/fixtures/docs';

// Dùng storage state per role để skip login
test.use({ storageState: storageStateFor('vanthu') });

test.describe('Smoke — <Module name>', () => {
  test('TC-XXX-NNN <Mô tả tiếng Việt> @smoke @P-High', async ({ page }) => {
    // Arrange: navigate
    await page.goto('/<route-vietnamese>');

    // Act: tương tác UI
    const button = page.getByRole('button', { name: /<button-text>/i });
    await button.click();

    // Assert: verify state
    await expect(page.locator('.ant-drawer-content').first()).toBeVisible({ timeout: 5000 });

    // Optional: dùng fixture data
    const adminUser = TEST_USERS.admin;  // { username: 'test_admin', password: 'Test@123', ... }
    const docId = TEST_DOCS.incoming.new.id;  // 90001
  });
});
```

**Convention:**
- File 1 module/spec (auth.spec.ts, incoming-doc.spec.ts, etc.)
- Title BẮT BUỘC bắt đầu `TC-XXX-NNN ` (regex `^(TC-[A-Z0-9-]+)`)
- Tag `@smoke` để filter (PR gate run smoke)
- Tag `@P-High` cho priority (per Excel column)
- Dùng `test.use({ storageState })` ở top file (KHÔNG login lại trong test)

#### 7.5.2. Integration test (Vitest + supertest) — API contract

File path: `e_office_app_new/backend/tests/integration/<module>/<endpoint>.test.ts`

```typescript
import { describe, test, expect, beforeAll, afterAll } from 'vitest';
import request from 'supertest';
import { app } from '@/server';  // backend Express app
import { TEST_USERS } from '../../fixtures/users';
import { withTransaction, closeTestPool } from '../../helpers/db';

describe('TC-API-NNN <Endpoint description>', () => {
  let token: string;

  beforeAll(async () => {
    const res = await request(app)
      .post('/api/auth/login')
      .send({ username: TEST_USERS.vanthu.username, password: TEST_USERS.vanthu.password });
    token = res.body.accessToken;
  });

  afterAll(async () => {
    await closeTestPool();
  });

  test('TC-API-NNN-001 GET /api/<route> trả 200 + data shape đúng', async () => {
    await withTransaction(async () => {
      const res = await request(app)
        .get('/api/<route>')
        .set('Authorization', `Bearer ${token}`);

      expect(res.status).toBe(200);
      expect(res.body).toHaveProperty('items');
      expect(Array.isArray(res.body.items)).toBe(true);
    });
    // withTransaction auto-rollback → DB không thay đổi
  });
});
```

**Convention:**
- 1 file test/endpoint
- Dùng `withTransaction()` cho tests modify DB (auto rollback)
- Login 1 lần ở `beforeAll`, reuse `token` cho các test trong describe
- TC-ID `TC-API-XXX` cho integration (vs `TC-AUTH/VBD/HSCV` cho UI smoke)

#### 7.5.3. E2E full flow (Playwright) — Multi-step business

File path: `tests/e2e/<scenario>.spec.ts` (Phase 23 backlog — example structure)

```typescript
import { test, expect } from '@playwright/test';
import { storageStateFor } from '../fixtures/auth';

test.describe('E2E — Full lifecycle <scenario>', () => {
  test('TC-E2E-NNN <Scenario name> @e2e @P-High', async ({ browser }) => {
    // 2 contexts cho 2 user khác nhau
    const vanthuCtx = await browser.newContext({ storageState: storageStateFor('vanthu') });
    const lanhdaoCtx = await browser.newContext({ storageState: storageStateFor('lanhdao') });

    const vanthuPage = await vanthuCtx.newPage();
    const lanhdaoPage = await lanhdaoCtx.newPage();

    // Step 1: vanthu vào sổ VB đến
    await vanthuPage.goto('/van-ban-den');
    // ... click Vào sổ, fill form, save

    // Step 2: lanhdao phê duyệt
    await lanhdaoPage.goto('/van-ban-den');
    // ... click VB vừa tạo, approve

    // Cleanup
    await vanthuCtx.close();
    await lanhdaoCtx.close();
  });
});
```

#### 7.5.4. Checklist trước khi commit test mới

- [ ] Test title có TC-ID prefix (`TC-XXX-NNN `)
- [ ] Tag `@smoke` / `@regression` / `@e2e` theo loại
- [ ] Tag priority `@P-High` / `@P-Medium` / `@P-Low`
- [ ] Dùng `getByRole` / `getByLabel` / `[data-testid]` thay vì raw `text=` hay CSS class
- [ ] TC-ID đã có trong `docs/hdsd/20260505_Testcase_QLVB_V2.xlsx` template (Excel parser warn nếu thiếu)
- [ ] Chạy local: `npx playwright test --grep <TC-ID>` PASS
- [ ] Nếu test modify DB → dùng `withTransaction()` (integration) hoặc reset DB sau (E2E)
- [ ] Comment Vietnamese có dấu OK trong test title (UI render đúng cho Vietnamese QA review)

* * *

## 8. Troubleshooting
```

**Bước 2 — Verify section 7.5 đã thêm + section 8 vẫn ở đúng chỗ:**

```powershell
# Section 7.5 phai exist
grep -c "^### 7.5. Add test mới" docs/automation-test/README.md
# Phai = 1

# Section 8 vẫn nguyên
grep -c "^## 8. Troubleshooting" docs/automation-test/README.md
# Phai = 1

# 3 boilerplate sub-section
grep -c "^#### 7.5" docs/automation-test/README.md
# Phai >= 4 (7.5.1 / 7.5.2 / 7.5.3 / 7.5.4 checklist)
```

Commit:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(21-07): README add section 7.5 Add test mới boilerplate" --files docs/automation-test/README.md
```
  </action>
  <verify>
    <automated>grep -c "^### 7.5. Add test mới" docs/automation-test/README.md — phai = 1</automated>
    <automated>grep -c "^#### 7.5" docs/automation-test/README.md — phai >= 4 (7.5.1/7.5.2/7.5.3/7.5.4)</automated>
    <automated>grep -c "TEST_USERS\|TEST_DOCS" docs/automation-test/README.md — phai >= 2 (boilerplate import)</automated>
    <automated>grep -c "withTransaction" docs/automation-test/README.md — phai >= 1 (integration boilerplate)</automated>
    <automated>grep -c "getByRole\|getByLabel\|data-testid" docs/automation-test/README.md — phai >= 6 (4 trong section 7.4 cũ + 2+ mới trong 7.5)</automated>
    <automated>grep -A 2 "^## 8. Troubleshooting" docs/automation-test/README.md | grep -c "spawnSync\|psql ENOENT" — phai >= 1 (verify task 3.2 không bị overwrite)</automated>
  </verify>
  <acceptance_criteria>
    - Section `### 7.5. Add test mới (boilerplate)` exist sau section 7.4 và trước section 8
    - 3 sub-section boilerplate: 7.5.1 (smoke E2E), 7.5.2 (integration API), 7.5.3 (E2E full flow)
    - Mỗi boilerplate có code block TypeScript đầy đủ với import + structure + comment
    - Sub-section 7.5.4 checklist 8 item trước khi commit test mới
    - Boilerplate reference đúng path import: `TEST_USERS` từ `e_office_app_new/backend/tests/fixtures/users`, `storageStateFor` từ `../fixtures/auth`
    - Section 8 Troubleshooting (đã thêm row spawnSync ở task 3.2) VẪN GIỮ NGUYÊN — không bị overwrite
    - README total > 400 dòng (cũ 348 + new ~80 dòng)
  </acceptance_criteria>
  <done>
    Section 7.5 với 3 boilerplate template đã thêm — QA mới copy-paste là viết được test đầu tiên trong < 5 phút. Đóng 1/3 sub-gap của Gap 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.4: README — thêm section mới "8.5 CI integration"</name>
  <files>docs/automation-test/README.md</files>
  <read_first>
    - docs/automation-test/README.md (TÒAN BỘ — đặc biệt section 8 line 289-302 + section 9 Tài nguyên line 306-314 — vị trí insert giữa 2 section)
    - .planning/phases/21-automation-foundation/21-06-SUMMARY.md (đọc để biết test-pr.yml chi tiết: 3 job parallel, ubuntu-latest, 8 min timeout, artifact upload)
    - .planning/phases/21-automation-foundation/21-UAT.md (Gap 2 missing[] line 149 — exact instruction)
  </read_first>
  <action>
**Mục tiêu:** Thêm section MỚI `## 8.5. CI integration` SAU section 8 Troubleshooting và TRƯỚC `## 9. Tài nguyên`. Section này giải thích:
1. Workflow `.github/workflows/test-pr.yml` chạy gì (3 job parallel)
2. Khi nào trigger (mỗi PR + manual workflow_dispatch)
3. Cách trigger thủ công từ GitHub UI / gh CLI
4. Cách đọc artifact GitHub Actions khi test fail (download Excel + Playwright HTML report)
5. Mapping giữa local smoke run vs CI smoke run (giống nhau, chỉ khác PG_DOCKER_CONTAINER off)

> **⚠️ LƯU Ý CHO EXECUTOR:** Block markdown trong task này dùng `* * *` thay vì `---` (markdown horizontal rule). **Khi áp dụng vào file README thực tế, dùng `---` (3 dashes) NHƯ NGUYÊN BẢN.**

**Bước 1 — Tìm vị trí insert** (sau section 8 cuối + trước section 9):

Section 8 hiện tại kết thúc tại row table cuối cùng (`Smoke fail vì search input...`). Section 9 bắt đầu `## 9. Tài nguyên`.

Find (last row của section 8 + section 9 header):
```
| Smoke fail vì search input không tìm thấy | UI chưa có `data-testid="search-input"` | Test có `test.skip()` với reason — không fail (Phase 22 fix) |

* * *

## 9. Tài nguyên
```

Replace với:
```
| Smoke fail vì search input không tìm thấy | UI chưa có `data-testid="search-input"` | Test có `test.skip()` với reason — không fail (Phase 22 fix) |

* * *

## 8.5. CI integration

QLVB CI workflow `.github/workflows/test-pr.yml` (Phase 21-06) chạy tự động trên mỗi PR + có thể trigger thủ công.

### 8.5.1. Workflow trigger

| Sự kiện | Action |
|---------|--------|
| `pull_request` opened/synchronize trên `main` | Auto-run 3 job parallel |
| `workflow_dispatch` (manual) | Run từ GitHub UI hoặc `gh` CLI |
| `push` trên branch không phải `main` | KHÔNG run (chỉ PR mới run — tránh tốn minute) |

### 8.5.2. 3 job parallel

| Job | Thời gian | Công việc |
|-----|-----------|-----------|
| **build-check** | ~3 phút | TS check backend + frontend (`tsc --noEmit`) + lint + Next.js build |
| **integration-tests** | ~5 phút | Vitest integration (Phase 22 sẽ wire — Phase 21 stub) |
| **e2e-smoke** | ~6 phút | Playwright smoke 30 TC P-High (`npm run test:smoke`) + Excel sync |

**Total wall time:** ~6-8 phút (3 job song song, max trong nhánh dài nhất). PR fail bất kỳ job nào → block merge.

### 8.5.3. Trigger workflow thủ công

**Từ GitHub UI:**
1. Repo → Tab "Actions" → Workflow "Test PR"
2. Click "Run workflow" → Chọn branch → Click "Run workflow" button
3. Workflow run xuất hiện ngay đầu list

**Từ `gh` CLI** (preferred — không cần switch context):
```powershell
# Trigger trên branch hiện tại
gh workflow run test-pr.yml --ref $(git branch --show-current)

# Trigger trên branch khác
gh workflow run test-pr.yml --ref feature/xyz

# Watch tiến trình
gh run watch
```

### 8.5.4. Đọc artifact khi CI fail

Khi job `e2e-smoke` fail, CI upload 2 artifact:

| Artifact | Nội dung | Cách dùng |
|----------|----------|-----------|
| `playwright-report` | HTML trace viewer + screenshots khi fail | Download → unzip → mở `index.html` trong browser → time-travel debug |
| `test-results-excel` | `<YYYYMMDD>_Testcase_QLVB_V2_results.xlsx` | Download → mở Excel → filter cột "Trạng thái" = Fail |

**Cách download artifact:**

**Từ GitHub UI:**
1. Click vào run fail → Scroll xuống section "Artifacts" → Click tên artifact để download .zip

**Từ `gh` CLI:**
```powershell
# List run gần đây
gh run list --workflow=test-pr.yml --limit 5

# Download artifact của run cụ thể
gh run download <run-id>
# Mặc định save vào current dir, sub-folder theo tên artifact
```

### 8.5.5. Local smoke vs CI smoke — sự khác biệt

| Khía cạnh | Local | CI ubuntu-latest |
|-----------|-------|------------------|
| Postgres | Docker container `qlvb_postgres` | Service container PostgreSQL native |
| `PG_DOCKER_CONTAINER` env | Set (Windows) hoặc auto-detect | KHÔNG set (psql native trong PATH) |
| Workers | 4 (default) | 2 (giới hạn để tránh OOM trên runner 7GB RAM) |
| Browser | Chromium từ Playwright local install | Chromium từ `npx playwright install --with-deps` step |
| Frontend | `npm run dev` (Next.js dev mode) | `npm run dev` (giống local) |
| Backend | `npm run dev` (tsx watch) | `npm run dev` (giống local) |
| Test DB reset | Mỗi lần chạy manual | Reset auto trong workflow step trước test |

**Quy tắc:** Test PASS local → 95% PASS CI. 5% fail do font/locale render khác (CI ubuntu không có font Việt) → fix bằng `data-testid` thay vì `getByText('Tiếng Việt')`.

### 8.5.6. Khi muốn skip CI cho 1 commit

**KHÔNG khuyến khích**, nhưng nếu cần (VD: docs-only commit):

```powershell
git commit -m "docs: update README [skip ci]"
# Hoặc:
git commit -m "docs: update README" --allow-empty -m "[skip ci]"
```

Tag `[skip ci]` trong commit message → workflow KHÔNG trigger. Lưu ý: nếu commit này merge vào PR có code change, CI sẽ vẫn chạy ở commit khác.

* * *

## 9. Tài nguyên
```

**Bước 2 — Verify section 8.5 đã thêm:**

```powershell
grep -c "^## 8.5. CI integration" docs/automation-test/README.md
# Phai = 1
grep -c "^### 8.5" docs/automation-test/README.md
# Phai >= 6 (8.5.1/8.5.2/8.5.3/8.5.4/8.5.5/8.5.6)
grep -c "test-pr.yml" docs/automation-test/README.md
# Phai >= 3 (multiple mention)
grep -c "gh workflow run\|gh run download" docs/automation-test/README.md
# Phai >= 2 (gh CLI examples)
```

Commit:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(21-07): README add section 8.5 CI integration" --files docs/automation-test/README.md
```
  </action>
  <verify>
    <automated>grep -c "^## 8.5. CI integration" docs/automation-test/README.md — phai = 1</automated>
    <automated>grep -c "^### 8.5" docs/automation-test/README.md — phai >= 6</automated>
    <automated>grep -c "test-pr.yml" docs/automation-test/README.md — phai >= 3</automated>
    <automated>grep -c "gh workflow run\|gh run download\|gh run list" docs/automation-test/README.md — phai >= 3</automated>
    <automated>grep -c "build-check\|integration-tests\|e2e-smoke" docs/automation-test/README.md — phai >= 3 (3 job name)</automated>
    <automated>grep -c "playwright-report\|test-results-excel" docs/automation-test/README.md — phai >= 2 (2 artifact)</automated>
    <automated>grep -A 1 "^## 8.5" docs/automation-test/README.md | head -3 — verify section nam giua section 8 + 9</automated>
    <automated>wc -l docs/automation-test/README.md — phai >= 480 (cũ 348 + section 7.5 ~80 + section 8.5 ~70)</automated>
  </verify>
  <acceptance_criteria>
    - Section `## 8.5. CI integration` exist sau `## 8. Troubleshooting` và trước `## 9. Tài nguyên`
    - 6 sub-section: 8.5.1 trigger / 8.5.2 jobs / 8.5.3 manual run / 8.5.4 artifact / 8.5.5 local vs CI / 8.5.6 skip ci
    - Có table/list cho 3 job (build-check + integration-tests + e2e-smoke) với thời gian + công việc
    - Có code block `gh workflow run test-pr.yml --ref ...` cho gh CLI
    - Có code block `gh run download <run-id>` cho download artifact
    - Có table compare local vs CI (>=5 row khác biệt)
    - Section 9 Tài nguyên VẪN giữ nguyên — không bị overwrite
    - Tổng README > 480 dòng
    - Tất cả Vietnamese có dấu (markdown OK với UTF-8)
  </acceptance_criteria>
  <done>
    Section 8.5 với 6 sub-section đã thêm — QA mới hiểu rõ CI workflow, biết cách trigger manual + download artifact + debug khi CI fail. Đóng 1/3 sub-gap cuối của Gap 2.
  </done>
</task>

</tasks>

<verification>

**Final integration check** (sau khi 7 task complete):

```bash
# 1. test-db-reset.ts: auto-detect Windows OK
cd e_office_app_new/backend
grep -c "process.platform === 'win32'" tools/test-db-reset.ts  # >= 1
npx tsc --noEmit  # exit 0
npm run test:db:reset  # < 30s, auto-detect work

# 2. .env.test.example exist với PG_DOCKER_CONTAINER
test -f .env.test.example
grep "PG_DOCKER_CONTAINER=qlvb_postgres" .env.test.example

# 3. Seed 003 có dấu
grep -c "Quản trị\|Văn thư\|Lãnh đạo\|Cán bộ" ../database/seed/003_test_fixtures.sql  # >= 4

# 4. Login API trả fullName có dấu
$env:PG_DATABASE='qlvb_test'
Start-Job -ScriptBlock { npm run dev } | Out-Null
Start-Sleep 8
$r = Invoke-RestMethod -Uri 'http://localhost:4000/api/auth/login' -Method POST -ContentType 'application/json' -Body '{"username":"test_admin","password":"Test@123"}'
$r.user.fullName -match 'Quản trị'  # True
Get-Process node | Stop-Process -Force

# 5. README có 4 entry mới
cd ../..
grep -c "PG_DOCKER_CONTAINER" docs/automation-test/README.md  # >= 2
grep -c "spawnSync psql ENOENT" docs/automation-test/README.md  # = 1
grep -c "^### 7.5" docs/automation-test/README.md  # >= 4 (7.5.1..7.5.4)
grep -c "^## 8.5" docs/automation-test/README.md  # = 1
```

**3 gap closure verification:**

| Gap | Test trước | Test sau | Closed? |
|-----|------------|----------|---------|
| Gap 1 BLOCKER | T1 fail "spawnSync psql ENOENT" trên Windows | T1 PASS auto-detect, .env.test.example template ready | ✓ |
| Gap 2 MAJOR | T7 README thiếu 3 section | T7 PASS — 4 entry mới (1.3 callout / 8 troubleshoot / 7.5 boilerplate / 8.5 CI) | ✓ |
| Gap 3 MINOR | T3 fixture full_name không dấu | T3 PASS — login API trả "Quản trị" / "Văn thư" / "Lãnh đạo" / "Cán bộ" | ✓ |

</verification>

<success_criteria>

- **AUTO-02 satisfied (re-verified):** `npm run test:db:reset` chạy < 30s trên Windows fresh checkout (không cần thao tác thủ công thêm khi có Docker container `qlvb_postgres` UP)
- **AUTO-04 satisfied (re-verified):** 5 user fixture (test_admin/vanthu/lanhdao/canbo/canbo_x) login OK với fullName tiếng Việt CÓ DẤU + test_locked → 401/403
- **RPT-07 satisfied (re-verified):** README ≤ 30 phút onboarding khả thi cho Windows QA mới — gap 1 + 2 đóng → không còn blocker step DB reset + có CI integration + boilerplate template

**Phase 21 ready for re-verification:**
- 3 PASS test (T2 + T3 + T4) giữ pass
- 2 ISSUE (T1 + T7) đóng → upgrade thành PASS
- 2 BLOCKED (T5 + T6) deferred CI verify (per UAT note — không phải code bug, chỉ là RAM constraint)

</success_criteria>

<output>
Sau plan này hoàn thành, tạo `.planning/phases/21-automation-foundation/21-07-SUMMARY.md` ghi nhận:
- 3 gap closure mapping (Gap 1 BLOCKER → fix code path + template / Gap 2 MAJOR → 4 README entry / Gap 3 MINOR → seed diacritics)
- Files modified: 3 + 1 NEW
- Verification: re-run T1 + T3 + T7 từ UAT phải PASS
- Note: T5 + T6 vẫn deferred CI verify (local RAM constraint — không phải gap code)
- Branch info: `v3.1/phase-21-foundation` — gap closure commit cùng branch để PR auto-update
</output>
