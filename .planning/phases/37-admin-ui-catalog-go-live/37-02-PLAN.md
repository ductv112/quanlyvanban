---
phase: 37-admin-ui-catalog-go-live
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - e_office_app_new/backend/src/routes/admin-lgsp.ts                  # extend với 2 endpoint mới
  - e_office_app_new/backend/src/repositories/lgsp.repository.ts       # extend getOverviewStats
autonomous: true
requirements: [LGSP-UI-03, LGSP-UI-04, LGSP-UI-06]
tags: [backend, admin, lgsp, test-connection, overview, phase-37]

must_haves:
  truths:
    - "Admin gọi POST /api/admin/lgsp-agency-config/:id/test → backend lookup credential decrypted → gọi LGSPRealService.receiveDocuments(now-1d, now) → trả về { ok, message, http_status, response_summary: { count } | null }"
    - "Test connection KHÔNG có side effect — KHÔNG INSERT incoming_docs, KHÔNG cập nhật last_synced_at"
    - "Khi credential sai → endpoint vẫn trả HTTP 200 (request OK) nhưng data.ok=false + data.message Vietnamese mapped từ LGSP error (vd 'Sai SystemId hoặc SecretKey')"
    - "Admin gọi GET /api/admin/lgsp-overview → trả về { units: [{ unit_id, unit_name, lgsp_org_code, prod_config, sandbox_config, send_today, receive_today, pending_today, error_today }], totals }"
    - "Admin gọi POST /api/admin/inter-organizations/sync → endpoint reuse logic Phase 18 organizations/sync nhưng gắn với credential của 1 trong 6 DN active đầu tiên → batch UPSERT inter_organizations"
    - "TypeScript backend strict tsc --noEmit exit 0"
  artifacts:
    - path: "e_office_app_new/backend/src/routes/admin-lgsp.ts"
      provides: "Extend 3 endpoint mới: test connection, overview dashboard, inter-organizations sync"
      contains: "POST /lgsp-agency-config/:id/test"
    - path: "e_office_app_new/backend/src/repositories/lgsp.repository.ts"
      provides: "Method getOverviewStats — count today aggregations per unit"
      contains: "getOverviewStats"
  key_links:
    - from: "admin-lgsp.ts POST /test"
      to: "lgspAgencyConfigRepository.getByIdWithDecryptedSecret (Plan 37-01) + createLgspRealService (services/lgsp-real.service.ts)"
      via: "lazy create instance + call receiveDocuments"
      pattern: "createLgspRealService|getByIdWithDecryptedSecret"
    - from: "admin-lgsp.ts GET /lgsp-overview"
      to: "lgspRepository.getOverviewStats + lgspAgencyConfigRepository.list"
      via: "aggregate count today per unit"
      pattern: "getOverviewStats"
    - from: "admin-lgsp.ts POST /inter-organizations/sync"
      to: "getLgspService(active_unit_id) + interOrganizationRepository.createForAdmin/updateForAdmin"
      via: "batch upsert"
      pattern: "syncOrganizations|getLgspService"
---

<objective>
Mở rộng `routes/admin-lgsp.ts` (Plan 37-01) với 3 endpoint mới mà Plan 37-03 (UI cấu hình kết nối) + Plan 37-04 (UI cơ quan ngoài) + Plan 37-05 (UI overview) sẽ gọi:
1. `POST /admin/lgsp-agency-config/:id/test` — Test connection sandbox/prod thật (lightweight read-only call `/v1/syncReceivedEdocList` per CONTEXT D-02)
2. `GET /admin/lgsp-overview` — Dashboard summary aggregated per unit (count today: gửi/nhận/pending/error)
3. `POST /admin/inter-organizations/sync` — Batch sync danh sách cơ quan từ trục (admin button "Sync từ LGSP")

Purpose: Plan 37-03/04/05 UI pages cần đủ 12 endpoint sẵn sàng trước khi build frontend. Plan 37-01 đã làm 9 endpoint, Plan 37-02 thêm 3 endpoint còn lại.

Output: Extend admin-lgsp.ts + 1 method mới ở lgsp.repository.ts. Chạy SAU Plan 37-01 vì cùng file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-admin-ui-catalog-go-live/37-CONTEXT.md
@.planning/phases/37-admin-ui-catalog-go-live/37-01-PLAN.md

# Code reference
@e_office_app_new/backend/src/services/lgsp-real.service.ts
@e_office_app_new/backend/src/services/lgsp/error-codes.ts
@e_office_app_new/backend/src/services/lgsp.service.ts
@e_office_app_new/backend/src/repositories/lgsp.repository.ts
@e_office_app_new/backend/src/routes/lgsp.ts

<interfaces>
```typescript
// services/lgsp.service.ts (đã có Phase 33)
export interface ILgspService {
  receiveDocuments(fromDateYmd: string, toDateYmd: string): Promise<LgspReceivedDocSummary[]>;
  syncOrganizations(): Promise<LgspOrganization[]>;
  // ...
}
export async function getLgspService(unitId?: number, environment?: 'sandbox' | 'prod'): Promise<ILgspService>;

// services/lgsp-real.service.ts (đã có Phase 33)
export function createLgspRealService(credentials: {
  baseUrl: string;
  systemId: string;
  secretKey: string;
}): ILgspService;

// services/lgsp/error-codes.ts (Phase 34)
export class LgspSendError extends Error { code?: string; }
export function mapLgspError(code: string): string;  // → Vietnamese

// Repository từ Plan 37-01:
lgspAgencyConfigRepository.getByIdWithDecryptedSecret(id): Promise<{ id, unit_id, environment, system_id, base_url, is_active, secret_key_plaintext } | null>

// CẦN THÊM:
lgspRepository.getOverviewStats(): Promise<OverviewStatsRow[]>
// Row shape:
// {
//   unit_id, unit_name, lgsp_org_code,
//   prod_config_id, prod_is_active, prod_last_synced_at, prod_last_sync_error,
//   sandbox_config_id, sandbox_is_active, sandbox_last_synced_at, sandbox_last_sync_error,
//   send_today_count, send_today_success, send_today_error, send_today_pending,
//   receive_today_count, receive_today_success, receive_today_error,
//   outbox_today_pending, outbox_today_error
// }
```

Date format helper:
```typescript
// services/lgsp/edxml-builder.ts hoặc inline:
function formatLgspDate(d: Date): string {
  // LGSP API format: YYYY/MM/DD
  const yyyy = d.getFullYear();
  const mm = String(d.getMonth() + 1).padStart(2, '0');
  const dd = String(d.getDate()).padStart(2, '0');
  return `${yyyy}/${mm}/${dd}`;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Thêm method getOverviewStats vào lgsp.repository.ts</name>
  <read_first>
    - e_office_app_new/backend/src/repositories/lgsp.repository.ts (full)
    - e_office_app_new/database/schema/000_schema_v3.0.sql (search "lgsp_tracking", "lgsp_status_outbox", "lgsp_agency_config" để biết column names exact)
  </read_first>
  <files>
    e_office_app_new/backend/src/repositories/lgsp.repository.ts
  </files>
  <action>
    THÊM interface + method vào lgspRepository (CONST object pattern):

    ```typescript
    /**
     * Phase 37 Plan 37-02: Dashboard overview row per root unit (DN).
     * Aggregate count today (UTC date) cho: send tracking, receive incoming_docs, outbox status events.
     * 6 DN × 2 env cấu hình → 6 row (env stacked horizontally trong cùng row).
     */
    export interface LgspOverviewRow {
      unit_id: number;
      unit_name: string;
      lgsp_org_code: string | null;
      prod_config_id: number | null;
      prod_is_active: boolean | null;
      prod_last_synced_at: string | null;
      prod_last_sync_error: string | null;
      sandbox_config_id: number | null;
      sandbox_is_active: boolean | null;
      sandbox_last_synced_at: string | null;
      sandbox_last_sync_error: string | null;
      send_today_total: number;
      send_today_success: number;
      send_today_error: number;
      send_today_pending: number;
      receive_today_total: number;
      outbox_today_pending: number;
      outbox_today_error: number;
    }

    // Thêm cuối repo lgspRepository (sau resetTrackingForRetry):

    /**
     * Phase 37 Plan 37-02: Get overview stats per root unit (6 DN).
     * Subquery aggregation per category — count today (DATE = CURRENT_DATE UTC).
     * Used by GET /api/admin/lgsp-overview cho dashboard.
     *
     * Strategy:
     *   - Base CTE = root unit có lgsp_org_code IS NOT NULL (6 DN Lạng Sơn)
     *   - LEFT JOIN lgsp_agency_config per env (prod/sandbox stacked)
     *   - LEFT JOIN aggregation send (lgsp_tracking direction='send' grouped by outgoing_docs.unit_id)
     *   - LEFT JOIN aggregation receive (incoming_docs WHERE source_type='external_lgsp' grouped by unit_id)
     *   - LEFT JOIN aggregation outbox (lgsp_status_outbox JOIN incoming_docs.unit_id)
     */
    async getOverviewStats(): Promise<LgspOverviewRow[]> {
      return rawQuery<LgspOverviewRow>(
        `WITH root_units AS (
            SELECT id::bigint AS unit_id, name AS unit_name, lgsp_org_code
              FROM public.departments
             WHERE lgsp_org_code IS NOT NULL
             ORDER BY lgsp_org_code
          ),
          send_today AS (
            SELECT od.unit_id::bigint AS unit_id,
                   COUNT(*)::bigint AS total,
                   COUNT(*) FILTER (WHERE t.status = 'success')::bigint AS success,
                   COUNT(*) FILTER (WHERE t.status = 'error')::bigint   AS error,
                   COUNT(*) FILTER (WHERE t.status = 'pending')::bigint AS pending
              FROM edoc.lgsp_tracking t
              JOIN edoc.outgoing_docs od ON od.id = t.outgoing_doc_id
             WHERE t.direction = 'send'
               AND t.created_at::date = CURRENT_DATE
             GROUP BY od.unit_id
          ),
          receive_today AS (
            SELECT id.unit_id::bigint AS unit_id,
                   COUNT(*)::bigint AS total
              FROM edoc.incoming_docs id
             WHERE id.source_type = 'external_lgsp'
               AND id.created_at::date = CURRENT_DATE
             GROUP BY id.unit_id
          ),
          outbox_today AS (
            SELECT id.unit_id::bigint AS unit_id,
                   COUNT(*) FILTER (WHERE o.sent_status = 'pending')::bigint AS pending,
                   COUNT(*) FILTER (WHERE o.sent_status = 'error')::bigint   AS error
              FROM edoc.lgsp_status_outbox o
              JOIN edoc.incoming_docs id ON id.id = o.incoming_doc_id
             WHERE o.created_at::date = CURRENT_DATE
             GROUP BY id.unit_id
          )
          SELECT ru.unit_id,
                 ru.unit_name,
                 ru.lgsp_org_code,
                 prod.id::bigint                AS prod_config_id,
                 prod.is_active                 AS prod_is_active,
                 prod.last_synced_at            AS prod_last_synced_at,
                 prod.last_sync_error           AS prod_last_sync_error,
                 sb.id::bigint                  AS sandbox_config_id,
                 sb.is_active                   AS sandbox_is_active,
                 sb.last_synced_at              AS sandbox_last_synced_at,
                 sb.last_sync_error             AS sandbox_last_sync_error,
                 COALESCE(st.total, 0)::bigint     AS send_today_total,
                 COALESCE(st.success, 0)::bigint   AS send_today_success,
                 COALESCE(st.error, 0)::bigint     AS send_today_error,
                 COALESCE(st.pending, 0)::bigint   AS send_today_pending,
                 COALESCE(rt.total, 0)::bigint     AS receive_today_total,
                 COALESCE(ot.pending, 0)::bigint   AS outbox_today_pending,
                 COALESCE(ot.error, 0)::bigint     AS outbox_today_error
            FROM root_units ru
            LEFT JOIN edoc.lgsp_agency_config prod
                   ON prod.unit_id = ru.unit_id AND prod.environment = 'prod'
            LEFT JOIN edoc.lgsp_agency_config sb
                   ON sb.unit_id = ru.unit_id AND sb.environment = 'sandbox'
            LEFT JOIN send_today    st ON st.unit_id = ru.unit_id
            LEFT JOIN receive_today rt ON rt.unit_id = ru.unit_id
            LEFT JOIN outbox_today  ot ON ot.unit_id = ru.unit_id
           ORDER BY ru.lgsp_org_code`,
        [],
      );
    },
    ```

    **LƯU Ý:**
    - Verify column `lgsp_tracking.status` (Phase 18 đã có) — KHÔNG phải `sent_status`
    - Verify column `outgoing_docs.unit_id` tồn tại
    - Verify `incoming_docs.source_type` enum value `'external_lgsp'` (Phase 35-01)
    - Verify `lgsp_status_outbox.sent_status` enum value (Phase 33-01: pending/success/error)
    - Nếu bất kỳ column nào KHÔNG match → chạy `\d edoc.<table>` trong docker để confirm names chính xác trước khi adjust query (Phase rule #5 CLAUDE.md)
  </action>
  <verify>
    <automated>cd e_office_app_new/backend && npx tsc --noEmit 2>&1 | grep "lgsp\.repository" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "LgspOverviewRow" e_office_app_new/backend/src/repositories/lgsp.repository.ts` → 0
    - `grep -q "getOverviewStats" e_office_app_new/backend/src/repositories/lgsp.repository.ts` → 0
    - `grep -q "send_today_total\|receive_today_total\|outbox_today_pending" e_office_app_new/backend/src/repositories/lgsp.repository.ts` (cả 3 substring) → 0 cả 3
    - `cd e_office_app_new/backend && npx tsc --noEmit` → exit 0
  </acceptance_criteria>
  <done>
    lgsp.repository.ts có method getOverviewStats + interface LgspOverviewRow. Query aggregate count today per root unit cho 4 metric (send / receive / outbox). TS strict PASS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend routes/admin-lgsp.ts với 3 endpoint mới (test, overview, inter-org sync)</name>
  <read_first>
    - e_office_app_new/backend/src/routes/admin-lgsp.ts (output Plan 37-01)
    - e_office_app_new/backend/src/services/lgsp-real.service.ts (createLgspRealService signature)
    - e_office_app_new/backend/src/services/lgsp/error-codes.ts (mapLgspError + LgspSendError)
    - e_office_app_new/backend/src/repositories/lgsp.repository.ts (sau khi Task 1 thêm getOverviewStats)
  </read_first>
  <files>
    e_office_app_new/backend/src/routes/admin-lgsp.ts
  </files>
  <action>
    THÊM 3 endpoint vào CUỐI file admin-lgsp.ts (trước `export default router;`):

    **A) Helper inline format LGSP date** (đặt sau imports, trước router):

    ```typescript
    /** LGSP API date format YYYY/MM/DD */
    function formatLgspDate(d: Date): string {
      const yyyy = d.getFullYear();
      const mm = String(d.getMonth() + 1).padStart(2, '0');
      const dd = String(d.getDate()).padStart(2, '0');
      return `${yyyy}/${mm}/${dd}`;
    }
    ```

    **B) Import thêm** (sau imports Plan 37-01):

    ```typescript
    import { createLgspRealService } from '../services/lgsp-real.service.js';
    import { getLgspService } from '../services/lgsp.service.js';
    ```

    **C) 3 endpoint mới** (thêm cuối router, trước export default):

    ```typescript
    // ============================================================
    // POST /lgsp-agency-config/:id/test — Test connection sandbox/prod thật
    //
    // Lookup credential decrypted → instantiate LGSPRealService riêng (KHÔNG dùng cache singleton)
    // → call receiveDocuments(now-1d, now) (lightweight read-only) → trả về kết quả
    // KHÔNG có side effect (KHÔNG INSERT incoming_docs, KHÔNG update last_synced_at)
    //
    // Response shape (CONTEXT D-02):
    //   { success: true, data: { ok, message, http_status, response_summary: { count } | null } }
    //
    // success luôn = true (request OK), data.ok = true/false tùy LGSP response
    // ============================================================
    router.post('/lgsp-agency-config/:id/test', async (req: Request, res: Response) => {
      try {
        const id = Number(req.params.id);
        const config = await lgspAgencyConfigRepository.getByIdWithDecryptedSecret(id);
        if (!config) {
          res.status(404).json({ success: false, message: 'Không tìm thấy cấu hình LGSP' });
          return;
        }

        // Tạo instance riêng (KHÔNG cache) — admin có thể edit form chưa save, test trước với credential tạm
        // (Note: hiện endpoint dùng credential ĐÃ LƯU; if cần test credential CHƯA LƯU thì
        //  frontend gọi PUT save trước → rồi POST test. Đơn giản hơn cho v3.2.)
        const svc = createLgspRealService({
          baseUrl: config.base_url,
          systemId: config.system_id,
          secretKey: config.secret_key_plaintext,
        });

        const yesterday = new Date(Date.now() - 24 * 60 * 60 * 1000);
        const today = new Date();

        try {
          const result = await svc.receiveDocuments(formatLgspDate(yesterday), formatLgspDate(today));
          res.json({
            success: true,
            data: {
              ok: true,
              message: 'Kết nối LGSP thành công',
              http_status: 200,
              response_summary: { count: Array.isArray(result) ? result.length : 0 },
            },
          });
        } catch (err) {
          // LGSP gọi được nhưng credential sai / endpoint down — request OK, test fail
          const errAny = err as { code?: string; message?: string; status?: number };
          const vnMessage =
            errAny.code
              ? `${errAny.message ?? 'Lỗi LGSP'} (Mã ${errAny.code})`
              : errAny.message ?? 'Không thể kết nối LGSP';
          res.json({
            success: true,
            data: {
              ok: false,
              message: vnMessage,
              http_status: errAny.status ?? 0,
              response_summary: null,
            },
          });
        }
      } catch (error) {
        handleDbError(error, res);
      }
    });

    // ============================================================
    // GET /lgsp-overview — Dashboard summary aggregated per DN
    // Trả về 6 row (1 per root unit có lgsp_org_code) + tổng quan today
    // ============================================================
    router.get('/lgsp-overview', async (_req: Request, res: Response) => {
      try {
        const rows = await lgspRepository.getOverviewStats();
        const totals = rows.reduce(
          (acc, r) => ({
            send_today: acc.send_today + Number(r.send_today_total ?? 0),
            send_success: acc.send_success + Number(r.send_today_success ?? 0),
            send_error: acc.send_error + Number(r.send_today_error ?? 0),
            receive_today: acc.receive_today + Number(r.receive_today_total ?? 0),
            outbox_pending: acc.outbox_pending + Number(r.outbox_today_pending ?? 0),
            outbox_error: acc.outbox_error + Number(r.outbox_today_error ?? 0),
            active_count: acc.active_count + (r.prod_is_active ? 1 : 0) + (r.sandbox_is_active ? 1 : 0),
          }),
          { send_today: 0, send_success: 0, send_error: 0, receive_today: 0, outbox_pending: 0, outbox_error: 0, active_count: 0 },
        );
        res.json({ success: true, data: { units: rows, totals } });
      } catch (error) {
        handleDbError(error, res);
      }
    });

    // ============================================================
    // POST /inter-organizations/sync — Batch sync danh sách từ LGSP
    //
    // Reuse Phase 18 service.syncOrganizations() qua getLgspService() default behavior.
    // Phase 37: lookup 1 DN active đầu tiên có lgsp_agency_config.is_active=TRUE để dùng credential
    // ============================================================
    router.post('/inter-organizations/sync', async (_req: Request, res: Response) => {
      try {
        // Lookup 1 DN active đầu tiên (any env) để dùng credential cho call /v1/getAgenciesList
        const allConfigs = await lgspAgencyConfigRepository.list();
        const activeConfig = allConfigs.find((c) => c.is_active);
        if (!activeConfig) {
          res.status(400).json({
            success: false,
            message: 'Cần ít nhất 1 cấu hình LGSP đang bật (is_active=TRUE) để gọi sync danh sách cơ quan ngoài',
          });
          return;
        }

        const svc = await getLgspService(Number(activeConfig.unit_id), activeConfig.environment);
        const orgs = await svc.syncOrganizations();

        let created = 0;
        let updated = 0;
        let failed = 0;
        for (const org of orgs) {
          const existing = await interOrganizationRepository.findByCode(org.org_code);
          if (existing) {
            const r = await interOrganizationRepository.updateForAdmin(existing.id, {
              name: org.org_name,
              address: org.address,
              email: org.email,
              phone: org.phone,
              isActive: true,
            });
            if (r.success) updated++; else failed++;
          } else {
            const r = await interOrganizationRepository.createForAdmin({
              code: org.org_code,
              name: org.org_name,
              lgspOrganId: org.org_code,
              address: org.address,
              email: org.email,
              phone: org.phone,
              isActive: true,
            });
            if (r.success) created++; else failed++;
          }
        }

        res.json({
          success: true,
          message: `Đồng bộ thành công: thêm mới ${created}, cập nhật ${updated}${failed > 0 ? `, lỗi ${failed}` : ''}`,
          data: { total: orgs.length, created, updated, failed },
        });
      } catch (error) {
        handleDbError(error, res);
      }
    });
    ```

    **LƯU Ý:**
    - Endpoint /test KHÔNG cache LGSP service (dùng createLgspRealService trực tiếp) — admin test sau khi UPDATE credential, cache đã invalidate ở PUT /lgsp-agency-config/:id
    - Endpoint /lgsp-overview dùng `_req` (underscore prefix) vì không đọc params
    - Endpoint /inter-organizations/sync block sequential await trong loop để dễ debug — không cần tối ưu cho ~50 cơ quan
  </action>
  <verify>
    <automated>cd e_office_app_new/backend && npx tsc --noEmit 2>&1 | grep "routes/admin-lgsp" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "router\.\(get\|post\|put\|patch\|delete\)" e_office_app_new/backend/src/routes/admin-lgsp.ts` → exactly 12 (9 từ Plan 37-01 + 3 mới)
    - `grep -q "/lgsp-agency-config/:id/test" e_office_app_new/backend/src/routes/admin-lgsp.ts` → 0
    - `grep -q "/lgsp-overview" e_office_app_new/backend/src/routes/admin-lgsp.ts` → 0
    - `grep -q "/inter-organizations/sync" e_office_app_new/backend/src/routes/admin-lgsp.ts` → 0
    - `grep -q "formatLgspDate\|createLgspRealService" e_office_app_new/backend/src/routes/admin-lgsp.ts` → 0
    - `grep -q "Kết nối LGSP thành công\|Đồng bộ thành công\|Không thể kết nối LGSP" e_office_app_new/backend/src/routes/admin-lgsp.ts` (Vietnamese diacritics) → 0
    - `cd e_office_app_new/backend && npx tsc --noEmit` → exit 0
    - `cd e_office_app_new/backend && npm run build` → exit 0
  </acceptance_criteria>
  <done>
    admin-lgsp.ts có đủ 12 endpoints. Test connection trả structure ổn định {ok, message, http_status, response_summary}. Overview trả 6 unit + totals aggregated. Inter-organizations sync reuse Phase 18 syncOrganizations qua getLgspService với active config đầu tiên. TS strict + production build PASS.
  </done>
</task>

</tasks>

<verification>
**Smoke test sau khi dev backend running:**
```bash
# 1. Test connection (sandbox active DN.001)
curl -s -X POST http://localhost:4000/api/admin/lgsp-agency-config/1/test \
  -H "Authorization: Bearer <admin_token>" | jq
# Expected: { success: true, data: { ok: true/false, message: "...", http_status: 200/0, response_summary: {...} | null } }

# 2. Overview
curl -s http://localhost:4000/api/admin/lgsp-overview \
  -H "Authorization: Bearer <admin_token>" | jq
# Expected: { success: true, data: { units: [...6 rows], totals: {...} } }

# 3. Sync (requires >=1 active config)
curl -s -X POST http://localhost:4000/api/admin/inter-organizations/sync \
  -H "Authorization: Bearer <admin_token>" | jq
# Expected: { success: true, message: "Đồng bộ thành công: thêm mới N, cập nhật M", data: {...} }
```
</verification>

<success_criteria>
- routes/admin-lgsp.ts có 12 endpoints
- lgsp.repository.ts có method getOverviewStats + interface LgspOverviewRow
- Test connection KHÔNG có side effect (KHÔNG INSERT/UPDATE)
- Test fail trả response ổn định (success=true, data.ok=false + Vietnamese error)
- Overview trả 6 row + totals object
- TS strict + production build backend PASS
</success_criteria>

<output>
After completion, create `.planning/phases/37-admin-ui-catalog-go-live/37-02-SUMMARY.md` per template.
</output>
