---
phase: 37-admin-ui-catalog-go-live
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/lgsp/cau-hinh/page.tsx   # NEW
autonomous: true
requirements: [LGSP-UI-01, LGSP-UI-02, LGSP-UI-03, LGSP-UI-04]
tags: [frontend, ui, admin, lgsp, cau-hinh, antd, phase-37]

must_haves:
  truths:
    - "Admin truy cập /lgsp/cau-hinh thấy bảng 12 row (6 DN × 2 env) với columns: DN, Mã LGSP, Môi trường, SystemId, base_url, Trạng thái (Switch), Lần đồng bộ cuối, Hành động (Sửa | Kiểm tra kết nối)"
    - "Click 'Sửa' mở Drawer 720 hiển thị form với fields: environment (Radio sandbox/prod read-only), system_id (Input maxLength=13), base_url (Input maxLength=500), secret_key (Input.Password placeholder 'Để trống nếu giữ nguyên'), is_active (Switch)"
    - "Save Drawer → gọi PUT /api/admin/lgsp-agency-config/:id → toast 'Đã cập nhật cấu hình LGSP' → table refresh"
    - "Click 'Kiểm tra kết nối' → Modal mở với spinner → gọi POST /api/admin/lgsp-agency-config/:id/test → display kết quả inline với Alert success/error + count VB sync được"
    - "Toggle Switch is_active inline trong table row (KHÔNG cần mở Drawer) → gọi PATCH /api/admin/lgsp-agency-config/:id/active → toast 'Đã bật/tắt kết nối LGSP'"
    - "Frontend TS strict không có NEW error (4 pre-existing TS2345 Phase 33-05 ignored per CLAUDE.md scope boundary)"
    - "Production build frontend PASS (npm run build exit 0)"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/lgsp/cau-hinh/page.tsx"
      provides: "Admin LGSP credential config page — Table 12 row + Edit Drawer + Test Connection Modal + active toggle"
      contains: "Cấu hình kết nối LGSP"
      min_lines: 350
  key_links:
    - from: "frontend/src/app/(main)/lgsp/cau-hinh/page.tsx"
      to: "GET /api/admin/lgsp-agency-config"
      via: "api.get on mount + after save/toggle"
      pattern: "api\\.get.*admin/lgsp-agency-config"
    - from: "frontend/src/app/(main)/lgsp/cau-hinh/page.tsx"
      to: "PUT /api/admin/lgsp-agency-config/:id"
      via: "api.put inside handleSave"
      pattern: "api\\.put.*admin/lgsp-agency-config"
    - from: "frontend/src/app/(main)/lgsp/cau-hinh/page.tsx"
      to: "POST /api/admin/lgsp-agency-config/:id/test"
      via: "api.post inside handleTestConnection"
      pattern: "api\\.post.*admin/lgsp-agency-config.*test"
    - from: "frontend/src/app/(main)/lgsp/cau-hinh/page.tsx"
      to: "PATCH /api/admin/lgsp-agency-config/:id/active"
      via: "api.patch inside handleToggleActive"
      pattern: "api\\.patch.*admin/lgsp-agency-config.*active"
---

<objective>
Tạo trang admin `/lgsp/cau-hinh` mirror pattern `/ky-so/cau-hinh` (1291 dòng closest analog) cho 12 row LGSP agency config (6 DN × 2 env): bảng list + Drawer edit credential + Modal test connection + Switch toggle active inline. Vietnamese diacritics ALL UI text, AntD 6 (Drawer size= không width=, Input.Password masked, Form validateTrigger='onSubmit').

Purpose: LGSP-UI-01 + 02 + 03 + 04. Admin có UI tự nhập credential thật từ Excel KH cung cấp (`QLVBDNAgencies.xlsx` + `List.txt`) thay vì SSH/SQL update direct.

Output: NEW file page.tsx ~400-500 dòng theo pattern admin của project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-admin-ui-catalog-go-live/37-CONTEXT.md
@.planning/phases/37-admin-ui-catalog-go-live/37-01-PLAN.md
@.planning/phases/37-admin-ui-catalog-go-live/37-02-PLAN.md

# Closest analog page (1291 lines per-row credential edit + test connection)
@e_office_app_new/frontend/src/app/(main)/ky-so/cau-hinh/page.tsx

# Other admin page pattern references
@e_office_app_new/frontend/src/app/(main)/lgsp/co-quan/page.tsx
@e_office_app_new/frontend/src/lib/api.ts

<interfaces>
Backend endpoints (Plan 37-01 + 37-02):

```typescript
// GET /api/admin/lgsp-agency-config → 12 rows
interface LgspAgencyConfigListRow {
  id: number;
  unit_id: number;
  unit_name: string;
  lgsp_org_code: string | null;
  environment: 'sandbox' | 'prod';
  system_id: string;
  base_url: string;
  is_active: boolean;
  last_synced_at: string | null;
  last_sync_error: string | null;
  created_at: string;
  updated_at: string;
  secret_key_masked: string;  // luôn '***'
}

// PUT /api/admin/lgsp-agency-config/:id
// Body: { systemId?, secretKey?, baseUrl?, environment? }
// secretKey rỗng → giữ nguyên ciphertext cũ

// PATCH /api/admin/lgsp-agency-config/:id/active
// Body: { is_active: boolean }

// POST /api/admin/lgsp-agency-config/:id/test
// Response: { success: true, data: { ok, message, http_status, response_summary: { count } | null } }
```

AntD 6 patterns trong project (đọc /ky-so/cau-hinh để xem):
- `Drawer size={720}` thay vì `width={720}`
- `Modal.confirm` cho destructive action — nhưng test/toggle KHÔNG destructive nên dùng inline
- Skeleton loading cho Table
- `App.useApp()` → `const { message, modal } = App.useApp()`
- Form validateTrigger='onSubmit'
- Vietnamese: 'Cấu hình kết nối', 'Sửa cấu hình', 'Kiểm tra kết nối', 'Đã bật/tắt', 'Sandbox' / 'Production'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tạo NEW page.tsx /lgsp/cau-hinh với Table + Edit Drawer + Test Modal + active toggle</name>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/ky-so/cau-hinh/page.tsx (đọc full file để học pattern Drawer + masked secret + test connection)
    - e_office_app_new/frontend/src/app/(main)/lgsp/co-quan/page.tsx (đọc full)
    - e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx (đọc full)
    - e_office_app_new/frontend/src/config/theme.ts (Deep Navy color)
  </read_first>
  <files>
    e_office_app_new/frontend/src/app/(main)/lgsp/cau-hinh/page.tsx
  </files>
  <action>
    **TẠO MỚI** file `e_office_app_new/frontend/src/app/(main)/lgsp/cau-hinh/page.tsx`. Cấu trúc skeleton sau (chi tiết implementation theo pattern /ky-so/cau-hinh):

    ```tsx
    'use client';

    /**
     * Trang: /lgsp/cau-hinh — Cấu hình kết nối LGSP (Admin only)
     *
     * Phase 37 Plan 37-03. REQ: LGSP-UI-01/02/03/04.
     *
     * Layout:
     *   1. Page header: title "Cấu hình kết nối LGSP" + alert info inline help
     *   2. Stat cards: số DN đang active, số sandbox active, số prod active, lần đồng bộ gần nhất
     *   3. Table 12 row (6 DN × 2 env): columns DN | Mã LGSP | Môi trường | SystemId | base_url | Trạng thái Switch | Lần đồng bộ cuối | Hành động
     *   4. Drawer 720 "Sửa cấu hình — DN.001 (sandbox)" với form
     *   5. Modal "Kiểm tra kết nối" hiển thị kết quả test
     *
     * Endpoints (Plan 37-01 + 37-02):
     *   GET    /api/admin/lgsp-agency-config              → load 12 row
     *   PUT    /api/admin/lgsp-agency-config/:id          → save form
     *   POST   /api/admin/lgsp-agency-config/:id/test     → test connection
     *   PATCH  /api/admin/lgsp-agency-config/:id/active   → toggle is_active
     */

    import React, { useState, useEffect, useCallback } from 'react';
    import {
      Card, Table, Button, Drawer, Form, Input, Switch, Tag, Space,
      Modal, Alert, Tooltip, Skeleton, App, Row, Col, Radio, Empty, Spin,
    } from 'antd';
    import type { ColumnsType } from 'antd/es/table';
    import {
      ApiOutlined, EditOutlined, ThunderboltOutlined, CheckCircleOutlined,
      CloseCircleOutlined, ReloadOutlined, SettingOutlined, KeyOutlined,
      LinkOutlined, ClockCircleOutlined,
    } from '@ant-design/icons';
    import { api } from '@/lib/api';
    import dayjs from 'dayjs';

    // ============================================================
    // Types — match GET /api/admin/lgsp-agency-config response
    // ============================================================
    interface LgspConfigRow {
      id: number;
      unit_id: number;
      unit_name: string;
      lgsp_org_code: string | null;
      environment: 'sandbox' | 'prod';
      system_id: string;
      base_url: string;
      is_active: boolean;
      last_synced_at: string | null;
      last_sync_error: string | null;
      created_at: string;
      updated_at: string;
      secret_key_masked: string;
    }

    interface TestResult {
      ok: boolean;
      message: string;
      http_status: number;
      response_summary: { count: number } | null;
    }

    interface FormValues {
      system_id?: string;
      base_url?: string;
      secret_key?: string;
      is_active?: boolean;
    }

    // ============================================================
    // Component
    // ============================================================
    export default function LgspCauHinhPage(): React.ReactElement {
      const { message: msg, modal } = App.useApp();
      const [data, setData] = useState<LgspConfigRow[]>([]);
      const [loading, setLoading] = useState<boolean>(true);
      const [drawerOpen, setDrawerOpen] = useState<boolean>(false);
      const [editingRow, setEditingRow] = useState<LgspConfigRow | null>(null);
      const [saving, setSaving] = useState<boolean>(false);
      const [testModalOpen, setTestModalOpen] = useState<boolean>(false);
      const [testingRow, setTestingRow] = useState<LgspConfigRow | null>(null);
      const [testing, setTesting] = useState<boolean>(false);
      const [testResult, setTestResult] = useState<TestResult | null>(null);
      const [form] = Form.useForm<FormValues>();

      // ── Fetch list ──────────────────────────────────────
      const fetchData = useCallback(async () => {
        setLoading(true);
        try {
          const { data: res } = await api.get('/admin/lgsp-agency-config');
          if (res?.success) setData(res.data || []);
          else msg.error(res?.message || 'Không tải được cấu hình LGSP');
        } catch {
          msg.error('Không tải được cấu hình LGSP');
        } finally {
          setLoading(false);
        }
      }, [msg]);

      useEffect(() => { fetchData(); }, [fetchData]);

      // ── Drawer open / close ─────────────────────────────
      const handleEdit = (row: LgspConfigRow) => {
        setEditingRow(row);
        form.setFieldsValue({
          system_id: row.system_id,
          base_url: row.base_url,
          secret_key: '',           // KHÔNG prefill — admin nhập mới nếu muốn thay đổi
          is_active: row.is_active,
        });
        setDrawerOpen(true);
      };

      const handleCloseDrawer = () => {
        setDrawerOpen(false);
        setEditingRow(null);
        form.resetFields();
      };

      // ── Save form ───────────────────────────────────────
      const handleSave = async () => {
        if (!editingRow) return;
        try {
          const values = await form.validateFields();
          setSaving(true);
          const { data: res } = await api.put(`/admin/lgsp-agency-config/${editingRow.id}`, {
            systemId: values.system_id?.trim(),
            secretKey: values.secret_key?.trim() || undefined,
            baseUrl: values.base_url?.trim(),
          });
          if (res?.success) {
            msg.success('Đã cập nhật cấu hình LGSP');
            handleCloseDrawer();
            fetchData();
          } else {
            msg.error(res?.message || 'Lưu thất bại');
          }
        } catch (err: unknown) {
          const errAny = err as { response?: { data?: { message?: string } } };
          msg.error(errAny.response?.data?.message || 'Lưu thất bại');
        } finally {
          setSaving(false);
        }
      };

      // ── Toggle active inline ────────────────────────────
      const handleToggleActive = async (row: LgspConfigRow, next: boolean) => {
        try {
          const { data: res } = await api.patch(`/admin/lgsp-agency-config/${row.id}/active`, {
            is_active: next,
          });
          if (res?.success) {
            msg.success(res.message || (next ? 'Đã bật kết nối LGSP' : 'Đã tắt kết nối LGSP'));
            fetchData();
          } else {
            msg.error(res?.message || 'Không thể cập nhật trạng thái');
          }
        } catch {
          msg.error('Không thể cập nhật trạng thái');
        }
      };

      // ── Test connection ─────────────────────────────────
      const handleOpenTest = (row: LgspConfigRow) => {
        setTestingRow(row);
        setTestResult(null);
        setTestModalOpen(true);
      };

      const handleRunTest = async () => {
        if (!testingRow) return;
        setTesting(true);
        setTestResult(null);
        try {
          const { data: res } = await api.post(`/admin/lgsp-agency-config/${testingRow.id}/test`);
          if (res?.success && res.data) {
            setTestResult(res.data as TestResult);
          } else {
            setTestResult({ ok: false, message: res?.message || 'Lỗi không xác định', http_status: 0, response_summary: null });
          }
        } catch (err: unknown) {
          const errAny = err as { response?: { data?: { message?: string } } };
          setTestResult({
            ok: false,
            message: errAny.response?.data?.message || 'Không thể kết nối tới backend',
            http_status: 0,
            response_summary: null,
          });
        } finally {
          setTesting(false);
        }
      };

      // ── Columns ─────────────────────────────────────────
      const columns: ColumnsType<LgspConfigRow> = [
        {
          title: 'Đơn vị',
          dataIndex: 'unit_name',
          width: 220,
          render: (val: string, row) => (
            <Space direction="vertical" size={0}>
              <span style={{ fontWeight: 600 }}>{val}</span>
              {row.lgsp_org_code && <Tag color="blue" style={{ fontSize: 11 }}>{row.lgsp_org_code}</Tag>}
            </Space>
          ),
        },
        {
          title: 'Môi trường',
          dataIndex: 'environment',
          width: 110,
          render: (val: 'sandbox' | 'prod') =>
            val === 'prod' ? <Tag color="red">Production</Tag> : <Tag color="orange">Sandbox</Tag>,
        },
        { title: 'SystemId', dataIndex: 'system_id', width: 140, ellipsis: true },
        {
          title: 'Base URL',
          dataIndex: 'base_url',
          ellipsis: true,
          render: (val: string) => <Tooltip title={val}><span style={{ fontFamily: 'monospace', fontSize: 12 }}>{val}</span></Tooltip>,
        },
        {
          title: 'Trạng thái',
          dataIndex: 'is_active',
          width: 110,
          render: (val: boolean, row) => (
            <Switch
              checked={val}
              checkedChildren="Bật"
              unCheckedChildren="Tắt"
              onChange={(next) => handleToggleActive(row, next)}
            />
          ),
        },
        {
          title: 'Đồng bộ cuối',
          dataIndex: 'last_synced_at',
          width: 160,
          render: (val: string | null, row) => (
            <Tooltip title={row.last_sync_error || undefined}>
              <Space direction="vertical" size={0}>
                {val
                  ? <span style={{ fontSize: 12 }}>{dayjs(val).format('DD/MM HH:mm')}</span>
                  : <span style={{ color: '#bfbfbf', fontSize: 12 }}>Chưa đồng bộ</span>}
                {row.last_sync_error && <Tag color="red" style={{ fontSize: 10 }}>Lỗi</Tag>}
              </Space>
            </Tooltip>
          ),
        },
        {
          title: 'Hành động',
          key: 'actions',
          width: 220,
          render: (_, row) => (
            <Space size={4}>
              <Button size="small" icon={<EditOutlined />} onClick={() => handleEdit(row)}>Sửa</Button>
              <Button size="small" icon={<ThunderboltOutlined />} onClick={() => handleOpenTest(row)}>Kiểm tra</Button>
            </Space>
          ),
        },
      ];

      // ── Stat counts ────────────────────────────────────
      const activeCount = data.filter((d) => d.is_active).length;
      const prodActive = data.filter((d) => d.is_active && d.environment === 'prod').length;
      const sandboxActive = data.filter((d) => d.is_active && d.environment === 'sandbox').length;

      // ── Render ─────────────────────────────────────────
      return (
        <>
          <div className="page-header">
            <h2 className="page-title">
              <SettingOutlined style={{ marginRight: 8, color: '#1B3A5C' }} />
              Cấu hình kết nối LGSP
            </h2>
            <Button icon={<ReloadOutlined />} onClick={fetchData}>Làm mới</Button>
          </div>

          <Alert
            type="info"
            showIcon
            style={{ marginBottom: 16 }}
            message="Cấu hình kết nối tỉnh Lạng Sơn"
            description={
              <>
                Mỗi đơn vị có 2 môi trường: <b>Sandbox</b> (kiểm thử) và <b>Production</b> (vận hành thật).
                Nhập <code>SystemId</code> + <code>SecretKey</code> từ tài liệu LGSP (file{' '}
                <code>QLVBDNAgencies.xlsx</code> hoặc <code>List.txt</code>) →{' '}
                <b>Kiểm tra kết nối</b> đạt → bật <b>Trạng thái</b> để cron tự động đồng bộ.
              </>
            }
          />

          <Row gutter={16} style={{ marginBottom: 16 }}>
            <Col xs={24} sm={8}>
              <Card size="small">
                <Space>
                  <ApiOutlined style={{ fontSize: 24, color: '#1B3A5C' }} />
                  <div>
                    <div style={{ fontSize: 12, color: '#8c8c8c' }}>Đang kết nối</div>
                    <div style={{ fontSize: 22, fontWeight: 700, color: '#1B3A5C' }}>{activeCount} / {data.length}</div>
                  </div>
                </Space>
              </Card>
            </Col>
            <Col xs={24} sm={8}>
              <Card size="small">
                <Space>
                  <CheckCircleOutlined style={{ fontSize: 24, color: '#059669' }} />
                  <div>
                    <div style={{ fontSize: 12, color: '#8c8c8c' }}>Production active</div>
                    <div style={{ fontSize: 22, fontWeight: 700, color: '#059669' }}>{prodActive}</div>
                  </div>
                </Space>
              </Card>
            </Col>
            <Col xs={24} sm={8}>
              <Card size="small">
                <Space>
                  <ClockCircleOutlined style={{ fontSize: 24, color: '#D97706' }} />
                  <div>
                    <div style={{ fontSize: 12, color: '#8c8c8c' }}>Sandbox active</div>
                    <div style={{ fontSize: 22, fontWeight: 700, color: '#D97706' }}>{sandboxActive}</div>
                  </div>
                </Space>
              </Card>
            </Col>
          </Row>

          <Card className="page-card">
            {loading ? (
              <Skeleton active paragraph={{ rows: 6 }} />
            ) : data.length === 0 ? (
              <Empty description="Chưa có cấu hình LGSP nào" />
            ) : (
              <Table<LgspConfigRow>
                rowKey="id"
                columns={columns}
                dataSource={data}
                pagination={false}
                scroll={{ x: 1100 }}
                size="small"
              />
            )}
          </Card>

          {/* ── Drawer Edit ── */}
          <Drawer
            title={editingRow ? `Sửa cấu hình — ${editingRow.unit_name} (${editingRow.environment === 'prod' ? 'Production' : 'Sandbox'})` : 'Sửa cấu hình'}
            size={720}
            open={drawerOpen}
            onClose={handleCloseDrawer}
            rootClassName="drawer-gradient"
            extra={
              <Space>
                <Button onClick={handleCloseDrawer}>Hủy</Button>
                <Button type="primary" loading={saving} onClick={handleSave}>Lưu</Button>
              </Space>
            }
          >
            <Form form={form} layout="vertical" validateTrigger="onSubmit" autoComplete="off">
              <Row gutter={16}>
                <Col span={12}>
                  <Form.Item label="Môi trường">
                    <Radio.Group value={editingRow?.environment} disabled>
                      <Radio.Button value="sandbox">Sandbox</Radio.Button>
                      <Radio.Button value="prod">Production</Radio.Button>
                    </Radio.Group>
                  </Form.Item>
                </Col>
                <Col span={12}>
                  <Form.Item label="Mã LGSP">
                    <Input value={editingRow?.lgsp_org_code ?? ''} disabled />
                  </Form.Item>
                </Col>
              </Row>

              <Form.Item
                label="SystemId"
                name="system_id"
                rules={[{ required: true, message: 'Bắt buộc nhập SystemId' }]}
              >
                <Input placeholder="VD: H37.DN.001" maxLength={13} />
              </Form.Item>

              <Form.Item
                label="Base URL"
                name="base_url"
                rules={[
                  { required: true, message: 'Bắt buộc nhập Base URL' },
                  { type: 'url', message: 'Base URL không hợp lệ' },
                ]}
              >
                <Input placeholder="VD: https://apiltvb.langson.gov.vn" maxLength={500} />
              </Form.Item>

              <Form.Item
                label={<span>SecretKey <span style={{ color: '#8c8c8c', fontWeight: 400 }}>(để trống nếu giữ nguyên)</span></span>}
                name="secret_key"
                extra="Backend tự encrypt bằng pgp_sym_encrypt trước khi lưu — plaintext KHÔNG bao giờ tồn tại trong DB"
              >
                <Input.Password
                  prefix={<KeyOutlined />}
                  placeholder="Để trống nếu giữ secret_key hiện tại"
                  autoComplete="new-password"
                  maxLength={500}
                />
              </Form.Item>

              <Alert
                type="warning"
                showIcon
                message="Lưu ý quan trọng"
                description={
                  <>
                    Sau khi lưu credential mới → bấm <b>Kiểm tra kết nối</b> để xác nhận{' '}
                    backend gọi được LGSP → nếu OK mới bật <b>Trạng thái</b> để cron tự đồng bộ.
                  </>
                }
              />
            </Form>
          </Drawer>

          {/* ── Modal Test Connection ── */}
          <Modal
            title={
              <Space>
                <ThunderboltOutlined style={{ color: '#1B3A5C' }} />
                <span>Kiểm tra kết nối — {testingRow?.unit_name} ({testingRow?.environment === 'prod' ? 'Production' : 'Sandbox'})</span>
              </Space>
            }
            open={testModalOpen}
            onCancel={() => { setTestModalOpen(false); setTestResult(null); setTestingRow(null); }}
            footer={[
              <Button key="close" onClick={() => { setTestModalOpen(false); setTestResult(null); setTestingRow(null); }}>
                Đóng
              </Button>,
              <Button key="run" type="primary" icon={<ThunderboltOutlined />} loading={testing} onClick={handleRunTest}>
                {testResult ? 'Kiểm tra lại' : 'Bắt đầu kiểm tra'}
              </Button>,
            ]}
            width={600}
          >
            <Alert
              type="info"
              showIcon
              style={{ marginBottom: 16 }}
              message="Cách thức kiểm tra"
              description={
                <>
                  Backend gọi <code>POST /v1/syncReceivedEdocList</code> với credential hiện tại (đọc 24h gần nhất, KHÔNG ghi DB).
                  Trả về tổng số VB có sẵn trên trục → nếu OK = credential đúng.
                </>
              }
            />
            {testing ? (
              <div style={{ textAlign: 'center', padding: 32 }}>
                <Spin size="large" />
                <div style={{ marginTop: 12, color: '#8c8c8c' }}>Đang kiểm tra kết nối tới LGSP...</div>
              </div>
            ) : testResult ? (
              testResult.ok ? (
                <Alert
                  type="success"
                  icon={<CheckCircleOutlined />}
                  showIcon
                  message="Kết nối thành công"
                  description={
                    <>
                      <div>{testResult.message}</div>
                      {testResult.response_summary && (
                        <div style={{ marginTop: 8 }}>
                          <b>Số VB trên trục 24h qua:</b> {testResult.response_summary.count}
                        </div>
                      )}
                      <div style={{ marginTop: 8, color: '#8c8c8c', fontSize: 12 }}>
                        HTTP Status: {testResult.http_status}
                      </div>
                    </>
                  }
                />
              ) : (
                <Alert
                  type="error"
                  icon={<CloseCircleOutlined />}
                  showIcon
                  message="Kết nối thất bại"
                  description={
                    <>
                      <div>{testResult.message}</div>
                      <div style={{ marginTop: 8, color: '#8c8c8c', fontSize: 12 }}>
                        HTTP Status: {testResult.http_status || 'Không phản hồi'}
                      </div>
                      <div style={{ marginTop: 12 }}>
                        <b>Gợi ý kiểm tra:</b>
                        <ul style={{ marginTop: 4, paddingLeft: 24 }}>
                          <li>SystemId + SecretKey khớp với <code>QLVBDNAgencies.xlsx</code> chưa?</li>
                          <li>Base URL có truy cập được từ server không (firewall / DNS)?</li>
                          <li>Tài khoản LGSP còn hiệu lực không?</li>
                        </ul>
                      </div>
                    </>
                  }
                />
              )
            ) : (
              <Empty description="Bấm 'Bắt đầu kiểm tra' để gọi LGSP" />
            )}
          </Modal>
        </>
      );
    }
    ```

    **LƯU Ý:**
    - File length ~400-500 dòng
    - Sử dụng CSS classes `page-header`, `page-title`, `page-card`, `drawer-gradient` đã có trong `globals.css`
    - Vietnamese diacritics ALL UI text
    - AntD 6 syntax: Drawer `size={720}`, Switch checkedChildren
    - Switch toggle inline trong table — KHÔNG cần confirm modal vì có thể undo dễ dàng
    - Input.Password `autoComplete="new-password"` (chống browser autofill)
    - secret_key field placeholder rõ ràng "Để trống nếu giữ nguyên"
    - validateTrigger='onSubmit' (per project convention)
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx tsc --noEmit 2>&1 | grep "lgsp/cau-hinh" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f e_office_app_new/frontend/src/app/\(main\)/lgsp/cau-hinh/page.tsx`
    - File length ≥ 350 lines: `wc -l e_office_app_new/frontend/src/app/\(main\)/lgsp/cau-hinh/page.tsx`
    - `grep -q "Cấu hình kết nối LGSP" e_office_app_new/frontend/src/app/\(main\)/lgsp/cau-hinh/page.tsx` (Vietnamese diacritics) → 0
    - `grep -q "Kiểm tra kết nối\|Kết nối thành công\|Kết nối thất bại" e_office_app_new/frontend/src/app/\(main\)/lgsp/cau-hinh/page.tsx` → 0
    - `grep -q "admin/lgsp-agency-config" e_office_app_new/frontend/src/app/\(main\)/lgsp/cau-hinh/page.tsx` (call endpoint) → 0
    - `grep -q "/test" e_office_app_new/frontend/src/app/\(main\)/lgsp/cau-hinh/page.tsx` (test endpoint) → 0
    - `grep -q "/active" e_office_app_new/frontend/src/app/\(main\)/lgsp/cau-hinh/page.tsx` (active endpoint) → 0
    - `grep -q "size={720}" e_office_app_new/frontend/src/app/\(main\)/lgsp/cau-hinh/page.tsx` (AntD 6 Drawer) → 0
    - `grep -q "Input.Password\|InputPassword" e_office_app_new/frontend/src/app/\(main\)/lgsp/cau-hinh/page.tsx` → 0
    - `grep -q "autoComplete=\"new-password\"" e_office_app_new/frontend/src/app/\(main\)/lgsp/cau-hinh/page.tsx` → 0
    - `cd e_office_app_new/frontend && npx tsc --noEmit 2>&1 | grep "lgsp/cau-hinh" | wc -l` → exactly 0 (zero NEW errors)
  </acceptance_criteria>
  <done>
    Page /lgsp/cau-hinh hoạt động độc lập với 4 luồng chính: load 12 row → Edit Drawer save → Test Connection Modal → Toggle active. Vietnamese diacritics ALL. AntD 6 syntax. Pattern mirror /ky-so/cau-hinh. Frontend TS strict không có NEW error.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Production build verify</name>
  <read_first>
    - CLAUDE.md (deploy pitfall #2 — NODE_ENV unset cho npm run build frontend)
  </read_first>
  <files></files>
  <action>
    Chạy production build frontend, verify thành công:

    ```powershell
    cd e_office_app_new/frontend
    Remove-Item Env:NODE_ENV -ErrorAction SilentlyContinue
    npm run build 2>&1 | Tee-Object $env:TEMP\frontend-build-37-03.log | Select-Object -Last 30
    ```

    Verify:
    - Exit code 0
    - `.next/build-manifest.json` tồn tại
    - Route `/lgsp/cau-hinh` xuất hiện trong build output (compiled OK)

    Nếu fail → in 50 dòng cuối log + identify error, fix, rebuild.
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd e_office_app_new/frontend && npm run build` exit 0
    - File `.next/build-manifest.json` tồn tại sau build
    - Build log có chứa `/lgsp/cau-hinh` (route compiled)
    - Zero NEW TS error in Phase 37-03 file
  </acceptance_criteria>
  <done>
    Production build frontend PASS. Page /lgsp/cau-hinh compile OK. Sẵn sàng cho Plan 37-06 unhide menu.
  </done>
</task>

</tasks>

<verification>
**Frontend TS check:**
```bash
cd e_office_app_new/frontend && npx tsc --noEmit | grep -v "ho-so-cong-viec\|van-ban-den/page.tsx\|van-ban-di/page.tsx\|van-ban-du-thao"
# Expected: empty (4 pre-existing Phase 33-05 errors filtered)
```

**Manual visual (defer Plan 37-07 E2E):**
1. Backend running dev + admin login
2. Navigate /lgsp/cau-hinh (temporary: edit hidden-routes.ts hoặc gõ URL trực tiếp)
3. Verify Table load 12 row với DN.001..006 × sandbox/prod
4. Click Sửa DN.001 sandbox → Drawer mở với form prefilled (secret_key trống)
5. Nhập secret_key thật → Lưu → toast 'Đã cập nhật cấu hình LGSP'
6. Click Kiểm tra DN.001 sandbox → Modal mở → Bắt đầu kiểm tra → spinner → Alert success/error
7. Toggle Switch is_active → toast 'Đã bật/tắt kết nối LGSP' → table refresh
</verification>

<success_criteria>
- NEW file page.tsx ≥ 350 lines, Vietnamese diacritics, AntD 6
- 4 luồng UI hoạt động: load list, edit drawer, test modal, toggle active
- Backend endpoints (Plan 37-01/02) được gọi đúng method + path
- Frontend TS strict không có NEW error
- Production build frontend PASS
</success_criteria>

<output>
After completion, create `.planning/phases/37-admin-ui-catalog-go-live/37-03-SUMMARY.md` per template.
</output>
