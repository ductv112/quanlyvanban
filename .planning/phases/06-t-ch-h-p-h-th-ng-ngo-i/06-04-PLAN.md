---
phase: 06-t-ch-h-p-h-th-ng-ngo-i
plan: 04
type: execute
wave: 4
depends_on: ["06-02", "06-03"]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx
  - e_office_app_new/frontend/src/app/(main)/lgsp/co-quan/page.tsx
  - e_office_app_new/frontend/src/components/signing/SigningModal.tsx
  - e_office_app_new/frontend/src/app/(main)/thong-bao-kenh/page.tsx
  - e_office_app_new/frontend/src/components/layout/MainLayout.tsx
autonomous: false
requirements:
  - LGSP-03
  - LGSP-04
  - SIGN-03
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04

must_haves:
  truths:
    - "User can view LGSP tracking list and trigger org sync"
    - "User can open signing modal on VB di/du thao, choose method, and see mock signed status"
    - "User can view and toggle notification channel preferences"
    - "Sidebar shows new menu items for LGSP and notification channels"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx"
      provides: "LGSP tracking list page"
      min_lines: 80
    - path: "e_office_app_new/frontend/src/components/signing/SigningModal.tsx"
      provides: "Signing modal component"
      min_lines: 100
    - path: "e_office_app_new/frontend/src/app/(main)/thong-bao-kenh/page.tsx"
      provides: "Notification channel preferences page"
      min_lines: 60
  key_links:
    - from: "e_office_app_new/frontend/src/components/signing/SigningModal.tsx"
      to: "/api/ky-so/sign/smart-ca"
      via: "api.post"
      pattern: "api\\.post.*ky-so"
    - from: "e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx"
      to: "/api/lgsp/tracking"
      via: "api.get"
      pattern: "api\\.get.*lgsp"
    - from: "e_office_app_new/frontend/src/app/(main)/thong-bao-kenh/page.tsx"
      to: "/api/thong-bao-kenh/preferences"
      via: "api.get/put"
      pattern: "api\\.(get|put).*thong-bao-kenh"
---

<objective>
Build frontend pages for LGSP tracking, signing modal, and notification preferences. Update sidebar navigation.

Purpose: Per D-03/D-06/D-07/D-09 — Users need UI to interact with LGSP, signing, and notification features. Per Sprint 15.3 — Signing modal must be reusable on VB di and VB du thao detail pages.
Output: LGSP pages, signing modal component, notification preferences page, sidebar updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-CONTEXT.md
@.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-02-SUMMARY.md
@.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-03-SUMMARY.md
@e_office_app_new/ROADMAP.md (Sprint 14, 15, 16 sections)

<interfaces>
<!-- Backend API endpoints from Plan 02 (LGSP) -->
GET  /api/lgsp/tracking?direction=&status=&page=&pageSize=  -> { data: LgspTrackingRow[], pagination }
GET  /api/lgsp/tracking/doc/:outgoingDocId                  -> { data: LgspTrackingByDocRow[] }
GET  /api/lgsp/organizations?search=&page=&pageSize=        -> { data: LgspOrgRow[], pagination }
POST /api/lgsp/organizations/sync                           -> { data: { synced_count } }
POST /api/lgsp/gui-lien-thong                               -> { data: { tracking_id } }  body: { outgoing_doc_id, dest_org_code, dest_org_name }

<!-- Backend API endpoints from Plan 03 (Signing) -->
POST /api/ky-so/sign/smart-ca     -> { data: SignResult }  body: { doc_id, doc_type, file_path }
POST /api/ky-so/sign/esign-neac   -> { data: SignResult }  body: { doc_id, doc_type, file_path, ca_provider }
POST /api/ky-so/sign/verify-otp   -> { data: SignResult }  body: { signature_id, otp }
GET  /api/ky-so/doc/:docId/:docType -> { data: DigitalSignatureRow[] }

<!-- Backend API endpoints from Plan 03 (Notification) -->
GET  /api/thong-bao-kenh/preferences              -> { data: NotificationPrefRow[] }
PUT  /api/thong-bao-kenh/preferences               -> { data: MutationResult }  body: { channel, is_enabled }
GET  /api/thong-bao-kenh/logs?channel=&send_status=&page=&pageSize= -> { data: NotificationLogRow[], pagination }
POST /api/thong-bao-kenh/device-tokens             -> { data: MutationResult }  body: { device_token, device_type }
POST /api/thong-bao-kenh/send-test                 -> { data: { message } }

<!-- Existing frontend patterns -->
From e_office_app_new/frontend/src/lib/api.ts:
```typescript
const api = axios.create({ baseURL: '/api', ... });
export default api;  // used as api.get(), api.post(), etc.
```

<!-- UI patterns from CLAUDE.md -->
- Drawer: size={720}, rootClassName="drawer-gradient"
- Table: Dropdown with MoreOutlined for actions
- Notifications: message.success/error via App.useApp()
- CSS: page-header, page-title, page-card classes
- Vietnamese with diacritics for all UI text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LGSP tracking page + organization page + signing modal component</name>
  <files>
    e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx
    e_office_app_new/frontend/src/app/(main)/lgsp/co-quan/page.tsx
    e_office_app_new/frontend/src/components/signing/SigningModal.tsx
  </files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/van-ban-lien-thong/page.tsx (similar list page pattern)
    - e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx (VB di page — signing button integration point)
    - e_office_app_new/frontend/src/lib/api.ts (axios instance)
    - e_office_app_new/frontend/src/app/globals.css (CSS classes: page-header, page-card, filter-row)
    - e_office_app_new/backend/src/repositories/lgsp.repository.ts (Row interfaces — field names to use in frontend)
    - e_office_app_new/backend/src/repositories/digital-signature.repository.ts (Row interfaces)
    - e_office_app_new/backend/src/routes/lgsp.ts (exact API paths)
    - e_office_app_new/backend/src/routes/digital-signature.ts (exact API paths)
  </read_first>
  <action>
**1. Create `frontend/src/app/(main)/lgsp/page.tsx`** — LGSP Tracking page:

'use client' page with:
- Page header: "Lien thong van ban" (with diacritics: "Lien thong van ban")
- Filter row: Select for `direction` ('send'/'receive'/all), Select for `status` (pending/processing/success/error/all)
- Table columns (from LgspTrackingRow — field names MUST match backend exactly):
  - `id` — hidden
  - `direction` — Tag: send = blue "Gui di", receive = green "Nhan ve"
  - `dest_org_name` — "Co quan" (with diacritics)
  - `lgsp_doc_id` — "Ma LGSP"
  - `status` — Tag color: pending=default, processing=blue, success=green, error=red
  - `sent_at` or `received_at` — "Thoi gian" formatted with dayjs
  - `created_at` — "Ngay tao"
  - `error_message` — show in Tooltip if exists
- Pagination with page/pageSize
- API: `api.get('/lgsp/tracking', { params: { direction, status, page, pageSize } })`

**2. Create `frontend/src/app/(main)/lgsp/co-quan/page.tsx`** — LGSP Organizations page:

'use client' page with:
- Page header: "Co quan lien thong" ("Co quan lien thong")
- Button "Dong bo" ("Dong bo") — calls `api.post('/lgsp/organizations/sync')`, shows message.success with synced count
- Search input for filtering
- Table columns (from LgspOrgRow):
  - `org_code` — "Ma co quan"
  - `org_name` — "Ten co quan"
  - `address` — "Dia chi"
  - `email` — "Email"
  - `phone` — "Dien thoai"
  - `is_active` — Tag: true=green "Hoat dong", false=red "Ngung"
  - `synced_at` — "Lan dong bo cuoi" formatted with dayjs
- API: `api.get('/lgsp/organizations', { params: { search, page, pageSize } })`

**3. Create `frontend/src/components/signing/SigningModal.tsx`** — Reusable signing modal per D-06/D-07:

Props:
```typescript
interface SigningModalProps {
  open: boolean;
  onClose: () => void;
  docId: number;
  docType: 'outgoing' | 'drafting';
  filePath: string;  // original file MinIO path
  onSigned?: () => void;  // callback after successful signing
}
```

Modal with 3 steps:
1. **Chon phuong thuc ky** ("Chon phuong thuc ky"): Radio group with 3 options:
   - SmartCA (VNPT) — icon SafetyCertificateOutlined
   - EsignNEAC — icon FileProtectOutlined
   - USB Token — icon UsbOutlined (disabled, show "Sap ho tro")
2. **Xac thuc** ("Xac thuc"): 
   - SmartCA: Show OTP input (Input with 6 digit), button "Xac nhan OTP"
   - EsignNEAC: Select CA provider (Viettel CA, VNPT CA, BKAV CA), button "Ky ngay"
3. **Ket qua** ("Ket qua"): Show success/error result with certificate info

State machine:
- `step`: 'choose' | 'verify' | 'result'
- `method`: 'smart_ca' | 'esign_neac' | 'usb_token'
- `signatureId`: number | null
- `loading`: boolean

API calls:
- SmartCA: `api.post('/ky-so/sign/smart-ca', { doc_id: docId, doc_type: docType, file_path: filePath })` -> get signature_id -> show OTP step -> `api.post('/ky-so/sign/verify-otp', { signature_id, otp })` -> show result
- EsignNEAC: `api.post('/ky-so/sign/esign-neac', { doc_id: docId, doc_type: docType, file_path: filePath, ca_provider })` -> show result directly

Display existing signatures below modal: `api.get('/ky-so/doc/' + docId + '/' + docType)` — show list with staff_name, sign_method, sign_status, signed_at.

Use Ant Design: Modal, Steps (or custom), Radio.Group, Input, Select, Button, Tag, Table.
All text in Vietnamese with diacritics.
  </action>
  <verify>
    <automated>cd D:/ClaudeProject/quanlyvanban/e_office_app_new/frontend && npx next lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/app/(main)/lgsp/page.tsx` exists with `api.get('/lgsp/tracking'`
    - File `frontend/src/app/(main)/lgsp/co-quan/page.tsx` exists with `api.post('/lgsp/organizations/sync'`
    - File `frontend/src/components/signing/SigningModal.tsx` exists with `interface SigningModalProps`
    - SigningModal calls `api.post('/ky-so/sign/smart-ca'` and `api.post('/ky-so/sign/esign-neac'`
    - SigningModal calls `api.post('/ky-so/sign/verify-otp'`
    - SigningModal accepts props: open, onClose, docId, docType, filePath
    - LGSP page uses field names: direction, dest_org_name, lgsp_doc_id, status, sent_at, received_at, created_at
    - All UI text is in Vietnamese with diacritics
    - No ESLint errors
  </acceptance_criteria>
  <done>LGSP tracking page, LGSP organizations page, and SigningModal component created with correct API calls and Vietnamese UI</done>
</task>

<task type="auto">
  <name>Task 2: Notification preferences page + sidebar nav update</name>
  <files>
    e_office_app_new/frontend/src/app/(main)/thong-bao-kenh/page.tsx
    e_office_app_new/frontend/src/components/layout/MainLayout.tsx
  </files>
  <read_first>
    - e_office_app_new/frontend/src/components/layout/MainLayout.tsx (sidebar menu items — add new entries)
    - e_office_app_new/frontend/src/app/(main)/thong-bao/page.tsx (existing notification page pattern)
    - e_office_app_new/backend/src/repositories/notification.repository.ts (Row interfaces — field names)
    - e_office_app_new/backend/src/routes/notification.ts (exact API paths)
  </read_first>
  <action>
**1. Create `frontend/src/app/(main)/thong-bao-kenh/page.tsx`** — Notification channel preferences per D-09:

'use client' page with:
- Page header: "Cau hinh thong bao" ("Cau hinh thong bao")
- Card "Kenh thong bao" ("Kenh thong bao"): List of 4 channels with Switch toggle:
  - FCM (Push notification) — icon BellOutlined
  - Zalo OA — icon MessageOutlined
  - SMS — icon MobileOutlined
  - Email — icon MailOutlined
  Each row: icon + channel name + description + Switch (checked = is_enabled)
  On Switch change: `api.put('/thong-bao-kenh/preferences', { channel, is_enabled })` then message.success

- Card "Lich su gui thong bao" ("Lich su gui thong bao"):
  - Filter: Select for channel, Select for send_status
  - Table with columns from NotificationLogRow:
    - `channel` — Tag (fcm=blue, zalo=cyan, sms=orange, email=green)
    - `event_type` — Text (map: new_doc="VB moi", task_assigned="Giao viec", deadline_warning="Sap het han", meeting_reminder="Nhac hop")
    - `title` — Text
    - `send_status` — Tag (pending=default, sent=green, failed=red)
    - `sent_at` — dayjs format
    - `error_message` — Tooltip if exists
  - API: `api.get('/thong-bao-kenh/logs', { params: { channel, send_status, page, pageSize } })`

- Load preferences on mount: `api.get('/thong-bao-kenh/preferences')`

**2. Update `frontend/src/components/layout/MainLayout.tsx`** — Add Phase 6 menu items to sidebar:

Read the current sidebar menu items array. Add new items in appropriate groups:

Under "Tich hop" or "He thong" group (create new group if needed):
```typescript
{
  key: 'tich-hop',
  label: 'Tich hop',
  icon: <ApiOutlined />,
  children: [
    { key: '/lgsp', label: 'Lien thong van ban', icon: <SwapOutlined /> },
    { key: '/lgsp/co-quan', label: 'Co quan lien thong', icon: <BankOutlined /> },
    { key: '/thong-bao-kenh', label: 'Cau hinh thong bao', icon: <NotificationOutlined /> },
  ],
}
```

Import required Ant Design icons at top: ApiOutlined, SwapOutlined, BankOutlined, NotificationOutlined.

NOTE: SigningModal is NOT a separate page — it's triggered from VB di / VB du thao detail pages. No sidebar entry needed for signing. The actual integration of SigningModal into VB di/du thao detail pages is a small wiring task that the executor should do if those pages exist and are accessible.
  </action>
  <verify>
    <automated>cd D:/ClaudeProject/quanlyvanban/e_office_app_new/frontend && npx next lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/app/(main)/thong-bao-kenh/page.tsx` exists
    - Contains `api.get('/thong-bao-kenh/preferences'`
    - Contains `api.put('/thong-bao-kenh/preferences'`
    - Contains `api.get('/thong-bao-kenh/logs'`
    - Contains Switch component for 4 channels (fcm, zalo, sms, email)
    - `MainLayout.tsx` contains menu item with key '/lgsp'
    - `MainLayout.tsx` contains menu item with key '/lgsp/co-quan'
    - `MainLayout.tsx` contains menu item with key '/thong-bao-kenh'
    - All UI text in Vietnamese with diacritics
    - No ESLint errors
  </acceptance_criteria>
  <done>Notification preferences page created, sidebar updated with LGSP and notification menu items</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Phase 6 integration</name>
  <action>Human verification checkpoint — no automated action. User verifies LGSP, signing, and notification UI end-to-end.</action>
  <what-built>
    LGSP integration (tracking list, org sync), Digital signing modal (SmartCA + EsignNEAC mock), Notification preferences with channel toggles, all with mock backends.
  </what-built>
  <how-to-verify>
    1. Start backend (`cd e_office_app_new/backend && npm run dev`) and frontend (`cd e_office_app_new/frontend && npm run dev`)
    2. Login and navigate to sidebar "Tich hop" group
    3. Click "Lien thong van ban" — verify tracking table loads (may be empty)
    4. Click "Co quan lien thong" — click "Dong bo" button — verify mock organizations appear
    5. Click "Cau hinh thong bao" — verify 4 channel switches load, toggle one and verify message.success
    6. Navigate to any VB di detail page — if signing button exists, click it and verify SigningModal opens with SmartCA/EsignNEAC options
    7. Check backend console for "MOCK:" log messages when actions are triggered
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /api/* | All API calls go through authenticated axios instance |
| SigningModal -> /api/ky-so | Signing actions require authentication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-12 | Info Disclosure | SigningModal | mitigate | Modal only shows signatures for the specific doc — no cross-doc leakage |
| T-06-13 | Spoofing | Notification preferences | mitigate | Backend uses staffId from JWT, not from request body |
</threat_model>

<verification>
Frontend builds and all pages render correctly. Sidebar shows new menu items. API calls reach backend and return data.
</verification>

<success_criteria>
LGSP tracking page shows mock data after sync. Signing modal opens and completes mock flow. Notification preferences page loads and toggles work. Sidebar navigation updated.
</success_criteria>

<output>
After completion, create `.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-04-SUMMARY.md`
</output>
