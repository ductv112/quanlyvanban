---
phase: 06-t-ch-h-p-h-th-ng-ngo-i
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx
  - e_office_app_new/frontend/src/app/(main)/lgsp/[id]/page.tsx
  - e_office_app_new/frontend/src/app/(main)/lgsp/co-quan/page.tsx
  - e_office_app_new/frontend/src/app/(main)/ky-so/page.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx
  - e_office_app_new/frontend/src/components/SigningModal.tsx
  - e_office_app_new/frontend/src/app/(main)/thong-bao-kenh/page.tsx
  - e_office_app_new/frontend/src/components/MainLayout.tsx
autonomous: false
requirements:
  - LGSP-02
  - LGSP-03
  - LGSP-04
  - SIGN-03
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04

must_haves:
  truths:
    - "User can see list of LGSP documents (sent/received) with status tracking"
    - "User can view LGSP agency directory"
    - "User can initiate digital signing from VB di or VB du thao detail page"
    - "Signing modal shows provider selection, mock OTP confirmation, and signed status"
    - "User can manage notification channel preferences"
    - "Sidebar navigation includes LGSP, signing, and notification sections"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx"
      provides: "LGSP document tracking list page"
      contains: "lgsp"
    - path: "e_office_app_new/frontend/src/components/SigningModal.tsx"
      provides: "Reusable signing modal component"
      contains: "SigningModal"
    - path: "e_office_app_new/frontend/src/app/(main)/thong-bao-kenh/page.tsx"
      provides: "Notification channel preferences page"
      contains: "notification"
  key_links:
    - from: "SigningModal.tsx"
      to: "/api/ky-so"
      via: "api.post('/ky-so/sign')"
      pattern: "ky-so"
    - from: "lgsp/page.tsx"
      to: "/api/lgsp"
      via: "api.get('/lgsp')"
      pattern: "/lgsp"
---

<objective>
Build frontend pages for LGSP tracking, digital signing UI, and notification preferences. Add signing buttons to VB di and VB du thao detail pages. Update sidebar navigation.

Purpose: Complete user-facing UI for all Phase 6 integrations per D-03, D-06, D-07, D-09.
Output: LGSP pages, signing modal, notification preferences page, sidebar updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-CONTEXT.md
@.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-02-SUMMARY.md
@.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-03-SUMMARY.md

@e_office_app_new/frontend/src/lib/api.ts
@e_office_app_new/frontend/src/components/MainLayout.tsx
@e_office_app_new/frontend/src/app/globals.css
@e_office_app_new/frontend/src/config/theme.ts
@e_office_app_new/frontend/src/config/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: LGSP pages + Signing modal + VB detail integration</name>
  <files>
    e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx,
    e_office_app_new/frontend/src/app/(main)/lgsp/[id]/page.tsx,
    e_office_app_new/frontend/src/app/(main)/lgsp/co-quan/page.tsx,
    e_office_app_new/frontend/src/app/(main)/ky-so/page.tsx,
    e_office_app_new/frontend/src/components/SigningModal.tsx,
    e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx,
    e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx
  </files>
  <read_first>
    - e_office_app_new/backend/src/routes/lgsp.ts (API endpoints: GET /api/lgsp, GET /api/lgsp/:id, GET /api/lgsp/agencies, POST /api/lgsp/agencies/sync, POST /api/lgsp/send)
    - e_office_app_new/backend/src/routes/signing.ts (API endpoints: GET /api/ky-so/certificates, POST /api/ky-so/sign, GET /api/ky-so/status/:txnId, GET /api/ky-so/doc/:docType/:docId, GET /api/ky-so)
    - e_office_app_new/backend/src/repositories/lgsp.repository.ts (Row interfaces — field names for frontend columns)
    - e_office_app_new/backend/src/repositories/signing.repository.ts (Row interfaces — field names)
    - e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx (existing VB di detail — add signing button)
    - e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx (existing VB du thao detail — add signing button)
    - e_office_app_new/frontend/src/app/(main)/van-ban-lien-thong/page.tsx (existing inter-incoming pattern for reference)
    - e_office_app_new/frontend/src/lib/api.ts (axios instance)
    - e_office_app_new/frontend/src/config/constants.ts (existing constants for status maps)
    - e_office_app_new/frontend/src/app/globals.css (page-header, page-card classes)
  </read_first>
  <action>
**Per D-03 (LGSP tracking UI), D-06 (signing UI from .NET), D-07 (mock signing flow):**

1. **`lgsp/page.tsx`** — LGSP document tracking list:
   - Page header: "Lien thong LGSP" with `page-header` / `page-title` CSS classes
   - Filter row: Tabs for direction (Tat ca / Gui di / Nhan ve), status Select (initial/processing/done/fail), search Input
   - Table columns (field names from `LgspDocumentRow`):
     - `notation` — "So ky hieu"
     - `subject` — "Trich yeu"
     - `from_agency_name` — "Co quan gui"
     - `to_agency_name` — "Co quan nhan"
     - `direction` — Tag: 'send'=blue 'receive'=green
     - `status` — Tag color: initial=default, processing=blue, done=green, fail=red
     - `created_at` — "Ngay" formatted with dayjs
     - Actions: Dropdown with "Xem chi tiet" linking to `/lgsp/${id}`
   - Pagination with `page`, `pageSize` query params
   - API: `api.get('/lgsp', { params: { direction, status, search, page, pageSize } })`

2. **`lgsp/[id]/page.tsx`** — LGSP document detail:
   - Descriptions component showing all fields from `fn_lgsp_document_get_by_id`
   - Status timeline showing status changes
   - EdXML content display (collapsible, code block)
   - Back button to list
   - API: `api.get('/lgsp/${id}')`

3. **`lgsp/co-quan/page.tsx`** — LGSP agency directory:
   - Page header: "Danh sach co quan lien thong"
   - Button "Dong bo" to trigger sync: `api.post('/lgsp/agencies/sync')`
   - Table columns from `LgspAgencyRow`: agency_code, agency_name, agency_type (Tag), level, email, synced_at
   - Search input, agency_type filter
   - API: `api.get('/lgsp/agencies', { params: { search, agency_type, page, pageSize } })`

4. **`ky-so/page.tsx`** — Digital signature list (admin view):
   - Page header: "Ky so dien tu"
   - Table columns from `DigitalSignatureListRow`: doc_type (Tag), staff_name, provider (Tag: VNPT=blue, NEAC=orange), status (Tag with colors), signed_at, created_at
   - Filter: status Select, provider Select
   - API: `api.get('/ky-so', { params: { status, provider, page, pageSize } })`

5. **`SigningModal.tsx`** — Reusable signing modal component:
   Props: `{ open, onClose, docType, docId, onSuccess }`
   
   Step 1: "Chon phuong thuc ky" — Radio group: VNPT SmartCA / EsignNEAC
   Step 2: "Xac nhan" — Show certificate info from `api.get('/ky-so/certificates')`, confirm button
   Step 3: "Dang ky..." — Spin loading, call `api.post('/ky-so/sign', { doc_type: docType, doc_id: docId, provider })`, then poll `api.get('/ky-so/status/${transactionId}')` every 2s until status !== 'PENDING'
   Step 4: "Ky thanh cong" — Show success result with CheckCircleOutlined icon, green text

   Use Ant Design Modal with Steps component for the wizard flow.
   On success: call `onSuccess()` to refresh parent page.

6. **Update `van-ban-di/[id]/page.tsx`**:
   - Add "Ky so" button (SafetyCertificateOutlined icon) in toolbar/action area
   - Import and render `<SigningModal docType="outgoing" docId={id} />`
   - Show existing signatures section below document detail: `api.get('/ky-so/doc/outgoing/${id}')` -> render list of signatures with status tags

7. **Update `van-ban-du-thao/[id]/page.tsx`**:
   - Same pattern as VB di: add "Ky so" button + SigningModal + signatures section
   - `docType="drafting"`

IMPORTANT:
- All text in Vietnamese with diacritics
- Use Ant Design 6 conventions: `size={720}` for Drawer, `App.useApp()` for message
- Field names MUST match backend Row interfaces exactly (snake_case)
- Use `dayjs` for date formatting
- Use existing CSS classes from globals.css
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'api/lgsp' e_office_app_new/frontend/src/app/(main)/lgsp/page.tsx` finds API call
    - `grep 'SigningModal' e_office_app_new/frontend/src/components/SigningModal.tsx` finds component
    - `grep 'SigningModal' e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx` finds import
    - `grep 'SigningModal' e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx` finds import
    - `grep 'ky-so/sign' e_office_app_new/frontend/src/components/SigningModal.tsx` finds signing API call
    - `grep 'ky-so/certificates' e_office_app_new/frontend/src/components/SigningModal.tsx` finds certificate fetch
    - `grep 'lgsp/agencies/sync' e_office_app_new/frontend/src/app/(main)/lgsp/co-quan/page.tsx` finds sync button
    - Frontend builds without errors
  </acceptance_criteria>
  <done>LGSP tracking pages (list + detail + agencies), signing modal with wizard flow, signing buttons on VB di/du thao detail pages, signature admin list page</done>
</task>

<task type="auto">
  <name>Task 2: Notification preferences page + sidebar navigation update</name>
  <files>
    e_office_app_new/frontend/src/app/(main)/thong-bao-kenh/page.tsx,
    e_office_app_new/frontend/src/components/MainLayout.tsx
  </files>
  <read_first>
    - e_office_app_new/backend/src/routes/notification.ts (API: GET /api/thong-bao-kenh/preferences, PUT /api/thong-bao-kenh/preferences/:channel, GET /api/thong-bao-kenh/logs)
    - e_office_app_new/backend/src/repositories/notification.repository.ts (NotificationPreferenceRow, NotificationLogRow — field names)
    - e_office_app_new/frontend/src/components/MainLayout.tsx (current sidebar menu items — add new sections)
    - e_office_app_new/frontend/src/app/globals.css (CSS classes)
    - e_office_app_new/frontend/src/config/constants.ts (existing constants)
  </read_first>
  <action>
**Per D-09 (notification preferences UI):**

1. **`thong-bao-kenh/page.tsx`** — Notification channel preferences + log:
   
   **Section 1: "Cai dat kenh thong bao"**
   - Card grid (2 columns on lg, 1 on mobile) showing 4 channels
   - Each card:
     - Icon + channel name (BellOutlined=FCM, MessageOutlined=Zalo, PhoneOutlined=SMS, MailOutlined=Email)
     - Switch toggle for is_enabled
     - Channel-specific input (shown when enabled):
       - FCM: disabled Input showing device_token (auto-detected)
       - Zalo: Input for zalo_user_id
       - SMS: Input for phone_number with pattern validation
       - Email: Input for email with type='email' validation
     - Save button per card
   - Load preferences: `api.get('/thong-bao-kenh/preferences')`
   - Save: `api.put('/thong-bao-kenh/preferences/${channel}', { is_enabled, device_token, phone_number, email, zalo_user_id })`

   **Section 2: "Lich su gui thong bao"** (collapsible)
   - Table with columns from NotificationLogRow: channel (Tag), title, body (truncated), status (Tag), sent_at, created_at
   - Filter by channel, status
   - API: `api.get('/thong-bao-kenh/logs', { params: { channel, status, page, pageSize } })`

2. **Update `MainLayout.tsx`** — add sidebar menu items:

   Add new menu group "Tich hop" (ApiOutlined icon) with children:
   - "Lien thong LGSP" -> `/lgsp` (SwapOutlined icon)
   - "Co quan lien thong" -> `/lgsp/co-quan` (BankOutlined icon)
   - "Ky so dien tu" -> `/ky-so` (SafetyCertificateOutlined icon)
   - "Kenh thong bao" -> `/thong-bao-kenh` (NotificationOutlined icon)

   Place this group after existing menu items, before any settings/admin section.
   
   Use `getItem()` helper if it exists in current MainLayout, or follow existing menu item pattern.

IMPORTANT:
- All labels in Vietnamese with diacritics
- Field names from backend Row interfaces (snake_case)
- Use `message.success('Cap nhat thanh cong')` from `App.useApp()`
- Icons from `@ant-design/icons`
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'thong-bao-kenh/preferences' e_office_app_new/frontend/src/app/(main)/thong-bao-kenh/page.tsx` finds API call
    - `grep 'thong-bao-kenh/logs' e_office_app_new/frontend/src/app/(main)/thong-bao-kenh/page.tsx` finds logs API
    - `grep -i 'lgsp' e_office_app_new/frontend/src/components/MainLayout.tsx` finds LGSP menu item
    - `grep 'ky-so' e_office_app_new/frontend/src/components/MainLayout.tsx` finds signing menu item
    - `grep 'thong-bao-kenh' e_office_app_new/frontend/src/components/MainLayout.tsx` finds notification menu item
    - `grep 'Tich hop' e_office_app_new/frontend/src/components/MainLayout.tsx` finds integration menu group label (Vietnamese)
    - Frontend builds without errors
  </acceptance_criteria>
  <done>Notification preferences page with 4 channel cards + log table, sidebar updated with Tich hop menu group containing 4 links</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Phase 6 integration UI end-to-end</name>
  <files>none</files>
  <action>
    Human verifies all Phase 6 integration UI pages work correctly:
    1. LGSP document tracking (list + detail + agency sync)
    2. Digital signing modal (wizard with mock OTP flow)
    3. Signing buttons on VB di and VB du thao detail pages
    4. Notification channel preferences with log history
    5. Sidebar navigation with "Tich hop" section
  </action>
  <what-built>
    Complete Phase 6 integration UI covering LGSP tracking, digital signing, and notification channels.
  </what-built>
  <how-to-verify>
    1. Navigate to Sidebar > "Tich hop" > "Lien thong LGSP" — verify table loads with mock data
    2. Click "Co quan lien thong" — verify agency list, click "Dong bo" button
    3. Navigate to any VB di detail page — verify "Ky so" button visible
    4. Click "Ky so" — verify modal wizard: select provider -> show certificate -> confirm -> success
    5. Navigate to "Ky so dien tu" page — verify signature list
    6. Navigate to "Kenh thong bao" — verify 4 channel cards with toggle switches
    7. Toggle a channel on/off and save — verify success message
    8. Check "Lich su gui thong bao" section — verify log table
  </how-to-verify>
  <verify>Human confirms all 8 verification steps pass</verify>
  <done>All Phase 6 UI pages render correctly, signing flow works, notification preferences save successfully</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend -> backend API | All calls authenticated via JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | S (Spoofing) | SigningModal | mitigate | Staff ID from JWT, not from frontend form data |
| T-06-12 | I (Info Disclosure) | notification preferences | mitigate | API only returns current user's preferences, scoped by JWT staffId |
</threat_model>

<verification>
Frontend builds. All pages render. API calls use correct endpoints and field names. Signing modal wizard flow works end-to-end with mock.
</verification>

<success_criteria>
- LGSP tracking pages display mock data correctly
- Signing modal wizard completes full flow: select -> confirm -> signing -> success
- VB di and VB du thao detail pages have signing buttons
- Notification preferences page shows 4 channels with toggle and input
- Sidebar has "Tich hop" menu group with 4 child items
</success_criteria>

<output>
After completion, create `.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-04-SUMMARY.md`
</output>
