---
phase: 06-t-ch-h-p-h-th-ng-ngo-i
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - e_office_app_new/backend/src/services/signing.service.ts
  - e_office_app_new/backend/src/services/signing-mock.service.ts
  - e_office_app_new/backend/src/repositories/digital-signature.repository.ts
  - e_office_app_new/backend/src/routes/digital-signature.ts
  - e_office_app_new/backend/src/repositories/notification.repository.ts
  - e_office_app_new/backend/src/routes/notification.ts
  - e_office_app_new/backend/src/server.ts
  - e_office_app_new/workers/src/index.ts
  - e_office_app_new/backend/src/lib/error-handler.ts
autonomous: true
requirements:
  - SIGN-01
  - SIGN-02
  - SIGN-03
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04

must_haves:
  truths:
    - "Signing API endpoints accept sign requests and return mock signed results"
    - "Notification API endpoints manage device tokens and preferences"
    - "All 4 notification workers (FCM, Zalo, SMS, Email) process jobs with mock logic"
    - "Error handler includes new constraint mappings for Phase 6 tables"
  artifacts:
    - path: "e_office_app_new/backend/src/services/signing.service.ts"
      provides: "Signing service interface"
      exports: ["ISigningService", "getSigningService"]
    - path: "e_office_app_new/backend/src/repositories/digital-signature.repository.ts"
      provides: "Digital signature repository"
      exports: ["digitalSignatureRepository"]
    - path: "e_office_app_new/backend/src/repositories/notification.repository.ts"
      provides: "Notification repository"
      exports: ["notificationRepository"]
    - path: "e_office_app_new/backend/src/routes/digital-signature.ts"
      provides: "Signing API routes"
    - path: "e_office_app_new/backend/src/routes/notification.ts"
      provides: "Notification API routes"
  key_links:
    - from: "e_office_app_new/backend/src/routes/digital-signature.ts"
      to: "e_office_app_new/backend/src/services/signing.service.ts"
      via: "getSigningService()"
      pattern: "getSigningService"
    - from: "e_office_app_new/backend/src/routes/notification.ts"
      to: "e_office_app_new/backend/src/repositories/notification.repository.ts"
      via: "import notificationRepository"
      pattern: "notificationRepository"
    - from: "e_office_app_new/workers/src/index.ts"
      to: "edoc.fn_notification_log_update_status"
      via: "pool.query after mock send"
      pattern: "fn_notification_log_update_status"
---

<objective>
Build signing service (SmartCA + EsignNEAC mock) with API routes, notification repository + routes, and implement all 4 notification workers with mock logic.

Purpose: Per D-05/D-06/D-07 — signing must be fully mocked with real interfaces. Per D-08/D-09/D-10 — all notification channels mocked with BullMQ workers.
Output: Signing + Notification API endpoints, all workers implemented with mock send logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-CONTEXT.md
@.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-01-SUMMARY.md
@.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-02-SUMMARY.md
@e_office_app_new/ROADMAP.md (Sprint 15, 16 sections)

<interfaces>
<!-- DB helpers -->
From e_office_app_new/backend/src/lib/db/query.ts:
```typescript
export async function callFunction<T>(functionName: string, params: unknown[]): Promise<T[]>;
export async function callFunctionOne<T>(functionName: string, params: unknown[]): Promise<T | null>;
```

<!-- Queue client (created in Plan 02) -->
From e_office_app_new/backend/src/lib/queue/client.ts:
```typescript
export const notificationQueue: Queue;
export const emailQueue: Queue;
export const smsQueue: Queue;
export const fcmQueue: Queue;
export const zaloQueue: Queue;
```

<!-- SP names from migration 020 (digital signing) -->
SP: edoc.fn_digital_signature_create(p_doc_id, p_doc_type, p_staff_id, p_sign_method, p_original_file_path)
SP: edoc.fn_digital_signature_update_status(p_id, p_sign_status, p_certificate_serial, p_certificate_subject, p_certificate_issuer, p_signed_file_path, p_error_message)
SP: edoc.fn_digital_signature_get_by_doc(p_doc_id, p_doc_type)
SP: edoc.fn_digital_signature_get_by_id(p_id)

<!-- SP names from migration 021 (notifications) -->
SP: edoc.fn_device_token_upsert(p_staff_id, p_device_token, p_device_type)
SP: edoc.fn_device_token_get_by_staff(p_staff_id)
SP: edoc.fn_device_token_delete(p_id, p_staff_id)
SP: edoc.fn_notification_log_create(p_staff_id, p_channel, p_event_type, p_title, p_body, p_ref_type, p_ref_id)
SP: edoc.fn_notification_log_update_status(p_id, p_send_status, p_error_message)
SP: edoc.fn_notification_log_get_list(p_staff_id, p_channel, p_send_status, p_page, p_page_size)
SP: edoc.fn_notification_pref_upsert(p_staff_id, p_channel, p_is_enabled)
SP: edoc.fn_notification_pref_get_by_staff(p_staff_id)

<!-- Error handler pattern -->
From e_office_app_new/backend/src/lib/error-handler.ts:
```typescript
export function handleDbError(error: unknown, res: Response): void;
// Add new constraint names for Phase 6 tables
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Signing service interface/mock + repository + routes</name>
  <files>
    e_office_app_new/backend/src/services/signing.service.ts
    e_office_app_new/backend/src/services/signing-mock.service.ts
    e_office_app_new/backend/src/repositories/digital-signature.repository.ts
    e_office_app_new/backend/src/routes/digital-signature.ts
  </files>
  <read_first>
    - e_office_app_new/backend/src/services/auth.service.ts (service pattern)
    - e_office_app_new/backend/src/repositories/meeting.repository.ts (recent repository pattern)
    - e_office_app_new/backend/src/routes/meeting.ts (recent route pattern)
    - e_office_app_new/database/migrations/020_sprint15_digital_signing.sql (SP names and RETURNS TABLE columns)
    - e_office_app_new/ROADMAP.md (Sprint 15 section — lines 1001-1019)
  </read_first>
  <action>
**1. Create `backend/src/services/signing.service.ts`** — Interface + factory per D-05:

```typescript
export interface ISigningService {
  signSmartCA(params: SmartCASignParams): Promise<SignResult>;
  signEsignNEAC(params: EsignNEACParams): Promise<SignResult>;
  verifyOTP(signatureId: number, otp: string): Promise<SignResult>;
}

export interface SmartCASignParams {
  doc_id: number;
  doc_type: 'outgoing' | 'drafting';
  staff_id: number;
  file_path: string;  // MinIO path of original file
}

export interface EsignNEACParams {
  doc_id: number;
  doc_type: 'outgoing' | 'drafting';
  staff_id: number;
  file_path: string;
  ca_provider: string;  // e.g. 'viettel-ca', 'vnpt-ca', 'bkav-ca'
}

export interface SignResult {
  success: boolean;
  signature_id?: number;
  message: string;
  requires_otp?: boolean;  // SmartCA requires OTP step
  certificate_serial?: string;
  certificate_subject?: string;
  signed_file_path?: string;
}

export async function getSigningService(): Promise<ISigningService> {
  if (process.env.MOCK_EXTERNAL === 'true') {
    const mod = await import('./signing-mock.service.js');
    return mod.signingMockService;
  }
  throw new Error('Real signing service not implemented yet');
}
```

**2. Create `backend/src/services/signing-mock.service.ts`** — Mock per D-07:

Implement ISigningService:
- `signSmartCA()`: Log "MOCK: Initiating SmartCA signing for doc {doc_id}". Return `{ success: true, requires_otp: true, message: 'MOCK: OTP sent to registered phone' }`. Set signature status to 'signing' in DB.
- `verifyOTP()`: Log "MOCK: Verifying OTP for signature {signatureId}". Return `{ success: true, message: 'MOCK: Document signed successfully', certificate_serial: 'MOCK-CERT-' + Date.now(), certificate_subject: 'CN=Nguyen Van A, O=UBND tinh Lao Cai', signed_file_path: 'signed/' + uuid + '.pdf' }`. Update signature status to 'signed'.
- `signEsignNEAC()`: Log "MOCK: EsignNEAC signing for doc {doc_id} via {ca_provider}". Return immediate success (no OTP step): `{ success: true, certificate_serial: ..., certificate_subject: ..., signed_file_path: ..., message: 'MOCK: Document signed via EsignNEAC' }`.

Both methods call `digitalSignatureRepository.create()` and `digitalSignatureRepository.updateStatus()` to persist state.

**3. Create `backend/src/repositories/digital-signature.repository.ts`** — Per D-00b:

Row interfaces matching SP output EXACTLY:
- `DigitalSignatureRow`: { id, doc_id, doc_type, staff_id, staff_name, sign_method, certificate_serial, certificate_subject, certificate_issuer, signed_file_path, original_file_path, sign_status, error_message, signed_at, created_at }
- `MutationResultRow`: { success, message, id }

Methods:
- `create(docId, docType, staffId, signMethod, originalFilePath)` calls `edoc.fn_digital_signature_create`
- `updateStatus(id, signStatus, certSerial, certSubject, certIssuer, signedFilePath, errorMessage)` calls `edoc.fn_digital_signature_update_status`
- `getByDoc(docId, docType)` calls `edoc.fn_digital_signature_get_by_doc` — returns array
- `getById(id)` calls `edoc.fn_digital_signature_get_by_id` — returns single or null

**4. Create `backend/src/routes/digital-signature.ts`** — Per D-06, Sprint 15.3:

Endpoints:
- `POST /sign/smart-ca` — Body: `{ doc_id, doc_type, file_path }`. Uses req.user.staffId. Calls getSigningService().signSmartCA(). Returns 201.
- `POST /sign/esign-neac` — Body: `{ doc_id, doc_type, file_path, ca_provider }`. Calls getSigningService().signEsignNEAC(). Returns 201.
- `POST /sign/verify-otp` — Body: `{ signature_id, otp }`. Calls getSigningService().verifyOTP(). Returns 200.
- `GET /doc/:docId/:docType` — Get signatures for a document. Calls digitalSignatureRepository.getByDoc(). Returns 200.
- `GET /:id` — Get single signature. Calls digitalSignatureRepository.getById(). Returns 200 or 404.

Use handleDbError for catch blocks.
  </action>
  <verify>
    <automated>cd D:/ClaudeProject/quanlyvanban/e_office_app_new/backend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/src/services/signing.service.ts` exports ISigningService, getSigningService, SmartCASignParams, EsignNEACParams, SignResult
    - File `backend/src/services/signing-mock.service.ts` exports signingMockService implementing ISigningService
    - Mock logs contain "MOCK:" prefix
    - File `backend/src/repositories/digital-signature.repository.ts` calls exact SP names: `edoc.fn_digital_signature_create`, `edoc.fn_digital_signature_update_status`, `edoc.fn_digital_signature_get_by_doc`, `edoc.fn_digital_signature_get_by_id`
    - File `backend/src/routes/digital-signature.ts` has 5 endpoints: POST /sign/smart-ca, POST /sign/esign-neac, POST /sign/verify-otp, GET /doc/:docId/:docType, GET /:id
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Signing service interface + mock + repository + routes all created, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Notification repository + routes + worker implementations + error-handler update + server.ts mount</name>
  <files>
    e_office_app_new/backend/src/repositories/notification.repository.ts
    e_office_app_new/backend/src/routes/notification.ts
    e_office_app_new/backend/src/lib/error-handler.ts
    e_office_app_new/backend/src/server.ts
    e_office_app_new/workers/src/index.ts
  </files>
  <read_first>
    - e_office_app_new/backend/src/repositories/meeting.repository.ts (repository pattern)
    - e_office_app_new/backend/src/routes/meeting.ts (route pattern)
    - e_office_app_new/backend/src/lib/error-handler.ts (current error handler — add new constraints)
    - e_office_app_new/backend/src/server.ts (current route mounting — read FRESH to see Plan 02 additions)
    - e_office_app_new/workers/src/index.ts (current workers — read FRESH to see Plan 02 additions)
    - e_office_app_new/backend/src/lib/queue/client.ts (queue client created by Plan 02 — import queues for notification enqueue)
    - e_office_app_new/database/migrations/021_sprint16_notifications.sql (SP names and columns)
  </read_first>
  <action>
**1. Create `backend/src/repositories/notification.repository.ts`** — Per D-00b:

Row interfaces matching SP output EXACTLY:
- `DeviceTokenRow`: { id, device_token, device_type, is_active, created_at }
- `NotificationLogRow`: { id, staff_id, channel, event_type, title, body, ref_type, ref_id, send_status, error_message, sent_at, created_at, total_count }
- `NotificationPrefRow`: { id, channel, is_enabled }
- `MutationResultRow`: { success, message, id }

Methods:
- `upsertDeviceToken(staffId, deviceToken, deviceType)` calls `edoc.fn_device_token_upsert`
- `getDeviceTokensByStaff(staffId)` calls `edoc.fn_device_token_get_by_staff`
- `deleteDeviceToken(id, staffId)` calls `edoc.fn_device_token_delete`
- `createLog(staffId, channel, eventType, title, body, refType, refId)` calls `edoc.fn_notification_log_create`
- `updateLogStatus(id, sendStatus, errorMessage)` calls `edoc.fn_notification_log_update_status`
- `getLogList(staffId, channel, sendStatus, page, pageSize)` calls `edoc.fn_notification_log_get_list`
- `upsertPreference(staffId, channel, isEnabled)` calls `edoc.fn_notification_pref_upsert`
- `getPreferences(staffId)` calls `edoc.fn_notification_pref_get_by_staff`

**2. Create `backend/src/routes/notification.ts`**:

Endpoints:
- `POST /device-tokens` — Register device token. Body: `{ device_token, device_type }`. Uses req.user.staffId. Returns 201.
- `GET /device-tokens` — List user's device tokens. Returns 200.
- `DELETE /device-tokens/:id` — Remove device token. Uses req.user.staffId for ownership check. Returns 200.
- `GET /logs` — List notification logs for current user. Query: `channel`, `send_status`, `page`, `pageSize`. Returns 200.
- `GET /preferences` — Get user's notification preferences. Returns 200.
- `PUT /preferences` — Update preferences. Body: `{ channel, is_enabled }`. Returns 200.
- `POST /send-test` — Debug endpoint: enqueue a test notification to all 4 channels. Creates notification_log records and enqueues to each queue (import from `@/lib/queue/client.js`). Returns 201.

**3. Update `backend/src/lib/error-handler.ts`**:
Add to messageMap:
```typescript
'uq_lgsp_org_code': 'Ma co quan LGSP da ton tai',
'uq_device_token': 'Device token da duoc dang ky',
'uq_notif_pref_staff_channel': 'Cau hinh kenh thong bao da ton tai',
```

**4. Update `backend/src/server.ts`** — Read FRESH (Plan 02 already added lgspRoutes):
Add imports and mounts in Phase 6 section (after lgspRoutes added by Plan 02):
```typescript
import digitalSignatureRoutes from './routes/digital-signature.js';
import notificationRoutes from './routes/notification.js';

app.use('/api/ky-so', authenticate, digitalSignatureRoutes);
app.use('/api/thong-bao-kenh', authenticate, notificationRoutes);
```

**5. Update `workers/src/index.ts`** — Read FRESH (Plan 02 already added lgsp-send, zalo-send, pg Pool):

Implement mock logic for all notification workers per D-08/D-10. Each worker:
1. Reads job data: `{ staff_id, channel, event_type, title, body, ref_type, ref_id, notification_log_id }`
2. Logs mock send: `logger.info({ ... }, 'MOCK: Sending {channel} notification to staff {staff_id}')`
3. Updates notification_log status via `pool.query('SELECT * FROM edoc.fn_notification_log_update_status($1, $2, $3)', [job.data.notification_log_id, 'sent', null])`

Workers to update:
- `email-send`: Log "MOCK: Email sent to {to} — Subject: {title}"
- `sms-send`: Log "MOCK: SMS sent — {body}"
- `fcm-push`: Log "MOCK: FCM push sent to device token"
- `zalo-send` (already added by Plan 02 as stub): Update with mock logic + notification_log update

Each worker updates notification_log status to 'sent' on success, 'failed' on error.
  </action>
  <verify>
    <automated>cd D:/ClaudeProject/quanlyvanban/e_office_app_new/backend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/src/repositories/notification.repository.ts` calls exact SP names: `edoc.fn_device_token_upsert`, `edoc.fn_notification_log_create`, `edoc.fn_notification_pref_upsert`, etc.
    - File `backend/src/routes/notification.ts` has 7 endpoints: POST/GET/DELETE device-tokens, GET logs, GET/PUT preferences, POST send-test
    - `error-handler.ts` contains `'uq_lgsp_org_code'`, `'uq_device_token'`, `'uq_notif_pref_staff_channel'`
    - `server.ts` contains `import digitalSignatureRoutes` and `import notificationRoutes`
    - `server.ts` contains `app.use('/api/ky-so'` and `app.use('/api/thong-bao-kenh'`
    - `workers/src/index.ts` contains "MOCK: Email sent", "MOCK: SMS sent", "MOCK: FCM push sent", "MOCK: Zalo OA message sent"
    - `workers/src/index.ts` contains `edoc.fn_notification_log_update_status`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Notification repository + routes created, all 4 notification workers have mock implementations, error-handler updated, server.ts mounts all Phase 6 routes, TypeScript compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/ky-so | Signing endpoints require authentication |
| client -> /api/thong-bao-kenh | Notification endpoints require authentication |
| worker -> external APIs | All external calls are mocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-08 | Spoofing | POST /sign/smart-ca | mitigate | authenticate middleware ensures staffId from JWT, not request body |
| T-06-09 | Elevation | DELETE /device-tokens/:id | mitigate | SP receives p_staff_id from JWT — cannot delete other user's tokens |
| T-06-10 | Info Disclosure | GET /logs | mitigate | Logs filtered by req.user.staffId — user only sees own notifications |
| T-06-11 | Tampering | POST /send-test | accept | Debug endpoint — disable in production or add admin role check |
</threat_model>

<verification>
```bash
cd e_office_app_new/backend && npx tsc --noEmit
grep -n "ky-so\|thong-bao-kenh\|lgsp" e_office_app_new/backend/src/server.ts
grep -c "MOCK:" e_office_app_new/workers/src/index.ts
```
</verification>

<success_criteria>
Signing and Notification backend fully functional. 5 signing endpoints + 7 notification endpoints mounted. All workers have mock implementations. Error handler updated.
</success_criteria>

<output>
After completion, create `.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-03-SUMMARY.md`
</output>
