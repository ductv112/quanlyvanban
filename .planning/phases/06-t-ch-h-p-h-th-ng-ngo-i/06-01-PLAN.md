---
phase: 06-t-ch-h-p-h-th-ng-ngo-i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/database/migrations/019_sprint14_lgsp_integration.sql
  - e_office_app_new/database/migrations/020_sprint15_digital_signatures.sql
  - e_office_app_new/database/migrations/021_sprint16_notification_channels.sql
autonomous: true
requirements:
  - LGSP-01
  - LGSP-02
  - LGSP-03
  - LGSP-04
  - SIGN-01
  - SIGN-02
  - SIGN-03
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04

must_haves:
  truths:
    - "LGSP tracking tables exist for send/receive document lifecycle"
    - "Digital signature records can be stored per document"
    - "Notification logs track delivery status across all 4 channels"
    - "LGSP agency sync table stores external organization directory"
  artifacts:
    - path: "e_office_app_new/database/migrations/019_sprint14_lgsp_integration.sql"
      provides: "LGSP tables + stored functions for send/receive/track/agency sync"
      contains: "edoc.lgsp_documents"
    - path: "e_office_app_new/database/migrations/020_sprint15_digital_signatures.sql"
      provides: "Digital signature tables + stored functions"
      contains: "edoc.digital_signatures"
    - path: "e_office_app_new/database/migrations/021_sprint16_notification_channels.sql"
      provides: "Notification channel tables + stored functions"
      contains: "edoc.notification_logs"
  key_links:
    - from: "edoc.lgsp_documents"
      to: "edoc.outgoing_docs"
      via: "outgoing_doc_id FK"
      pattern: "REFERENCES edoc.outgoing_docs"
    - from: "edoc.digital_signatures"
      to: "edoc.outgoing_docs"
      via: "doc_id + doc_type FK"
      pattern: "edoc.digital_signatures"
    - from: "edoc.notification_logs"
      to: "public.staff"
      via: "staff_id FK"
      pattern: "REFERENCES public.staff"
---

<objective>
Create all database tables and stored functions for Phase 6: LGSP integration tracking, digital signature records, notification channel logs, and LGSP agency directory.

Purpose: Database layer must be complete and verified before any backend/frontend work begins (per D-00e).
Output: 3 migration SQL files covering Sprint 14 (LGSP), Sprint 15 (Signatures), Sprint 16 (Notifications).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-CONTEXT.md

@e_office_app_new/database/migrations/018_sprint13_meetings.sql
@e_office_app_new/backend/src/lib/db/query.ts
@e_office_app_new/backend/src/lib/error-handler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 019 — LGSP integration tables and stored functions</name>
  <files>e_office_app_new/database/migrations/019_sprint14_lgsp_integration.sql</files>
  <read_first>
    - e_office_app_new/database/migrations/018_sprint13_meetings.sql (continue numbering pattern)
    - e_office_app_new/database/migrations/002_edoc_tables.sql (edoc schema pattern)
    - docs/source_code_cu/sources/LGSP-LANGSON-API-GUIDE.md (LGSP API structure: docId, status codes, agency fields)
    - docs/source_code_cu/sources/LGSP-Token-Manager-Example.cs (token management pattern)
  </read_first>
  <action>
Create migration file `019_sprint14_lgsp_integration.sql` with:

**Per D-01, D-02, D-03, D-04: LGSP tables and SPs**

1. **Table `edoc.lgsp_config`** — LGSP connection configuration per unit:
   - `id BIGSERIAL PRIMARY KEY`
   - `unit_id BIGINT NOT NULL REFERENCES public.departments(id)`
   - `endpoint VARCHAR(500) NOT NULL` — LGSP API base URL
   - `username VARCHAR(200) NOT NULL`
   - `password_encrypted VARCHAR(500) NOT NULL`
   - `application_code VARCHAR(200) NOT NULL`
   - `system_id VARCHAR(100) NOT NULL` — e.g. '001.21.07.N92'
   - `secret_key VARCHAR(500) NOT NULL`
   - `is_active BOOLEAN DEFAULT true`
   - `created_by BIGINT NOT NULL`
   - `created_at TIMESTAMP DEFAULT NOW()`
   - `updated_at TIMESTAMP DEFAULT NOW()`
   - UNIQUE constraint: `uq_lgsp_config_unit` on (unit_id)

2. **Table `edoc.lgsp_agencies`** — synced external agencies directory (per D-04):
   - `id BIGSERIAL PRIMARY KEY`
   - `agency_code VARCHAR(100) NOT NULL` — e.g. 'J26'
   - `agency_name VARCHAR(500) NOT NULL`
   - `agency_type VARCHAR(50)` — 'NATIONAL' or 'INTERNAL'
   - `level INTEGER`
   - `parent_code VARCHAR(100)`
   - `email VARCHAR(200)`
   - `is_active BOOLEAN DEFAULT true`
   - `synced_at TIMESTAMP DEFAULT NOW()`
   - UNIQUE constraint: `uq_lgsp_agencies_code` on (agency_code)

3. **Table `edoc.lgsp_documents`** — tracking sent/received LGSP docs (per D-02, D-03):
   - `id BIGSERIAL PRIMARY KEY`
   - `unit_id BIGINT NOT NULL`
   - `direction VARCHAR(10) NOT NULL` — 'send' or 'receive'
   - `lgsp_doc_id VARCHAR(200)` — docId from LGSP
   - `outgoing_doc_id BIGINT REFERENCES edoc.outgoing_docs(id)` — for send
   - `incoming_doc_id BIGINT REFERENCES edoc.incoming_docs(id)` — for receive (after import)
   - `from_agency_code VARCHAR(100)`
   - `from_agency_name VARCHAR(500)`
   - `to_agency_code VARCHAR(100)`
   - `to_agency_name VARCHAR(500)`
   - `subject VARCHAR(1000)`
   - `notation VARCHAR(200)`
   - `edxml_content TEXT` — raw edXML content
   - `status VARCHAR(50) NOT NULL DEFAULT 'initial'` — initial/processing/done/fail
   - `status_code VARCHAR(10)` — LGSP status code (01-16)
   - `error_message TEXT`
   - `sent_at TIMESTAMP`
   - `received_at TIMESTAMP`
   - `created_by BIGINT`
   - `created_at TIMESTAMP DEFAULT NOW()`
   - `updated_at TIMESTAMP DEFAULT NOW()`

4. **Stored functions:**

   a) `edoc.fn_lgsp_agency_upsert(p_agency_code, p_agency_name, p_agency_type, p_level, p_parent_code, p_email)` — UPSERT by agency_code. RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT).

   b) `edoc.fn_lgsp_agency_get_list(p_search TEXT DEFAULT '', p_agency_type TEXT DEFAULT '', p_page INT DEFAULT 1, p_page_size INT DEFAULT 50)` — search/filter agencies. RETURNS TABLE(id, agency_code, agency_name, agency_type, "level", parent_code, email, is_active, synced_at, total_count).

   c) `edoc.fn_lgsp_document_create(p_unit_id, p_direction, p_lgsp_doc_id, p_outgoing_doc_id, p_from_agency_code, p_from_agency_name, p_to_agency_code, p_to_agency_name, p_subject, p_notation, p_edxml_content, p_status, p_created_by)` — create tracking record. RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT).

   d) `edoc.fn_lgsp_document_update_status(p_id BIGINT, p_status TEXT, p_status_code TEXT, p_error_message TEXT)` — update status. RETURNS TABLE(success BOOLEAN, message TEXT).

   e) `edoc.fn_lgsp_document_get_list(p_unit_id BIGINT, p_direction TEXT DEFAULT '', p_status TEXT DEFAULT '', p_search TEXT DEFAULT '', p_page INT DEFAULT 1, p_page_size INT DEFAULT 20)` — list with pagination. RETURNS TABLE(id, unit_id, direction, lgsp_doc_id, outgoing_doc_id, incoming_doc_id, from_agency_code, from_agency_name, to_agency_code, to_agency_name, subject, notation, status, status_code, error_message, sent_at, received_at, created_at, total_count).

   f) `edoc.fn_lgsp_document_get_by_id(p_id BIGINT)` — single doc detail. Same columns as get_list minus total_count, plus edxml_content.

IMPORTANT: Quote reserved words in RETURNS TABLE: `"level"`. Use explicit table aliases (NOT single letters that conflict with column names).
  </action>
  <verify>
    <automated>docker exec eoffice-postgres psql -U postgres -d qlvb -f /docker-entrypoint-initdb.d/migrations/019_sprint14_lgsp_integration.sql 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `e_office_app_new/database/migrations/019_sprint14_lgsp_integration.sql`
    - Tables `edoc.lgsp_config`, `edoc.lgsp_agencies`, `edoc.lgsp_documents` created without error
    - `grep -c 'CREATE TABLE' e_office_app_new/database/migrations/019_sprint14_lgsp_integration.sql` returns 3
    - `grep -c 'CREATE.*FUNCTION' e_office_app_new/database/migrations/019_sprint14_lgsp_integration.sql` returns at least 6
    - `grep 'uq_lgsp_agencies_code' e_office_app_new/database/migrations/019_sprint14_lgsp_integration.sql` finds unique constraint
    - `grep 'uq_lgsp_config_unit' e_office_app_new/database/migrations/019_sprint14_lgsp_integration.sql` finds unique constraint
    - All functions use `{schema}.fn_{module}_{action}` naming convention (per D-12)
  </acceptance_criteria>
  <done>3 LGSP tables created, 6+ stored functions operational, migration runs without error in Docker PostgreSQL</done>
</task>

<task type="auto">
  <name>Task 2: Migration 020 — Digital signature tables and stored functions</name>
  <files>e_office_app_new/database/migrations/020_sprint15_digital_signatures.sql</files>
  <read_first>
    - e_office_app_new/database/migrations/019_sprint14_lgsp_integration.sql (just created, confirm pattern)
    - docs/source_code_cu/sources/OneWin.WebApp/EsignNEAC/Service/Login/Login.cs (signing flow: VNPT_CA, BKAV_CA, MISA_CA)
    - docs/source_code_cu/sources/OneWin.WebApp/EsignNEAC/Service/Signer/Pdf_Signer.cs (signature placement, serial_number, transaction_id)
    - docs/source_code_cu/sources/OneWin.WebApp/EsignNEAC/Models/Req_SignFile_eSign.cs (request model fields)
    - docs/source_code_cu/sources/OneWin.WebApp/EsignNEAC/Common/Static_String.cs (CA provider constants)
  </read_first>
  <action>
Create migration file `020_sprint15_digital_signatures.sql` with:

**Per D-05, D-06, D-07: Digital signature tables**

1. **Table `edoc.signing_config`** — CA provider configuration:
   - `id BIGSERIAL PRIMARY KEY`
   - `unit_id BIGINT NOT NULL REFERENCES public.departments(id)`
   - `provider VARCHAR(50) NOT NULL` — 'VNPT_SMARTCA' or 'ESIGN_NEAC'
   - `api_endpoint VARCHAR(500) NOT NULL`
   - `username VARCHAR(200)`
   - `password_encrypted VARCHAR(500)`
   - `is_active BOOLEAN DEFAULT true`
   - `created_by BIGINT NOT NULL`
   - `created_at TIMESTAMP DEFAULT NOW()`
   - `updated_at TIMESTAMP DEFAULT NOW()`
   - UNIQUE constraint: `uq_signing_config_unit_provider` on (unit_id, provider)

2. **Table `edoc.digital_signatures`** — signature records per document:
   - `id BIGSERIAL PRIMARY KEY`
   - `doc_type VARCHAR(20) NOT NULL` — 'outgoing' or 'drafting'
   - `doc_id BIGINT NOT NULL` — references outgoing_docs or drafting_docs
   - `staff_id BIGINT NOT NULL REFERENCES public.staff(id)`
   - `provider VARCHAR(50) NOT NULL` — 'VNPT_SMARTCA' or 'ESIGN_NEAC'
   - `serial_number VARCHAR(200)` — certificate serial number
   - `transaction_id VARCHAR(200)` — from CA provider
   - `status VARCHAR(30) NOT NULL DEFAULT 'pending'` — pending/signing/signed/rejected/expired/failed
   - `signed_at TIMESTAMP`
   - `certificate_info TEXT` — JSON: issuer, subject, valid_from, valid_to
   - `signature_position TEXT` — JSON: page, x, y, width, height
   - `error_message TEXT`
   - `created_at TIMESTAMP DEFAULT NOW()`
   - `updated_at TIMESTAMP DEFAULT NOW()`
   - INDEX: `idx_digital_signatures_doc` on (doc_type, doc_id)

3. **Stored functions:**

   a) `edoc.fn_digital_signature_create(p_doc_type, p_doc_id, p_staff_id, p_provider, p_serial_number, p_transaction_id)` — create new signature record. RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT).

   b) `edoc.fn_digital_signature_update_status(p_id BIGINT, p_status TEXT, p_signed_at TIMESTAMP, p_certificate_info TEXT, p_error_message TEXT)` — update status after CA response. RETURNS TABLE(success BOOLEAN, message TEXT).

   c) `edoc.fn_digital_signature_get_by_doc(p_doc_type TEXT, p_doc_id BIGINT)` — list signatures for a document. RETURNS TABLE(id, doc_type, doc_id, staff_id, staff_name, provider, serial_number, transaction_id, status, signed_at, certificate_info, signature_position, error_message, created_at). JOIN with staff to get staff_name.

   d) `edoc.fn_digital_signature_get_list(p_unit_id BIGINT, p_status TEXT DEFAULT '', p_provider TEXT DEFAULT '', p_page INT DEFAULT 1, p_page_size INT DEFAULT 20)` — list all signatures for admin view. RETURNS TABLE(id, doc_type, doc_id, staff_id, staff_name, provider, status, signed_at, created_at, total_count). JOIN staff, filter by unit_id through staff.unit_id.

IMPORTANT: Quote `"position"` if used as column name. Use `staff_name` (concatenated from staff table).
  </action>
  <verify>
    <automated>docker exec eoffice-postgres psql -U postgres -d qlvb -f /docker-entrypoint-initdb.d/migrations/020_sprint15_digital_signatures.sql 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `e_office_app_new/database/migrations/020_sprint15_digital_signatures.sql`
    - Tables `edoc.signing_config`, `edoc.digital_signatures` created
    - `grep -c 'CREATE TABLE' e_office_app_new/database/migrations/020_sprint15_digital_signatures.sql` returns 2
    - `grep -c 'CREATE.*FUNCTION' e_office_app_new/database/migrations/020_sprint15_digital_signatures.sql` returns at least 4
    - `grep 'uq_signing_config_unit_provider' e_office_app_new/database/migrations/020_sprint15_digital_signatures.sql` finds constraint
    - `grep 'idx_digital_signatures_doc' e_office_app_new/database/migrations/020_sprint15_digital_signatures.sql` finds index
  </acceptance_criteria>
  <done>2 signing tables created, 4 stored functions operational, migration runs without error</done>
</task>

<task type="auto">
  <name>Task 3: Migration 021 — Notification channel tables and stored functions</name>
  <files>e_office_app_new/database/migrations/021_sprint16_notification_channels.sql</files>
  <read_first>
    - e_office_app_new/database/migrations/020_sprint15_digital_signatures.sql (just created)
    - docs/source_code_cu/sources/FcmWindowsService/FcmMessage.cs (FCM pattern: DeviceToken, body, title, PageName, success tracking via SP)
    - docs/source_code_cu/sources/ZaloMessage/MainFunction.cs (Zalo OA pattern: access_token refresh, user_id, template messages, status tracking)
    - e_office_app_new/database/migrations/013_sprint8_messages_notices.sql (existing notice tables for reference)
  </read_first>
  <action>
Create migration file `021_sprint16_notification_channels.sql` with:

**Per D-08, D-09, D-10: Notification channel tables**

1. **Table `edoc.notification_channels`** — channel configuration:
   - `id BIGSERIAL PRIMARY KEY`
   - `channel VARCHAR(20) NOT NULL` — 'fcm', 'zalo', 'sms', 'email'
   - `display_name VARCHAR(100) NOT NULL` — 'Firebase Push', 'Zalo OA', 'SMS', 'Email'
   - `config_json TEXT` — JSON config: api_key, endpoint, etc.
   - `is_active BOOLEAN DEFAULT true`
   - `created_at TIMESTAMP DEFAULT NOW()`
   - `updated_at TIMESTAMP DEFAULT NOW()`
   - UNIQUE constraint: `uq_notification_channels_channel` on (channel)

2. **Table `edoc.notification_preferences`** — per-staff channel preferences (per D-09):
   - `id BIGSERIAL PRIMARY KEY`
   - `staff_id BIGINT NOT NULL REFERENCES public.staff(id)`
   - `channel VARCHAR(20) NOT NULL` — 'fcm', 'zalo', 'sms', 'email'
   - `is_enabled BOOLEAN DEFAULT true`
   - `device_token VARCHAR(500)` — FCM device token
   - `phone_number VARCHAR(50)` — for SMS/Zalo
   - `email VARCHAR(200)` — for email
   - `zalo_user_id VARCHAR(100)` — Zalo OA user_id
   - `created_at TIMESTAMP DEFAULT NOW()`
   - `updated_at TIMESTAMP DEFAULT NOW()`
   - UNIQUE constraint: `uq_notification_prefs_staff_channel` on (staff_id, channel)

3. **Table `edoc.notification_logs`** — delivery tracking per message per channel:
   - `id BIGSERIAL PRIMARY KEY`
   - `staff_id BIGINT NOT NULL REFERENCES public.staff(id)`
   - `channel VARCHAR(20) NOT NULL`
   - `title VARCHAR(500)`
   - `body TEXT`
   - `data_json TEXT` — JSON payload (action, doc_id, etc.)
   - `status VARCHAR(20) NOT NULL DEFAULT 'pending'` — pending/sent/delivered/failed
   - `error_message TEXT`
   - `external_id VARCHAR(200)` — message_id from provider
   - `sent_at TIMESTAMP`
   - `created_at TIMESTAMP DEFAULT NOW()`
   - INDEX: `idx_notification_logs_staff` on (staff_id, created_at DESC)
   - INDEX: `idx_notification_logs_status` on (status, created_at)

4. **Stored functions:**

   a) `edoc.fn_notification_log_create(p_staff_id, p_channel, p_title, p_body, p_data_json)` — insert log entry. RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT).

   b) `edoc.fn_notification_log_update_status(p_id BIGINT, p_status TEXT, p_external_id TEXT, p_error_message TEXT)` — update after send attempt. RETURNS TABLE(success BOOLEAN, message TEXT).

   c) `edoc.fn_notification_log_get_list(p_staff_id BIGINT DEFAULT NULL, p_channel TEXT DEFAULT '', p_status TEXT DEFAULT '', p_page INT DEFAULT 1, p_page_size INT DEFAULT 20)` — list logs with pagination. RETURNS TABLE(id, staff_id, staff_name, channel, title, body, status, error_message, external_id, sent_at, created_at, total_count).

   d) `edoc.fn_notification_preference_upsert(p_staff_id, p_channel, p_is_enabled, p_device_token, p_phone_number, p_email, p_zalo_user_id)` — upsert preference. RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT).

   e) `edoc.fn_notification_preference_get_by_staff(p_staff_id BIGINT)` — get all channel preferences for a staff. RETURNS TABLE(id, staff_id, channel, is_enabled, device_token, phone_number, email, zalo_user_id).

5. **Seed default channels:**
   ```sql
   INSERT INTO edoc.notification_channels (channel, display_name, is_active) VALUES
     ('fcm', 'Firebase Push Notification', true),
     ('zalo', 'Zalo OA', true),
     ('sms', 'SMS Gateway', true),
     ('email', 'Email', true)
   ON CONFLICT (channel) DO NOTHING;
   ```
  </action>
  <verify>
    <automated>docker exec eoffice-postgres psql -U postgres -d qlvb -f /docker-entrypoint-initdb.d/migrations/021_sprint16_notification_channels.sql 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `e_office_app_new/database/migrations/021_sprint16_notification_channels.sql`
    - Tables `edoc.notification_channels`, `edoc.notification_preferences`, `edoc.notification_logs` created
    - `grep -c 'CREATE TABLE' e_office_app_new/database/migrations/021_sprint16_notification_channels.sql` returns 3
    - `grep -c 'CREATE.*FUNCTION' e_office_app_new/database/migrations/021_sprint16_notification_channels.sql` returns at least 5
    - 4 default channels seeded via INSERT
    - All indexes created on notification_logs
  </acceptance_criteria>
  <done>3 notification tables created, 5 stored functions operational, 4 default channels seeded, migration runs without error</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB stored functions | All input from backend goes through parameterized SP calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | lgsp_config.password_encrypted | mitigate | Column named `password_encrypted` — backend must encrypt before storing, never store plaintext |
| T-06-02 | I (Info Disclosure) | edoc.lgsp_config | mitigate | secret_key and password must not appear in SELECT * queries from frontend — SPs must exclude sensitive columns |
| T-06-03 | T (Tampering) | notification_preferences | mitigate | Staff can only update own preferences — SP must validate p_staff_id matches authenticated user |
</threat_model>

<verification>
All 3 migration files run successfully in Docker PostgreSQL. Tables exist in edoc schema. All stored functions callable via `SELECT * FROM edoc.fn_xxx()`.
</verification>

<success_criteria>
- 3 migration files created and executed without errors
- 8 new tables across LGSP, signing, and notification subsystems
- 15+ stored functions following `edoc.fn_{module}_{action}` convention
- All FK references valid, all unique constraints in place
- Default notification channels seeded
</success_criteria>

<output>
After completion, create `.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-01-SUMMARY.md`
</output>
