---
phase: 06-t-ch-h-p-h-th-ng-ngo-i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/database/migrations/019_sprint14_lgsp.sql
  - e_office_app_new/database/migrations/020_sprint15_digital_signing.sql
  - e_office_app_new/database/migrations/021_sprint16_notifications.sql
autonomous: true
requirements:
  - LGSP-01
  - LGSP-02
  - LGSP-03
  - LGSP-04
  - SIGN-01
  - SIGN-02
  - SIGN-03
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04

must_haves:
  truths:
    - "Tables for LGSP tracking, digital signatures, notification logs exist in DB"
    - "Stored functions for LGSP CRUD, signing CRUD, notification CRUD are callable"
    - "Indexes on frequently queried columns (status, staff_id, created_at) exist"
  artifacts:
    - path: "e_office_app_new/database/migrations/019_sprint14_lgsp.sql"
      provides: "LGSP tables + stored functions"
      contains: "edoc.lgsp_tracking"
    - path: "e_office_app_new/database/migrations/020_sprint15_digital_signing.sql"
      provides: "Digital signing tables + stored functions"
      contains: "edoc.digital_signatures"
    - path: "e_office_app_new/database/migrations/021_sprint16_notifications.sql"
      provides: "Notification tables + stored functions"
      contains: "edoc.notification_logs"
  key_links:
    - from: "edoc.lgsp_tracking"
      to: "edoc.outgoing_docs"
      via: "FK outgoing_doc_id"
      pattern: "REFERENCES edoc.outgoing_docs"
    - from: "edoc.digital_signatures"
      to: "edoc.outgoing_docs"
      via: "FK doc_id + doc_type"
      pattern: "REFERENCES"
    - from: "edoc.notification_logs"
      to: "public.staff"
      via: "FK staff_id"
      pattern: "REFERENCES public.staff"
---

<objective>
Create 3 database migration files for Sprint 14 (LGSP), Sprint 15 (Digital Signing), Sprint 16 (Notifications).

Purpose: All backend repositories and routes depend on these tables and stored functions existing first. Per D-00e, migrations MUST run successfully before any backend work.
Output: 3 migration SQL files ready to execute in Docker PostgreSQL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-CONTEXT.md
@e_office_app_new/ROADMAP.md (Sprint 14, 15, 16 sections)
@e_office_app_new/database/migrations/018_sprint13_meetings.sql (pattern reference)

<interfaces>
<!-- DB helper functions used by repositories -->
From e_office_app_new/backend/src/lib/db/query.ts:
```typescript
export async function callFunction<T>(functionName: string, params: unknown[]): Promise<T[]>;
export async function callFunctionOne<T>(functionName: string, params: unknown[]): Promise<T | null>;
export async function callProcedure(procedureName: string, params: unknown[]): Promise<void>;
```

SP naming convention: `{schema}.fn_{module}_{action}`
- edoc schema for document-related tables
- public schema for system tables
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 019 — Sprint 14 LGSP tables and stored functions</name>
  <files>e_office_app_new/database/migrations/019_sprint14_lgsp.sql</files>
  <read_first>
    - e_office_app_new/database/migrations/018_sprint13_meetings.sql (pattern: CREATE TABLE, SP format, RETURNS TABLE quoting)
    - e_office_app_new/database/migrations/012_sprint7_inter_incoming.sql (inter-incoming doc pattern — LGSP builds on this)
    - e_office_app_new/ROADMAP.md (Sprint 14 section — lines 972-998)
  </read_first>
  <action>
Create migration `019_sprint14_lgsp.sql` with:

**Tables:**

1. `edoc.lgsp_organizations` — Danh sach co quan lien thong dong bo tu LGSP
   - `id BIGSERIAL PRIMARY KEY`
   - `org_code VARCHAR(100) NOT NULL` (ma co quan LGSP)
   - `org_name VARCHAR(500) NOT NULL`
   - `parent_code VARCHAR(100)` (ma co quan cha)
   - `address VARCHAR(500)`
   - `email VARCHAR(200)`
   - `phone VARCHAR(50)`
   - `is_active BOOLEAN DEFAULT true`
   - `synced_at TIMESTAMPTZ DEFAULT NOW()`
   - `created_at TIMESTAMPTZ DEFAULT NOW()`
   - `CONSTRAINT uq_lgsp_org_code UNIQUE(org_code)`

2. `edoc.lgsp_tracking` — Tracking trang thai gui/nhan VB lien thong
   - `id BIGSERIAL PRIMARY KEY`
   - `outgoing_doc_id BIGINT REFERENCES edoc.outgoing_docs(id)` (nullable — null for received)
   - `incoming_doc_id BIGINT REFERENCES edoc.incoming_docs(id)` (nullable — null for sent)
   - `direction VARCHAR(10) NOT NULL CHECK(direction IN ('send', 'receive'))`
   - `lgsp_doc_id VARCHAR(200)` (ID tu LGSP)
   - `dest_org_code VARCHAR(100)` (co quan dich khi gui)
   - `dest_org_name VARCHAR(500)`
   - `edxml_content TEXT` (edXML payload)
   - `status VARCHAR(50) NOT NULL DEFAULT 'pending'` CHECK(status IN ('pending', 'processing', 'success', 'error'))
   - `error_message TEXT`
   - `sent_at TIMESTAMPTZ`
   - `received_at TIMESTAMPTZ`
   - `created_at TIMESTAMPTZ DEFAULT NOW()`
   - `created_by BIGINT REFERENCES public.staff(id)`
   - Indexes: `idx_lgsp_tracking_outgoing ON (outgoing_doc_id)`, `idx_lgsp_tracking_status ON (status)`, `idx_lgsp_tracking_direction ON (direction)`

**Stored Functions (per D-12 naming convention `edoc.fn_lgsp_{action}`):**

1. `edoc.fn_lgsp_org_sync(p_org_code, p_org_name, p_parent_code, p_address, p_email, p_phone)` RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT) — UPSERT on org_code
2. `edoc.fn_lgsp_org_get_list(p_search, p_page, p_page_size)` RETURNS TABLE(id, org_code, org_name, parent_code, address, email, phone, is_active, synced_at, total_count BIGINT)
3. `edoc.fn_lgsp_tracking_create(p_outgoing_doc_id, p_direction, p_dest_org_code, p_dest_org_name, p_edxml_content, p_created_by)` RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT)
4. `edoc.fn_lgsp_tracking_update_status(p_id, p_status, p_lgsp_doc_id, p_error_message)` RETURNS TABLE(success BOOLEAN, message TEXT)
5. `edoc.fn_lgsp_tracking_get_list(p_direction, p_status, p_page, p_page_size)` RETURNS TABLE(id, outgoing_doc_id, incoming_doc_id, direction, lgsp_doc_id, dest_org_code, dest_org_name, status, error_message, sent_at, received_at, created_at, total_count BIGINT)
6. `edoc.fn_lgsp_tracking_get_by_doc(p_outgoing_doc_id)` RETURNS TABLE(id, direction, lgsp_doc_id, dest_org_code, dest_org_name, status, error_message, sent_at, created_at)

CRITICAL: Quote all PostgreSQL reserved words in RETURNS TABLE: `"position"`, `"order"`, `"type"`, `"name"`, `"key"`, `"value"`, `"comment"`. In this migration specifically watch for `"status"` in CHECK constraints (not reserved but be safe in RETURNS).
  </action>
  <verify>
    <automated>docker exec eoffice-postgres psql -U postgres -d qlvb -f /docker-entrypoint-initdb.d/migrations/019_sprint14_lgsp.sql 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `e_office_app_new/database/migrations/019_sprint14_lgsp.sql`
    - Contains `CREATE TABLE edoc.lgsp_organizations`
    - Contains `CREATE TABLE edoc.lgsp_tracking`
    - Contains `edoc.fn_lgsp_org_sync`
    - Contains `edoc.fn_lgsp_org_get_list`
    - Contains `edoc.fn_lgsp_tracking_create`
    - Contains `edoc.fn_lgsp_tracking_update_status`
    - Contains `edoc.fn_lgsp_tracking_get_list`
    - Contains `edoc.fn_lgsp_tracking_get_by_doc`
    - Contains `REFERENCES edoc.outgoing_docs(id)`
    - Contains `REFERENCES edoc.incoming_docs(id)`
    - SQL executes without error in PostgreSQL
  </acceptance_criteria>
  <done>Migration 019 creates lgsp_organizations and lgsp_tracking tables with 6 stored functions, all FK constraints valid, SQL executes cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Migration 020 — Sprint 15 Digital Signing tables and stored functions</name>
  <files>e_office_app_new/database/migrations/020_sprint15_digital_signing.sql</files>
  <read_first>
    - e_office_app_new/database/migrations/018_sprint13_meetings.sql (pattern reference)
    - e_office_app_new/ROADMAP.md (Sprint 15 section — lines 1001-1019)
  </read_first>
  <action>
Create migration `020_sprint15_digital_signing.sql` with:

**Tables:**

1. `edoc.digital_signatures` — Luu thong tin chu ky so tren van ban
   - `id BIGSERIAL PRIMARY KEY`
   - `doc_id BIGINT NOT NULL` (ID van ban — co the la outgoing_docs hoac drafting_docs)
   - `doc_type VARCHAR(20) NOT NULL CHECK(doc_type IN ('outgoing', 'drafting'))` (loai van ban)
   - `staff_id BIGINT NOT NULL REFERENCES public.staff(id)`
   - `sign_method VARCHAR(30) NOT NULL CHECK(sign_method IN ('smart_ca', 'esign_neac', 'usb_token'))` (phuong thuc ky)
   - `certificate_serial VARCHAR(200)` (serial number chung thu so)
   - `certificate_subject VARCHAR(500)` (thong tin chu so huu)
   - `certificate_issuer VARCHAR(500)` (nha cung cap CA)
   - `signed_file_path VARCHAR(1000)` (duong dan file da ky tren MinIO)
   - `original_file_path VARCHAR(1000)` (duong dan file goc)
   - `sign_status VARCHAR(20) NOT NULL DEFAULT 'pending' CHECK(sign_status IN ('pending', 'signing', 'signed', 'error', 'rejected'))`
   - `error_message TEXT`
   - `signed_at TIMESTAMPTZ`
   - `created_at TIMESTAMPTZ DEFAULT NOW()`
   - Indexes: `idx_digsig_doc ON (doc_id, doc_type)`, `idx_digsig_staff ON (staff_id)`, `idx_digsig_status ON (sign_status)`

**Stored Functions:**

1. `edoc.fn_digital_signature_create(p_doc_id, p_doc_type, p_staff_id, p_sign_method, p_original_file_path)` RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT)
2. `edoc.fn_digital_signature_update_status(p_id, p_sign_status, p_certificate_serial, p_certificate_subject, p_certificate_issuer, p_signed_file_path, p_error_message)` RETURNS TABLE(success BOOLEAN, message TEXT) — Updates status + cert info after signing
3. `edoc.fn_digital_signature_get_by_doc(p_doc_id, p_doc_type)` RETURNS TABLE(id, doc_id, doc_type, staff_id, staff_name VARCHAR, sign_method, certificate_serial, certificate_subject, certificate_issuer, signed_file_path, original_file_path, sign_status, error_message, signed_at, created_at) — JOIN staff to get staff_name
4. `edoc.fn_digital_signature_get_by_id(p_id)` RETURNS TABLE(same columns as get_by_doc)
  </action>
  <verify>
    <automated>docker exec eoffice-postgres psql -U postgres -d qlvb -f /docker-entrypoint-initdb.d/migrations/020_sprint15_digital_signing.sql 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `e_office_app_new/database/migrations/020_sprint15_digital_signing.sql`
    - Contains `CREATE TABLE edoc.digital_signatures`
    - Contains `edoc.fn_digital_signature_create`
    - Contains `edoc.fn_digital_signature_update_status`
    - Contains `edoc.fn_digital_signature_get_by_doc`
    - Contains `edoc.fn_digital_signature_get_by_id`
    - Contains `JOIN public.staff` in get_by_doc function
    - Contains `CHECK(doc_type IN ('outgoing', 'drafting'))`
    - Contains `CHECK(sign_method IN ('smart_ca', 'esign_neac', 'usb_token'))`
    - SQL executes without error in PostgreSQL
  </acceptance_criteria>
  <done>Migration 020 creates digital_signatures table with 4 stored functions, FK to staff valid, SQL executes cleanly</done>
</task>

<task type="auto">
  <name>Task 3: Migration 021 — Sprint 16 Notification tables and stored functions</name>
  <files>e_office_app_new/database/migrations/021_sprint16_notifications.sql</files>
  <read_first>
    - e_office_app_new/database/migrations/018_sprint13_meetings.sql (pattern reference)
    - e_office_app_new/ROADMAP.md (Sprint 16 section — lines 1022-1046)
  </read_first>
  <action>
Create migration `021_sprint16_notifications.sql` with:

**Tables:**

1. `edoc.device_tokens` — FCM device tokens cho push notification
   - `id BIGSERIAL PRIMARY KEY`
   - `staff_id BIGINT NOT NULL REFERENCES public.staff(id)`
   - `device_token VARCHAR(500) NOT NULL`
   - `device_type VARCHAR(20) DEFAULT 'web' CHECK(device_type IN ('web', 'android', 'ios'))`
   - `is_active BOOLEAN DEFAULT true`
   - `created_at TIMESTAMPTZ DEFAULT NOW()`
   - `updated_at TIMESTAMPTZ DEFAULT NOW()`
   - `CONSTRAINT uq_device_token UNIQUE(device_token)`

2. `edoc.notification_logs` — Log tat ca thong bao gui qua cac kenh
   - `id BIGSERIAL PRIMARY KEY`
   - `staff_id BIGINT NOT NULL REFERENCES public.staff(id)`
   - `channel VARCHAR(20) NOT NULL CHECK(channel IN ('fcm', 'zalo', 'sms', 'email'))` (kenh gui)
   - `event_type VARCHAR(50) NOT NULL` (loai su kien: 'new_doc', 'task_assigned', 'deadline_warning', 'meeting_reminder')
   - `title VARCHAR(500)`
   - `body TEXT`
   - `ref_type VARCHAR(30)` (loai doi tuong: 'incoming_doc', 'outgoing_doc', 'handling_doc', 'meeting')
   - `ref_id BIGINT` (ID doi tuong)
   - `send_status VARCHAR(20) NOT NULL DEFAULT 'pending' CHECK(send_status IN ('pending', 'sent', 'failed'))`
   - `error_message TEXT`
   - `sent_at TIMESTAMPTZ`
   - `created_at TIMESTAMPTZ DEFAULT NOW()`
   - Indexes: `idx_notif_log_staff ON (staff_id)`, `idx_notif_log_channel ON (channel)`, `idx_notif_log_status ON (send_status)`, `idx_notif_log_created ON (created_at DESC)`

3. `edoc.notification_preferences` — Cau hinh kenh thong bao theo user (optional per D-09)
   - `id BIGSERIAL PRIMARY KEY`
   - `staff_id BIGINT NOT NULL REFERENCES public.staff(id)`
   - `channel VARCHAR(20) NOT NULL CHECK(channel IN ('fcm', 'zalo', 'sms', 'email'))`
   - `is_enabled BOOLEAN DEFAULT true`
   - `created_at TIMESTAMPTZ DEFAULT NOW()`
   - `updated_at TIMESTAMPTZ DEFAULT NOW()`
   - `CONSTRAINT uq_notif_pref_staff_channel UNIQUE(staff_id, channel)`

**Stored Functions:**

1. `edoc.fn_device_token_upsert(p_staff_id, p_device_token, p_device_type)` RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT) — UPSERT on device_token
2. `edoc.fn_device_token_get_by_staff(p_staff_id)` RETURNS TABLE(id, device_token, device_type, is_active, created_at)
3. `edoc.fn_device_token_delete(p_id, p_staff_id)` RETURNS TABLE(success BOOLEAN, message TEXT)
4. `edoc.fn_notification_log_create(p_staff_id, p_channel, p_event_type, p_title, p_body, p_ref_type, p_ref_id)` RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT)
5. `edoc.fn_notification_log_update_status(p_id, p_send_status, p_error_message)` RETURNS TABLE(success BOOLEAN, message TEXT)
6. `edoc.fn_notification_log_get_list(p_staff_id, p_channel, p_send_status, p_page, p_page_size)` RETURNS TABLE(id, staff_id, channel, event_type, title, body, ref_type, ref_id, send_status, error_message, sent_at, created_at, total_count BIGINT)
7. `edoc.fn_notification_pref_upsert(p_staff_id, p_channel, p_is_enabled)` RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT)
8. `edoc.fn_notification_pref_get_by_staff(p_staff_id)` RETURNS TABLE(id, channel, is_enabled)
  </action>
  <verify>
    <automated>docker exec eoffice-postgres psql -U postgres -d qlvb -f /docker-entrypoint-initdb.d/migrations/021_sprint16_notifications.sql 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `e_office_app_new/database/migrations/021_sprint16_notifications.sql`
    - Contains `CREATE TABLE edoc.device_tokens`
    - Contains `CREATE TABLE edoc.notification_logs`
    - Contains `CREATE TABLE edoc.notification_preferences`
    - Contains `edoc.fn_device_token_upsert`
    - Contains `edoc.fn_notification_log_create`
    - Contains `edoc.fn_notification_log_get_list`
    - Contains `edoc.fn_notification_pref_upsert`
    - Contains `edoc.fn_notification_pref_get_by_staff`
    - Contains `REFERENCES public.staff(id)` in all 3 tables
    - SQL executes without error in PostgreSQL
  </acceptance_criteria>
  <done>Migration 021 creates 3 notification tables (device_tokens, notification_logs, notification_preferences) with 8 stored functions, SQL executes cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL injection | All SPs use parameterized queries via callFunction() |
| Data integrity | FK constraints enforce referential integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-01 | Tampering | lgsp_tracking.edxml_content | mitigate | TEXT column stores raw edXML but only written by backend service, never from user input directly |
| T-06-02 | Info Disclosure | digital_signatures.certificate_serial | accept | Certificate serial is not secret — it is part of the public certificate |
| T-06-03 | Spoofing | device_tokens | mitigate | device_token_upsert requires p_staff_id from JWT — cannot register token for another user |
</threat_model>

<verification>
All 3 migration files execute without errors in PostgreSQL. Tables exist with correct columns and constraints. Stored functions are callable.
```bash
docker exec eoffice-postgres psql -U postgres -d qlvb -c "\dt edoc.lgsp_*"
docker exec eoffice-postgres psql -U postgres -d qlvb -c "\dt edoc.digital_*"
docker exec eoffice-postgres psql -U postgres -d qlvb -c "\dt edoc.device_*"
docker exec eoffice-postgres psql -U postgres -d qlvb -c "\dt edoc.notification_*"
```
</verification>

<success_criteria>
3 migration files created and executed successfully. All tables and stored functions exist in PostgreSQL.
</success_criteria>

<output>
After completion, create `.planning/phases/06-t-ch-h-p-h-th-ng-ngo-i/06-01-SUMMARY.md`
</output>
