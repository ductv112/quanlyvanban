---
phase: 36-status-callback-chain-9-ma-qd-28
plan: 04
type: execute
wave: 2
depends_on: ["36-01", "36-02"]
files_modified:
  - e_office_app_new/frontend/src/lib/lgsp-status-labels.ts
  - e_office_app_new/frontend/src/components/lgsp-status-timeline.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx
autonomous: true
requirements:
  - LGSP-STATUS-10   # UI tag trạng thái LGSP (01..16) màu phân biệt + tooltip Vietnamese
  - LGSP-STATUS-07   # "Chuyển lại" → "Từ chối tiếp nhận" label (defer full SP rename Phase 37; UI helper Phase 36 — DEFER full button refactor Phase 37 per CONTEXT scope)

must_haves:
  truths:
    - "frontend/src/lib/lgsp-status-labels.ts exports STATUS_LABELS (9 mã) + STATUS_COLORS + STATUS_DESCRIPTIONS"
    - "frontend/src/components/lgsp-status-timeline.tsx exports LgspStatusTimeline component using AntD Timeline"
    - "VB đến detail page extends Phase 35-04 với section 'Lịch sử trạng thái LGSP' (conditional source_type='external_lgsp')"
    - "Timeline render đúng 3 sent_status: pending (orange spin), success (green check), error (red exclamation + tooltip error_message)"
    - "Timeline fetch từ GET /api/van-ban-den/:id/lgsp-status-history khi mount component"
    - "Polling 10s khi có >=1 row pending để cập nhật trạng thái sau worker xử lý xong"
    - "Frontend Next.js production build pass"
    - "Tiếng Việt có dấu trong UI labels (theo CLAUDE.md)"
  artifacts:
    - path: "e_office_app_new/frontend/src/lib/lgsp-status-labels.ts"
      contains: "Đã tiếp nhận"
    - path: "e_office_app_new/frontend/src/components/lgsp-status-timeline.tsx"
      contains: "LgspStatusTimeline"
    - path: "e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx"
      contains: "LgspStatusTimeline"
  key_links:
    - from: "VB đến detail page"
      to: "GET /api/van-ban-den/:id/lgsp-status-history"
      via: "api.get(...) trong useEffect khi source_type='external_lgsp'"
      pattern: "lgsp-status-history"
    - from: "LgspStatusTimeline component"
      to: "AntD Timeline + Tag"
      via: "@ant-design/icons + antd Timeline+Tag+Tooltip+Spin"
      pattern: "import.*Timeline.*from 'antd'"
---

<objective>
Wave 2 (parallel với Plan 36-03 — không conflict files) — Frontend extends Phase 35-04 detail page VB đến (`/van-ban-den/[id]`) thêm section "Lịch sử trạng thái LGSP" conditional render khi `source_type='external_lgsp'`. Section dùng AntD `<Timeline>` component hiển thị chronological list outbox events từ `GET /api/van-ban-den/:id/lgsp-status-history` (Plan 36-03 endpoint). Mỗi entry: target_status tag (9 mã QĐ 28, màu phân biệt theo CONTEXT D-14), sent_status badge (pending/success/error), timestamp, error tooltip. Polling 10s khi có pending event để auto-refresh sau worker xử lý xong.

Tách helper `lgsp-status-labels.ts` (map 9 mã → tiếng Việt + màu + description) cho reuse Phase 37 admin UI sau này, và component `lgsp-status-timeline.tsx` riêng (re-usable nếu VB đi cũng cần Timeline status — defer Phase 37).

Purpose: User văn thư DN.001 mở chi tiết VB đến nguồn LGSP thấy realtime trạng thái callback đã gửi lên trục (03 đã gửi, 04 đã gửi, 05 pending, 06 chưa fire). Đáp ứng REQ LGSP-STATUS-10 (UI display).

Output: 3 files (1 NEW helper + 1 NEW component + 1 MODIFIED detail page). Frontend build clean.

**Out-of-scope per CONTEXT defer:**
- LGSP-STATUS-07 full button refactor "Chuyển lại" → "Từ chối tiếp nhận" + Modal title + placeholder → Phase 37 (cần touch action menu logic + i18n)
- Mã 13/15/16 sender-side UI → Phase 37
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-status-callback-chain-9-ma-qd-28/36-CONTEXT.md
@e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx
@e_office_app_new/frontend/src/lib/lgsp-source-badge.tsx
@e_office_app_new/frontend/src/lib/api.ts
@e_office_app_new/frontend/src/config/theme.ts
@CLAUDE.md

<interfaces>
<!-- Existing patterns to mirror in frontend -->

From `e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx` (Phase 35-04 EXTENDED):
- File path: page.tsx
- DocDetail interface includes Phase 35-04 fields: `source_type?: DocSourceType`, `external_doc_id?: string | null`, `lgsp_sender_org_code?: string | null`
- Phase 35-04 "Nguồn LGSP" Card section renders at line ~555-587 (Conditional `{doc.source_type === 'external_lgsp' && (...)})`
- Plan 36-04 adds 2nd Card "Lịch sử trạng thái LGSP" right AFTER Phase 35-04 Nguồn LGSP section (line ~588), BEFORE `<Row gutter={16}>`
- `api` instance: `import { api } from '@/lib/api'` (axios with JWT interceptor)
- `dayjs` already imported for date formatting

From `e_office_app_new/frontend/src/lib/lgsp-source-badge.tsx` (Phase 35-04 — pattern mirror cho helper exports):
- `export type DocSourceType = 'manual' | 'internal' | 'external_lgsp';`
- `export function LgspSourceBadge(props): React.ReactElement | null;`
- File is at `frontend/src/lib/lgsp-source-badge.tsx`

AntD 6 Timeline API (verify with Context7 if uncertain):
```typescript
import { Timeline } from 'antd';
<Timeline mode="left">
  <Timeline.Item key="1" color="green" label="DD/MM HH:mm">content</Timeline.Item>
</Timeline>
```
NOTE: AntD 6 may have moved Timeline.Item to `items` prop. Use Context7 MCP if available to verify:
```typescript
<Timeline items={[
  { color: 'green', label: 'DD/MM HH:mm', children: <Tag>...</Tag>, key: '1' },
]} />
```
Both APIs work in v6 — prefer `items` prop (functional). Adapt during execution if Context7 confirms a specific version requirement.

From Plan 36-03 backend GET endpoint response shape:
```typescript
// GET /api/van-ban-den/:id/lgsp-status-history
{
  success: true,
  data: Array<{
    id: number;                            // BIGINT -> Number()
    target_status: '01'|'02'|'03'|'04'|'05'|'06'|'13'|'15'|'16';
    sent_status: 'pending'|'success'|'error';
    sent_at: string | null;                 // ISO timestamp
    retry_count: number;
    error_message: string | null;
    created_at: string;                     // ISO timestamp
  }>
}
```

From CONTEXT D-14:
- Color mapping: success path '03/04/05'='vàng' (yellow/orange), '06'='xanh lá' (green), '02/16'='đỏ' (red), '13'='xám' (gray), '01'='xanh dương' (blue), '15'='xanh' (green)
- Tooltip Vietnamese label full
- AntD Tag color tokens: 'default' | 'success' (green) | 'warning' (orange) | 'error' (red) | 'processing' (blue) | 'gold' (yellow) | 'magenta' | 'cyan' | 'volcano' | hex like '#1B3A5C'

CLAUDE.md design system:
- Vietnamese có dấu trong UI
- Tag style consistent with existing patterns
- Card padding/border-radius theo theme
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → backend `/api/van-ban-den/:id/lgsp-status-history` | Existing auth flow (JWT in localStorage, axios interceptor). Permission check on backend (`loadDocAndPerms`). |
| Display error_message field | error_message originates from Vietnamese-mapped LgspSendError + may include LGSP raw response text (truncated 1000 chars). Render as text via React `{error_message}` (auto-escaped — safe). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-10 | I (Information Disclosure) | error_message may include internal error trace | accept | error_message is Vietnamese-mapped via Phase 34 `mapLgspError()` + `LgspSendError.vietnameseMessage`. Truncated 1000 chars at DB INSERT. Risk acceptable — admin/user need debug info. |
| T-36-11 | XSS via target_status / payload | mitigate | React auto-escapes `{value}` interpolation. payload field NOT rendered (only id/target_status/sent_status/timestamps/error_message). |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: NEW frontend/src/lib/lgsp-status-labels.ts — map 9 mã + màu + description</name>
  <files>e_office_app_new/frontend/src/lib/lgsp-status-labels.ts</files>

  <read_first>
    1. Read `e_office_app_new/frontend/src/lib/lgsp-source-badge.tsx` — pattern reference cho helper file (Vietnamese labels + Tag color).
    2. Read Phase 36 CONTEXT D-14 (helper map specs).
  </read_first>

  <action>
    Tạo file mới `e_office_app_new/frontend/src/lib/lgsp-status-labels.ts`:

    ```typescript
    // ============================================================
    // LGSP Status Labels + Colors + Descriptions — Phase 36 Plan 36-04
    // REQ: LGSP-STATUS-10
    //
    // Map 9 mã trạng thái QĐ 28/2018/QĐ-TTg → Vietnamese label + AntD Tag color + tooltip description
    //
    // Spec: Quyết định 28/2018/QĐ-TTg — "Quy định về việc gửi nhận VB điện tử"
    //
    // Sử dụng tại:
    //   - frontend/src/components/lgsp-status-timeline.tsx (Plan 36-04 detail page Timeline)
    //   - frontend/src/app/(main)/quan-tri/lgsp-config/ (Phase 37 admin UI — reuse)
    //   - frontend/src/app/(main)/van-ban-di/[id]/ (Phase 37 sender-side UI 13/15/16 — reuse)
    // ============================================================

    /**
     * 9 mã trạng thái QĐ 28/2018:
     *   01 — Đã gửi (sender-side, Phase 35 receive auto-INSERT khi nhận VB từ trục)
     *   02 — Từ chối tiếp nhận (receiver-side, Phase 36 chuyen-lai LGSP doc)
     *   03 — Đã tiếp nhận (receiver-side, Phase 36 mark-read)
     *   04 — Phân công (receiver-side, Phase 36 giao-viec)
     *   05 — Đang xử lý (receiver-side, Phase 36 but-phe + them-vao-hscv)
     *   06 — Hoàn thành (receiver-side, Phase 36 chuyen-luu-tru + HSCV complete)
     *   13 — Lấy lại VB (sender-side, defer Phase 37)
     *   15 — Đồng ý lấy lại (receiver-side, defer Phase 37)
     *   16 — Từ chối lấy lại (receiver-side, defer Phase 37)
     */
    export type LgspTargetStatus = '01' | '02' | '03' | '04' | '05' | '06' | '13' | '15' | '16';

    export const LGSP_STATUS_LABELS: Record<LgspTargetStatus, string> = {
      '01': 'Đã gửi',
      '02': 'Từ chối tiếp nhận',
      '03': 'Đã tiếp nhận',
      '04': 'Phân công',
      '05': 'Đang xử lý',
      '06': 'Hoàn thành',
      '13': 'Lấy lại',
      '15': 'Đồng ý lấy lại',
      '16': 'Từ chối lấy lại',
    };

    /**
     * AntD Tag color (token name hoặc hex).
     * Per CONTEXT D-14: '06'=xanh lá, '02/16'=đỏ, '03/04/05'=vàng/orange, '13'=xám, '15'=xanh, '01'=xanh dương
     */
    export const LGSP_STATUS_COLORS: Record<LgspTargetStatus, string> = {
      '01': 'blue',       // Đã gửi (initial state khi nhận VB)
      '02': 'red',        // Từ chối tiếp nhận (negative)
      '03': 'gold',       // Đã tiếp nhận (in-progress)
      '04': 'orange',     // Phân công (in-progress)
      '05': 'processing', // Đang xử lý (in-progress -- AntD processing color = blue animated)
      '06': 'green',      // Hoàn thành (success final)
      '13': 'default',    // Lấy lại (neutral request)
      '15': 'green',      // Đồng ý lấy lại (success)
      '16': 'red',        // Từ chối lấy lại (negative)
    };

    /**
     * Tooltip description đầy đủ (Vietnamese, hover ra full label).
     */
    export const LGSP_STATUS_DESCRIPTIONS: Record<LgspTargetStatus, string> = {
      '01': 'Đơn vị gửi đã đẩy VB lên trục, đơn vị nhận đã nhận được',
      '02': 'Văn thư đơn vị nhận đã từ chối tiếp nhận VB (kèm lý do)',
      '03': 'Văn thư đơn vị nhận đã tiếp nhận VB vào hệ thống',
      '04': 'Lãnh đạo đã giao việc/phân công VB cho cán bộ xử lý',
      '05': 'Cán bộ đang xử lý VB (có bút phê hoặc đã thêm vào HSCV)',
      '06': 'VB đã được xử lý xong (chuyển lưu trữ hoặc HSCV đóng)',
      '13': 'Đơn vị gửi yêu cầu lấy lại VB đã gửi',
      '15': 'Đơn vị nhận đồng ý cho lấy lại VB',
      '16': 'Đơn vị nhận từ chối yêu cầu lấy lại',
    };

    /**
     * Tag color cho sent_status badge (worker xử lý outbox).
     */
    export const SENT_STATUS_COLORS: Record<'pending' | 'success' | 'error', string> = {
      pending: 'orange',
      success: 'green',
      error: 'red',
    };

    export const SENT_STATUS_LABELS: Record<'pending' | 'success' | 'error', string> = {
      pending: 'Đang chờ gửi',
      success: 'Đã gửi',
      error: 'Lỗi',
    };
    ```

    KHÔNG dùng `<Tag>` JSX trong file này — chỉ data constants. Component dùng dữ liệu này.
  </action>

  <verify>
    <automated>
      cd D:/ProjectAI/quanlyvanban/e_office_app_new/frontend && npx tsc --noEmit && echo "FRONTEND TS OK"
    </automated>
    <automated>
      cd D:/ProjectAI/quanlyvanban && grep -q "export const LGSP_STATUS_LABELS" e_office_app_new/frontend/src/lib/lgsp-status-labels.ts && grep -q "Đã tiếp nhận" e_office_app_new/frontend/src/lib/lgsp-status-labels.ts && grep -q "'06': 'Hoàn thành'" e_office_app_new/frontend/src/lib/lgsp-status-labels.ts && grep -q "export const LGSP_STATUS_COLORS" e_office_app_new/frontend/src/lib/lgsp-status-labels.ts && grep -q "export const LGSP_STATUS_DESCRIPTIONS" e_office_app_new/frontend/src/lib/lgsp-status-labels.ts && grep -q "SENT_STATUS_COLORS" e_office_app_new/frontend/src/lib/lgsp-status-labels.ts && echo "GREP OK"
    </automated>
  </verify>

  <acceptance_criteria>
    - File exists with 5 const exports: `LGSP_STATUS_LABELS`, `LGSP_STATUS_COLORS`, `LGSP_STATUS_DESCRIPTIONS`, `SENT_STATUS_COLORS`, `SENT_STATUS_LABELS`
    - Type `LgspTargetStatus` exported
    - All 9 mã có entry trong 3 record (LABELS + COLORS + DESCRIPTIONS)
    - Vietnamese diacritics present: "Đã tiếp nhận", "Hoàn thành", "Từ chối tiếp nhận", etc.
    - `cd e_office_app_new/frontend && npx tsc --noEmit` exit 0
  </acceptance_criteria>

  <done>
    Helper module exists, exports 9-mã maps + Vietnamese labels + AntD colors + descriptions. Frontend TS clean. Commit: `feat(36-04): them helper lgsp-status-labels.ts (9 ma QD 28 labels + colors + descriptions)`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: NEW frontend/src/components/lgsp-status-timeline.tsx — AntD Timeline component</name>
  <files>e_office_app_new/frontend/src/components/lgsp-status-timeline.tsx</files>

  <read_first>
    1. Read `e_office_app_new/frontend/src/lib/lgsp-source-badge.tsx` — component pattern reference (props interface, AntD usage).
    2. Read `e_office_app_new/frontend/src/lib/api.ts` — axios instance usage pattern.
    3. (Optional) If Context7 MCP available, query for AntD 6 Timeline API: `mcp__context7__get-library-docs` for `/ant-design/ant-design` topic "Timeline component v6".
  </read_first>

  <action>
    Tạo file mới `e_office_app_new/frontend/src/components/lgsp-status-timeline.tsx`:

    ```typescript
    'use client';

    import React, { useCallback, useEffect, useState } from 'react';
    import { Timeline, Tag, Tooltip, Card, Empty, Skeleton, Space, Typography, Spin } from 'antd';
    import { ApiOutlined, ClockCircleOutlined, CheckCircleOutlined, CloseCircleOutlined } from '@ant-design/icons';
    import dayjs from 'dayjs';
    import { api } from '@/lib/api';
    import {
      LGSP_STATUS_LABELS,
      LGSP_STATUS_COLORS,
      LGSP_STATUS_DESCRIPTIONS,
      SENT_STATUS_COLORS,
      SENT_STATUS_LABELS,
      type LgspTargetStatus,
    } from '@/lib/lgsp-status-labels';

    const { Text } = Typography;

    // ============================================================
    // LGSP Status Timeline Component — Phase 36 Plan 36-04
    // REQ: LGSP-STATUS-10
    //
    // Render chronological list outbox events cho 1 VB nguồn LGSP.
    // Fetch tu GET /api/van-ban-den/:id/lgsp-status-history (Plan 36-03).
    // Polling 10s khi co >=1 row pending (auto-refresh sau worker xu ly xong).
    // ============================================================

    /**
     * Row shape tu backend GET /lgsp-status-history (Plan 36-03).
     * BIGINT id pg driver tra string -> Number() wrap.
     */
    export interface LgspStatusHistoryRow {
      id: number;
      target_status: LgspTargetStatus;
      sent_status: 'pending' | 'success' | 'error';
      sent_at: string | null;
      retry_count: number;
      error_message: string | null;
      created_at: string;
    }

    interface Props {
      incomingDocId: number;
    }

    const POLL_INTERVAL_MS = 10_000;

    export function LgspStatusTimeline({ incomingDocId }: Props): React.ReactElement {
      const [rows, setRows] = useState<LgspStatusHistoryRow[]>([]);
      const [loading, setLoading] = useState<boolean>(true);
      const [error, setError] = useState<string | null>(null);

      const fetchHistory = useCallback(async () => {
        try {
          const res = await api.get<{ success: boolean; data: LgspStatusHistoryRow[] }>(
            `/van-ban-den/${incomingDocId}/lgsp-status-history`,
          );
          if (res.data?.success && Array.isArray(res.data.data)) {
            // BIGINT id pg driver tra string -- normalize qua Number()
            setRows(res.data.data.map((r) => ({ ...r, id: Number(r.id), retry_count: Number(r.retry_count) })));
            setError(null);
          } else {
            setRows([]);
          }
        } catch (err: unknown) {
          const msg = err instanceof Error ? err.message : 'Khong tai duoc lich su trang thai LGSP';
          setError(msg);
        } finally {
          setLoading(false);
        }
      }, [incomingDocId]);

      // Initial fetch + polling khi co pending
      useEffect(() => {
        fetchHistory();
      }, [fetchHistory]);

      useEffect(() => {
        const hasPending = rows.some((r) => r.sent_status === 'pending');
        if (!hasPending) return;
        const id = setInterval(() => { fetchHistory(); }, POLL_INTERVAL_MS);
        return () => clearInterval(id);
      }, [rows, fetchHistory]);

      // Render
      const cardTitle = (
        <Space>
          <ApiOutlined style={{ color: '#1B3A5C' }} />
          <span style={{ color: '#1B3A5C', fontWeight: 600 }}>Lịch sử trạng thái LGSP</span>
          <Tag>{rows.length} sự kiện</Tag>
        </Space>
      );

      if (loading) {
        return (
          <Card size="small" title={cardTitle} style={{ marginBottom: 16, borderRadius: 10, boxShadow: '0 1px 3px rgba(0,0,0,0.06)' }}>
            <Skeleton active paragraph={{ rows: 3 }} />
          </Card>
        );
      }

      if (error) {
        return (
          <Card size="small" title={cardTitle} style={{ marginBottom: 16, borderRadius: 10, boxShadow: '0 1px 3px rgba(0,0,0,0.06)' }}>
            <Text type="danger">Lỗi: {error}</Text>
          </Card>
        );
      }

      if (rows.length === 0) {
        return (
          <Card size="small" title={cardTitle} style={{ marginBottom: 16, borderRadius: 10, boxShadow: '0 1px 3px rgba(0,0,0,0.06)' }}>
            <Empty description="Chưa có sự kiện trạng thái LGSP nào cho văn bản này" image={Empty.PRESENTED_IMAGE_SIMPLE} />
          </Card>
        );
      }

      // AntD v6 prefer items prop. Map rows -> Timeline items.
      const items = rows.map((r) => {
        const targetLabel = LGSP_STATUS_LABELS[r.target_status] ?? r.target_status;
        const targetTooltip = LGSP_STATUS_DESCRIPTIONS[r.target_status] ?? targetLabel;
        const targetColor = LGSP_STATUS_COLORS[r.target_status] ?? 'default';
        const sentLabel = SENT_STATUS_LABELS[r.sent_status];
        const sentColor = SENT_STATUS_COLORS[r.sent_status];

        // Timeline dot color: ưu tiên sent_status (vì worker chưa đẩy thì chưa biết success)
        let dotColor: string;
        let dot: React.ReactNode | undefined;
        if (r.sent_status === 'success') {
          dotColor = 'green';
          dot = <CheckCircleOutlined style={{ fontSize: 16, color: '#52c41a' }} />;
        } else if (r.sent_status === 'error') {
          dotColor = 'red';
          dot = <CloseCircleOutlined style={{ fontSize: 16, color: '#ff4d4f' }} />;
        } else {
          dotColor = 'orange';
          dot = <Spin size="small" />;
        }

        const labelText = (
          <Text style={{ fontSize: 12, color: '#8c8c8c' }}>
            {dayjs(r.created_at).format('DD/MM HH:mm')}
          </Text>
        );

        const children = (
          <Space direction="vertical" size={4} style={{ width: '100%' }}>
            <Space size={8}>
              <Tooltip title={targetTooltip}>
                <Tag color={targetColor}>{r.target_status} — {targetLabel}</Tag>
              </Tooltip>
              <Tag color={sentColor}>{sentLabel}</Tag>
              {r.sent_status === 'error' && r.retry_count > 0 && (
                <Tooltip title={`Đã retry ${r.retry_count} lần`}>
                  <Tag icon={<ClockCircleOutlined />}>{r.retry_count}/5</Tag>
                </Tooltip>
              )}
            </Space>
            {r.sent_status === 'success' && r.sent_at && (
              <Text type="secondary" style={{ fontSize: 12 }}>
                Đã đẩy lên trục lúc {dayjs(r.sent_at).format('DD/MM/YYYY HH:mm:ss')}
              </Text>
            )}
            {r.sent_status === 'error' && r.error_message && (
              <Tooltip title={r.error_message}>
                <Text type="danger" style={{ fontSize: 12, display: 'block', maxWidth: 600, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>
                  {r.error_message}
                </Text>
              </Tooltip>
            )}
          </Space>
        );

        return {
          key: String(r.id),
          color: dotColor,
          dot,
          label: labelText,
          children,
        };
      });

      return (
        <Card
          size="small"
          title={cardTitle}
          style={{ marginBottom: 16, borderRadius: 10, boxShadow: '0 1px 3px rgba(0,0,0,0.06)' }}
        >
          <Timeline mode="left" items={items} />
        </Card>
      );
    }
    ```

    KHÔNG dùng `<Timeline.Item>` JSX (deprecated v6) — dùng `items` prop. Polling: chỉ start khi có pending, stop khi không còn pending (saves bandwidth).
  </action>

  <verify>
    <automated>
      cd D:/ProjectAI/quanlyvanban/e_office_app_new/frontend && npx tsc --noEmit && echo "FRONTEND TS OK"
    </automated>
    <automated>
      cd D:/ProjectAI/quanlyvanban && grep -q "export function LgspStatusTimeline" e_office_app_new/frontend/src/components/lgsp-status-timeline.tsx && grep -q "import { Timeline, Tag, Tooltip" e_office_app_new/frontend/src/components/lgsp-status-timeline.tsx && grep -q "/lgsp-status-history" e_office_app_new/frontend/src/components/lgsp-status-timeline.tsx && grep -q "POLL_INTERVAL_MS = 10_000" e_office_app_new/frontend/src/components/lgsp-status-timeline.tsx && grep -q "items={items}" e_office_app_new/frontend/src/components/lgsp-status-timeline.tsx && grep -q "Lịch sử trạng thái LGSP" e_office_app_new/frontend/src/components/lgsp-status-timeline.tsx && echo "GREP OK"
    </automated>
  </verify>

  <acceptance_criteria>
    - File exists with `export function LgspStatusTimeline({incomingDocId}: Props)`
    - Uses AntD `Timeline items={items}` (v6 functional API)
    - Fetches `/van-ban-den/${incomingDocId}/lgsp-status-history` via api.get
    - Polling 10s khi có pending event (useEffect cleanup with clearInterval)
    - 3 render states: loading (Skeleton), error (text), empty (Empty), data (Timeline)
    - BIGINT id wrapped via `Number(r.id)` (CLAUDE.md pitfall #9)
    - Vietnamese có dấu trong UI labels
    - Import từ `@/lib/lgsp-status-labels` (Task 1 module)
    - `cd e_office_app_new/frontend && npx tsc --noEmit` exit 0
  </acceptance_criteria>

  <done>
    Component exists, renders Timeline với 3 state + polling 10s pending. Reuses Task 1 labels. Frontend TS clean. Commit: `feat(36-04): them component LgspStatusTimeline (AntD Timeline items + polling 10s pending)`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire LgspStatusTimeline vào VB đến detail page</name>
  <files>e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx</files>

  <read_first>
    1. Read `e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx` lines 1-30 (imports — verify Timeline đã có hay chưa, có thể remove dòng `Timeline` từ AntD imports nếu trùng).
    2. Read `e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx` lines 555-590 (Phase 35-04 Nguồn LGSP section + line 589 start `<Row gutter={16}>`) — vị trí insert Timeline section.
  </read_first>

  <action>
    Edit `e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx`:

    **Phần A: Add import** (after existing imports):
    ```typescript
    import { LgspStatusTimeline } from '@/components/lgsp-status-timeline';
    ```

    Lưu ý: file hiện đã import `Timeline` từ antd ở line 5 — KHÔNG remove (page dùng cho event history Timeline ở section khác).

    **Phần B: Insert Card section MỚI** ngay SAU Phase 35-04 Nguồn LGSP card (line ~587), TRƯỚC `<Row gutter={16}>` (line ~589):

    ```tsx
    {/* ====== Phase 36 Plan 36-04: Lịch sử trạng thái LGSP — chỉ hiển thị khi source_type='external_lgsp' ====== */}
    {doc.source_type === 'external_lgsp' && (
      <LgspStatusTimeline incomingDocId={Number(doc.id)} />
    )}
    ```

    Vị trí: trong same conditional block với Nguồn LGSP card (tức là chỉ render khi source_type='external_lgsp'), nhưng tách 2 Card riêng (Nguồn LGSP info ở trên, Timeline lịch sử ở dưới).

    KHÔNG sửa Phase 35-04 Nguồn LGSP card. KHÔNG thay đổi conditional Phase 35-04. Chỉ INSERT thêm 1 Card mới sau nó.
  </action>

  <verify>
    <automated>
      cd D:/ProjectAI/quanlyvanban/e_office_app_new/frontend && npx tsc --noEmit && echo "FRONTEND TS OK"
    </automated>
    <automated>
      cd D:/ProjectAI/quanlyvanban/e_office_app_new/frontend && npm run build 2>&1 | tail -30
    </automated>
    <automated>
      cd D:/ProjectAI/quanlyvanban && grep -q "import { LgspStatusTimeline } from '@/components/lgsp-status-timeline'" e_office_app_new/frontend/src/app/\(main\)/van-ban-den/\[id\]/page.tsx && grep -q "LgspStatusTimeline incomingDocId" e_office_app_new/frontend/src/app/\(main\)/van-ban-den/\[id\]/page.tsx && echo "GREP OK"
    </automated>
  </verify>

  <acceptance_criteria>
    - Page imports LgspStatusTimeline component
    - `grep -q "<LgspStatusTimeline incomingDocId={Number(doc.id)} />"` exit 0 (BIGINT safety per CLAUDE.md)
    - Insertion is INSIDE `{doc.source_type === 'external_lgsp' && (...)}` conditional
    - `cd e_office_app_new/frontend && npx tsc --noEmit` exit 0
    - `cd e_office_app_new/frontend && npm run build` exit 0 (Next.js production build pass — CLAUDE.md pitfall #4)
  </acceptance_criteria>

  <done>
    Detail page renders LgspStatusTimeline conditional dưới Phase 35-04 Nguồn LGSP card khi source_type='external_lgsp'. Frontend TS + production build clean. Commit: `feat(36-04): wire LgspStatusTimeline vao VB den detail page (conditional external_lgsp)`.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks shipped:

1. **TypeScript frontend clean:**
   - `cd e_office_app_new/frontend && npx tsc --noEmit` exit 0
   - `cd e_office_app_new/frontend && npm run build` exit 0

2. **Visual smoke test** (manual after Plan 36-03 wired):
   - Open `/van-ban-den/<id>` for a doc with `source_type='external_lgsp'`
   - Verify Phase 35-04 "Nguồn LGSP" card renders
   - Verify Phase 36-04 "Lịch sử trạng thái LGSP" card renders BELOW it
   - If no outbox events yet → "Chưa có sự kiện trạng thái LGSP nào cho văn bản này" Empty state
   - If events present → Timeline with mã+sent_status tags + timestamps

3. **Acceptance grep total:**
   - 3 new files created (lib + components + edit page)
   - All Vietnamese diacritics preserved
</verification>

<success_criteria>
- 3 files (2 NEW + 1 MODIFIED)
- AntD Timeline v6 items prop pattern used
- Polling 10s khi có pending event (saves bandwidth khi không cần)
- 3 render states: loading/error/empty/data
- BIGINT id normalized via Number()
- Vietnamese có dấu trong UI
- Conditional render chỉ khi source_type='external_lgsp'
- Frontend TS strict + Next.js production build clean
- 3 commits with feat(36-04): prefix
</success_criteria>

<output>
After completion, create `.planning/phases/36-status-callback-chain-9-ma-qd-28/36-04-SUMMARY.md` with:
- Frontmatter (phase, plan, subsystem=frontend, tags, requirements: [LGSP-STATUS-10, LGSP-STATUS-07 partial])
- dependency_graph (requires: 36-01 schema + 36-03 GET endpoint; provides: 1 helper lib + 1 component + page extension)
- key_files (created: 2, modified: 1)
- decisions honored (D-14 UI Timeline pattern + color mapping)
- Vietnamese diacritics preserved
- AntD v6 Timeline items prop verified
- Polling 10s when pending only (saves bandwidth)
- next_steps: Plan 36-05 E2E verification including UI visual check
</output>
