---
phase: 32-hdsd-audit-update
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/hdsd/HDSD_van_ban_den.md
  - docs/hdsd/HDSD_van_ban_di.md
  - docs/hdsd/HDSD_van_ban_du_thao.md
  - docs/hdsd/HDSD_van_ban_danh_dau.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "HDSD_van_ban_den.md has new subsection 'Sau khi giao việc — văn bản xuất hiện ở đâu?' (GAP 1)"
    - "HDSD_van_ban_di.md has new subsection 'Luồng kỹ thuật khi Gửi (3 bước nối tiếp)' (GAP 3)"
    - "HDSD_van_ban_di.md mục 3.8 has new subsection 'Sau khi giao việc — văn bản xuất hiện ở đâu?' + gap note about no bell (GAP 2)"
    - "All 4 VB HDSD files reflect current button/menu/field state per audit report"
  artifacts:
    - path: "docs/hdsd/HDSD_van_ban_den.md"
      provides: "VB đến HDSD with GAP 1 filled"
      contains: "Sau khi giao việc — văn bản xuất hiện ở đâu"
    - path: "docs/hdsd/HDSD_van_ban_di.md"
      provides: "VB đi HDSD with GAP 2 + GAP 3 filled"
      contains: "Luồng kỹ thuật khi Gửi"
    - path: "docs/hdsd/HDSD_van_ban_du_thao.md"
      provides: "VB dự thảo HDSD aligned with current UI"
    - path: "docs/hdsd/HDSD_van_ban_danh_dau.md"
      provides: "VB đánh dấu HDSD aligned with current UI"
  key_links:
    - from: "docs/hdsd/HDSD_van_ban_den.md"
      to: "32-CONTEXT.md <specifics> block 1"
      via: "Exact text inserted verbatim"
      pattern: "TK được chọn làm Người phụ trách"
    - from: "docs/hdsd/HDSD_van_ban_di.md"
      to: "32-CONTEXT.md <specifics> block 2"
      via: "Exact text inserted verbatim"
      pattern: "outgoing_doc_recipients"
---

<objective>
Update text of 4 VB HDSD files (đến, đi, dự thảo, đánh dấu) per audit report. Critical: insert 3 GAP subsections with exact text from CONTEXT.md `<specifics>`.

Purpose: VB module is the core nghiệp vụ — testers and end-users hit these flows daily. The 3 GAPs are tester-reported and must be addressed verbatim.

Output: 4 updated `docs/hdsd/HDSD_van_ban_*.md` files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md
@.planning/phases/32-hdsd-audit-update/32-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update HDSD_van_ban_den.md — insert GAP 1 + align text per audit</name>
  <read_first>
    - .planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md (section for HDSD_van_ban_den.md)
    - .planning/phases/32-hdsd-audit-update/32-CONTEXT.md (lines 188-206 — exact text for GAP 1)
    - docs/hdsd/HDSD_van_ban_den.md (full file, especially lines 270-300)
    - e_office_app_new/backend/src/routes/incoming-doc.ts lines 909-973 (giao việc + notifyBell)
    - e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx lines 182-202
  </read_first>
  <files>docs/hdsd/HDSD_van_ban_den.md</files>
  <action>
    1. Find the line ending the "Thông báo của hệ thống" table in mục 3.6 (currently around line 296 — the row "Không có quyền | Không có quyền giao xử lý văn bản đến này").
    2. After the table (next blank line after the row), INSERT verbatim (copy exact characters from 32-CONTEXT.md `<specifics>` block 1, lines 192-206 inside the ```markdown fence):

    ```
    #### Sau khi giao việc — văn bản xuất hiện ở đâu của TK được giao?

    Khi bấm "Tạo và giao việc", hệ thống tạo một Hồ sơ công việc (HSCV) mới và liên kết với văn bản đến này:

    1. **TK được chọn làm Người phụ trách** sẽ nhận:
       - **Chuông thông báo** (góc trên màn hình) với nội dung "Bạn vừa được giao việc..." — bấm vào điều hướng sang HSCV chi tiết.
       - HSCV mới xuất hiện ở trang **Hồ sơ công việc** (menu trái), tab **"Mới tạo"** (status mặc định khi giao việc).

    2. **Vào chi tiết HSCV**, người được giao thấy:
       - Thông tin HSCV (tên, ngày bắt đầu, hạn, ghi chú, người phụ trách)
       - Văn bản đến nguồn được link sẵn — bấm để mở chi tiết văn bản

    3. **Văn bản đến** vẫn nằm ở trang VB đến cũ của người tiếp nhận ban đầu (lãnh đạo) — KHÔNG bị chuyển đi.
    ```

    3. Apply remaining diffs from audit report for HDSD_van_ban_den.md (button names, field maxLength, notifications). If audit verdict is "No additional change" beyond GAP 1, skip to verification.
    4. Verify final file: subsection heading "#### Sau khi giao việc — văn bản xuất hiện ở đâu của TK được giao?" appears exactly once.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content -Raw docs/hdsd/HDSD_van_ban_den.md; if (($c -match 'Sau khi giao việc — văn bản xuất hiện ở đâu của TK được giao') -and ($c -match 'TK được chọn làm Người phụ trách')) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>
    - File contains new subsection heading
    - Exact text from CONTEXT.md `<specifics>` block 1 is present
    - Inserted right after mục 3.6 "Thông báo của hệ thống" table, before "### 3.7. Modal chuyển lại văn bản"
    - No duplicate insertion
  </done>
</task>

<task type="auto">
  <name>Task 2: Update HDSD_van_ban_di.md — insert GAP 2 (3.8) + GAP 3 (3.7) + align text</name>
  <read_first>
    - .planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md (section for HDSD_van_ban_di.md)
    - .planning/phases/32-hdsd-audit-update/32-CONTEXT.md (lines 213-238 — exact text for GAP 3)
    - docs/hdsd/HDSD_van_ban_di.md (full file, especially lines 280-360)
    - e_office_app_new/backend/src/routes/outgoing-doc.ts lines 651-719 (Ban hành & Gửi 3 SP)
    - e_office_app_new/backend/src/routes/outgoing-doc.ts lines 899-926 (giao việc VB đi — KHÔNG notifyBell)
    - e_office_app_new/database/schema/000_schema_v3.0.sql lines 27846-27948 (SP send_to_recipients)
    - e_office_app_new/workers/src/index.ts lines 210-243 (worker LGSP send)
    - e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx lines 204-217, 336-357, 495
  </read_first>
  <files>docs/hdsd/HDSD_van_ban_di.md</files>
  <action>
    **Step A — GAP 3 (mục 3.7 Modal Ban hành & Gửi):**

    1. Find the end of "Thông báo của hệ thống" table in mục 3.7 (currently around line 309, last row "Ban hành & Gửi thành công").
    2. After that table (next blank line), INSERT verbatim from 32-CONTEXT.md `<specifics>` block 2:

    ```
    #### Luồng kỹ thuật khi Gửi (3 bước nối tiếp)

    Khi bấm nút "Ban hành & Gửi" hoặc "Gửi" trong modal, hệ thống thực hiện 3 bước nối tiếp:

    **Bước 1 — Lưu danh sách đơn vị nhận:**
    - Mỗi đơn vị/cơ quan được chọn được lưu thành 1 dòng trong bảng `outgoing_doc_recipients` với `sent_status='pending'`.
    - Phân loại: `internal_unit` (đơn vị trong tỉnh) hoặc `external_org` (cơ quan ngoài tỉnh — gửi qua LGSP).

    **Bước 2 — Ban hành cấp số:**
    - Hệ thống cấp số văn bản đi chính thức và đánh dấu `is_released = TRUE`.
    - Trạng thái văn bản đi chuyển sang **"Đã ban hành"**.

    **Bước 3 — Gửi (loop từng recipient):**

    - **Nếu là đơn vị Nội bộ:** Hệ thống tự sinh 1 Văn bản đến cho đơn vị nhận:
      - Văn bản đến nằm ngay ở trang **Văn bản đến** của đơn vị nhận, trạng thái "Chưa duyệt".
      - Người nhận có thể đăng nhập và xử lý ngay (duyệt → giao việc → ban hành công văn trả lời...).
      - Số văn bản đến được cấp tự động theo sổ của đơn vị nhận.

    - **Nếu là cơ quan ngoài LGSP:** Hệ thống tạo bản ghi trong `lgsp_tracking` (trạng thái `pending`):
      - Badge **"Đang chờ worker đẩy LGSP"** hiển thị ở khung "Đơn vị / Cơ quan nhận" trên trang chi tiết.
      - Worker BullMQ (`lgsp-send`) sẽ pick lên, gọi API LGSP thật (apiltvb.langson.gov.vn), update trạng thái → `success` / `error`.
      - Khi worker xong, badge đổi thành **"Đã gửi LGSP"** hoặc **"Lỗi gửi LGSP"** (kèm thông báo lỗi).

    Trạng thái cuối cùng của văn bản đi: **"Đã gửi"** (nội bộ done ngay, LGSP đợi worker).
    ```

    **Step B — GAP 2 (mục 3.8 Drawer giao việc):**

    1. Find the end of mục 3.8 "Thông báo của hệ thống" table.
    2. After that table, INSERT:

    ```
    #### Sau khi giao việc — văn bản xuất hiện ở đâu?

    Khi bấm "Tạo và giao việc" trong Drawer giao việc của VB đi, hệ thống tạo một Hồ sơ công việc (HSCV) mới liên kết với văn bản đi này:

    1. **TK được chọn làm Người phụ trách** sẽ thấy:
       - HSCV mới xuất hiện ở trang **Hồ sơ công việc** (menu trái), tab **"Mới tạo"**.
       - **KHÔNG có chuông thông báo** (gap code chưa fix — sẽ bổ sung ở phiên bản sau v3.2+). Người được giao phải tự vào trang HSCV để biết có việc mới.

    2. **Vào chi tiết HSCV**, người được giao thấy:
       - Thông tin HSCV (tên, ngày bắt đầu, hạn, ghi chú, người phụ trách).
       - Văn bản đi nguồn được link sẵn — bấm để mở chi tiết văn bản.

    3. **Văn bản đi** vẫn nằm ở trang VB đi của người soạn — KHÔNG bị chuyển đi.

    **Lưu ý:** form Drawer giao việc VB đi hiện KHÔNG validate `Người phụ trách` rỗng và `Hạn hoàn thành` rỗng — đề nghị người dùng nhập đủ các trường trước khi bấm Tạo. Validation sẽ bổ sung ở phiên bản sau (v3.2+).
    ```

    **Step C — Apply remaining audit diffs** (button/field/notification per audit report). Skip if "No additional change".

    Verify both subsections present, in correct positions, no duplicates.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content -Raw docs/hdsd/HDSD_van_ban_di.md; $a = $c -match 'Luồng kỹ thuật khi Gửi'; $b = $c -match 'outgoing_doc_recipients'; $d = $c -match 'KHÔNG có chuông thông báo'; $e = $c -match 'lgsp_tracking'; if ($a -and $b -and $d -and $e) { exit 0 } else { Write-Host \"a=$a b=$b d=$d e=$e\"; exit 1 }"</automated>
  </verify>
  <done>
    - Subsection "Luồng kỹ thuật khi Gửi (3 bước nối tiếp)" inserted in mục 3.7 (after Thông báo bảng)
    - Subsection "Sau khi giao việc — văn bản xuất hiện ở đâu?" + lưu ý gap inserted in mục 3.8
    - Both subsections contain exact technical terms (`outgoing_doc_recipients`, `lgsp_tracking`, `KHÔNG có chuông thông báo`)
    - No duplicate insertions
  </done>
</task>

<task type="auto">
  <name>Task 3: Update HDSD_van_ban_du_thao.md + HDSD_van_ban_danh_dau.md per audit</name>
  <read_first>
    - .planning/phases/32-hdsd-audit-update/32-AUDIT-REPORT.md (sections for both files)
    - docs/hdsd/HDSD_van_ban_du_thao.md (full)
    - docs/hdsd/HDSD_van_ban_danh_dau.md (full)
    - e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx
    - e_office_app_new/frontend/src/app/(main)/van-ban-danh-dau/page.tsx
  </read_first>
  <files>docs/hdsd/HDSD_van_ban_du_thao.md, docs/hdsd/HDSD_van_ban_danh_dau.md</files>
  <action>
    For each file, apply text diffs listed in audit report:

    1. **Button/menu names** — Use exact label from current frontend (grep `<Button>...</Button>` in pages). If HDSD says "Lưu nháp" but UI says "Lưu bản nháp" → update HDSD.
    2. **Field maxLength** — If audit notes field length changed in DB schema v3.0, update the "Mô tả & ràng buộc" column.
    3. **Workflow notes** — If audit flags a workflow gap (e.g., new status transitions), insert a brief note.
    4. **Screenshot references** — Verify `![](screenshots/xxx.png)` references match files in `docs/hdsd/screenshots/`. If audit says a screenshot will be renamed, update reference now (Plan 06 will overwrite the file).

    If audit verdict for a file is "No change needed", leave the file untouched but document in this task's commit message that the file was reviewed.

    Common cross-check pattern:
    - Open frontend page.tsx → list visible buttons/labels
    - Open HDSD → list mentioned buttons/labels
    - Diff → align HDSD to UI
  </action>
  <verify>
    <automated>powershell -Command "$f1 = Test-Path docs/hdsd/HDSD_van_ban_du_thao.md; $f2 = Test-Path docs/hdsd/HDSD_van_ban_danh_dau.md; if ($f1 -and $f2) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>
    - Both files exist and reflect audit-listed diffs
    - Button/field names match current UI
    - Screenshot path references valid
  </done>
</task>

</tasks>

<verification>
- `grep -c "Sau khi giao việc — văn bản xuất hiện ở đâu" docs/hdsd/HDSD_van_ban_den.md` should be ≥ 1
- `grep -c "Luồng kỹ thuật khi Gửi" docs/hdsd/HDSD_van_ban_di.md` should be ≥ 1
- `grep -c "KHÔNG có chuông thông báo" docs/hdsd/HDSD_van_ban_di.md` should be ≥ 1
</verification>

<success_criteria>
- All 3 GAP subsections appear in their target files at the correct insertion points
- Text matches CONTEXT.md `<specifics>` verbatim
- Audit-listed text diffs applied
</success_criteria>

<output>
After completion, create `.planning/phases/32-hdsd-audit-update/32-02-SUMMARY.md` listing which lines were inserted/changed in each file.
</output>
