---
phase: 04-l-ch-danh-b-dashboard
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/lich/ca-nhan/page.tsx
  - e_office_app_new/frontend/src/app/(main)/lich/co-quan/page.tsx
  - e_office_app_new/frontend/src/app/(main)/lich/lanh-dao/page.tsx
  - e_office_app_new/frontend/src/app/(main)/danh-ba/page.tsx
  - e_office_app_new/frontend/src/components/layout/MainLayout.tsx
autonomous: false
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-04

must_haves:
  truths:
    - "User can view personal calendar with events rendered on date cells"
    - "User can create event via Popover on click and Drawer on double-click"
    - "Admin can manage unit calendar with list/calendar toggle"
    - "User can search directory by name and filter by department"
    - "Sidebar menu shows Lich and Danh ba navigation entries"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/lich/ca-nhan/page.tsx"
      provides: "Personal calendar page"
      min_lines: 100
    - path: "e_office_app_new/frontend/src/app/(main)/lich/co-quan/page.tsx"
      provides: "Unit calendar page"
      min_lines: 80
    - path: "e_office_app_new/frontend/src/app/(main)/lich/lanh-dao/page.tsx"
      provides: "Leader calendar page"
      min_lines: 80
    - path: "e_office_app_new/frontend/src/app/(main)/danh-ba/page.tsx"
      provides: "Staff directory page"
      min_lines: 80
  key_links:
    - from: "e_office_app_new/frontend/src/app/(main)/lich/ca-nhan/page.tsx"
      to: "/api/lich/events"
      via: "api.get with scope=personal"
      pattern: "api\\.(get|post).*lich/events"
    - from: "e_office_app_new/frontend/src/app/(main)/danh-ba/page.tsx"
      to: "/api/danh-ba"
      via: "api.get with pagination"
      pattern: "api\\.get.*danh-ba"
    - from: "e_office_app_new/frontend/src/components/layout/MainLayout.tsx"
      to: "lich/ca-nhan, lich/co-quan, lich/lanh-dao, danh-ba"
      via: "menuItems array"
      pattern: "lich/ca-nhan"
---

<objective>
Create 3 calendar pages (personal, unit, leader), directory page, and update sidebar navigation.

Purpose: Users need calendar views to manage events and a directory to look up staff contacts. Sidebar needs new menu entries for navigation.
Output: 4 new page files + updated MainLayout with menu items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-l-ch-danh-b-dashboard/04-CONTEXT.md
@.planning/phases/04-l-ch-danh-b-dashboard/04-02-SUMMARY.md

<interfaces>
Calendar API endpoints (from Plan 02):
```
GET /api/lich/events?scope=personal&start=2026-04-01&end=2026-04-30 → CalendarEventRow[]
GET /api/lich/events/:id → CalendarEventRow
POST /api/lich/events → { title, description, start_time, end_time, all_day, color, repeat_type, scope }
PUT /api/lich/events/:id → { ...same fields }
DELETE /api/lich/events/:id → { success, message }
```

Directory API endpoint (from Plan 02):
```
GET /api/danh-ba?unit_id=1&department_id=2&search=abc&page=1&page_size=20 → { data: DirectoryRow[], total: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calendar pages — personal, unit, leader</name>
  <files>
    e_office_app_new/frontend/src/app/(main)/lich/ca-nhan/page.tsx
    e_office_app_new/frontend/src/app/(main)/lich/co-quan/page.tsx
    e_office_app_new/frontend/src/app/(main)/lich/lanh-dao/page.tsx
  </files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/dashboard/page.tsx (page structure pattern)
    - e_office_app_new/frontend/src/app/globals.css (page-header, page-card, drawer-gradient CSS)
    - e_office_app_new/frontend/src/lib/api.ts (axios instance)
    - e_office_app_new/frontend/src/stores/auth.store.ts (useAuthStore for staffId, unitId)
  </read_first>
  <action>
**Personal Calendar — `/lich/ca-nhan/page.tsx` (per D-01, D-02, D-03, D-04):**

1. Page header: "Lich ca nhan" with page-header/page-title classes
2. Use Ant Design `<Calendar />` component in month view as primary display
3. `cellRender` / `dateCellRender` to show event cards on calendar cells:
   - Fetch events from `api.get('/lich/events', { params: { scope: 'personal', start, end } })` on mount and when month changes
   - Render small colored badges/tags for each event on that date (use event.color)
   - Show event title truncated to fit cell
4. **Click day → Popover** (per D-02): Show Popover with quick-create form (title, time range, color select). On submit POST to `/lich/events` with scope='personal'.
5. **Double-click day → Drawer** (per D-02): Open Drawer (width 720, rootClassName="drawer-gradient") with full form:
   - Fields: Tieu de (title, required), Mo ta (description, textarea), Thoi gian bat dau (DatePicker showTime), Thoi gian ket thuc (DatePicker showTime), Ca ngay (Switch for all_day), Mau sac (ColorPicker — use Ant Design ColorPicker for D-03), Lap lai (Select: Khong lap lai/Hang ngay/Hang tuan/Hang thang)
   - Footer: Luu + Huy buttons
   - On save: POST or PUT to /lich/events
6. Click existing event on calendar → open Drawer with pre-filled data for editing
7. Delete: Button in Drawer footer with Modal.confirm
8. Loading state: Skeleton while fetching events
9. All text Vietnamese with diacritics

**Unit Calendar — `/lich/co-quan/page.tsx` (per D-05, D-06, D-07):**

1. Same Calendar component as personal but with `scope='unit'`
2. Fetch events with scope=unit and unitId from auth store
3. Only admin/secretary role can create events (check user roles, hide create button for others)
4. **Toggle view** (per D-06): Add Segmented control or Radio.Group at top: "Lich" (calendar view) | "Danh sach" (list view)
   - Calendar view: same as personal
   - List view: Ant Design Table with columns: Tieu de, Thoi gian, Mo ta, Nguoi tao. Filtered to current week. Sorted by start_time.
5. Same Drawer for add/edit as personal calendar
6. Page header: "Lich co quan"

**Leader Calendar — `/lich/lanh-dao/page.tsx` (per D-08, D-09):**

1. Same structure as unit calendar but with `scope='leader'`
2. Fetch events with scope=leader and unitId
3. Access control: only users with leader role or configured permission can view
4. If user lacks permission, show Empty component with message "Ban khong co quyen xem lich lanh dao"
5. Create/edit same Drawer pattern
6. Page header: "Lich lanh dao"

**Code reuse:** Extract a shared `CalendarView` component pattern — or duplicate with scope param since each page is ~150 lines and the scope logic differs. Prefer duplication over premature abstraction for 3 similar-but-not-identical pages.

Use `dayjs` for date formatting. Use `App.useApp()` for message.success/error notifications.
  </action>
  <verify>
    <automated>ls e_office_app_new/frontend/src/app/\(main\)/lich/ca-nhan/page.tsx e_office_app_new/frontend/src/app/\(main\)/lich/co-quan/page.tsx e_office_app_new/frontend/src/app/\(main\)/lich/lanh-dao/page.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 3 calendar page files exist at correct routes
    - Personal calendar fetches scope=personal events and renders on Ant Design Calendar
    - Click day shows Popover for quick create, double-click opens Drawer
    - Drawer has all fields: title, description, start/end time, all_day, color picker, repeat_type
    - Unit calendar has calendar/list toggle view (Segmented control)
    - Leader calendar checks permission before showing content
    - All text Vietnamese with diacritics
    - All pages use page-header, page-card, drawer-gradient CSS classes
  </acceptance_criteria>
  <done>3 calendar pages render events on calendar view, support CRUD via Popover/Drawer, with correct scope filtering</done>
</task>

<task type="auto">
  <name>Task 2: Directory page + sidebar navigation update</name>
  <files>
    e_office_app_new/frontend/src/app/(main)/danh-ba/page.tsx
    e_office_app_new/frontend/src/components/layout/MainLayout.tsx
  </files>
  <read_first>
    - e_office_app_new/frontend/src/components/layout/MainLayout.tsx (sidebar menuItems array + breadcrumbMap)
    - e_office_app_new/frontend/src/lib/tree-utils.ts (buildTree for department filter, per D-11)
    - e_office_app_new/frontend/src/app/(main)/quan-tri/nguoi-dung/page.tsx (staff table pattern reference)
  </read_first>
  <action>
**Directory page — `/danh-ba/page.tsx` (per D-10, D-11, D-12):**

1. Page header: "Danh ba dien thoai" with page-header/page-title classes
2. Filter row (filter-row class): 
   - TreeSelect for don vi/phong ban (per D-11): Fetch department tree from `/quan-tri/don-vi/tree` (existing API), use buildTree from tree-utils. On change, update unit_id/department_id params.
   - Input.Search for name search (per D-11)
3. Table (per D-10) with columns:
   - Ho ten (full_name) — bold text
   - Chuc vu (position_name)
   - Phong ban (department_name)
   - So dien thoai (phone or mobile) — display mobile preferred, fallback to phone
   - Email (email)
   - Avatar: show image if exists, otherwise UserOutlined avatar
4. Pagination: Server-side via page/page_size params. Display total count.
5. Fetch from `api.get('/danh-ba', { params: { unit_id, department_id, search, page, page_size: 20 } })`
6. Loading: `loading` prop on Table
7. Empty state: "Khong tim thay can bo nao"

**MainLayout.tsx sidebar update:**

Add 2 new menu groups to the `menuItems` array, placed AFTER "Thong bao" and BEFORE the divider:

```typescript
{
  key: 'lich',
  icon: <CalendarOutlined />,
  label: 'Lich',
  children: [
    { key: '/lich/ca-nhan', icon: <UserOutlined />, label: 'Lich ca nhan' },
    { key: '/lich/co-quan', icon: <BankOutlined />, label: 'Lich co quan' },
    { key: '/lich/lanh-dao', icon: <SolutionOutlined />, label: 'Lich lanh dao' },
  ],
},
{
  key: '/danh-ba',
  icon: <ContactsOutlined />,  // Import ContactsOutlined from @ant-design/icons
  label: 'Danh ba',
},
```

Also add breadcrumb mappings for new routes:
```typescript
'/lich/ca-nhan': 'Lich ca nhan',
'/lich/co-quan': 'Lich co quan',
'/lich/lanh-dao': 'Lich lanh dao',
'/danh-ba': 'Danh ba dien thoai',
```

Import `ContactsOutlined` from @ant-design/icons (add to existing import list).

**All Vietnamese text MUST have diacritics** in the actual code. The action description above omits diacritics for ASCII compatibility — the executor MUST write proper Vietnamese (e.g., "Lich ca nhan" → "Lịch cá nhân", "Danh ba dien thoai" → "Danh bạ điện thoại").
  </action>
  <verify>
    <automated>grep -n "lich/ca-nhan\|danh-ba\|ContactsOutlined" e_office_app_new/frontend/src/components/layout/MainLayout.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - danh-ba/page.tsx exists with Table showing full_name, position_name, department_name, phone, email columns
    - Directory page has TreeSelect filter for department and Input.Search for name
    - Directory page fetches from /api/danh-ba with pagination
    - MainLayout.tsx menuItems includes Lich submenu (3 children) and Danh ba entry
    - MainLayout.tsx breadcrumbMap includes all 4 new routes
    - ContactsOutlined icon imported and used
    - All UI text is Vietnamese with diacritics
  </acceptance_criteria>
  <done>Directory page shows searchable staff table, sidebar has calendar and directory menu entries</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify calendar, directory, and dashboard</name>
  <files>none</files>
  <action>
Human verification of the complete Phase 4 implementation. No automated action — present the verification checklist to the user.
  </action>
  <what-built>
    Complete Phase 4: 3 calendar pages with event CRUD, directory with search/filter, dashboard with real KPI data and draggable widgets.
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/dashboard — verify 4 KPI cards show numbers (may be 0 if no data), click a card to navigate to its list
    2. On dashboard, verify 3 widgets load (may show empty states). Try dragging a widget to reorder. Refresh page — layout should persist.
    3. Visit http://localhost:3000/lich/ca-nhan — verify calendar renders. Click a date to see Popover. Double-click to open Drawer. Create an event and verify it appears on the calendar.
    4. Visit http://localhost:3000/lich/co-quan — verify calendar/list toggle works
    5. Visit http://localhost:3000/lich/lanh-dao — verify calendar loads (or shows permission denied)
    6. Visit http://localhost:3000/danh-ba — verify staff table with department filter and search
    7. Check sidebar — "Lich" submenu and "Danh ba" entries visible and navigable
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint"</automated>
  </verify>
  <done>User confirms all 7 verification steps pass</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Frontend | User input in calendar forms and search |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-11 | T (Tampering) | Calendar form input | accept | Validation done server-side in SP; frontend form validation is UX only |
| T-04-12 | I (Info Disclosure) | Directory contact info | accept | Directory shows only public contact info that all authenticated users should see |
</threat_model>

<verification>
- All 4 new pages render without console errors
- Sidebar navigation includes Lich and Danh ba
- Calendar CRUD flow works end-to-end
- Directory search returns filtered results
</verification>

<success_criteria>
Users can manage 3 types of calendars, search staff directory, and navigate via updated sidebar.
</success_criteria>

<output>
After completion, create `.planning/phases/04-l-ch-danh-b-dashboard/04-04-SUMMARY.md`
</output>
