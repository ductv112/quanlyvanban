---
phase: 04-l-ch-danh-b-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/dashboard/page.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "Dashboard page displays 4 KPI cards with real data from API"
    - "Clicking a KPI card navigates to the corresponding list page"
    - "3 widgets show real data: recent incoming, upcoming tasks, recent outgoing"
    - "Widgets can be dragged and reordered using react-grid-layout"
    - "Widget layout persists in localStorage across page reloads"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/dashboard/page.tsx"
      provides: "Complete dashboard with KPI cards + draggable widgets"
      min_lines: 150
  key_links:
    - from: "e_office_app_new/frontend/src/app/(main)/dashboard/page.tsx"
      to: "/api/dashboard/stats"
      via: "api.get on mount"
      pattern: "api\\.get.*dashboard/stats"
    - from: "e_office_app_new/frontend/src/app/(main)/dashboard/page.tsx"
      to: "react-grid-layout"
      via: "import ResponsiveGridLayout"
      pattern: "react-grid-layout"
---

<objective>
Rewrite the dashboard page to display real KPI data and draggable widgets using react-grid-layout.

Purpose: Replace placeholder/hardcoded dashboard with live data widgets that users can rearrange (per D-13 through D-21).
Output: Fully functional dashboard page with 4 KPI cards + 3 data widgets + drag-drop layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-l-ch-danh-b-dashboard/04-CONTEXT.md
@.planning/phases/04-l-ch-danh-b-dashboard/04-02-SUMMARY.md

<interfaces>
Dashboard API endpoints (from Plan 02):
```
GET /api/dashboard/stats → { incoming_unread, outgoing_pending, handling_total, handling_overdue }
GET /api/dashboard/recent-incoming?limit=10 → [{ id, doc_code, abstract, received_date, urgency_name, sender_name }]
GET /api/dashboard/upcoming-tasks?limit=10 → [{ id, title, open_date, status, progress_percent, deadline }]
GET /api/dashboard/recent-outgoing?limit=10 → [{ id, doc_code, abstract, sent_date, doc_type_name }]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-grid-layout + rewrite dashboard with KPI cards</name>
  <files>e_office_app_new/frontend/src/app/(main)/dashboard/page.tsx</files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/dashboard/page.tsx (current placeholder dashboard)
    - e_office_app_new/frontend/src/app/globals.css (stat-card, page-card, section-card-header CSS classes)
    - e_office_app_new/frontend/src/lib/api.ts (axios instance)
    - e_office_app_new/frontend/src/stores/auth.store.ts (useAuthStore for user info)
    - e_office_app_new/frontend/src/config/theme.ts (brand colors reference)
  </read_first>
  <action>
First install react-grid-layout: `cd e_office_app_new/frontend && npm install react-grid-layout @types/react-grid-layout`

Then completely rewrite `dashboard/page.tsx`:

**KPI Cards section (per D-13, D-14, D-15):**
- Fetch stats from `api.get('/dashboard/stats')` on mount via useEffect
- Display 4 gradient stat cards using existing `.stat-card` CSS class pattern
- Cards: "VB den chua doc" (navy), "VB di chua duyet" (teal), "Ho so cong viec" (green), "Viec sap toi han" (amber)
- Each card shows real count from API response (not hardcoded)
- onClick on each card navigates via `router.push()`:
  - VB den → `/van-ban-den`
  - VB di → `/van-ban-di`
  - HSCV → `/ho-so-cong-viec`
  - Viec sap han → `/ho-so-cong-viec` (filtered)
- Use Skeleton loading while stats are fetching
- Keep the welcome header: "Xin chao, {user.fullName}"

**Widget section (per D-16, D-17, D-18, D-19, D-20, D-21):**
- Import `Responsive, WidthProvider` from 'react-grid-layout'
- Create `const ResponsiveGridLayout = WidthProvider(Responsive)`
- Import react-grid-layout CSS: `import 'react-grid-layout/css/styles.css'` and `import 'react-resizable/css/styles.css'`
- Define 3 widget components inline or as const:

**Widget 1 — "Van ban moi nhan" (per D-16):**
- Fetch from `api.get('/dashboard/recent-incoming?limit=5')`
- Ant Design Table with columns: So hieu (doc_code), Trich yeu (abstract, ellipsis), Ngay nhan (received_date formatted), Do khan (urgency_name as Tag)
- "Xem them" link at bottom → navigate to `/van-ban-den`
- Card title with section-card-header + FileTextOutlined icon

**Widget 2 — "Viec sap toi han" (per D-17):**
- Fetch from `api.get('/dashboard/upcoming-tasks?limit=5')`
- Ant Design List with items showing: title, deadline (formatted with dayjs), status Tag, Progress bar (progress_percent)
- "Xem them" link → `/ho-so-cong-viec`
- Card title with ClockCircleOutlined icon

**Widget 3 — "Van ban di moi" (per D-18):**
- Fetch from `api.get('/dashboard/recent-outgoing?limit=5')`
- Ant Design Table with columns: So hieu (doc_code), Trich yeu (abstract), Ngay gui (sent_date), Loai VB (doc_type_name)
- "Xem them" → `/van-ban-di`
- Card title with SendOutlined icon

**react-grid-layout configuration (per D-19, D-20, D-21):**
- Default layout: `{ lg: [{i:'incoming', x:0, y:0, w:8, h:4}, {i:'tasks', x:8, y:0, w:4, h:4}, {i:'outgoing', x:0, y:4, w:12, h:4}] }`
- Breakpoints per D-21: `{ lg: 1200, md: 996, sm: 768 }`
- Cols: `{ lg: 12, md: 10, sm: 6 }`
- `rowHeight: 60`
- `onLayoutChange` handler saves to localStorage key `'dashboard-layout'` (per D-20)
- On mount, read layout from localStorage if exists, otherwise use default
- `draggableHandle=".widget-drag-handle"` — add a drag handle icon in each widget card title

**All text in Vietnamese with diacritics.** Use dayjs for date formatting. Use numeral for number display if needed.
  </action>
  <verify>
    <automated>grep -n "react-grid-layout\|dashboard/stats\|recent-incoming\|upcoming-tasks\|recent-outgoing\|localStorage" e_office_app_new/frontend/src/app/(main)/dashboard/page.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard fetches from /api/dashboard/stats and displays 4 KPI cards with real values
    - KPI cards are clickable and navigate to correct list pages
    - 3 widgets fetch real data from dashboard API endpoints
    - react-grid-layout wraps widgets with drag-drop capability
    - Layout saved to and loaded from localStorage key 'dashboard-layout'
    - Responsive breakpoints configured at lg:1200, md:996, sm:768
    - All UI text is Vietnamese with diacritics
    - Loading state uses Skeleton component
  </acceptance_criteria>
  <done>Dashboard displays real KPI data with 3 draggable widgets persisting layout to localStorage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Frontend | Dashboard data displayed to authenticated user only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-09 | I (Info Disclosure) | Dashboard stats | accept | Stats are already scoped by unitId on backend; frontend only displays what API returns |
| T-04-10 | T (Tampering) | localStorage layout | accept | Layout is cosmetic preference only, no security impact |
</threat_model>

<verification>
- `cd e_office_app_new/frontend && npx next build` completes without errors
- Dashboard page loads without console errors
</verification>

<success_criteria>
Dashboard page shows 4 live KPI cards and 3 data widgets in a draggable grid layout.
</success_criteria>

<output>
After completion, create `.planning/phases/04-l-ch-danh-b-dashboard/04-03-SUMMARY.md`
</output>
