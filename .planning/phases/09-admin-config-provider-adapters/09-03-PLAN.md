---
phase: 09-admin-config-provider-adapters
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/ky-so/cau-hinh/page.tsx
  - e_office_app_new/frontend/src/components/layout/MainLayout.tsx
  - e_office_app_new/frontend/src/app/globals.css
autonomous: false
requirements: [SIGN-01, SIGN-02, CFG-01, CFG-02, CFG-03, CFG-04, CFG-07]
tags: [signing, admin-ui, ant-design, drawer, sidebar]

must_haves:
  truths:
    - "Admin truy cập /ky-so/cau-hinh thấy sidebar menu mới 'Ký số' chứa submenu 'Cấu hình ký số hệ thống'"
    - "Page hiển thị 5 KPI stat cards từ provider active (tổng user / verified / giao dịch tháng / hoàn thành / thất bại)"
    - "Form chọn provider (Radio SmartCA/MySign) — render fields dynamic theo provider (MySign có profile_id)"
    - "Button 'Kiểm tra kết nối' gọi POST /test-connection, disable khi loading, hiển thị kết quả rõ ràng"
    - "Button 'Lưu & Kích hoạt' chỉ enable sau khi test connection thành công trong form hiện tại"
    - "Table existing configs có actions: Kích hoạt / Xóa (Popconfirm) / Cập nhật (mở Drawer)"
    - "Non-admin user truy cập URL bị MainLayout (via requireRoles server-side 403) — FE có guard kiểm isAdmin"
  artifacts:
    - path: e_office_app_new/frontend/src/app/(main)/ky-so/cau-hinh/page.tsx
      provides: "Admin config page — stats + form + test-connection + table + drawer edit"
      min_lines: 400
      contains: "cau-hinh"
    - path: e_office_app_new/frontend/src/components/layout/MainLayout.tsx
      provides: "Sidebar menu 'Ký số' group với submenu 'Cấu hình ký số hệ thống' (admin-only)"
      contains: "/ky-so/cau-hinh"
    - path: e_office_app_new/frontend/src/app/globals.css
      provides: "Optional CSS class cho stat cards KPI của trang ký số (reuse existing .stat-card if possible)"
      contains: ".stat-card"
  key_links:
    - from: "frontend /ky-so/cau-hinh/page.tsx"
      to: "backend GET /api/ky-so/cau-hinh"
      via: "api.get — initial load + refetch after mutations"
      pattern: "api\\.get\\(.*ky-so/cau-hinh"
    - from: "frontend page"
      to: "backend POST /api/ky-so/cau-hinh/test-connection"
      via: "api.post — nút Kiểm tra kết nối"
      pattern: "test-connection"
    - from: "frontend page"
      to: "backend POST + PUT /api/ky-so/cau-hinh"
      via: "api.post (create) / api.put (edit); auto-encrypt client_secret backend-side"
      pattern: "api\\.(post|put)\\(.*ky-so/cau-hinh"
    - from: "MainLayout sidebar"
      to: "/ky-so/cau-hinh route"
      via: "Menu item key + router.push onClick"
      pattern: "/ky-so/cau-hinh"
---

<objective>
Tạo trang Admin `/ky-so/cau-hinh` và thêm menu 'Ký số' vào sidebar. Trang có: 5 stat cards từ fn_signing_stats, form chọn provider với dynamic fields, nút test connection (gọi real API), table danh sách configs + Drawer edit.

Purpose: Đóng UI loop cho Phase 9 — Admin có thể E2E: nhập credentials → test thật → lưu (encrypted) → kích hoạt → xem stats. Hoàn thành 7 REQ-IDs của Phase 9.

Output: 1 page.tsx mới + cập nhật MainLayout + minor CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-admin-config-provider-adapters/09-01-PLAN.md
@.planning/phases/09-admin-config-provider-adapters/09-02-PLAN.md

Phase 9 Plan 02 outputs consumed (API contract):
@e_office_app_new/backend/src/routes/ky-so-cau-hinh.ts

Pattern references:
@e_office_app_new/frontend/src/app/(main)/quan-tri/so-van-ban/page.tsx (admin CRUD page — Drawer + table + form pattern)
@e_office_app_new/frontend/src/components/layout/MainLayout.tsx (sidebar menu structure + admin-only guard)
@e_office_app_new/frontend/src/app/globals.css (existing stat-card class, drawer-gradient, page-header)
@e_office_app_new/frontend/src/lib/api.ts (axios instance + auto token attach)
@e_office_app_new/frontend/src/stores/auth.store.ts (user + isAdmin for client-side guard)

<interfaces>
<!-- Backend API contract (Plan 02 endpoints under /api/ky-so/cau-hinh) -->
```typescript
// GET /api/ky-so/cau-hinh → admin-only
interface GetConfigResponse {
  success: true;
  data: {
    providers: Array<{
      id: number | null;                  // null if never configured
      provider_code: 'SMARTCA_VNPT' | 'MYSIGN_VIETTEL';
      provider_name: string;               // 'SmartCA VNPT' | 'MySign Viettel'
      base_url: string | null;
      client_id: string | null;
      client_secret_masked: '***' | null;  // NEVER plaintext
      profile_id: string | null;
      is_active: boolean;
      last_tested_at: string | null;
      test_result: string | null;          // 'OK' | 'FAILED' | null
      created_at: string | null;
      updated_at: string | null;
      stats: {
        total_users: number;
        verified_users: number;
        monthly_transactions: number;
        monthly_completed: number;
        monthly_failed: number;
      };
    }>;
    active_code: 'SMARTCA_VNPT' | 'MYSIGN_VIETTEL' | null;
  };
}

// POST /api/ky-so/cau-hinh/test-connection
interface TestConnectionBody {
  provider_code: 'SMARTCA_VNPT' | 'MYSIGN_VIETTEL';
  base_url: string;
  client_id: string;
  client_secret: string;            // PLAINTEXT — backend encrypts nothing (test only)
  profile_id?: string | null;       // MySign only
}
interface TestConnectionResponse {
  success: true;
  data: { test_result: 'OK' | 'FAILED'; message: string; certificate_subject: string | null };
}

// POST /api/ky-so/cau-hinh → upsert new config (will encrypt server-side)
interface CreateConfigBody extends TestConnectionBody {
  provider_name: string;
  extra_config?: Record<string, unknown>;
  set_active?: boolean;
}
interface CreateConfigResponse { success: true; message: string; data: { id: number }; }

// PUT /api/ky-so/cau-hinh/:id → update (client_secret optional — omit = keep old encrypted)
interface UpdateConfigBody extends Omit<CreateConfigBody, 'client_secret'> {
  client_secret?: string;  // optional — if provided, re-encrypt
}

// PATCH /api/ky-so/cau-hinh/:id/active
// DELETE /api/ky-so/cau-hinh/:id  → 409 if is_active=true
```

<!-- MainLayout pattern (from existing code) -->
```typescript
// In buildMenuItems(), add new group + submenu under HỆ THỐNG section (admin only).
// Icon: SafetyCertificateOutlined (import from @ant-design/icons)
// Menu key: '/ky-so/cau-hinh' — triggers router.push on click
// Breadcrumb map entry: '/ky-so': 'Ký số', '/ky-so/cau-hinh': 'Cấu hình ký số hệ thống'
```

<!-- Existing CSS classes to reuse -->
- `.main-layout`, `.main-content`, `.page-header`, `.page-title`, `.page-card`
- `.drawer-gradient` for Drawer
- `.stat-card` — 5 cards grid (existing pattern from dashboard)
- `.filter-row`, `.empty-center`
</interfaces>

<design_guidance>
Follow global design guidelines (from user's CLAUDE.md):
- Primary color `#1B3A5C` (deep navy), Accent `#0891B2` (teal), already in theme
- Stat cards: gradient icon headers, subtle shadows, translateY(-3px) on hover (reuse .stat-card)
- Form in Drawer size={720} with rootClassName="drawer-gradient"
- Spacing: 16-24px card padding, 8-12px border-radius
- Vietnamese diacritics everywhere, maxLength on inputs matching DB varchar limits
- AntD 6: use `size={720}` NOT `width={720}` on Drawer
</design_guidance>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin config page /ky-so/cau-hinh with stats + form + table + drawer</name>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/quan-tri/so-van-ban/page.tsx (full — CRUD pattern template)
    - e_office_app_new/backend/src/routes/ky-so-cau-hinh.ts (Plan 02 output — API contract source of truth)
    - e_office_app_new/frontend/src/lib/api.ts (axios instance + error handling)
    - e_office_app_new/frontend/src/stores/auth.store.ts (user.isAdmin)
    - e_office_app_new/frontend/src/app/globals.css (search for `.stat-card`, `.drawer-gradient`, `.page-header`)
    - e_office_app_new/frontend/src/app/(main)/dashboard/page.tsx (if exists — reference stat card usage)
  </read_first>
  <files>
    - e_office_app_new/frontend/src/app/(main)/ky-so/cau-hinh/page.tsx (CREATE — new route)
  </files>
  <behavior>
    **Layout structure (top to bottom):**

    1. **Page header** — Title "Cấu hình ký số hệ thống" + subtitle "Chọn nhà cung cấp dịch vụ ký số và cấu hình credentials"

    2. **Active provider banner** (conditional):
       - If `active_code === null`: show Alert warning "Chưa có provider nào được kích hoạt. Vui lòng cấu hình và kích hoạt 1 provider để bật tính năng ký số."
       - If active_code set: show Alert success "Provider đang hoạt động: {active.provider_name}" + link "Xem stats bên dưới"

    3. **Stats section** (from active provider, or hidden if no active):
       - 5 stat cards in Row gutter=[16,16]: Tổng người dùng / Đã xác thực / Giao dịch tháng / Thành công / Thất bại
       - Each card: icon + number + label (use existing `.stat-card` class pattern)
       - Colors: default navy, verified=success green, transactions=accent teal, success=green, failed=red

    4. **Providers table** (Card with title "Danh sách cấu hình"):
       - Columns: Provider / Base URL (ellipsis) / Client ID (ellipsis) / Trạng thái (Tag) / Kết quả test / Cập nhật / Actions
       - Status Tag: color=success if is_active, default if not, orange if never configured (id=null)
       - Test result Tag: green if 'OK', red if 'FAILED', default if null
       - Row actions Dropdown with MoreOutlined:
         * Cập nhật (opens Drawer with existing data)
         * Kích hoạt (disabled if already active — PATCH /active)
         * Xóa (Modal.confirm danger — only if id !== null && !is_active)

    5. **Button toolbar** (right-aligned): "Thêm/Cấu hình mới" → opens Drawer in create mode

    6. **Drawer** (size={720}, rootClassName="drawer-gradient"):
       - Title: dynamic — "Thêm cấu hình" if editingRecord null, else "Cập nhật cấu hình {provider_name}"
       - Form fields (Form.Item validateTrigger="onSubmit"):
         * Provider (Radio): 'SMARTCA_VNPT' | 'MYSIGN_VIETTEL' — required. Disabled in edit mode (can't change provider type of existing row)
         * Base URL (Input maxLength=500): required, rules include URL pattern starting with `https://` or `http://localhost`
         * Client ID (Input maxLength=200): required
         * Client Secret (Input.Password maxLength=500): required in create mode; optional in edit mode with placeholder "(để trống nếu giữ nguyên)"
         * Profile ID (Input maxLength=200): conditional render — only when Provider === 'MYSIGN_VIETTEL'. Required for MySign.
       - Footer extra buttons:
         * "Kiểm tra kết nối" (secondary) — calls POST /test-connection with current form values. Disabled during request. Shows Spin. On success: green Alert "Kết nối OK — CN: {certificate_subject ?? '—'}"; on fail: red Alert with message. Sets state `testedOk: true` only if success.
         * "Lưu & Kích hoạt" (primary) — disabled until `testedOk === true`. Calls POST (create) or PUT (update), then PATCH /active. Close drawer, refetch list, toast "Kích hoạt thành công".
         * "Chỉ lưu" (default, edit mode only) — saves without activating.
         * "Hủy" — close drawer, reset form.

    **State management:**
    - `providers: Provider[]` — from GET /cau-hinh
    - `activeCode: ProviderCode | null`
    - `editingRecord: Provider | null`
    - `drawerOpen: boolean`
    - `testedOk: boolean` — resets to false on drawer open and when form values change
    - `testing: boolean` / `saving: boolean` loading states
    - `form` from Form.useForm()
    - `{ message, modal } = App.useApp()`

    **Important behaviors:**
    - `form.useWatch('provider_code')` — watch provider selection to conditionally render profile_id
    - `form.useWatch(['base_url', 'client_id', 'client_secret', 'profile_id'])` — reset testedOk when any changes
    - Client-side admin guard: `if (!user?.isAdmin) return <Empty description="Bạn không có quyền truy cập trang này" />`
    - maxLength on all Inputs per CLAUDE.md rules (base_url 500, client_id 200, client_secret 500, profile_id 200)
    - Error handling: setBackendFieldError pattern for common backend messages

    **Stats computation:** When `active_code` changes, display stats of that provider; before switching, show hint "Xem stats provider: SmartCA VNPT" with small switcher tabs.
  </behavior>
  <action>
    Step 1 — Create directory + file:
    ```
    mkdir -p e_office_app_new/frontend/src/app/\(main\)/ky-so/cau-hinh
    ```
    Create `e_office_app_new/frontend/src/app/(main)/ky-so/cau-hinh/page.tsx`.

    Step 2 — Component skeleton:
    ```tsx
    'use client';
    import React, { useState, useEffect, useCallback, useMemo } from 'react';
    import { Card, Table, Button, Input, Form, Radio, Drawer, Tag, Alert, Row, Col, Space, Dropdown, Modal, App, Empty, Spin } from 'antd';
    import type { ColumnsType } from 'antd/es/table';
    import { PlusOutlined, SafetyCertificateOutlined, CheckCircleOutlined, CloseCircleOutlined, MoreOutlined, ThunderboltOutlined, UserOutlined, TeamOutlined, HistoryOutlined } from '@ant-design/icons';
    import { api } from '@/lib/api';
    import { useAuthStore } from '@/stores/auth.store';

    interface ProviderStats { total_users: number; verified_users: number; monthly_transactions: number; monthly_completed: number; monthly_failed: number; }
    interface ProviderConfig {
      id: number | null;
      provider_code: 'SMARTCA_VNPT' | 'MYSIGN_VIETTEL';
      provider_name: string;
      base_url: string | null;
      client_id: string | null;
      client_secret_masked: '***' | null;
      profile_id: string | null;
      is_active: boolean;
      last_tested_at: string | null;
      test_result: string | null;
      created_at: string | null;
      updated_at: string | null;
      stats: ProviderStats;
    }

    export default function KySoCauHinhPage() {
      const { message, modal } = App.useApp();
      const user = useAuthStore(s => s.user);
      const [form] = Form.useForm();
      // ... state
    }
    ```

    Step 3 — Implement all behaviors in spec above. Key functions:
    - `fetchConfig()` — GET /api/ky-so/cau-hinh, set providers + activeCode
    - `handleTestConnection()` — validateFields → POST /test-connection → setTestedOk
    - `handleSave(withActivate: boolean)` — POST or PUT → if withActivate, PATCH /active → refetch → close drawer
    - `handleActivate(id, code)` — Modal.confirm → PATCH /:id/active → refetch
    - `handleDelete(id)` — Modal.confirm danger → DELETE /:id → refetch; handle 409 'đang kích hoạt' as info message

    Step 4 — Provider selector in Drawer (dynamic fields):
    ```tsx
    const selectedProvider = Form.useWatch('provider_code', form);
    // ...
    <Form.Item name="provider_code" label="Nhà cung cấp" rules={[{required: true}]}>
      <Radio.Group disabled={!!editingRecord}>
        <Radio value="SMARTCA_VNPT">SmartCA VNPT</Radio>
        <Radio value="MYSIGN_VIETTEL">MySign Viettel</Radio>
      </Radio.Group>
    </Form.Item>
    {selectedProvider === 'MYSIGN_VIETTEL' && (
      <Form.Item name="profile_id" label="Profile ID" rules={[{required: true, message: 'Profile ID là bắt buộc với MySign Viettel'}]}>
        <Input maxLength={200} placeholder="VD: adss:ras:profile:001" />
      </Form.Item>
    )}
    ```

    Step 5 — Test connection button with state reset on form change:
    ```tsx
    const watchedValues = Form.useWatch([], form);
    useEffect(() => { setTestedOk(false); }, [watchedValues]);

    async function onTestConnection() {
      try {
        const values = await form.validateFields(['provider_code','base_url','client_id','client_secret','profile_id']);
        setTesting(true);
        const { data: res } = await api.post('/ky-so/cau-hinh/test-connection', {
          provider_code: values.provider_code,
          base_url: values.base_url,
          client_id: values.client_id,
          client_secret: values.client_secret,
          profile_id: values.profile_id ?? null,
        });
        if (res.data?.test_result === 'OK') {
          setTestedOk(true);
          setTestResult({ok: true, message: res.data.message, subject: res.data.certificate_subject});
          message.success('Kiểm tra kết nối thành công');
        } else {
          setTestedOk(false);
          setTestResult({ok: false, message: res.data?.message ?? 'Kết nối thất bại'});
        }
      } catch (err: any) {
        setTestedOk(false);
        setTestResult({ok: false, message: err?.response?.data?.message ?? 'Lỗi kết nối'});
      } finally {
        setTesting(false);
      }
    }
    ```

    Step 6 — Admin guard at top of component:
    ```tsx
    if (!user) return <Skeleton active paragraph={{rows: 6}} />;
    if (!user.isAdmin) return <Empty description="Bạn không có quyền truy cập trang này" image={Empty.PRESENTED_IMAGE_SIMPLE} />;
    ```

    Step 7 — Stats cards row — reuse `.stat-card` class pattern if available. If page CSS doesn't have stat-card styles, fall back to inline gradient styles matching dashboard pattern.

    Step 8 — Verify compile:
    ```
    cd e_office_app_new/frontend && npx next build 2>&1 | tail -30
    # OR just tsc:
    npx tsc --noEmit 2>&1 | grep "ky-so/cau-hinh" | head -10
    ```
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx tsc --noEmit 2>&1 | grep "ky-so/cau-hinh" | head -10 ; echo "---" ; wc -l src/app/\(main\)/ky-so/cau-hinh/page.tsx ; echo "---" ; grep -c "api\\.(get\\|post\\|put\\|patch\\|delete)" src/app/\(main\)/ky-so/cau-hinh/page.tsx</automated>
  </verify>
  <done>
    - File exists at `frontend/src/app/(main)/ky-so/cau-hinh/page.tsx` ≥ 400 lines
    - tsc --noEmit shows 0 errors in this file
    - Uses api.get, api.post, api.put, api.patch, api.delete (grep ≥ 5 matches)
    - Admin guard checks `user.isAdmin`
    - Drawer uses `size={720}` (AntD 6)
    - All Inputs have maxLength attributes matching DB varchar limits
    - No English UI text — all Vietnamese diacritics
    - test connection button disabled when testing; save+activate disabled until testedOk
  </done>
</task>

<task type="auto">
  <name>Task 2: Update MainLayout sidebar — add 'Ký số' group + breadcrumb</name>
  <read_first>
    - e_office_app_new/frontend/src/components/layout/MainLayout.tsx (full — understand buildMenuItems + breadcrumbMap + HIDDEN_ROUTES)
  </read_first>
  <files>
    - e_office_app_new/frontend/src/components/layout/MainLayout.tsx (MODIFY)
  </files>
  <behavior>
    Add new menu group 'KÝ SỐ' with submenu for admin. Place BEFORE 'HỆ THỐNG' group (logical flow: nghiệp vụ → quản lý → tích hợp → ký số → hệ thống). 

    - Icon: `SafetyCertificateOutlined` (import from @ant-design/icons)
    - Group header: `{ key: 'grp-kyso', type: 'group', label: 'KÝ SỐ' }` — admin only
    - Submenu (admin only for now — Plan 10 will add user submenu):
      * `{ key: '/ky-so/cau-hinh', icon: <SafetyCertificateOutlined />, label: 'Cấu hình ký số hệ thống' }`
    - Ensure key `/ky-so/cau-hinh` NOT in HIDDEN_ROUTES
    - Add to `breadcrumbMap`:
      * `'/ky-so': 'Ký số'`
      * `'/ky-so/cau-hinh': 'Cấu hình ký số hệ thống'`
  </behavior>
  <action>
    Step 1 — Import icon. Update import block at top (around line 6-44). Add `SafetyCertificateOutlined`:
    ```tsx
    import {
      // ...existing,
      SafetyCertificateOutlined,
    } from '@ant-design/icons';
    ```

    Step 2 — In `buildMenuItems()` function, find the `if (isAdmin) { items.push(...HỆ THỐNG section...) }` block (around line 278). Insert BEFORE it:
    ```tsx
    // ── KÝ SỐ ── (Admin sees config; Plan 10+ adds user/danh-sach submenu)
    if (isAdmin) {
      items.push(
        { key: 'grp-kyso', type: 'group', label: 'KÝ SỐ' },
        {
          key: '/ky-so/cau-hinh',
          icon: <SafetyCertificateOutlined />,
          label: 'Cấu hình ký số hệ thống',
        },
      );
    }
    ```

    Step 3 — Update `breadcrumbMap` (around line 319-362). Add two entries:
    ```tsx
    '/ky-so': 'Ký số',
    '/ky-so/cau-hinh': 'Cấu hình ký số hệ thống',
    ```

    Step 4 — Verify `/ky-so/cau-hinh` NOT in HIDDEN_ROUTES (lines 66-102). It is not there — confirm with grep.

    Step 5 — TypeScript check:
    ```
    cd e_office_app_new/frontend && npx tsc --noEmit 2>&1 | grep "MainLayout" | head -5
    ```

    Step 6 — If dev server running, navigate as admin to `/ky-so/cau-hinh` → sidebar highlights menu, breadcrumb shows "Ký số > Cấu hình ký số hệ thống".
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx tsc --noEmit 2>&1 | grep "MainLayout" | head -5 ; echo "---" ; grep -n "SafetyCertificateOutlined\\|grp-kyso\\|/ky-so/cau-hinh" src/components/layout/MainLayout.tsx | head -20</automated>
  </verify>
  <done>
    - `SafetyCertificateOutlined` imported in MainLayout
    - Menu block `grp-kyso` group + `/ky-so/cau-hinh` item present, inside `if (isAdmin)`
    - breadcrumbMap has both `/ky-so` and `/ky-so/cau-hinh` entries
    - `/ky-so/cau-hinh` NOT in HIDDEN_ROUTES
    - tsc --noEmit 0 new errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — E2E Admin config flow</name>
  <what-built>
    - Frontend page `/ky-so/cau-hinh` (admin only) with stats + form + test connection + table + drawer
    - Sidebar menu "Ký số > Cấu hình ký số hệ thống" visible for admin users
    - Full wiring: GET config → display; POST test-connection → real provider API call; POST/PUT → encrypt + save; PATCH /active → switch active; DELETE → 409 if active
  </what-built>
  <how-to-verify>
    **Prerequisite:** Backend running on port 4000, Frontend on 3000, Docker PostgreSQL healthy.

    **Step 1 — Sidebar appears:**
    1. Login as Admin user
    2. Verify sidebar shows new group "KÝ SỐ" with item "Cấu hình ký số hệ thống" (admin only)
    3. Click menu → navigate to `/ky-so/cau-hinh`
    4. Breadcrumb shows "Ký số > Cấu hình ký số hệ thống"

    **Step 2 — Non-admin guard:**
    1. Logout, login as non-admin user (VD: văn thư)
    2. Menu item "Ký số" should NOT appear in sidebar
    3. Manually navigate to `/ky-so/cau-hinh` → should show "Bạn không có quyền truy cập"

    **Step 3 — Initial state (no config):**
    1. As admin, load `/ky-so/cau-hinh`
    2. Alert warning "Chưa có provider nào được kích hoạt" visible
    3. Stats cards all show 0
    4. Table shows 2 rows (SmartCA VNPT + MySign Viettel) with status "Chưa cấu hình"

    **Step 4 — Create SmartCA config + test connection:**
    1. Click "Thêm/Cấu hình mới" → Drawer opens
    2. Select "SmartCA VNPT" — profile_id field NOT visible
    3. Fill:
       - Base URL: `https://gwsca.vnpt.vn/sca/sp769/v1` (or test env)
       - Client ID: `test_client_id`
       - Client Secret: `test_secret`
    4. Click "Kiểm tra kết nối"
    5. Expected: Button disabled during request, Alert hiển thị kết quả (OK or FAILED with message)
    6. Verify: regardless OK/FAILED, NO plaintext secret shown in Network tab response body of GET /cau-hinh after save
    7. If OK: "Lưu & Kích hoạt" button now enabled

    **Step 5 — MySign Viettel dynamic field:**
    1. Cancel current drawer, reopen
    2. Select "MySign Viettel" — profile_id field APPEARS with required asterisk
    3. Fill all 4 fields: base_url `https://remotesigning.viettel.vn`, client_id, client_secret, profile_id `adss:ras:profile:001`
    4. Click Kiểm tra kết nối → observe result

    **Step 6 — Save + activate:**
    1. (Using whichever provider you trust will pass test) Fill valid creds → Kiểm tra OK
    2. Click "Lưu & Kích hoạt"
    3. Toast "Kích hoạt thành công", drawer closes
    4. Table row updated: is_active = true, test_result = OK
    5. Alert banner now shows "Provider đang hoạt động: {name}"
    6. Stats cards still 0 (no transactions yet)

    **Step 7 — Switch provider:**
    1. Create second provider config (other type) via drawer — test, save & activate
    2. Verify: old provider is_active auto-flipped to false (CFG-01 enforced by partial unique index)
    3. Only 1 row has is_active = true

    **Step 8 — Update with secret omitted:**
    1. Click "Cập nhật" action on existing row → Drawer opens
    2. Client Secret field shows placeholder "(để trống nếu giữ nguyên)" — field empty
    3. Change only base_url, leave client_secret empty
    4. Click Kiểm tra kết nối — test_result should still work (backend reuses existing encrypted secret on save)
    5. Wait — test connection needs plaintext. Field empty means test can't be run. UX decision: disable test button if secret empty AND editing. Save-only works in edit mode.

    **Step 9 — Delete protection:**
    1. Try deleting active provider → Modal.confirm → DELETE → expect 409 toast "Không thể xóa provider đang được kích hoạt"
    2. Deactivate first (switch to other), then delete → success

    **Step 10 — Security audit (Network tab):**
    1. Open DevTools Network tab, reload page
    2. GET /api/ky-so/cau-hinh response — search JSON for `client_secret` → should be `client_secret_masked: '***'` only, NEVER full BYTEA or plaintext

    **Expected outcomes:**
    - [ ] Sidebar menu "Ký số" visible only for admin
    - [ ] Non-admin blocked (either sidebar hidden + URL guard)
    - [ ] 5 stats cards render (all 0 initially)
    - [ ] Form profile_id visible only for MySign
    - [ ] Test connection hits real provider (HTTPS call in Network tab to {base_url})
    - [ ] Save+activate works, auto-deactivates other provider
    - [ ] Delete blocked when active
    - [ ] No client_secret plaintext in any GET response
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 steps pass; or describe specific failures for fixes</resume-signal>
  <files>Manual verification checkpoint — no files modified. Validates outputs from Task 1 + Task 2.</files>
  <action>Executor pauses and presents the 10-step verification checklist above to the human. Do not proceed past this checkpoint until human types approved or provides specific failures requiring fixes. If failures reported, create quick-fix tasks and re-present checklist.</action>
  <verify>
    <automated>echo "Checkpoint: human must manually validate 10 UI/security steps — no automated check"</automated>
  </verify>
  <done>Human types approved after completing all 10 verification steps. Network-tab inspection confirms no client_secret plaintext in any GET response. All 8 expected outcome checkboxes ticked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → POST /test-connection | Plaintext `client_secret` sent from form — HTTPS in production. In dev (localhost) acceptable over HTTP. |
| Browser → GET /cau-hinh | Response must never include `client_secret` plaintext or BYTEA — only mask '***'. |
| localStorage JWT → backend | Existing auth.store pattern; token auto-attached by api.ts interceptor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Elevation of Privilege | page.tsx (client-side guard) | mitigate | `if (!user.isAdmin) return <Empty>` — defense in depth. Primary enforcement is backend `requireRoles('Quản trị hệ thống')` from Plan 02 (cannot be bypassed). |
| T-09-14 | Information Disclosure | form state | mitigate | Client Secret field is `Input.Password` type (masked display). After successful save, form is reset — plaintext not retained in memory long-term. |
| T-09-15 | Cross-Site Scripting | table render | accept | AntD Tag/Input auto-escapes by default; test_result is constrained enum 'OK'|'FAILED' — no user HTML allowed. |
| T-09-16 | Tampering | sidebar menu | accept | Menu visibility is cosmetic (user could edit DevTools to show hidden items) — real authorization is backend requireRoles. Non-admin can't execute any mutation. |
| T-09-17 | Information Disclosure | browser history / URL | accept | `/ky-so/cau-hinh` path is non-sensitive (no IDs, no PII). Credentials sent in POST body, not URL. |
</threat_model>

<verification>
- File `src/app/(main)/ky-so/cau-hinh/page.tsx` exists ≥ 400 lines
- TypeScript compiles with 0 errors in page.tsx + MainLayout.tsx
- Sidebar grep: `SafetyCertificateOutlined` + `grp-kyso` + `/ky-so/cau-hinh` all present in MainLayout.tsx
- breadcrumbMap has both `/ky-so` and `/ky-so/cau-hinh` entries
- Checkpoint: human verifies 10 steps pass (UI + security audit)
</verification>

<success_criteria>
- Admin user can E2E: nav sidebar → page → form → test → save & activate → see stats
- Non-admin user sees no sidebar entry; direct URL access blocked by client guard + backend 403
- client_secret plaintext NEVER returned from backend (network inspection confirms)
- CFG-01 single-active enforced visibly (activating B auto-deactivates A)
- Switch provider keeps old config in table (can re-activate later — CFG-02 schema level)
- UI matches design guidelines: navy primary, teal accent, 12px border-radius, translateY hover
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-config-provider-adapters/09-03-SUMMARY.md` with:
- requirements-completed: [SIGN-01, SIGN-02, CFG-01, CFG-02, CFG-03, CFG-04, CFG-07]
- Files created/modified with line counts
- Screenshots locations (if captured during Task 3 verification)
- Any UX refinements identified during human testing
- Phase 9 is NOW COMPLETE (7/7 REQ-IDs done) — ready for Phase 10 user config page
</output>
