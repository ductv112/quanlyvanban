---
phase: 09-admin-config-provider-adapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/backend/src/services/signing/providers/provider.interface.ts
  - e_office_app_new/backend/src/services/signing/providers/smartca-vnpt.provider.ts
  - e_office_app_new/backend/src/services/signing/providers/mysign-viettel.provider.ts
  - e_office_app_new/backend/src/services/signing/providers/provider-factory.ts
  - e_office_app_new/backend/src/services/signing/providers/provider.test.ts
autonomous: true
requirements: [SIGN-01, SIGN-02, CFG-03]
tags: [signing, provider-adapter, strategy-pattern, smartca, mysign]

must_haves:
  truths:
    - "SmartCA VNPT adapter gọi được 3 endpoint thật: get_certificate, sign, status"
    - "MySign Viettel adapter gọi được 4 endpoint thật: login, certificates/info, signHash, requests/status"
    - "testConnection(adminConfig) cho mỗi provider trả {success, message} rõ ràng — dùng cho CFG-03"
    - "getActiveProvider() đọc DB + decrypt client_secret + trả instance adapter ready-to-call"
    - "Adapter interface chung cho phép Phase 11 worker poll bất kỳ provider nào"
  artifacts:
    - path: e_office_app_new/backend/src/services/signing/providers/provider.interface.ts
      provides: "SigningProvider interface (testConnection, listCertificates, signHash, getSignStatus)"
      contains: "interface SigningProvider"
    - path: e_office_app_new/backend/src/services/signing/providers/smartca-vnpt.provider.ts
      provides: "SmartCA VNPT adapter implementing SigningProvider — 3 real endpoints"
      contains: "smartcaVnptProvider"
    - path: e_office_app_new/backend/src/services/signing/providers/mysign-viettel.provider.ts
      provides: "MySign Viettel adapter implementing SigningProvider — 4 real endpoints"
      contains: "mysignViettelProvider"
    - path: e_office_app_new/backend/src/services/signing/providers/provider-factory.ts
      provides: "getActiveProvider() + getProviderByCode() — factory reads DB, decrypts secret, returns adapter"
      contains: "getActiveProvider"
    - path: e_office_app_new/backend/src/services/signing/providers/provider.test.ts
      provides: "Unit tests với axios mock — verify URL/body shape match postman + Model.cs"
      min_lines: 80
  key_links:
    - from: provider-factory.ts
      to: signingProviderConfigRepository.getActive()
      via: "import từ repositories/signing-provider-config.repository.js"
      pattern: "signingProviderConfigRepository\\.getActive"
    - from: provider-factory.ts
      to: decryptSecret()
      via: "import từ services/signing/crypto.js để giải mã client_secret BYTEA"
      pattern: "decryptSecret"
    - from: smartca-vnpt.provider.ts
      to: "{base_url}/sca/sp769/v1/credentials/get_certificate"
      via: "axios.post — 3 endpoints theo Model.cs"
      pattern: "credentials/get_certificate"
    - from: mysign-viettel.provider.ts
      to: "{base_url}/vtss/service/ras/v1/login"
      via: "axios.post — login → lấy access_token → Bearer cho 3 endpoint sau"
      pattern: "vtss/service/ras/v1/login"
---

<objective>
Tạo strategy-pattern interface `SigningProvider` + 2 adapter implementations (SmartCA VNPT, MySign Viettel) + factory đọc provider active từ DB. Mỗi adapter gọi đúng endpoints thật theo reference code cũ (Model.cs) và postman collection Viettel.

Purpose: Phase 9 API route (Plan 02) gọi `getActiveProvider().testConnection()` để CFG-03 test connection; Phase 11 worker gọi `provider.getSignStatus(txnId)` để poll status. Strategy pattern giúp thêm provider mới (FPT CA, EasyCA...) chỉ cần implement interface mới — không sửa route/worker.

Output: 5 files trong `backend/src/services/signing/providers/` — interface, 2 adapters, factory, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-schema-foundation-pdf-signing-generic-layer/08-04-SUMMARY.md

Reference source cũ (đối chiếu trước khi implement):
@docs/source_code_cu/sources/OneWin.WebApp/SmartCA_VNPT/Model.cs
@docs/huong_dan_tich_hop_ky_so_MySign_Viettel/Viettel Mysign Gateway.postman_collection.json

Phase 8 outputs consumed:
@e_office_app_new/backend/src/services/signing/crypto.ts
@e_office_app_new/backend/src/repositories/signing-provider-config.repository.ts
@e_office_app_new/backend/src/services/signing/types.ts

<interfaces>
<!-- From signing-provider-config.repository.ts (Phase 8) -->
```typescript
export interface SigningProviderConfigActiveRow {
  id: number;
  provider_code: string;         // 'SMARTCA_VNPT' | 'MYSIGN_VIETTEL'
  provider_name: string;
  base_url: string;
  client_id: string;
  client_secret: Buffer;         // BYTEA — must decryptSecret() before use
  profile_id: string | null;     // MySign only
  extra_config: Record<string, unknown>;
}

export const signingProviderConfigRepository = {
  async getActive(): Promise<SigningProviderConfigActiveRow | null>;
  async getByCode(providerCode: string): Promise<SigningProviderConfigFullRow | null>;
};
```

<!-- From services/signing/crypto.ts (Phase 8) -->
```typescript
export async function decryptSecret(cipher: Buffer): Promise<string>;
```
</interfaces>

<reference_endpoints>
**SmartCA VNPT (Model.cs):**
- `POST {base_url}/sca/sp769/v1/credentials/get_certificate`
  - Body: `{ sp_id: client_id, sp_password: client_secret, user_id, serial_number?: '', transaction_id?: '' }`
  - Response: `{ status_code, message, data: { user_certificates: [{ cert_id, cert_data, cert_subject, cert_valid_from, cert_valid_to, serial_number, ... }] } }`
- `POST {base_url}/sca/sp769/v1/signatures/sign`
  - Body: `{ sp_id, sp_password, user_id, transaction_desc, transaction_id: '', sign_files: [{ data_to_be_signed, doc_id, file_type: 'pdf', sign_type: 'hash' }], serial_number }`
  - Response: `{ status_code, message, data: { transaction_id, tran_code } }` → transaction_id để poll status
- `GET {base_url}/sca/sp769/v1/signatures/sign/{transaction_id}/status`
  - Response: `{ status_code, message, data: { transaction_id, signatures: [{ doc_id, signature_value, timestamp_signature }] } }` — khi signatures=null → chưa confirm OTP; khi có signatures[0].signature_value → completed

**MySign Viettel (postman):**
- `POST {base_url}/vtss/service/ras/v1/login`
  - Body: `{ client_id, user_id, client_secret, profile_id }` (no Authorization header)
  - Response: `{ access_token, refresh_token, token_type: 'Bearer', expires_in: '3600' }` — dùng access_token cho 3 endpoint sau (Bearer)
- `POST {base_url}/vtss/service/certificates/info` + Bearer token
  - Body: `{ client_id, client_secret, profile_id, user_id, certInfo: true, authInfo: true, certificates: 'chain' }`
  - Response: Array<{ credential_id, cert: { certificates: [pem...], subjectDN, issuerDN, serialNumber, validFrom, validTo, status }, key: { status, algo, len } }>
- `POST {base_url}/vtss/service/signHash` + Bearer token
  - Body: `{ client_id, client_secret, credentialID, numSignatures: 1, documents: [{ document_id, document_name }], hash: [hex_hash], hashAlgo: '2.16.840.1.101.3.4.2.1', signAlgo: '1.2.840.113549.1.1.1', async: 1 }` — async=1 means async flow
  - Response (async=1): `{ transactionId }`
- `POST {base_url}/vtss/service/requests/status` + Bearer token
  - Body: `{ transactionId }`
  - Response: `{ signatures: [base64...], status: '1' }` — status='1' đã ký xong; status='0' đang chờ OTP
</reference_endpoints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define SigningProvider interface + unit test scaffold</name>
  <read_first>
    - docs/source_code_cu/sources/OneWin.WebApp/SmartCA_VNPT/Model.cs (lines 29-130 — response DTOs)
    - docs/huong_dan_tich_hop_ky_so_MySign_Viettel/Viettel Mysign Gateway.postman_collection.json
    - e_office_app_new/backend/src/repositories/signing-provider-config.repository.ts (Active row shape)
    - e_office_app_new/backend/src/services/signing/crypto.ts (decryptSecret signature)
    - e_office_app_new/backend/src/services/signing/pdf-signer.test.ts (node:test pattern để follow)
  </read_first>
  <files>
    - e_office_app_new/backend/src/services/signing/providers/provider.interface.ts (CREATE)
    - e_office_app_new/backend/src/services/signing/providers/provider.test.ts (CREATE — scaffold)
  </files>
  <behavior>
    - Interface export types:
      * `ProviderCode = 'SMARTCA_VNPT' | 'MYSIGN_VIETTEL'`
      * `TestConnectionResult = { success: boolean; message: string; certificateSubject?: string }`
      * `CertificateInfo = { credentialId: string; subject: string; serialNumber: string; validFrom: string; validTo: string; certificateBase64: string; status: string }`
      * `SignHashRequest = { hashHex: string; documentName: string; documentId: string }`
      * `SignHashResult = { providerTransactionId: string }`
      * `GetStatusResult = { status: 'pending' | 'completed' | 'failed' | 'expired'; signatureBase64?: string; errorMessage?: string }`
      * `AdminCredentials = { baseUrl: string; clientId: string; clientSecretPlaintext: string; profileId?: string | null }`
      * `UserConfig = { userId: string; credentialId?: string | null }`
    - Interface `SigningProvider` methods:
      * `testConnection(admin: AdminCredentials): Promise<TestConnectionResult>` — CFG-03
      * `listCertificates(admin: AdminCredentials, user: UserConfig): Promise<CertificateInfo[]>` — Phase 10
      * `signHash(admin: AdminCredentials, user: UserConfig, req: SignHashRequest): Promise<SignHashResult>` — Phase 11
      * `getSignStatus(admin: AdminCredentials, user: UserConfig, providerTxnId: string): Promise<GetStatusResult>` — Phase 11
    - Interface MUST also export `readonly code: ProviderCode` property
    - Unit test scaffold file imports types but has no real tests yet — Tasks 2+3 add tests
  </behavior>
  <action>
    Create `provider.interface.ts` with:
    1. All 7 type aliases above exported (TestConnectionResult, CertificateInfo, SignHashRequest, SignHashResult, GetStatusResult, AdminCredentials, UserConfig)
    2. Export `type ProviderCode = 'SMARTCA_VNPT' | 'MYSIGN_VIETTEL';`
    3. Export `interface SigningProvider { readonly code: ProviderCode; testConnection(...); listCertificates(...); signHash(...); getSignStatus(...); }` — all methods async returning the typed result
    4. Add TSDoc comments for each method referencing which REQ-ID consumes it (CFG-03 / CFG-05 / SIGN-03 / SIGN-05)

    Create `provider.test.ts` using `node:test` + `tsx` pattern from existing `pdf-signer.test.ts`:
    1. Import `describe, it` from `node:test`
    2. Add placeholder `describe('SigningProvider interface', () => { it('exports expected types', () => {...}) })` — just import check
    3. Real tests added in Tasks 2 + 3 (per provider)

    Run `cd e_office_app_new/backend && npx tsc --noEmit` — must show 0 NEW errors (21 pre-existing routes errors OK).
  </action>
  <verify>
    <automated>cd e_office_app_new/backend && npx tsc --noEmit 2>&1 | grep "services/signing/providers" ; echo "---" ; grep -c "export" src/services/signing/providers/provider.interface.ts ; grep -c "SigningProvider" src/services/signing/providers/provider.interface.ts</automated>
  </verify>
  <done>
    - `provider.interface.ts` exports `SigningProvider`, `ProviderCode`, + 6 DTOs
    - `provider.test.ts` exists, node:test import compiles
    - `npx tsc --noEmit` shows 0 new errors in `services/signing/providers/`
    - grep `SigningProvider` matches ≥ 1
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SmartCA VNPT adapter + MySign Viettel adapter + tests</name>
  <read_first>
    - e_office_app_new/backend/src/services/signing/providers/provider.interface.ts (Task 1 output)
    - docs/source_code_cu/sources/OneWin.WebApp/SmartCA_VNPT/Model.cs (lines 500-560 — poll loop reference)
    - docs/huong_dan_tich_hop_ky_so_MySign_Viettel/Viettel Mysign Gateway.postman_collection.json (full)
    - e_office_app_new/backend/package.json (confirm axios 1.15.0 available)
    - e_office_app_new/backend/src/services/signing/pdf-signer.test.ts (test pattern: mock-free, deterministic)
  </read_first>
  <files>
    - e_office_app_new/backend/src/services/signing/providers/smartca-vnpt.provider.ts (CREATE)
    - e_office_app_new/backend/src/services/signing/providers/mysign-viettel.provider.ts (CREATE)
    - e_office_app_new/backend/src/services/signing/providers/provider.test.ts (EXTEND)
  </files>
  <behavior>
    **SmartCA VNPT adapter:**
    - `code: 'SMARTCA_VNPT'` readonly
    - `testConnection({baseUrl, clientId, clientSecretPlaintext})`: POST `{baseUrl}/sca/sp769/v1/credentials/get_certificate` with body `{sp_id: clientId, sp_password: clientSecretPlaintext, user_id: 'test_connection', serial_number: '', transaction_id: ''}`. VNPT returns status_code=0 or status_code != 0 for invalid creds vs not-found user. Rule: if `status_code === 0` OR response message contains 'not found' (user bịa) → credentials OK (provider reachable, auth OK). If HTTP 401/403 OR status_code indicates invalid client → credentials BAD. Return `{success, message}` with Vietnamese message.
    - `listCertificates(admin, {userId})`: same endpoint but real userId → parse response.data.user_certificates → map to CertificateInfo[]
    - `signHash(admin, {userId}, {hashHex, documentName, documentId})`: POST `{baseUrl}/sca/sp769/v1/signatures/sign` with body `{sp_id: admin.clientId, sp_password: admin.clientSecretPlaintext, user_id: userId, transaction_desc: documentName, transaction_id: '', sign_files: [{data_to_be_signed: hashHex, doc_id: documentId, file_type: 'pdf', sign_type: 'hash'}], serial_number: ''}`. Return `{providerTransactionId: response.data.transaction_id}`. Throw on status_code !== 0 with message.
    - `getSignStatus(admin, user, providerTxnId)`: GET `{baseUrl}/sca/sp769/v1/signatures/sign/{providerTxnId}/status` with Basic auth header `{sp_id, sp_password}` if required — follow Model.cs `_getStatus` helper. Response.data.signatures === null or undefined → `{status: 'pending'}`. signatures[0].signature_value exists → `{status: 'completed', signatureBase64: signatures[0].signature_value}`.

    **MySign Viettel adapter:**
    - `code: 'MYSIGN_VIETTEL'` readonly
    - Private helper `_login(admin, userId)`: POST `{baseUrl}/vtss/service/ras/v1/login` body `{client_id, user_id, client_secret, profile_id}` → returns `access_token` string; throw Error('Login Viettel thất bại: ...') on non-200 or missing token
    - `testConnection(admin)`: call `_login(admin, 'test_connection')` — if returns token → credentials OK. If HTTP 401 or login error mentioning 'client_id' or 'client_secret' → credentials BAD. Return Vietnamese message. Note: MySign might require real user_id for login — in that case, detect 'user_id' error messages as "credentials OK but user_id invalid" → still success=true (admin creds valid, user_id issue is CFG-05 concern).
    - `listCertificates(admin, {userId})`: first `_login`, then POST `{baseUrl}/vtss/service/certificates/info` with Bearer header `Authorization: Bearer ${token}` and body `{client_id, client_secret, profile_id, user_id: userId, certInfo: true, authInfo: true, certificates: 'chain'}`. Parse response array → map `{credentialId: item.credential_id, subject: item.cert.subjectDN, serialNumber: item.cert.serialNumber, validFrom: item.cert.validFrom, validTo: item.cert.validTo, certificateBase64: item.cert.certificates[0], status: item.cert.status}`
    - `signHash(admin, {userId, credentialId}, {hashHex, documentName, documentId})`: _login, then POST `{baseUrl}/vtss/service/signHash` Bearer body `{client_id, client_secret, credentialID: credentialId, numSignatures: 1, documents: [{document_id: documentId, document_name: documentName}], hash: [hashHex], hashAlgo: '2.16.840.1.101.3.4.2.1', signAlgo: '1.2.840.113549.1.1.1', async: 1}`. Return `{providerTransactionId: response.transactionId}`. Throw Error('...') if transactionId missing.
    - `getSignStatus(admin, {userId}, providerTxnId)`: _login, then POST `{baseUrl}/vtss/service/requests/status` Bearer body `{transactionId: providerTxnId}`. response.status === '1' → `{status: 'completed', signatureBase64: response.signatures[0]}`. response.status === '0' → `{status: 'pending'}`. Else → `{status: 'failed', errorMessage: response.message ?? 'Unknown'}`.

    **Tests** (extend `provider.test.ts`):
    - Use Node builtin `node:test` + `mock` (Node 22+ has `mock.fn()` — confirm via tsc). If not available, use `nock` or inject an axios-like fn via constructor (preferred — constructor injection for test).
    - Design decision: export both adapters as plain objects AND factory constructors accepting `httpClient?: AxiosInstance` so tests can inject a mock `{post: mock.fn(), get: mock.fn()}`. Default to real axios instance.
    - Tests verify URL path + body shape matches postman/Model.cs literally:
      * SmartCA testConnection POST goes to `/sca/sp769/v1/credentials/get_certificate`
      * SmartCA body has `sp_id, sp_password, user_id` keys exactly
      * MySign login POST goes to `/vtss/service/ras/v1/login` with body `{client_id, user_id, client_secret, profile_id}` exactly
      * MySign signHash has Bearer header + body with `credentialID` key (case-sensitive — NOT credential_id)
      * MySign signHash body has `hashAlgo: '2.16.840.1.101.3.4.2.1'` + `signAlgo: '1.2.840.113549.1.1.1'`
    - Minimum 8 tests total (4 per provider)
  </behavior>
  <action>
    Step 1 — Create `smartca-vnpt.provider.ts`:
    - Import types from `./provider.interface.js`
    - Import `axios, { AxiosInstance }` from 'axios'
    - Export factory: `export function createSmartCaVnptProvider(httpClient?: AxiosInstance): SigningProvider` — default httpClient = `axios.create({ timeout: 15000 })`
    - Implement all 4 methods following Model.cs signatures above
    - Export convenience singleton: `export const smartcaVnptProvider: SigningProvider = createSmartCaVnptProvider();`

    Step 2 — Create `mysign-viettel.provider.ts`:
    - Same factory pattern `createMysignViettelProvider(httpClient?)`
    - `_login` as internal helper inside factory closure
    - Cache access_token NOT allowed (stateless) — every call does fresh login; production optimization (token cache) deferred to Phase 11
    - Export singleton `mysignViettelProvider`

    Step 3 — Extend `provider.test.ts`:
    - Use `mock.fn()` from `node:test` (Node 20+ supports). Inject mock into factory.
    - 4 tests SmartCA: testConnection-URL, testConnection-body, signHash-body, getSignStatus-URL
    - 4 tests MySign: login-URL-body, certificates-info-body-has-Bearer, signHash-body-credentialID-casing, requests-status-URL
    - Each test: create mock client, call method, assert `mock.calls[0].arguments` matches expected URL + body keys

    Step 4 — Run tests:
    ```
    cd e_office_app_new/backend
    SIGNING_SECRET_KEY=test_key_at_least_16_chars_long_xyz npx tsx --test src/services/signing/providers/provider.test.ts
    ```
    Expect ≥ 8 tests pass.

    Step 5 — Run tsc:
    ```
    cd e_office_app_new/backend && npx tsc --noEmit
    ```
    Zero new errors in `services/signing/providers/**`.
  </action>
  <verify>
    <automated>cd e_office_app_new/backend && SIGNING_SECRET_KEY=test_key_at_least_16_chars_long_xyz npx tsx --test src/services/signing/providers/provider.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>
    - Both adapter files exist with factory + singleton exports
    - All 4 methods implemented in each adapter
    - ≥ 8 tests pass (4 per provider)
    - tsc --noEmit shows 0 new errors in providers/
    - URL paths and body keys match Model.cs + postman EXACTLY (credentialID not credential_id for MySign signHash)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Provider factory reading DB + decrypting secrets</name>
  <read_first>
    - e_office_app_new/backend/src/services/signing/providers/provider.interface.ts (Task 1)
    - e_office_app_new/backend/src/services/signing/providers/smartca-vnpt.provider.ts (Task 2)
    - e_office_app_new/backend/src/services/signing/providers/mysign-viettel.provider.ts (Task 2)
    - e_office_app_new/backend/src/repositories/signing-provider-config.repository.ts (getActive + getByCode)
    - e_office_app_new/backend/src/services/signing/crypto.ts (decryptSecret)
  </read_first>
  <files>
    - e_office_app_new/backend/src/services/signing/providers/provider-factory.ts (CREATE)
    - e_office_app_new/backend/src/services/signing/providers/provider.test.ts (EXTEND — 2 more tests)
  </files>
  <behavior>
    - Export `getProviderByCode(code: ProviderCode): SigningProvider` — pure dispatcher (no DB), returns singleton from Task 2 based on code. Throw `Error('Provider không hỗ trợ: ' + code)` if code unknown.
    - Export `async function getActiveProviderWithCredentials(): Promise<{ provider: SigningProvider; credentials: AdminCredentials } | null>`:
      1. Call `signingProviderConfigRepository.getActive()`
      2. If null → return null (no active provider configured)
      3. Validate `activeRow.provider_code` is one of ProviderCode values — else throw
      4. Call `decryptSecret(activeRow.client_secret)` to get plaintext
      5. Build `credentials: AdminCredentials = { baseUrl, clientId, clientSecretPlaintext, profileId }`
      6. Call `getProviderByCode(activeRow.provider_code)` for provider instance
      7. Return `{ provider, credentials }`
    - Export `async function getProviderByCodeWithCredentials(code: ProviderCode): Promise<{ provider: SigningProvider; credentials: AdminCredentials } | null>` — same but for getByCode (used by Admin test-connection of inactive provider)
    - 2 extra tests: `getProviderByCode('SMARTCA_VNPT')` returns smartcaVnptProvider singleton; `getProviderByCode('INVALID_CODE')` throws
  </behavior>
  <action>
    Create `provider-factory.ts`:
    ```
    import { signingProviderConfigRepository } from '../../../repositories/signing-provider-config.repository.js';
    import { decryptSecret } from '../crypto.js';
    import { smartcaVnptProvider } from './smartca-vnpt.provider.js';
    import { mysignViettelProvider } from './mysign-viettel.provider.js';
    import type { AdminCredentials, ProviderCode, SigningProvider } from './provider.interface.js';

    export function getProviderByCode(code: string): SigningProvider {
      switch (code) {
        case 'SMARTCA_VNPT': return smartcaVnptProvider;
        case 'MYSIGN_VIETTEL': return mysignViettelProvider;
        default: throw new Error('Provider không hỗ trợ: ' + code);
      }
    }

    export async function getActiveProviderWithCredentials(): Promise<{ provider: SigningProvider; credentials: AdminCredentials } | null> {
      const active = await signingProviderConfigRepository.getActive();
      if (!active) return null;
      const plaintextSecret = await decryptSecret(active.client_secret);
      const provider = getProviderByCode(active.provider_code);
      const credentials: AdminCredentials = {
        baseUrl: active.base_url,
        clientId: active.client_id,
        clientSecretPlaintext: plaintextSecret,
        profileId: active.profile_id,
      };
      return { provider, credentials };
    }

    export async function getProviderByCodeWithCredentials(code: ProviderCode): Promise<{ provider: SigningProvider; credentials: AdminCredentials } | null> {
      const row = await signingProviderConfigRepository.getByCode(code);
      if (!row) return null;
      const plaintextSecret = await decryptSecret(row.client_secret);
      const provider = getProviderByCode(code);
      return { provider, credentials: { baseUrl: row.base_url, clientId: row.client_id, clientSecretPlaintext: plaintextSecret, profileId: row.profile_id } };
    }
    ```

    Extend `provider.test.ts`:
    - Test: `getProviderByCode('SMARTCA_VNPT').code === 'SMARTCA_VNPT'`
    - Test: `getProviderByCode('UNKNOWN')` throws (use `assert.throws`)
    - SKIP integration test of `getActiveProviderWithCredentials` — requires DB row; deferred to Plan 02 API integration

    Run tsc + tests:
    ```
    cd e_office_app_new/backend && npx tsc --noEmit
    SIGNING_SECRET_KEY=test_key_at_least_16_chars_long_xyz npx tsx --test src/services/signing/providers/provider.test.ts
    ```
    Expect ≥ 10 tests pass (8 adapter + 2 factory).
  </action>
  <verify>
    <automated>cd e_office_app_new/backend && npx tsc --noEmit 2>&1 | grep -E "providers/.*error" | head -5 ; echo "---" ; SIGNING_SECRET_KEY=test_key_at_least_16_chars_long_xyz npx tsx --test src/services/signing/providers/provider.test.ts 2>&1 | grep -E "(pass|fail|ok)" | tail -5</automated>
  </verify>
  <done>
    - `provider-factory.ts` exports 3 functions
    - `getProviderByCode` switch handles both codes + default throw
    - `getActiveProviderWithCredentials` decrypts secret + returns typed tuple
    - ≥ 10 tests pass total
    - tsc --noEmit shows 0 new errors in providers/
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → VNPT/Viettel provider API | Outbound HTTPS carrying client_secret plaintext (in Authorization/body). Provider trusted for production, but DEV/test env could have MITM — MUST use HTTPS only (reject http://). |
| DB → Backend | client_secret BYTEA decrypted in memory briefly; must not log, not include in error messages returned to HTTP caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | smartca-vnpt.provider.ts | mitigate | Never log full request body (contains sp_password). Error messages thrown MUST scrub client_secret before surfacing. Use `{...req, sp_password: '***'}` in logs. |
| T-09-02 | Information Disclosure | mysign-viettel.provider.ts | mitigate | Same — scrub `client_secret` and `access_token` before logging. Do NOT cache access_token in shared memory (race condition + disclosure risk if backend shared by multiple tenants in future). |
| T-09-03 | Tampering | provider-factory.ts | mitigate | `getProviderByCode` uses switch with default-throw (not dynamic import by string) — prevents code injection via DB `provider_code` value. |
| T-09-04 | Spoofing | both adapters (baseUrl) | mitigate | Validate `baseUrl.startsWith('https://')` in AdminCredentials at adapter entry; throw `Error('baseUrl phải là HTTPS')` if http:// to prevent MITM. Exception: allow `http://localhost` for dev. |
| T-09-05 | Denial of Service | both adapters (timeout) | mitigate | Set `axios.create({ timeout: 15000 })` (15s) in default httpClient — prevents hanging requests blocking Phase 11 worker. |
| T-09-06 | Repudiation | provider.test.ts | accept | Tests don't hit real provider — mocked. Real auditability comes from `sign_transactions` table (Phase 8) + worker logs (Phase 11). |
</threat_model>

<verification>
- All 3 files in `providers/` compile with `npx tsc --noEmit` (0 new errors)
- Test suite runs: ≥ 10 tests pass (4 SmartCA + 4 MySign + 2 factory)
- URL paths verified by grep: `grep -n "sca/sp769/v1" smartca-vnpt.provider.ts` → 3 matches; `grep -n "vtss/service" mysign-viettel.provider.ts` → 4 matches
- Body key `credentialID` (capital D) in MySign signHash — case-sensitive match with postman
</verification>

<success_criteria>
- 5 files exist under `backend/src/services/signing/providers/`
- `SigningProvider` interface + 2 concrete implementations + factory
- ≥ 10 unit tests pass without real HTTP calls
- Factory decrypts `client_secret` via `decryptSecret()` (not direct Buffer access)
- No plaintext client_secret logged anywhere (grep confirms no `console.log.*client_secret`)
- HTTPS enforcement: baseUrl http:// rejected (except localhost)
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-config-provider-adapters/09-01-SUMMARY.md` with:
- requirements-completed: [SIGN-01, SIGN-02, CFG-03]
- provides: provider.interface.ts, 2 adapters, provider-factory.ts (consumed by Plan 02 route)
- key-decisions: factory pattern, httpClient DI for testing, no token caching, HTTPS-only baseUrl
- Files created + line counts
- Test output excerpt
</output>
