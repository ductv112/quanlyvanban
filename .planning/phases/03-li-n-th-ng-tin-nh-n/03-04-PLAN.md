---
phase: 03-li-n-th-ng-tin-nh-n
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/van-ban-lien-thong/page.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-lien-thong/[id]/page.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx
autonomous: true
requirements:
  - VBLT-01
  - VBLT-02
  - VBLT-03
must_haves:
  truths:
    - "User sees VB lien thong list with 8 columns and filter row"
    - "User clicks row to navigate to detail page with info grid and action toolbar"
    - "User can Giao viec from VB den detail via drawer with pre-filled fields"
    - "User can Nhan ban giao / Chuyen lai / Huy duyet from VB den detail toolbar"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/van-ban-lien-thong/page.tsx"
      provides: "VB lien thong list page"
      min_lines: 120
    - path: "e_office_app_new/frontend/src/app/(main)/van-ban-lien-thong/[id]/page.tsx"
      provides: "VB lien thong detail page"
      min_lines: 150
  key_links:
    - from: "van-ban-lien-thong/page.tsx"
      to: "/api/van-ban-lien-thong"
      via: "api.get"
      pattern: "api\\.get.*van-ban-lien-thong"
    - from: "van-ban-den/[id]/page.tsx"
      to: "/api/van-ban-den/:id/giao-viec"
      via: "api.post"
      pattern: "api\\.post.*giao-viec"
---

<objective>
Create frontend pages for VB lien thong (list + detail) and add toolbar action buttons (Giao viec, Nhan ban giao, Chuyen lai, Huy duyet) to VB den detail page.

Purpose: Users can view and manage inter-agency documents. Users can quickly assign work from incoming docs.
Output: 2 new pages (VB lien thong list + detail), modified VB den detail page with 4 new toolbar actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-li-n-th-ng-tin-nh-n/03-CONTEXT.md
@.planning/phases/03-li-n-th-ng-tin-nh-n/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: VB lien thong list + detail pages</name>
  <files>
    e_office_app_new/frontend/src/app/(main)/van-ban-lien-thong/page.tsx,
    e_office_app_new/frontend/src/app/(main)/van-ban-lien-thong/[id]/page.tsx
  </files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx (MUST read — reuse exact pattern per D-01)
    - e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx (detail page pattern per D-02)
    - e_office_app_new/frontend/src/lib/api.ts (axios instance)
    - e_office_app_new/frontend/src/app/globals.css (CSS classes: .page-header, .page-card, .filter-row, .detail-header, .info-grid, .doc-abstract-box)
    - .planning/phases/03-li-n-th-ng-tin-nh-n/03-UI-SPEC.md (page inventory sections 1 and 2)
  </read_first>
  <action>
**List page: `van-ban-lien-thong/page.tsx`** (per D-01, D-03)

Reuse VB den list page pattern exactly. Key differences:
- API endpoint: `/van-ban-lien-thong` (GET)
- Page title: "Van ban lien thong" (with diacritics: "Van ban lien thong" -> "Van ban lien thong")
- Wait — ALL UI text MUST be Vietnamese with diacritics per CLAUDE.md.

Title: "Van ban lien thong" with proper diacritics. Actually reading the UI spec:
Page title uses `.page-title` class.

Columns (per UI spec section 1):
1. STT — 60px, center, render index
2. Ngay nhan — 110px, render dayjs format
3. Ky hieu — 160px, bold color #1B3A5C
4. Trich yeu — flexible, ellipsis with Tooltip
5. Han tra loi — 110px, red text if overdue (dayjs().isAfter check)
6. Don vi phat hanh — 180px
7. Nguoi ky — 140px
8. Trang thai — 120px, Tag component

Filter row (`.filter-row`): Input keyword search + Select trang thai + RangePicker ngay nhan.

State management: useState for data, loading, filters, pagination. useEffect to fetch on filter/page change.

Row click: `router.push('/van-ban-lien-thong/${record.id}')`.

Pagination: Ant Design Table pagination prop.

**Detail page: `van-ban-lien-thong/[id]/page.tsx`** (per D-02, D-07, D-08)

Reuse VB den detail page pattern. Fetch from `GET /api/van-ban-lien-thong/${id}`.

Layout: `.detail-header` toolbar with 3 action buttons + card body with `.info-grid`.

Toolbar actions (per UI spec section 2):
- "Nhan ban giao" — `<Button type="primary" style={{ backgroundColor: '#059669' }}>` with Popconfirm: "Ban co chac chan nhan ban giao van ban nay?" OK: "Xac nhan", Cancel: "Huy". On confirm: POST `/api/van-ban-den/${id}/nhan-ban-giao` then message.success.
- "Chuyen lai" — `<Button>` default. On click: Modal.confirm with Form containing TextArea for reason (required). On OK: POST `/api/van-ban-den/${id}/chuyen-lai` with { reason }.
- "Huy duyet" — `<Button danger>` with Popconfirm: "Huy duyet van ban? Van ban se duoc chuyen ve trang thai chua duyet. Thao tac nay khong the hoan tac." OK: "Huy duyet" (danger), Cancel: "Dong". On confirm: POST `/api/van-ban-den/${id}/huy-duyet`.

Info grid: 2-column layout showing all VB fields. `.doc-abstract-box` for trich yeu.

Use `App.useApp()` for message instance. Use `'use client'` directive.

ALL text in Vietnamese with diacritics.
  </action>
  <verify>
    <automated>cd D:/ProjectAI/quanlyvanban/e_office_app_new/frontend && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - van-ban-lien-thong/page.tsx renders Table with 8 columns matching UI spec
    - Filter row has keyword Input, trang thai Select, RangePicker
    - Row click navigates to /van-ban-lien-thong/[id]
    - Detail page has 3 toolbar buttons: Nhan ban giao (Popconfirm), Chuyen lai (Modal with TextArea), Huy duyet (Popconfirm)
    - All Vietnamese text has diacritics
    - Uses CSS classes from globals.css (not inline styles for layout)
    - Next.js build succeeds
  </acceptance_criteria>
  <done>VB lien thong list and detail pages render with full functionality</done>
</task>

<task type="auto">
  <name>Task 2: Add Giao viec + action buttons to VB den detail page</name>
  <files>e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx</files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx (FULL file — modifying existing page)
    - .planning/phases/03-li-n-th-ng-tin-nh-n/03-UI-SPEC.md (section 3 — Giao viec button spec)
    - e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx (HSCV drawer pattern for reference)
  </read_first>
  <action>
**Modify existing VB den detail page** (per D-04, D-05, D-06, D-07, D-08)

Add to the toolbar/header area of the VB den detail page:

1. **"Giao viec" button** — `<Button type="primary" icon={<ThunderboltOutlined />} style={{ backgroundColor: '#0891B2' }}>Giao viec</Button>` (with diacritics). On click: open giao viec Drawer.

2. **"Nhan ban giao" button** — same as VB lien thong detail (Popconfirm). POST to `/api/van-ban-den/${id}/nhan-ban-giao`.

3. **"Chuyen lai" button** — same as VB lien thong detail (Modal with TextArea). POST to `/api/van-ban-den/${id}/chuyen-lai`.

4. **"Huy duyet" button** — same as VB lien thong detail (Popconfirm danger). POST to `/api/van-ban-den/${id}/huy-duyet`.

**Giao viec Drawer** (per UI spec section 3):
- Width: 720, rootClassName: "drawer-gradient"
- Title: "Giao viec" (with diacritics)
- Form fields:
  1. Ten ho so (required) — `<Input>` pre-filled from VB den's `abstract` field
  2. Han xu ly (required) — `<DatePicker>` pre-filled from VB den's `expired_date`
  3. Nguoi phu trach (required) — `<Select mode="multiple">` fetching staff list from `/api/quan-tri/nhan-vien` or existing staff endpoint. Search by name.
  4. Ghi chu — `<Input.TextArea>` optional
- Footer: "Tao va giao viec" (primary) | "Huy" (default)
- On submit: POST `/api/van-ban-den/${id}/giao-viec` with { name, start_date: today, end_date, curator_ids, note }
- On success: close drawer, message.success('Giao viec thanh cong'), optionally navigate to HSCV.

Add imports: ThunderboltOutlined from @ant-design/icons, Drawer, DatePicker, Select from antd.

Use `Form` with `validateTrigger="onSubmit"` (project convention).

Pre-fill logic (per D-05): when drawer opens, form.setFieldsValue({ name: docData.abstract, end_date: docData.expired_date ? dayjs(docData.expired_date) : undefined }).

Vietnamese text with diacritics throughout.
  </action>
  <verify>
    <automated>cd D:/ProjectAI/quanlyvanban/e_office_app_new/frontend && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - VB den detail page has 4 new buttons in toolbar: Giao viec, Nhan ban giao, Chuyen lai, Huy duyet
    - Giao viec opens Drawer width 720 with rootClassName="drawer-gradient"
    - Drawer pre-fills ten ho so from abstract and han xu ly from expired_date
    - Chuyen lai uses Modal.confirm with TextArea for reason (required validation)
    - Huy duyet uses Popconfirm with danger styling
    - ThunderboltOutlined icon on Giao viec button
    - All Vietnamese text with diacritics
    - Next.js build succeeds
  </acceptance_criteria>
  <done>VB den detail page has all 4 action buttons functional with proper UI patterns</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> API | All actions go through authenticated API calls (axios with JWT interceptor) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | Giao viec form | mitigate | Server validates required fields — frontend validation is UX only |
| T-03-13 | Info Disclosure | VB lien thong list | mitigate | API filters by unitId from JWT — frontend cannot see other units' docs |
</threat_model>

<verification>
Next.js build succeeds. Navigate to /van-ban-lien-thong to see list. Click row for detail. Navigate to VB den detail to see new toolbar buttons.
</verification>

<success_criteria>
- 2 new pages created and accessible via routing
- VB den detail page has 4 new action buttons
- Giao viec drawer pre-fills from VB den data
- All actions call correct API endpoints
- All UI text in Vietnamese with diacritics
</success_criteria>

<output>
After completion, create `.planning/phases/03-li-n-th-ng-tin-nh-n/03-04-SUMMARY.md`
</output>
