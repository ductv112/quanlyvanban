---
phase: 02-h-s-c-ng-vi-c
plan: 05
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx
  - e_office_app_new/frontend/src/app/globals.css
autonomous: true
requirements: [HSCV-03, HSCV-04, HSCV-05, HSCV-06, HSCV-07]

must_haves:
  truths:
    - "User can view HSCV detail with 6 card tabs"
    - "User can assign staff via Transfer panel in tab 3"
    - "User can add opinions in tab 4"
    - "User can link documents in tab 2"
    - "User can upload/download/delete attachments in tab 5"
    - "User can transition HSCV status via toolbar buttons"
    - "User can create child HSCV from tab 6"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx"
      provides: "HSCV detail page with 6 tabs and toolbar"
      min_lines: 500
  key_links:
    - from: "[id]/page.tsx"
      to: "/api/ho-so-cong-viec/:id"
      via: "api.get"
      pattern: "api\\.get.*ho-so-cong-viec"
    - from: "[id]/page.tsx"
      to: "tree-utils.ts"
      via: "import buildTree/flattenTreeForSelect"
      pattern: "tree-utils"
---

<objective>
Create the HSCV detail page with 6 card tabs (per D-06, D-07), dynamic toolbar actions (per D-08, D-13), Transfer panel for staff assignment (per D-09, D-10), comment feed (per D-11), document linking (per D-12), file attachments, and child HSCV listing.

Purpose: This is the most content-rich page in HSCV — it shows everything about a single record and allows all workflow operations.

Output: Detail page.tsx + additional CSS classes in globals.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-h-s-c-ng-vi-c/02-CONTEXT.md
@.planning/phases/02-h-s-c-ng-vi-c/02-UI-SPEC.md
@.planning/phases/02-h-s-c-ng-vi-c/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HSCV detail page with 6 tabs</name>
  <files>e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx</files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx (reference for page structure, api calls)
    - e_office_app_new/frontend/src/app/globals.css (existing CSS classes: .detail-header, .info-grid, .info-label, .info-value, .doc-abstract-box, .section-title)
    - e_office_app_new/frontend/src/lib/tree-utils.ts (buildTree, flattenTreeForSelect — for department tree in Transfer panel)
    - e_office_app_new/frontend/src/types/tree.ts (TreeNode type)
    - e_office_app_new/frontend/src/lib/api.ts
    - e_office_app_new/frontend/src/stores/auth.store.ts
    - .planning/phases/02-h-s-c-ng-vi-c/02-UI-SPEC.md (sections 3, 4, 7 — detail page, toolbar, interactions)
  </read_first>
  <action>
Create `[id]/page.tsx` with `'use client'` directive. This is a large but single-concern page.

**Imports:** React hooks (useState, useEffect, useCallback), Ant Design (Card, Tabs, Table, Button, Form, Input, TextArea, Select, DatePicker, Tag, Badge, Progress, Upload, List, Avatar, Divider, Modal, Slider, InputNumber, Space, Breadcrumb, Skeleton, Tree as DirectoryTree, Radio, Popconfirm, message from App.useApp()), dayjs, api, useAuthStore, useRouter/useParams from next/navigation, icons (ArrowLeftOutlined, FolderOutlined, FileTextOutlined, TeamOutlined, CommentOutlined, PaperClipOutlined, ApartmentOutlined, SaveOutlined, UploadOutlined, DeleteOutlined, DownloadOutlined, PlusOutlined, ExclamationCircleOutlined), buildTree/flattenTreeForSelect from '@/lib/tree-utils'.

**Reuse STATUS_MAP constant** (same as list page — define locally or extract to shared constants later).

**Page params:** `const { id } = useParams<{ id: string }>();`

**State:**
- `detail` (full HSCV detail record or null)
- `loading` (boolean)
- `activeTab` (string, default 'info')
- Tab-specific data: `linkedDocs`, `staffList`, `opinions`, `attachments`, `children` — each with own loading state
- `departments` (for Transfer panel tree)
- `departmentStaff` (staff list for selected department)
- `selectedDepartment` (for Transfer panel)
- `assignedStaff` (right side of Transfer)
- `opinionForm` (Form.useForm for adding opinions)
- `assignForm` (Form.useForm for assignment)

**Data fetching:**
- `fetchDetail()` — GET `/ho-so-cong-viec/${id}` — call on mount
- Tab data fetched lazily when tab is activated:
  - Tab 2: GET `/ho-so-cong-viec/${id}/van-ban-lien-ket`
  - Tab 3: GET `/ho-so-cong-viec/${id}/can-bo` + GET `/quan-tri/don-vi/tree` for department tree
  - Tab 4: GET `/ho-so-cong-viec/${id}/y-kien`
  - Tab 5: GET `/ho-so-cong-viec/${id}/dinh-kem`
  - Tab 6: GET `/ho-so-cong-viec/${id}/hscv-con`

**Layout:**
1. Breadcrumb: `Trang chủ / Hồ sơ công việc / {detail.name}`
2. `.detail-header` bar:
   - Left: ArrowLeftOutlined button (onClick router.back()) + Tag trạng thái (size default) + Tên HSCV (22px, 700 weight)
   - Right: Dynamic action buttons (see Task 2 below — implemented inline in this file)
3. Card with Tabs type="card":

**Tab 1 — Thông tin chung (key='info'):**
- `.info-grid` layout (2 columns) showing: Ngày mở, Hạn giải quyết, Lĩnh vực, Loại VB, Quy trình, Trạng thái, Người phụ trách, Lãnh đạo ký, Tiến độ (Progress component)
- If `detail.comments`: show in `.doc-abstract-box` under `.section-title` "Ghi chú"

**Tab 2 — Văn bản liên kết (key='linked-docs'):**
- Header: "Văn bản liên kết" + Badge count + Button "Thêm văn bản" (primary ghost)
- Table: Số VB | Trích yếu | Loại (Tag) | Ngày | Thao tác (nút "Gỡ liên kết")
- "Thêm văn bản" opens Modal:
  - Input.Search for keyword
  - Tabs: VB đến / VB đi / Dự thảo
  - Table with checkbox selection
  - On OK: POST `/ho-so-cong-viec/${id}/lien-ket-van-ban` for each selected doc
- "Gỡ liên kết": DELETE `/ho-so-cong-viec/${id}/lien-ket-van-ban/${linkId}` with Popconfirm
- Empty state: "Chưa có văn bản liên kết. Nhấn "Thêm văn bản" để liên kết văn bản đến/đi/dự thảo."

**Tab 3 — Cán bộ xử lý (key='staff'):**
- Custom Transfer panel (NOT Ant Design Transfer component — custom layout per D-09):
  - Left panel: DirectoryTree of departments (use buildTree from tree-utils.ts per D-10). On select department → fetch staff list for that department via GET `/quan-tri/don-vi/${deptId}/nhan-vien` or similar.
  - Staff list below tree: checkboxes + name + position. Button "Thêm >>" to move selected to right.
  - Right panel: Assigned staff list. Each row: name, Radio.Group (Phụ trách=1 / Phối hợp=2), DatePicker Hạn xử lý, Delete button.
  - Phụ trách badge color: `#059669`, Phối hợp: `#0891B2`
  - Button "Lưu phân công" at bottom → POST `/ho-so-cong-viec/${id}/phan-cong` with staff_ids, role_type, deadline
  - message.success('Phân công cán bộ thành công')

**Tab 4 — Ý kiến xử lý (key='opinions'):**
- List component rendering opinions: Avatar (32px circle, first letter of staff_name, stable color from id), staff_name (600 weight), timestamp (12px, #64748B), content below
- Divider
- Form: TextArea rows={4} placeholder "Nhập ý kiến xử lý..." + Button "Gửi ý kiến" (primary)
- On submit: POST `/ho-so-cong-viec/${id}/y-kien` with { content }, refetch opinions
- Empty state: "Chưa có ý kiến xử lý. Hãy là người đầu tiên thêm ý kiến."

**Tab 5 — File đính kèm (key='attachments'):**
- Upload.Dragger: accept `.pdf,.doc,.docx,.xls,.xlsx,.png,.jpg`, maxCount unlimited but maxSize 50MB each
- customRequest: POST `/ho-so-cong-viec/${id}/dinh-kem` with FormData containing file
- File list below: icon (file type), file_name, file_size formatted, created_at, Button Tải xuống (GET presigned URL), Button Xóa (Popconfirm "Xóa file này?")
- Download: window.open(presignedUrl)

**Tab 6 — HSCV con (key='children'):**
- Header: Button "Tạo HSCV con" (primary ghost)
- Table: compact version of main list (Tên HSCV clickable, Ngày mở, Hạn, Trạng thái, Tiến độ) — no Phụ trách/Lãnh đạo ký columns
- Click name → router.push(`/ho-so-cong-viec/${childId}`)
- "Tạo HSCV con" → opens same Drawer as list page but with parent_id pre-filled = current id. Reuse Drawer component inline or navigate to list page with query param. Simplest: open a local Drawer with the create form, pre-fill parent_id.
- Empty state: "Chưa có hồ sơ con. Nhấn "Tạo HSCV con" để thêm hồ sơ con."

**Dynamic Toolbar Actions (per D-08, D-13 — implemented at top of detail header):**

Based on `detail.status`, render different buttons:

```typescript
const getToolbarButtons = (status: number) => {
  switch (status) {
    case 0: // Mới tạo
      return [
        { label: 'Chuyển xử lý', type: 'primary', action: 'change', newStatus: 1 },
        { label: 'Sửa', type: 'default', action: 'edit' },
        { label: 'Xóa', type: 'default', danger: true, ghost: true, action: 'delete' },
      ];
    case 1: // Đang xử lý
      return [
        { label: 'Trình ký', type: 'primary', action: 'submit' },
        { label: 'Cập nhật tiến độ', type: 'default', action: 'progress' },
        { label: 'Tạm dừng', type: 'default', action: 'change', newStatus: 5 },
      ];
    case 2: // Chờ trình ký -> mapped to Đang duyệt in UI
      return [
        { label: 'Duyệt hồ sơ', type: 'primary', action: 'approve', style: { backgroundColor: '#059669', borderColor: '#059669' } },
        { label: 'Từ chối', type: 'primary', danger: true, action: 'reject' },
        { label: 'Trả về', type: 'default', action: 'return' },
      ];
    case 4: // Hoàn thành
      return [{ label: 'Xem lịch sử', type: 'default', action: 'history' }];
    case -1: case -2: // Từ chối / Trả về
      return [
        { label: 'Xử lý lại', type: 'primary', action: 'change', newStatus: 1 },
        { label: 'Hủy HSCV', type: 'default', danger: true, ghost: true, action: 'change', newStatus: -3 },
      ];
    default: return [];
  }
};
```

**Status transition handlers:**
- `handleSubmit`: PATCH `/ho-so-cong-viec/${id}/trang-thai` body: `{ action: 'submit' }`
- `handleApprove`: PATCH with `{ action: 'approve' }`
- `handleReject`: Modal.confirm with TextArea for reason (required) → PATCH with `{ action: 'reject', reason }`
- `handleReturn`: Modal.confirm with TextArea for reason (required) → PATCH with `{ action: 'return', reason }`
- `handleComplete`: PATCH with `{ action: 'complete' }`
- `handleUpdateProgress`: Modal with Slider 0-100 + InputNumber → PATCH `/ho-so-cong-viec/${id}/tien-do` body: `{ progress }`
- `handleDelete`: Modal.confirm danger → DELETE `/ho-so-cong-viec/${id}` → router.push('/ho-so-cong-viec')
- After any transition: refetch detail + counts

**Loading state:** Skeleton active paragraph rows 6 while detail is null.
  </action>
  <verify>
    <automated>grep -c "use client" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `'use client'`
    - `grep "useParams" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - `grep "Thông tin chung" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - `grep "Văn bản liên kết" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - `grep "Cán bộ xử lý" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - `grep "Ý kiến xử lý" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - `grep "File đính kèm" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - `grep "HSCV con" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - `grep "tree-utils" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - `grep "trang-thai" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - `grep "Modal.confirm" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - `grep "Upload.Dragger\|Upload" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - `grep "Lưu phân công" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - `grep "Gửi ý kiến" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` returns a match
    - All Vietnamese text has diacritics
  </acceptance_criteria>
  <done>HSCV detail page with all 6 tabs functional, dynamic toolbar, Transfer panel for staff assignment, comment feed, doc linking with search modal, file upload/download, child HSCV listing.</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS classes for detail page components</name>
  <files>e_office_app_new/frontend/src/app/globals.css</files>
  <read_first>
    - e_office_app_new/frontend/src/app/globals.css (current state — check what classes already exist)
  </read_first>
  <action>
Add these CSS classes to globals.css (append to end of file, DO NOT modify existing classes):

```css
/* ============================
   HSCV Detail Page
   ============================ */

/* Detail header bar */
.detail-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 24px;
  background: #fff;
  border-radius: 8px;
  margin-bottom: 16px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
}

.detail-header-left {
  display: flex;
  align-items: center;
  gap: 12px;
}

.detail-header-right {
  display: flex;
  align-items: center;
  gap: 8px;
}

.detail-header-title {
  font-size: 22px;
  font-weight: 700;
  color: #1B3A5C;
  margin: 0;
}

/* Transfer panel for staff assignment */
.transfer-panel {
  display: flex;
  gap: 16px;
  min-height: 400px;
}

.transfer-panel-left {
  flex: 1;
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  padding: 12px;
  overflow-y: auto;
  max-height: 500px;
}

.transfer-panel-right {
  flex: 1;
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  padding: 12px;
  overflow-y: auto;
  max-height: 500px;
}

.transfer-panel-actions {
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 8px;
}

/* Staff assignment row */
.staff-assign-row {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 0;
  border-bottom: 1px solid #f0f0f0;
}

.staff-assign-row:last-child {
  border-bottom: none;
}

/* Comment / Opinion feed */
.opinion-item {
  display: flex;
  gap: 12px;
  padding: 12px 0;
}

.opinion-item-content {
  flex: 1;
}

.opinion-item-header {
  display: flex;
  align-items: baseline;
  gap: 8px;
  margin-bottom: 4px;
}

.opinion-item-name {
  font-weight: 600;
  font-size: 14px;
  color: #1B3A5C;
}

.opinion-item-time {
  font-size: 12px;
  color: #64748B;
}

.opinion-item-text {
  font-size: 14px;
  line-height: 1.5;
  color: #334155;
}

/* File attachment list */
.attachment-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 8px 12px;
  border: 1px solid #f0f0f0;
  border-radius: 6px;
  margin-bottom: 8px;
}

.attachment-item:hover {
  background: #f8fafc;
}

.attachment-info {
  display: flex;
  align-items: center;
  gap: 8px;
  flex: 1;
}

.attachment-info .file-name {
  font-weight: 500;
  color: #1B3A5C;
}

.attachment-info .file-meta {
  font-size: 12px;
  color: #64748B;
}

/* Section title inside tabs */
.section-title {
  font-size: 16px;
  font-weight: 600;
  color: #1B3A5C;
  margin-bottom: 12px;
  padding-bottom: 8px;
  border-bottom: 1px solid #f0f0f0;
}
```

**IMPORTANT:** First read the entire globals.css to check if any of these classes already exist (e.g., `.detail-header`, `.section-title` may already be defined). If they exist, do NOT duplicate — only add the ones that are missing. If an existing class needs additional properties, add them to the existing definition.
  </action>
  <verify>
    <automated>grep -c "detail-header\|transfer-panel\|opinion-item\|attachment-item" e_office_app_new/frontend/src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep "transfer-panel" e_office_app_new/frontend/src/app/globals.css` returns a match
    - `grep "opinion-item" e_office_app_new/frontend/src/app/globals.css` returns a match
    - `grep "attachment-item" e_office_app_new/frontend/src/app/globals.css` returns a match
    - `grep "detail-header" e_office_app_new/frontend/src/app/globals.css` returns a match
    - No duplicate class definitions (each class defined exactly once)
    - No inline styles used for layout in page.tsx — all layout via CSS classes
  </acceptance_criteria>
  <done>All CSS classes for HSCV detail page components added to globals.css without duplicates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> API | File uploads go through API with size validation |
| User input -> Display | Opinion content displayed as text (not HTML) — XSS safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | File upload | mitigate | Accept list restricted to safe extensions (.pdf,.doc,.docx,.xls,.xlsx,.png,.jpg); server validates separately |
| T-02-16 | Spoofing | Status transitions | mitigate | staffId from JWT, not from UI — backend validates permissions |
| T-02-17 | Information Disclosure | File download | mitigate | Presigned URLs with 1-hour expiry from MinIO — no persistent public links |
</threat_model>

<verification>
- Detail page renders at /ho-so-cong-viec/:id
- All 6 tabs load data lazily
- Toolbar buttons change based on status
- Transfer panel shows department tree and staff
</verification>

<success_criteria>
Complete HSCV detail page with all 6 tabs and workflow toolbar matching UI-SPEC exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-h-s-c-ng-vi-c/02-05-SUMMARY.md`
</output>
