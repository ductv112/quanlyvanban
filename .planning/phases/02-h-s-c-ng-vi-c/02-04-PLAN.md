---
phase: 02-h-s-c-ng-vi-c
plan: 04
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx
autonomous: true
requirements: [HSCV-01, HSCV-02]

must_haves:
  truths:
    - "User can view paginated HSCV list with 10 filter tabs and badge counts"
    - "User can create/edit HSCV via 720px Drawer with all form fields"
    - "User can delete HSCV via Modal.confirm"
    - "User can click HSCV name to navigate to detail page"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx"
      provides: "HSCV list page with filter tabs, table, drawer, CRUD"
      min_lines: 300
  key_links:
    - from: "page.tsx"
      to: "/api/ho-so-cong-viec"
      via: "api.get/post/put/delete"
      pattern: "api\\.(get|post|put|delete).*ho-so-cong-viec"
---

<objective>
Create the HSCV list page with filter tabs (10 statuses + badge counts), data table, and Drawer for create/edit.

Purpose: This is the main entry point for the HSCV module — users land here to browse, filter, create, edit, and delete HSCV records. Per D-01 (tab filter bar), D-02 (badge count), D-03 (table columns), D-04 (Drawer 720px), D-05 (form fields).

Output: Single page.tsx file covering list view + CRUD drawer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-h-s-c-ng-vi-c/02-CONTEXT.md
@.planning/phases/02-h-s-c-ng-vi-c/02-UI-SPEC.md
@.planning/phases/02-h-s-c-ng-vi-c/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HSCV list page with filter tabs, table, and CRUD drawer</name>
  <files>e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx</files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx (reference pattern — 'use client', useState, useEffect, Table, Drawer, Form, api calls, filter, pagination)
    - e_office_app_new/frontend/src/app/globals.css (available CSS classes: .page-header, .page-title, .page-card, .drawer-gradient, .filter-row)
    - e_office_app_new/frontend/src/lib/api.ts (axios instance)
    - e_office_app_new/frontend/src/config/constants.ts (existing constants pattern)
    - e_office_app_new/frontend/src/config/theme.ts (theme tokens)
    - e_office_app_new/frontend/src/stores/auth.store.ts (useAuthStore for user info)
    - .planning/phases/02-h-s-c-ng-vi-c/02-UI-SPEC.md (full UI specification)
  </read_first>
  <action>
Create `page.tsx` with `'use client'` directive. Follow the van-ban-den page pattern exactly.

**Imports:** React hooks, Ant Design (Table, Tabs, Badge, Button, Drawer, Form, Input, Select, DatePicker, Progress, Tag, Dropdown, Modal, Space, Row, Col, Skeleton, message from App.useApp()), dayjs, api from '@/lib/api', useAuthStore, PlusOutlined, MoreOutlined, ExclamationCircleOutlined, SearchOutlined, SaveOutlined, useRouter from next/navigation.

**Constants (top of file):**

```typescript
const STATUS_MAP: Record<number, { label: string; color: string }> = {
  0: { label: 'Mới tạo', color: '#1677FF' },
  1: { label: 'Đang xử lý', color: '#0891B2' },
  2: { label: 'Chờ trình ký', color: '#D97706' },
  3: { label: 'Đã trình ký', color: '#7C3AED' },
  // Note: status 3 in DB is "Đã duyệt" but UI maps it differently per spec
  // Adjust based on actual SP mapping
  4: { label: 'Hoàn thành', color: '#059669' },
  '-1': { label: 'Từ chối', color: '#DC2626' },
  '-2': { label: 'Trả về', color: '#F59E0B' },
};

const FILTER_TABS = [
  { key: 'all', label: 'Tất cả' },
  { key: 'created_by_me', label: 'Tôi tạo' },
  { key: 'rejected', label: 'Bị từ chối' },
  { key: 'returned', label: 'Trả về bổ sung' },
  { key: 'pending_primary', label: 'Chưa XL phụ trách' },
  { key: 'pending_coord', label: 'Chưa XL phối hợp' },
  { key: 'submitting', label: 'Trình ký' },
  { key: 'in_progress', label: 'Đang giải quyết' },
  { key: 'proposed_complete', label: 'Đề xuất hoàn thành' },
  { key: 'completed', label: 'Đã hoàn thành' },
];
```

**State variables:**
- `data`, `loading`, `total`, `page`, `pageSize` (table)
- `filterType` (active tab key, default 'all')
- `keyword`, `fieldId`, `unitId`, `dateRange` (filter row)
- `statusCounts` (Record<string, number> for badge counts)
- `drawerOpen`, `editingRecord`, `formLoading` (drawer)
- `form` (Form.useForm)
- `docTypes`, `docFields`, `staffList`, `workflows`, `parentHscvs` (select options)

**Data fetching:**
- `fetchList()` — GET `/ho-so-cong-viec?filter_type=${filterType}&keyword=...&page=...&page_size=...` — call on mount + when filterType/page/filters change
- `fetchCounts()` — GET `/ho-so-cong-viec/count-by-status` — call on mount
- `fetchOptions()` — GET doc types, doc fields, staff, workflows for Drawer selects — call on mount

**Filter Tabs (per D-01):**
- Ant Design `Tabs` with `type="line"` (per UI-SPEC)
- Each tab: `label={<span>{tab.label} <Badge count={statusCounts[tab.key] || 0} size="small" style={{ backgroundColor: filterType === tab.key ? '#0891B2' : '#94A3B8' }} /></span>}`
- `onChange` sets `filterType` and resets page to 1

**Filter Row (per UI-SPEC):**
- Inside `.filter-row` div
- Input search (240px, placeholder "Tìm kiếm tên hồ sơ..."), Select Lĩnh vực (160px), Select Đơn vị (160px), RangePicker (220px), Button "Tìm kiếm" (primary), Button "Đặt lại" (default)

**Table Columns (per D-03):**
1. STT — 56px, center, render: `(_, __, index) => (page - 1) * pageSize + index + 1`
2. Tên hồ sơ công việc — flex, render: clickable link `<a onClick={() => router.push('/ho-so-cong-viec/' + record.id)} style={{ fontWeight: 500, color: '#1B3A5C' }}>{record.name}</a>`
3. Ngày mở — 110px, `dayjs(record.start_date).format('DD/MM/YYYY')`
4. Hạn giải quyết — 110px, if overdue (end_date < now and status not completed) show red text + ExclamationCircleOutlined
5. Trạng thái — 130px, `<Tag color={STATUS_MAP[record.status]?.color}>{STATUS_MAP[record.status]?.label}</Tag>`
6. Phụ trách — 150px, `record.curator_name`
7. Lãnh đạo ký — 150px, `record.signer_name`
8. Tiến độ — 140px, `<Progress percent={record.progress} size="small" strokeColor="#0891B2" />`
9. Thao tác — 80px, Dropdown with MoreOutlined: Xem, Sửa, Xóa (with Divider before Xóa)

**Header:** `.page-header` with `.page-title` "Hồ sơ công việc" on left, Button "Tạo hồ sơ mới" with PlusOutlined on right.

**Delete:** Modal.confirm with `type: 'warning'`, `okType: 'danger'`, `okText: 'Xóa'`, `cancelText: 'Hủy bỏ'`, content "Bạn có chắc muốn xóa hồ sơ này? Hành động này không thể hoàn tác."

**Drawer (per D-04, D-05):**
- `width={720}`, `rootClassName="drawer-gradient"`
- Title: `editingRecord ? 'Chỉnh sửa hồ sơ công việc' : 'Tạo hồ sơ công việc'`
- Footer extra: Button "Lưu hồ sơ" (primary, SaveOutlined, loading), Button "Hủy" (default)
- Form `validateTrigger="onSubmit"`, layout `vertical`
- 2-column layout: `Row gutter={16}`, `Col span={12}` for half-width fields

Form fields:
- Tên hồ sơ công việc: Input, required, maxLength 500, full width (Col span={24})
- Loại văn bản: Select, optional, half width — options from docTypes
- Lĩnh vực: Select, optional, half width — options from docFields
- Ngày mở: DatePicker format="DD/MM/YYYY", required, half width
- Hạn giải quyết: DatePicker format="DD/MM/YYYY", required, half width — validator: must be >= Ngày mở
- Người phụ trách: Select showSearch optionFilterProp="label", required, half width — options from staffList
- Lãnh đạo ký: Select showSearch optionFilterProp="label", required, half width — options from staffList
- Quy trình: Select, optional, half width — options from workflows
- HSCV cha: Select showSearch, optional, half width — options from parentHscvs (only show if explicitly creating sub-HSCV)
- Ghi chú: TextArea rows={3}, optional, maxLength 2000, full width (Col span={24})

**Form submit:**
- If editingRecord: PUT `/ho-so-cong-viec/${editingRecord.id}` with form values
- Else: POST `/ho-so-cong-viec` with form values
- On success: message.success('Tạo hồ sơ thành công' or 'Lưu hồ sơ thành công'), close drawer, refetch list + counts
- On error: message.error from response or default

**Empty state:** When data is empty and no filters, show Ant Design Empty with description "Chưa có hồ sơ công việc" + "Nhấn "Tạo hồ sơ mới" để bắt đầu quản lý công việc của bạn."

**Loading:** Table loading prop, Skeleton for initial load.

**Pagination:** `pagination={{ current: page, pageSize, total, showSizeChanger: true, showTotal: (t) => \`Tổng ${t} hồ sơ\` }}`
  </action>
  <verify>
    <automated>grep -c "use client" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `'use client'`
    - `grep "FILTER_TABS" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx` returns a match
    - `grep "STATUS_MAP" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx` returns a match
    - `grep "count-by-status" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx` returns a match
    - `grep "drawer-gradient" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx` returns a match
    - `grep "Tạo hồ sơ mới" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx` returns a match
    - `grep "validateTrigger" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx` returns a match
    - `grep "Modal.confirm" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx` returns a match
    - `grep "router.push.*ho-so-cong-viec" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx` returns a match
    - `grep "Progress" e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/page.tsx` returns a match
    - File imports from '@/lib/api'
    - All Vietnamese text has diacritics
  </acceptance_criteria>
  <done>HSCV list page fully functional with 10 filter tabs + badge counts, data table with all columns per D-03, and Drawer for create/edit per D-04/D-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> API | All data display comes from API — no local trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Spoofing | page.tsx | mitigate | Uses axios instance with JWT auth interceptor from api.ts — no manual token handling |
| T-02-14 | Information Disclosure | page.tsx | accept | List data is scoped by unitId on backend — frontend just displays what API returns |
</threat_model>

<verification>
- Page renders without errors at /ho-so-cong-viec
- Filter tabs change the table data
- Drawer opens for create and edit
- Delete shows confirmation modal
</verification>

<success_criteria>
HSCV list page with full CRUD functionality matching the UI-SPEC exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-h-s-c-ng-vi-c/02-04-SUMMARY.md`
</output>
