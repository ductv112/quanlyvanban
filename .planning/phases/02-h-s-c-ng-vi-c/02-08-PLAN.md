---
phase: 02-h-s-c-ng-vi-c
plan: 08
type: execute
wave: 4
depends_on: [02-04, 02-05, 02-06, 02-07]
files_modified:
  - e_office_app_new/backend/seed_sprint5.js
autonomous: false
requirements: [HSCV-01, HSCV-02, HSCV-03, HSCV-04, HSCV-05, HSCV-06, HSCV-07, HSCV-08, HSCV-09, HSCV-10]

must_haves:
  truths:
    - "HSCV list page loads and filter tabs work"
    - "HSCV detail page shows all 6 tabs with data"
    - "Workflow designer loads ReactFlow canvas"
    - "KPI dashboard shows stat cards and charts"
    - "Reports page shows data tables"
  artifacts:
    - path: "e_office_app_new/backend/seed_sprint5.js"
      provides: "Seed data for testing HSCV module"
  key_links: []
---

<objective>
Create seed data for HSCV testing and verify the entire module end-to-end via human checkpoint.

Purpose: Ensure all HSCV pages render correctly, API endpoints work, and the full workflow is functional before moving to Phase 3.

Output: Seed script + human verification of all HSCV features.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-h-s-c-ng-vi-c/02-04-SUMMARY.md
@.planning/phases/02-h-s-c-ng-vi-c/02-05-SUMMARY.md
@.planning/phases/02-h-s-c-ng-vi-c/02-06-SUMMARY.md
@.planning/phases/02-h-s-c-ng-vi-c/02-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seed script for HSCV test data</name>
  <files>e_office_app_new/backend/seed_sprint5.js</files>
  <read_first>
    - e_office_app_new/backend/seed_sprint4.js (existing seed script pattern — pg pool, INSERT statements)
    - e_office_app_new/database/migrations/002_edoc_tables.sql (lines 331-450 — HSCV table schemas)
    - e_office_app_new/database/migrations/011_sprint6_workflow_tables_sps.sql (workflow table schemas)
  </read_first>
  <action>
Create `seed_sprint5.js` following the seed_sprint4.js pattern (Node.js script using `pg` pool).

**Seed data to insert:**

1. **Workflow (doc_flows):** 2 workflows
   - "Quy trình xử lý văn bản thông thường" (version "1.0", is_active=true)
   - "Quy trình xử lý văn bản mật" (version "1.0", is_active=true)

2. **Workflow steps (doc_flow_steps):** For workflow 1:
   - Start node (type='start', position 250,50)
   - "Tiếp nhận" (type='process', position 250,150, deadline_days=2)
   - "Xử lý" (type='process', position 250,250, deadline_days=5)
   - "Phê duyệt" (type='process', position 250,350, allow_sign=true, deadline_days=3)
   - End node (type='end', position 250,450)

3. **Workflow step links:** Start->Tiep nhan, Tiep nhan->Xu ly, Xu ly->Phe duyet, Phe duyet->End

4. **Handling docs:** 15 records with varied statuses (0, 1, 2, 3, 4, -1, -2), varied progress (0-100), varied dates. Use existing staff IDs and department IDs from seed data. Include 2 child HSCV records (parent_id set).

5. **Staff assignments (staff_handling_docs):** 8-10 assignments across the HSCV records, mix of role=1 (phu trach) and role=2 (phoi hop).

6. **Opinions (opinion_handling_docs):** 5-6 sample opinions on different HSCV records.

7. **Handling doc links:** 3-4 links connecting HSCV to existing VB den/di records (if any exist from seed_sprint4).

**Script structure:**
```javascript
const { Pool } = require('pg');
// Use same DB connection pattern as seed_sprint4.js
// Wrap in async IIFE
// INSERT with RETURNING id for FK references
// Console.log progress
// Handle errors gracefully
```

**IMPORTANT:** Use ON CONFLICT DO NOTHING or check existence to make script idempotent (can run multiple times without error).
  </action>
  <verify>
    <automated>grep -c "INSERT INTO" e_office_app_new/backend/seed_sprint5.js</automated>
  </verify>
  <acceptance_criteria>
    - File contains INSERT INTO statements for: edoc.doc_flows, edoc.doc_flow_steps, edoc.doc_flow_step_links, edoc.handling_docs, edoc.staff_handling_docs, edoc.opinion_handling_docs
    - Script uses `pg` Pool with same connection config as seed_sprint4.js
    - At least 15 handling_docs records with varied statuses
    - At least 2 workflow records
    - Script is idempotent (ON CONFLICT or existence checks)
  </acceptance_criteria>
  <done>Seed script creates comprehensive test data for all HSCV module features.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete HSCV module end-to-end</name>
  <files>none</files>
  <action>
Human verifies all HSCV module features work correctly after running migrations and seed data.

**Prerequisites:** Run migrations 010 + 011 and seed script:
```bash
cd e_office_app_new/backend
psql -f ../database/migrations/010_sprint5_handling_doc_sps.sql
psql -f ../database/migrations/011_sprint6_workflow_tables_sps.sql
node seed_sprint5.js
```

Then start backend + frontend:
```bash
cd e_office_app_new/backend && npm run dev
cd e_office_app_new/frontend && npm run dev
```

**Test checklist:**

1. **Danh sach HSCV** — Visit http://localhost:3000/ho-so-cong-viec
   - Filter tabs hien thi voi badge count
   - Click tab thay doi danh sach
   - Table hien thi dung columns (Ten, Ngay mo, Han, Trang thai Tag, Phu trach, Lanh dao ky, Tien do %)
   - Tim kiem theo keyword hoat dong
   - Click ten HSCV chuyen sang trang chi tiet

2. **Tao/Sua HSCV** — Click "Tao ho so moi"
   - Drawer 720px mo voi gradient header
   - Form validation hoat dong (ten bat buoc, han >= ngay mo)
   - Luu thanh cong, danh sach cap nhat
   - Sua HSCV: Drawer pre-fill dung du lieu

3. **Chi tiet HSCV** — Visit /ho-so-cong-viec/:id
   - Header hien thi trang thai + ten + back button
   - Tab "Thong tin chung" hien thi info-grid
   - Tab "Van ban lien ket" hien thi bang + nut Them VB
   - Tab "Can bo xu ly" hien thi Transfer panel (tree trai, list phai)
   - Tab "Y kien xu ly" hien thi comment list + form them y kien
   - Tab "File dinh kem" co Upload.Dragger
   - Tab "HSCV con" hien thi bang + nut tao con
   - Toolbar buttons hien thi dung theo trang thai

4. **Workflow Designer** — Visit /quan-tri/quy-trinh
   - Danh sach quy trinh hien thi
   - Click "Thiet ke" chuyen sang trang designer
   - ReactFlow canvas hien thi nodes va edges
   - Keo tha node moi vao canvas
   - Tao connection giua nodes
   - Click node mo panel properties ben phai
   - Luu quy trinh hoat dong

5. **Bao cao** — Visit /ho-so-cong-viec/bao-cao
   - 6 KPI cards gradient hien thi so lieu
   - Bar chart va Pie chart hien thi
   - 3 tab bao cao chuyen doi duoc
   - Filter theo ngay hoat dong
   - Xuat Excel tai file .xlsx
  </action>
  <verify>Human confirms all checklist items pass</verify>
  <done>All 5 test sections verified by human. Phase 2 HSCV module complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed data | Test data only — no production secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-22 | Information Disclosure | Seed script | accept | Test data with no real PII — only for development |
</threat_model>

<verification>
- Seed script runs without errors
- All pages load without console errors
- Full HSCV workflow testable end-to-end
</verification>

<success_criteria>
Human verifies all 5 test sections pass. Phase 2 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-h-s-c-ng-vi-c/02-08-SUMMARY.md`
</output>
