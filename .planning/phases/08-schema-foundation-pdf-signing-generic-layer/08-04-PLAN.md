---
phase: 08-schema-foundation-pdf-signing-generic-layer
plan: 04
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - e_office_app_new/backend/src/services/signing/crypto.ts
  - e_office_app_new/backend/src/services/signing/crypto.test.ts
  - e_office_app_new/backend/src/repositories/signing-provider-config.repository.ts
  - e_office_app_new/backend/src/repositories/staff-signing-config.repository.ts
  - e_office_app_new/backend/src/repositories/sign-transaction.repository.ts
autonomous: true
requirements:
  - MIG-01
  - MIG-02
requirements_addressed:
  - MIG-01
  - MIG-02
must_haves:
  truths:
    - "Hàm `encryptSecret(plaintext: string): Promise<Buffer>` dùng pgp_sym_encrypt + key từ env SIGNING_SECRET_KEY, trả BYTEA"
    - "Hàm `decryptSecret(cipher: Buffer): Promise<string>` dùng pgp_sym_decrypt, trả plaintext"
    - "Encrypt rồi decrypt ra đúng plaintext ban đầu (roundtrip test pass)"
    - "Repository `signingProviderConfigRepository` có methods: list, getByCode, getActive, upsert, setActive (match 5 SPs Plan 01)"
    - "Repository `staffSigningConfigRepository` có methods: listByStaff, get, upsert, delete (match 4 SPs Plan 01)"
    - "Repository `signTransactionRepository` có methods: create, setProviderTxn, complete, updateStatus, incrementRetry, getById (match 6 SPs Plan 01)"
    - "Tất cả Row interfaces match exact column names từ SP RETURNS TABLE (snake_case)"
  artifacts:
    - path: "e_office_app_new/backend/src/services/signing/crypto.ts"
      provides: "encryptSecret / decryptSecret wrapping pgp_sym_encrypt/decrypt qua rawQuery"
      min_lines: 80
    - path: "e_office_app_new/backend/src/repositories/signing-provider-config.repository.ts"
      provides: "Repository cho public.signing_provider_config (5 methods)"
    - path: "e_office_app_new/backend/src/repositories/staff-signing-config.repository.ts"
      provides: "Repository cho public.staff_signing_config (4 methods)"
    - path: "e_office_app_new/backend/src/repositories/sign-transaction.repository.ts"
      provides: "Repository cho edoc.sign_transactions (6 methods)"
  key_links:
    - from: "repositories/*.repository.ts"
      to: "lib/db/query.ts"
      via: "callFunction / callFunctionOne"
      pattern: "import \\{ callFunction"
    - from: "services/signing/crypto.ts"
      to: "pgcrypto extension (pgp_sym_encrypt)"
      via: "rawQuery('SELECT pgp_sym_encrypt(...)')"
      pattern: "pgp_sym_encrypt"
---

<objective>
Tạo 2 thành phần backend thuần sẵn sàng cho Phase 9+:

1. **Crypto helper** (`services/signing/crypto.ts`): Wrapper tiện lợi quanh PostgreSQL `pgp_sym_encrypt`/`pgp_sym_decrypt` (pgcrypto đã enabled từ migration 000). Lấy key từ env `SIGNING_SECRET_KEY`. Dùng để encrypt `client_secret` trước khi lưu vào `signing_provider_config`.

2. **3 Repositories** gọi 15 SPs tạo ở Plan 01:
   - `signingProviderConfigRepository` — 5 methods (list, getByCode, getActive, upsert, setActive)
   - `staffSigningConfigRepository` — 4 methods (listByStaff, get, upsert, delete)
   - `signTransactionRepository` — 6 methods (create, setProviderTxn, complete, updateStatus, incrementRetry, getById)

**Why pgp_sym_encrypt qua SQL (không AES-256-GCM Node-side):**
- pgcrypto đã có sẵn, không thêm dependency
- Key rotation đơn giản (chỉ cần re-encrypt qua `UPDATE ... SET client_secret = pgp_sym_encrypt(pgp_sym_decrypt(client_secret, OLD_KEY), NEW_KEY)`)
- Backup/restore DB consistent (secret và data cùng 1 nơi)

**Why match SP RETURNS TABLE exact:** Tuân thủ CLAUDE.md Wave 2 rule — query `\d` bảng thật + đọc SP RETURNS TABLE để tên Row interface snake_case KHÔNG alias sang camelCase.

Output: 5 files backend TS (crypto + 3 repositories + 1 test file), tsc compile sạch, unit test encrypt roundtrip pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-schema-foundation-pdf-signing-generic-layer/08-01-PLAN.md
@e_office_app_new/backend/src/repositories/digital-signature.repository.ts
@e_office_app_new/backend/src/repositories/signer.repository.ts
@e_office_app_new/backend/src/lib/db/query.ts
@e_office_app_new/backend/src/repositories/department.repository.ts

<interfaces>
**lib/db/query.ts exports:**
```typescript
export async function callFunction<T extends QueryResultRow>(
  functionName: string,
  params: unknown[] = []
): Promise<T[]>;

export async function callFunctionOne<T extends QueryResultRow>(
  functionName: string,
  params: unknown[] = []
): Promise<T | null>;

export async function rawQuery<T extends QueryResultRow>(
  sql: string,
  params: unknown[] = []
): Promise<T[]>;
```

**Repository pattern (copy từ `digital-signature.repository.ts`):**
```typescript
import { callFunction, callFunctionOne } from '../lib/db/query.js';

export interface FooRow { id: number; name: string; /* match SP columns */ }

export const fooRepository = {
  async list(): Promise<FooRow[]> {
    return callFunction<FooRow>('schema.fn_foo_list', []);
  },
  async getById(id: number): Promise<FooRow | null> {
    return callFunctionOne<FooRow>('schema.fn_foo_get_by_id', [id]);
  },
};
```

**SPs từ Plan 01 — SIGNATURES CHÍNH XÁC (copy từ migration 040):**

```sql
-- signing_provider_config (public schema)
public.fn_signing_provider_config_list() RETURNS TABLE(
  id BIGINT, provider_code VARCHAR, provider_name VARCHAR, base_url VARCHAR,
  client_id VARCHAR, profile_id VARCHAR, extra_config JSONB,
  is_active BOOLEAN, last_tested_at TIMESTAMPTZ, test_result TEXT,
  created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ
)  -- NOTE: list() KHÔNG có client_secret (che plaintext từ UI)

public.fn_signing_provider_config_get_by_code(p_provider_code VARCHAR) RETURNS TABLE(
  id BIGINT, provider_code VARCHAR, provider_name VARCHAR, base_url VARCHAR,
  client_id VARCHAR, client_secret BYTEA, profile_id VARCHAR, extra_config JSONB,
  is_active BOOLEAN, last_tested_at TIMESTAMPTZ, test_result TEXT
)  -- Trả BYTEA cho backend decrypt

public.fn_signing_provider_config_get_active() RETURNS TABLE(
  id BIGINT, provider_code VARCHAR, provider_name VARCHAR, base_url VARCHAR,
  client_id VARCHAR, client_secret BYTEA, profile_id VARCHAR, extra_config JSONB
)

public.fn_signing_provider_config_upsert(
  p_provider_code VARCHAR, p_provider_name VARCHAR, p_base_url VARCHAR,
  p_client_id VARCHAR, p_client_secret BYTEA, p_profile_id VARCHAR,
  p_extra_config JSONB, p_last_tested_at TIMESTAMPTZ, p_test_result TEXT,
  p_updated_by INT
) RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT)

public.fn_signing_provider_config_set_active(
  p_provider_code VARCHAR, p_updated_by INT
) RETURNS TABLE(success BOOLEAN, message TEXT)

-- staff_signing_config (public schema)
public.fn_staff_signing_config_list_by_staff(p_staff_id INT) RETURNS TABLE(
  staff_id INT, provider_code VARCHAR, user_id VARCHAR, credential_id VARCHAR,
  certificate_subject VARCHAR, certificate_serial VARCHAR,
  is_verified BOOLEAN, last_verified_at TIMESTAMPTZ, last_error TEXT,
  created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ
)

public.fn_staff_signing_config_get(p_staff_id INT, p_provider_code VARCHAR) RETURNS TABLE(
  staff_id INT, provider_code VARCHAR, user_id VARCHAR, credential_id VARCHAR,
  certificate_data TEXT, certificate_subject VARCHAR, certificate_serial VARCHAR,
  is_verified BOOLEAN, last_verified_at TIMESTAMPTZ
)

public.fn_staff_signing_config_upsert(
  p_staff_id INT, p_provider_code VARCHAR, p_user_id VARCHAR, p_credential_id VARCHAR,
  p_certificate_data TEXT, p_certificate_subject VARCHAR, p_certificate_serial VARCHAR,
  p_is_verified BOOLEAN, p_last_verified_at TIMESTAMPTZ, p_last_error TEXT
) RETURNS TABLE(success BOOLEAN, message TEXT)

public.fn_staff_signing_config_delete(p_staff_id INT, p_provider_code VARCHAR) RETURNS TABLE(success BOOLEAN, message TEXT)

-- sign_transactions (edoc schema)
edoc.fn_sign_transaction_create(
  p_staff_id INT, p_provider_code VARCHAR, p_attachment_id BIGINT,
  p_attachment_type VARCHAR, p_doc_id BIGINT, p_doc_type VARCHAR,
  p_file_hash_sha256 VARCHAR
) RETURNS TABLE(success BOOLEAN, message TEXT, id BIGINT)

edoc.fn_sign_transaction_set_provider_txn(p_id BIGINT, p_provider_txn_id VARCHAR) RETURNS TABLE(success BOOLEAN, message TEXT)

edoc.fn_sign_transaction_complete(p_id BIGINT, p_signature_base64 TEXT, p_signed_file_path VARCHAR) RETURNS TABLE(success BOOLEAN, message TEXT)

edoc.fn_sign_transaction_update_status(p_id BIGINT, p_status VARCHAR, p_error_message TEXT) RETURNS TABLE(success BOOLEAN, message TEXT)

edoc.fn_sign_transaction_increment_retry(p_id BIGINT) RETURNS TABLE(success BOOLEAN, retry_count INT)

edoc.fn_sign_transaction_get_by_id(p_id BIGINT) RETURNS TABLE(
  id BIGINT, staff_id INT, provider_code VARCHAR, provider_txn_id VARCHAR,
  attachment_id BIGINT, attachment_type VARCHAR, doc_id BIGINT, doc_type VARCHAR,
  file_hash_sha256 VARCHAR, signature_base64 TEXT, signed_file_path VARCHAR,
  "status" VARCHAR, error_message TEXT, retry_count INT,
  created_at TIMESTAMPTZ, started_at TIMESTAMPTZ, completed_at TIMESTAMPTZ, expires_at TIMESTAMPTZ
)  -- NOTE: cột `status` quoted trong SP, nhưng Node pg driver trả key không có quote → TS field là `status` bình thường
```

**DbResult / DbResultWithId (cross-repo shared):**
```typescript
// e_office_app_new/backend/src/repositories/doc-book.repository.ts exports:
export interface DbResult { success: boolean; message: string; }
export interface DbResultWithId { success: boolean; message: string; id: number; }
```

**Existing env loading pattern:**
`backend/src/lib/db/pool.ts` dùng `process.env.DATABASE_URL`. Reuse same pattern.
</interfaces>

<datatypes_mapping>
**DB type → TypeScript type (CLAUDE.md checklist #8):**
- `BIGINT` → `number` (IDs OK cho < 2^53)
- `INT / INTEGER / SMALLINT` → `number`
- `VARCHAR / TEXT` → `string`
- `BOOLEAN` → `boolean`
- `TIMESTAMPTZ` → `string` (pg driver trả ISO string, không `Date`)
- `BYTEA` → `Buffer` (pg driver trả Buffer)
- `JSONB` → generic `unknown` hoặc strict interface
</datatypes_mapping>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crypto helper + unit test encrypt/decrypt roundtrip</name>
  <files>
    e_office_app_new/backend/src/services/signing/crypto.ts,
    e_office_app_new/backend/src/services/signing/crypto.test.ts
  </files>
  <read_first>
    - e_office_app_new/backend/src/lib/db/query.ts (export rawQuery + callFunctionOne)
    - e_office_app_new/backend/src/lib/db/pool.ts (pg pool config + env loading)
    - CLAUDE.md (Import Organization: .js suffix, error handling pattern)
    - e_office_app_new/database/migrations/000_full_schema.sql (line 34-37 pgcrypto enabled)
  </read_first>
  <behavior>
    **Test 1:** `encryptSecret('plain_text')` trả Buffer non-empty, length > 16 bytes.
    **Test 2:** `decryptSecret(encryptSecret('hello'))` === 'hello' (roundtrip).
    **Test 3:** 2 lần encrypt cùng plaintext trả khác nhau (pgp_sym_encrypt có random IV).
    **Test 4:** `decryptSecret(invalid_buffer)` throw Error với message tiếng Việt.
    **Test 5:** `encryptSecret('')` throw Error "plaintext rỗng".
    **Test 6:** Missing env `SIGNING_SECRET_KEY` throw Error rõ ràng.
  </behavior>
  <action>
**Tạo file `e_office_app_new/backend/src/services/signing/crypto.ts`:**

```typescript
/**
 * Crypto helper cho signing credentials.
 * Wrap PostgreSQL pgcrypto `pgp_sym_encrypt`/`pgp_sym_decrypt` qua rawQuery.
 *
 * Key management:
 * - Lấy từ env `SIGNING_SECRET_KEY` — deploy setup phải set key ngẫu nhiên >= 32 ký tự
 * - Rotate: chạy UPDATE ... SET client_secret = pgp_sym_encrypt(pgp_sym_decrypt(client_secret, OLD), NEW)
 *
 * Usage:
 *   const cipher = await encryptSecret('my_client_secret');   // Buffer
 *   const plain  = await decryptSecret(cipher);               // 'my_client_secret'
 */

import { rawQuery } from '../../lib/db/query.js';

interface EncryptRow {
  cipher: Buffer;
}

interface DecryptRow {
  plaintext: string;
}

/**
 * Lấy secret key từ env. Throw nếu chưa set (fail-fast — không default value).
 */
function getSecretKey(): string {
  const key = process.env.SIGNING_SECRET_KEY;
  if (!key || key.trim() === '') {
    throw new Error(
      'Env SIGNING_SECRET_KEY chưa được set — cấu hình trong .env trước khi start backend'
    );
  }
  if (key.length < 16) {
    throw new Error('SIGNING_SECRET_KEY quá ngắn (tối thiểu 16 ký tự, khuyến nghị 32+)');
  }
  return key;
}

/**
 * Encrypt plaintext string → BYTEA Buffer dùng pgp_sym_encrypt.
 *
 * @throws Error nếu plaintext rỗng hoặc env chưa set
 */
export async function encryptSecret(plaintext: string): Promise<Buffer> {
  if (typeof plaintext !== 'string' || plaintext === '') {
    throw new Error('Plaintext rỗng — không thể encrypt');
  }

  const key = getSecretKey();

  const rows = await rawQuery<EncryptRow>(
    'SELECT pgp_sym_encrypt($1::TEXT, $2::TEXT) AS cipher',
    [plaintext, key]
  );

  if (!rows[0] || !Buffer.isBuffer(rows[0].cipher)) {
    throw new Error('Encrypt thất bại — pgp_sym_encrypt không trả Buffer');
  }

  return rows[0].cipher;
}

/**
 * Decrypt BYTEA cipher → plaintext string dùng pgp_sym_decrypt.
 *
 * @throws Error nếu cipher invalid hoặc key sai
 */
export async function decryptSecret(cipher: Buffer): Promise<string> {
  if (!Buffer.isBuffer(cipher) || cipher.length === 0) {
    throw new Error('Cipher Buffer không hợp lệ (rỗng hoặc không phải Buffer)');
  }

  const key = getSecretKey();

  try {
    const rows = await rawQuery<DecryptRow>(
      'SELECT pgp_sym_decrypt($1::BYTEA, $2::TEXT) AS plaintext',
      [cipher, key]
    );

    if (!rows[0] || typeof rows[0].plaintext !== 'string') {
      throw new Error('Decrypt thất bại — pgp_sym_decrypt không trả plaintext');
    }

    return rows[0].plaintext;
  } catch (err: unknown) {
    const msg = err instanceof Error ? err.message : String(err);
    // pgp_sym_decrypt throw nếu cipher corrupt hoặc key sai
    throw new Error(`Không thể decrypt: ${msg}`);
  }
}

/**
 * Mask plaintext hiển thị UI — trả "***" hoặc "ab***yz" tùy length.
 * Không liên quan encrypt/decrypt, chỉ tiện utility dùng trong repository.
 */
export function maskSecret(plaintext: string): string {
  if (!plaintext || plaintext.length === 0) return '';
  if (plaintext.length <= 4) return '***';
  if (plaintext.length <= 8) return plaintext.slice(0, 2) + '***';
  return plaintext.slice(0, 2) + '***' + plaintext.slice(-2);
}
```

**Tạo test file `e_office_app_new/backend/src/services/signing/crypto.test.ts`:**

```typescript
/**
 * Integration test cho crypto.ts — yêu cầu qlvb_postgres đang chạy với pgcrypto extension.
 * Run: SIGNING_SECRET_KEY=test_key_min_16_chars_random_xyz npx tsx --test src/services/signing/crypto.test.ts
 */

import { describe, it, before } from 'node:test';
import assert from 'node:assert/strict';

// Set test env trước khi import module (encrypt/decrypt đọc env tại call time)
process.env.SIGNING_SECRET_KEY = process.env.SIGNING_SECRET_KEY || 'test_key_min_16_chars_random_xyz_aaa';

import { encryptSecret, decryptSecret, maskSecret } from './crypto.js';

describe('encryptSecret + decryptSecret roundtrip', () => {
  it('encrypt rồi decrypt ra plaintext gốc', async () => {
    const plain = 'my_super_secret_client_secret_2026';
    const cipher = await encryptSecret(plain);
    assert.ok(Buffer.isBuffer(cipher), 'encrypt phải trả Buffer');
    assert.ok(cipher.length > 16, 'cipher phải > 16 bytes');

    const decrypted = await decryptSecret(cipher);
    assert.equal(decrypted, plain, 'roundtrip phải ra plaintext gốc');
  });

  it('encrypt cùng plaintext 2 lần trả cipher khác nhau (random IV)', async () => {
    const plain = 'hello_world';
    const cipher1 = await encryptSecret(plain);
    const cipher2 = await encryptSecret(plain);
    assert.notDeepEqual(cipher1, cipher2, 'pgp_sym_encrypt phải random IV');
    // Nhưng decrypt cả 2 ra giống nhau
    const plain1 = await decryptSecret(cipher1);
    const plain2 = await decryptSecret(cipher2);
    assert.equal(plain1, plain);
    assert.equal(plain2, plain);
  });

  it('encrypt plaintext rỗng throw Error', async () => {
    await assert.rejects(() => encryptSecret(''), /Plaintext rỗng/);
  });

  it('decrypt Buffer rỗng throw Error', async () => {
    await assert.rejects(() => decryptSecret(Buffer.alloc(0)), /không hợp lệ/);
  });

  it('decrypt cipher corrupt throw Error', async () => {
    await assert.rejects(
      () => decryptSecret(Buffer.from('garbage_not_real_cipher_data_here')),
      /Không thể decrypt/
    );
  });
});

describe('maskSecret', () => {
  it('mask empty string', () => {
    assert.equal(maskSecret(''), '');
  });
  it('mask short string (<=4)', () => {
    assert.equal(maskSecret('abc'), '***');
    assert.equal(maskSecret('abcd'), '***');
  });
  it('mask medium string (<=8)', () => {
    assert.equal(maskSecret('abcdef'), 'ab***');
  });
  it('mask long string', () => {
    assert.equal(maskSecret('secret_2026'), 'se***26');
  });
});
```

**Chạy test:**
```bash
cd e_office_app_new/backend
SIGNING_SECRET_KEY=test_key_min_16_chars_random_xyz_aaa npx tsx --test src/services/signing/crypto.test.ts
```

(Test yêu cầu postgres container chạy + DATABASE_URL set trong `.env`.)
  </action>
  <verify>
    <automated>
      cd e_office_app_new/backend && test -f src/services/signing/crypto.ts && test -f src/services/signing/crypto.test.ts && grep -q "export async function encryptSecret" src/services/signing/crypto.ts && grep -q "export async function decryptSecret" src/services/signing/crypto.ts && grep -q "export function maskSecret" src/services/signing/crypto.ts && grep -q "pgp_sym_encrypt" src/services/signing/crypto.ts && grep -q "pgp_sym_decrypt" src/services/signing/crypto.ts && grep -q "SIGNING_SECRET_KEY" src/services/signing/crypto.ts && npx tsc --noEmit 2>&1 | tee /tmp/tsc3.log && ! grep -i "error TS" /tmp/tsc3.log && SIGNING_SECRET_KEY=test_key_min_16_chars_random_xyz_aaa npx tsx --test src/services/signing/crypto.test.ts 2>&1 | tee /tmp/crypto_test.log && grep -qE "pass [0-9]+" /tmp/crypto_test.log && ! grep -qE "^not ok|fail [1-9]" /tmp/crypto_test.log
    </automated>
  </verify>
  <acceptance_criteria>
    - File `e_office_app_new/backend/src/services/signing/crypto.ts` tồn tại (min 80 lines)
    - File export `encryptSecret`, `decryptSecret`, `maskSecret` (grep confirms)
    - File dùng `rawQuery` với `pgp_sym_encrypt($1::TEXT, $2::TEXT)` và `pgp_sym_decrypt($1::BYTEA, $2::TEXT)`
    - File đọc env `process.env.SIGNING_SECRET_KEY` + throw nếu unset/ngắn
    - `npx tsc --noEmit` không có `error TS`
    - Test chạy pass: roundtrip, random IV, error cases (6+ cases)
    - Test log chứa "pass" count >= 8 và KHÔNG "not ok"
  </acceptance_criteria>
  <done>Crypto helper sẵn sàng dùng cho Phase 9 (admin config upsert), unit test roundtrip PASS.</done>
</task>

<task type="auto">
  <name>Task 2: 3 Repositories gọi 15 SPs từ Plan 01</name>
  <files>
    e_office_app_new/backend/src/repositories/signing-provider-config.repository.ts,
    e_office_app_new/backend/src/repositories/staff-signing-config.repository.ts,
    e_office_app_new/backend/src/repositories/sign-transaction.repository.ts
  </files>
  <read_first>
    - e_office_app_new/backend/src/repositories/digital-signature.repository.ts (template pattern)
    - e_office_app_new/backend/src/repositories/doc-book.repository.ts (DbResult, DbResultWithId exports)
    - e_office_app_new/database/migrations/040_signing_schema.sql (từ Plan 01 — source of truth cho SP signatures)
    - e_office_app_new/backend/src/lib/db/query.ts
    - CLAUDE.md (Naming Patterns: kebab-case file, camelCase methods, Row suffix)
  </read_first>
  <action>
Tạo 3 file repository. **QUAN TRỌNG:** Row interfaces PHẢI match tên cột snake_case từ SP RETURNS TABLE — KHÔNG alias sang camelCase.

======

**File 1: `e_office_app_new/backend/src/repositories/signing-provider-config.repository.ts`**

```typescript
import { callFunction, callFunctionOne } from '../lib/db/query.js';
import type { DbResult, DbResultWithId } from './doc-book.repository.js';

// ============================================================
// Row interfaces — match SP RETURNS TABLE columns EXACTLY (snake_case)
// ============================================================

/** public.fn_signing_provider_config_list output (KHÔNG có client_secret) */
export interface SigningProviderConfigListRow {
  id: number;
  provider_code: string;
  provider_name: string;
  base_url: string;
  client_id: string;
  profile_id: string | null;
  extra_config: Record<string, unknown>;
  is_active: boolean;
  last_tested_at: string | null;
  test_result: string | null;
  created_at: string;
  updated_at: string;
}

/** public.fn_signing_provider_config_get_by_code output (có client_secret BYTEA) */
export interface SigningProviderConfigFullRow {
  id: number;
  provider_code: string;
  provider_name: string;
  base_url: string;
  client_id: string;
  client_secret: Buffer;
  profile_id: string | null;
  extra_config: Record<string, unknown>;
  is_active: boolean;
  last_tested_at: string | null;
  test_result: string | null;
}

/** public.fn_signing_provider_config_get_active output (subset) */
export interface SigningProviderConfigActiveRow {
  id: number;
  provider_code: string;
  provider_name: string;
  base_url: string;
  client_id: string;
  client_secret: Buffer;
  profile_id: string | null;
  extra_config: Record<string, unknown>;
}

// ============================================================
// Repository
// ============================================================

export const signingProviderConfigRepository = {

  /** List all provider configs (Admin UI). Không trả client_secret. */
  async list(): Promise<SigningProviderConfigListRow[]> {
    return callFunction<SigningProviderConfigListRow>(
      'public.fn_signing_provider_config_list',
      []
    );
  },

  /** Get config đầy đủ (bao gồm client_secret BYTEA) by provider_code. */
  async getByCode(providerCode: string): Promise<SigningProviderConfigFullRow | null> {
    return callFunctionOne<SigningProviderConfigFullRow>(
      'public.fn_signing_provider_config_get_by_code',
      [providerCode]
    );
  },

  /** Get provider đang active (0 hoặc 1 row). */
  async getActive(): Promise<SigningProviderConfigActiveRow | null> {
    return callFunctionOne<SigningProviderConfigActiveRow>(
      'public.fn_signing_provider_config_get_active',
      []
    );
  },

  /** Upsert config (client_secret PHẢI đã encrypt bằng services/signing/crypto.encryptSecret). */
  async upsert(params: {
    providerCode: string;
    providerName: string;
    baseUrl: string;
    clientId: string;
    clientSecret: Buffer;   // ENCRYPTED
    profileId: string | null;
    extraConfig: Record<string, unknown> | null;
    lastTestedAt: string | null;
    testResult: string | null;
    updatedBy: number;
  }): Promise<DbResultWithId> {
    const row = await callFunctionOne<DbResultWithId>(
      'public.fn_signing_provider_config_upsert',
      [
        params.providerCode,
        params.providerName,
        params.baseUrl,
        params.clientId,
        params.clientSecret,
        params.profileId,
        params.extraConfig ?? {},
        params.lastTestedAt,
        params.testResult,
        params.updatedBy,
      ]
    );
    return row ?? { success: false, message: 'Không thể lưu cấu hình provider', id: 0 };
  },

  /** Set provider active (atomic: deactivate tất cả rồi activate 1). */
  async setActive(providerCode: string, updatedBy: number): Promise<DbResult> {
    const row = await callFunctionOne<DbResult>(
      'public.fn_signing_provider_config_set_active',
      [providerCode, updatedBy]
    );
    return row ?? { success: false, message: 'Không thể kích hoạt provider' };
  },
};
```

======

**File 2: `e_office_app_new/backend/src/repositories/staff-signing-config.repository.ts`**

```typescript
import { callFunction, callFunctionOne } from '../lib/db/query.js';
import type { DbResult } from './doc-book.repository.js';

// ============================================================
// Row interfaces — match fn_staff_signing_config_* SPs
// ============================================================

/** public.fn_staff_signing_config_list_by_staff (KHÔNG có certificate_data vì list nhẹ) */
export interface StaffSigningConfigListRow {
  staff_id: number;
  provider_code: string;
  user_id: string;
  credential_id: string | null;
  certificate_subject: string | null;
  certificate_serial: string | null;
  is_verified: boolean;
  last_verified_at: string | null;
  last_error: string | null;
  created_at: string;
  updated_at: string;
}

/** public.fn_staff_signing_config_get (có certificate_data TEXT) */
export interface StaffSigningConfigFullRow {
  staff_id: number;
  provider_code: string;
  user_id: string;
  credential_id: string | null;
  certificate_data: string | null;
  certificate_subject: string | null;
  certificate_serial: string | null;
  is_verified: boolean;
  last_verified_at: string | null;
}

// ============================================================
// Repository
// ============================================================

export const staffSigningConfigRepository = {

  /** List tất cả config của 1 staff (có thể nhiều provider). */
  async listByStaff(staffId: number): Promise<StaffSigningConfigListRow[]> {
    return callFunction<StaffSigningConfigListRow>(
      'public.fn_staff_signing_config_list_by_staff',
      [staffId]
    );
  },

  /** Get config cụ thể 1 staff + 1 provider (cho sign flow). */
  async get(staffId: number, providerCode: string): Promise<StaffSigningConfigFullRow | null> {
    return callFunctionOne<StaffSigningConfigFullRow>(
      'public.fn_staff_signing_config_get',
      [staffId, providerCode]
    );
  },

  /** Upsert config user (gọi khi user save form hoặc verify). */
  async upsert(params: {
    staffId: number;
    providerCode: string;
    userId: string;
    credentialId: string | null;
    certificateData: string | null;
    certificateSubject: string | null;
    certificateSerial: string | null;
    isVerified: boolean;
    lastVerifiedAt: string | null;
    lastError: string | null;
  }): Promise<DbResult> {
    const row = await callFunctionOne<DbResult>(
      'public.fn_staff_signing_config_upsert',
      [
        params.staffId,
        params.providerCode,
        params.userId,
        params.credentialId,
        params.certificateData,
        params.certificateSubject,
        params.certificateSerial,
        params.isVerified,
        params.lastVerifiedAt,
        params.lastError,
      ]
    );
    return row ?? { success: false, message: 'Không thể lưu cấu hình cá nhân' };
  },

  /** Delete config (composite key staff_id + provider_code). */
  async delete(staffId: number, providerCode: string): Promise<DbResult> {
    const row = await callFunctionOne<DbResult>(
      'public.fn_staff_signing_config_delete',
      [staffId, providerCode]
    );
    return row ?? { success: false, message: 'Không tìm thấy cấu hình' };
  },
};
```

======

**File 3: `e_office_app_new/backend/src/repositories/sign-transaction.repository.ts`**

```typescript
import { callFunction, callFunctionOne } from '../lib/db/query.js';
import type { DbResult, DbResultWithId } from './doc-book.repository.js';

// ============================================================
// Row interfaces — match edoc.fn_sign_transaction_* SPs
// NOTE: SP dùng "status" (quoted) vì reserved word, nhưng pg driver
// trả về key `status` (không quote trong JSON) — TS field `status` OK
// ============================================================

/** edoc.fn_sign_transaction_get_by_id output (full context) */
export interface SignTransactionRow {
  id: number;
  staff_id: number;
  provider_code: string;
  provider_txn_id: string | null;
  attachment_id: number;
  attachment_type: string;   // 'incoming' | 'outgoing' | 'drafting'
  doc_id: number | null;
  doc_type: string | null;
  file_hash_sha256: string | null;
  signature_base64: string | null;
  signed_file_path: string | null;
  status: string;            // 'pending' | 'completed' | 'failed' | 'cancelled' | 'expired'
  error_message: string | null;
  retry_count: number;
  created_at: string;
  started_at: string | null;
  completed_at: string | null;
  expires_at: string | null;
}

/** edoc.fn_sign_transaction_increment_retry output */
export interface IncrementRetryRow {
  success: boolean;
  retry_count: number;
}

// ============================================================
// Repository
// ============================================================

export const signTransactionRepository = {

  /** Tạo transaction mới (status='pending', expires_at = now + 3 phút). */
  async create(params: {
    staffId: number;
    providerCode: string;
    attachmentId: number;
    attachmentType: 'incoming' | 'outgoing' | 'drafting';
    docId: number | null;
    docType: string | null;
    fileHashSha256: string | null;
  }): Promise<DbResultWithId> {
    const row = await callFunctionOne<DbResultWithId>(
      'edoc.fn_sign_transaction_create',
      [
        params.staffId,
        params.providerCode,
        params.attachmentId,
        params.attachmentType,
        params.docId,
        params.docType,
        params.fileHashSha256,
      ]
    );
    return row ?? { success: false, message: 'Không thể tạo giao dịch ký số', id: 0 };
  },

  /** Lưu provider_txn_id (sau khi call provider API thành công). */
  async setProviderTxn(id: number, providerTxnId: string): Promise<DbResult> {
    const row = await callFunctionOne<DbResult>(
      'edoc.fn_sign_transaction_set_provider_txn',
      [id, providerTxnId]
    );
    return row ?? { success: false, message: 'Không thể cập nhật provider_txn_id' };
  },

  /** Complete transaction (signature + signed file path). */
  async complete(id: number, signatureBase64: string, signedFilePath: string): Promise<DbResult> {
    const row = await callFunctionOne<DbResult>(
      'edoc.fn_sign_transaction_complete',
      [id, signatureBase64, signedFilePath]
    );
    return row ?? { success: false, message: 'Không thể hoàn tất giao dịch' };
  },

  /** Update status sang failed/expired/cancelled. */
  async updateStatus(
    id: number,
    status: 'failed' | 'expired' | 'cancelled',
    errorMessage: string | null
  ): Promise<DbResult> {
    const row = await callFunctionOne<DbResult>(
      'edoc.fn_sign_transaction_update_status',
      [id, status, errorMessage]
    );
    return row ?? { success: false, message: 'Không thể cập nhật trạng thái' };
  },

  /** Increment retry_count (worker mỗi lần poll thất bại). */
  async incrementRetry(id: number): Promise<IncrementRetryRow> {
    const row = await callFunctionOne<IncrementRetryRow>(
      'edoc.fn_sign_transaction_increment_retry',
      [id]
    );
    return row ?? { success: false, retry_count: 0 };
  },

  /** Get transaction full context by ID. */
  async getById(id: number): Promise<SignTransactionRow | null> {
    return callFunctionOne<SignTransactionRow>(
      'edoc.fn_sign_transaction_get_by_id',
      [id]
    );
  },
};
```

**Verify TypeScript:**
```bash
cd e_office_app_new/backend
npx tsc --noEmit
```
  </action>
  <verify>
    <automated>
      cd e_office_app_new/backend && test -f src/repositories/signing-provider-config.repository.ts && test -f src/repositories/staff-signing-config.repository.ts && test -f src/repositories/sign-transaction.repository.ts && grep -q "export const signingProviderConfigRepository" src/repositories/signing-provider-config.repository.ts && grep -q "fn_signing_provider_config_list" src/repositories/signing-provider-config.repository.ts && grep -q "fn_signing_provider_config_get_by_code" src/repositories/signing-provider-config.repository.ts && grep -q "fn_signing_provider_config_get_active" src/repositories/signing-provider-config.repository.ts && grep -q "fn_signing_provider_config_upsert" src/repositories/signing-provider-config.repository.ts && grep -q "fn_signing_provider_config_set_active" src/repositories/signing-provider-config.repository.ts && grep -q "export const staffSigningConfigRepository" src/repositories/staff-signing-config.repository.ts && grep -q "fn_staff_signing_config_list_by_staff" src/repositories/staff-signing-config.repository.ts && grep -q "fn_staff_signing_config_get" src/repositories/staff-signing-config.repository.ts && grep -q "fn_staff_signing_config_upsert" src/repositories/staff-signing-config.repository.ts && grep -q "fn_staff_signing_config_delete" src/repositories/staff-signing-config.repository.ts && grep -q "export const signTransactionRepository" src/repositories/sign-transaction.repository.ts && grep -q "fn_sign_transaction_create" src/repositories/sign-transaction.repository.ts && grep -q "fn_sign_transaction_set_provider_txn" src/repositories/sign-transaction.repository.ts && grep -q "fn_sign_transaction_complete" src/repositories/sign-transaction.repository.ts && grep -q "fn_sign_transaction_update_status" src/repositories/sign-transaction.repository.ts && grep -q "fn_sign_transaction_increment_retry" src/repositories/sign-transaction.repository.ts && grep -q "fn_sign_transaction_get_by_id" src/repositories/sign-transaction.repository.ts && npx tsc --noEmit 2>&1 | tee /tmp/tsc4.log && ! grep -i "error TS" /tmp/tsc4.log
    </automated>
  </verify>
  <acceptance_criteria>
    - 3 files tồn tại trong `e_office_app_new/backend/src/repositories/`
    - File 1 (signing-provider-config): exports `signingProviderConfigRepository`, chứa cả 5 SP names: `fn_signing_provider_config_list`, `_get_by_code`, `_get_active`, `_upsert`, `_set_active`
    - File 1: Row interfaces `SigningProviderConfigListRow`, `SigningProviderConfigFullRow`, `SigningProviderConfigActiveRow` — tên cột snake_case (provider_code, client_secret, is_active, last_tested_at)
    - File 1: `client_secret: Buffer` type (BYTEA mapping), KHÔNG string
    - File 2 (staff-signing-config): exports `staffSigningConfigRepository`, chứa cả 4 SP names: `fn_staff_signing_config_list_by_staff`, `_get`, `_upsert`, `_delete`
    - File 2: Row interfaces `StaffSigningConfigListRow`, `StaffSigningConfigFullRow` — cột snake_case (staff_id, provider_code, credential_id, is_verified, last_verified_at)
    - File 3 (sign-transaction): exports `signTransactionRepository`, chứa cả 6 SP names: `fn_sign_transaction_create`, `_set_provider_txn`, `_complete`, `_update_status`, `_increment_retry`, `_get_by_id`
    - File 3: Row interface `SignTransactionRow` chứa `status: string` (KHÔNG quote trong TS, vì pg driver parse key), `attachment_type`, `provider_txn_id`, `file_hash_sha256`, `retry_count`, `expires_at`
    - `npx tsc --noEmit` exit 0 không có `error TS`
    - Tất cả method signatures dùng camelCase params object (VD `providerCode`, `staffId`) để clean API khi consumer import
  </acceptance_criteria>
  <done>3 repositories ready cho Phase 9+ consume, TypeScript compile clean, tất cả 15 SP names present.</done>
</task>

</tasks>

<verification>
**Sau cả 2 tasks:**

1. Files: `ls src/services/signing/ && ls src/repositories/ | grep -E 'signing|sign-transaction'`
2. TypeScript: `cd e_office_app_new/backend && npx tsc --noEmit` → exit 0
3. Crypto test: `SIGNING_SECRET_KEY=test_key_min_16_chars_xyz_abc npx tsx --test src/services/signing/crypto.test.ts` → tất cả pass
4. Repository smoke test via psql:
   ```bash
   docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
     SELECT * FROM public.fn_signing_provider_config_list();
     SELECT * FROM public.fn_staff_signing_config_list_by_staff(1);
     SELECT * FROM edoc.fn_sign_transaction_create(1, 'SMARTCA_VNPT', 1, 'outgoing', NULL, NULL, 'test_hash');
   "
   ```
   → 3 queries chạy không lỗi (empty result sets OK).
</verification>

<success_criteria>
- [x] `crypto.ts` + `crypto.test.ts` implemented với encrypt/decrypt roundtrip
- [x] 3 repository files tạo với 15 methods (5+4+6)
- [x] Row interfaces match exact SP RETURNS TABLE column names (snake_case)
- [x] `client_secret: Buffer` mapping đúng (BYTEA)
- [x] `status: string` trong SignTransactionRow (pg driver parse quoted column OK)
- [x] Crypto test roundtrip PASS (>= 8 test cases)
- [x] `npx tsc --noEmit` exit 0
- [x] MIG-01 / MIG-02 đã có repository layer consume được từ Phase 9+
</success_criteria>

<output>
After completion, create `.planning/phases/08-schema-foundation-pdf-signing-generic-layer/08-04-SUMMARY.md` documenting:
- 5 files tạo với path
- Exported API: `encryptSecret`, `decryptSecret`, `maskSecret`, 3 repositories với 15 methods
- Env requirement: `SIGNING_SECRET_KEY` phải set >= 16 chars (cập nhật .env.example)
- Note cho Phase 9 (admin config): dùng `encryptSecret(clientSecret)` → `signingProviderConfigRepository.upsert({ clientSecret: ciphertext })`
</output>
