---
phase: 10-user-config-page
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/thong-tin-ca-nhan/page.tsx
autonomous: true
requirements:
  - UX-13
tags: [signing, ux-migration, thong-tin-ca-nhan, cleanup, wave-3]

must_haves:
  truths:
    - "Tab 'Chữ ký số' cũ trong /thong-tin-ca-nhan (dùng để cấu hình sign_phone SmartCA) bị REMOVE — chỉ còn tab 'Đổi mật khẩu' và tab mới 'Ảnh chữ ký' (hoặc không có tab nếu ảnh chữ ký di chuyển sang section khác)"
    - "User nào truy cập /thong-tin-ca-nhan không còn thấy field sign_phone (đã drop khỏi DB từ Phase 8 MIG-04)"
    - "Descriptions panel bên trái KHÔNG còn row 'Tài khoản ký số' (user.signPhone đã deprecated) — thay bằng pointer đến /ky-so/tai-khoan"
    - "Tab hoặc section Ảnh chữ ký VẪN TỒN TẠI (signImageUrl còn dùng cho PDF stamp — không được xóa)"
    - "Có Alert info trong page hoặc tab mới chỉ dẫn user: 'Thông tin chữ ký số đã chuyển sang menu Ký số → Tài khoản ký số cá nhân' với Link điều hướng"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/thong-tin-ca-nhan/page.tsx"
      provides: "Profile page without sign_phone field, with migration pointer Alert"
      contains: "ky-so/tai-khoan"
      excludes_patterns: ["sign_phone", "Tài khoản ký số \\(SmartCA\\)", "chu-ky-so"]
  key_links:
    - from: "thong-tin-ca-nhan/page.tsx Alert/Link"
      to: "/ky-so/tai-khoan"
      via: "Next.js Link or anchor tag"
      pattern: "ky-so/tai-khoan"
---

<objective>
Remove tab "Chữ ký số" cũ trong `/thong-tin-ca-nhan` (đang dùng `sign_phone` — cột đã bị DROP từ Phase 8 MIG-04 → broken). Giữ phần upload **Ảnh chữ ký** (signImageUrl còn cần cho PDF stamping). Thêm Alert info chỉ dẫn user sang `/ky-so/tai-khoan`.

**Purpose:**
- UX-13: Migrate tab chữ ký số cũ — xóa phần cấu hình provider (sign_phone), giữ phần ảnh chữ ký

**Output:**
- Trang `/thong-tin-ca-nhan` clean: chỉ còn "Đổi mật khẩu" + "Ảnh chữ ký" tabs (hoặc consolidate khác nếu hợp lý)
- Descriptions panel không còn row "Tài khoản ký số"
- Alert info pointer đến `/ky-so/tai-khoan`

**Không làm:**
- KHÔNG xóa endpoint backend `/api/ho-so-ca-nhan/anh-chu-ky` (vẫn cần cho upload ảnh)
- KHÔNG xóa endpoint `/api/ho-so-ca-nhan/chu-ky-so` ngay (có thể giữ lại để tránh break deployed backend — chỉ remove FE caller)
- KHÔNG đụng đến auth.store.ts `signPhone` field (deprecated nhưng không gây lỗi runtime)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@CLAUDE.md

# Reference
@e_office_app_new/frontend/src/app/(main)/thong-tin-ca-nhan/page.tsx
@e_office_app_new/frontend/src/stores/auth.store.ts

<current_state_analysis>
**Trang hiện tại (thong-tin-ca-nhan/page.tsx) có 2 tab:**
1. Tab "Đổi mật khẩu" (passwordPanel) — KEEP nguyên, không đụng
2. Tab "Chữ ký số" (signaturePanel) — CẦN SỬA:
   - Field "Tài khoản ký số (SmartCA)" → call PATCH /ho-so-ca-nhan/chu-ky-so với sign_phone → **BROKEN** (sign_phone column đã drop). REMOVE.
   - Field "Ảnh chữ ký (PNG)" upload → call POST /ho-so-ca-nhan/anh-chu-ky → **VẪN HOẠT ĐỘNG** (signImageUrl stored separately). KEEP.
   - Hiển thị "Chữ ký hiện tại" (user.signImageUrl) → KEEP.

**Descriptions panel (profile card bên trái) line 351-353 có:**
```tsx
<Descriptions.Item label={<><EditOutlined style={{ marginRight: 6 }} />Tài khoản ký số</>}>
  {user.signPhone || <span style={{ color: '#94a3b8' }}>Chưa cấu hình</span>}
</Descriptions.Item>
```
→ REMOVE hoàn toàn row này. signPhone bây giờ null/undefined vì DB column đã drop.

**Các state/function liên quan cần dọn:**
- `signatureFile` state — KEEP (vẫn cần cho upload ảnh)
- `savingSignature` state — KEEP
- `watchedSignPhone`, `hasChanges` (dùng cả signatureFile + sign_phone) — **RENAME thành `hasImageChange` và chỉ check signatureFile !== null**
- `signForm` Form — KEEP nhưng field sign_phone bị loại khỏi form items
- `handleSaveSignature` — SIMPLIFY: chỉ còn upload ảnh, bỏ phần PATCH /chu-ky-so sign_phone
- `useEffect` pre-fill sign_phone (line 37-41) — REMOVE

**Không có URL riêng cho tab "Chữ ký số":**
- URL của trang là `/thong-tin-ca-nhan` (không có query param khớp tab). Tab được quản lý qua `defaultActiveKey="password"` trong `<Tabs>`.
- Nghĩa là KHÔNG cần redirect URL riêng. Chỉ cần remove tab khỏi `<Tabs items=[]>`.
</current_state_analysis>

<design_decision>
**Option A — Giữ 2 tabs:** "Đổi mật khẩu" + "Ảnh chữ ký" (rename từ "Chữ ký số")
**Option B — Chỉ 1 tab, không còn Tabs component:** Gộp ảnh chữ ký vào main card, để Tabs chỉ có password

→ **Chọn Option A** vì:
- Giữ pattern quen thuộc (user từng biết tab "Chữ ký số")
- Ít thay đổi code hơn → ít risk
- Dễ add thêm tab trong tương lai

Tab "Chữ ký số" rename thành **"Ảnh chữ ký"** (rõ ràng hơn — chỉ upload ảnh để stamp PDF).

Alert pointer đến `/ky-so/tai-khoan` — đặt Ở ĐẦU tab "Ảnh chữ ký" (user mở tab sẽ thấy ngay):
```
[Info Alert]
Thông tin cấu hình tài khoản ký số (SmartCA/MySign) đã được chuyển sang menu
"Ký số → Tài khoản ký số cá nhân". [→ Đi đến trang]
```
</design_decision>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sửa thong-tin-ca-nhan/page.tsx — remove sign_phone, keep image upload, thêm Alert pointer</name>
  <files>e_office_app_new/frontend/src/app/(main)/thong-tin-ca-nhan/page.tsx</files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/thong-tin-ca-nhan/page.tsx (đọc ENTIRE file trước khi sửa — 397 lines)
    - e_office_app_new/frontend/src/stores/auth.store.ts (xác nhận signPhone vẫn còn trong UserInfo interface — không đụng file này)
  </read_first>
  <action>
Edit `e_office_app_new/frontend/src/app/(main)/thong-tin-ca-nhan/page.tsx` với 5 thay đổi cụ thể:

**Change 1 — Remove `watchedSignPhone` và `hasChanges` logic liên quan sign_phone (line 26-30):**

Replace:
```typescript
// Track sign_phone value để tính hasChanges (disable Save khi không có thay đổi)
const watchedSignPhone = Form.useWatch('sign_phone', signForm) ?? '';
const hasChanges =
  signatureFile !== null ||
  String(watchedSignPhone).trim() !== String(user?.signPhone || '').trim();
```

Bằng:
```typescript
// Chỉ track signatureFile (sign_phone đã deprecated — migrated sang /ky-so/tai-khoan)
const hasImageChange = signatureFile !== null;
```

**Change 2 — Remove useEffect pre-fill sign_phone (line 36-41):**

Xóa hoàn toàn khối:
```typescript
// Pre-fill sign_phone vào signForm khi user load
useEffect(() => {
  if (user) {
    signForm.setFieldsValue({ sign_phone: user.signPhone || '' });
  }
}, [user, signForm]);
```

**Change 3 — Simplify `handleSaveSignature` (line 86-129):**

Replace toàn bộ function bằng:
```typescript
const handleSaveSignature = async () => {
  if (!signatureFile) {
    message.warning('Vui lòng chọn ảnh chữ ký mới');
    return;
  }

  try {
    setSavingSignature(true);

    const fd = new FormData();
    fd.append('file', signatureFile);
    await api.post('/ho-so-ca-nhan/anh-chu-ky', fd, {
      headers: { 'Content-Type': 'multipart/form-data' },
    });

    message.success('Đã cập nhật ảnh chữ ký');
    await fetchMe(); // refresh store để cập nhật signImageUrl mới
    setSignatureFile(null);
  } catch (err: any) {
    if (err?.response) {
      message.error(err?.response?.data?.message || 'Lưu thất bại');
    } else {
      message.error('Có lỗi xảy ra, vui lòng thử lại');
    }
  } finally {
    setSavingSignature(false);
  }
};
```

**Change 4 — Replace `signaturePanel` (line 204-275) — remove sign_phone field, thêm Alert pointer, rename button:**

Replace toàn bộ `const signaturePanel = (...)` bằng:
```typescript
const signaturePanel = (
  <>
    <Alert
      type="info"
      showIcon
      style={{ marginBottom: 16, borderRadius: 8 }}
      message="Thông tin cấu hình ký số đã chuyển trang"
      description={
        <span>
          Cấu hình tài khoản ký số với nhà cung cấp (SmartCA VNPT / MySign Viettel) đã
          chuyển sang menu{' '}
          <Link href="/ky-so/tai-khoan" style={{ fontWeight: 600 }}>
            Ký số → Tài khoản ký số cá nhân
          </Link>
          . Trang này chỉ còn quản lý ảnh chữ ký để in trên PDF khi ký.
        </span>
      }
    />

    <p style={{ fontSize: 13, color: '#64748b', marginBottom: 20 }}>
      Tải lên ảnh chữ ký PNG để hệ thống chèn lên văn bản PDF khi ký số.
    </p>

    <Form form={signForm} layout="vertical" autoComplete="off">
      <Form.Item
        label="Ảnh chữ ký (PNG, khuyến nghị 150×150)"
        extra="Chỉ chấp nhận file PNG, kích thước tối đa 2MB."
      >
        <Upload {...uploadProps}>
          <Button icon={<UploadOutlined />}>Chọn file PNG</Button>
        </Upload>
      </Form.Item>

      {user.signImageUrl ? (
        <Form.Item label="Chữ ký hiện tại">
          <div style={{
            padding: 12,
            background: '#F9FAFB',
            border: '1px dashed #CBD5E1',
            borderRadius: 8,
            display: 'inline-block',
          }}>
            <Avatar
              shape="square"
              size={150}
              src={user.signImageUrl}
              alt="Ảnh chữ ký hiện tại"
              style={{ background: '#fff', objectFit: 'contain' }}
            />
          </div>
        </Form.Item>
      ) : (
        <Form.Item label="Chữ ký hiện tại">
          <span style={{ color: '#94a3b8', fontSize: 13 }}>Chưa cập nhật ảnh chữ ký</span>
        </Form.Item>
      )}

      <Space>
        <Button
          type="primary"
          icon={<EditOutlined />}
          loading={savingSignature}
          disabled={!hasImageChange}
          onClick={handleSaveSignature}
          style={{ borderRadius: 8, height: 40, fontWeight: 600 }}
        >
          Lưu ảnh chữ ký
        </Button>
      </Space>
    </Form>
  </>
);
```

**Change 5 — Remove "Tài khoản ký số" row trong Descriptions (line 351-353):**

Xóa khối:
```tsx
<Descriptions.Item label={<><EditOutlined style={{ marginRight: 6 }} />Tài khoản ký số</>}>
  {user.signPhone || <span style={{ color: '#94a3b8' }}>Chưa cập nhật</span>}
</Descriptions.Item>
```

**Change 6 — Update Tab label (line 380-386):**

Replace:
```tsx
{
  key: 'signature',
  label: (
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 6 }}>
      <EditOutlined />
      Chữ ký số
    </span>
  ),
  children: signaturePanel,
  forceRender: true,
},
```

Bằng:
```tsx
{
  key: 'signature',
  label: (
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 6 }}>
      <EditOutlined />
      Ảnh chữ ký
    </span>
  ),
  children: signaturePanel,
  forceRender: true,
},
```

**Change 7 — Update page description (line 283-285):**

Replace:
```tsx
<p className="page-description">
  Xem thông tin tài khoản, đổi mật khẩu và cấu hình chữ ký số
</p>
```

Bằng:
```tsx
<p className="page-description">
  Xem thông tin tài khoản, đổi mật khẩu và quản lý ảnh chữ ký
</p>
```

**Change 8 — Add `Link` import + `Alert` import (nếu chưa có):**

Check dòng import:
```typescript
import {
  Card, Row, Col, Descriptions, Avatar, Form, Input, Button, App, Tag, Tabs, Upload, Space,
} from 'antd';
```

Thêm `Alert` vào list:
```typescript
import {
  Card, Row, Col, Descriptions, Avatar, Form, Input, Button, App, Tag, Tabs, Upload, Space, Alert,
} from 'antd';
```

Thêm import Link (Next.js):
```typescript
import Link from 'next/link';
```

**Change 9 — Clean up unused imports:**

Sau các change trên, có thể có imports không còn dùng:
- `Input` — có thể vẫn dùng trong passwordPanel (`Input.Password`) → KEEP
- `Tag` — check xem còn dùng trong page không; nếu không → remove
- `Form.useWatch` — không còn dùng (Change 1 đã xóa) — remove nếu không xuất hiện elsewhere

Chạy `npx tsc --noEmit` để xác nhận không có TS errors về unused imports (TS strict mode sẽ báo).

**Summary of final page structure:**
- Left card (Descriptions profile) — KHÔNG còn row "Tài khoản ký số"
- Right card Tabs:
  - Tab 1 "Đổi mật khẩu" — NOT TOUCHED
  - Tab 2 "Ảnh chữ ký" (renamed từ "Chữ ký số") — chứa Alert pointer + upload ảnh + display current image
- Page description cập nhật
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "thong-tin-ca-nhan" | grep -q "^0$"</automated>
    Additional checks:
    - `grep -c "sign_phone" e_office_app_new/frontend/src/app/\(main\)/thong-tin-ca-nhan/page.tsx` returns 0
    - `grep -c "chu-ky-so" e_office_app_new/frontend/src/app/\(main\)/thong-tin-ca-nhan/page.tsx` returns 0 (only `anh-chu-ky` remains)
    - `grep -c "anh-chu-ky" e_office_app_new/frontend/src/app/\(main\)/thong-tin-ca-nhan/page.tsx` returns 1 (upload endpoint vẫn gọi)
    - `grep -c "ky-so/tai-khoan" e_office_app_new/frontend/src/app/\(main\)/thong-tin-ca-nhan/page.tsx` returns 1 (Link pointer)
    - `grep -c "Tài khoản ký số (SmartCA)" e_office_app_new/frontend/src/app/\(main\)/thong-tin-ca-nhan/page.tsx` returns 0 (old field label removed)
    - `grep "Ảnh chữ ký" e_office_app_new/frontend/src/app/\(main\)/thong-tin-ca-nhan/page.tsx` returns ≥ 2 lines (tab label + heading)
    - `grep "from 'next/link'" e_office_app_new/frontend/src/app/\(main\)/thong-tin-ca-nhan/page.tsx` returns 1
  </verify>
  <done>
    - File compiles (0 new TS errors)
    - sign_phone / chu-ky-so references REMOVED khỏi page.tsx (verified via grep=0)
    - anh-chu-ky endpoint VẪN còn (upload ảnh still works)
    - Alert pointer `/ky-so/tai-khoan` hiện diện (grep=1)
    - Tab "Ảnh chữ ký" (renamed từ "Chữ ký số")
    - Descriptions panel không còn row "Tài khoản ký số"
    - handleSaveSignature simplified: chỉ upload ảnh
    - UI test manual (khi dev server chạy): vào /thong-tin-ca-nhan → thấy Alert pointer, upload ảnh vẫn hoạt động, password tab nguyên vẹn
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Không có trust boundary mới — plan này chỉ dọn dẹp UI. Endpoint `/api/ho-so-ca-nhan/anh-chu-ky` đã có guard từ trước.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Information Disclosure | Legacy field user.signPhone exposed in auth.store | accept | signPhone luôn null (DB column dropped Phase 8); field giữ trong store chỉ để không break existing imports. Planned full cleanup sau Phase 14 (deployment). |
| T-10-11 | Broken functionality | User bookmark URL cũ | accept | Không có URL riêng cho tab "Chữ ký số" (tab state internal). User vẫn đến /thong-tin-ca-nhan OK, thấy Alert pointer đến /ky-so/tai-khoan. |
</threat_model>

<verification>
- Compile clean
- sign_phone refs removed (grep=0)
- Alert pointer exists with Link to /ky-so/tai-khoan
- Upload ảnh chữ ký flow still works
- Descriptions panel không còn hiển thị signPhone null/undefined
</verification>

<success_criteria>
1. Tab "Chữ ký số" cũ đã REMOVED (rename thành "Ảnh chữ ký", field sign_phone biến mất)
2. Upload ảnh chữ ký VẪN hoạt động (signImageUrl preserve cho PDF stamping)
3. Descriptions panel không còn row "Tài khoản ký số" (user.signPhone deprecated)
4. Alert info pointer đến `/ky-so/tai-khoan` với Next.js Link — user click được sang trang mới
5. Page description cập nhật (không còn ghi "cấu hình chữ ký số")
6. Zero TypeScript errors
7. UX-13 requirement delivered — tab cũ broken đã cleanup, user được điều hướng đến trang Phase 10 mới
</success_criteria>

<output>
After completion, create `.planning/phases/10-user-config-page/10-03-SUMMARY.md`.
</output>
