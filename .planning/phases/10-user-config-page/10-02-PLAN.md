---
phase: 10-user-config-page
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - e_office_app_new/frontend/src/app/(main)/ky-so/tai-khoan/page.tsx
  - e_office_app_new/frontend/src/components/layout/MainLayout.tsx
autonomous: false
requirements:
  - CFG-05
  - CFG-06
tags: [signing, user-config, frontend, antd, sidebar, wave-2, ui]

must_haves:
  truths:
    - "User thấy submenu 'Tài khoản ký số cá nhân' dưới group 'KÝ SỐ' trong sidebar (hiển thị cho MỌI user, không phân quyền admin)"
    - "User truy cập /ky-so/tai-khoan thấy card 'Provider đang hoạt động' với tên provider + base_url (hoặc empty state nếu Admin chưa kích hoạt)"
    - "Khi provider active là SmartCA VNPT: form hiển thị 1 field user_id (số ĐT/CCCD). Khi là MySign Viettel: form hiển thị 2 field user_id + Select credential_id + button 'Tải danh sách CTS'"
    - "User bấm 'Tải danh sách CTS' (MySign only) → call POST /certificates với user_id → Select options cập nhật"
    - "User bấm 'Lưu cấu hình' → POST / → toast success + badge 'Chưa xác thực' (vàng)"
    - "User bấm 'Kiểm tra kết nối' → POST /verify → toast success + badge 'Đã xác thực' (xanh) + hiển thị certificate_subject + cert_valid_to"
  artifacts:
    - path: "e_office_app_new/frontend/src/app/(main)/ky-so/tai-khoan/page.tsx"
      provides: "User signing config page with dynamic form"
      min_lines: 300
    - path: "e_office_app_new/frontend/src/components/layout/MainLayout.tsx"
      provides: "Sidebar submenu 'Tài khoản ký số cá nhân' visible to ALL users"
      contains: "/ky-so/tai-khoan"
  key_links:
    - from: "page.tsx"
      to: "/api/ky-so/tai-khoan (4 endpoints)"
      via: "api.get/post from lib/api.ts"
      pattern: "api\\.(get|post).*ky-so/tai-khoan"
    - from: "MainLayout.tsx ── group KÝ SỐ"
      to: "/ky-so/tai-khoan"
      via: "Menu item added to KÝ SỐ group (visible for ALL users)"
      pattern: "tai-khoan.*label.*cá nhân"
---

<objective>
Tạo trang `/ky-so/tai-khoan` — mỗi user tự cấu hình tài khoản ký số cá nhân theo provider đang active, và thêm submenu vào sidebar group "KÝ SỐ" cho MỌI user (không chỉ admin).

**Purpose:**
- CFG-05: Form dynamic theo provider active (SmartCA: 1 field; MySign: 2 field + Tải CTS)
- CFG-06: Button "Kiểm tra" verify config qua provider, hiển thị badge "Đã xác thực"

**Output:**
- `frontend/src/app/(main)/ky-so/tai-khoan/page.tsx` — user config page
- `frontend/src/components/layout/MainLayout.tsx` — thêm submenu "Tài khoản ký số cá nhân" vào group KÝ SỐ (visible cho mọi user; group KÝ SỐ hiện tại chỉ admin thấy → cần MỞ RỘNG sang cho mọi user)
- Update breadcrumbMap thêm `/ky-so/tai-khoan`: 'Tài khoản ký số cá nhân'
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@CLAUDE.md

# Reference files
@e_office_app_new/frontend/src/app/(main)/ky-so/cau-hinh/page.tsx
@e_office_app_new/frontend/src/components/layout/MainLayout.tsx
@e_office_app_new/frontend/src/stores/auth.store.ts
@e_office_app_new/frontend/src/lib/api.ts
@.planning/phases/10-user-config-page/10-01-PLAN.md

<backend_api_shape>
<!-- Plan 10-01 vừa tạo 4 endpoint — shape response đã fix, FE CHỈ consume -->

// GET /api/ky-so/tai-khoan
interface GetResponse {
  success: true;
  data: {
    active: {
      provider_code: 'SMARTCA_VNPT' | 'MYSIGN_VIETTEL';
      provider_name: string;
      base_url: string;
    } | null;
    config: {
      staff_id: number;
      provider_code: string;
      user_id: string;
      credential_id: string | null;
      certificate_subject: string | null;
      certificate_serial: string | null;
      is_verified: boolean;
      last_verified_at: string | null;
    } | null;
    message?: string;  // có khi active=null
  };
}

// POST /api/ky-so/tai-khoan
// Body: { user_id: string, credential_id?: string | null }
// Response 201: { success: true, message: string }
// Response 400: { success: false, message: string }

// POST /api/ky-so/tai-khoan/certificates
// Body: { user_id: string }
// Response 200: { success: true, data: { certificates: ClientCert[] } }
interface ClientCert {
  credential_id: string;
  subject: string;
  serial_number: string;
  valid_from: string;
  valid_to: string;
  status: string;
}

// POST /api/ky-so/tai-khoan/verify
// Body: không có hoặc {}
// Response 200 verified: { success: true, data: { verified: true, certificate_subject, cert_valid_to, last_verified_at } }
// Response 200 not verified: { success: true, data: { verified: false, message } }
// Response 502: { success: false, message }
</backend_api_shape>

<existing_sidebar_pattern>
<!-- Từ MainLayout.tsx line 278-288 hiện tại -->

// ── KÝ SỐ ── (Admin only — Plan 10 sẽ thêm menu user-level)
if (isAdmin) {
  items.push(
    { key: 'grp-kyso', type: 'group', label: 'KÝ SỐ' },
    {
      key: '/ky-so/cau-hinh',
      icon: <SafetyCertificateOutlined />,
      label: 'Cấu hình ký số hệ thống',
    },
  );
}

// Comment "Plan 10 sẽ thêm menu user-level" — LẦN NÀY ĐẾN LÚC ĐỔI!
// Yêu cầu: group KÝ SỐ PHẢI hiển thị cho MỌI user, không chỉ admin.
// Submenu "Cấu hình ký số hệ thống" CHỈ admin thấy (giữ nguyên guard).
// Submenu mới "Tài khoản ký số cá nhân" HIỂN THỊ cho MỌI user.
</existing_sidebar_pattern>

<form_design_spec>
**Layout page (top → bottom):**

1. **Page header:**
   - Title: "Tài khoản ký số cá nhân" (SafetyCertificateOutlined icon)
   - Description: "Cấu hình tài khoản ký số của bạn theo nhà cung cấp mà hệ thống đang sử dụng."

2. **Card 1 — Provider Info (alert-style):**
   - Nếu `data.active === null` (Admin chưa kích hoạt): Alert type="warning" với title "Hệ thống chưa kích hoạt provider ký số" + description "Vui lòng liên hệ Quản trị viên để cấu hình và kích hoạt provider." — phần form bên dưới HIDDEN.
   - Nếu có active: Alert type="info" title "Provider đang hoạt động: {provider_name}" + description "Base URL: {base_url}"

3. **Card 2 — Form cấu hình** (hiển thị khi có active):
   - Input `user_id` — bắt buộc. Label + Tooltip động theo provider:
     - SmartCA: Tooltip = "Số CMND/CCCD hoặc Mã định danh đã đăng ký với VNPT SmartCA"; placeholder "Ví dụ: 012345678901"
     - MySign: Tooltip = "Mã định danh cá nhân đã đăng ký với Viettel MySign"; placeholder "Ví dụ: CMT_123456"
     - `maxLength={200}` (match DB `VARCHAR(200)` trong staff_signing_config)
     - required rule với message tiếng Việt
   - **Chỉ khi MySign Viettel:**
     - Button "Tải danh sách CTS từ MySign" bên dưới user_id — icon `ReloadOutlined`, disabled nếu user_id trống
     - Bấm → call POST /certificates với body.user_id → set state `certificates` (array) → populate Select options
     - Select `credential_id` — bắt buộc. Options hiển thị: "{subject} (hết hạn: {valid_to_vi})". Disabled nếu `certificates.length === 0`
     - Nếu `certificates.length > 0` và user đã chọn → show small card bên dưới với chi tiết cert (subject, serial, valid_from → valid_to)
   - Badge trạng thái verified (trên cùng của form):
     - `config.is_verified === true`: Tag xanh "Đã xác thực" + tooltip show cert_subject
     - `config.is_verified === false || config === null`: Tag vàng "Chưa xác thực"
     - Nếu đã verified: hiển thị thêm dòng nhỏ "Kiểm tra gần nhất: {last_verified_at format vi-VN}"

4. **Action buttons (bottom của Card 2):**
   - "Lưu cấu hình" (primary, ghost=false, icon SaveOutlined) — call POST /
   - "Kiểm tra kết nối" (primary, icon CheckCircleOutlined) — call POST /verify. Disabled nếu chưa có config lưu (config===null).

**Dynamic behavior:**
- Load: useEffect → fetch GET /api/ky-so/tai-khoan → populate form state + certificates (nếu MySign và có credential_id → auto-fetch list để hiển thị subject của cert đã chọn).
- Sau save thành công: re-fetch GET → form reset về state mới, badge "Chưa xác thực" (backend auto-reset is_verified=false trên mỗi upsert).
- Sau verify thành công: re-fetch GET → form hiển thị "Đã xác thực" + cert info.
- Sau verify failed (response.data.verified === false): toast warning với data.message, KHÔNG re-fetch (state không đổi).

**Empty state when active===null:**
- Hide toàn bộ Card 2 form
- Alert type="warning" + hướng dẫn "Liên hệ Quản trị viên" + button "Làm mới" (refetch)

**Validation & error mapping (setBackendFieldError pattern):**
- 400 "Vui lòng nhập user_id" → inline `user_id` field error
- 400 "Vui lòng chọn chứng thư số..." → inline `credential_id` field error
- 400 "Admin chưa kích hoạt..." → toast warning (không inline)
- Khác → toast error
</form_design_spec>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tạo trang /ky-so/tai-khoan page.tsx</name>
  <files>e_office_app_new/frontend/src/app/(main)/ky-so/tai-khoan/page.tsx</files>
  <read_first>
    - e_office_app_new/frontend/src/app/(main)/ky-so/cau-hinh/page.tsx (reference pattern — Form, setBackendFieldError, Alert, Badge/Tag styling)
    - e_office_app_new/frontend/src/stores/auth.store.ts (user info shape)
    - e_office_app_new/frontend/src/lib/api.ts (axios shared instance)
    - .planning/phases/10-user-config-page/10-01-PLAN.md (backend API shape đã fix)
  </read_first>
  <action>
Tạo file `e_office_app_new/frontend/src/app/(main)/ky-so/tai-khoan/page.tsx` với các thành phần:

**Component structure:**

```tsx
'use client';

/**
 * Trang: /ky-so/tai-khoan — Tài khoản ký số cá nhân (mọi user)
 *
 * Mỗi user tự cấu hình user_id (+ credential_id cho MySign) theo provider đang
 * active — form dynamic theo provider_code. Button "Kiểm tra kết nối" verify
 * config qua provider listCertificates và cập nhật badge "Đã xác thực".
 *
 * API (Plan 10-01):
 *   GET    /ky-so/tai-khoan              → { active, config }
 *   POST   /ky-so/tai-khoan              → upsert config
 *   POST   /ky-so/tai-khoan/certificates → list cert cho MySign "Tải danh sách CTS"
 *   POST   /ky-so/tai-khoan/verify       → verify + lưu cert snapshot
 */

import React, { useState, useEffect, useCallback } from 'react';
import {
  Card, Button, Input, Form, Alert, Tag, Space, App, Empty, Skeleton,
  Select, Tooltip, Descriptions,
} from 'antd';
import {
  SafetyCertificateOutlined, CheckCircleOutlined, WarningOutlined,
  ReloadOutlined, SaveOutlined, QuestionCircleOutlined,
  ExclamationCircleOutlined,
} from '@ant-design/icons';
import { api } from '@/lib/api';
import { useAuthStore } from '@/stores/auth.store';

// ============================================================================
// Types — match Plan 10-01 API shape
// ============================================================================

type ProviderCode = 'SMARTCA_VNPT' | 'MYSIGN_VIETTEL';

interface ActiveProvider {
  provider_code: ProviderCode;
  provider_name: string;
  base_url: string;
}

interface UserConfig {
  staff_id: number;
  provider_code: string;
  user_id: string;
  credential_id: string | null;
  certificate_subject: string | null;
  certificate_serial: string | null;
  is_verified: boolean;
  last_verified_at: string | null;
}

interface ClientCert {
  credential_id: string;
  subject: string;
  serial_number: string;
  valid_from: string;
  valid_to: string;
  status: string;
}

interface FormValues {
  user_id: string;
  credential_id?: string;
}

// ============================================================================
// Helpers
// ============================================================================

function formatDateTime(iso: string | null): string {
  if (!iso) return '—';
  try {
    const d = new Date(iso);
    if (Number.isNaN(d.getTime())) return '—';
    return d.toLocaleString('vi-VN', {
      day: '2-digit', month: '2-digit', year: 'numeric',
      hour: '2-digit', minute: '2-digit',
    });
  } catch { return '—'; }
}

function formatDate(iso: string): string {
  try {
    return new Date(iso).toLocaleDateString('vi-VN');
  } catch { return iso; }
}

// Provider-specific UI metadata
const PROVIDER_UX: Record<ProviderCode, {
  userIdLabel: string; userIdTooltip: string; userIdPlaceholder: string;
  needsCertList: boolean;
}> = {
  SMARTCA_VNPT: {
    userIdLabel: 'Mã định danh SmartCA',
    userIdTooltip: 'Số CMND/CCCD hoặc Mã định danh đã đăng ký với VNPT SmartCA',
    userIdPlaceholder: 'Ví dụ: 012345678901',
    needsCertList: false,
  },
  MYSIGN_VIETTEL: {
    userIdLabel: 'Mã định danh MySign',
    userIdTooltip: 'Mã định danh cá nhân đã đăng ký với Viettel MySign (do Viettel cấp)',
    userIdPlaceholder: 'Ví dụ: CMT_123456',
    needsCertList: true,
  },
};

// ============================================================================
// Component
// ============================================================================

export default function KySoTaiKhoanPage() {
  const { message } = App.useApp();
  const user = useAuthStore((s) => s.user);
  const [form] = Form.useForm<FormValues>();

  const [loading, setLoading] = useState(true);
  const [active, setActive] = useState<ActiveProvider | null>(null);
  const [config, setConfig] = useState<UserConfig | null>(null);
  const [activeMessage, setActiveMessage] = useState<string | null>(null);

  // Certificates (MySign only)
  const [certificates, setCertificates] = useState<ClientCert[]>([]);
  const [loadingCerts, setLoadingCerts] = useState(false);

  const [saving, setSaving] = useState(false);
  const [verifying, setVerifying] = useState(false);

  // ──────────────────────────────────────────────────────────────────────────
  // Data fetching
  // ──────────────────────────────────────────────────────────────────────────

  const fetchConfig = useCallback(async () => {
    setLoading(true);
    try {
      const { data: res } = await api.get<{
        success: boolean;
        data: {
          active: ActiveProvider | null;
          config: UserConfig | null;
          message?: string;
        };
      }>('/ky-so/tai-khoan');
      setActive(res.data?.active ?? null);
      setConfig(res.data?.config ?? null);
      setActiveMessage(res.data?.message ?? null);

      if (res.data?.config) {
        form.setFieldsValue({
          user_id: res.data.config.user_id,
          credential_id: res.data.config.credential_id ?? undefined,
        });
      } else {
        form.resetFields();
      }
    } catch (err) {
      const msg = (err as { response?: { data?: { message?: string } } })
        ?.response?.data?.message ?? 'Không tải được cấu hình';
      message.error(msg);
    } finally {
      setLoading(false);
    }
  }, [form, message]);

  useEffect(() => {
    if (user) fetchConfig();
  }, [user, fetchConfig]);

  // ──────────────────────────────────────────────────────────────────────────
  // Fetch certificates (MySign — "Tải danh sách CTS")
  // ──────────────────────────────────────────────────────────────────────────

  const onLoadCertificates = async () => {
    try {
      const userId = (form.getFieldValue('user_id') ?? '').trim();
      if (!userId) {
        form.setFields([{ name: 'user_id', errors: ['Vui lòng nhập Mã định danh trước'] }]);
        return;
      }
      setLoadingCerts(true);
      const { data: res } = await api.post<{
        success: boolean;
        data: { certificates: ClientCert[] };
      }>('/ky-so/tai-khoan/certificates', { user_id: userId });
      const certs = res.data?.certificates ?? [];
      setCertificates(certs);
      if (certs.length === 0) {
        message.warning('Không tìm thấy chứng thư nào cho mã định danh này');
      } else {
        message.success(`Đã tải ${certs.length} chứng thư số`);
      }
    } catch (err) {
      const msg = (err as { response?: { data?: { message?: string } } })
        ?.response?.data?.message ?? 'Không tải được danh sách chứng thư';
      message.error(msg);
      setCertificates([]);
    } finally {
      setLoadingCerts(false);
    }
  };

  // ──────────────────────────────────────────────────────────────────────────
  // Save & Verify
  // ──────────────────────────────────────────────────────────────────────────

  const setBackendFieldError = (msg: string): boolean => {
    const map: Record<string, keyof FormValues> = {
      'Vui lòng nhập user_id': 'user_id',
    };
    if (msg.includes('chứng thư số')) {
      form.setFields([{ name: 'credential_id', errors: [msg] }]);
      return true;
    }
    const field = map[msg];
    if (field) {
      form.setFields([{ name: field, errors: [msg] }]);
      return true;
    }
    return false;
  };

  const onSave = async () => {
    if (!active) return;
    try {
      const values = await form.validateFields();
      setSaving(true);
      await api.post('/ky-so/tai-khoan', {
        user_id: values.user_id.trim(),
        credential_id: values.credential_id?.trim() || null,
      });
      message.success('Lưu cấu hình thành công. Vui lòng bấm "Kiểm tra kết nối" để xác thực.');
      await fetchConfig();
    } catch (err) {
      const httpErr = err as {
        response?: { data?: { message?: string } };
        errorFields?: unknown[];
      };
      if (httpErr?.errorFields) return;
      const msg = httpErr?.response?.data?.message ?? 'Lưu cấu hình thất bại';
      if (!setBackendFieldError(msg)) message.error(msg);
    } finally {
      setSaving(false);
    }
  };

  const onVerify = async () => {
    if (!active || !config) {
      message.warning('Vui lòng lưu cấu hình trước khi kiểm tra');
      return;
    }
    try {
      setVerifying(true);
      const { data: res } = await api.post<{
        success: boolean;
        data: {
          verified: boolean;
          message?: string;
          certificate_subject?: string;
          cert_valid_to?: string;
          last_verified_at?: string;
        };
      }>('/ky-so/tai-khoan/verify');
      if (res.data?.verified) {
        message.success('Kiểm tra thành công — chứng thư hợp lệ');
      } else {
        message.warning(res.data?.message ?? 'Kiểm tra thất bại');
      }
      await fetchConfig();
    } catch (err) {
      const msg = (err as { response?: { data?: { message?: string } } })
        ?.response?.data?.message ?? 'Không kết nối được provider';
      message.error(msg);
    } finally {
      setVerifying(false);
    }
  };

  // ──────────────────────────────────────────────────────────────────────────
  // Guards
  // ──────────────────────────────────────────────────────────────────────────

  if (!user) {
    return <Skeleton active paragraph={{ rows: 6 }} />;
  }

  // ──────────────────────────────────────────────────────────────────────────
  // Render
  // ──────────────────────────────────────────────────────────────────────────

  const providerMeta = active ? PROVIDER_UX[active.provider_code] : null;

  return (
    <div>
      {/* Page Header */}
      <div className="page-header">
        <h2 className="page-title">
          <SafetyCertificateOutlined style={{ color: '#0891B2' }} />
          Tài khoản ký số cá nhân
        </h2>
        <p className="page-description">
          Cấu hình tài khoản ký số của bạn theo nhà cung cấp dịch vụ mà hệ thống đang sử dụng.
        </p>
      </div>

      {/* Provider Info / Empty State */}
      {loading ? (
        <Card className="page-card" variant="borderless">
          <Skeleton active paragraph={{ rows: 4 }} />
        </Card>
      ) : !active ? (
        <Card className="page-card" variant="borderless">
          <Alert
            type="warning"
            showIcon
            icon={<ExclamationCircleOutlined />}
            style={{ borderRadius: 12 }}
            title="Hệ thống chưa kích hoạt provider ký số"
            description={
              <Space direction="vertical" size={8}>
                <span>
                  {activeMessage ??
                    'Vui lòng liên hệ Quản trị viên để cấu hình và kích hoạt nhà cung cấp dịch vụ ký số.'}
                </span>
                <Button
                  icon={<ReloadOutlined />}
                  size="small"
                  onClick={fetchConfig}
                >
                  Làm mới
                </Button>
              </Space>
            }
          />
        </Card>
      ) : (
        <>
          {/* Card 1 — Active Provider Info */}
          <Alert
            type="info"
            showIcon
            style={{ marginBottom: 16, borderRadius: 12 }}
            title={
              <div style={{ display: 'flex', alignItems: 'center',
                justifyContent: 'space-between', flexWrap: 'wrap', gap: 8 }}>
                <span>
                  <strong>Provider đang hoạt động:</strong> {active.provider_name}
                  <span style={{ marginLeft: 8, color: '#64748B', fontSize: 12,
                    fontFamily: 'monospace' }}>
                    ({active.base_url})
                  </span>
                </span>
                <Button icon={<ReloadOutlined />} size="small" onClick={fetchConfig}>
                  Làm mới
                </Button>
              </div>
            }
          />

          {/* Card 2 — Config Form */}
          <Card className="page-card" variant="borderless"
            title={
              <Space>
                <span>Cấu hình tài khoản</span>
                {config?.is_verified ? (
                  <Tooltip title={config.certificate_subject ?? ''}>
                    <Tag color="success" icon={<CheckCircleOutlined />}>
                      Đã xác thực
                    </Tag>
                  </Tooltip>
                ) : (
                  <Tag color="warning" icon={<WarningOutlined />}>
                    Chưa xác thực
                  </Tag>
                )}
              </Space>
            }
            extra={
              config?.is_verified && config.last_verified_at && (
                <span style={{ fontSize: 12, color: '#64748B' }}>
                  Kiểm tra gần nhất: {formatDateTime(config.last_verified_at)}
                </span>
              )
            }
          >
            <Form form={form} layout="vertical" autoComplete="off"
              validateTrigger="onSubmit">
              <Form.Item
                name="user_id"
                label={
                  <span>
                    {providerMeta?.userIdLabel ?? 'Mã định danh'}{' '}
                    <Tooltip title={providerMeta?.userIdTooltip ?? ''}>
                      <QuestionCircleOutlined style={{ color: '#94A3B8', fontSize: 13 }} />
                    </Tooltip>
                  </span>
                }
                rules={[
                  { required: true,
                    message: `Vui lòng nhập ${providerMeta?.userIdLabel ?? 'mã định danh'}` },
                  { max: 200, message: 'Tối đa 200 ký tự' },
                ]}
              >
                <Input
                  maxLength={200}
                  placeholder={providerMeta?.userIdPlaceholder ?? ''}
                  allowClear
                />
              </Form.Item>

              {providerMeta?.needsCertList && (
                <>
                  <Form.Item>
                    <Button
                      icon={<ReloadOutlined />}
                      onClick={onLoadCertificates}
                      loading={loadingCerts}
                    >
                      Tải danh sách chứng thư từ MySign
                    </Button>
                    <span style={{ marginLeft: 12, fontSize: 12, color: '#94A3B8' }}>
                      Nhập Mã định danh trước, sau đó bấm để lấy danh sách chứng thư.
                    </span>
                  </Form.Item>

                  <Form.Item
                    name="credential_id"
                    label={
                      <span>
                        Chứng thư số{' '}
                        <Tooltip title="Chọn một trong các chứng thư số đã tải về từ MySign">
                          <QuestionCircleOutlined style={{ color: '#94A3B8', fontSize: 13 }} />
                        </Tooltip>
                      </span>
                    }
                    rules={[{ required: true, message: 'Vui lòng chọn chứng thư số' }]}
                  >
                    <Select
                      placeholder={certificates.length === 0
                        ? 'Bấm "Tải danh sách chứng thư" để chọn'
                        : 'Chọn chứng thư số'}
                      disabled={certificates.length === 0}
                      options={certificates.map((c) => ({
                        value: c.credential_id,
                        label: `${c.subject} (hết hạn: ${formatDate(c.valid_to)})`,
                      }))}
                      allowClear
                    />
                  </Form.Item>
                </>
              )}

              {/* Cert info if verified */}
              {config?.is_verified && config.certificate_subject && (
                <Descriptions column={1} size="small" bordered
                  style={{ marginBottom: 16 }}
                  labelStyle={{ width: 160, background: '#F8FAFC', color: '#64748B' }}>
                  <Descriptions.Item label="Chủ thể chứng thư">
                    <span style={{ fontFamily: 'monospace', fontSize: 12 }}>
                      {config.certificate_subject}
                    </span>
                  </Descriptions.Item>
                  {config.certificate_serial && (
                    <Descriptions.Item label="Số serial">
                      <span style={{ fontFamily: 'monospace', fontSize: 12 }}>
                        {config.certificate_serial}
                      </span>
                    </Descriptions.Item>
                  )}
                </Descriptions>
              )}

              {/* Actions */}
              <Space>
                <Button
                  type="primary"
                  icon={<SaveOutlined />}
                  loading={saving}
                  onClick={onSave}
                >
                  Lưu cấu hình
                </Button>
                <Button
                  icon={<CheckCircleOutlined />}
                  loading={verifying}
                  disabled={!config}
                  onClick={onVerify}
                >
                  Kiểm tra kết nối
                </Button>
              </Space>
            </Form>
          </Card>
        </>
      )}
    </div>
  );
}
```

**Ghi chú triển khai:**
- Sử dụng `App.useApp()` để lấy `message` (KHÔNG dùng `notification` global — theo project convention)
- CSS classes `.page-header`, `.page-title`, `.page-card` đã có sẵn trong `globals.css`
- AntD 6: `Alert` dùng `title` thay vì `message` (deprecated). `Drawer` không cần ở đây (không có add/edit drawer).
- `Form.useWatch` có thể dùng để re-render certificates clear khi user đổi user_id — optional enhancement; hiện tại chỉ reload khi user bấm "Tải danh sách"
- Pattern setBackendFieldError — map backend Vietnamese msg → inline field error
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "ky-so/tai-khoan" | grep -q "^0$"</automated>
    Additional checks:
    - `ls e_office_app_new/frontend/src/app/\(main\)/ky-so/tai-khoan/page.tsx` — file exists
    - `grep -c "api\.post\|api\.get" e_office_app_new/frontend/src/app/\(main\)/ky-so/tai-khoan/page.tsx` returns ≥ 4 (1 GET + 3 POST)
    - `grep "'use client'" e_office_app_new/frontend/src/app/\(main\)/ky-so/tai-khoan/page.tsx` returns 1 line
    - `grep "SMARTCA_VNPT\|MYSIGN_VIETTEL" e_office_app_new/frontend/src/app/\(main\)/ky-so/tai-khoan/page.tsx` returns ≥ 2 lines
  </verify>
  <done>
    - File tồn tại với ≥ 300 dòng
    - TypeScript không lỗi mới
    - Page render đúng 2 case: (1) active=null → empty state, (2) active=SmartCA/MySign → form dynamic
    - Badge verified state thay đổi theo `config.is_verified`
    - All text tiếng Việt có dấu
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Thêm submenu sidebar + breadcrumb cho /ky-so/tai-khoan</name>
  <files>e_office_app_new/frontend/src/components/layout/MainLayout.tsx</files>
  <read_first>
    - e_office_app_new/frontend/src/components/layout/MainLayout.tsx (section line 278-288 — group KÝ SỐ hiện tại + breadcrumbMap line 375-376)
  </read_first>
  <action>
Edit `e_office_app_new/frontend/src/components/layout/MainLayout.tsx`:

**Change 1 — Mở rộng group KÝ SỐ cho MỌI user** (hiện tại line 278-288):

Replace khối này:
```typescript
// ── KÝ SỐ ── (Admin only — Plan 10 sẽ thêm menu user-level)
if (isAdmin) {
  items.push(
    { key: 'grp-kyso', type: 'group', label: 'KÝ SỐ' },
    {
      key: '/ky-so/cau-hinh',
      icon: <SafetyCertificateOutlined />,
      label: 'Cấu hình ký số hệ thống',
    },
  );
}
```

Bằng:
```typescript
// ── KÝ SỐ ── (Group hiển thị cho MỌI user; submenu admin-only được guard riêng)
items.push(
  { key: 'grp-kyso', type: 'group', label: 'KÝ SỐ' },
);
// Submenu admin: Cấu hình ký số hệ thống
if (isAdmin) {
  items.push({
    key: '/ky-so/cau-hinh',
    icon: <SafetyCertificateOutlined />,
    label: 'Cấu hình ký số hệ thống',
  });
}
// Submenu cho MỌI user: Tài khoản ký số cá nhân
items.push({
  key: '/ky-so/tai-khoan',
  icon: <SafetyCertificateOutlined />,
  label: 'Tài khoản ký số cá nhân',
});
```

**Change 2 — Thêm breadcrumb entry** (line 375-376):

Replace:
```typescript
'/ky-so': 'Ký số',
'/ky-so/cau-hinh': 'Cấu hình ký số hệ thống',
```

Bằng:
```typescript
'/ky-so': 'Ký số',
'/ky-so/cau-hinh': 'Cấu hình ký số hệ thống',
'/ky-so/tai-khoan': 'Tài khoản ký số cá nhân',
```

**LƯU Ý:**
- Group header `{ type: 'group', label: 'KÝ SỐ' }` giờ hiển thị cho MỌI user (trước chỉ admin). Điều này OK vì user nào cũng cần thấy menu "Tài khoản ký số cá nhân".
- Icon `SafetyCertificateOutlined` đã có import sẵn từ trước (Plan 9).
- `filterMenuItems` ở cuối function có thể có logic filter theo permission — kiểm tra; nếu cần whitelist `/ky-so/tai-khoan` cho mọi user thì thêm vào whitelist (nếu có).

**Sau khi edit, kiểm tra 'SafetyCertificateOutlined' đã được import:**
```bash
grep "SafetyCertificateOutlined" e_office_app_new/frontend/src/components/layout/MainLayout.tsx | head -1
```
Nếu chưa có → thêm vào import block từ `@ant-design/icons`.
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "MainLayout" | grep -q "^0$"</automated>
    Additional checks:
    - `grep -n "'/ky-so/tai-khoan'" e_office_app_new/frontend/src/components/layout/MainLayout.tsx` returns 2 lines (menu item + breadcrumbMap)
    - `grep -n "Tài khoản ký số cá nhân" e_office_app_new/frontend/src/components/layout/MainLayout.tsx` returns ≥ 2 lines (label + breadcrumb)
    - `grep -n "grp-kyso" e_office_app_new/frontend/src/components/layout/MainLayout.tsx` — group push NOT inside `if (isAdmin)` block anymore
  </verify>
  <done>
    - Group "KÝ SỐ" hiển thị cho mọi user (pushed unconditionally)
    - Submenu "Cấu hình ký số hệ thống" vẫn chỉ admin thấy (wrapped in `if (isAdmin)`)
    - Submenu "Tài khoản ký số cá nhân" hiển thị cho mọi user (pushed unconditionally)
    - Breadcrumb map có entry `/ky-so/tai-khoan`
    - Zero new TypeScript errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — UI trang /ky-so/tai-khoan chạy đúng 2 case</name>
  <what-built>
    Backend endpoints (Plan 10-01): 4 user-level API `/api/ky-so/tai-khoan*`.
    Frontend page (Task 1): `/ky-so/tai-khoan` với form dynamic theo provider active.
    Sidebar submenu (Task 2): "Tài khoản ký số cá nhân" hiển thị cho mọi user.
  </what-built>
  <how-to-verify>
**Bước 1 — Start dev servers:**
```bash
cd e_office_app_new/backend && npm run dev   # port 4000
cd e_office_app_new/frontend && npm run dev  # port 3000
```

**Bước 2 — Kiểm tra khi Admin chưa kích hoạt provider:**
1. Login bằng tài khoản **admin**, vào `/ky-so/cau-hinh`. Nếu đang có provider active → bấm kích hoạt provider khác trước? KHÔNG — đơn giản hơn: tạm thời ngưng active bằng SQL:
   ```sql
   UPDATE public.signing_provider_config SET is_active = false;
   ```
2. Truy cập `/ky-so/tai-khoan`:
   - [ ] Thấy Alert vàng "Hệ thống chưa kích hoạt provider ký số"
   - [ ] Có button "Làm mới"
   - [ ] KHÔNG thấy form cấu hình
3. Restore:
   ```sql
   UPDATE public.signing_provider_config SET is_active = true WHERE provider_code = 'SMARTCA_VNPT';
   ```
4. Refresh trang → thấy form xuất hiện

**Bước 3 — Sidebar hiển thị đúng cho 2 role:**
1. Login bằng **user thường** (không admin):
   - [ ] Sidebar có group "KÝ SỐ"
   - [ ] Chỉ thấy submenu "Tài khoản ký số cá nhân"
   - [ ] KHÔNG thấy "Cấu hình ký số hệ thống"
2. Login bằng **admin**:
   - [ ] Sidebar group "KÝ SỐ" có CẢ 2 submenu

**Bước 4 — Form SmartCA VNPT (1 field):**
1. Set provider active = SMARTCA_VNPT (qua /ky-so/cau-hinh)
2. Login user thường, vào `/ky-so/tai-khoan`:
   - [ ] Thấy Alert info "Provider đang hoạt động: SmartCA VNPT"
   - [ ] Card "Cấu hình tài khoản" có 1 field `Mã định danh SmartCA`
   - [ ] KHÔNG có button "Tải danh sách CTS"
   - [ ] KHÔNG có Select credential
3. Nhập user_id (VD: `0123456789`), bấm "Lưu cấu hình":
   - [ ] Toast success "Lưu cấu hình thành công..."
   - [ ] Badge chuyển từ không có → "Chưa xác thực" (vàng)
4. Bấm "Kiểm tra kết nối":
   - [ ] Có thể lỗi network (credentials SmartCA dev có thể hết hạn) — toast error acceptable
   - [ ] Nếu lỗi → badge vẫn "Chưa xác thực"
   - [ ] Nếu thành công → badge "Đã xác thực" (xanh) + hiển thị cert info

**Bước 5 — Form MySign Viettel (2 field + Tải CTS):**
1. Set active = MYSIGN_VIETTEL (cần provider này đã config credentials — nếu chưa → skip bước này và note)
2. Login user thường, vào `/ky-so/tai-khoan`:
   - [ ] Form có 2 field: `Mã định danh MySign` + button "Tải danh sách chứng thư"
   - [ ] Select `Chứng thư số` disabled
3. Nhập user_id, bấm "Tải danh sách chứng thư":
   - [ ] Loading spinner
   - [ ] Nếu MySign dev không có credentials → toast error acceptable
   - [ ] Nếu có credentials và user_id valid → Select có options
4. Chọn cert, bấm "Lưu" → toast success
5. Bấm "Kiểm tra kết nối" → verify + badge "Đã xác thực"

**Bước 6 — Backend log không leak secret:**
- Xem backend logs khi test → KHÔNG có `client_secret` plaintext hoặc `access_token` literal trong log
  </how-to-verify>
  <resume-signal>
    Type "approved" nếu:
    - Cả 2 case (active=null / active có) render đúng
    - Sidebar hiển thị đúng cho user thường vs admin
    - Form SmartCA 1 field, form MySign 2 field + Tải CTS button
    - Save + Verify flow work (có thể Verify fail nếu dev không có credentials thật — KHÔNG phải blocker)

    Nếu có lỗi UI/logic bug → describe issues, Claude sẽ fix.
  </resume-signal>
  <files>(checkpoint — không sửa file, chỉ human verify)</files>
  <action>
    Đây là checkpoint human-verify — executor PAUSE và trình bày với user 3 điều cần kiểm chứng:
    1. UI render đúng 2 case (active=null → empty state; active=SmartCA/MySign → form dynamic)
    2. Sidebar hiển thị đúng theo role (user thường chỉ thấy 'Tài khoản cá nhân'; admin thấy cả 2 submenu)
    3. Save + Verify flow hoạt động end-to-end

    Xem chi tiết các bước trong &lt;how-to-verify&gt; block.
  </action>
  <verify>
    Human verification theo checklist trong &lt;how-to-verify&gt;. Không có automated check.
  </verify>
  <done>User trả lời 'approved' hoặc mô tả lỗi cần fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Frontend | User input vào Form (user_id, credential_id select) |
| Frontend → Backend API | JSON body qua api.post, token auto-attached |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Information Disclosure | Certificate data exposure | mitigate | Backend đã không trả `certificate_data` (Plan 10-01); FE chỉ hiển thị subject/serial từ response |
| T-10-08 | Tampering | User A xem config của user B | mitigate | Backend lấy staffId từ JWT (Plan 10-01 T-10-01); FE không truyền staffId trong body |
| T-10-09 | Spoofing (UI confusion) | User mistake Admin's provider | accept | UI hiển thị rõ "Provider đang hoạt động: {name}" ở top — user thấy rõ |
</threat_model>

<verification>
- TypeScript clean
- Sidebar submenu visible cho mọi user
- Form dynamic theo provider_code active
- Save → badge "Chưa xác thực"
- Verify OK → badge "Đã xác thực" + cert info
- Empty state khi active=null
</verification>

<success_criteria>
1. Trang `/ky-so/tai-khoan` render đúng theo provider active (SmartCA: 1 field; MySign: 2 field + Tải CTS button)
2. Sidebar group "KÝ SỐ" hiển thị cho MỌI user; submenu "Cấu hình hệ thống" chỉ admin, submenu "Tài khoản cá nhân" cho mọi user
3. Empty state khi active===null (Alert vàng + Làm mới button)
4. Save flow: POST /ky-so/tai-khoan → toast success → badge chuyển "Chưa xác thực"
5. Verify flow: POST /ky-so/tai-khoan/verify → badge "Đã xác thực" + hiển thị certificate_subject + cert_valid_to
6. Load CTS flow (MySign): POST /ky-so/tai-khoan/certificates → Select options cập nhật
7. Breadcrumb map có `/ky-so/tai-khoan`
8. Inline field error khi backend trả validation error (setBackendFieldError pattern)
9. Human-verify checkpoint passed
10. CFG-05 + CFG-06 delivered từ user perspective (observable truths)
</success_criteria>

<output>
After completion, create `.planning/phases/10-user-config-page/10-02-SUMMARY.md`.
</output>
