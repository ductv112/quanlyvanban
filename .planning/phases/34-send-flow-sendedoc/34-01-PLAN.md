---
phase: 34-send-flow-sendedoc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/backend/src/services/lgsp-real.service.ts
  - e_office_app_new/backend/src/services/lgsp/error-codes.ts
  - e_office_app_new/backend/src/services/lgsp/edxml-builder.ts
  - e_office_app_new/backend/package.json
autonomous: true
requirements:
  - LGSP-SEND-01
  - LGSP-SEND-02
  - LGSP-SEND-04
  - LGSP-SEND-05
tags: [backend, service, lgsp, edxml, multipart, error-mapping]

must_haves:
  truths:
    - "LGSPRealService.sendDocument() POST đúng `{baseUrl}/v1/sendEdoc` multipart/form-data với `edocFile` (Buffer) + `messageType=edoc` text field"
    - "Authentication qua HTTP headers `X-SystemId` + `X-SecretKey` (KHÔNG dùng login token / OAuth2 flow cho document API)"
    - "Response parse được 2 shape: `{success, message, docId}` hoặc `{success, message, data: {docId, status, errorCode, errorDesc}}`"
    - "Error map 9 mã LGSP (0/10/15/18/19/20/21/22/23) → Vietnamese message"
    - "LgspSendError class extends Error có `code: string` + `vietnameseMessage: string` cho structured throw"
    - "edxml-builder.ts export `buildEdxml(input)` trả về `{buffer, docId, destOrgCode, docCode}` đúng spec QĐ 28 với 9 thành phần MessageHeader (From, To, Code, PromulgationInfo, DocumentType, Subject, SignerInfo, OtherInfo, DocumentId, TraceHeaderList)"
    - "Builder attachment: tải file qua MinIO `getObject`, encode base64, embed trong `<Manifest><Attachment><Content>base64</Content><FileName/><FileType/></Attachment></Manifest>`"
    - "Optional/null field fallback `N/A` (string) hoặc `0` (number) với pino warn — không reject"
    - "TypeScript compile pass cho 3 file mới"
  artifacts:
    - path: "e_office_app_new/backend/src/services/lgsp-real.service.ts"
      provides: "Fixed sendDocument() multipart + headers (signature: sendDocument(edxmlBuffer: Buffer, destOrgCode: string, docCode: string))"
      contains: "form.append('edocFile'"
    - path: "e_office_app_new/backend/src/services/lgsp/error-codes.ts"
      provides: "LGSP_ERROR_CODES map + LgspSendError class + mapLgspError() helper"
      min_lines: 60
    - path: "e_office_app_new/backend/src/services/lgsp/edxml-builder.ts"
      provides: "buildEdxml(input) helper produce edXML envelope from outgoing_doc + recipient + attachments"
      min_lines: 200
    - path: "e_office_app_new/backend/package.json"
      provides: "Add xmlbuilder2 + form-data deps"
      contains: "xmlbuilder2"
  key_links:
    - from: "LGSPRealService.sendDocument(buffer, destOrgCode, docCode)"
      to: "POST {baseUrl}/v1/sendEdoc multipart/form-data"
      via: "form-data lib FormData + headers X-SystemId/X-SecretKey"
      pattern: "form.append + fetch + parse SendEdocResponse"
    - from: "buildEdxml(input)"
      to: "xmlbuilder2 create() + .ele() chain → .end({prettyPrint: false})"
      via: "MessageHeader from outgoing_docs + sender lgsp_org_code + dest from inter_organizations.code"
      pattern: "QĐ 28 envelope"
    - from: "mapLgspError(errorCode)"
      to: "LGSP_ERROR_CODES[code] → Vietnamese message"
      via: "Lookup table 9 codes, fallback 'Lỗi không xác định (Code X)'"
      pattern: "structured error mapping (D-12)"
---

<objective>
Fix `LGSPRealService.sendDocument()` để dùng đúng LGSP API real (theo Postman collection authoritative): `POST {baseUrl}/v1/sendEdoc` multipart/form-data với `edocFile` (Buffer) + `messageType=edoc`, authentication qua HTTP headers `X-SystemId` + `X-SecretKey`. Tạo 2 module mới `services/lgsp/error-codes.ts` (9 mã LGSP → Vietnamese message + LgspSendError class) + `services/lgsp/edxml-builder.ts` (build edXML envelope đúng spec QĐ 28 với MessageHeader đầy đủ + attachment base64 trong Manifest).

**Purpose:** Phase 18 v3.0 sendDocument() hiện dùng endpoint sai (`/api/lgspedoc/send-edoc`) và body sai (JSON với `edocContent`). Postman collection chính thức từ tỉnh Lạng Sơn xác định endpoint là `/v1/sendEdoc` multipart. Phase 34 phải fix trước khi worker Phase 34-02 consume.

**Output:** 3 file source + 2 npm deps mới, sẵn sàng cho Plan 34-02 worker consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-send-flow-sendedoc/34-CONTEXT.md
@.planning/phases/33-database-core-infrastructure/33-05-SUMMARY.md
@CLAUDE.md
@e_office_app_new/backend/src/services/lgsp-real.service.ts
@e_office_app_new/backend/src/services/lgsp.service.ts
@e_office_app_new/backend/src/lib/minio/client.ts
@e_office_app_new/backend/src/lib/logger.ts
@docs/Trục EDOC Lạng Sơn - QLVB Doanh nghiệp/LTVB_API_TRUC_PROD_TICHHOP.postman_collection.json

<read_first>
- `e_office_app_new/backend/src/services/lgsp-real.service.ts` (271 dòng, sendDocument line 161-184 — BROKEN, cần fix)
- `e_office_app_new/backend/src/services/lgsp.service.ts` (factory exposing getLgspService)
- `docs/Trục EDOC Lạng Sơn - QLVB Doanh nghiệp/LTVB_API_TRUC_PROD_TICHHOP.postman_collection.json` line 235-279 — AUTHORITATIVE sendEdoc shape
- `docs/Trục EDOC Lạng Sơn - QLVB Doanh nghiệp/HuongDanKetNoiLienThongVB_v2.2.pdf` (46 trang HDSD v2.2 — edXML envelope spec section 3)
- `e_office_app_new/backend/src/lib/minio/client.ts` — `getObject` method usage
- `e_office_app_new/backend/package.json` — dependencies list (verify xmlbuilder2 + form-data CHƯA có)
</read_first>

<interfaces>
**LGSP Postman sendEdoc spec (AUTHORITATIVE):**
```
POST {baseUrl}/v1/sendEdoc
Headers:
  X-SystemId: H37.DN.001
  X-SecretKey: O5UMG/19k+wvwM0PV1dckYANhSoW80JYifgn05ZvGc8=
Body: multipart/form-data
  edocFile: <binary file *.edxml> (Content-Type: application/xml)
  messageType: edoc
```

**Response shape (2 variants):**
```json
{ "success": true, "message": "OK", "docId": "e14ab3ab-b6f6-488e-8d17-15ab5f3abec8" }
// HOẶC:
{ "success": false, "message": "Loi", "data": { "docId": null, "status": "error", "errorCode": "18", "errorDesc": "Don vi nhan khong ton tai" } }
```

**Existing LGSPRealService signature (PHẢI giữ để Plan 34-02 worker gọi):**
```typescript
// BEFORE Phase 34:
async sendDocument(edxmlContent: string, destOrgCode: string): Promise<LgspSendResult>;
// AFTER Phase 34 — đổi signature:
async sendDocument(edxmlBuffer: Buffer, destOrgCode: string, docCode: string): Promise<LgspSendResult>;
//   edxmlBuffer: Buffer = edXML bytes (built bởi edxml-builder)
//   destOrgCode: string = code đơn vị nhận (VD 'H37.DN.002', cho log)
//   docCode: string = mã VB (VD '123/UBND-VP', dùng đặt filename trong multipart)
```

**ILgspService interface (lgsp.service.ts line 37-42) — CẦN UPDATE để match signature mới:**
```typescript
export interface ILgspService {
  getToken(): Promise<string>;
  receiveDocuments(): Promise<LgspReceivedDoc[]>;
  sendDocument(edxmlBuffer: Buffer, destOrgCode: string, docCode: string): Promise<LgspSendResult>;
  syncOrganizations(): Promise<LgspOrganization[]>;
}
```

**LgspSendResult shape (giữ nguyên — Plan 34-02 consume):**
```typescript
export interface LgspSendResult {
  success: boolean;
  lgsp_doc_id: string;   // docId trả về từ LGSP (empty string nếu fail)
  message: string;       // raw message từ LGSP (Vietnamese mapping ở error-codes.ts)
  errorCode?: string;    // mới Phase 34 — mã 0/10/15/18/19/20/21/22/23
}
```

**Builder input (edxml-builder.ts mới):**
```typescript
export interface BuildEdxmlInput {
  // Sender (DN gửi) — từ lgsp_agency_config + departments
  senderOrgCode: string;        // departments.lgsp_org_code (VD 'H37.DN.001')
  senderOrgName: string;        // departments.name
  // Recipient (đơn vị ngoài) — từ inter_organizations
  destOrgCode: string;          // inter_organizations.code (VD 'H37.DN.002')
  destOrgName: string;          // inter_organizations.name
  // Document — từ outgoing_docs (xem schema line 13943-13979)
  notation: string | null;          // outgoing_docs.notation (VD '123/UBND-VP')
  documentCode: string | null;      // outgoing_docs.document_code
  abstract: string | null;          // outgoing_docs.abstract
  publishDate: Date | string | null; // outgoing_docs.publish_date
  signer: string | null;            // outgoing_docs.signer
  signDate: Date | string | null;   // outgoing_docs.sign_date
  signerPosition: string | null;    // optional — Phase 34 hardcode 'N/A' nếu null
  docTypeName: string | null;       // lookup từ doc_types qua doc_type_id
  numberPaper: number | null;       // outgoing_docs.number_paper
  appendix: string | null;          // optional
  // Attachments — list từ attachment_outgoing_docs + buffer load từ MinIO
  attachments: Array<{
    fileName: string;       // attachment_outgoing_docs.file_name
    fileType: string;       // content_type ('application/pdf', 'application/msword'...)
    contentBase64: string;  // base64 encoded buffer
  }>;
}

export interface BuildEdxmlResult {
  buffer: Buffer;        // utf-8 encoded edXML string buffer
  docId: string;         // UUID generated (DocumentId trong MessageHeader)
  destOrgCode: string;   // for log/multipart filename
  docCode: string;       // notation hoặc documentCode, fallback 'EDOC-<docId>'
}
```

**Error codes (LGSP spec từ HDSD v2.2 + Postman):**
| Code | Vietnamese |
|---|---|
| 0 | Thành công |
| 10 | File không hợp lệ |
| 15 | Sai SystemId hoặc SecretKey |
| 18 | Đơn vị nhận không tồn tại |
| 19 | Đơn vị gửi không đăng ký |
| 20 | Lỗi hệ thống |
| 21 | Lỗi xử lý file |
| 22 | Format edXML không đúng |
| 23 | Đơn vị nhận đã khóa |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tạo error-codes.ts (LGSP_ERROR_CODES map + LgspSendError class + mapLgspError helper)</name>
  <files>
    e_office_app_new/backend/src/services/lgsp/error-codes.ts
  </files>
  <action>
**Tạo NEW file** `e_office_app_new/backend/src/services/lgsp/error-codes.ts` chứa table mapping 9 mã ErrorCode LGSP (CONTEXT D-12) + custom Error class + helper.

```typescript
// ============================================================
// LGSP Error Code Mapping (Phase 34 — CONTEXT D-12)
// Source: docs/Truc EDOC Lang Son/HuongDanKetNoiLienThongVB_v2.2.pdf section 4
// + LTVB_API_TRUC_PROD_TICHHOP.postman_collection.json response examples
//
// Worker (Phase 34-02) consume:
//   - Code = '0' -> success path, update lgsp_tracking status='success' + lgsp_doc_id
//   - Code in {10,15,18,19,20,21,22,23} -> no-retry path, update tracking status='error' + Vietnamese error_message
//   - Network/timeout/5xx -> throw LgspSendError + ko-set-code -> BullMQ retry per D-11
// ============================================================

/**
 * 9 ErrorCode LGSP -> Vietnamese message (CONTEXT D-12).
 *
 * Tat ca message KHONG CO DAU (chuan PowerShell 5.1 + log file).
 * Khi hien thi UI (Plan 34-04 badge) frontend hardcode message Tieng Viet co dau.
 */
export const LGSP_ERROR_CODES: Record<string, string> = {
  '0': 'Thanh cong',
  '10': 'File khong hop le',
  '15': 'Sai SystemId hoac SecretKey',
  '18': 'Don vi nhan khong ton tai',
  '19': 'Don vi gui khong dang ky',
  '20': 'Loi he thong tu phia LGSP',
  '21': 'Loi xu ly file',
  '22': 'Format edXML khong dung chuan QD 28',
  '23': 'Don vi nhan da khoa',
};

/**
 * Custom Error class cho structured throw trong sendDocument().
 *
 * Worker handler (Plan 34-02) catch -> instanceof check -> nếu có `code` 4xx -> no-retry,
 * nếu không có code (network/timeout) -> rethrow để BullMQ retry per D-11.
 */
export class LgspSendError extends Error {
  public readonly code: string | undefined;
  public readonly vietnameseMessage: string;
  public readonly rawMessage: string;

  constructor(rawMessage: string, code?: string) {
    const vietnamese = code && LGSP_ERROR_CODES[code]
      ? LGSP_ERROR_CODES[code]
      : `Loi khong xac dinh${code ? ` (Code ${code})` : ''}: ${rawMessage}`;
    super(vietnamese);
    this.name = 'LgspSendError';
    this.code = code;
    this.vietnameseMessage = vietnamese;
    this.rawMessage = rawMessage;
  }
}

/**
 * Map ErrorCode -> Vietnamese message (utility cho route/worker khi không throw).
 *
 * @param errorCode Mã string từ LGSP response (VD '15', '18'). Có thể null/undefined.
 * @param fallbackMessage Raw message từ LGSP (dùng khi errorCode không trong table).
 * @returns Vietnamese message có format hợp lý cho UI display.
 */
export function mapLgspError(errorCode: string | null | undefined, fallbackMessage: string): string {
  if (!errorCode) return fallbackMessage || 'Loi khong xac dinh';
  const known = LGSP_ERROR_CODES[errorCode];
  if (known) return `${known} (Code ${errorCode})`;
  return `${fallbackMessage || 'Loi khong xac dinh'} (Code ${errorCode})`;
}

/**
 * Classify error code thành retry vs no-retry (Phase 34 worker dùng — CONTEXT D-11).
 *
 * @returns `true` = no-retry (4xx LGSP error đã rõ), `false` = retry (network/timeout/5xx)
 */
export function isLgspNonRetryableError(errorCode: string | null | undefined): boolean {
  if (!errorCode) return false;
  return errorCode in LGSP_ERROR_CODES && errorCode !== '0';
}
```

**Tạo dir nếu chưa có:** `e_office_app_new/backend/src/services/lgsp/` (mkdir trước khi Write).

**KHÔNG dùng tieng Viet co dau trong message hardcode** — vì worker log + tracking.error_message lưu DB + có thể qua PowerShell terminal. Frontend Plan 34-04 hardcode Vietnamese có dấu khi render badge.
  </action>
  <verify>
    <automated>
      Test-Path e_office_app_new/backend/src/services/lgsp/error-codes.ts

      # Verify 9 mã code hiện diện
      Select-String -Path e_office_app_new/backend/src/services/lgsp/error-codes.ts -Pattern "'0':|'10':|'15':|'18':|'19':|'20':|'21':|'22':|'23':"

      # Verify 3 export
      Select-String -Path e_office_app_new/backend/src/services/lgsp/error-codes.ts -Pattern "export const LGSP_ERROR_CODES"
      Select-String -Path e_office_app_new/backend/src/services/lgsp/error-codes.ts -Pattern "export class LgspSendError"
      Select-String -Path e_office_app_new/backend/src/services/lgsp/error-codes.ts -Pattern "export function mapLgspError"
      Select-String -Path e_office_app_new/backend/src/services/lgsp/error-codes.ts -Pattern "export function isLgspNonRetryableError"

      # File length >= 60 lines
      (Get-Content e_office_app_new/backend/src/services/lgsp/error-codes.ts).Count
    </automated>
  </verify>
  <done>
    File `services/lgsp/error-codes.ts` tồn tại với 9 mã code + LgspSendError class + 2 helper, TypeScript syntax valid.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add xmlbuilder2 + form-data deps + tạo edxml-builder.ts</name>
  <files>
    e_office_app_new/backend/package.json
    e_office_app_new/backend/src/services/lgsp/edxml-builder.ts
  </files>
  <action>
**Bước 1: Add 2 deps vào `e_office_app_new/backend/package.json`:**

```powershell
cd e_office_app_new/backend
$env:NODE_ENV='development'
npm install xmlbuilder2@^3.1.1 form-data@^4.0.1 --save
# CLAUDE.md pitfall #2: NODE_ENV=development TRƯỚC install
```

Verify added vào dependencies section (KHÔNG devDependencies):
```powershell
Select-String -Path e_office_app_new/backend/package.json -Pattern '"xmlbuilder2":'
Select-String -Path e_office_app_new/backend/package.json -Pattern '"form-data":'
```

**Bước 2: Tạo NEW file** `e_office_app_new/backend/src/services/lgsp/edxml-builder.ts`:

```typescript
// ============================================================
// edXML Builder (Phase 34 - CONTEXT D-05, D-06, D-08, D-09)
// Source spec: docs/Truc EDOC Lang Son/HuongDanKetNoiLienThongVB_v2.2.pdf section 3
// Library: xmlbuilder2 (type-safe, escape XML auto)
//
// Output: Buffer chua edXML envelope theo chuan QD 28/2018/QD-TTg
//         + DocumentId UUID generated (luu vao lgsp_tracking.lgsp_doc_id PLACEHOLDER
//           se overwrite bang docId LGSP tra ve sau send thanh cong)
// ============================================================

import { create } from 'xmlbuilder2';
import { randomUUID } from 'crypto';
import pino from 'pino';

const logger = pino({ name: 'edxml-builder' });

/**
 * Input data cho builder. Caller (Plan 34-02 worker) build object nay tu:
 *   - lgsp_agency_config (sender systemId/lgsp_org_code)
 *   - departments (sender name)
 *   - inter_organizations (recipient code + name)
 *   - outgoing_docs (notation, document_code, abstract, signer, ...)
 *   - doc_types (docTypeName via lookup)
 *   - attachment_outgoing_docs + MinIO buffer (base64)
 */
export interface BuildEdxmlInput {
  // Sender
  senderOrgCode: string;        // departments.lgsp_org_code (VD 'H37.DN.001')
  senderOrgName: string;        // departments.name

  // Recipient
  destOrgCode: string;          // inter_organizations.code
  destOrgName: string;          // inter_organizations.name

  // Document fields (outgoing_docs)
  notation: string | null;
  documentCode: string | null;
  abstract: string | null;
  publishDate: Date | string | null;
  signer: string | null;
  signDate: Date | string | null;
  signerPosition: string | null;
  docTypeName: string | null;
  numberPaper: number | null;
  appendix: string | null;

  // Attachments (already loaded from MinIO + base64 encoded)
  attachments: Array<{
    fileName: string;
    fileType: string;        // VD 'application/pdf'
    contentBase64: string;
  }>;
}

export interface BuildEdxmlResult {
  buffer: Buffer;           // UTF-8 edXML bytes
  docId: string;            // UUID generated (DocumentId in MessageHeader)
  destOrgCode: string;
  docCode: string;          // notation || documentCode || 'EDOC-<docId>'
}

/**
 * Fallback string cho field null/empty (CONTEXT D-09).
 *
 * Log warning để dev know — KHONG reject ban hanh vi UX nhe nhang
 * (KH chua quen field bắt buộc spec QĐ 28, va nhieu VB cu khong day du).
 */
function strOrNa(value: string | null | undefined, fieldName: string, ctx: { docCode: string }): string {
  if (value && value.trim()) return value.trim();
  logger.warn({ field: fieldName, docCode: ctx.docCode }, 'edXML field rỗng, fallback "N/A"');
  return 'N/A';
}

function numOrZero(value: number | null | undefined, fieldName: string, ctx: { docCode: string }): number {
  if (typeof value === 'number' && !isNaN(value)) return value;
  logger.warn({ field: fieldName, docCode: ctx.docCode }, 'edXML số field rỗng, fallback 0');
  return 0;
}

function toIsoDateString(value: Date | string | null | undefined, fieldName: string, ctx: { docCode: string }): string {
  if (!value) {
    logger.warn({ field: fieldName, docCode: ctx.docCode }, 'edXML date field rỗng, fallback NOW');
    return new Date().toISOString();
  }
  if (value instanceof Date) return value.toISOString();
  // String (từ pg driver TIMESTAMP -> ISO string)
  return value;
}

/**
 * Build edXML envelope đúng spec QĐ 28/2018/QĐ-TTg.
 *
 * Structure (theo HDSD v2.2 section 3):
 *   <EdXMLEnvelope xmlns="http://www.go.vn/eDoc">
 *     <MessageHeader>
 *       <From><OrganId/><OrganName/></From>
 *       <To><OrganId/><OrganName/></To>
 *       <Code><CodeNumber/><CodeNotation/></Code>
 *       <PromulgationInfo><Promulgator/><PromulgationDate/></PromulgationInfo>
 *       <DocumentType>{docTypeName}</DocumentType>
 *       <Subject>{abstract}</Subject>
 *       <SignerInfo><Signer/><Position/><Competence/></SignerInfo>
 *       <OtherInfo><PageAmount/><Appendix/></OtherInfo>
 *       <DocumentId>{uuid}</DocumentId>
 *       <TraceHeaderList>
 *         <TraceHeader>
 *           <Time/>
 *           <Path><From/><To/></Path>
 *         </TraceHeader>
 *       </TraceHeaderList>
 *     </MessageHeader>
 *     <Manifest>
 *       <Attachment>
 *         <Content>{base64}</Content>
 *         <FileName>{name}</FileName>
 *         <FileType>{mime}</FileType>
 *       </Attachment>
 *     </Manifest>
 *   </EdXMLEnvelope>
 *
 * @param input All fields needed - caller load từ DB + MinIO trước khi gọi.
 * @returns Result với buffer (Buffer UTF-8), docId (UUID), destOrgCode (cho log), docCode (cho multipart filename)
 */
export function buildEdxml(input: BuildEdxmlInput): BuildEdxmlResult {
  const docId = randomUUID();
  const docCode = input.notation?.trim() || input.documentCode?.trim() || `EDOC-${docId}`;
  const ctx = { docCode };
  const nowIso = new Date().toISOString();

  const doc = create({ version: '1.0', encoding: 'UTF-8' })
    .ele('EdXMLEnvelope', { xmlns: 'http://www.go.vn/eDoc' })
      .ele('MessageHeader')
        .ele('From')
          .ele('OrganId').txt(input.senderOrgCode).up()
          .ele('OrganName').txt(strOrNa(input.senderOrgName, 'senderOrgName', ctx)).up()
        .up()
        .ele('To')
          .ele('OrganId').txt(input.destOrgCode).up()
          .ele('OrganName').txt(strOrNa(input.destOrgName, 'destOrgName', ctx)).up()
        .up()
        .ele('Code')
          .ele('CodeNumber').txt(strOrNa(input.notation, 'notation', ctx)).up()
          .ele('CodeNotation').txt(strOrNa(input.documentCode, 'documentCode', ctx)).up()
        .up()
        .ele('PromulgationInfo')
          .ele('Promulgator').txt(strOrNa(input.signer, 'signer/promulgator', ctx)).up()
          .ele('PromulgationDate').txt(toIsoDateString(input.publishDate, 'publishDate', ctx)).up()
        .up()
        .ele('DocumentType').txt(strOrNa(input.docTypeName, 'docTypeName', ctx)).up()
        .ele('Subject').txt(strOrNa(input.abstract, 'abstract/subject', ctx)).up()
        .ele('SignerInfo')
          .ele('Signer').txt(strOrNa(input.signer, 'signer', ctx)).up()
          .ele('Position').txt(strOrNa(input.signerPosition, 'signerPosition', ctx)).up()
          .ele('Competence').txt('Truc tiep').up()
        .up()
        .ele('OtherInfo')
          .ele('PageAmount').txt(String(numOrZero(input.numberPaper, 'numberPaper', ctx))).up()
          .ele('Appendix').txt(input.appendix?.trim() || '').up()
        .up()
        .ele('DocumentId').txt(docId).up()
        .ele('TraceHeaderList')
          .ele('TraceHeader')
            .ele('Time').txt(nowIso).up()
            .ele('Path')
              .ele('From').txt(input.senderOrgCode).up()
              .ele('To').txt(input.destOrgCode).up()
            .up()
          .up()
        .up()
      .up()
      .ele('Manifest');

  // Append Attachment elements
  const manifest = doc;
  for (const att of input.attachments) {
    manifest
      .ele('Attachment')
        .ele('Content').txt(att.contentBase64).up()
        .ele('FileName').txt(att.fileName).up()
        .ele('FileType').txt(att.fileType || 'application/octet-stream').up()
      .up();
  }

  const xmlString = manifest.up().end({ prettyPrint: false });
  const buffer = Buffer.from(xmlString, 'utf8');

  logger.info(
    {
      docId,
      docCode,
      destOrgCode: input.destOrgCode,
      senderOrgCode: input.senderOrgCode,
      attachmentCount: input.attachments.length,
      bytes: buffer.length,
    },
    'Built edXML envelope',
  );

  return {
    buffer,
    docId,
    destOrgCode: input.destOrgCode,
    docCode,
  };
}
```

**Cri ti cal:**
- `xmlbuilder2` auto escape XML special chars (`& < > " '`) — KHÔNG cần manual sanitize
- Fallback `N/A` cho string null, `0` cho number null, `NOW` cho date null (CONTEXT D-09)
- DocumentId là UUID generated, sẽ ghi vào `lgsp_tracking.lgsp_doc_id` placeholder, sẽ overwrite bằng docId LGSP trả về sau send success
- `attachments` đã base64 encoded - builder không tải lại từ MinIO (worker Plan 34-02 sẽ tải)
- File length >= 200 dòng (kể cả comment + interface)
  </action>
  <verify>
    <automated>
      cd e_office_app_new/backend

      # Verify 2 deps đã add
      Select-String -Path package.json -Pattern '"xmlbuilder2":'
      Select-String -Path package.json -Pattern '"form-data":'
      Test-Path node_modules/xmlbuilder2
      Test-Path node_modules/form-data

      # Verify builder file
      Test-Path src/services/lgsp/edxml-builder.ts
      Select-String -Path src/services/lgsp/edxml-builder.ts -Pattern "export function buildEdxml"
      Select-String -Path src/services/lgsp/edxml-builder.ts -Pattern "export interface BuildEdxmlInput"
      Select-String -Path src/services/lgsp/edxml-builder.ts -Pattern "import { create } from 'xmlbuilder2'"
      Select-String -Path src/services/lgsp/edxml-builder.ts -Pattern "ele\('EdXMLEnvelope'"
      Select-String -Path src/services/lgsp/edxml-builder.ts -Pattern "ele\('MessageHeader'"
      Select-String -Path src/services/lgsp/edxml-builder.ts -Pattern "ele\('From'"
      Select-String -Path src/services/lgsp/edxml-builder.ts -Pattern "ele\('To'"
      Select-String -Path src/services/lgsp/edxml-builder.ts -Pattern "ele\('Code'"
      Select-String -Path src/services/lgsp/edxml-builder.ts -Pattern "ele\('TraceHeaderList'"
      Select-String -Path src/services/lgsp/edxml-builder.ts -Pattern "ele\('Manifest'"
      Select-String -Path src/services/lgsp/edxml-builder.ts -Pattern "ele\('Attachment'"
      Select-String -Path src/services/lgsp/edxml-builder.ts -Pattern "randomUUID"

      (Get-Content src/services/lgsp/edxml-builder.ts).Count
      # Phải >= 200 lines
    </automated>
  </verify>
  <done>
    `xmlbuilder2` + `form-data` deps installed; `services/lgsp/edxml-builder.ts` >= 200 lines với buildEdxml() đầy đủ 9 thành phần MessageHeader + Manifest attachment.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix LGSPRealService.sendDocument() — đổi signature + dùng /v1/sendEdoc multipart + headers X-SystemId/X-SecretKey</name>
  <files>
    e_office_app_new/backend/src/services/lgsp-real.service.ts
    e_office_app_new/backend/src/services/lgsp.service.ts
  </files>
  <action>
**Bước 1: Update `ILgspService` interface trong `lgsp.service.ts` (line 37-42):**

Đổi signature `sendDocument`:
```typescript
// TRƯỚC:
sendDocument(edxmlContent: string, destOrgCode: string): Promise<LgspSendResult>;
// SAU:
sendDocument(edxmlBuffer: Buffer, destOrgCode: string, docCode: string): Promise<LgspSendResult>;
```

Update `LgspSendResult` thêm `errorCode?`:
```typescript
export interface LgspSendResult {
  success: boolean;
  lgsp_doc_id: string;
  message: string;
  errorCode?: string;  // Phase 34: 0/10/15/18/19/20/21/22/23 từ LGSP response.data.errorCode
}
```

**Bước 2: Rewrite `sendDocument()` trong `lgsp-real.service.ts` (line 161-184) — DÙNG `/v1/sendEdoc` multipart + headers + signature mới.**

Import thêm `FormData` từ `form-data` + `LgspSendError` từ `./lgsp/error-codes.js` ở đầu file:

```typescript
import FormData from 'form-data';
import { LgspSendError, mapLgspError } from './lgsp/error-codes.js';
```

Replace `sendDocument()` method:

```typescript
/**
 * Send edXML doc qua LGSP `/v1/sendEdoc` (Phase 34 - CONTEXT D-07).
 *
 * AUTHORITATIVE shape (Postman collection 03.sendEdoc):
 *   POST {baseUrl}/v1/sendEdoc
 *   Headers: X-SystemId, X-SecretKey
 *   Body: multipart/form-data
 *     edocFile: <Buffer> (Content-Type: application/xml, filename: {docCode}.edxml)
 *     messageType: 'edoc'
 *
 * Response variants:
 *   { success: true, message: 'OK', docId: '<uuid>' }
 *   { success: false, message: 'Loi', data: { docId, status, errorCode, errorDesc } }
 *
 * NO login/token flow cho document API - khac voi Phase 18 dung OAuth2.
 *
 * @param edxmlBuffer edXML envelope bytes (built boi edxml-builder.ts)
 * @param destOrgCode Code don vi nhan (cho log only - khong gui trong body, LGSP parse tu edXML MessageHeader.To)
 * @param docCode Notation/document_code (lam filename trong multipart - .edxml extension)
 * @returns LgspSendResult { success, lgsp_doc_id, message, errorCode }
 * @throws LgspSendError neu network/timeout (no code = retryable per D-11)
 */
async sendDocument(edxmlBuffer: Buffer, destOrgCode: string, docCode: string): Promise<LgspSendResult> {
  const sysId = this.systemId;
  const secret = this.secretKey;
  if (!sysId || !secret) {
    throw new LgspSendError(
      `LGSP credential missing: systemId=${sysId ? 'OK' : 'EMPTY'}, secretKey=${secret ? 'OK' : 'EMPTY'}`,
    );
  }

  // Sanitize docCode for filename — replace path chars + extension
  const safeFilename = (docCode || 'edoc').replace(/[\\/:*?"<>|]/g, '_').slice(0, 100) + '.edxml';

  // Build multipart form
  const form = new FormData();
  form.append('edocFile', edxmlBuffer, {
    filename: safeFilename,
    contentType: 'application/xml',
    knownLength: edxmlBuffer.length,
  });
  form.append('messageType', 'edoc');

  // Fetch — Node 22+ native, no axios needed
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), 60000);  // 60s timeout (LGSP send may take long for big attachments)
  try {
    const res = await fetch(`${this.endpoint}/v1/sendEdoc`, {
      method: 'POST',
      headers: {
        'X-SystemId': sysId,
        'X-SecretKey': secret,
        ...form.getHeaders(),
      },
      // Node fetch + form-data: cast to any vì form-data tra ve readable stream (Node compatible)
      body: form as unknown as BodyInit,
      signal: controller.signal,
    });

    const text = await res.text();
    let json: SendEdocResponse;
    try {
      json = JSON.parse(text) as SendEdocResponse;
    } catch {
      // Server tra ve non-JSON (HTML error page, 502 Bad Gateway...)
      throw new LgspSendError(`LGSP /v1/sendEdoc HTTP ${res.status} non-JSON response: ${text.slice(0, 200)}`);
    }

    // Parse 2 shape variants (Postman docs)
    const docId = json.docId || json.data?.docId || '';
    const errorCode = json.data?.errorCode;
    const rawMessage = json.message || json.data?.errorDesc || 'unknown';

    if (!json.success) {
      // No-throw — return result để worker biết update tracking status='error' (no-retry per D-11)
      return {
        success: false,
        lgsp_doc_id: docId,
        message: mapLgspError(errorCode, rawMessage),
        errorCode: errorCode || undefined,
      };
    }

    return {
      success: true,
      lgsp_doc_id: docId,
      message: rawMessage,
      errorCode: errorCode || '0',
    };
  } catch (err: unknown) {
    // AbortController / network / DNS / connection — throw để BullMQ retry
    if (err instanceof LgspSendError) throw err;
    const msg = err instanceof Error ? err.message : String(err);
    throw new LgspSendError(`LGSP /v1/sendEdoc network/timeout: ${msg}`);
  } finally {
    clearTimeout(timer);
  }
}
```

**Bước 3: Cập nhật `SendEdocResponse` interface trong cùng file (đã có line 22-27 — verify shape khớp):**

```typescript
interface SendEdocResponse {
  success: boolean;
  message: string;
  docId?: string;
  data?: {
    docId?: string;
    status?: string;
    errorCode?: string;
    errorDesc?: string;
  };
}
```

**Bước 4: Run tsc để verify TypeScript pass:**

```powershell
cd e_office_app_new/backend
npx tsc --noEmit
# Exit code = 0
```

**Critical changes:**
- Endpoint đổi `/api/lgspedoc/send-edoc` → `/v1/sendEdoc`
- Body đổi JSON → multipart/form-data với `edocFile` (Buffer) + `messageType` (text)
- Auth headers `X-SystemId` + `X-SecretKey` (KHÔNG dùng login token)
- Signature đổi `(content: string, destOrgCode)` → `(buffer: Buffer, destOrgCode, docCode)` — KHÔNG là string nữa
- `destOrgCode` parameter chỉ dùng cho log (LGSP parse từ edXML MessageHeader.To, không gửi trong body)
- Throw `LgspSendError` cho network — return non-throw cho 4xx LGSP error (Plan 34-02 worker decide retry)
- KHÔNG đụng `getToken()` / `receiveDocuments()` / `syncOrganizations()` — Plan 35/36 sẽ fix nếu cần

**Backward compat:** `lgspRealService` singleton (line 270) và `routes/lgsp.ts` callers KHÔNG break — chỉ sendDocument signature đổi. Routes hiện tại KHÔNG gọi sendDocument trực tiếp, chỉ worker mới gọi.

Verify: `grep -n "sendDocument" backend/src/` để chắc không có caller khác ngoài Plan 34-02 worker (sẽ tạo).
  </action>
  <verify>
    <automated>
      cd e_office_app_new/backend

      # Verify signature mới
      Select-String -Path src/services/lgsp.service.ts -Pattern "sendDocument\(edxmlBuffer: Buffer"
      Select-String -Path src/services/lgsp-real.service.ts -Pattern "async sendDocument\(edxmlBuffer: Buffer"

      # Verify endpoint + body shape mới
      Select-String -Path src/services/lgsp-real.service.ts -Pattern "/v1/sendEdoc"
      Select-String -Path src/services/lgsp-real.service.ts -Pattern "form.append\('edocFile'"
      Select-String -Path src/services/lgsp-real.service.ts -Pattern "form.append\('messageType'"
      Select-String -Path src/services/lgsp-real.service.ts -Pattern "'X-SystemId':"
      Select-String -Path src/services/lgsp-real.service.ts -Pattern "'X-SecretKey':"

      # Verify NO endpoint cu
      $oldRefs = Select-String -Path src/services/lgsp-real.service.ts -Pattern "/api/lgspedoc/send-edoc" -SimpleMatch
      if ($oldRefs) { Write-Error "Old endpoint vẫn còn tham chiếu"; exit 1 }

      # Verify import form-data + LgspSendError
      Select-String -Path src/services/lgsp-real.service.ts -Pattern "import FormData from 'form-data'"
      Select-String -Path src/services/lgsp-real.service.ts -Pattern "LgspSendError"

      # Verify errorCode in LgspSendResult
      Select-String -Path src/services/lgsp.service.ts -Pattern "errorCode\?: string"

      # TypeScript compile check (CLAUDE.md pitfall #4)
      npx tsc --noEmit 2>&1 | Tee-Object -FilePath "$env:TEMP/tsc_34_01.log"
      $LASTEXITCODE
      # Phải = 0
    </automated>
  </verify>
  <done>
    `LGSPRealService.sendDocument()` dùng `/v1/sendEdoc` multipart/form-data + headers X-SystemId/X-SecretKey; signature `(Buffer, destOrgCode, docCode)`; LgspSendResult thêm `errorCode?`; TypeScript zero error.
  </done>
</task>

</tasks>

<verification>
**Integration check sau 3 tasks (Wave 1 part 1):**

```powershell
# 1. TypeScript compile cho cả backend (catch any breakage)
cd e_office_app_new/backend
$env:NODE_ENV='development'
npx tsc --noEmit 2>&1
# Exit code phải = 0

# 2. Verify 3 file mới tồn tại + có nội dung
Test-Path src/services/lgsp/error-codes.ts
Test-Path src/services/lgsp/edxml-builder.ts
Get-ChildItem src/services/lgsp-real.service.ts | Select Length

# 3. Smoke test builder (no DB needed)
$env:NODE_PATH='./node_modules'
node -e "
import('./dist/services/lgsp/edxml-builder.js').then(m => {
  const r = m.buildEdxml({
    senderOrgCode: 'H37.DN.001',
    senderOrgName: 'Cty CP Huu nghi Xuan Cuong',
    destOrgCode: 'H37.DN.002',
    destOrgName: 'Cty CP SX TM Lang Son',
    notation: '123/UBND-VP',
    documentCode: 'DOC-001',
    abstract: 'Test edXML',
    publishDate: new Date(),
    signer: 'Nguyen Van A',
    signDate: new Date(),
    signerPosition: 'Giam doc',
    docTypeName: 'Cong van',
    numberPaper: 2,
    appendix: null,
    attachments: [{ fileName: 'test.pdf', fileType: 'application/pdf', contentBase64: 'JVBERi0xLg==' }],
  });
  console.log('docId:', r.docId);
  console.log('docCode:', r.docCode);
  console.log('bytes:', r.buffer.length);
  console.log('xml head:', r.buffer.toString('utf8').slice(0, 200));
}).catch(e => { console.error(e); process.exit(1); });
"
# Phải in:
#   docId: <uuid>
#   docCode: 123/UBND-VP
#   bytes: > 800
#   xml head: <?xml version="1.0" encoding="UTF-8"?><EdXMLEnvelope xmlns="http://www.go.vn/eDoc"><MessageHeader><From>...
# (Nếu chưa build dist/, chạy `npm run build` trước)

# 4. Smoke test error mapper
node -e "
import('./dist/services/lgsp/error-codes.js').then(m => {
  console.log(m.mapLgspError('15', 'raw'));     // 'Sai SystemId hoac SecretKey (Code 15)'
  console.log(m.mapLgspError(null, 'fallback')); // 'fallback'
  console.log(m.mapLgspError('999', 'raw'));    // 'raw (Code 999)'
  console.log(m.isLgspNonRetryableError('18')); // true
  console.log(m.isLgspNonRetryableError(null)); // false
  const e = new m.LgspSendError('test', '15');
  console.log('e.code:', e.code, 'e.vietnameseMessage:', e.vietnameseMessage);
});
"
```

**Pass criteria:** Tất cả 4 bước in output đúng expected, no TypeScript error, no missing export.
</verification>

<success_criteria>
- [x] `services/lgsp/error-codes.ts` tồn tại với 9 mã code + LgspSendError class + mapLgspError + isLgspNonRetryableError
- [x] `services/lgsp/edxml-builder.ts` >= 200 dòng với buildEdxml() đầy đủ MessageHeader 9 thành phần + Manifest attachment
- [x] `package.json` có deps `xmlbuilder2` (^3.1.1) + `form-data` (^4.0.1) trong dependencies
- [x] `LGSPRealService.sendDocument()` signature đổi sang `(edxmlBuffer: Buffer, destOrgCode: string, docCode: string)`
- [x] sendDocument POST đúng `{baseUrl}/v1/sendEdoc` multipart/form-data
- [x] Headers `X-SystemId` + `X-SecretKey` (KHÔNG login token flow)
- [x] Response parse 2 shape variants (top-level `docId` hoặc `data.docId/errorCode/errorDesc`)
- [x] Non-throw return cho LGSP 4xx (errorCode set) + throw `LgspSendError` cho network/timeout
- [x] `LgspSendResult.errorCode?` thêm vào interface
- [x] ILgspService.sendDocument signature update khớp implementation
- [x] TypeScript compile zero error
- [x] Smoke test builder + mapper PASS
</success_criteria>

<output>
After completion, create `.planning/phases/34-send-flow-sendedoc/34-01-SUMMARY.md` với:
- Files modified: 4 (lgsp-real.service.ts + lgsp.service.ts + 2 new file lgsp/ + package.json)
- Endpoint fix: `/api/lgspedoc/send-edoc` (BROKEN) → `/v1/sendEdoc` multipart (CORRECT per Postman)
- Auth fix: login token body → `X-SystemId` + `X-SecretKey` headers
- Signature change: sendDocument string→Buffer + add docCode param
- New exports: LGSP_ERROR_CODES (9 mã), LgspSendError class, mapLgspError, isLgspNonRetryableError, buildEdxml, BuildEdxmlInput, BuildEdxmlResult
- Deps added: xmlbuilder2@^3.1.1, form-data@^4.0.1
- TypeScript: PASS / 0 error
- Smoke test builder: produces valid XML envelope với UUID DocumentId
- Smoke test error mapper: 9 codes mapped correctly
- Next: Plan 34-02 (BullMQ queue + worker module setup)
</output>
