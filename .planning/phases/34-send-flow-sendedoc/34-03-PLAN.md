---
phase: 34-send-flow-sendedoc
plan: 03
type: execute
wave: 2
depends_on:
  - 34-01
  - 34-02
files_modified:
  - e_office_app_new/backend/src/routes/outgoing-doc.ts
  - e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts
autonomous: true
requirements:
  - LGSP-SEND-03
  - LGSP-SEND-04
tags: [backend, route, enqueue, lgsp]

must_haves:
  truths:
    - "Route POST /api/van-ban-di/:id/gui-noi-bo sau khi SP fn_outgoing_doc_send_to_recipients return success, đọc danh sách external recipients (tracking_id mới tạo) + enqueue N jobs (1 per external)"
    - "Job data shape khớp LgspSendJobData: { recipient_id, outgoing_doc_id, tracking_id, sender_unit_id, environment }"
    - "sender_unit_id = root unit của user gửi (NOT departmentId — phải trace lên root qua helper)"
    - "environment = 'prod' mặc định; có thể override qua env var LGSP_DEFAULT_ENVIRONMENT='sandbox' (dev/test mode)"
    - "Response success extended: { internal_count, external_count, enqueued_count } để UI biết có job background đang chờ"
    - "Repository method `getExternalRecipientsPending(docId)` mới — trả về list { recipient_id, tracking_id, sender_unit_id } để route enqueue (CONTEXT D-03: enqueue sau commit)"
    - "Nếu enqueue fail (Redis down), tracking row đã pending sẵn, log error pino + return success cho user (admin retry sau qua Phase 37 hoặc cron retry)"
    - "TypeScript compile zero error"
  artifacts:
    - path: "e_office_app_new/backend/src/routes/outgoing-doc.ts"
      provides: "Extended POST /:id/gui-noi-bo handler with enqueue logic (line ~717-740)"
      contains: "enqueueLgspSendJob"
    - path: "e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts"
      provides: "New method getExternalRecipientsForSend(docId) trả về external recipients pending vừa được SP tạo tracking row"
      contains: "getExternalRecipientsForSend"
  key_links:
    - from: "Route POST /:id/gui-noi-bo (sau SP success)"
      to: "enqueueLgspSendJob({ recipient_id, outgoing_doc_id, tracking_id, sender_unit_id, environment })"
      via: "Loop external recipients query → for each enqueue 1 job"
      pattern: "Producer side fire-and-forget (D-03)"
    - from: "sender_unit_id resolution"
      to: "resolveAncestorUnit(departmentId) — existing helper"
      via: "Trace user.departmentId → root unit trong cây departments"
      pattern: "Same as other routes that need unit_id context"
---

<objective>
Wire enqueue logic vào route `POST /api/van-ban-di/:id/gui-noi-bo` (line 717-740 trong `backend/src/routes/outgoing-doc.ts`). Sau khi SP `fn_outgoing_doc_send_to_recipients` commit success (đã auto INSERT `lgsp_tracking` pending cho external recipients), route đọc danh sách external recipients + tracking_ids vừa tạo, enqueue 1 BullMQ job per external recipient (CONTEXT D-02 granularity per-recipient).

**Sender unit resolution:** `sender_unit_id` = root unit của user gửi (trace `user.departmentId → ancestor where parent_id IS NULL`). Dùng helper `resolveAncestorUnit()` đã có trong codebase (route hiện đang dùng line 634 `/danh-sach-gui`).

**Environment resolution:** Default `'prod'`. Override qua env var `LGSP_DEFAULT_ENVIRONMENT='sandbox'` cho dev/test mode (Phase 34-05 E2E test với DN.001 sandbox sẽ set).

**Repository method mới:** `getExternalRecipientsForSend(docId)` trả về list `{ recipient_id, tracking_id }` cho mỗi external recipient của VB. Route gọi sau khi SP success.

**Failure mode (enqueue fail Redis down):** Log error pino + return success cho user. Tracking row đã có status='pending' → Phase 37 admin có thể "Gửi lại" hoặc cron retry sau (defer).

Purpose: Hoàn tất producer-side wire để worker Phase 34-02 consume job thật từ user action.

Output: 2 file modify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-send-flow-sendedoc/34-CONTEXT.md
@.planning/phases/34-send-flow-sendedoc/34-01-PLAN.md
@.planning/phases/34-send-flow-sendedoc/34-02-PLAN.md
@CLAUDE.md
@e_office_app_new/backend/src/routes/outgoing-doc.ts
@e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts
@e_office_app_new/backend/src/lib/queue/lgsp-send-queue.ts

<read_first>
- `e_office_app_new/backend/src/routes/outgoing-doc.ts` (line 717-740 — route POST /:id/gui-noi-bo hiện tại + line 1-30 imports + line 632-640 resolveAncestorUnit usage)
- `e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts` (line 1-30 imports + line 220-227 sendToRecipients pattern + cuối file thêm method mới)
- `e_office_app_new/backend/src/lib/queue/lgsp-send-queue.ts` (Plan 34-02 mới — enqueueLgspSendJob signature)
- `e_office_app_new/database/schema/000_schema_v3.0.sql` line 27992-28074 — SP fn_outgoing_doc_send_to_recipients (đã có INSERT lgsp_tracking + UPDATE outgoing_doc_recipients.generated_lgsp_tracking_id)
</read_first>

<interfaces>
**Existing route handler (line 717-740 — Phase 17 v3.0):**
```typescript
router.post('/:id/gui-noi-bo', async (req: Request, res: Response) => {
  try {
    const { staffId, departmentId, isAdmin } = (req as AuthRequest).user;
    const docId = Number(req.params.id);
    const loaded = await loadDocAndPerms(docId, { staffId, departmentId, isAdmin });
    if (!loaded) { res.status(404).json({ success: false, message: 'Không tìm thấy văn bản đi' }); return; }
    if (!loaded.perms.canSend) { res.status(403).json({ success: false, message: 'Không có quyền gửi văn bản đi này' }); return; }
    const result = await outgoingDocRepository.sendToRecipients(docId, staffId);
    if (!result.success) {
      res.status(400).json({ success: false, message: result.message });
      return;
    }
    res.json({
      success: true,
      data: {
        message: result.message,
        internal_count: result.internal_count,
        external_count: result.external_count,
      },
    });
  } catch (error) {
    handleDbError(error, res);
  }
});
```

**Helper resolveAncestorUnit() đã có (line 632-640 area + import):** Verify name + signature bằng grep.

**Existing repo method sendToRecipients (line 223-227):**
```typescript
async sendToRecipients(docId: number, userId: number): Promise<DbResult & { internal_count?: number; external_count?: number }> {
  const row = await callFunctionOne<DbResult & { internal_count?: number; external_count?: number }>(
    'edoc.fn_outgoing_doc_send_to_recipients', [docId, userId]);
  return row ?? { success: false, message: 'Không thể gửi văn bản' };
}
```

**SP fn_outgoing_doc_send_to_recipients (schema line 27992-28074):** Sau commit, table state:
- `edoc.outgoing_doc_recipients` external rows: `sent_status='pending'`, `sent_at=NOW()`, `generated_lgsp_tracking_id=<id>`
- `edoc.lgsp_tracking` new rows: `status='pending'`, `direction='send'`, `outgoing_doc_id=<docId>`, `dest_org_code=<from inter_orgs>`, `dest_org_name`

→ Phase 34 enqueue query: `SELECT r.id AS recipient_id, r.generated_lgsp_tracking_id AS tracking_id FROM edoc.outgoing_doc_recipients r WHERE r.outgoing_doc_id = $1 AND r.recipient_type = 'external_org' AND r.generated_lgsp_tracking_id IS NOT NULL`.

**enqueueLgspSendJob signature (Plan 34-02 producer):**
```typescript
export async function enqueueLgspSendJob(data: LgspSendJobData): Promise<string>;
export interface LgspSendJobData {
  recipient_id: number;
  outgoing_doc_id: number;
  tracking_id: number;
  sender_unit_id: number;
  environment: 'sandbox' | 'prod';
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Repository — thêm method getExternalRecipientsForSend(docId)</name>
  <files>
    e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts
  </files>
  <action>
**Thêm method mới vào `outgoingDocRepository` const object** trong `e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts`. Vị trí: ngay sau `sendToRecipients()` (line 227).

Import `rawQuery` ở đầu file nếu chưa có (check existing imports):
```powershell
Select-String -Path e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts -Pattern "import.*rawQuery|from.*'.*db.*'"
```

Add method:

```typescript
  /**
   * Phase 34: Sau khi SP fn_outgoing_doc_send_to_recipients commit, lay danh sach
   * external recipient + tracking_id de route enqueue BullMQ send job (CONTEXT D-03).
   *
   * Chi tra ve recipient EXTERNAL co generated_lgsp_tracking_id (tuc SP da auto insert
   * lgsp_tracking row). Skip internal (sent through Phase 17 direct flow).
   *
   * @param docId outgoing_docs.id
   * @returns Array of { recipient_id, tracking_id } — empty array nếu không có external recipient
   */
  async getExternalRecipientsForSend(
    docId: number,
  ): Promise<Array<{ recipient_id: number; tracking_id: number }>> {
    const rows = await rawQuery<{ recipient_id: string | number; tracking_id: string | number }>(
      `SELECT r.id AS recipient_id,
              r.generated_lgsp_tracking_id AS tracking_id
       FROM edoc.outgoing_doc_recipients r
       WHERE r.outgoing_doc_id = $1
         AND r.recipient_type = 'external_org'
         AND r.generated_lgsp_tracking_id IS NOT NULL
         AND r.sent_status = 'pending'
       ORDER BY r.id`,
      [docId],
    );
    // CLAUDE.md pitfall #9: BIGINT → string trong pg driver — wrap Number()
    return rows.map((r) => ({
      recipient_id: Number(r.recipient_id),
      tracking_id: Number(r.tracking_id),
    }));
  },
```

Verify `rawQuery` import:
```powershell
Select-String -Path e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts -Pattern "rawQuery"
```

Nếu chưa import, add ở đầu file (cùng line với `callFunction`):
```typescript
import { callFunction, callFunctionOne, rawQuery } from '../lib/db/query.js';
```

(Verify path `../lib/db/query.js` bằng grep pattern hiện có trong file.)

**TypeScript compile:**
```powershell
cd e_office_app_new/backend
npx tsc --noEmit
# Exit code = 0
```
  </action>
  <verify>
    <automated>
      cd e_office_app_new/backend

      Select-String -Path src/repositories/outgoing-doc.repository.ts -Pattern "async getExternalRecipientsForSend"
      Select-String -Path src/repositories/outgoing-doc.repository.ts -Pattern "recipient_type = 'external_org'"
      Select-String -Path src/repositories/outgoing-doc.repository.ts -Pattern "generated_lgsp_tracking_id IS NOT NULL"
      Select-String -Path src/repositories/outgoing-doc.repository.ts -Pattern "rawQuery"

      npx tsc --noEmit
      # Exit code = 0
    </automated>
  </verify>
  <done>
    Method `getExternalRecipientsForSend(docId)` exported, query đúng filter `external_org + tracking_id IS NOT NULL + pending`, wrap Number() cho BIGINT cast, TS compile pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Route — extend POST /:id/gui-noi-bo enqueue logic sau SP success</name>
  <files>
    e_office_app_new/backend/src/routes/outgoing-doc.ts
  </files>
  <action>
**Bước 1: Add imports ở đầu `e_office_app_new/backend/src/routes/outgoing-doc.ts`:**

Tìm imports section + add:
```typescript
import { enqueueLgspSendJob } from '../lib/queue/lgsp-send-queue.js';
import pino from 'pino';

const lgspLogger = pino({ name: 'route-outgoing-doc-lgsp' });
```

(Verify `pino` import — file có thể đã có pino logger, reuse nếu có.)

**Bước 2: Verify helper `resolveAncestorUnit` đã có + signature:**

```powershell
Select-String -Path e_office_app_new/backend/src/routes/outgoing-doc.ts -Pattern "resolveAncestorUnit" -Context 0,3
```

Nếu helper trả về `Promise<number>` (departmentId → root unit_id) → use directly. Nếu helper khác signature → adjust.

Nếu helper KHÔNG ở file này (import từ shared helper), verify path. Nếu KHÔNG có → tạo inline:
```typescript
async function resolveSenderRootUnit(departmentId: number): Promise<number> {
  // Trace lên cây departments → root (parent_id IS NULL)
  const rows = await rawQuery<{ root_id: number | string }>(
    `WITH RECURSIVE chain AS (
       SELECT id, parent_id FROM public.departments WHERE id = $1
       UNION ALL
       SELECT d.id, d.parent_id FROM public.departments d
       JOIN chain c ON d.id = c.parent_id
     )
     SELECT id AS root_id FROM chain WHERE parent_id IS NULL LIMIT 1`,
    [departmentId],
  );
  if (!rows[0]) throw new Error(`Cannot resolve root unit cho department_id=${departmentId}`);
  return Number(rows[0].root_id);
}
```

(Nếu helper `resolveAncestorUnit` đã có thì DÙNG nó, KHÔNG tạo trùng.)

**Bước 3: REPLACE route handler `POST /:id/gui-noi-bo` (line 717-740):**

```typescript
// POST /:id/gui-noi-bo — Gửi tới recipients (auto-sinh incoming + lgsp_tracking + enqueue LGSP send job)
// Phase 34: sau khi SP commit, enqueue 1 BullMQ job per external recipient (CONTEXT D-02, D-03)
router.post('/:id/gui-noi-bo', async (req: Request, res: Response) => {
  try {
    const { staffId, departmentId, isAdmin } = (req as AuthRequest).user;
    const docId = Number(req.params.id);
    const loaded = await loadDocAndPerms(docId, { staffId, departmentId, isAdmin });
    if (!loaded) { res.status(404).json({ success: false, message: 'Không tìm thấy văn bản đi' }); return; }
    if (!loaded.perms.canSend) { res.status(403).json({ success: false, message: 'Không có quyền gửi văn bản đi này' }); return; }

    // 1. Goi SP — auto INSERT lgsp_tracking pending cho external recipient
    const result = await outgoingDocRepository.sendToRecipients(docId, staffId);
    if (!result.success) {
      res.status(400).json({ success: false, message: result.message });
      return;
    }

    // 2. Phase 34: enqueue LGSP send job cho moi external recipient (CONTEXT D-02, D-03)
    let enqueuedCount = 0;
    let enqueueErrors = 0;
    const externalCount = result.external_count ?? 0;
    if (externalCount > 0) {
      try {
        // Resolve sender root unit (trace user.departmentId -> ancestor)
        const senderUnitId = await resolveAncestorUnit(departmentId);
        const environment: 'sandbox' | 'prod' =
          (process.env.LGSP_DEFAULT_ENVIRONMENT === 'sandbox' ? 'sandbox' : 'prod');

        const recipients = await outgoingDocRepository.getExternalRecipientsForSend(docId);
        for (const r of recipients) {
          try {
            await enqueueLgspSendJob({
              recipient_id: r.recipient_id,
              outgoing_doc_id: docId,
              tracking_id: r.tracking_id,
              sender_unit_id: senderUnitId,
              environment,
            });
            enqueuedCount++;
          } catch (enqErr: unknown) {
            enqueueErrors++;
            const msg = enqErr instanceof Error ? enqErr.message : String(enqErr);
            lgspLogger.error(
              { docId, recipientId: r.recipient_id, trackingId: r.tracking_id, err: msg },
              'enqueueLgspSendJob failed — tracking remains pending',
            );
          }
        }
        lgspLogger.info(
          { docId, senderUnitId, environment, externalCount, enqueuedCount, enqueueErrors },
          'LGSP send jobs enqueued',
        );
      } catch (resolveErr: unknown) {
        const msg = resolveErr instanceof Error ? resolveErr.message : String(resolveErr);
        lgspLogger.error({ docId, departmentId, err: msg }, 'Failed to resolve sender unit / enqueue chain');
        // Don't fail user-facing response — tracking rows đã pending, admin Phase 37 retry sau.
      }
    }

    // 3. Response
    res.json({
      success: true,
      data: {
        message: result.message,
        internal_count: result.internal_count,
        external_count: externalCount,
        enqueued_count: enqueuedCount,
        ...(enqueueErrors > 0 ? { enqueue_errors: enqueueErrors } : {}),
      },
    });
  } catch (error) {
    handleDbError(error, res);
  }
});
```

**Critical notes:**
- Enqueue logic chỉ chạy nếu `external_count > 0` (tránh waste resolve unit cho all-internal VB)
- `resolveAncestorUnit(departmentId)` PHẢI dùng helper có sẵn — verify name + import path qua grep
- Per-recipient try/catch — 1 fail enqueue ko fail batch (CONTEXT D-02 spirit)
- Outer try/catch resolve+enqueue — Redis down → log + return success, KH thấy VB đã gửi (tracking pending)
- BIGINT cast Number(): repo đã wrap, route nhận đã type number
- Response thêm `enqueued_count` + optional `enqueue_errors` để UI biết
- Frontend Plan 34-04 polling 10s sẽ thấy tracking transition pending → success/error per worker process

**Bước 4: TypeScript compile:**

```powershell
cd e_office_app_new/backend
npx tsc --noEmit
# Exit code = 0
```
  </action>
  <verify>
    <automated>
      cd e_office_app_new/backend

      # Verify imports
      Select-String -Path src/routes/outgoing-doc.ts -Pattern "import { enqueueLgspSendJob }"

      # Verify enqueue logic + key shapes
      Select-String -Path src/routes/outgoing-doc.ts -Pattern "enqueueLgspSendJob\(\{"
      Select-String -Path src/routes/outgoing-doc.ts -Pattern "recipient_id: r.recipient_id"
      Select-String -Path src/routes/outgoing-doc.ts -Pattern "tracking_id: r.tracking_id"
      Select-String -Path src/routes/outgoing-doc.ts -Pattern "sender_unit_id: senderUnitId"
      Select-String -Path src/routes/outgoing-doc.ts -Pattern "environment"
      Select-String -Path src/routes/outgoing-doc.ts -Pattern "getExternalRecipientsForSend"
      Select-String -Path src/routes/outgoing-doc.ts -Pattern "enqueued_count"
      Select-String -Path src/routes/outgoing-doc.ts -Pattern "resolveAncestorUnit\(departmentId\)"

      # TS compile pass
      npx tsc --noEmit
      # Exit code = 0
    </automated>
  </verify>
  <done>
    Route POST /:id/gui-noi-bo extended với enqueue logic; resolve sender root unit + loop external recipients + per-recipient try/catch; response thêm enqueued_count; TS compile pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integration smoke test — trigger route + verify tracking rows + queue depth</name>
  <files>
    (không sửa file source — chỉ smoke test)
  </files>
  <action>
**Smoke test mục tiêu:** Verify route enqueue đúng số job theo external_count, KHÔNG cần worker run (worker test ở Plan 34-05).

**Bước 1: Build backend:**
```powershell
cd e_office_app_new/backend
$env:NODE_ENV='development'
npm run build 2>&1 | Select-Object -Last 20
```

**Bước 2: Smoke test bằng SQL trực tiếp + node script kiểm Redis queue:**

Vì test full HTTP request cần auth + setup user + VB ban hành, smoke test giới hạn:

(a) **SQL test:** Tạo tay 1 outgoing_docs + 2 external recipients (ngoài dev DB) HOẶC dùng existing VB nếu có. Skip — quá phức tạp cho integration smoke, defer Plan 34-05 E2E.

(b) **Pattern test:** Verify route handler compile + import resolves:
```powershell
cd e_office_app_new/backend
node -e "
import('./dist/lib/queue/lgsp-send-queue.js').then(m => {
  console.log('enqueueLgspSendJob:', typeof m.enqueueLgspSendJob);
  console.log('getLgspSendQueue:', typeof m.getLgspSendQueue);
});
import('./dist/repositories/outgoing-doc.repository.js').then(m => {
  console.log('getExternalRecipientsForSend:', typeof m.outgoingDocRepository.getExternalRecipientsForSend);
});
"
# Tất cả phải = function
```

(c) **Redis queue inspection script:** Tạo file tạm `e_office_app_new/backend/_phase34_03_inspect_queue.mjs`:

```javascript
import 'dotenv/config';
import IORedis from 'ioredis';

const r = new IORedis({
  host: process.env.REDIS_HOST || 'localhost',
  port: Number(process.env.REDIS_PORT) || 6379,
  password: process.env.REDIS_PASSWORD,
});

// BullMQ queue keys: bull:<queueName>:waiting/delayed/active/completed/failed
const keys = await r.keys('bull:lgsp-send:*');
console.log('lgsp-send queue Redis keys:', keys.length);
for (const k of keys.slice(0, 20)) {
  console.log(' -', k);
}

const waitingCount = await r.llen('bull:lgsp-send:waiting');
const delayedCount = await r.zcard('bull:lgsp-send:delayed');
console.log('waiting:', waitingCount, ' delayed:', delayedCount);

r.disconnect();
```

```powershell
cd e_office_app_new/backend
node _phase34_03_inspect_queue.mjs
# Initial run: keys=0 hoặc keys=meta-paused (queue chưa có job nào — OK)
```

(d) **Cleanup:**
```powershell
Remove-Item e_office_app_new/backend/_phase34_03_inspect_queue.mjs
```

**Bước 3: Verify route registration ko break:**

```powershell
cd e_office_app_new/backend
# Khởi động backend dev mode background terminal #N
$proc = Start-Process npm "run dev" -NoNewWindow -PassThru -RedirectStandardOutput "$env:TEMP/backend_start_34_03.log" -RedirectStandardError "$env:TEMP/backend_err_34_03.log"
Start-Sleep -Seconds 8
# Test health endpoint
$health = Invoke-WebRequest -Uri "http://localhost:4000/health" -ErrorAction SilentlyContinue
Write-Host "Health status: $($health.StatusCode)"
# Kill
Stop-Process -Id $proc.Id -Force
# Check log
Get-Content "$env:TEMP/backend_start_34_03.log" | Select-String -Pattern "started|listening|error" | Select -First 10
Get-Content "$env:TEMP/backend_err_34_03.log" | Select -First 20
```

Pass: backend khởi động thành công + health 200. Fail: có error log → fix trước.

**Bước 4: Document trong SUMMARY** rằng full E2E test sẽ ở Plan 34-05 (cần worker run + DN.001 sandbox active + admin tạo VB đi với attachment + bấm Gửi).
  </action>
  <verify>
    <automated>
      cd e_office_app_new/backend

      # Verify route + repo import chain OK
      npx tsc --noEmit
      # Exit code = 0

      # Verify dist build worked
      Test-Path dist/routes/outgoing-doc.js
      Test-Path dist/lib/queue/lgsp-send-queue.js
      Test-Path dist/repositories/outgoing-doc.repository.js

      # Verify exports cua repo method moi reachable
      node -e "import('./dist/repositories/outgoing-doc.repository.js').then(m => { if (typeof m.outgoingDocRepository.getExternalRecipientsForSend !== 'function') process.exit(1); }).catch(e => { console.error(e); process.exit(1); })"
      $LASTEXITCODE
      # Phải = 0
    </automated>
  </verify>
  <done>
    Backend build pass, route registration OK (health endpoint 200), method `getExternalRecipientsForSend` export reachable, Redis queue inspection ready cho Plan 34-05 E2E.
  </done>
</task>

</tasks>

<verification>
**Integration check sau 3 tasks (Wave 2 — route + repo wire):**

```powershell
cd e_office_app_new/backend

# 1. TypeScript compile pass
npx tsc --noEmit
# Exit = 0

# 2. Verify wire chain
node -e "
import('./dist/lib/queue/lgsp-send-queue.js').then(qm => {
  console.log('Producer queue:', qm.LGSP_SEND_QUEUE_NAME, qm.LGSP_SEND_JOB_NAME);
  console.log('Max attempts:', qm.LGSP_SEND_MAX_ATTEMPTS);
});
import('./dist/repositories/outgoing-doc.repository.js').then(rm => {
  console.log('Method:', typeof rm.outgoingDocRepository.getExternalRecipientsForSend);
});
"

# 3. Backend smoke start
$proc = Start-Process npm "run dev" -NoNewWindow -PassThru -RedirectStandardOutput "$env:TEMP/be_smoke.log"
Start-Sleep -Seconds 8
$health = (Invoke-WebRequest -Uri "http://localhost:4000/health" -UseBasicParsing -ErrorAction SilentlyContinue).StatusCode
Stop-Process -Id $proc.Id -Force
Write-Host "Health: $health"   # 200 = OK
```

**Pass criteria:** TS zero error + queue exports correct + backend starts (health 200) + no error in startup log.
</verification>

<success_criteria>
- [x] Repository: method `getExternalRecipientsForSend(docId)` thêm vào outgoingDocRepository
- [x] Repository: query đúng filter `recipient_type='external_org' + generated_lgsp_tracking_id IS NOT NULL + sent_status='pending'`
- [x] Repository: wrap Number() cast BIGINT (CLAUDE.md pitfall #9)
- [x] Route POST /:id/gui-noi-bo extend với enqueue logic sau SP success
- [x] Route: sender_unit_id qua `resolveAncestorUnit(departmentId)` (helper đã có) hoặc inline recursive query nếu helper missing
- [x] Route: environment default 'prod', override LGSP_DEFAULT_ENVIRONMENT='sandbox'
- [x] Route: per-recipient try/catch enqueue (1 fail ko fail batch)
- [x] Route: outer try/catch resolve+enqueue (Redis down → log + return success)
- [x] Response thêm `enqueued_count` + optional `enqueue_errors`
- [x] TypeScript compile zero error
- [x] Backend dev start OK (health 200)
</success_criteria>

<output>
After completion, create `.planning/phases/34-send-flow-sendedoc/34-03-SUMMARY.md` với:
- Files modified: 2 (outgoing-doc.repository.ts + outgoing-doc.ts route)
- Method mới: outgoingDocRepository.getExternalRecipientsForSend(docId)
- Route changes: extend POST /:id/gui-noi-bo enqueue 1 job per external recipient sau SP success
- Sender resolution: resolveAncestorUnit(departmentId) → root unit
- Environment: LGSP_DEFAULT_ENVIRONMENT env var override (default 'prod')
- Failure mode: per-recipient + outer try/catch, log + return success (tracking pending intact)
- Response shape: extended với enqueued_count + optional enqueue_errors
- TypeScript: PASS / 0 error
- Backend smoke start: health endpoint 200 verified
- Next: Plan 34-04 (frontend badge + polling hook)
- Note: E2E test với real worker + sandbox = Plan 34-05
</output>
