---
phase: 34-send-flow-sendedoc
plan: 04
type: execute
wave: 2
depends_on:
  - 34-01
  - 34-02
files_modified:
  - e_office_app_new/frontend/src/hooks/use-recipients-polling.ts
  - e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx
autonomous: true
requirements:
  - LGSP-SEND-06
tags: [frontend, ui, polling, badge, lgsp]

must_haves:
  truths:
    - "Hook `useRecipientsPolling(outgoingDocId, enabled)` poll `/api/van-ban-di/:id/noi-nhan` mỗi 10s (CONTEXT D-16)"
    - "Polling tự stop khi: tất cả recipient không pending HOẶC enabled=false HOẶC component unmount"
    - "Hook trả về `{ data, loading, hasPending, refetch }` để component render + force refresh"
    - "Badge state machine 4 state (CONTEXT D-17):"
    - "  internal_unit + sent → green 'Đã gửi nội bộ'"
    - "  external_org + sent_status=pending + lgsp_status=pending → orange 'Đang chờ LGSP'"
    - "  external_org + lgsp_status=success → green 'Đã gửi LGSP ✓'"
    - "  external_org + lgsp_status=error → red 'Lỗi LGSP: {lgsp_error_message}' + tooltip full error"
    - "Tooltip hover hiển thị: timestamp sent_at + full lgsp_error_message + lgsp_doc_id (nếu có)"
    - "Polling chạy CHỈ khi page chi tiết VB đi mở + có ít nhất 1 recipient pending"
    - "Tiếng Việt có dấu (CLAUDE.md project rule)"
    - "TypeScript compile pass"
  artifacts:
    - path: "e_office_app_new/frontend/src/hooks/use-recipients-polling.ts"
      provides: "Hook polling reusable cho future pages"
      min_lines: 70
    - path: "e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx"
      provides: "Extended panel recipients dùng hook + badge state machine 4 state"
      contains: "useRecipientsPolling"
  key_links:
    - from: "useRecipientsPolling hook"
      to: "GET /api/van-ban-di/:id/noi-nhan (existing endpoint Phase 19)"
      via: "setInterval 10s + clearInterval on unmount/stop"
      pattern: "React hook conditional polling (D-16)"
    - from: "Badge state computation"
      to: "{ recipient_type, sent_status, lgsp_status, lgsp_error_message } from /noi-nhan response"
      via: "Switch-case 4 state per row"
      pattern: "Pure function (testable)"
---

<objective>
Extend frontend page chi tiết VB đi (`van-ban-di/[id]/page.tsx`) với:
1. **Hook polling reusable** `useRecipientsPolling(outgoingDocId, enabled)` poll `/noi-nhan` mỗi 10s khi có recipient pending (CONTEXT D-16)
2. **Badge state machine 4 state** (CONTEXT D-17) per recipient với tooltip + color phân biệt

**Tiếng Việt có dấu** (CLAUDE.md project rule — globalrules + local rules).

**KHÔNG đụng** hidden-routes.ts (menu LGSP vẫn ẩn — Phase 37 unhide). Chỉ extend panel "Nơi nhận" hiện có.

Purpose: User văn thư bấm Gửi → response success external_count=N → page chi tiết hiển thị N badge "Đang chờ LGSP" orange → trong vòng 30-60s (worker process) badge transition green "Đã gửi LGSP ✓" hoặc red "Lỗi LGSP: ...". UX real-time không cần refresh page.

Output: 1 file mới (hook) + 1 file modify (page).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-send-flow-sendedoc/34-CONTEXT.md
@.planning/phases/34-send-flow-sendedoc/34-03-PLAN.md
@CLAUDE.md
@e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx
@e_office_app_new/frontend/src/lib/api.ts

<read_first>
- `e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx` (line 1-260+ — đọc structure: state, fetch functions, render panel "Nơi nhận")
- Existing `fetchNoiNhan` function (line 178) + `noiNhan` state shape (line 110-123)
- Existing render panel — tìm `noiNhan.map` để xác định vị trí render badge (grep)
- `e_office_app_new/frontend/src/lib/api.ts` — axios instance pattern
- `e_office_app_new/frontend/src/config/theme.ts` — color tokens (success/warning/error)
</read_first>

<interfaces>
**Existing noiNhan state shape (page.tsx line 110-123):**
```typescript
const [noiNhan, setNoiNhan] = useState<Array<{
  id: number;
  recipient_type: 'internal_unit' | 'external_org';
  recipient_unit_name: string | null;
  recipient_org_name: string | null;
  recipient_org_code: string | null;
  sent_at: string | null;
  sent_status: string;                 // 'pending' | 'sent'
  error_message: string | null;
  generated_incoming_doc_id: number | null;
  lgsp_doc_id: string | null;
  lgsp_status: string | null;          // 'pending' | 'processing' | 'success' | 'error'
  lgsp_error_message: string | null;
}>>([]);
```

**Existing endpoint:** `GET /api/van-ban-di/:id/noi-nhan` (route file line 484-521) — LEFT JOIN với `lgsp_tracking` đã trả đủ field cần. KHÔNG cần endpoint mới.

**Existing fetchNoiNhan (line 178):**
```typescript
const fetchNoiNhan = useCallback(async () => {
  try {
    const { data: res } = await api.get(`/van-ban-di/${docId}/noi-nhan`);
    setNoiNhan(res.data || []);
  } catch {}
}, [docId]);
```

**Hook plan (Phase 34 mới):**
```typescript
export interface RecipientStatus {
  id: number;
  recipient_type: 'internal_unit' | 'external_org';
  recipient_unit_name: string | null;
  recipient_org_name: string | null;
  recipient_org_code: string | null;
  sent_at: string | null;
  sent_status: string;
  error_message: string | null;
  generated_incoming_doc_id: number | null;
  lgsp_doc_id: string | null;
  lgsp_status: string | null;
  lgsp_error_message: string | null;
}

export function useRecipientsPolling(
  outgoingDocId: number,
  enabled: boolean,
  intervalMs: number = 10_000,
): {
  data: RecipientStatus[];
  loading: boolean;
  hasPending: boolean;
  refetch: () => Promise<void>;
};
```

**Badge state machine (CONTEXT D-17):**
| recipient_type | sent_status | lgsp_status | Label | Color |
|---|---|---|---|---|
| internal_unit | sent | (any) | "Đã gửi nội bộ" | green (#52c41a) |
| internal_unit | pending | (any) | "Chờ gửi nội bộ" | default (gray) |
| external_org | pending | pending | "Đang chờ LGSP" | orange (#fa8c16) |
| external_org | pending | processing | "Đang xử lý LGSP" | blue (#1677ff) |
| external_org | pending/sent | success | "Đã gửi LGSP ✓" | green (#52c41a) |
| external_org | pending/sent | error | "Lỗi LGSP: {msg}" | red (#f5222d) + tooltip |
| external_org | (any) | (null) | "Chờ enqueue" | default |

Use AntD `Tag` color prop (`success`, `warning`, `error`, `processing`, `default`) hoặc explicit hex.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tạo hook `useRecipientsPolling.ts`</name>
  <files>
    e_office_app_new/frontend/src/hooks/use-recipients-polling.ts
  </files>
  <action>
**Tạo NEW dir + file** `e_office_app_new/frontend/src/hooks/use-recipients-polling.ts`:

```powershell
# Tạo dir nếu chưa có
if (-not (Test-Path e_office_app_new/frontend/src/hooks)) { mkdir e_office_app_new/frontend/src/hooks }
```

```typescript
// ============================================================
// useRecipientsPolling — Phase 34 (CONTEXT D-16)
//
// Poll `/api/van-ban-di/:id/noi-nhan` mỗi `intervalMs` ms khi:
//   - enabled=true (page chi tiết VB đi đang mở)
//   - hasPending=true (còn ít nhất 1 recipient sent_status='pending' hoặc lgsp_status='pending')
//
// Stop khi: enabled=false / hasPending=false / component unmount.
//
// Trả về: { data, loading, hasPending, refetch } — component dùng `data` render badge state machine.
// ============================================================

import { useCallback, useEffect, useRef, useState } from 'react';
import { api } from '@/lib/api';

export interface RecipientStatus {
  id: number;
  recipient_type: 'internal_unit' | 'external_org';
  recipient_unit_name: string | null;
  recipient_org_name: string | null;
  recipient_org_code: string | null;
  sent_at: string | null;
  sent_status: string;
  error_message: string | null;
  generated_incoming_doc_id: number | null;
  lgsp_doc_id: string | null;
  lgsp_status: string | null;
  lgsp_error_message: string | null;
}

interface UseRecipientsPollingResult {
  data: RecipientStatus[];
  loading: boolean;
  hasPending: boolean;
  refetch: () => Promise<void>;
}

/**
 * Tính có recipient còn pending không.
 * "Pending" = sent_status='pending' HOẶC external + lgsp_status in {pending, processing}.
 */
function computeHasPending(rows: RecipientStatus[]): boolean {
  return rows.some((r) => {
    if (r.sent_status === 'pending') return true;
    if (r.recipient_type === 'external_org' && (r.lgsp_status === 'pending' || r.lgsp_status === 'processing')) return true;
    return false;
  });
}

export function useRecipientsPolling(
  outgoingDocId: number,
  enabled: boolean,
  intervalMs: number = 10_000,
): UseRecipientsPollingResult {
  const [data, setData] = useState<RecipientStatus[]>([]);
  const [loading, setLoading] = useState<boolean>(false);
  const [hasPending, setHasPending] = useState<boolean>(false);
  const intervalRef = useRef<ReturnType<typeof setInterval> | null>(null);

  const refetch = useCallback(async () => {
    if (!outgoingDocId || outgoingDocId <= 0) return;
    setLoading(true);
    try {
      const { data: res } = await api.get(`/van-ban-di/${outgoingDocId}/noi-nhan`);
      const rows: RecipientStatus[] = res?.data ?? [];
      setData(rows);
      setHasPending(computeHasPending(rows));
    } catch {
      // Silent — keep previous data, don't reset
    } finally {
      setLoading(false);
    }
  }, [outgoingDocId]);

  // Initial fetch khi enabled chuyển true
  useEffect(() => {
    if (enabled && outgoingDocId > 0) {
      refetch();
    }
  }, [enabled, outgoingDocId, refetch]);

  // Polling loop
  useEffect(() => {
    // Cleanup previous interval
    if (intervalRef.current) {
      clearInterval(intervalRef.current);
      intervalRef.current = null;
    }

    if (!enabled || !hasPending || outgoingDocId <= 0) {
      return;
    }

    // Setup interval
    intervalRef.current = setInterval(() => {
      refetch();
    }, intervalMs);

    return () => {
      if (intervalRef.current) {
        clearInterval(intervalRef.current);
        intervalRef.current = null;
      }
    };
  }, [enabled, hasPending, outgoingDocId, intervalMs, refetch]);

  return { data, loading, hasPending, refetch };
}
```

**Critical:**
- Polling chỉ start khi `enabled && hasPending && docId > 0` — tránh spam request cho page không cần
- Cleanup interval trong useEffect return — chống memory leak
- `hasPending` recompute mỗi lần `data` update — tự động stop polling khi all done
- Silent catch error — giữ data cũ, ko reset (UX tốt hơn nếu transient API fail)
- `refetch` export để component force refresh thủ công (VD: sau khi user bấm Gửi)
  </action>
  <verify>
    <automated>
      cd e_office_app_new/frontend

      Test-Path src/hooks/use-recipients-polling.ts
      (Get-Content src/hooks/use-recipients-polling.ts).Count
      # >= 70 lines

      Select-String -Path src/hooks/use-recipients-polling.ts -Pattern "export function useRecipientsPolling"
      Select-String -Path src/hooks/use-recipients-polling.ts -Pattern "export interface RecipientStatus"
      Select-String -Path src/hooks/use-recipients-polling.ts -Pattern "setInterval"
      Select-String -Path src/hooks/use-recipients-polling.ts -Pattern "clearInterval"
      Select-String -Path src/hooks/use-recipients-polling.ts -Pattern "lgsp_status"
      Select-String -Path src/hooks/use-recipients-polling.ts -Pattern "computeHasPending"

      npx tsc --noEmit
      # Exit code = 0
    </automated>
  </verify>
  <done>
    Hook `useRecipientsPolling` >= 70 dòng, polling 10s default, stop on hasPending=false / unmount / disabled, TS compile pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend page chi tiết VB đi — wire hook + render badge state machine 4 state</name>
  <files>
    e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx
  </files>
  <action>
**Bước 1: Add import ở đầu page** `e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx`:

```typescript
import { useRecipientsPolling, type RecipientStatus } from '@/hooks/use-recipients-polling';
```

**Bước 2: Replace state + fetch noiNhan với hook:**

(a) DELETE existing state declaration (line 110-123):
```typescript
const [noiNhan, setNoiNhan] = useState<Array<{ ... }>>([]);
```

(b) DELETE existing fetchNoiNhan callback (line 178):
```typescript
const fetchNoiNhan = useCallback(async () => { try { const { data: res } = await api.get(`/van-ban-di/${docId}/noi-nhan`); setNoiNhan(res.data || []); } catch {} }, [docId]);
```

(c) Replace với hook usage (gần cluster các state khác — line ~123):
```typescript
// Phase 34: hook polling 10s tracking recipients (CONTEXT D-16)
const {
  data: noiNhan,
  hasPending: noiNhanHasPending,
  refetch: refetchNoiNhan,
} = useRecipientsPolling(docId, !loading && !!doc, 10_000);
```

(d) Update `Promise.all` initial fetch (line 184) — REMOVE fetchNoiNhan() call (hook tự handle):
```typescript
// TRƯỚC:
Promise.all([fetchDoc(), fetchAttachments(), fetchRecipients(), fetchNoiNhan(), fetchHistory(), fetchLeaderNotes(), fetchBookmarkStatus()]).finally(() => setLoading(false));
// SAU:
Promise.all([fetchDoc(), fetchAttachments(), fetchRecipients(), fetchHistory(), fetchLeaderNotes(), fetchBookmarkStatus()]).finally(() => setLoading(false));
```

Và remove `fetchNoiNhan` khỏi deps array.

(e) Tìm chỗ gọi `fetchNoiNhan()` thủ công (sau Gửi nội bộ thành công, sau sửa recipients) — REPLACE bằng `refetchNoiNhan()`:

```powershell
Select-String -Path e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "fetchNoiNhan"
```

Replace tất cả `fetchNoiNhan` → `refetchNoiNhan` (hook tự handle polling start sau đó).

**Bước 3: Tạo helper function `getBadgeForRecipient(r)` ở đầu component (sau formatters):**

Thêm gần line ~96 (sau `maskPhone` function):

```typescript
// Phase 34: Badge state machine 4 state cho recipient (CONTEXT D-17)
interface BadgeInfo {
  label: string;
  color: 'success' | 'warning' | 'error' | 'processing' | 'default' | 'blue';
  tooltip?: string;
}

function getBadgeForRecipient(r: RecipientStatus): BadgeInfo {
  // Internal — flow Phase 17 v3.0
  if (r.recipient_type === 'internal_unit') {
    if (r.sent_status === 'sent') {
      return { label: 'Đã gửi nội bộ', color: 'success', tooltip: r.sent_at ? `Đã gửi lúc ${dayjs(r.sent_at).format('DD/MM/YYYY HH:mm')}` : undefined };
    }
    return { label: 'Chờ gửi nội bộ', color: 'default' };
  }
  // External — flow Phase 34 LGSP
  if (r.recipient_type === 'external_org') {
    if (r.lgsp_status === 'success') {
      const tip = [
        r.sent_at ? `Đã gửi lúc ${dayjs(r.sent_at).format('DD/MM/YYYY HH:mm')}` : null,
        r.lgsp_doc_id ? `LGSP docId: ${r.lgsp_doc_id}` : null,
      ].filter(Boolean).join('\n');
      return { label: 'Đã gửi LGSP ✓', color: 'success', tooltip: tip || undefined };
    }
    if (r.lgsp_status === 'error') {
      const errMsg = r.lgsp_error_message || r.error_message || 'Lỗi không xác định';
      return {
        label: `Lỗi LGSP: ${errMsg.length > 40 ? errMsg.slice(0, 40) + '…' : errMsg}`,
        color: 'error',
        tooltip: errMsg,
      };
    }
    if (r.lgsp_status === 'processing') {
      return { label: 'Đang xử lý LGSP', color: 'processing' };
    }
    if (r.lgsp_status === 'pending') {
      return { label: 'Đang chờ LGSP', color: 'warning' };
    }
    // lgsp_status null — chưa enqueue (tracking row chưa có hoặc đang transition)
    return { label: 'Chờ enqueue', color: 'default' };
  }
  return { label: 'Không xác định', color: 'default' };
}
```

**Bước 4: Update render panel "Nơi nhận" (tìm `noiNhan.map`):**

```powershell
Select-String -Path e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "noiNhan\.map" -Context 5,15
```

Find pattern hiện tại render row recipient. Replace badge section dùng `getBadgeForRecipient(r)`. Pattern mong muốn:

```tsx
{noiNhan.map((r) => {
  const badge = getBadgeForRecipient(r);
  const name = r.recipient_type === 'internal_unit'
    ? (r.recipient_unit_name || '—')
    : (r.recipient_org_name || '—') + (r.recipient_org_code ? ` (${r.recipient_org_code})` : '');
  return (
    <div key={r.id} style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', padding: '8px 12px', borderBottom: '1px solid #f0f0f0' }}>
      <div>
        <Text strong>{name}</Text>
        {r.sent_at && <Text type="secondary" style={{ marginLeft: 8, fontSize: 12 }}>{fmtDateTime(r.sent_at)}</Text>}
      </div>
      {badge.tooltip ? (
        <Tooltip title={badge.tooltip}>
          <Tag color={badge.color}>{badge.label}</Tag>
        </Tooltip>
      ) : (
        <Tag color={badge.color}>{badge.label}</Tag>
      )}
    </div>
  );
})}
```

**Imports Tooltip nếu chưa có:**
```powershell
Select-String -Path e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "Tooltip"
```

Nếu không có, add vào import `from 'antd'` ở line ~9:
```typescript
import { ..., Tooltip } from 'antd';
```

**Bước 5: Polling indicator (subtle UX — small dot khi đang poll):**

Add trong panel "Nơi nhận" header (cạnh title):
```tsx
{noiNhanHasPending && (
  <Tooltip title="Đang theo dõi trạng thái gửi LGSP, tự cập nhật mỗi 10 giây">
    <Badge status="processing" text="Đang theo dõi" style={{ fontSize: 12, marginLeft: 8 }} />
  </Tooltip>
)}
```

**Bước 6: Update API response handling sau bấm "Gửi nội bộ":**

Tìm hàm gọi `/gui-noi-bo` (grep `gui-noi-bo`):
```powershell
Select-String -Path e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "gui-noi-bo" -Context 1,10
```

Add message hiển thị `enqueued_count` khi external > 0:
```tsx
const res = await api.post(`/van-ban-di/${docId}/gui-noi-bo`);
const { internal_count, external_count, enqueued_count } = res.data?.data || {};
if (external_count && external_count > 0) {
  message.success(`Đã gửi ${internal_count} đơn vị nội bộ + đang gửi ${enqueued_count}/${external_count} cơ quan ngoài qua LGSP (theo dõi tại "Nơi nhận")`);
} else {
  message.success(res.data?.data?.message || `Đã gửi ${internal_count} đơn vị nội bộ`);
}
refetchNoiNhan();   // Force refetch để badge transition pending hiển thị ngay
```

**Bước 7: TypeScript compile + build check:**

```powershell
cd e_office_app_new/frontend
npx tsc --noEmit 2>&1 | Select-String -Pattern "error" -Context 0,3 | Select-Object -First 30
# Pre-existing 4 TS2345 lỗi từ Phase 33-05 deferred (HSCV + 3 VB pages TreeNode) — KHÔNG counted.
# CHỈ count error MỚI từ van-ban-di/[id]/page.tsx hoặc hooks/use-recipients-polling.ts
```

Nếu có lỗi MỚI trong 2 file mục tiêu → fix. Lỗi pre-existing trong file khác → SKIP (CLAUDE.md SCOPE BOUNDARY rule).
  </action>
  <verify>
    <automated>
      cd e_office_app_new/frontend

      # Verify hook usage + state replaced
      Select-String -Path src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "useRecipientsPolling"
      Select-String -Path src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "getBadgeForRecipient"
      Select-String -Path src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "refetchNoiNhan"

      # Verify state cũ đã remove
      $oldState = Select-String -Path src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "const \[noiNhan, setNoiNhan\]"
      if ($oldState) { Write-Error "State noiNhan cũ vẫn còn — phải thay bằng hook"; exit 1 }

      # Verify 4 state label hiện diện
      Select-String -Path src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "Đã gửi nội bộ"
      Select-String -Path src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "Đang chờ LGSP"
      Select-String -Path src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "Đã gửi LGSP"
      Select-String -Path src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "Lỗi LGSP"

      # Verify Tooltip imported
      Select-String -Path src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "Tooltip"

      # TypeScript pass — chỉ verify 2 file mới/sửa, ignore pre-existing
      npx tsc --noEmit 2>&1 | Tee-Object -FilePath "$env:TEMP/fe_tsc_34_04.log"
      $newErrors = Select-String -Path "$env:TEMP/fe_tsc_34_04.log" -Pattern "van-ban-di/\[id\]/page\.ts|use-recipients-polling\.ts"
      if ($newErrors) {
        Write-Host "TS errors trong file Phase 34 thay đổi:"
        $newErrors | Format-List
        exit 1
      } else {
        Write-Host "TS OK — không có lỗi mới trong van-ban-di/[id]/page.tsx hoặc use-recipients-polling.ts"
      }
    </automated>
  </verify>
  <done>
    Page wire hook polling, 4 state badge render với tooltip, refetchNoiNhan thay fetchNoiNhan, no NEW TS error (pre-existing TS2345 từ Phase 33-05 deferred ignored), tiếng Việt có dấu.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Frontend production build verify + manual visual check note</name>
  <files>
    (không sửa file — chỉ verify)
  </files>
  <action>
**Bước 1: Production build frontend (CLAUDE.md pitfall #2 — `Remove-Item Env:NODE_ENV` TRƯỚC next build):**

```powershell
cd e_office_app_new/frontend
Remove-Item Env:NODE_ENV -ErrorAction SilentlyContinue
npm run build 2>&1 | Tee-Object -FilePath "$env:TEMP/fe_build_34_04.log" | Select-Object -Last 40
```

Pass criteria: exit code 0 + build output show ".next" generated. Fail: TS error MỚI từ Phase 34 file → fix.

**Bước 2: Document manual visual check trong SUMMARY** (sẽ thực tế chạy trong Plan 34-05 E2E):

```
Manual visual check (defer Plan 34-05 E2E):
1. Đăng nhập user văn thư DN.001 (sandbox active)
2. Tạo VB đi với recipients mix: 2 internal + 1 external
3. Bấm "Gửi nội bộ"
4. Mở chi tiết VB → panel "Nơi nhận":
   - 2 row internal: badge green "Đã gửi nội bộ"
   - 1 row external: badge orange "Đang chờ LGSP"
   - Header panel: "Đang theo dõi" badge
5. Đợi 30-60s (worker process)
6. Badge external transition → green "Đã gửi LGSP ✓" (success) hoặc red "Lỗi LGSP: ..." (fail)
7. Tooltip hover badge → hiển thị timestamp + lgsp_doc_id (success) hoặc full error_message (fail)
8. Sau tất cả done → "Đang theo dõi" badge biến mất → polling stop
```

**Bước 3: Verify menu LGSP VẪN ẨN (Phase 34 KHÔNG đụng hidden-routes.ts — CLAUDE.md customer-facing scope):**

```powershell
Select-String -Path e_office_app_new/frontend/src/config/hidden-routes.ts -Pattern "lgsp"
# Phải vẫn match `/lgsp` + `/lgsp/co-quan`
```

**Bước 4: Quick sanity check — không có console.log dư:**

```powershell
Select-String -Path e_office_app_new/frontend/src/hooks/use-recipients-polling.ts -Pattern "console\."
Select-String -Path e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx -Pattern "console\.log" -CaseSensitive
# Nếu có → fix (chỉ pino backend mới được phép log structured)
```
  </action>
  <verify>
    <automated>
      cd e_office_app_new/frontend

      Remove-Item Env:NODE_ENV -ErrorAction SilentlyContinue
      $build = npm run build 2>&1 | Out-String
      Write-Host ($build | Select-Object -Last 30)
      # Verify build success — search 'Compiled successfully' or '.next/build-manifest.json' exists
      Test-Path .next/build-manifest.json

      # Verify menu LGSP VẪN ẨN (Phase 37 sẽ unhide)
      Select-String -Path src/config/hidden-routes.ts -Pattern "'/lgsp'"
      Select-String -Path src/config/hidden-routes.ts -Pattern "'/lgsp/co-quan'"

      # No console.log dư
      $consoleNew = Select-String -Path src/hooks/use-recipients-polling.ts -Pattern "console\."
      if ($consoleNew) { Write-Warning "console.log dư trong hook"; }
    </automated>
  </verify>
  <done>
    Frontend production build PASS (.next generated); hidden-routes.ts unchanged (menu LGSP vẫn ẩn); không có console.log dư.
  </done>
</task>

</tasks>

<verification>
**Integration check sau 3 tasks (Wave 2 — frontend):**

```powershell
cd e_office_app_new/frontend

# 1. TypeScript pass (ignore pre-existing errors per scope boundary)
npx tsc --noEmit 2>&1 | Tee-Object -FilePath "$env:TEMP/fe_tsc_final.log"
$newErrs = Select-String -Path "$env:TEMP/fe_tsc_final.log" -Pattern "van-ban-di/\[id\]/page\.ts|use-recipients-polling\.ts" -Context 0,2
if ($newErrs) { Write-Error "TS errors in Phase 34 files"; exit 1 }
Write-Host "TS pass — no new errors in Phase 34 modified files"

# 2. Production build
Remove-Item Env:NODE_ENV -ErrorAction SilentlyContinue
npm run build 2>&1 | Select-Object -Last 30

# 3. Verify file structure
Test-Path src/hooks/use-recipients-polling.ts
Test-Path src/app/(main)/van-ban-di/[id]/page.tsx

# 4. Verify hidden-routes unchanged
Select-String -Path src/config/hidden-routes.ts -Pattern "'/lgsp'"
# Phải còn match — Phase 37 mới unhide
```

**Pass criteria:** No new TS error + production build success + menu LGSP vẫn ẩn.
</verification>

<success_criteria>
- [x] Hook `useRecipientsPolling.ts` >= 70 dòng với setInterval/clearInterval + auto stop on hasPending=false
- [x] Hook export `RecipientStatus` interface khớp endpoint response
- [x] Page wire hook (replace state + fetchNoiNhan + refetch usages)
- [x] Badge state machine 4 state implement (internal sent/pending + external pending/processing/success/error)
- [x] Tiếng Việt có dấu cho tất cả label badge + tooltip + success message
- [x] Tooltip render full error_message (no truncate) + timestamp + lgsp_doc_id
- [x] "Đang theo dõi" badge ở header panel khi `hasPending=true`
- [x] Success message sau Gửi nội bộ show enqueued_count nếu external > 0
- [x] TypeScript compile no NEW error (pre-existing TS2345 từ Phase 33-05 deferred ignored)
- [x] Production build PASS (.next generated)
- [x] hidden-routes.ts KHÔNG đụng (menu LGSP vẫn ẩn — Phase 37 unhide)
</success_criteria>

<output>
After completion, create `.planning/phases/34-send-flow-sendedoc/34-04-SUMMARY.md` với:
- Files created: 1 (`hooks/use-recipients-polling.ts`)
- Files modified: 1 (page `van-ban-di/[id]/page.tsx`)
- Hook polling: 10s default, stop on hasPending=false / disabled / unmount
- Badge state machine: 4+ states phủ internal/external, tooltip cho timestamp + lgsp_doc_id + error_message
- Success message Gửi nội bộ extended: hiển thị enqueued_count khi external > 0
- TypeScript: no NEW error (pre-existing Phase 33-05 deferred ignored)
- Production build: PASS
- Hidden routes: NOT modified (menu LGSP vẫn ẩn — Phase 37 unhide)
- Manual visual check spec documented for Plan 34-05 E2E
- Next: Plan 34-05 (E2E sandbox test + verification toàn phase 34)
</output>
