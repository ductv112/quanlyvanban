---
phase: 260424-w4u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/database/seed/001_required_data.sql
  - e_office_app_new/backend/src/lib/permissions/drafting-doc.ts
  - e_office_app_new/backend/src/routes/drafting-doc.ts
  - e_office_app_new/backend/src/repositories/drafting-doc.repository.ts
  - e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx
autonomous: true
requirements:
  - PERM-DRAFT-01
  - PERM-DRAFT-02
  - PERM-DRAFT-03
  - PERM-DRAFT-04
  - PERM-DRAFT-05
user_setup: []

must_haves:
  truths:
    - "Admin (isAdmin=true) có đủ 5 quyền: canEdit, canApprove, canRelease, canSend, canRetract"
    - "Lãnh đạo (position.is_leader=TRUE) cùng đơn vị (unit_id = doc.unit_id) có canApprove + canRelease + canSend + canRetract"
    - "Nhân viên xử lý VB (position.is_handle_document=TRUE) cùng đơn vị HOẶC người soạn (staff_id = doc.drafting_user_id) có canEdit"
    - "Recipient khác đơn vị (unit_id != doc.unit_id) VÀ không phải người soạn VÀ không phải admin có 5 permission đều FALSE"
    - "Position GD/PGD/TP/PTP có is_leader=TRUE, is_handle_document=TRUE trong DB sau seed fix"
    - "Position CV có is_leader=FALSE, is_handle_document=TRUE"
    - "Position VT có is_leader=FALSE, is_handle_document=FALSE"
    - "Không hard-code role/position name trong backend code — chỉ đọc flag is_leader/is_handle_document + ownership + isAdmin"
    - "7 endpoint mutation trên drafting-doc trả 403 khi permission tương ứng = false"
    - "GET /:id response body có field data.permissions với 5 boolean"
    - "Frontend detail page chỉ render button khi permissions tương ứng = true (Sửa/Duyệt/Từ chối/Xóa/Phát hành/Gửi/Hủy duyệt/Thu hồi)"
  artifacts:
    - path: "e_office_app_new/database/seed/001_required_data.sql"
      provides: "UPDATE idempotent position flag sau INSERT positions"
      contains: "UPDATE public.positions SET is_leader = TRUE, is_handle_document = TRUE WHERE code IN"
    - path: "e_office_app_new/backend/src/lib/permissions/drafting-doc.ts"
      provides: "computeDraftingPermissions + DocInfo + DocPermissionContext + DraftingPermissions types"
      exports: ["computeDraftingPermissions", "DocInfo", "DocPermissionContext", "DraftingPermissions"]
      min_lines: 50
    - path: "e_office_app_new/backend/src/routes/drafting-doc.ts"
      provides: "7 mutation guard + GET /:id enrich permissions"
      contains: "computeDraftingPermissions"
    - path: "e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx"
      provides: "Conditional render action buttons dựa permissions"
      contains: "doc.permissions?.canEdit"
  key_links:
    - from: "e_office_app_new/backend/src/routes/drafting-doc.ts"
      to: "e_office_app_new/backend/src/lib/permissions/drafting-doc.ts"
      via: "import computeDraftingPermissions"
      pattern: "from '../lib/permissions/drafting-doc"
    - from: "computeDraftingPermissions"
      to: "public.staff + public.positions JOIN"
      via: "rawQuery SELECT is_leader, is_handle_document FROM staff s LEFT JOIN positions p"
      pattern: "FROM public\\.staff.*LEFT JOIN public\\.positions"
    - from: "frontend van-ban-du-thao/[id]/page.tsx"
      to: "doc.permissions (từ GET /:id response)"
      via: "conditional render `{doc.permissions?.canXxx && ...}`"
      pattern: "doc\\.permissions\\?\\.can"
---

<objective>
Fix permission logic cho module VB dự thảo — chuyển từ mô hình cũ (hard-code role name hoặc mở rộng) sang mô hình **v2 capability-based** dùng flag `position.is_leader` + `position.is_handle_document` + ownership + `isAdmin`.

Purpose: 3 bug hiện tại — (1) bất kỳ user đăng nhập nào cũng thấy nút Duyệt/Phát hành, (2) recipient khác đơn vị có thể xóa/sửa VB không phải của mình, (3) không có cách mở rộng khi thêm chức vụ mới. Mô hình v2 giải quyết cả 3 bằng flag trên DB — không cần code theo chức vụ.

Output:
- Seed file idempotent cập nhật flag vĩnh viễn cho production
- 1 helper backend compute permissions tái sử dụng
- 7 endpoint mutation + 1 endpoint detail được thêm permission guard
- Frontend detail page chỉ render nút khi user thực sự có quyền
- Smoke test 4 scenario pass bằng curl
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@e_office_app_new/backend/src/middleware/auth.ts
@e_office_app_new/backend/src/routes/drafting-doc.ts
@e_office_app_new/backend/src/repositories/drafting-doc.repository.ts
@e_office_app_new/backend/src/lib/department-subtree.ts
@e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx

<interfaces>
<!-- Key types the executor MUST use directly. Verified from codebase. -->

From backend/src/lib/auth/jwt.ts — TokenPayload (JWT user shape):
```typescript
export interface TokenPayload extends JWTPayload {
  staffId: number;
  departmentId: number;          // ⚠ Đây là department_id, KHÔNG phải unit_id
  username: string;
  roles: string[];
  isAdmin: boolean;
}
```

⚠ **LƯU Ý QUAN TRỌNG:** TokenPayload **KHÔNG có field `unitId`** — khác với task_context ban đầu mô tả. Do đó helper `computeDraftingPermissions` phải resolve `unitId` từ `departmentId` bằng `resolveAncestorUnit(departmentId)` (đã có sẵn tại `backend/src/lib/department-subtree.ts`).

From backend/src/lib/department-subtree.ts:
```typescript
export async function resolveAncestorUnit(departmentId: number): Promise<number>;
```

From backend/src/lib/db/query.ts:
```typescript
export function callFunctionOne<T>(name: string, params: unknown[]): Promise<T | null>;
export function rawQuery<T>(sql: string, params: unknown[]): Promise<T[]>;
```

From SP edoc.fn_drafting_doc_get_by_id — TRẢ VỀ các field quan trọng cho permission:
- `id BIGINT, unit_id INT, drafting_user_id INT, approver VARCHAR` (tên — **KHÔNG có approver_id**)

⚠ **approver_id KHÔNG TỒN TẠI:** Bảng `edoc.drafting_docs` chỉ có cột `approver VARCHAR(200)` lưu **tên người duyệt**, không có FK `approver_id`. Do đó rule `canRetract` **bỏ nhánh "approver_id === staffId"** — chỉ giữ 2 nhánh: `isAdmin || (sameUnit && is_leader)`. Drafter cũng không thu hồi được theo nghiệp vụ (thu hồi là quyền lãnh đạo).

From backend/src/repositories/drafting-doc.repository.ts — DraftingDocDetailRow:
```typescript
export interface DraftingDocDetailRow {
  id: number;
  unit_id: number;
  drafting_user_id: number;
  approver: string;              // Tên người duyệt (VARCHAR), không phải ID
  // ... các field khác
}
```

Staff table schema:
```
public.staff: id, department_id, unit_id, position_id, full_name, is_admin, ...
public.positions: id, name, code, is_leader BOOLEAN, is_handle_document BOOLEAN, sort_order
```

Test account mapping (từ seed 002 — đã chạy):
- admin (id=1, unit=1, dept=1, pos=1/GD) — isAdmin=true
- nguyenvana (id=2, unit=2, dept=2, pos=1/GD)
- tranthib (id=3, unit=3, dept=3, pos=1/GD)
- levand (id=4, unit=4, dept=4, pos=1/GD)
</interfaces>

</context>

<constraints>
- ❌ **KHÔNG auto-commit** sau khi tasks xong — chỉ commit khi user yêu cầu rõ ràng.
- ❌ **KHÔNG hard-code** role name ('Quản trị hệ thống', 'Ban Lãnh đạo') hoặc position name ('Giám đốc', 'Trưởng phòng') trong code permission. CHỈ dùng: `isAdmin` + `staff.position_id -> positions.is_leader/is_handle_document` + ownership (`staff.id = doc.drafting_user_id`) + same-unit (`staff.unit_id = doc.unit_id`).
- ❌ **KHÔNG kiểm tra `user.roles.includes(...)`** — roles là feature khác (menu RBAC), không dùng cho doc permission.
- ❌ **KHÔNG dùng trường `unitId`** từ `AuthRequest.user` — TokenPayload không có field này. Phải resolve qua `resolveAncestorUnit(departmentId)`.
- ❌ **KHÔNG check `approver_id === staffId`** trong canRetract — cột này không tồn tại trong `edoc.drafting_docs`.
- ✅ Seed file phải **idempotent** (UPDATE ... WHERE code IN ...) để production deploy an toàn.
- ✅ Apply UPDATE vào DB dev trực tiếp (qua `docker exec psql`) để test ngay trong phiên — KHÔNG reset full DB.
- ✅ Backend compute permissions server-side, frontend chỉ đọc và render conditionally — **không duplicate logic** ở frontend.
- ✅ 7 endpoint mutation trả `403` với message tiếng Việt có dấu khi permission fail.
- ✅ Verify bằng curl ít nhất 4 scenario (admin full / leader same-unit / handler same-unit / recipient khác unit) trước khi claim done.
- ✅ Test response body `GET /:id` phải có `data.permissions` với 5 boolean.
- ✅ File `e_office_app_new/frontend/CLAUDE.md` nhắc: "This is NOT the Next.js you know" — nếu executor viết Next.js code mới thì **PHẢI đọc `node_modules/next/dist/docs/`** trước. Trong plan này chỉ edit JSX có sẵn (không dùng Next.js API mới) nên không vướng.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Seed flag is_leader/is_handle_document cho positions (DB + seed file)</name>
  <files>e_office_app_new/database/seed/001_required_data.sql</files>
  <read_first>
    - Đọc lại seed file section "1. Positions" (line 30-39) để biết vị trí INSERT
    - Đọc CLAUDE.md section "DB Migration Strategy" để confirm seed phải idempotent
  </read_first>
  <action>
Bước 1 — Edit `e_office_app_new/database/seed/001_required_data.sql`:

Ngay sau dòng `SELECT setval('public.positions_id_seq', 10, true);` (dòng 39), thêm 2 UPDATE idempotent:

```sql
-- ─── Flag quyền theo chức vụ (idempotent, production-safe) ───
-- Lãnh đạo: được duyệt/phát hành/thu hồi/gửi VB
UPDATE public.positions
   SET is_leader = TRUE, is_handle_document = TRUE
 WHERE code IN ('GD', 'PGD', 'TP', 'PTP');

-- Nhân viên xử lý VB (sửa/xóa VB dự thảo cùng đơn vị)
UPDATE public.positions
   SET is_handle_document = TRUE
 WHERE code = 'CV';

-- Văn thư: giữ is_leader=FALSE, is_handle_document=FALSE (chỉ vào sổ + đọc)
-- Không cần UPDATE — default FALSE
```

Bước 2 — Apply trực tiếp vào DB dev để test ngay (không chờ reset full DB):

```bash
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
  UPDATE public.positions SET is_leader = TRUE, is_handle_document = TRUE WHERE code IN ('GD', 'PGD', 'TP', 'PTP');
  UPDATE public.positions SET is_handle_document = TRUE WHERE code = 'CV';
"
```

Bước 3 — Verify kết quả (phải match bảng Expected bên dưới):

```bash
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c \
  "SELECT code, name, is_leader, is_handle_document FROM public.positions ORDER BY id;"
```

Expected output:
```
 code | name              | is_leader | is_handle_document
------+-------------------+-----------+---------------------
 GD   | Giám đốc          | t         | t
 PGD  | Phó Giám đốc      | t         | t
 TP   | Trưởng phòng      | t         | t
 PTP  | Phó Trưởng phòng  | t         | t
 CV   | Chuyên viên       | f         | t
 VT   | Văn thư           | f         | f
```

Ngoài `code IN (...)`, **KHÔNG** update vị trí khác. Nếu có position custom khách hàng thêm → admin tự chỉnh qua UI (out of scope).
  </action>
  <verify>
    <automated>docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT COUNT(*) FROM public.positions WHERE code IN ('GD','PGD','TP','PTP') AND is_leader = TRUE AND is_handle_document = TRUE" | grep -q "^4$" && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT COUNT(*) FROM public.positions WHERE code = 'CV' AND is_leader = FALSE AND is_handle_document = TRUE" | grep -q "^1$" && docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT COUNT(*) FROM public.positions WHERE code = 'VT' AND is_leader = FALSE AND is_handle_document = FALSE" | grep -q "^1$" && grep -q "UPDATE public.positions" e_office_app_new/database/seed/001_required_data.sql && grep -q "is_leader = TRUE, is_handle_document = TRUE" e_office_app_new/database/seed/001_required_data.sql && echo OK</automated>
  </verify>
  <done>
    - DB dev có 4 row (GD/PGD/TP/PTP) với is_leader=TRUE, is_handle_document=TRUE
    - DB dev có 1 row CV với is_leader=FALSE, is_handle_document=TRUE
    - DB dev có 1 row VT với is_leader=FALSE, is_handle_document=FALSE
    - seed file `001_required_data.sql` chứa 2 block UPDATE idempotent sau `SELECT setval('public.positions_id_seq', ...)`
    - Re-apply seed file 2 lần không báo lỗi, không tạo duplicate
  </done>
</task>

<task type="auto">
  <name>Task 2: Tạo backend permission helper computeDraftingPermissions</name>
  <files>e_office_app_new/backend/src/lib/permissions/drafting-doc.ts</files>
  <read_first>
    - Đọc `e_office_app_new/backend/src/lib/db/query.ts` (signature `callFunctionOne`, `rawQuery`)
    - Đọc `e_office_app_new/backend/src/lib/department-subtree.ts` (signature `resolveAncestorUnit`)
    - Đọc `e_office_app_new/backend/src/lib/auth/jwt.ts` (TokenPayload — xác nhận KHÔNG có unitId)
    - Đọc CLAUDE.md section "Naming Patterns" + "Import Organization"
  </read_first>
  <action>
Tạo file mới `e_office_app_new/backend/src/lib/permissions/drafting-doc.ts` với nội dung:

```typescript
import { rawQuery } from '../db/query.js';
import { resolveAncestorUnit } from '../department-subtree.js';

export interface DocPermissionContext {
  staffId: number;
  departmentId: number;
  isAdmin: boolean;
}

export interface DocInfo {
  id: number;
  drafting_user_id: number | null;
  unit_id: number;
}

export interface DraftingPermissions {
  canEdit: boolean;
  canApprove: boolean;
  canRelease: boolean;
  canSend: boolean;
  canRetract: boolean;
}

interface StaffPositionRow {
  is_leader: boolean;
  is_handle_document: boolean;
}

/**
 * Query position flag của staff — cache tối ưu ở caller nếu gọi nhiều lần per request.
 * Trả FALSE cho cả 2 flag nếu staff không tồn tại hoặc chưa gán position.
 */
async function getStaffPosition(staffId: number): Promise<StaffPositionRow> {
  const rows = await rawQuery<StaffPositionRow>(
    `SELECT COALESCE(p.is_leader, FALSE) AS is_leader,
            COALESCE(p.is_handle_document, FALSE) AS is_handle_document
       FROM public.staff s
       LEFT JOIN public.positions p ON p.id = s.position_id
      WHERE s.id = $1`,
    [staffId],
  );
  return rows[0] ?? { is_leader: false, is_handle_document: false };
}

/**
 * Mô hình v2 capability-based permission cho VB dự thảo.
 *
 * Rules (KHÔNG hard-code role/position name — chỉ flag + ownership + admin):
 *   canEdit    = isAdmin || isDrafter || (sameUnit && is_handle_document)
 *   canApprove = isAdmin || (sameUnit && is_leader)
 *   canRelease = isAdmin || (sameUnit && is_leader)
 *   canSend    = isAdmin || isDrafter || (sameUnit && is_leader)
 *   canRetract = isAdmin || (sameUnit && is_leader)
 *
 * LƯU Ý: Bảng edoc.drafting_docs không có cột approver_id (chỉ có approver VARCHAR
 * lưu tên) — do đó canRetract bỏ nhánh "staffId === approver_id".
 */
export async function computeDraftingPermissions(
  user: DocPermissionContext,
  doc: DocInfo,
): Promise<DraftingPermissions> {
  if (user.isAdmin) {
    return {
      canEdit: true,
      canApprove: true,
      canRelease: true,
      canSend: true,
      canRetract: true,
    };
  }

  // Resolve unit_id của user từ departmentId (JWT không có unitId)
  const userUnitId = await resolveAncestorUnit(user.departmentId);

  const pos = await getStaffPosition(user.staffId);
  const sameUnit = userUnitId === doc.unit_id;
  const isDrafter = doc.drafting_user_id != null && user.staffId === doc.drafting_user_id;
  const isLeader = sameUnit && pos.is_leader;
  const isHandler = sameUnit && pos.is_handle_document;

  return {
    canEdit: isDrafter || isHandler,
    canApprove: isLeader,
    canRelease: isLeader,
    canSend: isDrafter || isLeader,
    canRetract: isLeader,
  };
}
```

**Lưu ý code style:**
- Extension `.js` trong import (ESM requirement — xem CLAUDE.md "Import Organization")
- 2 spaces indent, single quote, semicolons
- Export `const`/`interface` — không dùng class
- Không throw — trả FALSE an toàn nếu staff không tồn tại
  </action>
  <verify>
    <automated>cd e_office_app_new/backend && npx tsc --noEmit && grep -q "computeDraftingPermissions" src/lib/permissions/drafting-doc.ts && grep -q "is_leader" src/lib/permissions/drafting-doc.ts && grep -q "is_handle_document" src/lib/permissions/drafting-doc.ts && grep -q "resolveAncestorUnit" src/lib/permissions/drafting-doc.ts && ! grep -qi "Giám đốc\|Trưởng phòng\|'Ban Lãnh đạo'\|'Quản trị" src/lib/permissions/drafting-doc.ts && echo OK</automated>
  </verify>
  <done>
    - File `e_office_app_new/backend/src/lib/permissions/drafting-doc.ts` tồn tại, export `computeDraftingPermissions`, `DocInfo`, `DocPermissionContext`, `DraftingPermissions`
    - Code không hard-code role/position name (grep "Giám đốc|Trưởng phòng|Ban Lãnh đạo|Quản trị" = 0 match)
    - `tsc --noEmit` pass
    - Helper query `public.staff LEFT JOIN public.positions` + dùng `resolveAncestorUnit`
    - Admin early return { all true }
  </done>
</task>

<task type="auto">
  <name>Task 3: Thêm permission guard vào 7 endpoint mutation + enrich GET /:id</name>
  <files>e_office_app_new/backend/src/routes/drafting-doc.ts</files>
  <read_first>
    - Đọc lại `e_office_app_new/backend/src/routes/drafting-doc.ts` (line 243-327 cho PUT/DELETE/GET detail; line 483-567 cho duyet/huy-duyet/tu-choi/thu-hoi/phat-hanh; line 439-460 cho gui)
    - Đọc `e_office_app_new/backend/src/repositories/drafting-doc.repository.ts` line 170-172 để confirm `getById(id, staffId)` return `DraftingDocDetailRow | null` có field `id`, `unit_id`, `drafting_user_id`
    - Task 2 output — helper file vừa tạo xong
  </read_first>
  <action>
Edit `e_office_app_new/backend/src/routes/drafting-doc.ts`:

**Bước 1 — Thêm import ở đầu file (sau các import hiện có):**

```typescript
import { computeDraftingPermissions, type DocPermissionContext } from '../lib/permissions/drafting-doc.js';
```

**Bước 2 — Tạo helper internal trong cùng file (đặt sau các import, trước `const router = Router();`):**

```typescript
/**
 * Load doc + compute permissions. Trả null nếu doc không tồn tại.
 * Dùng cho 7 mutation endpoint.
 */
async function loadDocAndPerms(
  docId: number,
  userCtx: DocPermissionContext,
) {
  const doc = await draftingDocRepository.getById(docId, userCtx.staffId);
  if (!doc) return null;
  const perms = await computeDraftingPermissions(userCtx, {
    id: doc.id,
    drafting_user_id: doc.drafting_user_id ?? null,
    unit_id: doc.unit_id,
  });
  return { doc, perms };
}
```

**Bước 3 — Enrich endpoint `GET /:id` (hiện line 244-261):**

Sau khi build xong `doc` object (đã có `rej` merge), thêm block compute permissions rồi gắn vào response:

```typescript
router.get('/:id', async (req: Request, res: Response) => {
  try {
    const { staffId, departmentId, isAdmin } = (req as AuthRequest).user;
    const id = Number(req.params.id);
    const doc = await draftingDocRepository.getById(id, staffId);
    if (!doc) {
      res.status(404).json({ success: false, message: 'Không tìm thấy văn bản dự thảo' });
      return;
    }
    const rej = await rawQuery<{ rejected_by: number | null; rejection_reason: string | null }>(
      `SELECT rejected_by, rejection_reason FROM edoc.drafting_docs WHERE id = $1`, [id]
    );
    if (rej[0]) { (doc as any).rejected_by = rej[0].rejected_by; (doc as any).rejection_reason = rej[0].rejection_reason; }

    // Compute permissions và gắn vào response
    const permissions = await computeDraftingPermissions(
      { staffId, departmentId, isAdmin },
      { id: doc.id, drafting_user_id: doc.drafting_user_id ?? null, unit_id: doc.unit_id },
    );
    (doc as any).permissions = permissions;

    res.json({ success: true, data: doc });
  } catch (error) {
    handleDbError(error, res);
  }
});
```

**Bước 4 — Thêm guard cho 7 endpoint mutation.** Mapping:

| Route | Method | Permission |
|---|---|---|
| `PUT /:id` | canEdit | Line 264-312 |
| `DELETE /:id` | canEdit | Line 315-327 |
| `POST /:id/gui` | canSend | Line 439-460 |
| `PATCH /:id/duyet` | canApprove | Line 483-498 |
| `PATCH /:id/huy-duyet` | canApprove | Line 500-512 |
| `PATCH /:id/tu-choi` | canApprove | Line 514-527 |
| `POST /:id/thu-hoi` | canRetract | Line 529-546 |
| `POST /:id/phat-hanh` | canRelease | Line 548-567 |

Với **mỗi** endpoint, thêm đoạn sau phần extract `{ staffId }` + parse `docId`, TRƯỚC khi gọi repository method. Pattern chuẩn:

```typescript
router.patch('/:id/duyet', async (req: Request, res: Response) => {
  try {
    const { staffId, departmentId, isAdmin } = (req as AuthRequest).user;
    const docId = Number(req.params.id);

    // Permission guard
    const loaded = await loadDocAndPerms(docId, { staffId, departmentId, isAdmin });
    if (!loaded) {
      res.status(404).json({ success: false, message: 'Không tìm thấy văn bản dự thảo' });
      return;
    }
    if (!loaded.perms.canApprove) {
      res.status(403).json({ success: false, message: 'Không có quyền duyệt văn bản này' });
      return;
    }

    // Existing logic (giữ nguyên)
    const staffRows = await rawQuery<{ full_name: string }>('SELECT full_name FROM public.staff WHERE id = $1', [staffId]);
    const approverName = staffRows[0]?.full_name || `Staff #${staffId}`;
    const result = await draftingDocRepository.approve(docId, staffId, approverName);
    if (!result.success) {
      res.status(400).json({ success: false, message: result.message });
      return;
    }
    res.json({ success: true, data: { message: result.message } });
  } catch (error) {
    handleDbError(error, res);
  }
});
```

Áp dụng pattern trên cho **cả 8 endpoint** (7 mutation + GET detail). Vietnamese message cho 403:

| Permission | Message |
|---|---|
| canEdit | `'Không có quyền sửa văn bản này'` / `'Không có quyền xóa văn bản này'` |
| canApprove | `'Không có quyền duyệt văn bản này'` / `'Không có quyền từ chối văn bản này'` / `'Không có quyền hủy duyệt văn bản này'` |
| canRelease | `'Không có quyền phát hành văn bản này'` |
| canSend | `'Không có quyền gửi văn bản này'` |
| canRetract | `'Không có quyền thu hồi văn bản này'` |

**LƯU Ý 1 — `POST /` (tạo mới):** KHÔNG cần guard — bất kỳ user nào cũng được tạo dự thảo. Skip.

**LƯU Ý 2 — GET list (`/`) + các GET phụ (`/nguoi-nhan`, `/lich-su`, `/dinh-kem`, `/y-kien`, `/danh-sach-gui`):** KHÔNG cần guard — chỉ đọc, logic filter theo deptIds đã handle ở SP.

**LƯU Ý 3 — `POST /:id/y-kien`, `DELETE /:id/y-kien/:noteId`, `POST /:id/danh-dau`:** KHÔNG block — bất kỳ user đã nhận được VB (recipient) đều được thêm ý kiến / bookmark cá nhân.

**LƯU Ý 4 — Endpoint upload/download file (`POST /:id/dinh-kem`, `DELETE /:id/dinh-kem/:id`):** Scope out — nếu user yêu cầu sau thì mở rộng sau. Plan này KHÔNG đụng vào 3 endpoint attachment.
  </action>
  <verify>
    <automated>cd e_office_app_new/backend && npx tsc --noEmit && grep -q "loadDocAndPerms" src/routes/drafting-doc.ts && grep -q "computeDraftingPermissions" src/routes/drafting-doc.ts && grep -c "canApprove\|canRelease\|canSend\|canRetract\|canEdit" src/routes/drafting-doc.ts | awk '$1 >= 7 { exit 0 } { exit 1 }' && grep -c "Không có quyền" src/routes/drafting-doc.ts | awk '$1 >= 7 { exit 0 } { exit 1 }' && grep -q "permissions: permissions\|(doc as any).permissions = permissions" src/routes/drafting-doc.ts && echo OK</automated>
  </verify>
  <done>
    - File `src/routes/drafting-doc.ts` import `computeDraftingPermissions` + có helper `loadDocAndPerms`
    - 7 endpoint mutation (PUT, DELETE, gui, duyet, huy-duyet, tu-choi, thu-hoi, phat-hanh) đều có block `if (!loaded.perms.canXxx) res.status(403)...`
    - `GET /:id` response có field `data.permissions` với 5 boolean
    - `tsc --noEmit` pass
    - Backend start không lỗi (kiểm tra bằng `npm run dev` chạy 5s)
  </done>
</task>

<task type="auto">
  <name>Task 4: Conditional render action buttons trong frontend detail page</name>
  <files>e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx</files>
  <read_first>
    - Đọc lại `e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx` line 26-45 (interface DocDetail) và line 221-258 (button section)
    - Đọc frontend/CLAUDE.md + AGENTS.md — xác nhận KHÔNG dùng API Next.js mới (chỉ edit JSX có sẵn)
  </read_first>
  <action>
Edit `e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx`:

**Bước 1 — Extend interface `DocDetail` (line 26-41).** Thêm field optional `permissions`:

```typescript
interface DocDetail {
  id: number; unit_id: number; number: number; sub_number: string;
  notation: string; abstract: string;
  publish_date: string; signer: string; sign_date: string;
  doc_book_id: number; doc_type_id: number; doc_field_id: number;
  secret_id: number; urgent_id: number; number_paper: number; number_copies: number;
  expired_date: string; recipients: string; approver: string; approved: boolean;
  is_handling: boolean; archive_status: boolean;
  drafting_unit_id: number; drafting_user_id: number; publish_unit_id: number;
  is_released: boolean; released_date: string; reject_reason: string;
  received_date: string; document_code: string;
  drafting_unit_name: string; drafting_user_name: string; publish_unit_name: string;
  created_by: number; created_at: string; updated_by: number; updated_at: string;
  doc_book_name: string; doc_type_name: string; doc_type_code: string;
  doc_field_name: string; created_by_name: string; is_read: boolean;
  permissions?: {
    canEdit: boolean;
    canApprove: boolean;
    canRelease: boolean;
    canSend: boolean;
    canRetract: boolean;
  };
}
```

**Bước 2 — Conditional render action buttons.** Block hiện tại (line 221-258):

```tsx
<Space wrap>
  <Button icon={isBookmarked ? ... /> {/* Bookmark: GIỮ NGUYÊN — không cần guard */}

  {/* Chưa duyệt, chưa phát hành */}
  {!doc.approved && !doc.is_released && (
    <>
      <Button icon={<EditOutlined />} onClick={...}>Sửa</Button>
      <Button type="primary" icon={<CheckCircleOutlined />} onClick={handleApprove}>Duyệt</Button>
      <Dropdown menu={{ items: [
        { key: 'reject', ..., label: 'Từ chối', danger: true, onClick: ... },
        { type: 'divider' },
        { key: 'delete', ..., label: 'Xóa văn bản', danger: true, onClick: handleDelete },
      ] }}>
        <Button icon={<MoreOutlined />} />
      </Dropdown>
    </>
  )}

  {/* Đã duyệt, chưa phát hành */}
  {doc.approved && !doc.is_released && (
    <>
      <Button ... >Phát hành</Button>
      <Button ... >Gửi</Button>
      <Dropdown menu={{ items: [
        { key: 'unapprove', ..., label: 'Hủy duyệt', onClick: handleUnapprove },
        { key: 'retract', ..., label: 'Thu hồi', onClick: handleRetract },
      ] }}>
        <Button icon={<MoreOutlined />} />
      </Dropdown>
    </>
  )}
</Space>
```

Sửa thành (áp dụng permission guard cho mỗi button/item):

```tsx
<Space wrap>
  <Button icon={isBookmarked ? <StarFilled style={{ color: '#faad14' }} /> : <StarOutlined />} onClick={handleToggleBookmark} />

  {/* Chưa duyệt, chưa phát hành: Sửa, Duyệt, Từ chối, Xóa */}
  {!doc.approved && !doc.is_released && (
    <>
      {doc.permissions?.canEdit && (
        <Button icon={<EditOutlined />} onClick={() => router.push(`/van-ban-du-thao?edit=${doc.id}`)}>Sửa</Button>
      )}
      {doc.permissions?.canApprove && (
        <Button type="primary" icon={<CheckCircleOutlined />} onClick={handleApprove}>Duyệt</Button>
      )}
      {(doc.permissions?.canApprove || doc.permissions?.canEdit) && (() => {
        const items = [];
        if (doc.permissions?.canApprove && !(doc as any).rejected_by) {
          items.push({ key: 'reject', icon: <StopOutlined />, label: 'Từ chối', danger: true, onClick: () => { setRejectReason(''); setRejectModalOpen(true); } });
        }
        if (doc.permissions?.canEdit) {
          if (items.length > 0) items.push({ type: 'divider' as const });
          items.push({ key: 'delete', icon: <DeleteOutlined />, label: 'Xóa văn bản', danger: true, onClick: handleDelete });
        }
        return items.length > 0 ? (
          <Dropdown menu={{ items }}>
            <Button icon={<MoreOutlined />} />
          </Dropdown>
        ) : null;
      })()}
    </>
  )}

  {/* Đã duyệt, chưa phát hành: Phát hành, Gửi, Hủy duyệt, Thu hồi */}
  {doc.approved && !doc.is_released && (
    <>
      {doc.permissions?.canRelease && (
        <Button type="primary" style={{ background: '#52c41a', borderColor: '#52c41a' }} icon={<RocketOutlined />} onClick={handleRelease}>Phát hành</Button>
      )}
      {doc.permissions?.canSend && (
        <Button type="primary" icon={<SendOutlined />} onClick={openSendModal}>Gửi</Button>
      )}
      {(doc.permissions?.canApprove || doc.permissions?.canRetract) && (() => {
        const items = [];
        if (doc.permissions?.canApprove) {
          items.push({ key: 'unapprove', icon: <CloseCircleOutlined />, label: 'Hủy duyệt', onClick: handleUnapprove });
        }
        if (doc.permissions?.canRetract) {
          items.push({ key: 'retract', icon: <RollbackOutlined />, label: 'Thu hồi', onClick: handleRetract });
        }
        return items.length > 0 ? (
          <Dropdown menu={{ items }}>
            <Button icon={<MoreOutlined />} />
          </Dropdown>
        ) : null;
      })()}
    </>
  )}

  {/* Released: view only */}
  {doc.is_released && (
    <Tag color="success" style={{ fontSize: 13, padding: '4px 12px' }}>Đã phát hành ngày {fmtDate(doc.released_date)}</Tag>
  )}
</Space>
```

**Bước 3 — Ẩn nút "Thêm file" và "Xóa file" attachment khi không canEdit:**

Line 356: `{!doc.is_released && ...}` → thay bằng `{!doc.is_released && doc.permissions?.canEdit && ...}`
Line 391: `{!doc.is_released && <Popconfirm title="Xóa file?"...}` → thay bằng `{!doc.is_released && doc.permissions?.canEdit && <Popconfirm title="Xóa file?"...}`

(LƯU Ý: backend chưa guard 3 endpoint attachment trong plan này — frontend hide UI để tránh user confuse; nếu user cố gọi API thì backend trả 200 — đây là tech debt có thể đóng sau.)

**Bước 4 — KHÔNG đụng vào input leader notes (line 452-455).** Bất kỳ recipient cũng được thêm ý kiến — đó là flow nghiệp vụ đúng.
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx tsc --noEmit && grep -q "doc.permissions?.canEdit" "src/app/(main)/van-ban-du-thao/[id]/page.tsx" && grep -q "doc.permissions?.canApprove" "src/app/(main)/van-ban-du-thao/[id]/page.tsx" && grep -q "doc.permissions?.canRelease" "src/app/(main)/van-ban-du-thao/[id]/page.tsx" && grep -q "doc.permissions?.canSend" "src/app/(main)/van-ban-du-thao/[id]/page.tsx" && grep -q "doc.permissions?.canRetract" "src/app/(main)/van-ban-du-thao/[id]/page.tsx" && grep -q "permissions?:" "src/app/(main)/van-ban-du-thao/[id]/page.tsx" && echo OK</automated>
  </verify>
  <done>
    - Interface `DocDetail` có field `permissions?` với 5 boolean
    - 5 button chính (Sửa/Duyệt/Phát hành/Gửi) + 3 dropdown item (Từ chối/Xóa/Hủy duyệt/Thu hồi) đều được wrap conditional bằng `doc.permissions?.canXxx`
    - Nút "Thêm file" + "Xóa file" cũng check `canEdit`
    - `tsc --noEmit` frontend pass
    - Next.js dev server start không lỗi build
  </done>
</task>

<task type="auto">
  <name>Task 5: Smoke test 4 scenario bằng curl + report kết quả</name>
  <files>(no file edits — test only)</files>
  <read_first>
    - Xác nhận Task 1-4 đã done (seed flag DB + helper + routes + frontend)
    - Confirm backend restart tự động (tsx watch) sau Task 3
  </read_first>
  <action>
**Bước 1 — Chuẩn bị: tìm 1 drafting doc ID có sẵn để test.**

```bash
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c \
  "SELECT id, unit_id, drafting_user_id, approved, is_released FROM edoc.drafting_docs WHERE is_released = FALSE AND approved = FALSE ORDER BY id LIMIT 5;"
```

Ghi lại 1 ID có `unit_id = 1` (UBND) — dùng làm `DOC_ID` cho tests. Nếu không có → tạo bằng admin trước:

```bash
# Login admin, tạo 1 VB dự thảo test (fallback)
TOKEN_ADMIN=$(curl -s -X POST http://localhost:4000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"Admin@123"}' \
  | python -c "import sys,json; print(json.load(sys.stdin)['data']['accessToken'])")
```

**Bước 2 — Scenario 1: Admin (full quyền) — kỳ vọng 5 TRUE.**

```bash
TOKEN_ADMIN=$(curl -s -X POST http://localhost:4000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"Admin@123"}' \
  | python -c "import sys,json; print(json.load(sys.stdin)['data']['accessToken'])")

curl -s -H "Authorization: Bearer $TOKEN_ADMIN" \
  "http://localhost:4000/api/van-ban-du-thao/$DOC_ID" \
  | python -c "import sys,json; d=json.load(sys.stdin); print('admin:', d['data'].get('permissions'))"
```

**Expected:** `admin: {'canEdit': True, 'canApprove': True, 'canRelease': True, 'canSend': True, 'canRetract': True}`

**Bước 3 — Scenario 2: Leader cùng unit với doc (ví dụ `nguyenvana` nếu doc.unit_id=2) — kỳ vọng canApprove/canRelease/canSend/canRetract = TRUE, canEdit = TRUE (vì GD là is_handle_document=TRUE).**

```bash
TOKEN_LEADER=$(curl -s -X POST http://localhost:4000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"nguyenvana","password":"Admin@123"}' \
  | python -c "import sys,json; print(json.load(sys.stdin)['data']['accessToken'])")
```

Chọn `DOC_ID_SAMEUNIT` thuộc `unit_id = 2` (Sở Nội vụ):
```bash
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c \
  "SELECT id FROM edoc.drafting_docs WHERE unit_id = 2 AND approved = FALSE AND is_released = FALSE LIMIT 1;"
```

```bash
curl -s -H "Authorization: Bearer $TOKEN_LEADER" \
  "http://localhost:4000/api/van-ban-du-thao/$DOC_ID_SAMEUNIT" \
  | python -c "import sys,json; d=json.load(sys.stdin); print('nguyenvana SAME unit:', d['data'].get('permissions'))"
```

**Expected:** `{'canEdit': True, 'canApprove': True, 'canRelease': True, 'canSend': True, 'canRetract': True}`

**Bước 4 — Scenario 3: Recipient khác unit với doc (nguyenvana nhìn doc unit_id=1 của UBND) — kỳ vọng 5 FALSE (vì không phải admin, không cùng unit, không phải drafter).**

```bash
# Dùng DOC_ID từ Bước 1 (unit_id = 1, UBND) — nguyenvana unit = 2
curl -s -H "Authorization: Bearer $TOKEN_LEADER" \
  "http://localhost:4000/api/van-ban-du-thao/$DOC_ID" \
  | python -c "import sys,json; d=json.load(sys.stdin); print('nguyenvana CROSS unit:', d['data'].get('permissions'))"
```

**Expected:** `{'canEdit': False, 'canApprove': False, 'canRelease': False, 'canSend': False, 'canRetract': False}`

(LƯU Ý: nếu nguyenvana không được gửi doc này → API trả 404. Đó cũng OK — permission là FALSE implicit.)

**Bước 5 — Scenario 4: Test 403 guard — nguyenvana cố approve doc cross-unit:**

```bash
curl -s -o /dev/null -w "%{http_code}" \
  -X PATCH -H "Authorization: Bearer $TOKEN_LEADER" \
  "http://localhost:4000/api/van-ban-du-thao/$DOC_ID/duyet"
```

**Expected:** `403`

```bash
curl -s -X PATCH -H "Authorization: Bearer $TOKEN_LEADER" \
  "http://localhost:4000/api/van-ban-du-thao/$DOC_ID/duyet" \
  | python -c "import sys,json; d=json.load(sys.stdin); print(d.get('message'))"
```

**Expected:** `Không có quyền duyệt văn bản này`

**Bước 6 — Scenario 5: Test 403 phat-hanh cross-unit:**

```bash
curl -s -o /dev/null -w "%{http_code}" \
  -X POST -H "Authorization: Bearer $TOKEN_LEADER" \
  "http://localhost:4000/api/van-ban-du-thao/$DOC_ID/phat-hanh"
# Expected: 403
```

**Bước 7 — Scenario 6: Test 403 delete cross-unit:**

```bash
curl -s -o /dev/null -w "%{http_code}" \
  -X DELETE -H "Authorization: Bearer $TOKEN_LEADER" \
  "http://localhost:4000/api/van-ban-du-thao/$DOC_ID"
# Expected: 403
```

**Bước 8 — Report:** Tổng hợp kết quả 6 scenario vào message trả user:

```
## Smoke Test Results

| # | Scenario | Expected | Actual | Pass |
|---|---|---|---|---|
| 1 | admin GET /:id permissions | 5 TRUE | {...} | ✓ |
| 2 | nguyenvana same-unit GET permissions | 5 TRUE | {...} | ✓ |
| 3 | nguyenvana cross-unit GET permissions | 5 FALSE | {...} | ✓ |
| 4 | nguyenvana cross-unit PATCH duyet | 403 | 403 | ✓ |
| 5 | nguyenvana cross-unit POST phat-hanh | 403 | 403 | ✓ |
| 6 | nguyenvana cross-unit DELETE | 403 | 403 | ✓ |
```

**Nếu bất kỳ scenario fail → STOP, report user, không claim done.**
  </action>
  <verify>
    <automated>TOKEN_ADMIN=$(curl -s -X POST http://localhost:4000/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"Admin@123"}' | python -c "import sys,json; print(json.load(sys.stdin)['data']['accessToken'])"); DOC_ID=$(docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT id FROM edoc.drafting_docs ORDER BY id LIMIT 1"); RESULT=$(curl -s -H "Authorization: Bearer $TOKEN_ADMIN" "http://localhost:4000/api/van-ban-du-thao/$DOC_ID" | python -c "import sys,json; d=json.load(sys.stdin); p=d.get('data',{}).get('permissions',{}); print('OK' if all([p.get('canEdit'),p.get('canApprove'),p.get('canRelease'),p.get('canSend'),p.get('canRetract')]) else 'FAIL')"); [ "$RESULT" = "OK" ] && echo OK</automated>
  </verify>
  <done>
    - Scenario 1 (admin) — GET /:id trả về permissions với 5 TRUE
    - Scenario 2 (leader same-unit) — GET /:id trả 5 TRUE (hoặc canEdit=true, 4 còn lại tùy position GD)
    - Scenario 3 (cross-unit) — GET /:id trả 5 FALSE HOẶC 404 (recipient không được gửi)
    - Scenario 4-6 — 3 mutation endpoint cross-unit đều trả HTTP 403 với message tiếng Việt có dấu
    - Report table điền đủ 6 dòng Pass
  </done>
</task>

</tasks>

<verification>
## Phase-level checks

```bash
# 1. Backend TS check
cd e_office_app_new/backend && npx tsc --noEmit

# 2. Frontend TS check
cd e_office_app_new/frontend && npx tsc --noEmit

# 3. Seed idempotent (apply 2 lần liên tục — phải zero error)
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
  -c "SET app.signing_secret_key='qlvb-signing-dev-key-change-production-2026';" \
  -f - < e_office_app_new/database/seed/001_required_data.sql
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
  -c "SET app.signing_secret_key='qlvb-signing-dev-key-change-production-2026';" \
  -f - < e_office_app_new/database/seed/001_required_data.sql

# 4. No hard-coded role/position name in permission helper
grep -i "'Giám đốc'\|'Trưởng phòng'\|'Ban Lãnh đạo'\|'Quản trị hệ thống'" \
  e_office_app_new/backend/src/lib/permissions/drafting-doc.ts
# Expected: no match

# 5. Position flag post-seed state
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c \
  "SELECT code, name, is_leader, is_handle_document FROM public.positions ORDER BY id;"

# 6. Smoke test 3 endpoint key (admin 200 + leader 200 + recipient 403)
# (Task 5 đã chi tiết)
```
</verification>

<success_criteria>
1. Seed file `001_required_data.sql` có UPDATE idempotent, DB dev match expected state (6 position rows đúng flag)
2. Backend helper `drafting-doc.ts` exist, export 4 thứ (`computeDraftingPermissions`, `DocInfo`, `DocPermissionContext`, `DraftingPermissions`), grep "Giám đốc|Trưởng phòng|Ban Lãnh đạo" = 0 match
3. Route file có 7 guard mutation + 1 enrich GET detail, `tsc --noEmit` pass
4. Frontend page có interface extension + 8 conditional wrap, `tsc --noEmit` pass
5. Smoke test 6 scenario pass (admin 5T / leader-same-unit 5T / cross-unit 5F / 3x403)
6. KHÔNG auto-commit
</success_criteria>

<output>
Sau khi tất cả 5 task done, trả về user:
- Tóm tắt 5 task status (✓/✗)
- Table smoke test 6 scenario
- Files modified count
- Lời nhắc: "Không auto-commit — user review xong chạy `/gsd-commit` hoặc git commit thủ công."
</output>
