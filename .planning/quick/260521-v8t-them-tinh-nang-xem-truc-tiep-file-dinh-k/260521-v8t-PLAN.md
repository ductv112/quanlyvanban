---
phase: 260521-v8t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/backend/src/lib/office-converter.ts
  - e_office_app_new/backend/src/lib/attachment-preview.ts
  - e_office_app_new/backend/.env.example
  - e_office_app_new/backend/src/routes/incoming-doc.ts
  - e_office_app_new/backend/src/routes/outgoing-doc.ts
  - e_office_app_new/backend/src/routes/drafting-doc.ts
  - e_office_app_new/backend/src/routes/handling-doc.ts
  - e_office_app_new/frontend/src/lib/preview.ts
  - e_office_app_new/frontend/src/components/AttachmentPreviewModal.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx
  - e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx
  - deploy/MANUAL_UPDATE_PROD.md
autonomous: true
requirements: [QUICK-PREVIEW-01]

must_haves:
  truths:
    - "User mở chi tiết VB đến / VB đi / VB dự thảo / HSCV và bấm icon Mắt cạnh file đính kèm thì Modal mở ra hiển thị file trực tiếp trong browser, KHÔNG cần tải xuống"
    - "PDF, ảnh (jpg/png/gif/webp), text/plain hiển thị inline ngay trong iframe/img"
    - "File Office (doc/docx/xls/xlsx/ppt/pptx) được convert sang PDF tại backend qua LibreOffice headless và stream lại browser"
    - "MIME không hỗ trợ (zip, rar...) → Modal hiển thị thông báo 'Loại file không hỗ trợ xem trực tiếp' + nút Tải xuống fallback"
    - "Lần convert sau cho cùng attachment_id dùng cache MinIO previews/{id}.pdf (không re-convert)"
    - "Backend endpoint preview yêu cầu auth giống endpoint download hiện có — không phá CORS / quyền truy cập"
    - "Toast/Modal tiếng Việt có dấu đầy đủ"
  artifacts:
    - path: "e_office_app_new/backend/src/lib/office-converter.ts"
      provides: "convertOfficeToPdf(filePath, attachmentId) — spawn soffice headless, upload preview lên MinIO, trả về preview path. Cache check trước."
      exports: ["convertOfficeToPdf", "isOfficeMime", "OFFICE_MIME_TYPES"]
    - path: "e_office_app_new/backend/src/lib/attachment-preview.ts"
      provides: "handleAttachmentPreview(res, params) — branch theo MIME → stream PDF/image/text inline hoặc convert Office → stream"
      exports: ["handleAttachmentPreview", "isPreviewableMime"]
    - path: "e_office_app_new/frontend/src/components/AttachmentPreviewModal.tsx"
      provides: "Modal full-screen hiển thị preview: iframe (PDF/Office-converted), img (image), pre (text)"
    - path: "e_office_app_new/frontend/src/lib/preview.ts"
      provides: "buildPreviewUrl(module, docId, attachmentId) + isPreviewableMime + getPreviewKind"
      exports: ["buildPreviewUrl", "isPreviewableMime", "getPreviewKind"]
  key_links:
    - from: "Frontend [id]/page.tsx (4 module)"
      to: "AttachmentPreviewModal"
      via: "EyeOutlined button onClick → set state {open:true, url, fileName, mimeType}"
      pattern: "EyeOutlined.*onClick.*setPreview"
    - from: "AttachmentPreviewModal"
      to: "Backend GET /:id/dinh-kem/:attachmentId/preview"
      via: "iframe src / img src / fetch text — URL build qua buildPreviewUrl"
      pattern: "/dinh-kem/.+/preview"
    - from: "Backend route preview endpoints (4 file)"
      to: "lib/attachment-preview.ts handleAttachmentPreview"
      via: "import + gọi sau khi load attachment row"
      pattern: "handleAttachmentPreview\\("
    - from: "handleAttachmentPreview"
      to: "office-converter.ts convertOfficeToPdf"
      via: "if isOfficeMime(mime) → convert → streamFileToResponse(preview path, inline=true)"
      pattern: "convertOfficeToPdf\\("
---

<objective>
Thêm tính năng xem trực tiếp file đính kèm (preview inline) cho 4 module: Văn bản đến, Văn bản đi, Văn bản dự thảo, Hồ sơ công việc.

Mục tiêu: User không cần tải file về máy mới xem được nội dung — bấm icon Mắt cạnh tên file → Modal mở ra hiển thị PDF/ảnh/text trực tiếp, file Office (Word/Excel/PowerPoint) được convert tại backend qua LibreOffice headless rồi stream PDF về browser. Cache preview tại MinIO theo `previews/{attachment_id}.pdf` để lần xem sau không re-convert.

Output: 1 backend infra module (office-converter + attachment-preview helper), 4 backend endpoint mới (`GET /:id/dinh-kem/:attachmentId/preview`), 1 frontend Modal + helper, 4 trang chi tiết được cập nhật thêm nút Xem, và 1 bước doc update vào MANUAL_UPDATE_PROD.md cho prod cần cài LibreOffice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@e_office_app_new/backend/src/lib/minio/client.ts
@e_office_app_new/backend/src/routes/incoming-doc.ts
@e_office_app_new/backend/src/routes/outgoing-doc.ts
@e_office_app_new/backend/src/routes/drafting-doc.ts
@e_office_app_new/backend/src/routes/handling-doc.ts
@e_office_app_new/backend/src/repositories/incoming-doc.repository.ts
@e_office_app_new/backend/src/repositories/handling-doc.repository.ts
@e_office_app_new/frontend/src/lib/download.ts
@e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx
@e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx
@e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx
@e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx
@deploy/MANUAL_UPDATE_PROD.md

<interfaces>
<!-- Critical contracts the executor needs — extracted from codebase to avoid scavenger hunt -->

### Backend — MinIO helper đã có sẵn (KHÔNG sửa)
File: e_office_app_new/backend/src/lib/minio/client.ts
```typescript
export async function streamFileToResponse(
  res: Response,
  path: string,
  fileName: string,
  contentType?: string,
  inline = false,  // ← QUAN TRỌNG: helper ĐÃ support inline=true. Chỉ cần truyền true cho preview.
): Promise<void>;

export async function uploadFile(path: string, buffer: Buffer, contentType: string): Promise<string>;
export const minioClient: Minio.Client;
const BUCKET = process.env.MINIO_BUCKET || 'documents';
```

### Backend — AttachmentRow field naming (LƯU Ý: KHÁC NHAU giữa các module!)
- **incoming-doc.repository.ts** (line 110-125): `AttachmentRow { id, file_name, file_path, content_type }` (SP trả `content_type`)
- **handling-doc.repository.ts** (line 124-130): `AttachmentRow { id, file_name, file_path, content_type }` (SP trả `content_type`)
- **outgoing-doc.repository.ts**: AttachmentRow KHÔNG có field MIME rõ ràng — route hiện cast `(att as any).mime_type` (line 626) — **ĐỌC SP `edoc.fn_outgoing_doc_get_attachments` để xác định tên field thật trong RUNTIME**. Nếu SP trả `content_type` thì cast `(att as any).content_type`.
- **drafting-doc.repository.ts**: tương tự outgoing — route hiện cast `(att as any).mime_type` (line 548).

**Hành động bắt buộc trong Task 2:** verify SP output bằng `docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "\df+ edoc.fn_outgoing_doc_get_attachments"` HOẶC log row đầu tiên runtime. Nếu cả 4 SP đều trả `content_type` → chuẩn hóa code đọc `att.content_type ?? (att as any).mime_type` để safe cả 2 trường hợp.

### Backend — Pattern download endpoint hiện tại (mẫu để copy)
File: e_office_app_new/backend/src/routes/incoming-doc.ts (line 773-785)
```typescript
router.get('/:id/dinh-kem/:attachmentId/download', async (req: Request, res: Response) => {
  try {
    const attachments = await incomingDocRepository.getAttachments(Number(req.params.id));
    const att = attachments.find(a => Number(a.id) === Number(req.params.attachmentId));
    if (!att) {
      res.status(404).json({ success: false, message: 'Không tìm thấy file' });
      return;
    }
    await streamFileToResponse(res, att.file_path, att.file_name, (att as any).mime_type);
  } catch (error) {
    handleDbError(error, res);
  }
});
```

**LƯU Ý — handling-doc.ts (line 580-594) HIỆN TẠI KHÔNG CÓ ENDPOINT `/download`!** — Task 2 phải tạo MỚI cả `/download` (sao chép pattern) + `/preview`. Hoặc nếu khẳng định frontend HSCV đã có cách download riêng (window.open + presigned URL?), confirm không cần thêm download. **Hành động:** grep `e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx` cho `dinh-kem.*download` để xác minh trước khi quyết định. Phòng án mặc định: thêm cả 2 endpoint cho HSCV để đồng nhất với 3 module còn lại.

### Frontend — downloadAttachment helper hiện tại (KHÔNG sửa, tham khảo pattern)
File: e_office_app_new/frontend/src/lib/download.ts
```typescript
export async function downloadAttachment(apiPath: string, fileName: string): Promise<void> {
  const res = await api.get(apiPath, { responseType: 'blob' });
  // ... blob download
}
```
Preview KHÔNG dùng blob (vì iframe/img không cần blob — chỉ cần URL có cookie auth). Preview helper phải build URL FULL với baseURL của api, KHÔNG dùng axios get blob.

### Frontend — api instance baseURL
File: e_office_app_new/frontend/src/lib/api.ts — axios instance với token đính kèm qua interceptor. Tuy nhiên **iframe/img request KHÔNG đi qua axios → KHÔNG được token tự động**. Phải dùng cookie-based auth HOẶC mở URL kèm token query param HOẶC fetch blob qua axios → tạo blob URL → set vào iframe.

**Quyết định cho task này (đơn giản nhất, không phá auth):** Modal fetch file qua `api.get(path, { responseType: 'blob' })` → tạo `URL.createObjectURL(blob)` → set vào `<iframe src>` / `<img src>` / set content cho `<pre>` (nếu text). Cleanup blob URL khi Modal đóng.

### Existing AttachmentRow shape ở frontend (van-ban-den/[id]/page.tsx line ~700)
```typescript
type Attachment = {
  id: number;
  file_name: string;
  file_path: string;
  file_size: number;
  content_type?: string;     // có thể có
  mime_type?: string;        // hoặc có thể có (tùy module)
  created_at: string;
  is_ca?: boolean;
  ca_date?: string | null;
  signed_file_path?: string | null;
};
```

**Hành động:** trong AttachmentPreviewModal nhận prop `mimeType?: string` và đọc `att.content_type ?? att.mime_type` ở caller side để truyền vào.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 1 — Backend infra): office-converter + attachment-preview helper + env config</name>
  <files>
    e_office_app_new/backend/src/lib/office-converter.ts (CREATE)
    e_office_app_new/backend/src/lib/attachment-preview.ts (CREATE)
    e_office_app_new/backend/.env.example (EDIT — thêm LIBREOFFICE_PATH)
  </files>
  <action>
**Bước 1: Tạo `e_office_app_new/backend/src/lib/office-converter.ts`**

Module này expose 3 thứ:

```typescript
import { spawn } from 'node:child_process';
import { promises as fs } from 'node:fs';
import * as path from 'node:path';
import * as os from 'node:os';
import { minioClient, uploadFile } from './minio/client.js';
import { v4 as uuidv4 } from 'uuid';
import type { Readable } from 'node:stream';

const BUCKET = process.env.MINIO_BUCKET || 'documents';
const SOFFICE = process.env.LIBREOFFICE_PATH || (process.platform === 'win32'
  ? 'C:\\Program Files\\LibreOffice\\program\\soffice.exe'
  : '/usr/bin/soffice');

/** Office MIME types cần convert sang PDF để preview */
export const OFFICE_MIME_TYPES = new Set<string>([
  'application/msword',                                                          // .doc
  'application/vnd.openxmlformats-officedocument.wordprocessingml.document',     // .docx
  'application/vnd.ms-excel',                                                    // .xls
  'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',           // .xlsx
  'application/vnd.ms-powerpoint',                                               // .ppt
  'application/vnd.openxmlformats-officedocument.presentationml.presentation',   // .pptx
  'application/rtf', 'text/rtf',                                                 // .rtf
  'application/vnd.oasis.opendocument.text',                                     // .odt
  'application/vnd.oasis.opendocument.spreadsheet',                              // .ods
  'application/vnd.oasis.opendocument.presentation',                             // .odp
]);

export function isOfficeMime(mime: string | null | undefined): boolean {
  if (!mime) return false;
  return OFFICE_MIME_TYPES.has(mime.toLowerCase().split(';')[0].trim());
}

/**
 * Convert file Office sang PDF qua LibreOffice headless.
 *
 * Flow:
 *  1. Check cache MinIO `previews/{attachmentId}.pdf` — nếu có → return path ngay
 *  2. Download source từ MinIO về temp dir
 *  3. Spawn `soffice --headless --convert-to pdf --outdir <tmp> <input>` (timeout 60s)
 *  4. Upload PDF result lên MinIO `previews/{attachmentId}.pdf`
 *  5. Cleanup temp files
 *  6. Return MinIO preview path
 *
 * @param sourceMinioPath MinIO path của file gốc (vd: incoming/123/uuid.docx)
 * @param attachmentId DB id của attachment row — dùng làm cache key
 * @returns MinIO path của file PDF preview
 * @throws Error nếu conversion fail / timeout
 */
export async function convertOfficeToPdf(sourceMinioPath: string, attachmentId: number): Promise<string> {
  const previewPath = `previews/${attachmentId}.pdf`;

  // Step 1: Cache check
  try {
    await minioClient.statObject(BUCKET, previewPath);
    return previewPath;  // Cache hit
  } catch {
    // Cache miss — fall through to convert
  }

  // Step 2: Download source về temp dir
  const tmpDir = await fs.mkdtemp(path.join(os.tmpdir(), 'qlvb-preview-'));
  const sourceExt = path.extname(sourceMinioPath) || '.bin';
  const sourceFile = path.join(tmpDir, `src${sourceExt}`);

  try {
    const stream = await minioClient.getObject(BUCKET, sourceMinioPath) as Readable;
    const chunks: Buffer[] = [];
    for await (const chunk of stream) chunks.push(chunk as Buffer);
    await fs.writeFile(sourceFile, Buffer.concat(chunks));

    // Step 3: Spawn soffice convert
    await new Promise<void>((resolve, reject) => {
      const proc = spawn(SOFFICE, [
        '--headless', '--norestore', '--nolockcheck',
        '--convert-to', 'pdf',
        '--outdir', tmpDir,
        sourceFile,
      ], { windowsHide: true });

      let stderr = '';
      proc.stderr.on('data', (d) => { stderr += d.toString(); });

      const timeout = setTimeout(() => {
        proc.kill('SIGKILL');
        reject(new Error('Convert Office sang PDF quá lâu (timeout 60s)'));
      }, 60_000);

      proc.on('error', (err) => { clearTimeout(timeout); reject(err); });
      proc.on('exit', (code) => {
        clearTimeout(timeout);
        if (code !== 0) reject(new Error(`LibreOffice exit code ${code}: ${stderr.slice(0, 300)}`));
        else resolve();
      });
    });

    // Step 4: Upload PDF result lên MinIO
    const pdfFile = path.join(tmpDir, `src.pdf`);
    const pdfBuf = await fs.readFile(pdfFile);
    await uploadFile(previewPath, pdfBuf, 'application/pdf');

    return previewPath;
  } finally {
    // Step 5: Cleanup temp
    await fs.rm(tmpDir, { recursive: true, force: true }).catch(() => {});
  }
}
```

**Bước 2: Tạo `e_office_app_new/backend/src/lib/attachment-preview.ts`**

```typescript
import type { Response } from 'express';
import { streamFileToResponse } from './minio/client.js';
import { convertOfficeToPdf, isOfficeMime } from './office-converter.js';

/** MIME được preview NATIVE (không cần convert) */
const NATIVE_PREVIEW_MIME = new Set<string>([
  'application/pdf',
  'image/jpeg', 'image/png', 'image/gif', 'image/webp', 'image/bmp', 'image/svg+xml',
  'text/plain', 'text/csv',
]);

export function isPreviewableMime(mime: string | null | undefined): boolean {
  if (!mime) return false;
  const m = mime.toLowerCase().split(';')[0].trim();
  return NATIVE_PREVIEW_MIME.has(m) || isOfficeMime(m);
}

/**
 * Branch theo MIME:
 *  - PDF / image / text → stream inline luôn
 *  - Office (doc/docx/xls/...) → convert → stream PDF inline
 *  - MIME khác → trả 415 Unsupported (JSON), KHÔNG stream
 *
 * AuthN/AuthZ: caller phải kiểm tra quyền + load attachment row TRƯỚC khi gọi helper này.
 *
 * @param params.filePath MinIO path của file (đã verify ownership)
 * @param params.contentType MIME của file (từ AttachmentRow.content_type hoặc mime_type)
 * @param params.attachmentId DB id (dùng cache key cho Office convert)
 * @param params.fileName Tên file gốc (cho header Content-Disposition)
 */
export async function handleAttachmentPreview(
  res: Response,
  params: {
    filePath: string;
    contentType: string | null | undefined;
    attachmentId: number;
    fileName: string;
  },
): Promise<void> {
  const mime = (params.contentType || '').toLowerCase().split(';')[0].trim();

  if (NATIVE_PREVIEW_MIME.has(mime)) {
    await streamFileToResponse(res, params.filePath, params.fileName, mime, /* inline */ true);
    return;
  }

  if (isOfficeMime(mime)) {
    const previewPath = await convertOfficeToPdf(params.filePath, params.attachmentId);
    // Đổi extension trong filename: foo.docx → foo.pdf
    const pdfFileName = params.fileName.replace(/\.[^.]+$/, '') + '.pdf';
    await streamFileToResponse(res, previewPath, pdfFileName, 'application/pdf', /* inline */ true);
    return;
  }

  res.status(415).json({
    success: false,
    message: 'Loại file không hỗ trợ xem trực tiếp. Vui lòng tải xuống để mở bằng ứng dụng phù hợp.',
  });
}
```

**Bước 3: Cập nhật `e_office_app_new/backend/.env.example`**

Thêm 1 dòng vào section MinIO hoặc cuối file:

```
# --- LibreOffice (preview file Office — convert sang PDF) ---
# Windows mac dinh: C:\Program Files\LibreOffice\program\soffice.exe
# Linux mac dinh: /usr/bin/soffice
LIBREOFFICE_PATH=C:\Program Files\LibreOffice\program\soffice.exe
```

(LƯU Ý CLAUDE.md deploy gotcha #1: `.env.example` chỉ dùng tiếng Việt KHÔNG dấu hoặc tiếng Anh để tránh PowerShell 5.1 + UTF-8 no BOM parsing issue. Comment trên dùng "mac dinh" thay vì "mặc định".)
  </action>
  <verify>
    <automated>cd e_office_app_new/backend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - 2 file mới tồn tại: `office-converter.ts` + `attachment-preview.ts`
    - `.env.example` có dòng `LIBREOFFICE_PATH=`
    - `npx tsc --noEmit` PASS (zero TS error)
    - Module export đúng: `convertOfficeToPdf`, `isOfficeMime`, `OFFICE_MIME_TYPES`, `handleAttachmentPreview`, `isPreviewableMime`
  </done>
</task>

<task type="auto">
  <name>Task 2 (Wave 2 — Backend endpoints, 4 routes): thêm GET /:id/dinh-kem/:attachmentId/preview cho 4 module</name>
  <files>
    e_office_app_new/backend/src/routes/incoming-doc.ts (EDIT — thêm endpoint preview sau download line 785)
    e_office_app_new/backend/src/routes/outgoing-doc.ts (EDIT — thêm endpoint preview sau download line 630)
    e_office_app_new/backend/src/routes/drafting-doc.ts (EDIT — thêm endpoint preview sau download)
    e_office_app_new/backend/src/routes/handling-doc.ts (EDIT — thêm CẢ download + preview, hiện đang thiếu download)
  </files>
  <action>
**Pattern chung cho cả 4 file: import helper + thêm route mới NGAY SAU download endpoint hiện có.**

**Bước 0: Verify SP attachment field naming (chạy 1 lệnh để confirm).**

```powershell
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "SELECT pg_get_function_result(p.oid) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='edoc' AND p.proname IN ('fn_incoming_doc_get_attachments','fn_outgoing_doc_get_attachments','fn_drafting_doc_get_attachments','fn_handling_doc_get_attachments');"
```

Ghi nhận: SP trả field `content_type` hay `mime_type`. Code helper bên dưới dùng pattern `att.content_type ?? (att as any).mime_type` để safe.

**Bước 1: `incoming-doc.ts`** — thêm import + route mới NGAY SAU `/download` (sau line 785):

Sửa dòng import tại line 6:
```typescript
// Trước:
import { uploadFile, deleteFile, getFileUrl, streamFileToResponse } from '../lib/minio/client.js';
// Sau:
import { uploadFile, deleteFile, getFileUrl, streamFileToResponse } from '../lib/minio/client.js';
import { handleAttachmentPreview } from '../lib/attachment-preview.js';
```

Thêm route mới ngay sau endpoint `/download` (tức là sau line 785, trước section comment `// 3.5 SEND`):

```typescript
// GET /:id/dinh-kem/:attachmentId/preview — Xem truc tiep file dinh kem (inline)
// PDF/anh/text -> stream inline; Office (doc/docx/xls/...) -> convert qua LibreOffice -> stream PDF
router.get('/:id/dinh-kem/:attachmentId/preview', async (req: Request, res: Response) => {
  try {
    const attachments = await incomingDocRepository.getAttachments(Number(req.params.id));
    const att = attachments.find(a => Number(a.id) === Number(req.params.attachmentId));
    if (!att) {
      res.status(404).json({ success: false, message: 'Không tìm thấy file' });
      return;
    }
    await handleAttachmentPreview(res, {
      filePath: att.file_path,
      contentType: att.content_type ?? (att as any).mime_type ?? null,
      attachmentId: Number(att.id),
      fileName: att.file_name,
    });
  } catch (error) {
    handleDbError(error, res);
  }
});
```

**Bước 2: `outgoing-doc.ts`** — tương tự, sau line 630 (sau `/download`):

Thêm import:
```typescript
import { handleAttachmentPreview } from '../lib/attachment-preview.js';
```

Thêm route — LƯU Ý: outgoing có pattern dùng `signed_file_path` khi `is_ca=true`. Preview cũng ưu tiên file đã ký (đồng nhất với download line 625):

```typescript
router.get('/:id/dinh-kem/:attachmentId/preview', async (req: Request, res: Response) => {
  try {
    const attachments = await outgoingDocRepository.getAttachments(Number(req.params.id));
    const att = attachments.find(a => Number(a.id) === Number(req.params.attachmentId));
    if (!att) {
      res.status(404).json({ success: false, message: 'Không tìm thấy file' });
      return;
    }
    const filePath = (att.is_ca && att.signed_file_path) ? att.signed_file_path : att.file_path;
    await handleAttachmentPreview(res, {
      filePath,
      contentType: (att as any).content_type ?? (att as any).mime_type ?? null,
      attachmentId: Number(att.id),
      fileName: att.file_name,
    });
  } catch (error) {
    handleDbError(error, res);
  }
});
```

**Bước 3: `drafting-doc.ts`** — tương tự outgoing pattern (cũng có signed_file_path), thêm sau endpoint `/download`:

```typescript
import { handleAttachmentPreview } from '../lib/attachment-preview.js';
```

```typescript
router.get('/:id/dinh-kem/:attachmentId/preview', async (req: Request, res: Response) => {
  try {
    const attachments = await draftingDocRepository.getAttachments(Number(req.params.id));
    const att = attachments.find(a => Number(a.id) === Number(req.params.attachmentId));
    if (!att) {
      res.status(404).json({ success: false, message: 'Không tìm thấy file' });
      return;
    }
    const filePath = ((att as any).is_ca && (att as any).signed_file_path)
      ? (att as any).signed_file_path
      : (att as any).file_path;
    await handleAttachmentPreview(res, {
      filePath,
      contentType: (att as any).content_type ?? (att as any).mime_type ?? null,
      attachmentId: Number(att.id),
      fileName: (att as any).file_name,
    });
  } catch (error) {
    handleDbError(error, res);
  }
});
```

**Bước 4: `handling-doc.ts`** — HSCV hiện KHÔNG có endpoint `/download` (chỉ có `/dinh-kem` GET list + POST + DELETE). Thêm CẢ 2 endpoint (download + preview) ngay sau DELETE attachment để đồng bộ với 3 module còn lại:

Thêm import:
```typescript
// Trong block import minio (tìm dòng có streamFileToResponse — nếu chưa có, thêm)
import { uploadFile, deleteFile, streamFileToResponse } from '../lib/minio/client.js';
import { handleAttachmentPreview } from '../lib/attachment-preview.js';
```

Thêm 2 route mới NGAY SAU `router.delete('/:id/dinh-kem/:attachmentId', ...)` (tìm DELETE attachment trong handling-doc.ts):

```typescript
// GET /:id/dinh-kem/:attachmentId/download — Tai file dinh kem HSCV qua backend proxy
router.get('/:id/dinh-kem/:attachmentId/download', async (req: Request, res: Response) => {
  try {
    const attachments = await handlingDocRepository.getAttachments(Number(req.params.id));
    const att = attachments.find(a => Number(a.id) === Number(req.params.attachmentId));
    if (!att) {
      res.status(404).json({ success: false, message: 'Không tìm thấy file' });
      return;
    }
    await streamFileToResponse(res, att.file_path, att.file_name, att.content_type);
  } catch (error) {
    handleDbError(error, res);
  }
});

// GET /:id/dinh-kem/:attachmentId/preview — Xem truc tiep file dinh kem HSCV (inline)
router.get('/:id/dinh-kem/:attachmentId/preview', async (req: Request, res: Response) => {
  try {
    const attachments = await handlingDocRepository.getAttachments(Number(req.params.id));
    const att = attachments.find(a => Number(a.id) === Number(req.params.attachmentId));
    if (!att) {
      res.status(404).json({ success: false, message: 'Không tìm thấy file' });
      return;
    }
    await handleAttachmentPreview(res, {
      filePath: att.file_path,
      contentType: att.content_type ?? null,
      attachmentId: Number(att.id),
      fileName: att.file_name,
    });
  } catch (error) {
    handleDbError(error, res);
  }
});
```

**Integration check sau khi sửa 4 file (CLAUDE.md Phase Execution Rules — tích hợp sau wave):**

1. `cd e_office_app_new/backend && npx tsc --noEmit` → PASS zero error
2. Start backend `npm run dev` → verify boot OK + log không có error
3. Test 1 endpoint thực tế bằng curl (sau khi login lấy token):
   ```powershell
   # Test với 1 VB đến đã có file đính kèm PDF (giả sử id=1, attachmentId=1):
   curl -i -H "Authorization: Bearer <token>" "http://localhost:4000/api/van-ban-den/1/dinh-kem/1/preview" -o preview.pdf
   ```
   Kỳ vọng: status 200, Content-Type application/pdf, Content-Disposition `inline; filename=...`

**LƯU Ý CLAUDE.md TS gotcha #4:** Express 5 strict reject `async (req: AuthRequest, ...)` — phải dùng `Request` + cast `(req as AuthRequest).user`. Pattern trong incoming-doc.ts hiện tại đã đúng — copy y nguyên.
  </action>
  <verify>
    <automated>cd e_office_app_new/backend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - 4 file route đã có endpoint mới `GET /:id/dinh-kem/:attachmentId/preview`
    - handling-doc.ts có thêm cả `/download` (trước đây thiếu)
    - `npx tsc --noEmit` PASS
    - Backend boot không error
    - 1 curl test endpoint preview cho VB đến với file PDF trả 200 + Content-Type application/pdf + Content-Disposition `inline`
    - **Confirm field naming SP attachment:** chạy lệnh psql ở Bước 0, ghi nhận vào git commit message (`content_type` hay `mime_type`)
  </done>
</task>

<task type="auto">
  <name>Task 3 (Wave 3 — Frontend preview infra): AttachmentPreviewModal + helper lib</name>
  <files>
    e_office_app_new/frontend/src/lib/preview.ts (CREATE)
    e_office_app_new/frontend/src/components/AttachmentPreviewModal.tsx (CREATE)
  </files>
  <action>
**Bước 1: Tạo `e_office_app_new/frontend/src/lib/preview.ts`**

```typescript
/**
 * Helpers cho tính năng xem trực tiếp file đính kèm.
 *
 * Modal preview fetch file qua axios (responseType: 'blob') → tạo blob URL →
 * set vào iframe / img / pre. Cookie auth được giữ qua axios interceptor.
 */

/** Module type — quyết định API path prefix */
export type PreviewModule = 'van-ban-den' | 'van-ban-di' | 'van-ban-du-thao' | 'ho-so-cong-viec';

const MODULE_API_PREFIX: Record<PreviewModule, string> = {
  'van-ban-den': '/van-ban-den',
  'van-ban-di': '/van-ban-di',
  'van-ban-du-thao': '/van-ban-du-thao',
  'ho-so-cong-viec': '/ho-so-cong-viec',
};

/** Build API path cho preview endpoint (relative to api baseURL) */
export function buildPreviewUrl(module: PreviewModule, docId: number | string, attachmentId: number | string): string {
  return `${MODULE_API_PREFIX[module]}/${docId}/dinh-kem/${attachmentId}/preview`;
}

/** Native preview kinds (xác định cách render trong Modal) */
export type PreviewKind = 'pdf' | 'image' | 'text' | 'office' | 'unsupported';

const NATIVE_PDF = 'application/pdf';
const IMAGE_PREFIX = 'image/';
const TEXT_PREFIX = 'text/';
const OFFICE_MIMES = new Set<string>([
  'application/msword',
  'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
  'application/vnd.ms-excel',
  'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
  'application/vnd.ms-powerpoint',
  'application/vnd.openxmlformats-officedocument.presentationml.presentation',
  'application/rtf', 'text/rtf',
  'application/vnd.oasis.opendocument.text',
  'application/vnd.oasis.opendocument.spreadsheet',
  'application/vnd.oasis.opendocument.presentation',
]);

/**
 * Phán đoán preview kind từ MIME + file extension fallback.
 * MIME ưu tiên — extension chỉ dùng khi MIME thiếu (vì SP có thể chưa lưu).
 */
export function getPreviewKind(mime: string | null | undefined, fileName: string): PreviewKind {
  const m = (mime || '').toLowerCase().split(';')[0].trim();
  if (m === NATIVE_PDF) return 'pdf';
  if (m.startsWith(IMAGE_PREFIX)) return 'image';
  if (m.startsWith(TEXT_PREFIX)) return 'text';
  if (OFFICE_MIMES.has(m)) return 'office';

  // Fallback theo extension nếu MIME thiếu
  const ext = (fileName.split('.').pop() || '').toLowerCase();
  if (ext === 'pdf') return 'pdf';
  if (['jpg', 'jpeg', 'png', 'gif', 'webp', 'bmp', 'svg'].includes(ext)) return 'image';
  if (['txt', 'csv', 'log', 'md'].includes(ext)) return 'text';
  if (['doc', 'docx', 'xls', 'xlsx', 'ppt', 'pptx', 'rtf', 'odt', 'ods', 'odp'].includes(ext)) return 'office';

  return 'unsupported';
}

/** Có thể preview hay không */
export function isPreviewable(mime: string | null | undefined, fileName: string): boolean {
  return getPreviewKind(mime, fileName) !== 'unsupported';
}
```

**Bước 2: Tạo `e_office_app_new/frontend/src/components/AttachmentPreviewModal.tsx`**

```typescript
'use client';

import { useEffect, useState } from 'react';
import { Modal, Spin, Empty, Button, App } from 'antd';
import { DownloadOutlined } from '@ant-design/icons';
import { api } from '@/lib/api';
import { getPreviewKind, type PreviewKind } from '@/lib/preview';

export interface AttachmentPreviewModalProps {
  open: boolean;
  onClose: () => void;
  /** API path tương đối — vd: /van-ban-den/123/dinh-kem/456/preview */
  previewPath: string | null;
  /** API path tải xuống (fallback) — vd: /van-ban-den/123/dinh-kem/456/download */
  downloadPath?: string | null;
  fileName: string;
  /** MIME của file (từ AttachmentRow.content_type hoặc mime_type) */
  mimeType?: string | null;
}

/**
 * Modal full-screen hiển thị xem trực tiếp file đính kèm.
 *
 * - PDF / Office (đã convert PDF tại backend): iframe
 * - Image: <img>
 * - Text: <pre>
 * - MIME khác: thông báo không hỗ trợ + nút Tải xuống
 *
 * Fetch file qua axios (blob) -> URL.createObjectURL -> set vào iframe/img/pre.
 * Cleanup blob URL khi đóng Modal hoặc đổi file.
 */
export function AttachmentPreviewModal(props: AttachmentPreviewModalProps) {
  const { open, onClose, previewPath, downloadPath, fileName, mimeType } = props;
  const { message } = App.useApp();

  const [loading, setLoading] = useState(false);
  const [blobUrl, setBlobUrl] = useState<string | null>(null);
  const [textContent, setTextContent] = useState<string | null>(null);
  const [errorMsg, setErrorMsg] = useState<string | null>(null);

  const kind: PreviewKind = getPreviewKind(mimeType, fileName);

  useEffect(() => {
    if (!open || !previewPath) return;

    let cancelled = false;
    let createdBlobUrl: string | null = null;

    const fetchPreview = async () => {
      setLoading(true);
      setErrorMsg(null);
      setBlobUrl(null);
      setTextContent(null);

      try {
        if (kind === 'unsupported') {
          setErrorMsg('Loại file này không hỗ trợ xem trực tiếp. Vui lòng tải xuống để mở bằng ứng dụng phù hợp.');
          return;
        }

        const res = await api.get(previewPath, { responseType: 'blob' });
        if (cancelled) return;

        const responseType = (res.headers?.['content-type'] as string) || mimeType || 'application/octet-stream';

        if (kind === 'text') {
          const text = await (res.data as Blob).text();
          if (!cancelled) setTextContent(text);
        } else {
          const blob = new Blob([res.data], { type: responseType });
          createdBlobUrl = URL.createObjectURL(blob);
          if (!cancelled) setBlobUrl(createdBlobUrl);
        }
      } catch (err: unknown) {
        if (cancelled) return;
        const e = err as { response?: { status?: number; data?: { message?: string } } };
        if (e.response?.status === 415) {
          setErrorMsg(e.response.data?.message || 'Loại file không hỗ trợ xem trực tiếp.');
        } else if (e.response?.status === 404) {
          setErrorMsg('Không tìm thấy file đính kèm.');
        } else {
          setErrorMsg('Không thể tải file để xem trực tiếp. Vui lòng thử tải xuống.');
          // eslint-disable-next-line no-console
          console.error('Preview fetch error:', err);
        }
      } finally {
        if (!cancelled) setLoading(false);
      }
    };

    fetchPreview();

    return () => {
      cancelled = true;
      if (createdBlobUrl) URL.revokeObjectURL(createdBlobUrl);
    };
  }, [open, previewPath, kind, mimeType]);

  // Cleanup blob URL khi component unmount hoặc đóng Modal
  useEffect(() => {
    return () => {
      if (blobUrl) URL.revokeObjectURL(blobUrl);
    };
  }, [blobUrl]);

  const handleDownloadFallback = async () => {
    if (!downloadPath) return;
    try {
      const res = await api.get(downloadPath, { responseType: 'blob' });
      const url = URL.createObjectURL(new Blob([res.data]));
      const a = document.createElement('a');
      a.href = url;
      a.download = fileName || 'download';
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      setTimeout(() => URL.revokeObjectURL(url), 1000);
    } catch {
      message.error('Tải xuống thất bại');
    }
  };

  return (
    <Modal
      open={open}
      onCancel={onClose}
      title={`Xem trực tiếp: ${fileName}`}
      width="90vw"
      style={{ top: 20, maxWidth: 1400 }}
      styles={{ body: { height: 'calc(100vh - 200px)', overflow: 'hidden', padding: 0 } }}
      footer={[
        downloadPath ? (
          <Button key="download" icon={<DownloadOutlined />} onClick={handleDownloadFallback}>
            Tải xuống
          </Button>
        ) : null,
        <Button key="close" type="primary" onClick={onClose}>
          Đóng
        </Button>,
      ].filter(Boolean)}
      destroyOnHidden
    >
      {loading && (
        <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100%' }}>
          <Spin size="large" tip="Đang tải file để xem trực tiếp..." />
        </div>
      )}

      {!loading && errorMsg && (
        <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100%' }}>
          <Empty description={errorMsg} />
        </div>
      )}

      {!loading && !errorMsg && blobUrl && (kind === 'pdf' || kind === 'office') && (
        <iframe
          src={blobUrl}
          style={{ width: '100%', height: '100%', border: 'none' }}
          title={fileName}
        />
      )}

      {!loading && !errorMsg && blobUrl && kind === 'image' && (
        <div style={{
          display: 'flex', justifyContent: 'center', alignItems: 'center',
          height: '100%', background: '#0F1A2E', overflow: 'auto',
        }}>
          <img src={blobUrl} alt={fileName} style={{ maxWidth: '100%', maxHeight: '100%', objectFit: 'contain' }} />
        </div>
      )}

      {!loading && !errorMsg && textContent !== null && kind === 'text' && (
        <pre style={{
          width: '100%', height: '100%', margin: 0, padding: 16,
          overflow: 'auto', whiteSpace: 'pre-wrap', wordBreak: 'break-word',
          fontFamily: 'Consolas, Monaco, monospace', fontSize: 13, lineHeight: 1.5,
        }}>
          {textContent}
        </pre>
      )}
    </Modal>
  );
}
```

**Integration check sau Task 3:**

1. `cd e_office_app_new/frontend && npx tsc --noEmit` → PASS
2. Verify 2 file export đúng tên — import test:
   ```typescript
   // Trong 1 page bất kỳ (test thử):
   import { buildPreviewUrl, getPreviewKind, isPreviewable } from '@/lib/preview';
   import { AttachmentPreviewModal } from '@/components/AttachmentPreviewModal';
   ```
   Không TS error.

**LƯU Ý CLAUDE.md UI conventions:**
- Modal title tiếng Việt có dấu đầy đủ
- Nút "Tải xuống", "Đóng" tiếng Việt có dấu
- Không dùng `alert()` — dùng `App.useApp().message`
- Antd v6: `styles.body` thay vì `bodyStyle` deprecated; `destroyOnHidden` thay vì `destroyOnClose`
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - 2 file mới tồn tại: `lib/preview.ts` + `components/AttachmentPreviewModal.tsx`
    - `npx tsc --noEmit` PASS (zero TS error)
    - Export đúng tên: `buildPreviewUrl`, `getPreviewKind`, `isPreviewable`, `AttachmentPreviewModal`
    - Modal tiếng Việt có dấu đầy đủ ("Xem trực tiếp", "Tải xuống", "Đóng", "Đang tải file...")
  </done>
</task>

<task type="auto">
  <name>Task 4 (Wave 4 — Frontend integration, 4 pages): thêm nút Xem cạnh Download trong 4 trang chi tiết + cập nhật MANUAL_UPDATE_PROD.md</name>
  <files>
    e_office_app_new/frontend/src/app/(main)/van-ban-den/[id]/page.tsx (EDIT)
    e_office_app_new/frontend/src/app/(main)/van-ban-di/[id]/page.tsx (EDIT)
    e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/[id]/page.tsx (EDIT)
    e_office_app_new/frontend/src/app/(main)/ho-so-cong-viec/[id]/page.tsx (EDIT)
    deploy/MANUAL_UPDATE_PROD.md (EDIT)
  </files>
  <action>
**Pattern chung cho cả 4 trang:**

1. Thêm import (đầu file, cùng nhóm với import từ `@/lib`):
   ```typescript
   import { EyeOutlined } from '@ant-design/icons';
   import { AttachmentPreviewModal } from '@/components/AttachmentPreviewModal';
   import { buildPreviewUrl, isPreviewable, type PreviewModule } from '@/lib/preview';
   ```

2. Thêm state cho preview (gần các useState hiện có trong component):
   ```typescript
   const [previewState, setPreviewState] = useState<{
     open: boolean;
     attachmentId: number | null;
     fileName: string;
     mimeType: string | null;
   }>({ open: false, attachmentId: null, fileName: '', mimeType: null });

   const openPreview = (att: { id: number | string; file_name: string; content_type?: string | null; mime_type?: string | null }) => {
     setPreviewState({
       open: true,
       attachmentId: Number(att.id),
       fileName: att.file_name,
       mimeType: att.content_type ?? att.mime_type ?? null,
     });
   };
   const closePreview = () => setPreviewState((s) => ({ ...s, open: false }));
   ```

3. Tìm vùng render attachment có `<DownloadOutlined />` button (ví dụ trong van-ban-den/[id]/page.tsx line 712), THÊM nút `<EyeOutlined />` NGAY TRƯỚC nút Download:

   **Trước (line 712):**
   ```tsx
   <Space size={4}>
     <Button size="small" type="link" icon={<DownloadOutlined />} onClick={() => handleDownload(att)} />
     {!doc.approved && <Popconfirm ...><Button size="small" type="link" danger icon={<DeleteOutlined />} /></Popconfirm>}
   </Space>
   ```

   **Sau:**
   ```tsx
   <Space size={4}>
     {isPreviewable(att.content_type ?? att.mime_type ?? null, att.file_name) && (
       <Tooltip title="Xem trực tiếp">
         <Button size="small" type="link" icon={<EyeOutlined />} onClick={() => openPreview(att)} />
       </Tooltip>
     )}
     <Tooltip title="Tải xuống">
       <Button size="small" type="link" icon={<DownloadOutlined />} onClick={() => handleDownload(att)} />
     </Tooltip>
     {!doc.approved && <Popconfirm ...><Button size="small" type="link" danger icon={<DeleteOutlined />} /></Popconfirm>}
   </Space>
   ```

   (Nếu `Tooltip` chưa được import → thêm vào import từ `'antd'`.)

4. Trước khi đóng JSX của page component (gần cuối file, trước `</div>` ngoài cùng hoặc trước `</>`), thêm Modal:

   **Cho van-ban-den/[id]/page.tsx:**
   ```tsx
   <AttachmentPreviewModal
     open={previewState.open}
     onClose={closePreview}
     previewPath={previewState.attachmentId
       ? buildPreviewUrl('van-ban-den', doc.id, previewState.attachmentId)
       : null}
     downloadPath={previewState.attachmentId
       ? `/van-ban-den/${doc.id}/dinh-kem/${previewState.attachmentId}/download`
       : null}
     fileName={previewState.fileName}
     mimeType={previewState.mimeType}
   />
   ```

   **Cho van-ban-di/[id]/page.tsx:** thay `'van-ban-den'` → `'van-ban-di'` (cả module name lẫn URL download)
   **Cho van-ban-du-thao/[id]/page.tsx:** thay → `'van-ban-du-thao'`
   **Cho ho-so-cong-viec/[id]/page.tsx:** thay → `'ho-so-cong-viec'`

   **LƯU Ý:** `doc.id` ở mỗi page có thể là `doc.id` hoặc `id` từ route param `useParams()` — tùy file. Đọc state hiện có để dùng đúng biến (1 số page lưu `[doc, setDoc] = useState`, 1 số khác dùng `const { id } = useParams()`).

5. **Verify integration cho 4 file:** đảm bảo:
   - Import `EyeOutlined` từ `@ant-design/icons`
   - Import `Tooltip` từ `antd` (nếu chưa có)
   - Import `AttachmentPreviewModal` từ `@/components/AttachmentPreviewModal`
   - Import `buildPreviewUrl`, `isPreviewable` từ `@/lib/preview`
   - State `previewState` + handler `openPreview`/`closePreview`
   - Nút EyeOutlined render đúng vị trí
   - `<AttachmentPreviewModal>` được render trong page

**Bước cuối — `deploy/MANUAL_UPDATE_PROD.md`:**

Đọc file hiện tại, tìm section phù hợp (ví dụ "Pre-requisites" hoặc "Bước 0: chuẩn bị môi trường"), THÊM 1 section mới về LibreOffice. Nếu không có section pre-req, thêm vào ĐẦU file ngay sau intro/checklist tổng quan:

```markdown
## Pre-requisite: Cài LibreOffice (cho tính năng xem trực tiếp file Office)

Tính năng "Xem trực tiếp file đính kèm" (từ release 2026-05-21) yêu cầu LibreOffice headless để convert file Office (doc/docx/xls/xlsx/ppt/pptx) sang PDF.

**Windows Server:**
1. Tải LibreOffice mới nhất: https://www.libreoffice.org/download/download-libreoffice/
2. Cài đặt mặc định vào `C:\Program Files\LibreOffice\`
3. Verify: mở PowerShell chạy `& "C:\Program Files\LibreOffice\program\soffice.exe" --version` → in ra version
4. Thêm vào `backend\.env`:
   ```
   LIBREOFFICE_PATH=C:\Program Files\LibreOffice\program\soffice.exe
   ```
5. Restart backend: `pm2 restart all --update-env` (BẮT BUỘC `--update-env` để pickup biến mới — CLAUDE.md gotcha #13)

**Kiểm tra tính năng sau khi deploy:**
1. Login vào hệ thống
2. Mở 1 VB đến có file đính kèm .docx
3. Bấm icon Mắt (Xem trực tiếp) cạnh tên file
4. Modal mở → hiển thị nội dung file Word đã convert sang PDF

**Nếu fail:** check log `pm2 logs qlvb-backend` xem có message "LibreOffice exit code..." hay "ENOENT" → đường dẫn `LIBREOFFICE_PATH` sai hoặc chưa cài.

**Lưu ý:** Lần convert đầu tiên cho mỗi file mất ~3-10s tùy size. Lần sau hit cache MinIO `previews/{attachment_id}.pdf` → load ngay.

---
```

**Integration check sau Task 4 (CLAUDE.md mandate — sau wave):**

1. `cd e_office_app_new/frontend && npx tsc --noEmit` → PASS
2. `cd e_office_app_new/frontend && npm run build` → PASS production build (CLAUDE.md gotcha #4: TS strict ở prod build)
3. Start frontend `npm run dev` + backend `npm run dev` → mở browser http://localhost:3000
4. Login `admin / Admin@123`
5. Mở 1 VB đến có file đính kèm PDF → bấm icon Mắt → Modal mở → hiển thị PDF inline (loading spinner trước → iframe PDF)
6. (Optional, nếu môi trường dev có LibreOffice) Mở 1 VB có .docx → bấm icon Mắt → Modal hiển thị PDF đã convert
7. Mở 1 VB có file .zip → icon Mắt KHÔNG hiển thị (vì `isPreviewable` trả false). Chỉ có nút Download.
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend &amp;&amp; npx tsc --noEmit &amp;&amp; cd ../backend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - 4 trang chi tiết đã có:
      - Import EyeOutlined + Tooltip + AttachmentPreviewModal + buildPreviewUrl/isPreviewable
      - State previewState + handlers openPreview/closePreview
      - Nút EyeOutlined có Tooltip "Xem trực tiếp" cạnh nút Download (chỉ hiện khi `isPreviewable`)
      - `<AttachmentPreviewModal>` được render trong page
    - `deploy/MANUAL_UPDATE_PROD.md` có section "Pre-requisite: Cài LibreOffice"
    - `npx tsc --noEmit` PASS ở cả frontend lẫn backend
    - Manual smoke test: login → mở VB đến có file PDF → bấm Mắt → Modal hiển thị PDF inline đúng
    - File không hỗ trợ (.zip, .rar) → icon Mắt KHÔNG hiển thị
    - Tooltip + Modal title + tất cả text UI là tiếng Việt có dấu
  </done>
</task>

</tasks>

<verification>
**Sau khi cả 4 task hoàn thành — chạy manual smoke test (CLAUDE.md mandate: integration check cuối phase):**

1. **Backend boot + TS check:**
   ```powershell
   cd e_office_app_new/backend
   npx tsc --noEmit  # PASS
   npm run dev       # Start, no error
   ```

2. **Frontend boot + TS check + build:**
   ```powershell
   cd e_office_app_new/frontend
   npx tsc --noEmit  # PASS
   npm run build     # PASS production build (gotcha CLAUDE.md #4 — TS strict ở prod khác dev)
   npm run dev       # Start
   ```

3. **End-to-end smoke test** (login admin / Admin@123):

   | Module | URL test | File type | Kỳ vọng |
   |---|---|---|---|
   | VB đến | /van-ban-den/{id} (có PDF attachment) | PDF | Modal hiển thị PDF inline trong iframe |
   | VB đến | /van-ban-den/{id} (có .docx) | DOCX | Modal hiển thị PDF đã convert (cần LibreOffice local) |
   | VB đi | /van-ban-di/{id} | PDF | Modal hiển thị PDF (ưu tiên signed_file_path nếu có) |
   | VB dự thảo | /van-ban-du-thao/{id} | PDF/DOCX | Modal hiển thị inline |
   | HSCV | /ho-so-cong-viec/{id} | PDF | Modal hiển thị inline |
   | Bất kỳ | có file .zip | ZIP | Icon Mắt KHÔNG hiện. Bấm Download vẫn OK. |
   | Bất kỳ | có ảnh .png | PNG | Modal hiển thị ảnh fit-contain trên nền tối |

4. **Cache verification:** Mở 1 file .docx xem lần 1 (chờ convert ~5s) → đóng Modal → mở lại file đó → hiển thị NGAY (cache hit MinIO `previews/{id}.pdf`). Verify bằng MinIO console: `localhost:9001` → bucket `documents` → folder `previews/`.

5. **AuthN check:** Logout → gọi trực tiếp `http://localhost:4000/api/van-ban-den/1/dinh-kem/1/preview` (không token) → kỳ vọng 401 hoặc redirect login.

6. **Field naming verification (từ Task 2 Bước 0):** Confirm SP attachment trả `content_type` cho 4 module — ghi vào commit message nếu có discrepancy.

7. **Doc check:** `deploy/MANUAL_UPDATE_PROD.md` có section LibreOffice install với đường dẫn Windows + lệnh pm2 restart.
</verification>

<success_criteria>
- [ ] 4 backend endpoint `GET /:id/dinh-kem/:attachmentId/preview` hoạt động cho 4 module (incoming, outgoing, drafting, handling)
- [ ] handling-doc.ts có thêm cả `/download` (trước đây thiếu) để đồng bộ
- [ ] PDF / ảnh / text stream inline được trực tiếp qua iframe/img/pre
- [ ] File Office (.doc/.docx/.xls/.xlsx/.ppt/.pptx) convert qua LibreOffice headless và stream PDF được
- [ ] Cache MinIO `previews/{attachment_id}.pdf` hit lần 2+ (không re-convert)
- [ ] MIME không hỗ trợ trả 415 + UI hiển thị thông báo + nút Tải xuống fallback
- [ ] 4 trang chi tiết có nút EyeOutlined cạnh Download (chỉ hiện khi `isPreviewable`)
- [ ] Modal + tooltip + thông báo: tiếng Việt có dấu đầy đủ
- [ ] `npx tsc --noEmit` PASS ở cả backend lẫn frontend
- [ ] `npm run build` PASS ở frontend production build (CLAUDE.md gotcha #4)
- [ ] `deploy/MANUAL_UPDATE_PROD.md` có section LibreOffice install + `LIBREOFFICE_PATH` env + `pm2 restart --update-env`
- [ ] Auth giữ nguyên: endpoint preview yêu cầu auth token giống endpoint download (không downgrade security)
- [ ] KHÔNG auto-commit — chờ user yêu cầu rõ ràng (CLAUDE.md + MEMORY rule)
</success_criteria>

<output>
Sau khi hoàn thành cả 4 task:
1. Tạo file SUMMARY tại `.planning/quick/260521-v8t-them-tinh-nang-xem-truc-tiep-file-dinh-k/260521-v8t-SUMMARY.md` ghi:
   - Files modified (full list 14 file)
   - Field naming verification SP attachment (content_type vs mime_type — kết quả Bước 0 Task 2)
   - Manual smoke test results (table 7 row trong verification section)
   - Lưu ý cho deploy prod: cần cài LibreOffice TRƯỚC khi pull code (nếu không cài thì preview Office sẽ fail, nhưng PDF/ảnh/text vẫn OK)
2. KHÔNG auto-commit. Báo user rõ "Đã hoàn thành — đang chờ lệnh commit/deploy."
</output>
