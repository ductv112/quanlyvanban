---
phase: 260424-v5l-fix-gui-vb-du-thao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/database/schema/000_schema_v3.0.sql
  - e_office_app_new/backend/src/repositories/drafting-doc.repository.ts
  - e_office_app_new/backend/src/repositories/incoming-doc.repository.ts
  - e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts
  - e_office_app_new/backend/src/routes/drafting-doc.ts
  - e_office_app_new/backend/src/routes/incoming-doc.ts
  - e_office_app_new/backend/src/routes/outgoing-doc.ts
  - e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx
  - e_office_app_new/frontend/src/app/globals.css
autonomous: true
requirements:
  - BUG-A-recipient-visibility
  - BUG-B-exclude-self-in-sendable

must_haves:
  truths:
    - "Recipient staff sees drafting docs sent to them even when they belong to a different department subtree."
    - "Admin (and any logged-in user) does NOT see themselves in the sendable staff dialog for drafting/incoming/outgoing docs."
    - "SP signatures updated correctly: idempotent re-apply of schema master produces zero error, SP count >= 386, zero SP overload."
    - "Frontend drafting docs list shows an inline badge '📩 Gửi cho tôi' on rows where the current user is a recipient; unread recipient rows have a left border highlight."
  artifacts:
    - path: "e_office_app_new/database/schema/000_schema_v3.0.sql"
      provides: "Updated SPs: fn_drafting_doc_get_list, fn_drafting_doc_count_unread, fn_incoming_doc_get_sendable_staff (with p_exclude_staff_id)."
      contains: "p_exclude_staff_id"
    - path: "e_office_app_new/backend/src/repositories/drafting-doc.repository.ts"
      provides: "DraftingDocListRow with new fields i_am_recipient/sent_by_name/received_at; getSendableStaff(unitId, excludeStaffId) signature."
    - path: "e_office_app_new/backend/src/repositories/incoming-doc.repository.ts"
      provides: "getSendableStaff(unitId, excludeStaffId) signature updated."
    - path: "e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts"
      provides: "getSendableStaff(unitId, excludeStaffId) signature updated."
    - path: "e_office_app_new/backend/src/routes/drafting-doc.ts"
      provides: "GET /:id/danh-sach-gui passes staffId to repo.getSendableStaff."
    - path: "e_office_app_new/backend/src/routes/incoming-doc.ts"
      provides: "GET /:id/danh-sach-gui passes staffId to repo.getSendableStaff."
    - path: "e_office_app_new/backend/src/routes/outgoing-doc.ts"
      provides: "GET /:id/danh-sach-gui passes staffId to repo.getSendableStaff."
    - path: "e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx"
      provides: "DraftingDoc interface + columns render badge; Table rowClassName for unread recipient rows."
    - path: "e_office_app_new/frontend/src/app/globals.css"
      provides: ".drafting-row-unread-recipient CSS class."
      contains: "drafting-row-unread-recipient"
  key_links:
    - from: "edoc.fn_drafting_doc_get_list WHERE clause"
      to: "edoc.user_drafting_docs ud (JOIN on drafting_doc_id + staff_id)"
      via: "OR ud.drafting_doc_id IS NOT NULL — cross-dept recipient visibility."
    - from: "edoc.fn_incoming_doc_get_sendable_staff"
      to: "public.staff s"
      via: "AND (p_exclude_staff_id IS NULL OR s.id != p_exclude_staff_id)"
    - from: "drafting-doc.ts route /:id/danh-sach-gui"
      to: "draftingDocRepository.getSendableStaff"
      via: "passes staffId from JWT as second argument."
    - from: "frontend van-ban-du-thao/page.tsx columns[Trích yếu].render"
      to: "DraftingDoc.i_am_recipient / sent_by_name / received_at"
      via: "inline Tag with Tooltip when i_am_recipient === true."

---

<objective>
Fix 2 UAT bugs liên quan đến gửi VB dự thảo:

**Bug (a) — CRITICAL:** Recipient VB dự thảo được gửi KHÔNG thấy VB trong danh sách vì SP `fn_drafting_doc_get_list` filter quá chặt theo `department_id`. Sửa WHERE để cho phép recipient (có record trong `edoc.user_drafting_docs`) thấy VB bất kể dept. Đồng thời thêm metadata "ai gửi/khi nào" để UI badge.

**Bug (b) — UX:** Danh sách gửi chứa chính user đang login. Thêm param `p_exclude_staff_id` vào SP dùng chung `fn_incoming_doc_get_sendable_staff` → fix cho cả 3 module (VB đến / VB đi / VB dự thảo).

Purpose: Luồng "duyệt → gửi → recipient xem/xử lý" là core value của dự án; bug (a) block toàn bộ luồng này.

Output:
- 3 SPs updated trong `000_schema_v3.0.sql` (idempotent)
- 3 repos + 3 routes backend cập nhật
- 1 frontend page + globals.css cập nhật UI
- KHÔNG auto-commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@e_office_app_new/database/schema/000_schema_v3.0.sql
@e_office_app_new/backend/src/repositories/drafting-doc.repository.ts
@e_office_app_new/backend/src/repositories/incoming-doc.repository.ts
@e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts
@e_office_app_new/backend/src/routes/drafting-doc.ts
@e_office_app_new/backend/src/routes/incoming-doc.ts
@e_office_app_new/backend/src/routes/outgoing-doc.ts
@e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx

<interfaces>
<!-- Key contracts executor needs — extracted to avoid codebase scavenger hunt -->

## Current SP active definitions trong schema master (lần CREATE OR REPLACE cuối — là definition được apply)

### edoc.fn_drafting_doc_get_list (line ~21671)
Signature HIỆN TẠI:
```sql
CREATE OR REPLACE FUNCTION edoc.fn_drafting_doc_get_list(
  p_unit_id INT, p_staff_id INT,
  p_doc_book_id INT DEFAULT NULL, p_doc_type_id INT DEFAULT NULL, p_doc_field_id INT DEFAULT NULL,
  p_urgent_id SMALLINT DEFAULT NULL, p_is_released BOOLEAN DEFAULT NULL, p_approved BOOLEAN DEFAULT NULL,
  p_from_date TIMESTAMPTZ DEFAULT NULL, p_to_date TIMESTAMPTZ DEFAULT NULL, p_keyword TEXT DEFAULT NULL,
  p_page INT DEFAULT 1, p_page_size INT DEFAULT 20,
  p_dept_ids INT[] DEFAULT NULL
)
RETURNS TABLE (
  id BIGINT, unit_id INT, received_date TIMESTAMPTZ, number INT, sub_number VARCHAR,
  notation VARCHAR, document_code VARCHAR, abstract TEXT,
  drafting_unit_id INT, drafting_user_id INT, publish_unit_id INT,
  publish_date TIMESTAMPTZ, signer VARCHAR, sign_date TIMESTAMPTZ,
  doc_book_id INT, doc_type_id INT, doc_field_id INT, secret_id SMALLINT, urgent_id SMALLINT,
  number_paper INT, number_copies INT, expired_date TIMESTAMPTZ, recipients TEXT,
  approver VARCHAR, approved BOOLEAN, is_released BOOLEAN, released_date TIMESTAMPTZ,
  created_by INT, created_at TIMESTAMPTZ,
  doc_book_name VARCHAR, doc_type_name VARCHAR, doc_type_code VARCHAR, doc_field_name VARCHAR,
  drafting_unit_name VARCHAR, drafting_user_name VARCHAR, created_by_name VARCHAR,
  is_read BOOLEAN, read_at TIMESTAMPTZ, attachment_count BIGINT, total_count BIGINT
)
```

Body HIỆN TẠI đã có JOIN `LEFT JOIN edoc.user_drafting_docs ud ON ud.drafting_doc_id = d.id AND ud.staff_id = p_staff_id` → giữ nguyên.

WHERE HIỆN TẠI:
```sql
WHERE (p_dept_ids IS NULL OR d.department_id = ANY(p_dept_ids))
  AND (p_doc_book_id IS NULL OR d.doc_book_id = p_doc_book_id)
  AND (... các filter khác ...)
```

### edoc.fn_drafting_doc_count_unread (line ~21438)
```sql
CREATE OR REPLACE FUNCTION edoc.fn_drafting_doc_count_unread(
  p_unit_id INT, p_staff_id INT, p_dept_ids INT[] DEFAULT NULL
) RETURNS INT ...
DECLARE v_count INT;
BEGIN
  SELECT COUNT(*)::INT INTO v_count
  FROM edoc.drafting_docs d
  LEFT JOIN edoc.user_drafting_docs ud ON ud.drafting_doc_id = d.id AND ud.staff_id = p_staff_id
  WHERE (p_dept_ids IS NULL OR d.department_id = ANY(p_dept_ids))
    AND (ud.is_read IS NULL OR ud.is_read = FALSE);
  RETURN v_count;
END;
```

### edoc.fn_incoming_doc_get_sendable_staff (line ~24075)
Signature HIỆN TẠI:
```sql
DROP FUNCTION IF EXISTS edoc.fn_incoming_doc_get_sendable_staff(INT);
CREATE OR REPLACE FUNCTION edoc.fn_incoming_doc_get_sendable_staff(
  p_unit_id INT,
  p_dept_ids INT[] DEFAULT NULL
)
RETURNS TABLE (
  staff_id INT, full_name VARCHAR, position_name VARCHAR,
  department_id INT, department_name VARCHAR
)
```

Body:
```sql
SELECT s.id, s.full_name, p.name, s.department_id, d.name
FROM public.staff s
LEFT JOIN public.positions p ON p.id = s.position_id
LEFT JOIN public.departments d ON d.id = s.department_id
WHERE (
  p_dept_ids IS NOT NULL AND s.department_id = ANY(p_dept_ids)
  OR p_dept_ids IS NULL AND s.department_id IN (
    SELECT dep.id FROM public.departments dep
    WHERE dep.id = p_unit_id OR dep.parent_id = p_unit_id
  )
)
AND s.is_locked = FALSE AND s.is_deleted = FALSE
ORDER BY d.sort_order, d.name, s.full_name;
```

## Table edoc.user_drafting_docs (line ~13856)
```sql
CREATE TABLE IF NOT EXISTS edoc.user_drafting_docs (
    id bigint NOT NULL,
    drafting_doc_id bigint NOT NULL,
    staff_id integer NOT NULL,
    is_read boolean DEFAULT false,
    read_at timestamp with time zone,
    created_at timestamp with time zone DEFAULT now(),
    sent_by integer,
    expired_date timestamp with time zone
);
```

## Backend repo — existing interface DraftingDocListRow (drafting-doc.repository.ts:7)
```typescript
export interface DraftingDocListRow {
  id: number;
  // ... các field hiện có ...
  doc_field_name: string;
  drafting_unit_name: string;
  drafting_user_name: string;
  created_by_name: string;
  is_read: boolean;                // đã có sẵn
  read_at: string;
  attachment_count: number;
  total_count: number;
}
```
→ Thêm 3 field MỚI (i_am_recipient, sent_by_name, received_at). `is_read` đã tồn tại → KHÔNG thêm lại.

## Backend route — drafting-doc.ts:428 (endpoint danh-sach-gui)
```typescript
router.get('/:id/danh-sach-gui', async (req: Request, res: Response) => {
  try {
    const { departmentId } = (req as AuthRequest).user;
    const ancestorUnitId = await resolveAncestorUnit(departmentId);
    const rows = await draftingDocRepository.getSendableStaff(ancestorUnitId);
    res.json({ success: true, data: rows });
  } catch (error) { handleDbError(error, res); }
});
```

## Backend route — incoming-doc.ts:489 (endpoint danh-sach-gui — CÓ endpoint này)
```typescript
router.get('/:id/danh-sach-gui', async (req, res) => {
  // ...
  const rows = await incomingDocRepository.getSendableStaff(ancestorUnitId);
});
```

## Backend route — outgoing-doc.ts:522 (endpoint danh-sach-gui — CÓ endpoint này)
```typescript
router.get('/:id/danh-sach-gui', async (req, res) => {
  // ...
  const rows = await outgoingDocRepository.getSendableStaff(ancestorUnitId);
});
```

## Frontend — columns[Trích yếu] hiện tại (van-ban-du-thao/page.tsx:407)
```tsx
{
  title: 'Trích yếu', dataIndex: 'abstract', ellipsis: true,
  render: (val, r) => (
    <Tooltip title={val}>
      <a style={{ fontWeight: 500 }} href={`/van-ban-du-thao/${r.id}`}>{val}</a>
    </Tooltip>
  ),
},
```

## Frontend — Table (line 604) KHÔNG có `rowClassName` → cần thêm
```tsx
<Table<DraftingDoc>
  className="enhanced-table"
  rowKey="id"
  loading={loading}
  columns={columns}
  dataSource={data}
  // ... KHÔNG có rowClassName
/>
```

## Frontend — AuthRequest payload đã có staffId
AuthRequest user object: `{ staffId: number, departmentId: number, isAdmin: boolean, ... }`
</interfaces>
</context>

<constraints>
## CRITICAL — BẮT BUỘC TUÂN THỦ

1. **KHÔNG auto-commit.** User sẽ confirm commit thủ công sau khi test.
2. **KHÔNG tạo migration rời.** Edit trực tiếp `database/schema/000_schema_v3.0.sql` — theo "DB Migration Strategy v2.0+" trong CLAUDE.md.
3. **SP edit**: Edit trực tiếp block `CREATE OR REPLACE FUNCTION` tại line ~21671 (get_list), ~21438 (count_unread), ~24075 (sendable_staff) — KHÔNG thêm block mới ở cuối file. Grep `fn_drafting_doc_get_list` + `fn_drafting_doc_count_unread` sẽ cho nhiều match (kế thừa từ migrations cũ); LUÔN edit block CUỐI CÙNG (active definition) — dòng cao nhất.
4. **DROP FUNCTION với signature chính xác TRƯỚC CREATE** khi đổi số lượng params hoặc type params (Task 3 — thêm `p_exclude_staff_id`). Task 1 & Task 2 GIỮ signature → chỉ cần `CREATE OR REPLACE`.
5. **Reserved word check**: `number`, `position`, `order` là reserved — trong RETURNS TABLE vẫn OK (đã chạy được), nhưng cẩn thận khi reference qua alias.
6. **Field naming**: SP output MUST snake_case match DB (`i_am_recipient`, `sent_by_name`, `received_at`, `is_read`). Repo interface field phải copy từ SP output (theo CLAUDE.md checklist #1).
7. **Idempotent verify**: Apply schema master 2 lần liên tiếp phải zero error.
8. **SP count verify**: sau apply, count phải ≥ 386 (baseline Phase 11.1) và 0 overload.

## Out of scope (user confirmed defer)
- Depth >1 level của sendable subtree
- Tách tab UI "Tất cả / Gửi cho tôi / Đơn vị"
- Filter select ở thanh search
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: SP fn_drafting_doc_get_list — thêm recipient visibility + metadata output</name>
  <files>e_office_app_new/database/schema/000_schema_v3.0.sql</files>
  <action>
Edit block `CREATE OR REPLACE FUNCTION edoc.fn_drafting_doc_get_list(...)` active cuối cùng trong file (tại line ~21671, định danh bởi comment "-- 10. fn_drafting_doc_get_list" ngay phía trên).

**GIỮ NGUYÊN signature (param list)** — chỉ sửa RETURNS TABLE + body.

### 1. RETURNS TABLE — thêm 3 columns sau `attachment_count BIGINT, total_count BIGINT`:

Thay block:
```
  is_read BOOLEAN, read_at TIMESTAMPTZ, attachment_count BIGINT, total_count BIGINT
```
thành:
```
  is_read BOOLEAN, read_at TIMESTAMPTZ, attachment_count BIGINT, total_count BIGINT,
  i_am_recipient BOOLEAN, sent_by_name VARCHAR, received_at TIMESTAMPTZ
```

### 2. Body CTE `filtered` — thêm JOIN sender + 3 computed columns:

Trong CTE `filtered`, sau dòng `LEFT JOIN edoc.user_drafting_docs ud ON ud.drafting_doc_id = d.id AND ud.staff_id = p_staff_id`, thêm:
```
    LEFT JOIN public.staff sender ON sender.id = ud.sent_by
```

Và trong SELECT list của CTE, thêm 3 computed columns trước dòng `COUNT(*) OVER() AS _total_count`:
```
      (ud.drafting_doc_id IS NOT NULL) AS _i_am_recipient,
      sender.full_name AS _sent_by_name,
      ud.created_at AS _received_at,
```

### 3. WHERE clause — sửa recipient visibility:

Thay:
```
    WHERE (p_dept_ids IS NULL OR d.department_id = ANY(p_dept_ids))
```
thành:
```
    WHERE (
        (p_dept_ids IS NULL OR d.department_id = ANY(p_dept_ids))
        OR ud.drafting_doc_id IS NOT NULL
      )
```

(Giữ nguyên toàn bộ `AND (p_doc_book_id IS NULL OR ...)` etc. phía dưới.)

### 4. Outer SELECT — thêm 3 columns vào cuối danh sách SELECT:

Thay:
```
    COALESCE(f._is_read, FALSE), f._read_at, f._attachment_count, f._total_count
  FROM filtered f;
```
thành:
```
    COALESCE(f._is_read, FALSE), f._read_at, f._attachment_count, f._total_count,
    COALESCE(f._i_am_recipient, FALSE), f._sent_by_name, f._received_at
  FROM filtered f;
```

**Lưu ý:** SP signature KHÔNG đổi → dùng `CREATE OR REPLACE FUNCTION` (replace atomic), KHÔNG cần DROP FUNCTION.
  </action>
  <verify>
    <automated>grep -n "i_am_recipient BOOLEAN, sent_by_name VARCHAR, received_at TIMESTAMPTZ" e_office_app_new/database/schema/000_schema_v3.0.sql</automated>
  </verify>
  <done>RETURNS TABLE của fn_drafting_doc_get_list chứa 3 field mới; WHERE có clause `OR ud.drafting_doc_id IS NOT NULL`; JOIN sender có mặt.</done>
</task>

<task type="auto">
  <name>Task 2: SP fn_drafting_doc_count_unread — cho phép recipient</name>
  <files>e_office_app_new/database/schema/000_schema_v3.0.sql</files>
  <action>
Edit block `CREATE OR REPLACE FUNCTION edoc.fn_drafting_doc_count_unread(...)` active cuối cùng (tại line ~21438, định danh comment "-- 3. fn_drafting_doc_count_unread").

**GIỮ NGUYÊN signature.**

Thay WHERE:
```
  WHERE (p_dept_ids IS NULL OR d.department_id = ANY(p_dept_ids))
    AND (ud.is_read IS NULL OR ud.is_read = FALSE);
```
thành:
```
  WHERE (
      (p_dept_ids IS NULL OR d.department_id = ANY(p_dept_ids))
      OR ud.drafting_doc_id IS NOT NULL
    )
    AND (ud.is_read IS NULL OR ud.is_read = FALSE);
```

JOIN `LEFT JOIN edoc.user_drafting_docs ud ON ud.drafting_doc_id = d.id AND ud.staff_id = p_staff_id` đã có sẵn → giữ nguyên.

Dùng `CREATE OR REPLACE FUNCTION` (signature không đổi).
  </action>
  <verify>
    <automated>grep -c "OR ud.drafting_doc_id IS NOT NULL" e_office_app_new/database/schema/000_schema_v3.0.sql</automated>
  </verify>
  <done>WHERE của fn_drafting_doc_count_unread (block active line ~21438) chứa `OR ud.drafting_doc_id IS NOT NULL`. grep count ≥ 2 (task 1 + task 2 đều thêm clause này).</done>
</task>

<task type="auto">
  <name>Task 3: SP fn_incoming_doc_get_sendable_staff — thêm p_exclude_staff_id</name>
  <files>e_office_app_new/database/schema/000_schema_v3.0.sql</files>
  <action>
Edit block tại line ~24074 (comment "-- B.23 edoc.fn_incoming_doc_get_sendable_staff").

**Đổi signature** → phải DROP old signature TRƯỚC khi CREATE mới.

Thay toàn bộ block:
```sql
DROP FUNCTION IF EXISTS edoc.fn_incoming_doc_get_sendable_staff(INT);
CREATE OR REPLACE FUNCTION edoc.fn_incoming_doc_get_sendable_staff(
  p_unit_id INT,
  p_dept_ids INT[] DEFAULT NULL
)
RETURNS TABLE (
  staff_id INT, full_name VARCHAR, position_name VARCHAR,
  department_id INT, department_name VARCHAR
)
LANGUAGE plpgsql AS $$
BEGIN
  RETURN QUERY
  SELECT s.id, s.full_name, p.name, s.department_id, d.name
  FROM public.staff s
  LEFT JOIN public.positions p ON p.id = s.position_id
  LEFT JOIN public.departments d ON d.id = s.department_id
  WHERE (
    p_dept_ids IS NOT NULL AND s.department_id = ANY(p_dept_ids)
    OR p_dept_ids IS NULL AND s.department_id IN (
      SELECT dep.id FROM public.departments dep
      WHERE dep.id = p_unit_id OR dep.parent_id = p_unit_id
    )
  )
  AND s.is_locked = FALSE AND s.is_deleted = FALSE
  ORDER BY d.sort_order, d.name, s.full_name;
END;
$$;
```

thành:

```sql
DROP FUNCTION IF EXISTS edoc.fn_incoming_doc_get_sendable_staff(INT);
DROP FUNCTION IF EXISTS edoc.fn_incoming_doc_get_sendable_staff(INT, INT[]);
DROP FUNCTION IF EXISTS edoc.fn_incoming_doc_get_sendable_staff(INT, INT[], INT);
CREATE OR REPLACE FUNCTION edoc.fn_incoming_doc_get_sendable_staff(
  p_unit_id INT,
  p_dept_ids INT[] DEFAULT NULL,
  p_exclude_staff_id INT DEFAULT NULL
)
RETURNS TABLE (
  staff_id INT, full_name VARCHAR, position_name VARCHAR,
  department_id INT, department_name VARCHAR
)
LANGUAGE plpgsql AS $$
BEGIN
  RETURN QUERY
  SELECT s.id, s.full_name, p.name, s.department_id, d.name
  FROM public.staff s
  LEFT JOIN public.positions p ON p.id = s.position_id
  LEFT JOIN public.departments d ON d.id = s.department_id
  WHERE (
    p_dept_ids IS NOT NULL AND s.department_id = ANY(p_dept_ids)
    OR p_dept_ids IS NULL AND s.department_id IN (
      SELECT dep.id FROM public.departments dep
      WHERE dep.id = p_unit_id OR dep.parent_id = p_unit_id
    )
  )
  AND s.is_locked = FALSE AND s.is_deleted = FALSE
  AND (p_exclude_staff_id IS NULL OR s.id != p_exclude_staff_id)
  ORDER BY d.sort_order, d.name, s.full_name;
END;
$$;
```

**Lưu ý 3 DROP statements**: cover old signature (INT), current (INT, INT[]), và new (INT, INT[], INT) — đảm bảo idempotent khi re-apply schema vì `CREATE OR REPLACE` KHÔNG replace khi signature khác → sẽ tạo overload nếu không drop.

Giữ `DO $$ BEGIN RAISE NOTICE '032-B23: fn_incoming_doc_get_sendable_staff rewritten'; END $$;` phía dưới.
  </action>
  <verify>
    <automated>grep -n "p_exclude_staff_id INT DEFAULT NULL" e_office_app_new/database/schema/000_schema_v3.0.sql</automated>
  </verify>
  <done>Signature mới chứa `p_exclude_staff_id INT DEFAULT NULL`; 3 DROP FUNCTION statements với signatures (INT), (INT, INT[]), (INT, INT[], INT).</done>
</task>

<task type="auto">
  <name>Task 4: Apply schema master + verify idempotent + SP count + zero overload</name>
  <files>(runtime — no file write)</files>
  <action>
Chạy 4 commands tuần tự:

**1. Apply LẦN 1:**
```bash
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 < e_office_app_new/database/schema/000_schema_v3.0.sql
```

**2. Apply LẦN 2 (idempotent check — phải ZERO error):**
```bash
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 < e_office_app_new/database/schema/000_schema_v3.0.sql
```

Nếu lần 2 ERROR → FIX Task 1/2/3 (thường là thiếu DROP, hoặc reference column chưa tồn tại).

**3. Count SPs — phải ≥ 386:**
```bash
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM pg_proc WHERE pronamespace IN ('public'::regnamespace, 'edoc'::regnamespace) AND proname LIKE 'fn_%';"
```

**4. Overload check — phải = 0:**
```bash
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM (SELECT n.nspname, p.proname FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace WHERE n.nspname IN ('public','edoc') AND p.proname LIKE 'fn_%' GROUP BY 1,2 HAVING count(*) > 1) t;"
```

**5. Runtime smoke test — fn_drafting_doc_get_list với recipient thật:**

Verify bug (a) fixed bằng test reproduce đã biết:
```bash
# Test: nguyenvana (staff_id=2) phải thấy ít nhất 1 VB dự thảo nếu có record trong user_drafting_docs
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
SELECT COUNT(*) AS visible_count
FROM edoc.fn_drafting_doc_get_list(
  0, 2,  -- p_unit_id=0, p_staff_id=2 (nguyenvana)
  NULL, NULL, NULL, NULL, NULL, NULL, NULL, NULL, NULL,
  1, 100,
  ARRAY[(SELECT department_id FROM public.staff WHERE id = 2)]::INT[]
) WHERE i_am_recipient = TRUE;
"
```
→ Nếu có record trong `edoc.user_drafting_docs` với `staff_id=2`, số này phải ≥ 1.

**6. Runtime smoke test — fn_incoming_doc_get_sendable_staff exclude self:**
```bash
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
SELECT COUNT(*) FROM edoc.fn_incoming_doc_get_sendable_staff(1, NULL, 1) WHERE staff_id = 1;
"
```
→ Phải = 0 (admin ID=1 bị loại).
  </action>
  <verify>
    <automated>docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -tAc "SELECT count(*) FROM (SELECT n.nspname, p.proname FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace WHERE n.nspname IN ('public','edoc') AND p.proname LIKE 'fn_%' GROUP BY 1,2 HAVING count(*) > 1) t;"</automated>
  </verify>
  <done>
    - Apply lần 2 ZERO error
    - SP count ≥ 386
    - Overload count = 0
    - fn_incoming_doc_get_sendable_staff(1, NULL, 1) không return staff_id=1
    - fn_drafting_doc_get_list trả về column i_am_recipient
  </done>
</task>

<task type="auto">
  <name>Task 5: Update backend repos — drafting/incoming/outgoing getSendableStaff + DraftingDocListRow</name>
  <files>
e_office_app_new/backend/src/repositories/drafting-doc.repository.ts
e_office_app_new/backend/src/repositories/incoming-doc.repository.ts
e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts
  </files>
  <action>

### 1. `drafting-doc.repository.ts`

**A. Interface `DraftingDocListRow`** — thêm 3 field cuối (sau `total_count: number;`):
```typescript
  attachment_count: number;
  total_count: number;
  i_am_recipient: boolean;
  sent_by_name: string | null;
  received_at: string | null;
}
```
(`is_read` đã có ở line 44 → KHÔNG thêm lại.)

**B. Method `getSendableStaff`** — thêm param `excludeStaffId`:

Thay:
```typescript
  async getSendableStaff(unitId: number): Promise<SendableStaffRow[]> {
    return callFunction<SendableStaffRow>('edoc.fn_incoming_doc_get_sendable_staff', [unitId]);
  },
```
thành:
```typescript
  async getSendableStaff(unitId: number, excludeStaffId?: number | null): Promise<SendableStaffRow[]> {
    return callFunction<SendableStaffRow>(
      'edoc.fn_incoming_doc_get_sendable_staff',
      [unitId, null, excludeStaffId ?? null],
    );
  },
```

### 2. `incoming-doc.repository.ts` (line ~265)

Thay:
```typescript
  async getSendableStaff(unitId: number): Promise<SendableStaffRow[]> {
    return callFunction<SendableStaffRow>('edoc.fn_incoming_doc_get_sendable_staff', [unitId]);
  },
```
thành:
```typescript
  async getSendableStaff(unitId: number, excludeStaffId?: number | null): Promise<SendableStaffRow[]> {
    return callFunction<SendableStaffRow>(
      'edoc.fn_incoming_doc_get_sendable_staff',
      [unitId, null, excludeStaffId ?? null],
    );
  },
```

### 3. `outgoing-doc.repository.ts` (line ~197)

Thay tương tự như incoming-doc.repository.ts.

**Lưu ý**: KHÔNG đổi existing callers cách truyền unitId — chỉ thêm optional param. Routes cũ chưa pass excludeStaffId sẽ vẫn chạy đúng (excludeStaffId=null → không filter), nhưng Task 7 sẽ update route để pass staffId vào.
  </action>
  <verify>
    <automated>grep -n "excludeStaffId" e_office_app_new/backend/src/repositories/drafting-doc.repository.ts e_office_app_new/backend/src/repositories/incoming-doc.repository.ts e_office_app_new/backend/src/repositories/outgoing-doc.repository.ts</automated>
  </verify>
  <done>
    - DraftingDocListRow chứa i_am_recipient + sent_by_name + received_at
    - 3 repos đều có `getSendableStaff(unitId, excludeStaffId?)` với 3-element array truyền vào callFunction
    - `is_read` KHÔNG bị duplicate trong interface
  </done>
</task>

<task type="auto">
  <name>Task 6: Update backend routes — pass staffId to getSendableStaff (3 routes)</name>
  <files>
e_office_app_new/backend/src/routes/drafting-doc.ts
e_office_app_new/backend/src/routes/incoming-doc.ts
e_office_app_new/backend/src/routes/outgoing-doc.ts
  </files>
  <action>

### 1. `drafting-doc.ts` — endpoint `/:id/danh-sach-gui` (line ~428)

Thay:
```typescript
router.get('/:id/danh-sach-gui', async (req: Request, res: Response) => {
  try {
    const { departmentId } = (req as AuthRequest).user;
    const ancestorUnitId = await resolveAncestorUnit(departmentId);
    const rows = await draftingDocRepository.getSendableStaff(ancestorUnitId);
    res.json({ success: true, data: rows });
  } catch (error) {
    handleDbError(error, res);
  }
});
```
thành:
```typescript
router.get('/:id/danh-sach-gui', async (req: Request, res: Response) => {
  try {
    const { staffId, departmentId } = (req as AuthRequest).user;
    const ancestorUnitId = await resolveAncestorUnit(departmentId);
    const rows = await draftingDocRepository.getSendableStaff(ancestorUnitId, staffId);
    res.json({ success: true, data: rows });
  } catch (error) {
    handleDbError(error, res);
  }
});
```

### 2. `incoming-doc.ts` — endpoint `/:id/danh-sach-gui` (line ~489)

Tìm handler, destructure thêm `staffId` từ `(req as AuthRequest).user`, và pass `staffId` làm arg 2 của `getSendableStaff(ancestorUnitId, staffId)`.

### 3. `outgoing-doc.ts` — endpoint `/:id/danh-sach-gui` (line ~522)

Tương tự.

**Kiểm tra type**: `AuthRequest.user` đã có `staffId` sẵn (route line ~23 drafting-doc.ts đã dùng `const { staffId, departmentId, isAdmin } = (req as AuthRequest).user;` cho endpoint khác).

**KHÔNG đổi endpoint POST `/:id/gui`** — task này chỉ đụng endpoint GET danh-sach-gui.
  </action>
  <verify>
    <automated>grep -c "getSendableStaff(ancestorUnitId, staffId)" e_office_app_new/backend/src/routes/drafting-doc.ts e_office_app_new/backend/src/routes/incoming-doc.ts e_office_app_new/backend/src/routes/outgoing-doc.ts</automated>
  </verify>
  <done>
    - 3 files đều có pattern `getSendableStaff(ancestorUnitId, staffId)`
    - `staffId` được destructure từ AuthRequest.user
    - Backend compile clean: chạy `cd e_office_app_new/backend && npx tsc --noEmit` phải 0 error
  </done>
</task>

<task type="auto">
  <name>Task 7: Update frontend van-ban-du-thao/page.tsx — interface + badge render + rowClassName</name>
  <files>
e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx
e_office_app_new/frontend/src/app/globals.css
  </files>
  <action>

### 1. Interface `DraftingDoc` (line 28-66)

Thêm 4 field sau `created_at: string;` (line 58), TRƯỚC `doc_book_name`:
```typescript
  created_at: string;
  i_am_recipient: boolean;
  sent_by_name: string | null;
  received_at: string | null;
  is_read: boolean;
  doc_book_name: string;
  // ...
```

(`is_read` hiện CHƯA có trong interface này dù backend đã trả — thêm luôn để render rowClassName unread highlight.)

### 2. Import Tooltip đã có sẵn (line 6 `Tooltip` trong destructure antd) → không cần thêm.

### 3. Column "Trích yếu" (line 407) — thêm inline badge

Thay toàn bộ column definition:
```tsx
    {
      title: 'Trích yếu', dataIndex: 'abstract', ellipsis: true,
      render: (val, r) => (
        <Tooltip title={val}>
          <a style={{ fontWeight: 500 }} href={`/van-ban-du-thao/${r.id}`}>{val}</a>
        </Tooltip>
      ),
    },
```
thành:
```tsx
    {
      title: 'Trích yếu', dataIndex: 'abstract', ellipsis: true,
      render: (val, r) => (
        <div style={{ display: 'flex', alignItems: 'center', gap: 8, minWidth: 0 }}>
          <Tooltip title={val}>
            <a style={{ fontWeight: 500, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }} href={`/van-ban-du-thao/${r.id}`}>{val}</a>
          </Tooltip>
          {r.i_am_recipient && (
            <Tooltip title={`Do ${r.sent_by_name ?? 'đồng nghiệp'} gửi lúc ${r.received_at ? dayjs(r.received_at).format('DD/MM/YYYY HH:mm') : ''}`}>
              <Tag style={{ margin: 0, background: '#fff7e6', color: '#d48806', border: '1px solid #ffd591', flexShrink: 0 }}>
                📩 Gửi cho tôi
              </Tag>
            </Tooltip>
          )}
        </div>
      ),
    },
```

### 4. Table — thêm `rowClassName` prop

Tại `<Table<DraftingDoc>` (line 604), thêm prop SAU `scroll={{ x: 1100 }}`:
```tsx
  scroll={{ x: 1100 }}
  rowClassName={(record) => record.i_am_recipient && !record.is_read ? 'drafting-row-unread-recipient' : ''}
  rowSelection={{...}}
```

### 5. File `e_office_app_new/frontend/src/app/globals.css`

Thêm vào CUỐI FILE (sau block CSS hiện có):
```css
/* Fix gửi VB dự thảo — highlight unread recipient rows (2026-04-24) */
.drafting-row-unread-recipient td:first-child {
  border-left: 3px solid #fa8c16;
}
```

**Nguyên tắc CSS (CLAUDE.md)**: layout/structural styles dùng CSS class trong globals.css → tuân thủ.
  </action>
  <verify>
    <automated>grep -n "i_am_recipient" e_office_app_new/frontend/src/app/\(main\)/van-ban-du-thao/page.tsx && grep -n "drafting-row-unread-recipient" e_office_app_new/frontend/src/app/globals.css</automated>
  </verify>
  <done>
    - Interface `DraftingDoc` có `i_am_recipient`, `sent_by_name`, `received_at`, `is_read`
    - Column "Trích yếu" render inline Tag "📩 Gửi cho tôi" khi `i_am_recipient === true`
    - Table có `rowClassName` set `drafting-row-unread-recipient` cho unread recipient
    - globals.css chứa class `.drafting-row-unread-recipient` với border-left orange
    - Frontend typecheck: `cd e_office_app_new/frontend && npx tsc --noEmit` — 0 error
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: User verify UAT — recipient visibility + exclude self</name>
  <what-built>
Đã fix xong 2 bugs:
- Bug (a): SP `fn_drafting_doc_get_list` + `fn_drafting_doc_count_unread` cho phép recipient khác dept thấy VB.
- Bug (b): SP `fn_incoming_doc_get_sendable_staff` thêm `p_exclude_staff_id`; 3 routes (drafting/incoming/outgoing) đã pass staffId.
- Frontend: VB dự thảo list có badge "📩 Gửi cho tôi" + row border orange cho unread recipient.

Schema đã apply 2 lần (idempotent), SP count ≥ 386, zero overload.
  </what-built>
  <how-to-verify>
1. **Khởi động backend + frontend (nếu chưa chạy):**
   - Terminal 1: `cd e_office_app_new/backend && npm run dev`
   - Terminal 2: `cd e_office_app_new/frontend && npm run dev`

2. **Test Bug (a) — Recipient thấy VB được gửi:**
   - Mở `http://localhost:3000/login`
   - Login: `admin / Admin@123`
   - Vào Văn bản dự thảo → chọn 1 VB đã approved → click "Gửi" → chọn `nguyenvana` → submit
   - Logout → login lại: `nguyenvana / Admin@123`
   - Vào Văn bản dự thảo → phải thấy VB vừa gửi với badge màu cam "📩 Gửi cho tôi"
   - Row có viền trái màu orange (chưa đọc)
   - Hover badge → Tooltip hiển thị "Do [tên admin] gửi lúc [time]"

3. **Test Bug (b) — Admin KHÔNG thấy chính mình trong sendable:**
   - Login lại: `admin / Admin@123`
   - Vào VB dự thảo → mở 1 VB → click "Gửi"
   - Trong dialog chọn người nhận: KHÔNG có user "admin" (chính user đang login)
   - Tương tự: thử VB đến (/van-ban-den/[id] → Gửi), VB đi (/van-ban-di/[id] → Gửi) — đều không thấy chính mình.

4. **Không còn regression:**
   - Các VB dự thảo tạo bởi chính user (không phải recipient) vẫn hiển thị bình thường, không có badge.
   - Filter theo đơn vị / sổ VB / từ khóa vẫn hoạt động.
   - Count unread badge trên menu vẫn đúng.
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 bugs verified visually, or describe the issue.</resume-signal>
</task>

</tasks>

<verification>
## Phase-level verification (sau khi tất cả 8 task done)

### Automated
- [x] Schema apply 2 lần ZERO error (Task 4)
- [x] SP count ≥ 386 (Task 4)
- [x] Zero SP overload (Task 4)
- [x] Backend tsc --noEmit 0 error (Task 6)
- [x] Frontend tsc --noEmit 0 error (Task 7)
- [x] Runtime SQL: `fn_drafting_doc_get_list(0, 2, ...)` returns row with `i_am_recipient=true` khi staff 2 có entry trong `user_drafting_docs` (Task 4)
- [x] Runtime SQL: `fn_incoming_doc_get_sendable_staff(1, NULL, 1)` returns 0 rows với `staff_id=1` (Task 4)

### Manual (Task 8)
- [x] Recipient nguyenvana thấy VB admin gửi, badge "📩 Gửi cho tôi" hiển thị
- [x] Admin KHÔNG thấy chính mình trong dialog sendable (3 modules)
- [x] Unread recipient row có border orange bên trái
</verification>

<success_criteria>
## SUCCESS = all true

1. **Bug (a) resolved:** Recipient `nguyenvana` (dept khác admin) login vào `/van-ban-du-thao` thấy VB được admin gửi → đã verify trong UAT session.
2. **Bug (b) resolved:** Admin login → dialog "Gửi" ở 3 module (VB dự thảo, VB đến, VB đi) KHÔNG chứa chính admin.
3. **No regression:** Filter, search, count unread, approve/release/retract vẫn hoạt động bình thường.
4. **Schema hygiene:** `000_schema_v3.0.sql` vẫn idempotent, SP count stable ≥ 386, zero overload.
5. **TypeScript clean:** Backend + Frontend tsc --noEmit đều 0 error.
6. **NO auto-commit:** Code changes đã SAVE nhưng KHÔNG git commit (user sẽ confirm thủ công).
</success_criteria>

<output>
After all 8 tasks complete (incl. Task 8 human-verify approval):

1. Không tạo SUMMARY.md cho quick mode.
2. Trả kết quả cho user với tóm tắt thay đổi:
   - 3 SPs updated + idempotent verified (file line numbers)
   - 3 repos + 3 routes backend updated
   - 1 frontend page + globals.css updated
   - UAT result: Bug (a) ✅ / Bug (b) ✅
3. Note rõ "Chưa commit — chờ user confirm".
</output>
