---
phase: quick-260425-g4z
plan: fix-hscv-consolidate-4-archive-migration
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/database/schema/000_schema_v3.0.sql
  - e_office_app_new/database/seed/002_demo_data.sql
  - deploy/smoke-test-hscv.ps1
  - deploy/deploy-v2-kh-test.ps1
autonomous: false
requirements:
  - QLVB-HSCV-CONSOLIDATE
  - QLVB-HSCV-STATUS-FIX
  - QLVB-HSCV-SMOKE-TEST
must_haves:
  truths:
    - "Re-apply 000_schema_v3.0.sql lần thứ 2 chạy ZERO error (idempotent)"
    - "fn_handling_doc_get_by_id trả 36 cột bao gồm number, doc_book_id, doc_book_name, cancel_reason, cancelled_at, cancelled_by"
    - "Bảng edoc.handling_docs có 3 cột cancel_reason / cancelled_at / cancelled_by"
    - "Bảng edoc.opinion_handling_docs có 4 cột forwarded_to_staff_id / forwarded_at / forward_note / parent_opinion_id"
    - "Bảng edoc.handling_doc_history tồn tại với 2 index (handling_doc_id, created_at DESC)"
    - "7 SP mới hoạt động: fn_handling_doc_reopen, fn_handling_doc_get_next_number, fn_handling_doc_assign_number, fn_handling_doc_cancel, fn_handling_doc_transfer, fn_handling_doc_history_list, fn_opinion_forward"
    - "fn_handling_doc_approve check status=3 → set status=4 + progress=100 (KHÔNG còn 2→3)"
    - "fn_handling_doc_reject check status=3 → set status=-1"
    - "fn_handling_doc_cancel reject status IN (-1, -2) (Bug B fix)"
    - "Smoke test 16 testcase HSCV PASS hết 16/16 trên backend local"
    - "deploy-v2-kh-test.ps1 sau khi backend up phải gọi smoke test, fail thì block deploy"
  artifacts:
    - path: "e_office_app_new/database/schema/000_schema_v3.0.sql"
      provides: "Master schema consolidated với 4 archive migrations + status flow fixes"
      contains: "fn_handling_doc_reopen, fn_handling_doc_assign_number, fn_handling_doc_cancel, fn_handling_doc_transfer, fn_opinion_forward, edoc.handling_doc_history"
    - path: "e_office_app_new/database/seed/002_demo_data.sql"
      provides: "Demo data cho field mới (cancelled HSCV + history records)"
      contains: "cancel_reason, handling_doc_history INSERT"
    - path: "deploy/smoke-test-hscv.ps1"
      provides: "16-testcase API smoke test cho HSCV E2E flow"
      min_lines: 200
    - path: "deploy/deploy-v2-kh-test.ps1"
      provides: "Deploy script với hook smoke test sau backend health check"
      contains: "smoke-test-hscv.ps1"
  key_links:
    - from: "schema/000_schema_v3.0.sql"
      to: "Postgres edoc schema"
      via: "psql -f apply lần 2 zero error"
      pattern: "DROP FUNCTION IF EXISTS với signature cụ thể"
    - from: "deploy-v2-kh-test.ps1"
      to: "smoke-test-hscv.ps1"
      via: "& powershell -File ... -Force sau khi backend healthy"
      pattern: "if (\\$LASTEXITCODE -ne 0) { Err 'Smoke test fail' }"
---

<objective>
Fix HSCV (Hồ sơ Công việc) bằng 3 commit atomic:

1. **Commit 1** — Consolidate 4 archive migrations vào master schema + thêm seed demo cho field mới
2. **Commit 2** — Fix status flow trong 3 SP: approve check status=3 (thay vì 2), reject tương tự, cancel reject -1/-2
3. **Commit 3** — Tạo smoke-test-hscv.ps1 (16 testcase E2E) + hook vào deploy-v2-kh-test.ps1

Purpose:
- Schema master hiện thiếu 6 SP HSCV nâng cao (`fn_handling_doc_reopen`, `assign_number`, `get_next_number`, `cancel`, `transfer`, `history_list`) + 1 SP `fn_opinion_forward` + 7 cột schema mới đã được apply manual vào DB local nhưng CHƯA vào master file → next reset DB sẽ mất.
- 3 SP `fn_handling_doc_approve/reject/cancel` follow logic cũ (.NET) mâu thuẫn với 5-step flow đã shipped trên frontend → toolbar buttons "Duyệt"/"Từ chối"/"Hủy" trả lỗi runtime.
- Chưa có smoke test tự động cho HSCV → rủi ro regression khi deploy KH test.

Output:
- Master schema idempotent (re-apply lần 2 zero error)
- 3 SP fix status flow đúng với frontend
- Script smoke test 16 testcase + tích hợp vào deploy pipeline
- 3 commit atomic riêng biệt (user pause sau mỗi commit để review)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# 4 archive migrations — source code đầy đủ cho consolidation
@e_office_app_new/database/archive/v1.0-migrations/quick_260418_hlj_hscv.sql
@e_office_app_new/database/archive/v1.0-migrations/quick_260418_jsd_hscv_cancel.sql
@e_office_app_new/database/archive/v1.0-migrations/quick_260418_jsd_hscv_transfer.sql
@e_office_app_new/database/archive/v1.0-migrations/quick_260418_jsd_opinion_forward.sql

# Master schema — sẽ edit (27,335 dòng — chỉ đọc các phần liên quan HSCV)
@e_office_app_new/database/schema/000_schema_v3.0.sql

# Seed demo — sẽ edit (490 dòng — section HSCV ở dòng 270-316)
@e_office_app_new/database/seed/002_demo_data.sql

# Backend đã wired đầy đủ routes + repository — KHÔNG sửa, dùng làm reference cho smoke test
@e_office_app_new/backend/src/routes/handling-doc.ts
@e_office_app_new/backend/src/repositories/handling-doc.repository.ts

# Deploy pattern reference
@deploy/deploy-v2-kh-test.ps1
@deploy/pre-push-check.ps1

<interfaces>
<!-- ====== State hiện tại của master schema (đã verify bằng grep + Read) ====== -->

Vị trí trong master schema 000_schema_v3.0.sql:
  - Dòng 12695-12732: CREATE TABLE edoc.handling_docs (CHƯA có cancel_reason / cancelled_at / cancelled_by)
  - Dòng 13272-13279: CREATE TABLE edoc.opinion_handling_docs (CHƯA có forwarded_to_staff_id / forwarded_at / forward_note / parent_opinion_id)
  - Dòng 3149-3177: fn_handling_doc_approve — BUG: check v_status <> 2, set status=3 (CẦN sửa: <> 3, set status=4 + progress=100)
  - Dòng 3465-3513: fn_handling_doc_get_by_id — 28 cột (THIẾU 8 cột: number, sub_number, doc_book_id, doc_book_name, cancel_reason, cancelled_at, cancelled_by)
  - Dòng 3826-3860: fn_handling_doc_reject — BUG: check v_status <> 2 (CẦN sửa: <> 3)
  - Dòng 6157-6174: fn_opinion_get_list — 6 cột (CẦN extend: 11 cột với 5 cột forward)
  - KHÔNG có: fn_handling_doc_reopen, fn_handling_doc_get_next_number, fn_handling_doc_assign_number, fn_handling_doc_cancel, fn_handling_doc_transfer, fn_handling_doc_history_list, fn_opinion_forward, table edoc.handling_doc_history

Backend đã wired routes (KHÔNG cần sửa):
  - POST /:id/mo-lai → repository.reopen → fn_handling_doc_reopen (line 575)
  - POST /:id/lay-so → repository.assignNumber → fn_handling_doc_assign_number (line 595)
  - POST /:id/y-kien/:opinionId/chuyen-tiep → repository.forwardOpinion → fn_opinion_forward (line 622)
  - POST /:id/chuyen-tiep → repository.transfer → fn_handling_doc_transfer (line 652)
  - GET /:id/lich-su → repository.getHistory → fn_handling_doc_history_list (line 678)
  - POST /:id/huy → repository.cancel → fn_handling_doc_cancel (line 693)

Repository row interfaces (handling-doc.repository.ts) ĐÃ CÓ field cancel_reason/cancelled_at/cancelled_by ở
HandlingDocDetailRow (dòng 53-61) + forward fields ở OpinionRow (dòng 88-93). Khi SP master schema thiếu field
→ pg trả undefined → frontend render rỗng (KHÔNG crash, nhưng không hoạt động đúng).

Schema column types thực tế của handling_docs (line 12695-12732):
  - id BIGINT, unit_id INT, department_id INT, name VARCHAR(500)
  - doc_book_id INT (đã có), number INT (đã có), sub_number VARCHAR(20) (đã có)
  - status SMALLINT DEFAULT 0, progress SMALLINT DEFAULT 0
  - curator INT (KHÔNG phải curator_id — SP alias h.curator AS curator_id)
  - signer INT (KHÔNG phải signer_id)

Schema column types của opinion_handling_docs (line 13272-13279):
  - id BIGINT, handling_doc_id BIGINT NOT NULL, staff_id INT NOT NULL
  - content TEXT NOT NULL, attachment_path VARCHAR(1000), created_at TIMESTAMPTZ
  - 4 cột forward: CHƯA có
</interfaces>
</context>

<tasks>

<!-- ============================================================
     TASK 1 — COMMIT 1: Schema consolidation + seed demo
     ============================================================ -->

<task type="auto">
  <name>Task 1: Consolidate 4 archive migrations vào master schema + thêm seed demo cho field mới (Commit 1)</name>
  <files>
    e_office_app_new/database/schema/000_schema_v3.0.sql
    e_office_app_new/database/seed/002_demo_data.sql
  </files>
  <action>
**A. Edit `e_office_app_new/database/schema/000_schema_v3.0.sql`:**

A.1) **ALTER edoc.handling_docs** — thêm 3 cột cancel (idempotent với `ADD COLUMN IF NOT EXISTS`).

Đặt block ALTER NGAY SAU `CREATE TABLE IF NOT EXISTS edoc.handling_docs (...)` ở dòng 12695-12732 (trước CREATE SEQUENCE):

```sql
-- Gap D (HDSD III.2.5): Hủy HSCV với lý do
ALTER TABLE edoc.handling_docs
  ADD COLUMN IF NOT EXISTS cancel_reason TEXT,
  ADD COLUMN IF NOT EXISTS cancelled_at TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS cancelled_by INT;
```

A.2) **ALTER edoc.opinion_handling_docs** — thêm 4 cột forward + FK self-reference.

Đặt block NGAY SAU `CREATE TABLE IF NOT EXISTS edoc.opinion_handling_docs (...)` ở dòng 13272-13279:

```sql
-- Gap E (HDSD III.2.6): Chuyển tiếp ý kiến HSCV
ALTER TABLE edoc.opinion_handling_docs
  ADD COLUMN IF NOT EXISTS forwarded_to_staff_id INT,
  ADD COLUMN IF NOT EXISTS forwarded_at TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS forward_note TEXT,
  ADD COLUMN IF NOT EXISTS parent_opinion_id BIGINT;

DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = 'fk_opinion_parent') THEN
        ALTER TABLE edoc.opinion_handling_docs
          ADD CONSTRAINT fk_opinion_parent
          FOREIGN KEY (parent_opinion_id)
          REFERENCES edoc.opinion_handling_docs(id)
          ON DELETE SET NULL;
    END IF;
EXCEPTION WHEN duplicate_object THEN NULL;
END $$;
```

A.3) **CREATE TABLE edoc.handling_doc_history** + 2 index.

Tìm vị trí thích hợp gần các bảng handling khác (sau `staff_handling_docs` ở dòng ~13797), insert:

```sql
-- Gap F (HDSD III.2.7): History cho transfer HSCV
CREATE TABLE IF NOT EXISTS edoc.handling_doc_history (
    id BIGSERIAL PRIMARY KEY,
    handling_doc_id BIGINT NOT NULL REFERENCES edoc.handling_docs(id) ON DELETE CASCADE,
    action_type VARCHAR(50) NOT NULL,
    from_staff_id INT REFERENCES public.staff(id),
    to_staff_id INT REFERENCES public.staff(id),
    note TEXT,
    created_by INT REFERENCES public.staff(id),
    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_handling_doc_history_doc_id
  ON edoc.handling_doc_history(handling_doc_id);
CREATE INDEX IF NOT EXISTS idx_handling_doc_history_created_at
  ON edoc.handling_doc_history(created_at DESC);
```

A.4) **REPLACE fn_handling_doc_get_by_id** ở dòng 3465-3513 — bump từ 28 cột lên **36 cột** (thêm 4 cột số: number, sub_number, doc_book_id, doc_book_name + 3 cột cancel + JOIN doc_books).

Pattern: DROP với signature cụ thể TRƯỚC CREATE OR REPLACE (vì RETURNS TABLE thay đổi cấu trúc):

```sql
DROP FUNCTION IF EXISTS edoc.fn_handling_doc_get_by_id(bigint);

CREATE OR REPLACE FUNCTION edoc.fn_handling_doc_get_by_id(p_id BIGINT)
RETURNS TABLE(
    id bigint,
    unit_id integer,
    unit_name character varying,
    department_id integer,
    department_name character varying,
    name character varying,
    abstract text,
    comments text,
    doc_notation character varying,
    doc_type_id integer,
    doc_type_name character varying,
    doc_field_id integer,
    doc_field_name character varying,
    start_date timestamp with time zone,
    end_date timestamp with time zone,
    curator_id integer,
    curator_name text,
    signer_id integer,
    signer_name text,
    "status" smallint,
    progress smallint,
    workflow_id integer,
    workflow_name character varying,
    parent_id bigint,
    parent_name character varying,
    is_from_doc boolean,
    created_by integer,
    created_at timestamp with time zone,
    updated_at timestamp with time zone,
    "number" integer,
    sub_number character varying,
    doc_book_id integer,
    doc_book_name character varying,
    cancel_reason text,
    cancelled_at timestamp with time zone,
    cancelled_by integer
)
LANGUAGE plpgsql AS $$ ... $$;
```

Body copy từ `archive/quick_260418_jsd_hscv_cancel.sql` (dòng 99-149) — đã có đầy đủ 36 cột + LEFT JOIN doc_books.

A.5) **CREATE 7 SP mới** — copy từ archive với DROP IF EXISTS signature cụ thể:

Đặt mỗi SP ở vị trí đúng theo alphabet trong section functions (giữa các fn_handling_doc_* hoặc cuối):

1. `fn_handling_doc_reopen(BIGINT, INT)` — copy từ `quick_260418_hlj_hscv.sql` dòng 15-52 (status=4 → 1, GIỮ progress=100)
2. `fn_handling_doc_get_next_number(INT, INT)` — copy từ `quick_260418_hlj_hscv.sql` dòng 58-77
3. `fn_handling_doc_assign_number(BIGINT, INT, INT)` — copy từ `quick_260418_hlj_hscv.sql` dòng 84-132
4. `fn_handling_doc_cancel(BIGINT, INT, TEXT)` — copy từ `quick_260418_jsd_hscv_cancel.sql` dòng 10-51 — **THÊM check status IN (-1, -2) reject ngay sau check status=4** (Bug B fix sẽ thực hiện ở Commit 2 — Commit 1 copy nguyên bản):

```sql
-- Bug B fix sẽ làm ở Commit 2: thêm IF v_status IN (-1, -2) THEN reject
```

5. `fn_handling_doc_transfer(BIGINT, INT, INT, TEXT, INT)` — copy từ `quick_260418_jsd_hscv_transfer.sql` dòng 22-88
6. `fn_handling_doc_history_list(BIGINT)` — copy từ `quick_260418_jsd_hscv_transfer.sql` dòng 91-128
7. `fn_opinion_forward(BIGINT, INT, INT, TEXT)` — copy từ `quick_260418_jsd_opinion_forward.sql` dòng 25-76

Mỗi SP đặt trước `CREATE OR REPLACE FUNCTION`:
```sql
DROP FUNCTION IF EXISTS edoc.fn_handling_doc_reopen(bigint, integer);
CREATE OR REPLACE FUNCTION edoc.fn_handling_doc_reopen(p_id BIGINT, p_user_id INT) ...
```

A.6) **REPLACE fn_opinion_get_list** ở dòng 6157-6174 — bump từ 6 cột lên 11 cột (thêm 5 cột forward + LEFT JOIN staff cho forwarded_to_name).

Pattern: DROP signature cụ thể trước. Body copy từ `quick_260418_jsd_opinion_forward.sql` dòng 78-122.

```sql
DROP FUNCTION IF EXISTS edoc.fn_opinion_get_list(bigint);
CREATE OR REPLACE FUNCTION edoc.fn_opinion_get_list(p_doc_id BIGINT) ...
```

**B. Edit `e_office_app_new/database/seed/002_demo_data.sql`:**

KHÔNG sửa logic cũ (15 HSCV với status mix 0..-2). THÊM 3 INSERT block sau dòng 316 (`SELECT setval('edoc.handling_docs_id_seq', 1000, true);`):

```sql
-- ─── 14b. HSCV demo cho field mới (Gap D / Lấy số / Cancel) ────────
-- 1 HSCV đã lấy số (status=3, number=1, doc_book_id=1)
UPDATE edoc.handling_docs
SET number = 1, sub_number = NULL, doc_book_id = 1
WHERE id = 10 AND status = 3;

-- 1 HSCV bị hủy (chuyển status 13 (Hoàn thành) → -3 KHÔNG được, dùng id mới)
INSERT INTO edoc.handling_docs (
  id, unit_id, department_id, name, abstract, doc_notation,
  doc_type_id, doc_field_id, doc_book_id,
  start_date, end_date, received_date,
  curator, status, progress,
  cancel_reason, cancelled_at, cancelled_by, created_by
)
SELECT 16, 1, 1,
  'HSCV demo bị hủy 16',
  'Demo cho Gap D — hủy với lý do',
  'HSCV-16', 1, 1, 1,
  NOW() - INTERVAL '5 days', NOW() + INTERVAL '10 days', NOW() - INTERVAL '5 days',
  2, (-3)::smallint, 50::smallint,
  'Hủy do thay đổi yêu cầu', NOW() - INTERVAL '2 days', 1, 1
WHERE NOT EXISTS (SELECT 1 FROM edoc.handling_docs WHERE id = 16);

-- 3 record handling_doc_history demo
INSERT INTO edoc.handling_doc_history (
  handling_doc_id, action_type, from_staff_id, to_staff_id, note, created_by, created_at
)
SELECT * FROM (VALUES
  (1::bigint, 'transfer'::varchar, 2, 3, 'Chuyển cho anh xử lý giúp', 1, NOW() - INTERVAL '3 days'),
  (1::bigint, 'transfer'::varchar, 3, 4, 'Chuyển tiếp', 3, NOW() - INTERVAL '1 day'),
  (16::bigint, 'cancel'::varchar, 2, NULL::int, 'Hủy do thay đổi yêu cầu', 1, NOW() - INTERVAL '2 days')
) AS v(handling_doc_id, action_type, from_staff_id, to_staff_id, note, created_by, created_at)
WHERE NOT EXISTS (SELECT 1 FROM edoc.handling_doc_history WHERE handling_doc_id = v.handling_doc_id AND created_at = v.created_at);

-- (Optional) 1 opinion forward demo — KHÔNG bắt buộc, nếu opinion_handling_docs chưa có data demo thì bỏ qua
```

Cuối seed, update sequence guard trong DO block dòng 460-477:
- Bump baseline check `IF v_total < 220 THEN` → `IF v_total < 222 THEN` (thêm 1 HSCV mới + 3 history records – field lỗi cũ đã đếm)

**C. Verify (manual sau khi edit, trước khi commit):**

```bash
# 1. Re-apply schema lần 2 ngay lập tức — phải zero error
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
  -f - < e_office_app_new/database/schema/000_schema_v3.0.sql 2>&1 | tail -20

# 2. Count SPs handling_doc — phải ≥ 30
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -t -c "
SELECT count(*) FROM pg_proc p
JOIN pg_namespace n ON n.oid = p.pronamespace
WHERE n.nspname = 'edoc'
  AND (p.proname LIKE 'fn_handling_doc%' OR p.proname IN ('fn_opinion_forward', 'fn_opinion_get_list'));"

# 3. Verify fn_handling_doc_get_by_id có cancel_reason + doc_book_id
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -t -c "
SELECT pg_get_function_result(oid) FROM pg_proc
WHERE proname = 'fn_handling_doc_get_by_id'
  AND pronamespace = 'edoc'::regnamespace;" | grep -E 'cancel_reason|doc_book_id'

# 4. Verify NO SP overload duplicate
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -t -c "
SELECT count(*) FROM (
  SELECT proname, count(*) FROM pg_proc
  WHERE pronamespace IN ('public'::regnamespace, 'edoc'::regnamespace)
    AND proname LIKE 'fn_%'
  GROUP BY proname HAVING count(*) > 1
) t;"
# Phải = 0

# 5. Re-apply seed 002 lần 2 — phải zero error
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
  -f - < e_office_app_new/database/seed/002_demo_data.sql 2>&1 | tail -10
```

**D. Commit (CHỈ khi user yêu cầu rõ ràng "commit"):**

KHÔNG TỰ ĐỘNG COMMIT. PAUSE và báo user verify trước:
```
[COMMIT 1 READY] Đã consolidate 4 archive migrations + thêm seed demo.
Verify checklist:
  - Schema apply lần 2: ZERO error
  - 30+ fn_handling_doc/opinion SPs
  - get_by_id có cancel_reason + doc_book_id
  - 0 SP overload
  - Seed 002 apply lần 2: ZERO error
User confirm "commit 1" để proceed.
```

Khi user xác nhận, commit message:
```
feat(hscv): consolidate 4 archive migrations vao master schema v3.0

- ALTER handling_docs +3 col (cancel_reason, cancelled_at, cancelled_by)
- ALTER opinion_handling_docs +4 col (forwarded_*, parent_opinion_id) + FK
- CREATE TABLE handling_doc_history + 2 index
- CREATE 7 SP: reopen, get_next_number, assign_number, cancel, transfer, history_list, opinion_forward
- REPLACE fn_handling_doc_get_by_id: 28 → 36 col (number, doc_book, cancel_*)
- REPLACE fn_opinion_get_list: 6 → 11 col (forward fields)
- Seed 002: +1 HSCV cancelled + 3 history records
- Idempotent: re-apply lan 2 ZERO error verified
```
  </action>
  <verify>
    <automated>
      docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - < e_office_app_new/database/schema/000_schema_v3.0.sql 2>&1 | grep -iE 'error|fail' | head -5
      # Output phải RỖNG (zero error)

      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -t -c "SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace WHERE n.nspname = 'edoc' AND (p.proname LIKE 'fn_handling_doc%' OR p.proname IN ('fn_opinion_forward', 'fn_opinion_get_list'));"
      # Phải >= 30

      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -t -c "SELECT pg_get_function_result(oid) FROM pg_proc WHERE proname = 'fn_handling_doc_get_by_id' AND pronamespace = 'edoc'::regnamespace;" | grep -c -E 'cancel_reason|doc_book_id'
      # Phải >= 2

      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -t -c "SELECT count(*) FROM (SELECT proname, count(*) FROM pg_proc WHERE pronamespace IN ('public'::regnamespace, 'edoc'::regnamespace) AND proname LIKE 'fn_%' GROUP BY proname HAVING count(*) > 1) t;"
      # Phải = 0
    </automated>
  </verify>
  <done>
    - Schema master file 000_schema_v3.0.sql apply lần 2 ZERO error (idempotent)
    - 7 SP HSCV mới + 1 SP opinion_forward + 1 table history + 7 cột mới đều có trong master
    - fn_handling_doc_get_by_id trả 36 cột + JOIN doc_books
    - fn_opinion_get_list trả 11 cột với LEFT JOIN forwarded staff
    - Seed 002 apply lần 2 zero error, có +1 HSCV cancelled + 3 history records
    - 0 SP overload duplicate
    - **PAUSE** chờ user xác nhận "commit 1" trước khi git commit
  </done>
</task>

<!-- ============================================================
     TASK 2 — COMMIT 2: Fix SP status flow bugs
     ============================================================ -->

<task type="auto">
  <name>Task 2: Fix 3 SP status flow bugs (approve/reject/cancel) (Commit 2)</name>
  <files>
    e_office_app_new/database/schema/000_schema_v3.0.sql
  </files>
  <action>
**CHỈ edit `e_office_app_new/database/schema/000_schema_v3.0.sql`** — KHÔNG sửa file khác trong commit này (atomic).

**B.1) Fix `fn_handling_doc_approve`** — vị trí dòng 3149-3177:

Logic cũ (sai): check status=2, set status=3
Logic mới (đúng theo 5-step frontend): check status=3 (Đã trình ký), set status=4 (Hoàn thành) + progress=100

```sql
CREATE OR REPLACE FUNCTION edoc.fn_handling_doc_approve(p_id bigint, p_approved_by integer)
RETURNS TABLE(success boolean, message text)
LANGUAGE plpgsql AS $$
DECLARE v_status SMALLINT;
BEGIN
  SELECT status INTO v_status FROM edoc.handling_docs WHERE id = p_id;
  IF NOT FOUND THEN
    RETURN QUERY SELECT FALSE, 'Hồ sơ công việc không tồn tại'::TEXT;
    RETURN;
  END IF;

  -- 5-step flow: chỉ duyệt khi đã trình ký (status=3) → set Hoàn thành (status=4)
  IF v_status <> 3 THEN
    RETURN QUERY SELECT FALSE, 'Chỉ được duyệt khi hồ sơ ở trạng thái Đã trình ký'::TEXT;
    RETURN;
  END IF;

  UPDATE edoc.handling_docs SET
    status     = 4,
    progress   = 100,
    complete_date    = NOW(),
    complete_user_id = p_approved_by,
    updated_by = p_approved_by,
    updated_at = NOW()
  WHERE id = p_id;

  RETURN QUERY SELECT TRUE, 'Duyệt hồ sơ công việc thành công'::TEXT;
END;
$$;
```

**B.2) Fix `fn_handling_doc_reject`** — vị trí dòng 3826-3860:

Logic cũ (sai): check status=2, set status=-1
Logic mới (đúng): check status=3 (Đã trình ký), set status=-1 (Từ chối)

```sql
CREATE OR REPLACE FUNCTION edoc.fn_handling_doc_reject(p_id bigint, p_rejected_by integer, p_reason text)
RETURNS TABLE(success boolean, message text)
LANGUAGE plpgsql AS $$
DECLARE v_status SMALLINT;
BEGIN
  IF p_reason IS NULL OR TRIM(p_reason) = '' THEN
    RETURN QUERY SELECT FALSE, 'Lý do từ chối không được để trống'::TEXT;
    RETURN;
  END IF;

  SELECT status INTO v_status FROM edoc.handling_docs WHERE id = p_id;
  IF NOT FOUND THEN
    RETURN QUERY SELECT FALSE, 'Hồ sơ công việc không tồn tại'::TEXT;
    RETURN;
  END IF;

  -- 5-step flow: chỉ từ chối khi đã trình ký (status=3)
  IF v_status <> 3 THEN
    RETURN QUERY SELECT FALSE, 'Chỉ được từ chối khi hồ sơ ở trạng thái Đã trình ký'::TEXT;
    RETURN;
  END IF;

  UPDATE edoc.handling_docs SET
    status     = -1,
    comments   = COALESCE(comments, '') || E'\n[Từ chối] ' || TRIM(p_reason),
    updated_by = p_rejected_by,
    updated_at = NOW()
  WHERE id = p_id;

  RETURN QUERY SELECT TRUE, 'Từ chối hồ sơ công việc thành công'::TEXT;
END;
$$;
```

**B.3) Fix `fn_handling_doc_cancel`** — đã được CREATE ở Commit 1 từ archive. Logic archive cho phép cancel ở status=3 (sai theo decision: hủy CHỈ hợp lệ ở -1, -2).

Tìm SP `fn_handling_doc_cancel` đã insert ở Commit 1, REPLACE bằng version có check IN (-1, -2):

```sql
CREATE OR REPLACE FUNCTION edoc.fn_handling_doc_cancel(
    p_id BIGINT,
    p_user_id INT,
    p_reason TEXT
)
RETURNS TABLE(success BOOLEAN, message TEXT)
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
    v_status SMALLINT;
BEGIN
    IF p_reason IS NULL OR LENGTH(TRIM(p_reason)) = 0 THEN
        RETURN QUERY SELECT FALSE, 'Vui lòng nhập lý do hủy'::TEXT;
        RETURN;
    END IF;

    SELECT h.status INTO v_status FROM edoc.handling_docs h WHERE h.id = p_id;
    IF NOT FOUND THEN
        RETURN QUERY SELECT FALSE, 'Không tìm thấy hồ sơ công việc'::TEXT;
        RETURN;
    END IF;
    IF v_status = -3 THEN
        RETURN QUERY SELECT FALSE, 'HSCV đã hủy trước đó'::TEXT;
        RETURN;
    END IF;

    -- Bug B fix: chỉ hủy được khi đã từ chối (-1) hoặc trả về (-2)
    IF v_status NOT IN (-1, -2) THEN
        RETURN QUERY SELECT FALSE,
            'Chỉ được hủy HSCV ở trạng thái Từ chối (-1) hoặc Trả về (-2). Trạng thái hiện tại: ' || v_status::TEXT;
        RETURN;
    END IF;

    UPDATE edoc.handling_docs
    SET status = -3,
        cancel_reason = p_reason,
        cancelled_at = NOW(),
        cancelled_by = p_user_id,
        updated_at = NOW()
    WHERE id = p_id;

    RETURN QUERY SELECT TRUE, 'Đã hủy hồ sơ công việc'::TEXT;
END;
$$;
```

**Verify:**

```bash
# 1. Re-apply schema lần 2 — zero error
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
  -f - < e_office_app_new/database/schema/000_schema_v3.0.sql 2>&1 | tail -10

# 2. Smoke test bằng SQL trực tiếp:
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
-- Tạo HSCV test, đặt status=3
INSERT INTO edoc.handling_docs (id, unit_id, name, status, progress, created_by)
VALUES (9999, 1, 'Test approve flow', 3, 80, 1)
ON CONFLICT (id) DO UPDATE SET status=3, progress=80;

-- Approve → expect status=4 progress=100
SELECT * FROM edoc.fn_handling_doc_approve(9999, 1);
SELECT id, status, progress, complete_date IS NOT NULL AS has_complete
  FROM edoc.handling_docs WHERE id = 9999;
-- expect: status=4, progress=100, has_complete=t

-- Reset, test reject
UPDATE edoc.handling_docs SET status=3, progress=80, complete_date=NULL WHERE id=9999;
SELECT * FROM edoc.fn_handling_doc_reject(9999, 1, 'Test reject');
SELECT status FROM edoc.handling_docs WHERE id = 9999;
-- expect: status=-1

-- Test cancel — phải reject vì status=-1 (allow)
SELECT * FROM edoc.fn_handling_doc_cancel(9999, 1, 'Test cancel');
SELECT status, cancel_reason FROM edoc.handling_docs WHERE id = 9999;
-- expect: status=-3, cancel_reason='Test cancel'

-- Cleanup
DELETE FROM edoc.handling_docs WHERE id = 9999;
"

# 3. Test cancel REJECT khi status=3
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "
INSERT INTO edoc.handling_docs (id, unit_id, name, status, created_by)
VALUES (9998, 1, 'Test cancel reject', 3, 1)
ON CONFLICT (id) DO UPDATE SET status=3;
SELECT * FROM edoc.fn_handling_doc_cancel(9998, 1, 'Test');
-- expect: success=f, message chứa 'Chỉ được hủy ... -1 hoặc Trả về'
DELETE FROM edoc.handling_docs WHERE id = 9998;
"
```

**Commit (CHỈ khi user xác nhận):**

PAUSE báo:
```
[COMMIT 2 READY] Đã fix 3 SP status flow.
Verify:
  - approve(status=3) → status=4 + progress=100 + complete_date set
  - reject(status=3) → status=-1
  - cancel(status=-1) → status=-3 OK
  - cancel(status=3) → reject với message rõ ràng
User confirm "commit 2" để proceed.
```

Commit message:
```
fix(hscv): SP approve/reject check status=3 (5-step flow), cancel chi cho phep -1/-2

- fn_handling_doc_approve: check status=3 (Da trinh ky), set status=4 + progress=100
  + complete_date/complete_user_id (truoc: check status=2 -> 3)
- fn_handling_doc_reject: check status=3, set status=-1 (truoc: check status=2)
- fn_handling_doc_cancel: chi hop le khi status IN (-1, -2) (Bug B fix)
- Khop voi 5-step status flow tren frontend (toolbar Duyet/Tu choi/Huy)
- Idempotent verify: re-apply schema lan 2 zero error
```
  </action>
  <verify>
    <automated>
      docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 -f - < e_office_app_new/database/schema/000_schema_v3.0.sql 2>&1 | grep -iE 'error|fail' | head -5
      # Output phải RỖNG

      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -At -c "INSERT INTO edoc.handling_docs (id, unit_id, name, status, progress, created_by) VALUES (9999, 1, 'Test', 3, 80, 1) ON CONFLICT (id) DO UPDATE SET status=3, progress=80; SELECT (success::text || '|' || message) FROM edoc.fn_handling_doc_approve(9999, 1);"
      # Phải có "t|Duyệt hồ sơ công việc thành công"

      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -At -c "SELECT status::text || '|' || progress::text FROM edoc.handling_docs WHERE id = 9999;"
      # Phải = "4|100"

      docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -c "DELETE FROM edoc.handling_docs WHERE id IN (9999, 9998);"
    </automated>
  </verify>
  <done>
    - fn_handling_doc_approve: input status=3 → output status=4 + progress=100 + complete_date set
    - fn_handling_doc_reject: input status=3 → output status=-1
    - fn_handling_doc_cancel: input status IN (-1,-2) → status=-3 + cancel_reason populate; status khác (0,1,2,3,4) → reject với message
    - Re-apply schema lần 2: zero error
    - **PAUSE** chờ user "commit 2"
  </done>
</task>

<!-- ============================================================
     TASK 3 — COMMIT 3: Smoke test script + hook deploy
     ============================================================ -->

<task type="auto">
  <name>Task 3: Tạo smoke-test-hscv.ps1 (16 testcase E2E) + hook vào deploy-v2-kh-test.ps1 (Commit 3)</name>
  <files>
    deploy/smoke-test-hscv.ps1
    deploy/deploy-v2-kh-test.ps1
  </files>
  <action>
**A. Tạo `deploy/smoke-test-hscv.ps1`** — 16 testcase E2E API HSCV.

**Yêu cầu chung của script:**
- PowerShell 5.1 compatible (KHÔNG dùng `??`, `?.`, ConvertFrom-Json -AsHashtable v7)
- Tiếng Việt KHÔNG DẤU trong script (PS 5.1 UTF-8 BOM issue per CLAUDE.md pitfall #1)
- Param defaults: `$ApiUrl='http://localhost:4000'`, `$User='admin'`, `$Pass='Admin@123'`
- Output PASS/FAIL summary table
- Exit code 0 nếu 16/16 PASS, exit 1 nếu fail bất kỳ
- Dùng `Invoke-RestMethod` với `Headers @{ Authorization = "Bearer $token" }` cho các call sau login
- Wrap mỗi testcase trong `try { ... } catch { ... }` để 1 fail không kill script
- Cleanup HSCV test ở cuối (DELETE qua API hoặc qua psql) — dùng prefix tên `[SMOKE_HSCV_$timestamp]` để dễ filter
- Log mỗi step: `Write-Host "  TC{N}: {pass/fail} - {testcase}"` với màu green/red

**16 testcase chi tiết:**

| TC | Mô tả | Method+Path | Body / Param | Expect |
|----|-------|-------------|--------------|--------|
| TC1 | Login admin | POST /api/auth/login | {username, password} | 200 + accessToken |
| TC2 | List HSCV | GET /api/hscv | - | 200 + data[] + pagination |
| TC3 | Count by status | GET /api/hscv/count-by-status | - | 200 + data[] |
| TC4 | Tạo HSCV mới | POST /api/hscv | {name, comments, start_date, end_date, doc_field_id, doc_type_id, curator_id} | 201 + data.id, lưu `$hscvId` |
| TC5 | Đổi status 0→1 | PATCH /api/hscv/{id}/trang-thai | {status: 1} | 200 |
| TC6 | Lấy số | POST /api/hscv/{id}/lay-so | {doc_book_id: 1} | 200 + number > 0 |
| TC7 | Submit 1→2 | PATCH /api/hscv/{id}/trang-thai | {status: 2} | 200 |
| TC8 | Đã ký 2→3 | PATCH /api/hscv/{id}/trang-thai | {status: 3} | 200 |
| TC9 | Approve 3→4 | qua endpoint nào? — check route, có thể dùng PATCH /trang-thai status=4 hoặc endpoint riêng (đọc handling-doc.ts để xác định). Nếu chưa có → call SP qua PATCH trang-thai | - | 200 + status=4 + progress=100 |
| TC10 | Detail | GET /api/hscv/{id} | - | 200 + data.status=4 + data.number > 0 + data.doc_book_name |
| TC11 | Mở lại 4→1 | POST /api/hscv/{id}/mo-lai | - | 200 + sau đó GET detail data.status=1 + data.progress=100 (per A2) |
| TC12 | Tạo HSCV 2 + Chuyển tiếp | POST /api/hscv (tạo HSCV mới `$hscvId2` curator=user khác cùng đơn vị) → POST /api/hscv/{id2}/chuyen-tiep | {to_staff_id: <other staff cùng unit_id>, note: 'Test transfer'} | 200 |
| TC13 | Lịch sử | GET /api/hscv/{id2}/lich-su | - | 200 + data có ít nhất 1 record action_type='transfer' |
| TC14 | Thêm ý kiến | POST /api/hscv/{id2}/y-kien | {content: 'Y kien test'} | 201 + data.id, lưu `$opinionId` |
| TC15 | Chuyển tiếp ý kiến | POST /api/hscv/{id2}/y-kien/{opinionId}/chuyen-tiep | {to_staff_id, note: 'Forward test'} | 200 |
| TC16 | Reject + Cancel flow | Tạo HSCV 3 (`$hscvId3`), đẩy status=3, POST endpoint reject (qua PATCH trang-thai status=-1), → POST /api/hscv/{id3}/huy {reason: 'Test cancel'} → 200; THỬ POST /huy lần 2 trên HSCV status=3 mới (TC16b) → expect 400 + message "Chỉ được hủy" | - | TC16a 200, TC16b 400 |

**Lưu ý implementation:**
- Để tìm staff cùng đơn vị: trước TC12 gọi `GET /api/hscv/nhan-vien-cung-don-vi`, lấy 1 staff ≠ admin
- TC9: Đọc routes — endpoint `PATCH /:id/trang-thai` có gọi đúng `fn_handling_doc_approve` không? Nếu chưa, thử `body = {status: 4}` hoặc tìm endpoint `/duyet`. Nếu cần endpoint mới → SKIP TC9 và để comment "TODO: Backend chưa expose approve endpoint riêng"
- Cleanup cuối script: `DELETE /api/hscv/{id}` cho 3 HSCV test đã tạo (best-effort, ignore lỗi)

**Skeleton script structure:**

```powershell
# ===== smoke-test-hscv.ps1 =====
param(
    [string]$ApiUrl = 'http://localhost:4000',
    [string]$User = 'admin',
    [string]$Pass = 'Admin@123'
)

$ErrorActionPreference = 'Continue'
$results = @()  # array of [PSCustomObject]@{ TC; Name; Status; Detail }
$createdIds = @()

function Test-TC ($id, $name, $scriptBlock) {
    Write-Host "[$id] $name" -NoNewline
    try {
        $detail = & $scriptBlock
        $results += [PSCustomObject]@{ TC=$id; Name=$name; Status='PASS'; Detail=$detail }
        Write-Host " ... PASS" -ForegroundColor Green
    } catch {
        $results += [PSCustomObject]@{ TC=$id; Name=$name; Status='FAIL'; Detail=$_.Exception.Message }
        Write-Host " ... FAIL: $($_.Exception.Message)" -ForegroundColor Red
    }
}

# TC1
$loginBody = @{ username=$User; password=$Pass } | ConvertTo-Json
$loginRes = Invoke-RestMethod -Uri "$ApiUrl/api/auth/login" -Method POST -Body $loginBody -ContentType 'application/json'
$token = $loginRes.data.accessToken
$headers = @{ Authorization = "Bearer $token" }
# ... 15 TCs more

# Summary
$pass = ($results | Where-Object { $_.Status -eq 'PASS' }).Count
$fail = ($results | Where-Object { $_.Status -eq 'FAIL' }).Count
Write-Host ""
Write-Host "===== SMOKE TEST HSCV: $pass/$($results.Count) PASS =====" -ForegroundColor $(if ($fail -eq 0) { 'Green' } else { 'Red' })
$results | Format-Table -AutoSize

# Cleanup
foreach ($id in $createdIds) {
    try { Invoke-RestMethod -Uri "$ApiUrl/api/hscv/$id" -Method DELETE -Headers $headers | Out-Null } catch {}
}

if ($fail -gt 0) { exit 1 }
exit 0
```

**B. Edit `deploy/deploy-v2-kh-test.ps1`** — hook smoke test sau verify backend.

Vị trí: sau block `# -- 10. Verify` (dòng 300-324), TRƯỚC `# -- Summary` (dòng 326).

Thêm step 11:

```powershell
# -- 11. Smoke test HSCV ------------------------------------
Step '11. Smoke test HSCV (16 testcase E2E)'
$smokeScript = Join-Path $DEPLOY_DIR 'smoke-test-hscv.ps1'
if (-not (Test-Path $smokeScript)) {
    Warn 'Khong tim thay smoke-test-hscv.ps1 - skip'
} else {
    & powershell -ExecutionPolicy Bypass -File $smokeScript -ApiUrl 'http://localhost:4000' -User 'admin' -Pass 'Admin@123'
    if ($LASTEXITCODE -ne 0) {
        Err 'Smoke test HSCV that bai - deploy block. Kiem tra log phia tren.'
    }
    Log 'Smoke test HSCV PASS'
}
```

**C. Verify (manual run trên dev local trước khi commit):**

```bash
# 1. Backend đang chạy port 4000 (verify trước)
curl http://localhost:4000/api/health

# 2. Run smoke test stand-alone
powershell -ExecutionPolicy Bypass -File deploy/smoke-test-hscv.ps1
# Expect: 16/16 PASS, exit code 0

# 3. (Sanity) verify deploy script syntax
powershell -Command "& { $errors = $null; [void][System.Management.Automation.Language.Parser]::ParseFile('deploy/deploy-v2-kh-test.ps1', [ref]$null, [ref]\$errors); if (\$errors) { \$errors | Format-List; exit 1 } else { Write-Host 'Syntax OK' } }"
```

**Lưu ý nếu TC9 không tìm được endpoint approve:** Đọc kỹ `e_office_app_new/backend/src/routes/handling-doc.ts` ở dòng 484-543 (`router.patch('/:id/trang-thai', ...)`) — xem có map status=4 vào fn_handling_doc_approve không. Nếu chỉ map vào `fn_handling_doc_change_status` (generic) → SP approve không được gọi → cần PATCH 3→4 trực tiếp. Trong trường hợp này TC9 vẫn dùng PATCH /trang-thai {status: 4} và verify status=4 (KHÔNG verify progress=100 vì change_status không set progress). Comment rõ trong script.

**D. Commit (CHỈ khi user xác nhận):**

PAUSE báo:
```
[COMMIT 3 READY] Đã tạo smoke-test-hscv.ps1 16 testcase + hook vào deploy-v2-kh-test.ps1.
Verify:
  - Run smoke test local: 16/16 PASS, exit 0
  - PowerShell parse deploy-v2-kh-test.ps1 OK
User confirm "commit 3" để proceed.
```

Commit message:
```
test(hscv): add smoke-test-hscv.ps1 16 testcase + hook vao deploy-v2-kh-test

- deploy/smoke-test-hscv.ps1: E2E API smoke test cho HSCV
  TC1 login, TC2-3 list/count, TC4 create, TC5-9 status flow 0->1->2->3->4
  TC10 detail, TC11 reopen, TC12 transfer, TC13 history,
  TC14-15 opinion + forward, TC16 cancel + reject
- deploy/deploy-v2-kh-test.ps1: step 11 goi smoke test sau verify,
  fail -> block deploy
- PowerShell 5.1 compat: tieng Viet khong dau, no v7 syntax
- Pass/fail summary + exit code 0/1
```
  </action>
  <verify>
    <automated>
      powershell -ExecutionPolicy Bypass -File deploy/smoke-test-hscv.ps1 -ApiUrl http://localhost:4000 -User admin -Pass 'Admin@123'
      # Expect exit code 0 (16/16 PASS)

      powershell -Command "$errors = $null; [void][System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path 'deploy/deploy-v2-kh-test.ps1'), [ref]$null, [ref]$errors); if ($errors) { $errors; exit 1 } else { Write-Host 'OK' }"
      # Expect "OK" + exit 0

      Test-Path deploy/smoke-test-hscv.ps1
      # Expect True
    </automated>
  </verify>
  <done>
    - deploy/smoke-test-hscv.ps1 tồn tại, 16 testcase, exit 0 khi local backend đang chạy
    - deploy/deploy-v2-kh-test.ps1 có step 11 gọi smoke test, fail block deploy
    - PowerShell 5.1 compat (no v7 syntax, tiếng Việt KHÔNG DẤU)
    - **PAUSE** chờ user "commit 3"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| psql input → DB | SQL injection nếu unescape — tất cả SP dùng plpgsql với param binding, không string concat |
| API client → backend | Auth bypass — middleware authenticate đã enforce, smoke test phải dùng token hợp lệ |
| Smoke test script → DB | Cleanup leak — nếu test fail giữa chừng, HSCV test rác có thể còn lại |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g4z-01 | Tampering | fn_handling_doc_cancel | mitigate | Validate status IN (-1,-2) + non-empty reason TRƯỚC update; refuse status khác với message rõ |
| T-g4z-02 | Information Disclosure | fn_handling_doc_get_by_id | accept | 36 cột trả về đều public to authenticated user; backend route đã có authenticate middleware |
| T-g4z-03 | Denial of Service | smoke-test-hscv.ps1 | mitigate | Tạo tối đa 3 HSCV test với prefix `[SMOKE_HSCV_*]`, cleanup ở cuối; chỉ chạy với admin trên dev/staging |
| T-g4z-04 | Repudiation | fn_handling_doc_transfer/cancel | mitigate | Insert handling_doc_history record với from_staff_id + to_staff_id + created_by + timestamp |
| T-g4z-05 | Elevation of Privilege | SP với SECURITY DEFINER | accept | Các SP cancel/transfer/reopen dùng SECURITY DEFINER nhưng không expose escalation path — chỉ update bảng handling_docs với input validated |
</threat_model>

<verification>
## Phase-level checks (sau khi commit 3 xong)

```bash
# 1. Re-apply schema lần 3 — vẫn zero error
docker exec -i qlvb_postgres psql -U qlvb_admin -d qlvb_dev -v ON_ERROR_STOP=1 \
  -f - < e_office_app_new/database/schema/000_schema_v3.0.sql 2>&1 | grep -iE 'error' | head

# 2. SP count baseline check
docker exec qlvb_postgres psql -U qlvb_admin -d qlvb_dev -t -c "
SELECT count(*) FROM pg_proc WHERE pronamespace IN ('public'::regnamespace, 'edoc'::regnamespace) AND proname LIKE 'fn_%';"
# >= 386 + 7 = 393 (nếu baseline v2.0.2 = 386)

# 3. End-to-end smoke
powershell -File deploy/smoke-test-hscv.ps1
# 16/16 PASS
```

## Visual check (nếu dev frontend đang chạy port 3000)

1. Login `admin/Admin@123`
2. Vào "Hồ sơ công việc" → tạo HSCV mới
3. Đẩy status 0→1→2→3 qua toolbar
4. Click "Duyệt" → expect status=4 (Hoàn thành) + progress=100
5. Click "Mở lại" → expect status=1 + progress vẫn 100
6. Tạo HSCV khác, đẩy status=3, click "Từ chối" với lý do → expect status=-1
7. Click "Hủy" với lý do → expect status=-3 + cancel_reason hiển thị ở chi tiết
8. Tab "Lịch sử" hiển thị các transfer records
9. Tab "Ý kiến" → Thêm ý kiến → click "Chuyển tiếp" với staff khác → ý kiến mới có parent
</verification>

<success_criteria>
- [ ] **Commit 1** merged: master schema có 7 SP HSCV mới + 1 opinion_forward + 1 table history + 7 cột mới + fn_get_by_id 36 cột; seed 002 +1 cancelled HSCV +3 history
- [ ] **Commit 2** merged: 3 SP fix status flow (approve check=3 set 4+progress=100; reject check=3 set -1; cancel chỉ -1/-2)
- [ ] **Commit 3** merged: smoke-test-hscv.ps1 16 testcase + hook deploy
- [ ] Re-apply schema master lần 2 + 3: ZERO error (idempotent verified)
- [ ] 0 SP overload duplicate
- [ ] SP count >= 393 (baseline + 7 mới)
- [ ] Smoke test local: 16/16 PASS
- [ ] PowerShell parse deploy-v2-kh-test.ps1: OK
- [ ] User pause-and-review sau MỖI commit (3 lần) — KHÔNG tự động chain commit
</success_criteria>

<output>
After completion (3 commits), create concise summary:
- Commits SHAs
- Schema changes summary (7 SP + 1 table + 7 cột + 36 cột get_by_id)
- Smoke test result snapshot (16/16)
- Next step: chạy `deploy/pre-push-check.ps1` rồi push lên GitHub + deploy KH test
</output>
