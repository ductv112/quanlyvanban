---
phase: 260418-gs7
plan: 01
subsystem: frontend-ui-scope
tags: [phase1, demo-cleanup, menu-filter, feature-flag]
provides:
  - HIDDEN_ROUTES Set (MainLayout)
  - filterMenuItems recursive function (MainLayout)
  - PHASE1_HIDE_CUSTOM_FIELDS flag x3 (van-ban-den/di/du-thao)
  - Select mode="tags" cho publish_unit (van-ban-den)
requires:
  - /quan-tri/don-vi/tree (đã có sẵn — không thay đổi)
affects:
  - Sidebar render scope (4 menu group bị ảnh hưởng — NGHIỆP VỤ giữ, QUẢN LÝ ẩn, TÍCH HỢP ẩn, HỆ THỐNG cắt bớt)
  - Form Văn bản đến/đi/dự thảo (ẩn section "Trường bổ sung")
  - Form Văn bản đến field "Cơ quan ban hành" (Input → Select)
key-files:
  modified:
    - e_office_app_new/frontend/src/components/layout/MainLayout.tsx (+97 -1)
    - e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx (+50 -7)
    - e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx (+6 -1)
    - e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx (+6 -1)
  created: []
decisions:
  - "Task 3a: Chọn Option C (combobox-style) — Select mode='tags' maxCount={1} value=label, normalize array→string trong handleSave. KHÔNG đụng schema DB."
metrics:
  duration: ~25 min
  completed: 2026-04-18
  tasks_total: 3
  tasks_completed: 3
  commits: [a256057, 6cc4b59, 8afd6d7]
---

# Phase 260418-gs7 Plan 01: Ẩn các module/menu chưa có trong HDSD cũ — Summary

Phase 1 demo cleanup: ẩn 20 route khỏi sidebar bằng `HIDDEN_ROUTES` Set + recursive filter, ẩn section "Trường bổ sung" trên 3 form VB bằng flag, đổi field "Cơ quan ban hành" trên VB đến từ Input free-text sang Select bind cây đơn vị (combobox mode="tags").

## Tasks Completed

### Task 1 — Ẩn 20 menu route khỏi sidebar
**Commit:** `a256057` — feat: ẩn các module/menu chưa có trong HDSD cũ — Phase 1 demo

**Files modified:**
- `e_office_app_new/frontend/src/components/layout/MainLayout.tsx` (+97 -1)

**Changes:**
- Thêm `HIDDEN_ROUTES: ReadonlySet<string>` với 25 entry (20 route lá + 5 parent group key)
- Thêm `filterMenuItems(items)` recursive function:
  - Bước 1: Drop item có key trong Set; recurse vào children; drop parent nếu children rỗng sau filter
  - Bước 2: Drop group header (type='group') nếu không còn item thực nào theo sau
- Sửa `buildMenuItems` return từ `items` → `filterMenuItems(items)`

**Routes ẩn (20 + 5 parent):**

| Group | Routes ẩn |
|-------|-----------|
| Tin nhắn | `/tin-nhan` |
| Lịch | `lich` (parent) + `/lich/ca-nhan`, `/lich/co-quan`, `/lich/lanh-dao` |
| Danh bạ | `/danh-ba` |
| Kho lưu trữ | `kho-luu-tru` (parent) + `/kho-luu-tru`, `/kho-luu-tru/muon-tra` |
| Tài liệu | `/tai-lieu` |
| Hợp đồng | `/hop-dong` |
| Cuộc họp | `cuoc-hop` (parent) + `/cuoc-hop`, `/cuoc-hop/thong-ke` |
| Tích hợp | `/lgsp`, `/lgsp/co-quan`, `/thong-bao-kenh` |
| Quản trị | `/quan-tri/chuc-nang`, `/quan-tri/cau-hinh-truong`, `/quan-tri/co-quan`, `/quan-tri/nhom-lam-viec`, `/quan-tri/uy-quyen`, `/quan-tri/dia-ban`, `/quan-tri/lich-lam-viec`, `/quan-tri/mau-thong-bao`, `/quan-tri/cau-hinh` |

**Tác dụng phụ tự nhiên (ẩn group header trống):**
- Group **QUẢN LÝ** (Kho lưu trữ, Tài liệu, Hợp đồng, Cuộc họp) → ẨN cả header
- Group **TÍCH HỢP** (LGSP, Cơ quan liên thông, Kênh thông báo) → ẨN cả header

**Sidebar còn lại (Admin):**
- Tổng quan
- NGHIỆP VỤ → Văn bản (6 submenu), Hồ sơ công việc, Thông báo, Đối tác (4 link ngoài)
- HỆ THỐNG → Quản trị (4 item: Đơn vị / Chức vụ / Người dùng / Nhóm quyền), Danh mục (4 item: Sổ VB / Loại VB / Lĩnh vực / Người ký)

---

### Task 2 — Ẩn section "Trường bổ sung" trên 3 form VB
**Commit:** `6cc4b59` — feat: ẩn section 'Trường bổ sung' trên 3 form VB — Phase 1

**Files modified:**
- `e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx` (+5 -1)
- `e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx` (+6 -1)
- `e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx` (+6 -1)

**Changes:**
- Mỗi file thêm `const PHASE1_HIDE_CUSTOM_FIELDS = true;` ở module level
- Wrap render `extraColumns.length > 0` thành `!PHASE1_HIDE_CUSTOM_FIELDS && extraColumns.length > 0`
- Giữ nguyên `state extraColumns` + `fetchExtraColumns` callback + `useEffect` gọi fetch — chỉ JSX bị ẩn

**Lý do thiết kế:** API fetch vẫn chạy (no-op từ UI perspective) để Phase 2 chỉ cần flip 1 flag mà không phải re-wire data flow.

---

### Task 3a — Quyết định checkpoint
**User decision:** **Option C — Select mode="tags"** (kết hợp Option A "pure Select" với cho phép gõ tự do)

**Tóm tắt:** Giữ nguyên schema DB (TEXT), giữ nguyên label "Cơ quan ban hành", chỉ đổi component Input → `Select mode="tags" maxCount={1}` để user chọn từ cây đơn vị HOẶC gõ tên đơn vị ngoài cây.

### Task 3b — Đổi "Cơ quan ban hành" VB đến sang Select bind /quan-tri/don-vi/tree
**Commit:** `8afd6d7` — feat: đổi 'Cơ quan ban hành' VB đến sang Select bind đơn vị

**File modified:**
- `e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx` (+44 -6)

**Changes:**
1. Thêm `interface DepartmentNode { id, name, children? }`
2. Thêm helper `flattenDepartments(nodes, prefix)` (copy pattern từ van-ban-di / van-ban-du-thao)
3. Thêm state `departments: SelectOption[]`
4. **Gộp `/quan-tri/don-vi/tree` vào `Promise.all` của `fetchDropdowns`** (loại bỏ duplicate fetch — trước đây admin gọi riêng API này)
5. Đổi Form.Item `publish_unit` từ `<Input maxLength={500} />` thành:
   ```tsx
   <Select
     mode="tags"
     maxCount={1}
     placeholder="Chọn hoặc nhập cơ quan ban hành"
     allowClear
     showSearch
     optionFilterProp="label"
     options={departments.map((d) => ({ value: d.label, label: d.label }))}
   />
   ```
6. **Wrap khi edit:** `publish_unit: record.publish_unit ? [record.publish_unit] : []` (string → array cho mode="tags")
7. **Normalize khi submit:** `Array.isArray(publishUnitRaw) ? publishUnitRaw[0] : publishUnitRaw` → backend SP `edoc.fn_incoming_doc_create` nhận TEXT đúng schema cũ

**KHÔNG đụng:**
- Backend repo / route / SP
- Migration DB
- Column hiển thị `publish_unit` trên table

---

## Verification Results

### Type-check (`npx tsc --noEmit`)
- **Baseline (trước Task 1):** 17 lỗi pre-existing (calendar / ho-so-cong-viec / van-ban-* JSX duplicate-attribute / TreeNode interface mismatch)
- **Sau Task 1:** 17 lỗi (no change)
- **Sau Task 2:** 17 lỗi (no change)
- **Sau Task 3:** 17 lỗi (no change — line số shift do thêm code, lỗi cùng nội dung)
- **Kết luận:** 0 lỗi mới được introduce. Tất cả 17 lỗi đều out-of-scope (pre-existing, không liên quan đến scope plan).

### Grep verification
- `grep -c HIDDEN_ROUTES` MainLayout.tsx ≥ 2 ✓ (declare + usage trong filterMenuItems)
- `grep -c PHASE1_HIDE_CUSTOM_FIELDS` mỗi file VB = 2 ✓ (declare + 1 usage)
- `grep -c '/quan-tri/don-vi/tree'` van-ban-den/page.tsx = 1 ✓ (thêm mới trong Promise.all)
- `grep -c extraColumns` mỗi file VB ≥ 3 ✓ (state + fetch + render — không xóa code)

---

## Deviations from Plan

**None** — plan executed gần như exact:
- Task 1: triển khai 100% theo spec
- Task 2: triển khai 100% theo spec
- Task 3: User chọn **Option C (mode="tags" kết hợp Option A + combobox)** ở checkpoint 3a → executor implement đúng spec Task 3b với cách "tags maxCount=1" (combobox-style được nêu trong plan).

**Lưu ý nhỏ:** Plan note "type-check 0 lỗi" nhưng codebase đã có 17 lỗi pre-existing trước Plan này — out-of-scope, không fix theo Rule scope boundary. Sẽ flag vào Phase 6 backlog.

---

## Self-Check: PASSED

**Files exist:**
- `e_office_app_new/frontend/src/components/layout/MainLayout.tsx` ✓ FOUND
- `e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx` ✓ FOUND
- `e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx` ✓ FOUND
- `e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx` ✓ FOUND

**Commits exist (verified `git log --oneline -3`):**
- `a256057` ✓ FOUND — Task 1 (HIDDEN_ROUTES)
- `6cc4b59` ✓ FOUND — Task 2 (PHASE1_HIDE_CUSTOM_FIELDS)
- `8afd6d7` ✓ FOUND — Task 3 (publish_unit Select tags)

---

## Hướng dẫn bật lại ở Phase 2

### Bật lại menu (theo từng module)
Mở `e_office_app_new/frontend/src/components/layout/MainLayout.tsx` → xóa entry tương ứng khỏi `HIDDEN_ROUTES` Set:

```ts
// VD: bật lại "Tin nhắn" → xóa dòng:
'/tin-nhan',

// VD: bật lại "Lịch" → xóa cả 4 dòng:
'lich',
'/lich/ca-nhan', '/lich/co-quan', '/lich/lanh-dao',
```

Group header (QUẢN LÝ / TÍCH HỢP) sẽ tự hiện lại khi children không còn rỗng — không cần action thêm.

### Bật lại "Trường bổ sung" trên form VB
Mở 3 file:
- `e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx`
- `e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx`
- `e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx`

Đổi flag:
```ts
const PHASE1_HIDE_CUSTOM_FIELDS = false;  // ← từ true → false
```

### Bật lại Input free-text cho "Cơ quan ban hành"
Nếu Phase 2 muốn full migration sang FK `publish_unit_id` (như `van-ban-di`/`van-ban-du-thao`):

1. **Migration DB:**
   - Thêm column `publish_unit_id BIGINT REFERENCES public.departments(id)` vào `edoc.incoming_docs`
   - Backfill từ `publish_unit` TEXT (best-effort match name → id)
   - Sửa SP `edoc.fn_incoming_doc_create`, `fn_incoming_doc_update`, `fn_incoming_doc_get_by_id`, `fn_incoming_doc_list` thêm param/return cột mới

2. **Backend:**
   - Sửa `incoming-doc.repository.ts` Row interface + body payload
   - Sửa `incoming-doc.ts` route validation

3. **Frontend (van-ban-den/page.tsx):**
   - Đổi `name="publish_unit"` → `name="publish_unit_id"`, value type number
   - Bỏ `mode="tags"`, dùng Select normal + options giá trị `value: d.value` (số)
   - Bỏ normalize array→string trong `handleSave`
   - Bỏ wrap array trong `openDrawer`
   - Cột table dataIndex chuyển sang `publish_unit_name`

(Xem `van-ban-di/page.tsx` line 121-131 + 479 để đối chiếu pattern chuẩn.)

---

## Files Reference

| File | Lines added | Lines removed |
|------|-------------|---------------|
| `e_office_app_new/frontend/src/components/layout/MainLayout.tsx` | +97 | -1 |
| `e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx` | +50 | -7 |
| `e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx` | +6 | -1 |
| `e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx` | +6 | -1 |
| **Total** | **+159** | **-10** |

## Commits

| # | Hash | Message |
|---|------|---------|
| 1 | `a256057` | feat: ẩn các module/menu chưa có trong HDSD cũ — Phase 1 demo |
| 2 | `6cc4b59` | feat: ẩn section 'Trường bổ sung' trên 3 form VB — Phase 1 |
| 3 | `8afd6d7` | feat: đổi 'Cơ quan ban hành' VB đến sang Select bind đơn vị |
