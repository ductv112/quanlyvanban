---
phase: 260418-gs7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e_office_app_new/frontend/src/components/layout/MainLayout.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx
  - e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx
autonomous: false
requirements:
  - PHASE1-HIDE-MENU
  - PHASE1-HIDE-CUSTOM-FIELDS
  - PHASE1-UNIT-SELECT

must_haves:
  truths:
    - "Sidebar chỉ hiển thị các menu có trong HDSD cũ (20 route bị ẩn khỏi UI)"
    - "Các parent group có children rỗng sau khi filter phải bị ẩn luôn (Danh mục giữ lại — chỉ còn 4 item; nhóm QUẢN LÝ/TÍCH HỢP ẩn header nếu trống)"
    - "Form VB đến / VB đi / VB dự thảo KHÔNG còn render section 'Trường bổ sung' (dynamic fields)"
    - "Field 'Cơ quan ban hành' trên form VB đến trở thành Select bind tới cây đơn vị (/quan-tri/don-vi/tree)"
    - "Label tiếng Việt giữ nguyên: 'Cơ quan ban hành' trên VB đến; 'Đơn vị ban hành' / 'Đơn vị phát hành' trên VB đi & VB dự thảo không đổi"
    - "Type-check (tsc --noEmit) pass cho frontend sau mỗi task"
    - "KHÔNG xóa code — ẩn bằng Set / flag constant / comment"
  artifacts:
    - path: "e_office_app_new/frontend/src/components/layout/MainLayout.tsx"
      provides: "HIDDEN_ROUTES Set + filterMenuItems recursive function"
      contains: "HIDDEN_ROUTES"
    - path: "e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx"
      provides: "PHASE1_HIDE_CUSTOM_FIELDS flag + Select binding cho publish_unit"
      contains: "PHASE1_HIDE_CUSTOM_FIELDS"
    - path: "e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx"
      provides: "PHASE1_HIDE_CUSTOM_FIELDS flag"
      contains: "PHASE1_HIDE_CUSTOM_FIELDS"
    - path: "e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx"
      provides: "PHASE1_HIDE_CUSTOM_FIELDS flag"
      contains: "PHASE1_HIDE_CUSTOM_FIELDS"
  key_links:
    - from: "MainLayout.tsx buildMenuItems()"
      to: "items return"
      via: "filterMenuItems(items, HIDDEN_ROUTES)"
      pattern: "filterMenuItems"
    - from: "van-ban-den form (publish_unit field)"
      to: "/quan-tri/don-vi/tree API"
      via: "fetchDropdowns → flattenDepartments → Select options"
      pattern: "publish_unit.*Select"
---

<objective>
Phase 1 — Ẩn các module/menu/field chưa có trong HDSD cũ để bản demo chỉ phô diễn
đúng scope nghiệp vụ mà công chức đã quen dùng (theo docs/hdsd_cu.docx).

**Nguyên tắc:** KHÔNG xóa code. Dùng Set / flag constant. Phase 2 bật lại chỉ cần
xóa route khỏi Set hoặc flip flag `false`.

Purpose: Demo 2026-04-18/19 phải sạch — không có menu trống, không có field không
hiểu, không có UI nửa vời. Giữ lại code hoàn chỉnh cho sprint sau.

Output: 4 file frontend đã sửa (MainLayout + 3 trang van-ban-*), tất cả ẩn bằng
flag, type-check pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./e_office_app_new/frontend/AGENTS.md
@.planning/STATE.md

# Files sẽ sửa (đọc trước khi code)
@./e_office_app_new/frontend/src/components/layout/MainLayout.tsx
@./e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx
@./e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx
@./e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx

# Lib đã có sẵn (dùng lại, không cần sửa)
@./e_office_app_new/frontend/src/lib/tree-utils.ts

<interfaces>
<!-- Các hàm + type sẵn có trong codebase, dùng lại không tự viết -->

Từ MainLayout.tsx hiện tại:
```ts
type MenuItem = Required<MenuProps>['items'][number];
// MenuItem có thể là:
//   { key, icon?, label, children?, type? }  // normal/submenu
//   { key, type: 'group', label }             // group header
//   { type: 'divider' }                       // divider
// Lưu ý: item có 'children' thì KHÔNG có type='group'.
// Filter phải recurse vào children, nếu children rỗng sau filter → ẩn item cha.
// Group header (type='group') đứng ngay TRƯỚC các item thường trong items array
//   — phải dò sau khi filter: nếu group header ngay sau có 0 item thực → ẩn header.

function buildMenuItems({ badgeCounts, isAdmin, roles }: MenuBuildParams): MenuItem[]
// Hiện return items trực tiếp. Task 1 sẽ thêm 1 bước filter cuối.
```

Từ van-ban-den/page.tsx:
```ts
// Hiện tại (line 376):
<Form.Item name="publish_unit" label="Cơ quan ban hành">
  <Input placeholder="VD: UBND tỉnh Lạng Sơn" maxLength={500} />
</Form.Item>
// Field "publish_unit" là TEXT, không phải FK.
// Dropdowns load (line 96-114) CHƯA có /quan-tri/don-vi/tree trong Promise.all
//   (trừ khi user?.isAdmin). Task 3 phải thêm vào Promise.all cho mọi user.

// extraColumns (line 68, 403-430) — wrap bằng PHASE1_HIDE_CUSTOM_FIELDS
```

Từ van-ban-di/page.tsx:
```ts
// Line 121-131: đã load /quan-tri/don-vi/tree và setDepartments(flattenDepartments(...))
// Field publish_unit_id (line 479) Select options={departments} — KHÔNG đổi.
// extraColumns (line 92, 550-577) — wrap bằng PHASE1_HIDE_CUSTOM_FIELDS.
```

Từ van-ban-du-thao/page.tsx:
```ts
// Line 148-151: đã load /quan-tri/don-vi/tree
// Field publish_unit_id (line 709) — KHÔNG đổi.
// extraColumns (line 115, 794-...) — wrap bằng PHASE1_HIDE_CUSTOM_FIELDS.
```

Từ lib/tree-utils.ts:
```ts
// flattenDepartments(tree): DepartmentNode[] → SelectOption[]
// Đã dùng trong van-ban-di & van-ban-du-thao → pattern chuẩn để copy sang van-ban-den.
// SelectOption = { value: number; label: string }
// DepartmentNode API response: { id, parent_id, name, children? } (tree shape từ /quan-tri/don-vi/tree)
```
</interfaces>

# Quy ước quan trọng (CLAUDE.md)
- AntD 6: `Drawer size={720}` không `width`
- Snake_case field names khớp backend — KHÔNG đổi tên `publish_unit`, `extra_fields`, `publish_unit_id`
- Tiếng Việt có dấu cho mọi label, message
- ESM imports `.js` extension (cho backend) — frontend không cần
- Không commit tự động, không update ROADMAP.md
- Next.js 16 có breaking changes — đọc node_modules/next/dist/docs/ nếu đụng Next API

# Backend SP convention liên quan (đọc để hiểu, không sửa)
- SP `edoc.fn_incoming_doc_create` nhận `p_publish_unit TEXT` (TEXT, không phải ID)
  → Task 3 KHÔNG đổi schema, chỉ đổi UI component (Input → Select). Value gửi về
  backend vẫn là chuỗi tên đơn vị (label của option), KHÔNG phải id.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ẩn 20 menu route khỏi sidebar bằng HIDDEN_ROUTES Set + recursive filter</name>
  <files>e_office_app_new/frontend/src/components/layout/MainLayout.tsx</files>
  <action>
Mục tiêu: Sau khi `buildMenuItems` build xong items array, chạy qua 1 hàm filter
recursive để loại bỏ các route có key nằm trong `HIDDEN_ROUTES`, đồng thời ẩn
parent có children rỗng và group header không còn item nào theo sau.

Bước thực hiện:

1. Thêm ngay sau các constant role (sau dòng `const CHI_DAO_ROLE`, trước
   `interface MenuBuildParams`) block sau:

```ts
// ─── Phase 1 feature flag: ẩn menu chưa có trong HDSD cũ ──────────────────
// Phase 2: xóa route khỏi Set này để bật lại.
const HIDDEN_ROUTES: ReadonlySet<string> = new Set([
  // Tin nhắn
  '/tin-nhan',
  // Lịch (ẩn cả parent group 'lich' qua recursive check)
  'lich',
  '/lich/ca-nhan',
  '/lich/co-quan',
  '/lich/lanh-dao',
  // Danh bạ
  '/danh-ba',
  // Kho lưu trữ
  'kho-luu-tru',
  '/kho-luu-tru',
  '/kho-luu-tru/muon-tra',
  // Tài liệu
  '/tai-lieu',
  // Hợp đồng
  '/hop-dong',
  // Cuộc họp
  'cuoc-hop',
  '/cuoc-hop',
  '/cuoc-hop/thong-ke',
  // LGSP + kênh thông báo
  '/lgsp',
  '/lgsp/co-quan',
  '/thong-bao-kenh',
  // Quản trị items bị ẩn
  '/quan-tri/chuc-nang',
  '/quan-tri/cau-hinh-truong',
  '/quan-tri/co-quan',
  '/quan-tri/nhom-lam-viec',
  '/quan-tri/uy-quyen',
  '/quan-tri/dia-ban',
  '/quan-tri/lich-lam-viec',
  '/quan-tri/mau-thong-bao',
  '/quan-tri/cau-hinh',
]);

// Recursive filter:
// 1. Nếu item có key trong HIDDEN_ROUTES → drop.
// 2. Nếu item có children → filter children; nếu children rỗng sau filter → drop luôn cha.
// 3. Group header (type==='group') không tự drop qua HIDDEN_ROUTES —
//    thay vào đó: sau khi filter, nếu group header không còn item thực nào
//    ngay sau nó (trước group header khác) → drop header.
function filterMenuItems(items: MenuItem[]): MenuItem[] {
  const afterHide: MenuItem[] = [];
  for (const item of items) {
    if (!item) continue;
    // Divider luôn giữ
    if ('type' in item && item.type === 'divider') { afterHide.push(item); continue; }
    const key = 'key' in item ? (item.key as string | undefined) : undefined;
    // Group header: xử lý ở bước 2 dưới
    if ('type' in item && item.type === 'group') { afterHide.push(item); continue; }
    // Item thường / submenu
    if (key && HIDDEN_ROUTES.has(key)) continue;
    // Recurse children
    if ('children' in item && Array.isArray(item.children) && item.children.length > 0) {
      const filteredChildren = filterMenuItems(item.children as MenuItem[]);
      if (filteredChildren.length === 0) continue; // parent trống → drop
      afterHide.push({ ...item, children: filteredChildren });
      continue;
    }
    afterHide.push(item);
  }
  // Bước 2: ẩn group header trống
  const result: MenuItem[] = [];
  for (let i = 0; i < afterHide.length; i++) {
    const cur = afterHide[i];
    if (cur && 'type' in cur && cur.type === 'group') {
      // Đếm item thực sau cur cho tới group tiếp theo hoặc hết array
      let hasContent = false;
      for (let j = i + 1; j < afterHide.length; j++) {
        const next = afterHide[j];
        if (!next) continue;
        if ('type' in next && next.type === 'group') break;
        // Divider không tính là content
        if ('type' in next && next.type === 'divider') continue;
        hasContent = true; break;
      }
      if (!hasContent) continue; // group trống → skip
    }
    result.push(cur);
  }
  return result;
}
```

2. Trong hàm `buildMenuItems`, dòng cuối `return items;` đổi thành
   `return filterMenuItems(items);`

3. Giữ nguyên `breadcrumbMap` — người dùng không vào được route nhưng nếu lỡ
   điều hướng bằng URL trực tiếp thì breadcrumb vẫn hiển thị (no harm).

4. Giữ nguyên toàn bộ logic còn lại (role check, badge, socket...).

Kiểm tra thủ công kỳ vọng sau khi chạy:
- User Admin: sidebar chỉ thấy Tổng quan, nhóm NGHIỆP VỤ (Văn bản với 6 submenu,
  Hồ sơ công việc, Thông báo), Đối tác, nhóm HỆ THỐNG (Quản trị: 4 item — Đơn vị,
  Chức vụ, Người dùng, Nhóm quyền; Danh mục: 4 item — Sổ VB, Loại VB, Lĩnh vực,
  Người ký).
- Group QUẢN LÝ (kho/tài liệu/hợp đồng/cuộc họp) → ẨN cả header.
- Group TÍCH HỢP (chỉ lgsp + kênh thông báo) → ẨN cả header.
- User thường (không admin, không canManage): chỉ thấy Tổng quan + Văn bản + Hồ sơ
  công việc + Thông báo + Đối tác.
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
- HIDDEN_ROUTES Set tồn tại với đúng 25 entries (20 route + 5 parent key).
- filterMenuItems function được export hoặc dùng nội bộ, recurse được children.
- buildMenuItems return filterMenuItems(items) thay vì items.
- tsc --noEmit pass (0 lỗi).
- Grep -c 'HIDDEN_ROUTES' MainLayout.tsx ≥ 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ẩn section "Trường bổ sung" trên 3 form VB bằng flag PHASE1_HIDE_CUSTOM_FIELDS</name>
  <files>e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx, e_office_app_new/frontend/src/app/(main)/van-ban-di/page.tsx, e_office_app_new/frontend/src/app/(main)/van-ban-du-thao/page.tsx</files>
  <action>
Mục tiêu: Ẩn UI render "Trường bổ sung" (dynamic fields từ `extraColumns`) trên cả
3 trang list (van-ban-den, van-ban-di, van-ban-du-thao). Giữ nguyên API fetch
(`/van-ban-{den,di,du-thao}/truong-bo-sung`) — KHÔNG xóa gì cả.

Cách làm: tại mỗi file, thêm flag module-level ngay sau các import và trước
component function:

```ts
// ─── Phase 1 feature flag: ẩn dynamic "Trường bổ sung" ─────────────────────
// Phase 2: đổi thành false để bật lại.
const PHASE1_HIDE_CUSTOM_FIELDS = true;
```

Sau đó mỗi lần thấy block:
```tsx
{extraColumns.length > 0 && (
  <>
    <div style={{ borderTop: ... }}>
      <span>Trường bổ sung</span>
    </div>
    <Row gutter={16}>
      {extraColumns.map(...)}
    </Row>
  </>
)}
```
đổi điều kiện thành:
```tsx
{!PHASE1_HIDE_CUSTOM_FIELDS && extraColumns.length > 0 && (
  <>...</>
)}
```

Vị trí cụ thể (tham khảo):
- van-ban-den/page.tsx:403 — đổi `{extraColumns.length > 0 && (` thành
  `{!PHASE1_HIDE_CUSTOM_FIELDS && extraColumns.length > 0 && (`
- van-ban-di/page.tsx:550 — tương tự
- van-ban-du-thao/page.tsx:794 — tương tự

KHÔNG đụng vào:
- `setExtraColumns` state hook
- `fetchExtraColumns` callback
- `useEffect` gọi fetchExtraColumns
- Type interface ExtraColumn

Lý do: fetch vẫn chạy, chỉ UI bị ẩn. Nếu sau này bật lại chỉ cần flip flag.

Lưu ý CLAUDE.md:
- Không xóa code.
- Không đổi tên field `extra_fields` trong Form.Item.name (backend payload cần giữ
  nguyên shape vì submit handler vẫn có `extra_fields` trong values).
- Chú ý: khi flag bật và form submit, `values.extra_fields` sẽ là undefined —
  backend SP hiện đã handle case NULL nên OK (verify nhanh bằng test submit sau).

Check nhanh bằng grep sau khi xong:
- `grep -c PHASE1_HIDE_CUSTOM_FIELDS` trên mỗi file ≥ 2 (1 declare + 1 usage).
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
- 3 file đều có `const PHASE1_HIDE_CUSTOM_FIELDS = true;` ở module-level.
- 3 điều kiện JSX render "Trường bổ sung" đều được wrap bằng
  `!PHASE1_HIDE_CUSTOM_FIELDS && ...`.
- Fetch extraColumns và useEffect vẫn tồn tại nguyên vẹn.
- tsc --noEmit pass.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3a: QUYẾT ĐỊNH — cách chuyển "Cơ quan ban hành" trên VB đến sang Select</name>
  <what-built>
Scan code xong thấy task scope gốc của user không khớp hiện trạng:
- `/quan-tri/co-quan` API KHÔNG được form VB đến/đi/dự thảo dùng. Nó chỉ phục vụ
  trang admin `/quan-tri/co-quan` (Thông tin cơ quan — form đơn vị riêng của user).
- `van-ban-di` và `van-ban-du-thao` ĐÃ dùng `/quan-tri/don-vi/tree` + Select
  `publish_unit_id` (FK). Không cần đổi gì.
- Chỉ có `van-ban-den/page.tsx:376` đang dùng `<Input>` free-text cho
  `publish_unit` (TEXT column — xem migration SP `fn_incoming_doc_create`
  nhận `p_publish_unit TEXT`).

Vấn đề: đổi Input → Select sẽ không lưu id được vì schema là TEXT. Có 2 lựa chọn.
  </what-built>
  <decision>Cách implement việc chuyển "Cơ quan ban hành" trên VB đến</decision>
  <context>
Backend hiện tại nhận `publish_unit` là TEXT (tên đơn vị). Không có column
`publish_unit_id` trong bảng `incoming_docs`. Nếu đổi sang FK sẽ phải:
1. Migration DB (thêm cột, backfill, sửa SP create/update/detail/list)
2. Sửa backend repo + route
3. Sửa Row interface frontend + column hiển thị

Quá lớn cho quick task. Nhưng user vẫn muốn UI là Select bind tới đơn vị.
  </context>
  <options>
    <option id="option-a">
      <name>A. Select hiển thị label đơn vị, submit string name (không đổi schema)</name>
      <pros>
- KHÔNG đụng backend / migration.
- 30 phút thực hiện, an toàn cho demo.
- Form field vẫn tên `publish_unit`, value là string (tên đơn vị chọn từ cây).
- Nếu cần nhập tên đơn vị ngoài cây (đơn vị bên ngoài) → có thể giữ mode="tags" hoặc mode="combobox" để user gõ tự do.
      </pros>
      <cons>
- Value lưu DB là tên, không phải id → không join được sau này.
- User nhập tên lung tung không consistent (nếu để tags mode).
      </cons>
    </option>
    <option id="option-b">
      <name>B. Chỉ đổi label "Cơ quan ban hành" → giữ Input, không đổi gì khác (no-op)</name>
      <pros>
- Zero risk.
- Task 3 coi như không áp dụng với VB đến (vì scope gốc hiểu sai).
      </pros>
      <cons>
- Không đáp ứng yêu cầu "Đổi data source Cơ quan → Đơn vị" của user.
      </cons>
    </option>
    <option id="option-c">
      <name>C. Full: migration DB + backend + FE đổi sang FK publish_unit_id</name>
      <pros>
- Đúng nghiệp vụ.
      </pros>
      <cons>
- Phạm vi phase full — KHÔNG phù hợp quick task.
- Nguy cơ vỡ form nhập liệu khi demo sát (deadline 2026-04-18/19).
      </cons>
    </option>
  </options>
  <resume-signal>Chọn: option-a / option-b / option-c (hoặc đề xuất khác, ví dụ Select mode="combobox" cho phép gõ tên tự do hoặc chọn từ dropdown)</resume-signal>
</task>

<task type="auto">
  <name>Task 3b: Đổi "Cơ quan ban hành" trên VB đến sang Select bind /quan-tri/don-vi/tree (thực hiện theo quyết định Task 3a)</name>
  <files>e_office_app_new/frontend/src/app/(main)/van-ban-den/page.tsx</files>
  <action>
Điều kiện tiên quyết: User đã chọn option ở Task 3a. Dưới đây là cách thực hiện
**option A** (mặc định nếu user chọn A hoặc đề xuất combobox):

Bước 1: Thêm state `departments` và imports nếu chưa có. Đọc van-ban-di/page.tsx
làm reference (line 88-90 + 121-131 + `flattenDepartments` import).

```tsx
import { flattenDepartments, type DepartmentNode } from '@/lib/tree-utils';

type SelectOption = { value: number; label: string };
// ... trong component:
const [departments, setDepartments] = useState<SelectOption[]>([]);
```

Bước 2: Thêm API call vào `fetchDropdowns` useCallback (hiện đang ở line ~95):

```ts
const [bookRes, typeRes, fieldRes, deptRes] = await Promise.all([
  api.get('/quan-tri/so-van-ban', { params: { type_id: 1 } }),
  api.get('/quan-tri/loai-van-ban/tree'),
  api.get('/quan-tri/linh-vuc'),
  api.get('/quan-tri/don-vi/tree'),  // ← THÊM
]);
// ...
const deptTree: DepartmentNode[] = deptRes.data.data || [];
setDepartments(flattenDepartments(deptTree));
```

Lưu ý: block `if (user?.isAdmin)` bên dưới đã tự gọi `/quan-tri/don-vi/tree`
trùng lặp — gộp lại dùng chung response để tránh gọi 2 lần.

Bước 3: Đổi Form.Item tại line 376:

**Nếu user chọn option A (pure Select):**
```tsx
<Col span={12}>
  <Form.Item name="publish_unit" label="Cơ quan ban hành">
    <Select
      placeholder="Chọn cơ quan ban hành"
      allowClear
      showSearch
      optionFilterProp="label"
      options={departments}
      // Submit label thay vì value (backend nhận TEXT)
      onChange={(val, option) => {
        const label = !Array.isArray(option) ? (option as { label?: string })?.label : undefined;
        form.setFieldValue('publish_unit', label || '');
      }}
    />
  </Form.Item>
</Col>
```

Vì `publish_unit` là TEXT nhưng options value là number → phải đảo bằng cách
custom onChange. Alternative cleaner: dùng 2 field (hidden `publish_unit` +
display-only Select). Cách đơn giản hơn:

```tsx
<Form.Item name="publish_unit" label="Cơ quan ban hành">
  <Select
    placeholder="Chọn cơ quan ban hành"
    allowClear
    showSearch
    optionFilterProp="label"
    // Value = label (tên đơn vị) để đúng schema TEXT
    options={departments.map((d) => ({ value: d.label, label: d.label }))}
  />
</Form.Item>
```

→ DÙNG CÁCH NÀY. Ngắn gọn, không cần onChange custom, value đúng TEXT.

**Nếu user chọn combobox mode (cho phép gõ tên đơn vị ngoài cây):**
```tsx
<Form.Item name="publish_unit" label="Cơ quan ban hành">
  <Select
    mode="tags"
    maxCount={1}
    placeholder="Chọn hoặc nhập cơ quan ban hành"
    allowClear
    showSearch
    optionFilterProp="label"
    options={departments.map((d) => ({ value: d.label, label: d.label }))}
    // Với mode=tags, value là array → normalize khi submit
  />
</Form.Item>
```
Nếu combobox → phải normalize trong handleSave: `values.publish_unit = Array.isArray(values.publish_unit) ? values.publish_unit[0] : values.publish_unit;`

Bước 4: Khi set lại form cho edit (block `openDrawer` / `form.setFieldsValue`),
value `record.publish_unit` đang là string → Select sẽ match option nếu label
trùng hoặc hiện "out of range". Acceptable cho demo.

Bước 5 (nếu user chọn option B): bỏ qua tất cả bước trên, task này = no-op,
chỉ commit empty message "chore: giữ nguyên UI Cơ quan ban hành theo quyết định".
  </action>
  <verify>
    <automated>cd e_office_app_new/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
- van-ban-den/page.tsx có state departments + load từ /quan-tri/don-vi/tree.
- Form.Item publish_unit đổi từ <Input> sang <Select> (hoặc <Select mode="tags"> nếu user chọn combobox).
- Value submit giữ TEXT (tên đơn vị) — không phải id, không break backend SP.
- tsc --noEmit pass.
- Grep -c '/quan-tri/don-vi/tree' van-ban-den/page.tsx ≥ 1 (thêm mới).
- Label "Cơ quan ban hành" được giữ nguyên.
  </done>
</task>

</tasks>

<verification>
Sau khi hoàn tất cả 3 task (+ Task 3a decision):

1. **Type-check toàn frontend**: `cd e_office_app_new/frontend && npx tsc --noEmit` → 0 lỗi.
2. **Smoke test manual (demo-grade)**:
   - Đăng nhập role Admin: sidebar render đúng scope (chi tiết ở Task 1).
   - Đăng nhập role user thường: chỉ thấy menu cơ bản.
   - Mở form VB đến / VB đi / VB dự thảo: KHÔNG còn section "Trường bổ sung".
   - Form VB đến field "Cơ quan ban hành" là Select (không phải Input free-text).
3. **Regression check**:
   - Submit form VB đến mới vẫn hoạt động (backend nhận publish_unit dạng TEXT).
   - Submit form VB đi / VB dự thảo vẫn work (không đụng gì vào 2 trang này ngoài flag).
4. **Grep check không xóa code**:
   - `grep -c extraColumns` trên mỗi trang VB vẫn ≥ 3 (state + fetch + render).
   - `grep -c HIDDEN_ROUTES` MainLayout.tsx ≥ 2.
</verification>

<success_criteria>
- 4 file đã modify với diff tối thiểu, không xóa code.
- 3 flag / set feature được define rõ ràng có comment "Phase 1" + hướng dẫn cách bật lại ở Phase 2.
- Sidebar demo sạch — không menu trống, không group header treo một mình.
- Form VB đến/đi/dự thảo không còn dynamic "Trường bổ sung" UI.
- Form VB đến có Select bind tới cây đơn vị (theo quyết định Task 3a).
- Type-check pass toàn frontend.
- Không commit tự động. Chờ user review từng task rồi commit theo message tiếng Việt theo style dự án:
  - Task 1: `feat: ẩn các module/menu chưa có trong HDSD cũ — Phase 1 demo`
  - Task 2: `feat: ẩn section 'Trường bổ sung' trên 3 form VB — Phase 1`
  - Task 3: `feat: đổi 'Cơ quan ban hành' VB đến sang Select bind đơn vị`  (nếu option A/combobox)
  - hoặc skip commit nếu option B.
</success_criteria>

<output>
Sau khi hoàn tất, tạo file `.planning/quick/260418-gs7-phase-1-n-c-c-module-menu-ch-a-c-trong-h/260418-gs7-01-SUMMARY.md` tóm tắt:
- Các route/flag đã ẩn (list đầy đủ)
- Quyết định Task 3a (option nào được chọn)
- Các file đã sửa + số dòng diff
- Kết quả type-check
- Manual test sidebar screenshot (optional)
- Hướng dẫn bật lại Phase 2 (xóa route khỏi HIDDEN_ROUTES, flip 3 flag PHASE1_HIDE_CUSTOM_FIELDS = false, quyết định xử lý publish_unit Select vs full schema migration)
</output>
