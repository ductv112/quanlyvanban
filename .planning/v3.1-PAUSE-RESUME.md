---
title: v3.1 Pause — resume mai
paused: 2026-05-07T18:30 (chiều, user về nhà)
resume: 2026-05-08 sáng (user lên cty)
---

# Resume Notes — Mai làm tiếp

## Trạng thái hiện tại

**Branch:** `v3.1/phase-21-foundation` (local main, chưa push origin)

**Commits từ session 2026-05-06 → 2026-05-07:**
- 9 wave test commits (~1000 dòng test scripts + RESULTS)
- 15 fix commits (Phase 31 Group A/B/C, ~72 bugs resolved)
- 4 UI agent commits (74 SKIP UI → PASS)
- Excel sync commits

**Total bugs resolved: ~72 unique** (62 active + 10 deferred minor/architectural)

**Pass rate:**
- API/Functional 96.1% (689/717 PASS)
- UI gom thêm 74 TC PASS qua Playwright (Wave b/c/d/e/f/g/h)

## Còn TODO mai

### 1. Update aggregator để pick up UI agent results (~10 phút)

`tools/test-report/aggregate-wave-results.ts` hiện chỉ đọc Wave a-i RESULTS chính, KHÔNG đọc `.planning/phases/31-fix-gom-ui/RESULTS-{bc,de,f,gh}-ui.md`.

Cần add 4 file paths này vào aggregator → Excel sẽ thêm 74 PASS cho UI TC.

### 2. Re-sync Excel cuối cùng (~5 phút)

```bash
cd tools/test-report
npx tsx aggregate-wave-results.ts
npm run sync
```

Output: `docs/hdsd/<YYYYMMDD>_Testcase_QLVB_V2_results.xlsx` với 74 TC thêm = Pass.

### 3. User test thủ công ~20-25 TC còn SKIP (~30 phút)

| Loại | Count | Note |
|---|---|---|
| Real cert SmartCA / LGSP credentials | ~6 | Test khi có cert KH thật |
| Token expire 15 min wait | 2 | Skip OK |
| Browser print / Excel export | ~3 | Manual test 30s/TC |
| Composite UI flow đa user multi-tab phức tạp | ~5-10 | UI agent đã cover phần lớn |
| Visual UX subjective (animation, color subtle) | ~5 | Optional |

User đã nói "tối tôi về máy nhà tôi test thủ công nốt" — sẽ làm độc lập ở nhà.

### 4. Build prod + deploy KH test environment

Khi chạy build prod trên server thật, cần fix:
- **BUG-002 CORS multi-origin** (1-line fix server.ts)
- Update frontend `NEXT_PUBLIC_API_URL` = `/api` (relative, qua reverse proxy)
- Reverse proxy IIS/Nginx route `/api/*` → backend port 4000

Process:
```powershell
# Trên server prod (Windows + IIS hoặc Nginx)
cd C:\qlvb\quanlyvanban
git fetch + checkout v3.1/phase-21-foundation (hoặc merge vào main)
.\deploy\deploy-v2-kh-test.ps1 -Force   # nếu fresh KH test
# HOẶC manual update prod (nếu KH đã có data)
# Xem deploy/MANUAL_UPDATE_PROD.md
```

### 5. Demo + đàm phán KH

Evidence có:
- Excel `20260507_Testcase_QLVB_V2_results.xlsx` (cột Trạng thái + bug remaining)
- `.planning/v3.1-FIX-GOM-REPORT.md` (technical fix detail)
- `.planning/v3.1-FINAL-TEST-REPORT.md` (test methodology + coverage)

## Services đang chạy (sẽ tắt khi user shutdown máy)

- Backend port 4000 (NODE_ENV=test, qlvb_test)
- Frontend port 3000 (production build)
- Mocks 8181/8182/8183
- Docker: postgres + redis + mongo + minio (auto-start sau reboot)

## Mai resume command

```powershell
# Setup local (Windows)
cd D:\ProjectAI\quanlyvanban\e_office_app_new\backend
npm run test:db:reset
$env:NODE_ENV="test"
npm run dev    # background hoặc terminal riêng

cd ..\frontend
npm start   # đã build, chỉ start

cd ..\..\tools\mocks
npx tsx start.ts
```

Hoặc dùng script `deploy/setup-dev-windows.ps1` nếu cần fresh.

## Memory notes (đã save)

- `feedback_test_intent_correction.md` — Claude là tester, không build framework
- `feedback_resource_budget.md` — KHÔNG start nhiều dev server song song
- `feedback_phase_execution_flow.md` — Pause ở testable state
- `feedback_delegation_trust.md` — Proceed auto khi user approve scope
