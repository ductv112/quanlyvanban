# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
.next/
out/
build/

# Environment files (contain secrets)
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Docker volumes
postgres_data/
mongodb_data/
redis_data/
minio_data/

# TypeScript
*.tsbuildinfo

# Claude memory (personal)
.claude/

# Source code cũ (chỉ giữ local để tham khảo)
docs/source_code_cu/
docs/e_office_app_old/

# Estimate/báo giá nội bộ — không commit
docs/Function list & Estimate.xlsx

# Doc tích hợp ký số MySign Viettel (~400MB — SDK + demo code) — chỉ giữ local
docs/huong_dan_tich_hop_ky_so_MySign_Viettel/

# Credential LGSP Lạng Sơn (List.txt + QLVBDNAgencies.xlsx có secret_key/password) — NEVER commit
docs/Trục EDOC Lạng Sơn*/
docs/Trục EDOC*/

# HDSD nội bộ gửi KH — chỉ chia sẻ qua kênh khác, không commit
docs/hdsd/HDSD gửi KH (Ko commit git)/

# Test artifacts (Phase 21+ automation suite)
tests/results/
# Storage state JSON files generated runtime by tests/globalSetup.ts (Plan 21-04).
# Giu folder + .gitkeep nhung khong commit *.json (chua token thuc).
tests/.auth/*.json
tests/coverage/
playwright-report/
.env.test

# CI artifacts (Plan 21-06 — test-pr.yml workflow)
.github/.local
test-results/
tools/mocks/.pid
playwright-report/
tools/test-report/node_modules/
tools/mocks/node_modules/

# Tools internal dump outputs (analysis scratch — re-generated khi cần)
tools/test-report/_*-dump.txt
.vercel

# Signing encryption keys (local backup only — NEVER commit)
# Lý do: mất file = mất ALL credential KH đã encrypt; lộ file = ai cũng decrypt được DB
SIGNING_SECRET_KEY*.txt
**/SIGNING_SECRET_KEY*.txt
**/*SIGNING_KEY*.txt
**/.env.bak.*
